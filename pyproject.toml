# This file is auto-generated by oagen. Do not edit.

[build-system]
requires = ["hatchling>=1.27"]
build-backend = "hatchling.build"

[project]
name = "sellapp-sdk"
version = "0.1.0"
description = "Official Python SDK for the SellApp API"
readme = "README.md"
requires-python = ">=3.9"
license = { text = "MIT" }
dependencies = ["httpx>=0.27,<1"]

[project.optional-dependencies]
test = [
  "pytest>=8,<9",
  "pytest-asyncio>=0.24,<2",
  "pytest-httpx>=0.35,<1",
]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]

[tool.hatch.build.targets.wheel]
packages = ["src/sellapp_sdk"]

[project.urls]
Documentation = "https://sell.app/docs/api"
Repository = "https://github.com/sellapp/sellapp-python"
Issues = "https://github.com/sellapp/sellapp-python/issues"

[tool.hatch.build.targets.sdist]
include = ["/src", "/README.md", "/LICENSE.txt", "/NOTICE.txt", "/docs", "/examples"]
exclude = ["/examples/check.py"]
