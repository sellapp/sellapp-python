# This file is auto-generated by oagen. Do not edit.

from __future__ import annotations

from dataclasses import dataclass
from dataclasses import field as dataclass_field
from enum import Enum
from typing import Any, cast

from sellapp_sdk._types import (
    _preserve_unknown_fields,
    _raise_deserialize_error,
    _validate_model,
)
from sellapp_sdk.common.models.catalog_visibility import CatalogVisibility

from .create_add_on_draft_request_application_json_property_variant import (
    CreateAddOnDraftRequestApplicationJsonPropertyVariant,
)

_DOMAIN_RUNTIME_SCHEMA: dict[str, Any] = {
    "name": "SdkCreateAddOnDraftRequestApplicationJson",
    "required": ["title", "description", "visibility"],
    "properties": {
        "title": {
            "kind": "primitive",
            "type": "string",
            "constraints": {"maxLength": 255},
        },
        "slug": {
            "kind": "primitive",
            "type": "string",
            "constraints": {"maxLength": 255, "pattern": "^[a-z0-9]+(?:-[a-z0-9]+)*$"},
        },
        "description": {
            "kind": "primitive",
            "type": "string",
            "constraints": {"minLength": 5, "maxLength": 5000},
        },
        "visibility": {
            "kind": "enum",
            "values": ["PUBLIC", "ON_HOLD", "HIDDEN", "PRIVATE"],
        },
        "is_draft": {"kind": "primitive", "type": "boolean"},
        "parent_product_ids": {
            "kind": "array",
            "items": {"kind": "primitive", "type": "integer"},
            "constraints": {"uniqueItems": True},
        },
        "variant": {"kind": "any"},
    },
    "additionalProperties": True,
}
_WIRE_RUNTIME_SCHEMA: dict[str, Any] = {
    "name": "SdkCreateAddOnDraftRequestApplicationJson",
    "required": ["title", "description", "visibility"],
    "properties": {
        "title": {
            "kind": "primitive",
            "type": "string",
            "constraints": {"maxLength": 255},
        },
        "slug": {
            "kind": "primitive",
            "type": "string",
            "constraints": {"maxLength": 255, "pattern": "^[a-z0-9]+(?:-[a-z0-9]+)*$"},
        },
        "description": {
            "kind": "primitive",
            "type": "string",
            "constraints": {"minLength": 5, "maxLength": 5000},
        },
        "visibility": {
            "kind": "enum",
            "values": ["PUBLIC", "ON_HOLD", "HIDDEN", "PRIVATE"],
        },
        "is_draft": {"kind": "primitive", "type": "boolean"},
        "parent_product_ids": {
            "kind": "array",
            "items": {"kind": "primitive", "type": "integer"},
            "constraints": {"uniqueItems": True},
        },
        "variant": {"kind": "any"},
    },
    "additionalProperties": True,
}


@dataclass(slots=True)
class SdkCreateAddOnDraftRequestApplicationJson:
    """Sdk Create Add On Draft Request Application Json model."""

    title: str
    """Seller-facing title for the add-on."""
    description: str
    """Description shown when the add-on is offered."""
    visibility: CatalogVisibility
    slug: str | None = None
    """Optional store-unique slug. When omitted during creation, SellApp generates one from the title."""
    is_draft: bool | None = None
    """Defaults to true on creation. Set false to publish. Publication requires exactly one published, fixed-price, single-payment variant; creation can include that variant in the same request."""
    parent_product_ids: list[int] | None = None
    """Ordered IDs of same-store, non-subscription products, courses, or bookings to assign. Supplying the field replaces the complete assignment set."""
    variant: CreateAddOnDraftRequestApplicationJsonPropertyVariant | None = None
    additional_properties: dict[str, Any] = dataclass_field(
        default_factory=dict, repr=False
    )
    _from_response: bool = dataclass_field(default=False, repr=False, compare=False)

    @classmethod
    def from_dict(
        cls, data: dict[str, Any]
    ) -> SdkCreateAddOnDraftRequestApplicationJson:
        """Deserialize from a dictionary."""
        try:
            _validate_model(
                data,
                _WIRE_RUNTIME_SCHEMA,
                direction="response",
                allow_unknown_enums=True,
                allow_unknown_union_variants=True,
                allow_unknown_fields=True,
            )
            return cls(
                title=data["title"],
                description=data["description"],
                visibility=CatalogVisibility(data["visibility"]),
                slug=data.get("slug"),
                is_draft=data.get("is_draft"),
                parent_product_ids=data.get("parent_product_ids"),
                variant=CreateAddOnDraftRequestApplicationJsonPropertyVariant.from_dict(
                    cast(dict[str, Any], _v_variant)
                )
                if (_v_variant := data.get("variant")) is not None
                else None,
                additional_properties=_preserve_unknown_fields(
                    data,
                    [
                        "title",
                        "slug",
                        "description",
                        "visibility",
                        "is_draft",
                        "parent_product_ids",
                        "variant",
                    ],
                ),
                _from_response=True,
            )
        except (KeyError, ValueError) as e:
            _raise_deserialize_error("SdkCreateAddOnDraftRequestApplicationJson", e)

    def to_dict(self) -> dict[str, Any]:
        """Serialize to a dictionary."""
        _domain_data: dict[str, Any] = dict(self.additional_properties)
        _domain_data["title"] = self.title
        _domain_data["description"] = self.description
        _domain_data["visibility"] = self.visibility
        if self.slug is not None:
            _domain_data["slug"] = self.slug
        if self.is_draft is not None:
            _domain_data["is_draft"] = self.is_draft
        if self.parent_product_ids is not None:
            _domain_data["parent_product_ids"] = self.parent_product_ids
        if self.variant is not None:
            _domain_data["variant"] = self.variant
        _validate_model(
            _domain_data,
            _DOMAIN_RUNTIME_SCHEMA,
            direction="request",
            allow_unknown_enums=self._from_response,
            allow_unknown_union_variants=self._from_response,
            allow_unknown_fields=(self._from_response or True),
        )
        result: dict[str, Any] = dict(self.additional_properties)
        result["title"] = self.title
        result["description"] = self.description
        result["visibility"] = (
            self.visibility.value
            if isinstance(self.visibility, Enum)
            else self.visibility
        )
        if self.slug is not None:
            result["slug"] = self.slug
        if self.is_draft is not None:
            result["is_draft"] = self.is_draft
        if self.parent_product_ids is not None:
            result["parent_product_ids"] = self.parent_product_ids
        if self.variant is not None:
            result["variant"] = self.variant.to_dict()
        return result
