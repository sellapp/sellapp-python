# This file is auto-generated by oagen. Do not edit.

from __future__ import annotations

from typing import TYPE_CHECKING, Any, Dict, List, Optional, Union

from .._base_client import WithRawResponse

if TYPE_CHECKING:
    from .._client import AsyncSellAppClient, SellAppClient

from sellapp_sdk.bookings.models.sdk_list_booking_date_overrides_response_value_200_application_json import (
    SdkListBookingDateOverridesResponseValue200ApplicationJson,
)
from sellapp_sdk.bookings.models.sdk_set_booking_date_availability_response_value_201_application_json import (
    SdkSetBookingDateAvailabilityResponseValue201ApplicationJson,
)
from sellapp_sdk.common.models.bookings_status import BookingsStatus

from .._pagination import AsyncPage, SyncPage
from .._types import NOT_GIVEN, NotGiven, RequestOptions, _validate_model, enum_value


class BookingsCalendarEvents:
    """Bookings Calendar Events API resources."""

    def __init__(self, client: SellAppClient) -> None:
        self._client = client
        self.with_raw_response = WithRawResponse(self)

    def list(
        self,
        *,
        limit: Optional[int] = None,
        before: Optional[str] = None,
        after: Optional[str] = None,
        order: Optional[str] = None,
        product_variant_id: Optional[int] = None,
        from_: Optional[str] = None,
        to: Optional[str] = None,
        status: Optional[Union[BookingsStatus, str]] = None,
        pagination: Optional[bool] = None,
        page: Optional[int] = None,
        request_options: Optional[RequestOptions] = None,
    ) -> SyncPage[SdkListBookingDateOverridesResponseValue200ApplicationJson]:
        """List booking date overrides

        List store-wide or variant-specific booking date overrides. Date boundaries are stored in UTC and interpreted in the booking timezone. Requires the `listing` Sanctum ability. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            limit: Number of items to return per page. Defaults to `15`.
            before: The before.
            after: The after.
            order: The order.
            product_variant_id: Return the effective override hierarchy for a same-store booking variant, including inherited store-wide and product-wide rows plus exact variant rows.
            from_: Only return overrides ending after this instant.
            to: Only return overrides starting before this instant.
            status: Filter by override status.
            pagination: Set to false to return at most 100 matching overrides in one data array without pagination links or metadata. Defaults to `true`.
            page: Page number to return. Defaults to `1`.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SyncPage[SdkListBookingDateOverridesResponseValue200ApplicationJson]

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "listBookingDateOverrides",
        }
        params = {
            k: v
            for k, v in {
                "limit": limit,
                "before": before,
                "after": after,
                "order": order,
                "product_variant_id": product_variant_id,
                "from": from_,
                "to": to,
                "status": enum_value(status) if status is not None else None,
                "pagination": pagination,
                "page": page,
            }.items()
            if v is not None
        }
        return self._client.request_page(
            method="get",
            path=("v2", "booking-calendar-events"),
            model=SdkListBookingDateOverridesResponseValue200ApplicationJson,
            params=params,
            request_options=request_options,
        )

    def set(
        self,
        *,
        dates: List[str],
        available: bool,
        product_variant_id: Union[int, None, NotGiven] = NOT_GIVEN,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkSetBookingDateAvailabilityResponseValue201ApplicationJson:
        """Set booking date availability

        Atomically mark one or more local calendar dates unavailable or available for the store or one same-store booking variant. Requires the `listing` Sanctum ability. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            product_variant_id: The product variant id.
            dates: The dates.
            available: Set false to block the dates and true to clear the exact block or open an inherited block for the variant.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkSetBookingDateAvailabilityResponseValue201ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "setBookingDateAvailability",
        }
        body: Dict[str, Any] = {
            "dates": dates,
            "available": available,
        }
        if not isinstance(product_variant_id, NotGiven):
            body["product_variant_id"] = product_variant_id
        _validate_model(
            body,
            {
                "name": "SdkSetBookingDateAvailabilityRequestApplicationJson",
                "required": ["dates", "available"],
                "properties": {
                    "product_variant_id": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "integer",
                            "constraints": {"minimum": 1},
                        },
                    },
                    "dates": {
                        "kind": "array",
                        "items": {
                            "kind": "primitive",
                            "type": "string",
                            "format": "date",
                        },
                        "constraints": {
                            "minItems": 1,
                            "maxItems": 366,
                            "uniqueItems": True,
                        },
                    },
                    "available": {"kind": "primitive", "type": "boolean"},
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return self._client.request(
            method="post",
            path=("v2", "booking-calendar-events"),
            body=body,
            model=SdkSetBookingDateAvailabilityResponseValue201ApplicationJson,
            request_options=request_options,
        )


class AsyncBookingsCalendarEvents:
    """Bookings Calendar Events API resources (async)."""

    def __init__(self, client: AsyncSellAppClient) -> None:
        self._client = client
        self.with_raw_response = WithRawResponse(self)

    async def list(
        self,
        *,
        limit: Optional[int] = None,
        before: Optional[str] = None,
        after: Optional[str] = None,
        order: Optional[str] = None,
        product_variant_id: Optional[int] = None,
        from_: Optional[str] = None,
        to: Optional[str] = None,
        status: Optional[Union[BookingsStatus, str]] = None,
        pagination: Optional[bool] = None,
        page: Optional[int] = None,
        request_options: Optional[RequestOptions] = None,
    ) -> AsyncPage[SdkListBookingDateOverridesResponseValue200ApplicationJson]:
        """List booking date overrides

        List store-wide or variant-specific booking date overrides. Date boundaries are stored in UTC and interpreted in the booking timezone. Requires the `listing` Sanctum ability. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            limit: Number of items to return per page. Defaults to `15`.
            before: The before.
            after: The after.
            order: The order.
            product_variant_id: Return the effective override hierarchy for a same-store booking variant, including inherited store-wide and product-wide rows plus exact variant rows.
            from_: Only return overrides ending after this instant.
            to: Only return overrides starting before this instant.
            status: Filter by override status.
            pagination: Set to false to return at most 100 matching overrides in one data array without pagination links or metadata. Defaults to `true`.
            page: Page number to return. Defaults to `1`.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            AsyncPage[SdkListBookingDateOverridesResponseValue200ApplicationJson]

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "listBookingDateOverrides",
        }
        params = {
            k: v
            for k, v in {
                "limit": limit,
                "before": before,
                "after": after,
                "order": order,
                "product_variant_id": product_variant_id,
                "from": from_,
                "to": to,
                "status": enum_value(status) if status is not None else None,
                "pagination": pagination,
                "page": page,
            }.items()
            if v is not None
        }
        return await self._client.request_page(
            method="get",
            path=("v2", "booking-calendar-events"),
            model=SdkListBookingDateOverridesResponseValue200ApplicationJson,
            params=params,
            request_options=request_options,
        )

    async def set(
        self,
        *,
        dates: List[str],
        available: bool,
        product_variant_id: Union[int, None, NotGiven] = NOT_GIVEN,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkSetBookingDateAvailabilityResponseValue201ApplicationJson:
        """Set booking date availability

        Atomically mark one or more local calendar dates unavailable or available for the store or one same-store booking variant. Requires the `listing` Sanctum ability. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            product_variant_id: The product variant id.
            dates: The dates.
            available: Set false to block the dates and true to clear the exact block or open an inherited block for the variant.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkSetBookingDateAvailabilityResponseValue201ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "setBookingDateAvailability",
        }
        body: Dict[str, Any] = {
            "dates": dates,
            "available": available,
        }
        if not isinstance(product_variant_id, NotGiven):
            body["product_variant_id"] = product_variant_id
        _validate_model(
            body,
            {
                "name": "SdkSetBookingDateAvailabilityRequestApplicationJson",
                "required": ["dates", "available"],
                "properties": {
                    "product_variant_id": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "integer",
                            "constraints": {"minimum": 1},
                        },
                    },
                    "dates": {
                        "kind": "array",
                        "items": {
                            "kind": "primitive",
                            "type": "string",
                            "format": "date",
                        },
                        "constraints": {
                            "minItems": 1,
                            "maxItems": 366,
                            "uniqueItems": True,
                        },
                    },
                    "available": {"kind": "primitive", "type": "boolean"},
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return await self._client.request(
            method="post",
            path=("v2", "booking-calendar-events"),
            body=body,
            model=SdkSetBookingDateAvailabilityResponseValue201ApplicationJson,
            request_options=request_options,
        )
