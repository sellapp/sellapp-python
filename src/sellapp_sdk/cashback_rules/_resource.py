# This file is auto-generated by oagen. Do not edit.

from __future__ import annotations

from typing import TYPE_CHECKING, Any, Dict, List, Optional, Union

from .._base_client import WithRawResponse

if TYPE_CHECKING:
    from .._client import AsyncSellAppClient, SellAppClient

from .._types import NOT_GIVEN, NotGiven, RequestOptions, _validate_model
from .models import (
    SdkCreateCashbackRuleResponseValue201ApplicationJson,
    SdkListCashbackRulesResponseValue200ApplicationJson,
    SdkReplaceCashbackRuleResponseValue200ApplicationJson,
    SdkRestoreCashbackRuleResponseValue200ApplicationJson,
    SdkUpdateCashbackRuleResponseValue200ApplicationJson,
)


class CashbackRules:
    """Cashback Rules API resources."""

    def __init__(self, client: SellAppClient) -> None:
        self._client = client
        self.with_raw_response = WithRawResponse(self)

    def list(
        self,
        *,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkListCashbackRulesResponseValue200ApplicationJson:
        """List cashback rules

        List current and archived store records. Requires the `wallet` or `wallet:write` API ability. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Returns:
            SdkListCashbackRulesResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "listCashbackRules",
        }
        return self._client.request(
            method="get",
            path=("v2", "cashback-rules"),
            model=SdkListCashbackRulesResponseValue200ApplicationJson,
            request_options=request_options,
        )

    def create(
        self,
        *,
        percent_basis: int,
        maximum_cashback_cents: Optional[int],
        is_active: bool,
        product_ids: List[int],
        request_options: Optional[RequestOptions] = None,
    ) -> SdkCreateCashbackRuleResponseValue201ApplicationJson:
        """Create a cashback rule

        Create a cashback rule. Requires the `wallet:write` API ability and the store wallet permission. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            percent_basis: The percent basis.
            maximum_cashback_cents: The maximum cashback cents.
            is_active: The is active.
            product_ids: The product ids.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkCreateCashbackRuleResponseValue201ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "createCashbackRule",
        }
        body: Dict[str, Any] = {
            "percent_basis": percent_basis,
            "maximum_cashback_cents": maximum_cashback_cents,
            "is_active": is_active,
            "product_ids": product_ids,
        }
        _validate_model(
            body,
            {
                "name": "SdkCreateCashbackRuleRequestApplicationJson",
                "required": [
                    "percent_basis",
                    "maximum_cashback_cents",
                    "is_active",
                    "product_ids",
                ],
                "properties": {
                    "percent_basis": {
                        "kind": "primitive",
                        "type": "integer",
                        "constraints": {"minimum": 1, "maximum": 10000},
                    },
                    "maximum_cashback_cents": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "integer",
                            "constraints": {"minimum": 1, "maximum": 999999999999},
                        },
                    },
                    "is_active": {"kind": "primitive", "type": "boolean"},
                    "product_ids": {
                        "kind": "array",
                        "items": {"kind": "primitive", "type": "integer"},
                        "constraints": {"uniqueItems": True},
                    },
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return self._client.request(
            method="post",
            path=("v2", "cashback-rules"),
            body=body,
            model=SdkCreateCashbackRuleResponseValue201ApplicationJson,
            request_options=request_options,
        )

    def replace(
        self,
        cashback_rule: int,
        *,
        percent_basis: int,
        maximum_cashback_cents: Optional[int],
        is_active: bool,
        product_ids: List[int],
        request_options: Optional[RequestOptions] = None,
    ) -> SdkReplaceCashbackRuleResponseValue200ApplicationJson:
        """Update a cashback rule

        Update a cashback rule. Requires the `wallet:write` API ability and the store wallet permission. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            cashback_rule: The cashbackRule path parameter.
            percent_basis: The percent basis.
            maximum_cashback_cents: The maximum cashback cents.
            is_active: The is active.
            product_ids: The product ids.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkReplaceCashbackRuleResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "replaceCashbackRule",
        }
        body: Dict[str, Any] = {
            "percent_basis": percent_basis,
            "maximum_cashback_cents": maximum_cashback_cents,
            "is_active": is_active,
            "product_ids": product_ids,
        }
        _validate_model(
            body,
            {
                "name": "SdkReplaceCashbackRuleRequestApplicationJson",
                "required": [
                    "percent_basis",
                    "maximum_cashback_cents",
                    "is_active",
                    "product_ids",
                ],
                "properties": {
                    "percent_basis": {
                        "kind": "primitive",
                        "type": "integer",
                        "constraints": {"minimum": 1, "maximum": 10000},
                    },
                    "maximum_cashback_cents": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "integer",
                            "constraints": {"minimum": 1, "maximum": 999999999999},
                        },
                    },
                    "is_active": {"kind": "primitive", "type": "boolean"},
                    "product_ids": {
                        "kind": "array",
                        "items": {"kind": "primitive", "type": "integer"},
                        "constraints": {"uniqueItems": True},
                    },
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return self._client.request(
            method="put",
            path=("v2", "cashback-rules", str(cashback_rule)),
            body=body,
            model=SdkReplaceCashbackRuleResponseValue200ApplicationJson,
            request_options=request_options,
        )

    def update(
        self,
        cashback_rule: int,
        *,
        percent_basis: Optional[int] = None,
        maximum_cashback_cents: Union[int, None, NotGiven] = NOT_GIVEN,
        is_active: Optional[bool] = None,
        product_ids: Optional[List[int]] = None,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkUpdateCashbackRuleResponseValue200ApplicationJson:
        """Update a cashback rule

        Update a cashback rule. Requires the `wallet:write` API ability and the store wallet permission. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            cashback_rule: The cashbackRule path parameter.
            percent_basis: The percent basis.
            maximum_cashback_cents: The maximum cashback cents.
            is_active: The is active.
            product_ids: The product ids.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkUpdateCashbackRuleResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "updateCashbackRule",
        }
        body: Dict[str, Any] = {
            k: v
            for k, v in {
                "percent_basis": percent_basis,
                "is_active": is_active,
                "product_ids": product_ids,
            }.items()
            if v is not None
        }
        if not isinstance(maximum_cashback_cents, NotGiven):
            body["maximum_cashback_cents"] = maximum_cashback_cents
        _validate_model(
            body,
            {
                "name": "SdkUpdateCashbackRuleRequestApplicationJson",
                "required": [],
                "properties": {
                    "percent_basis": {
                        "kind": "primitive",
                        "type": "integer",
                        "constraints": {"minimum": 1, "maximum": 10000},
                    },
                    "maximum_cashback_cents": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "integer",
                            "constraints": {"minimum": 1, "maximum": 999999999999},
                        },
                    },
                    "is_active": {"kind": "primitive", "type": "boolean"},
                    "product_ids": {
                        "kind": "array",
                        "items": {"kind": "primitive", "type": "integer"},
                        "constraints": {"uniqueItems": True},
                    },
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return self._client.request(
            method="patch",
            path=("v2", "cashback-rules", str(cashback_rule)),
            body=body,
            model=SdkUpdateCashbackRuleResponseValue200ApplicationJson,
            request_options=request_options,
        )

    def archive(
        self,
        cashback_rule: int,
        *,
        request_options: Optional[RequestOptions] = None,
    ) -> None:
        """Archive a cashback rule

        Archive a cashback rule. Requires the `wallet:write` API ability and the store wallet permission. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            cashback_rule: The cashbackRule path parameter.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "archiveCashbackRule",
        }
        self._client.request(
            method="delete",
            path=("v2", "cashback-rules", str(cashback_rule)),
            request_options=request_options,
        )

    def restore(
        self,
        cashback_rule: int,
        *,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkRestoreCashbackRuleResponseValue200ApplicationJson:
        """Restore a cashback rule

        Restore a cashback rule. Requires the `wallet:write` API ability and the store wallet permission. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            cashback_rule: The cashbackRule path parameter.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkRestoreCashbackRuleResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "restoreCashbackRule",
        }
        return self._client.request(
            method="post",
            path=("v2", "cashback-rules", str(cashback_rule), "restore"),
            model=SdkRestoreCashbackRuleResponseValue200ApplicationJson,
            request_options=request_options,
        )


class AsyncCashbackRules:
    """Cashback Rules API resources (async)."""

    def __init__(self, client: AsyncSellAppClient) -> None:
        self._client = client
        self.with_raw_response = WithRawResponse(self)

    async def list(
        self,
        *,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkListCashbackRulesResponseValue200ApplicationJson:
        """List cashback rules

        List current and archived store records. Requires the `wallet` or `wallet:write` API ability. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Returns:
            SdkListCashbackRulesResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "listCashbackRules",
        }
        return await self._client.request(
            method="get",
            path=("v2", "cashback-rules"),
            model=SdkListCashbackRulesResponseValue200ApplicationJson,
            request_options=request_options,
        )

    async def create(
        self,
        *,
        percent_basis: int,
        maximum_cashback_cents: Optional[int],
        is_active: bool,
        product_ids: List[int],
        request_options: Optional[RequestOptions] = None,
    ) -> SdkCreateCashbackRuleResponseValue201ApplicationJson:
        """Create a cashback rule

        Create a cashback rule. Requires the `wallet:write` API ability and the store wallet permission. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            percent_basis: The percent basis.
            maximum_cashback_cents: The maximum cashback cents.
            is_active: The is active.
            product_ids: The product ids.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkCreateCashbackRuleResponseValue201ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "createCashbackRule",
        }
        body: Dict[str, Any] = {
            "percent_basis": percent_basis,
            "maximum_cashback_cents": maximum_cashback_cents,
            "is_active": is_active,
            "product_ids": product_ids,
        }
        _validate_model(
            body,
            {
                "name": "SdkCreateCashbackRuleRequestApplicationJson",
                "required": [
                    "percent_basis",
                    "maximum_cashback_cents",
                    "is_active",
                    "product_ids",
                ],
                "properties": {
                    "percent_basis": {
                        "kind": "primitive",
                        "type": "integer",
                        "constraints": {"minimum": 1, "maximum": 10000},
                    },
                    "maximum_cashback_cents": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "integer",
                            "constraints": {"minimum": 1, "maximum": 999999999999},
                        },
                    },
                    "is_active": {"kind": "primitive", "type": "boolean"},
                    "product_ids": {
                        "kind": "array",
                        "items": {"kind": "primitive", "type": "integer"},
                        "constraints": {"uniqueItems": True},
                    },
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return await self._client.request(
            method="post",
            path=("v2", "cashback-rules"),
            body=body,
            model=SdkCreateCashbackRuleResponseValue201ApplicationJson,
            request_options=request_options,
        )

    async def replace(
        self,
        cashback_rule: int,
        *,
        percent_basis: int,
        maximum_cashback_cents: Optional[int],
        is_active: bool,
        product_ids: List[int],
        request_options: Optional[RequestOptions] = None,
    ) -> SdkReplaceCashbackRuleResponseValue200ApplicationJson:
        """Update a cashback rule

        Update a cashback rule. Requires the `wallet:write` API ability and the store wallet permission. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            cashback_rule: The cashbackRule path parameter.
            percent_basis: The percent basis.
            maximum_cashback_cents: The maximum cashback cents.
            is_active: The is active.
            product_ids: The product ids.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkReplaceCashbackRuleResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "replaceCashbackRule",
        }
        body: Dict[str, Any] = {
            "percent_basis": percent_basis,
            "maximum_cashback_cents": maximum_cashback_cents,
            "is_active": is_active,
            "product_ids": product_ids,
        }
        _validate_model(
            body,
            {
                "name": "SdkReplaceCashbackRuleRequestApplicationJson",
                "required": [
                    "percent_basis",
                    "maximum_cashback_cents",
                    "is_active",
                    "product_ids",
                ],
                "properties": {
                    "percent_basis": {
                        "kind": "primitive",
                        "type": "integer",
                        "constraints": {"minimum": 1, "maximum": 10000},
                    },
                    "maximum_cashback_cents": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "integer",
                            "constraints": {"minimum": 1, "maximum": 999999999999},
                        },
                    },
                    "is_active": {"kind": "primitive", "type": "boolean"},
                    "product_ids": {
                        "kind": "array",
                        "items": {"kind": "primitive", "type": "integer"},
                        "constraints": {"uniqueItems": True},
                    },
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return await self._client.request(
            method="put",
            path=("v2", "cashback-rules", str(cashback_rule)),
            body=body,
            model=SdkReplaceCashbackRuleResponseValue200ApplicationJson,
            request_options=request_options,
        )

    async def update(
        self,
        cashback_rule: int,
        *,
        percent_basis: Optional[int] = None,
        maximum_cashback_cents: Union[int, None, NotGiven] = NOT_GIVEN,
        is_active: Optional[bool] = None,
        product_ids: Optional[List[int]] = None,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkUpdateCashbackRuleResponseValue200ApplicationJson:
        """Update a cashback rule

        Update a cashback rule. Requires the `wallet:write` API ability and the store wallet permission. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            cashback_rule: The cashbackRule path parameter.
            percent_basis: The percent basis.
            maximum_cashback_cents: The maximum cashback cents.
            is_active: The is active.
            product_ids: The product ids.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkUpdateCashbackRuleResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "updateCashbackRule",
        }
        body: Dict[str, Any] = {
            k: v
            for k, v in {
                "percent_basis": percent_basis,
                "is_active": is_active,
                "product_ids": product_ids,
            }.items()
            if v is not None
        }
        if not isinstance(maximum_cashback_cents, NotGiven):
            body["maximum_cashback_cents"] = maximum_cashback_cents
        _validate_model(
            body,
            {
                "name": "SdkUpdateCashbackRuleRequestApplicationJson",
                "required": [],
                "properties": {
                    "percent_basis": {
                        "kind": "primitive",
                        "type": "integer",
                        "constraints": {"minimum": 1, "maximum": 10000},
                    },
                    "maximum_cashback_cents": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "integer",
                            "constraints": {"minimum": 1, "maximum": 999999999999},
                        },
                    },
                    "is_active": {"kind": "primitive", "type": "boolean"},
                    "product_ids": {
                        "kind": "array",
                        "items": {"kind": "primitive", "type": "integer"},
                        "constraints": {"uniqueItems": True},
                    },
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return await self._client.request(
            method="patch",
            path=("v2", "cashback-rules", str(cashback_rule)),
            body=body,
            model=SdkUpdateCashbackRuleResponseValue200ApplicationJson,
            request_options=request_options,
        )

    async def archive(
        self,
        cashback_rule: int,
        *,
        request_options: Optional[RequestOptions] = None,
    ) -> None:
        """Archive a cashback rule

        Archive a cashback rule. Requires the `wallet:write` API ability and the store wallet permission. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            cashback_rule: The cashbackRule path parameter.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "archiveCashbackRule",
        }
        await self._client.request(
            method="delete",
            path=("v2", "cashback-rules", str(cashback_rule)),
            request_options=request_options,
        )

    async def restore(
        self,
        cashback_rule: int,
        *,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkRestoreCashbackRuleResponseValue200ApplicationJson:
        """Restore a cashback rule

        Restore a cashback rule. Requires the `wallet:write` API ability and the store wallet permission. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            cashback_rule: The cashbackRule path parameter.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkRestoreCashbackRuleResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "restoreCashbackRule",
        }
        return await self._client.request(
            method="post",
            path=("v2", "cashback-rules", str(cashback_rule), "restore"),
            model=SdkRestoreCashbackRuleResponseValue200ApplicationJson,
            request_options=request_options,
        )
