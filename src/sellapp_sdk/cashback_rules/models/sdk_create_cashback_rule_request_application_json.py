# This file is auto-generated by oagen. Do not edit.

from __future__ import annotations

from dataclasses import dataclass
from dataclasses import field as dataclass_field
from typing import Any, Dict, List, Optional

from sellapp_sdk._types import (
    _preserve_unknown_fields,
    _raise_deserialize_error,
    _validate_model,
)

_DOMAIN_RUNTIME_SCHEMA: Dict[str, Any] = {
    "name": "SdkCreateCashbackRuleRequestApplicationJson",
    "required": ["percent_basis", "maximum_cashback_cents", "is_active", "product_ids"],
    "properties": {
        "percent_basis": {
            "kind": "primitive",
            "type": "integer",
            "constraints": {"minimum": 1, "maximum": 10000},
        },
        "maximum_cashback_cents": {
            "kind": "nullable",
            "inner": {
                "kind": "primitive",
                "type": "integer",
                "constraints": {"minimum": 1, "maximum": 999999999999},
            },
        },
        "is_active": {"kind": "primitive", "type": "boolean"},
        "product_ids": {
            "kind": "array",
            "items": {"kind": "primitive", "type": "integer"},
            "constraints": {"uniqueItems": True},
        },
    },
    "additionalProperties": True,
}
_WIRE_RUNTIME_SCHEMA: Dict[str, Any] = {
    "name": "SdkCreateCashbackRuleRequestApplicationJson",
    "required": ["percent_basis", "maximum_cashback_cents", "is_active", "product_ids"],
    "properties": {
        "percent_basis": {
            "kind": "primitive",
            "type": "integer",
            "constraints": {"minimum": 1, "maximum": 10000},
        },
        "maximum_cashback_cents": {
            "kind": "nullable",
            "inner": {
                "kind": "primitive",
                "type": "integer",
                "constraints": {"minimum": 1, "maximum": 999999999999},
            },
        },
        "is_active": {"kind": "primitive", "type": "boolean"},
        "product_ids": {
            "kind": "array",
            "items": {"kind": "primitive", "type": "integer"},
            "constraints": {"uniqueItems": True},
        },
    },
    "additionalProperties": True,
}


@dataclass(slots=True)
class SdkCreateCashbackRuleRequestApplicationJson:
    """Sdk Create Cashback Rule Request Application Json model."""

    percent_basis: int
    maximum_cashback_cents: Optional[int]
    is_active: bool
    product_ids: List[int]
    additional_properties: Dict[str, Any] = dataclass_field(
        default_factory=dict, repr=False
    )
    _from_response: bool = dataclass_field(default=False, repr=False, compare=False)

    @classmethod
    def from_dict(
        cls, data: Dict[str, Any]
    ) -> SdkCreateCashbackRuleRequestApplicationJson:
        """Deserialize from a dictionary."""
        try:
            _validate_model(
                data,
                _WIRE_RUNTIME_SCHEMA,
                direction="response",
                allow_unknown_enums=True,
                allow_unknown_union_variants=True,
                allow_unknown_fields=True,
            )
            return cls(
                percent_basis=data["percent_basis"],
                maximum_cashback_cents=data["maximum_cashback_cents"],
                is_active=data["is_active"],
                product_ids=data["product_ids"],
                additional_properties=_preserve_unknown_fields(
                    data,
                    [
                        "percent_basis",
                        "maximum_cashback_cents",
                        "is_active",
                        "product_ids",
                    ],
                ),
                _from_response=True,
            )
        except (KeyError, ValueError) as e:
            _raise_deserialize_error("SdkCreateCashbackRuleRequestApplicationJson", e)

    def to_dict(self) -> Dict[str, Any]:
        """Serialize to a dictionary."""
        _domain_data: Dict[str, Any] = dict(self.additional_properties)
        _domain_data["percent_basis"] = self.percent_basis
        _domain_data["maximum_cashback_cents"] = self.maximum_cashback_cents
        _domain_data["is_active"] = self.is_active
        _domain_data["product_ids"] = self.product_ids
        _validate_model(
            _domain_data,
            _DOMAIN_RUNTIME_SCHEMA,
            direction="request",
            allow_unknown_enums=self._from_response,
            allow_unknown_union_variants=self._from_response,
            allow_unknown_fields=(self._from_response or True),
        )
        result: Dict[str, Any] = dict(self.additional_properties)
        result["percent_basis"] = self.percent_basis
        if self.maximum_cashback_cents is not None:
            result["maximum_cashback_cents"] = self.maximum_cashback_cents
        else:
            result["maximum_cashback_cents"] = None
        result["is_active"] = self.is_active
        result["product_ids"] = self.product_ids
        return result
