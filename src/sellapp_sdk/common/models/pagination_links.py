# This file is auto-generated by oagen. Do not edit.

from __future__ import annotations

from dataclasses import dataclass
from dataclasses import field as dataclass_field
from typing import Any, Dict, Optional

from sellapp_sdk._types import (
    _preserve_unknown_fields,
    _raise_deserialize_error,
    _validate_model,
)

_DOMAIN_RUNTIME_SCHEMA: Dict[str, Any] = {
    "name": "PaginationLinks",
    "required": ["first", "last", "prev", "next"],
    "properties": {
        "first": {"kind": "primitive", "type": "string"},
        "last": {"kind": "primitive", "type": "string"},
        "prev": {"kind": "nullable", "inner": {"kind": "primitive", "type": "string"}},
        "next": {"kind": "nullable", "inner": {"kind": "primitive", "type": "string"}},
    },
    "additionalProperties": True,
}
_WIRE_RUNTIME_SCHEMA: Dict[str, Any] = {
    "name": "PaginationLinks",
    "required": ["first", "last", "prev", "next"],
    "properties": {
        "first": {"kind": "primitive", "type": "string"},
        "last": {"kind": "primitive", "type": "string"},
        "prev": {"kind": "nullable", "inner": {"kind": "primitive", "type": "string"}},
        "next": {"kind": "nullable", "inner": {"kind": "primitive", "type": "string"}},
    },
    "additionalProperties": True,
}


@dataclass(slots=True)
class PaginationLinks:
    """Pagination Links model."""

    first: str
    last: str
    prev: Optional[str]
    next: Optional[str]
    additional_properties: Dict[str, Any] = dataclass_field(
        default_factory=dict, repr=False
    )
    _from_response: bool = dataclass_field(default=False, repr=False, compare=False)

    @classmethod
    def from_dict(cls, data: Dict[str, Any]) -> PaginationLinks:
        """Deserialize from a dictionary."""
        try:
            _validate_model(
                data,
                _WIRE_RUNTIME_SCHEMA,
                direction="response",
                allow_unknown_enums=True,
                allow_unknown_union_variants=True,
                allow_unknown_fields=True,
            )
            return cls(
                first=data["first"],
                last=data["last"],
                prev=data["prev"],
                next=data["next"],
                additional_properties=_preserve_unknown_fields(
                    data, ["first", "last", "prev", "next"]
                ),
                _from_response=True,
            )
        except (KeyError, ValueError) as e:
            _raise_deserialize_error("PaginationLinks", e)

    def to_dict(self) -> Dict[str, Any]:
        """Serialize to a dictionary."""
        _domain_data: Dict[str, Any] = dict(self.additional_properties)
        _domain_data["first"] = self.first
        _domain_data["last"] = self.last
        _domain_data["prev"] = self.prev
        _domain_data["next"] = self.next
        _validate_model(
            _domain_data,
            _DOMAIN_RUNTIME_SCHEMA,
            direction="request",
            allow_unknown_enums=self._from_response,
            allow_unknown_union_variants=self._from_response,
            allow_unknown_fields=(self._from_response or True),
        )
        result: Dict[str, Any] = dict(self.additional_properties)
        result["first"] = self.first
        result["last"] = self.last
        if self.prev is not None:
            result["prev"] = self.prev
        else:
            result["prev"] = None
        if self.next is not None:
            result["next"] = self.next
        else:
            result["next"] = None
        return result
