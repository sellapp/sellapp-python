# This file is auto-generated by oagen. Do not edit.

from __future__ import annotations

from dataclasses import dataclass
from dataclasses import field as dataclass_field
from typing import Any, Dict, List, Optional, cast

from sellapp_sdk._types import (
    _preserve_unknown_fields,
    _raise_deserialize_error,
    _validate_model,
)

from .pagination_meta_link import PaginationMetaLink

_DOMAIN_RUNTIME_SCHEMA: Dict[str, Any] = {
    "name": "PaginationMeta",
    "required": [
        "current_page",
        "from_",
        "last_page",
        "links",
        "path",
        "per_page",
        "to",
        "total",
    ],
    "properties": {
        "current_page": {"kind": "primitive", "type": "integer"},
        "from_": {
            "kind": "nullable",
            "inner": {"kind": "primitive", "type": "integer"},
        },
        "last_page": {"kind": "primitive", "type": "integer"},
        "links": {"kind": "array", "items": {"kind": "any"}},
        "path": {"kind": "primitive", "type": "string"},
        "per_page": {"kind": "primitive", "type": "integer"},
        "to": {"kind": "nullable", "inner": {"kind": "primitive", "type": "integer"}},
        "total": {"kind": "primitive", "type": "integer"},
    },
    "additionalProperties": True,
}
_WIRE_RUNTIME_SCHEMA: Dict[str, Any] = {
    "name": "PaginationMeta",
    "required": [
        "current_page",
        "from",
        "last_page",
        "links",
        "path",
        "per_page",
        "to",
        "total",
    ],
    "properties": {
        "current_page": {"kind": "primitive", "type": "integer"},
        "from": {"kind": "nullable", "inner": {"kind": "primitive", "type": "integer"}},
        "last_page": {"kind": "primitive", "type": "integer"},
        "links": {"kind": "array", "items": {"kind": "any"}},
        "path": {"kind": "primitive", "type": "string"},
        "per_page": {"kind": "primitive", "type": "integer"},
        "to": {"kind": "nullable", "inner": {"kind": "primitive", "type": "integer"}},
        "total": {"kind": "primitive", "type": "integer"},
    },
    "additionalProperties": True,
}


@dataclass(slots=True)
class PaginationMeta:
    """Pagination Meta model."""

    current_page: int
    from_: Optional[int]
    last_page: int
    links: List[PaginationMetaLink]
    path: str
    per_page: int
    to: Optional[int]
    total: int
    additional_properties: Dict[str, Any] = dataclass_field(
        default_factory=dict, repr=False
    )
    _from_response: bool = dataclass_field(default=False, repr=False, compare=False)

    @classmethod
    def from_dict(cls, data: Dict[str, Any]) -> PaginationMeta:
        """Deserialize from a dictionary."""
        try:
            _validate_model(
                data,
                _WIRE_RUNTIME_SCHEMA,
                direction="response",
                allow_unknown_enums=True,
                allow_unknown_union_variants=True,
                allow_unknown_fields=True,
            )
            return cls(
                current_page=data["current_page"],
                from_=data["from"],
                last_page=data["last_page"],
                links=[
                    PaginationMetaLink.from_dict(cast(Dict[str, Any], item))
                    for item in cast(list[Any], data["links"])
                ],
                path=data["path"],
                per_page=data["per_page"],
                to=data["to"],
                total=data["total"],
                additional_properties=_preserve_unknown_fields(
                    data,
                    [
                        "current_page",
                        "from",
                        "last_page",
                        "links",
                        "path",
                        "per_page",
                        "to",
                        "total",
                    ],
                ),
                _from_response=True,
            )
        except (KeyError, ValueError) as e:
            _raise_deserialize_error("PaginationMeta", e)

    def to_dict(self) -> Dict[str, Any]:
        """Serialize to a dictionary."""
        _domain_data: Dict[str, Any] = dict(self.additional_properties)
        _domain_data["current_page"] = self.current_page
        _domain_data["from_"] = self.from_
        _domain_data["last_page"] = self.last_page
        _domain_data["links"] = self.links
        _domain_data["path"] = self.path
        _domain_data["per_page"] = self.per_page
        _domain_data["to"] = self.to
        _domain_data["total"] = self.total
        _validate_model(
            _domain_data,
            _DOMAIN_RUNTIME_SCHEMA,
            direction="request",
            allow_unknown_enums=self._from_response,
            allow_unknown_union_variants=self._from_response,
            allow_unknown_fields=(self._from_response or True),
        )
        result: Dict[str, Any] = dict(self.additional_properties)
        result["current_page"] = self.current_page
        if self.from_ is not None:
            result["from"] = self.from_
        else:
            result["from"] = None
        result["last_page"] = self.last_page
        result["links"] = [item.to_dict() for item in self.links]
        result["path"] = self.path
        result["per_page"] = self.per_page
        if self.to is not None:
            result["to"] = self.to
        else:
            result["to"] = None
        result["total"] = self.total
        return result
