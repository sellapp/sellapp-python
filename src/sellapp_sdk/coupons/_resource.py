# This file is auto-generated by oagen. Do not edit.

from __future__ import annotations

from typing import TYPE_CHECKING, Any, Dict, List, Optional, Union

from .._base_client import WithRawResponse

if TYPE_CHECKING:
    from .._client import AsyncSellAppClient, SellAppClient

from sellapp_sdk.blacklists.models.batch_update_coupons_request_application_json_property_resources import (
    BatchUpdateCouponsRequestApplicationJsonPropertyResources,
)
from sellapp_sdk.blacklists.models.sdk_batch_update_coupons_response_value_200_application_json import (
    SdkBatchUpdateCouponsResponseValue200ApplicationJson,
)
from sellapp_sdk.blacklists.models.sdk_get_coupon_response_value_200_application_json import (
    SdkGetCouponResponseValue200ApplicationJson,
)
from sellapp_sdk.blacklists.models.sdk_replace_coupon_response_value_200_application_json import (
    SdkReplaceCouponResponseValue200ApplicationJson,
)
from sellapp_sdk.blacklists.models.sdk_update_coupon_response_value_200_application_json import (
    SdkUpdateCouponResponseValue200ApplicationJson,
)
from sellapp_sdk.blacklists.models.sdk_v_2_batch_create_coupons_response_value_200_application_json import (
    SdkV2BatchCreateCouponsResponseValue200ApplicationJson,
)
from sellapp_sdk.blacklists.models.sdk_v_2_batch_update_coupons_response_value_200_application_json import (
    SdkV2BatchUpdateCouponsResponseValue200ApplicationJson,
)
from sellapp_sdk.blacklists.models.sdk_v_2_create_coupon_response_value_201_application_json import (
    SdkV2CreateCouponResponseValue201ApplicationJson,
)
from sellapp_sdk.blacklists.models.sdk_v_2_get_coupon_response_value_200_application_json import (
    SdkV2GetCouponResponseValue200ApplicationJson,
)
from sellapp_sdk.blacklists.models.sdk_v_2_replace_coupon_response_value_200_application_json import (
    SdkV2ReplaceCouponResponseValue200ApplicationJson,
)
from sellapp_sdk.blacklists.models.sdk_v_2_update_coupon_response_value_200_application_json import (
    SdkV2UpdateCouponResponseValue200ApplicationJson,
)
from sellapp_sdk.blacklists.models.search_coupons_request_application_json_property_filters_item import (
    SearchCouponsRequestApplicationJsonPropertyFiltersItem,
)
from sellapp_sdk.blacklists.models.search_coupons_request_application_json_property_includes_item import (
    SearchCouponsRequestApplicationJsonPropertyIncludesItem,
)
from sellapp_sdk.blacklists.models.search_coupons_request_application_json_property_search import (
    SearchCouponsRequestApplicationJsonPropertySearch,
)
from sellapp_sdk.blacklists.models.search_coupons_request_application_json_property_sort_item import (
    SearchCouponsRequestApplicationJsonPropertySortItem,
)
from sellapp_sdk.blacklists.models.search_coupons_response_value_200_application_json_property_data_item import (
    SearchCouponsResponseValue200ApplicationJsonPropertyDataItem,
)
from sellapp_sdk.blacklists.models.v_2_batch_create_coupons_request_application_json_property_resources_item import (
    V2BatchCreateCouponsRequestApplicationJsonPropertyResourcesItem,
)
from sellapp_sdk.blacklists.models.v_2_batch_update_coupons_request_application_json_property_resources import (
    V2BatchUpdateCouponsRequestApplicationJsonPropertyResources,
)
from sellapp_sdk.blacklists.models.v_2_list_coupons_response_value_200_application_json_property_data_item import (
    V2ListCouponsResponseValue200ApplicationJsonPropertyDataItem,
)
from sellapp_sdk.blacklists.models.v_2_search_coupons_request_application_json_property_filters_item import (
    V2SearchCouponsRequestApplicationJsonPropertyFiltersItem,
)
from sellapp_sdk.blacklists.models.v_2_search_coupons_request_application_json_property_includes_item import (
    V2SearchCouponsRequestApplicationJsonPropertyIncludesItem,
)
from sellapp_sdk.blacklists.models.v_2_search_coupons_request_application_json_property_search import (
    V2SearchCouponsRequestApplicationJsonPropertySearch,
)
from sellapp_sdk.blacklists.models.v_2_search_coupons_request_application_json_property_sort_item import (
    V2SearchCouponsRequestApplicationJsonPropertySortItem,
)
from sellapp_sdk.blacklists.models.v_2_search_coupons_response_value_200_application_json_property_data_item import (
    V2SearchCouponsResponseValue200ApplicationJsonPropertyDataItem,
)
from sellapp_sdk.common.models.batch_create_coupons_request_application_json_property_resources_item import (
    BatchCreateCouponsRequestApplicationJsonPropertyResourcesItem,
)
from sellapp_sdk.common.models.list_coupons_response_value_200_application_json_property_data_item import (
    ListCouponsResponseValue200ApplicationJsonPropertyDataItem,
)
from sellapp_sdk.common.models.sdk_batch_create_coupons_response_value_200_application_json import (
    SdkBatchCreateCouponsResponseValue200ApplicationJson,
)
from sellapp_sdk.common.models.sdk_create_coupon_request_application_json_type import (
    SdkCreateCouponRequestApplicationJsonType,
)
from sellapp_sdk.common.models.sdk_create_coupon_response_value_201_application_json import (
    SdkCreateCouponResponseValue201ApplicationJson,
)
from sellapp_sdk.common.models.sdk_replace_coupon_request_application_json_type import (
    SdkReplaceCouponRequestApplicationJsonType,
)
from sellapp_sdk.common.models.sdk_update_coupon_request_application_json_type import (
    SdkUpdateCouponRequestApplicationJsonType,
)
from sellapp_sdk.common.models.sdk_v_2_create_coupon_request_application_json_type import (
    SdkV2CreateCouponRequestApplicationJsonType,
)
from sellapp_sdk.common.models.sdk_v_2_replace_coupon_request_application_json_type import (
    SdkV2ReplaceCouponRequestApplicationJsonType,
)
from sellapp_sdk.common.models.sdk_v_2_update_coupon_request_application_json_type import (
    SdkV2UpdateCouponRequestApplicationJsonType,
)

from .._pagination import AsyncPage, SyncPage
from .._types import NOT_GIVEN, NotGiven, RequestOptions, _validate_model, enum_value


class Coupons:
    """Coupons API resources."""

    def __init__(self, client: SellAppClient) -> None:
        self._client = client
        self.with_raw_response = WithRawResponse(self)

    def list(
        self,
        *,
        limit: Optional[int] = None,
        before: Optional[str] = None,
        after: Optional[str] = None,
        order: Optional[str] = None,
        page: Optional[int] = None,
        with_trashed: Optional[bool] = None,
        only_trashed: Optional[bool] = None,
        request_options: Optional[RequestOptions] = None,
    ) -> SyncPage[ListCouponsResponseValue200ApplicationJsonPropertyDataItem]:
        """List all coupons

        List your store's coupons, 15 per page by default.

        Args:
            limit: Number of items to return per page. Defaults to `15`.
            before: The before.
            after: The after.
            order: The order.
            page: Page number to return. Defaults to `1`.
            with_trashed: Include soft-deleted resources in the result. Defaults to `false`.
            only_trashed: Return only soft-deleted resources. Ignored when with_trashed is true. Defaults to `false`.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SyncPage[ListCouponsResponseValue200ApplicationJsonPropertyDataItem]

        Raises:
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [
                        {"schemeName": "bearerAuth", "scopes": []},
                        {"schemeName": "storeAuth", "scopes": []},
                    ]
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": False,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": True,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": False,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "listCoupons",
        }
        params = {
            k: v
            for k, v in {
                "limit": limit,
                "before": before,
                "after": after,
                "order": order,
                "page": page,
                "with_trashed": with_trashed,
                "only_trashed": only_trashed,
            }.items()
            if v is not None
        }
        return self._client.request_page(
            method="get",
            path=("v1", "coupons"),
            model=ListCouponsResponseValue200ApplicationJsonPropertyDataItem,
            params=params,
            request_options=request_options,
        )

    def create(
        self,
        *,
        code: str,
        type: Union[SdkCreateCouponRequestApplicationJsonType, str],
        discount: Union[float, str],
        store_wide: bool,
        products: Optional[List[int]] = None,
        product_variants: Optional[List[int]] = None,
        limit: Union[int, None, NotGiven] = NOT_GIVEN,
        expires_at: Union[str, None, NotGiven] = NOT_GIVEN,
        minimum_amount: Union[Union[float, str], None, NotGiven] = NOT_GIVEN,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkCreateCouponResponseValue201ApplicationJson:
        """Create a coupon

        Create a discount code for your store.

        Args:
            code: The code.
            type: The type.
            discount: A decimal discount value of at least 1. Percentage discounts cannot exceed 100; amount discounts use the store currency's major unit.
            store_wide: The store wide.
            products: Product IDs the coupon applies to when store_wide is false.
            product_variants: Optional variant restrictions. Every variant must belong to a selected product. Products without listed variants remain eligible on all variants.
            limit: The limit.
            expires_at: A future date and time, or null for no expiry.
            minimum_amount: A decimal minimum order amount in the store currency's major unit, or null for no minimum.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkCreateCouponResponseValue201ApplicationJson

        Raises:
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [
                        {"schemeName": "bearerAuth", "scopes": []},
                        {"schemeName": "storeAuth", "scopes": []},
                    ]
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": False,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": True,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": False,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "createCoupon",
        }
        body: Dict[str, Any] = {
            k: v
            for k, v in {
                "code": code,
                "type": enum_value(type),
                "discount": discount,
                "store_wide": store_wide,
                "products": products,
                "product_variants": product_variants,
            }.items()
            if v is not None
        }
        if not isinstance(limit, NotGiven):
            body["limit"] = limit
        if not isinstance(expires_at, NotGiven):
            body["expires_at"] = expires_at
        if not isinstance(minimum_amount, NotGiven):
            body["minimum_amount"] = minimum_amount
        _validate_model(
            body,
            {
                "name": "SdkCreateCouponRequestApplicationJson",
                "required": ["code", "type", "discount", "store_wide"],
                "properties": {
                    "code": {
                        "kind": "primitive",
                        "type": "string",
                        "constraints": {"maxLength": 255},
                    },
                    "type": {"kind": "enum", "values": ["PERCENTAGE", "AMOUNT"]},
                    "discount": {
                        "kind": "union",
                        "variants": [
                            {
                                "kind": "primitive",
                                "type": "number",
                                "constraints": {"minimum": 1, "maxLength": 255},
                            },
                            {
                                "kind": "primitive",
                                "type": "string",
                                "constraints": {"minimum": 1, "maxLength": 255},
                            },
                        ],
                    },
                    "store_wide": {"kind": "primitive", "type": "boolean"},
                    "products": {
                        "kind": "array",
                        "items": {"kind": "primitive", "type": "integer"},
                        "constraints": {"minItems": 1},
                    },
                    "product_variants": {
                        "kind": "array",
                        "items": {"kind": "primitive", "type": "integer"},
                    },
                    "limit": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "integer",
                            "constraints": {"minimum": 1},
                        },
                    },
                    "expires_at": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "string",
                            "format": "date-time",
                        },
                    },
                    "minimum_amount": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "union",
                            "variants": [
                                {
                                    "kind": "primitive",
                                    "type": "number",
                                    "constraints": {"minimum": 1, "maxLength": 255},
                                },
                                {
                                    "kind": "primitive",
                                    "type": "string",
                                    "constraints": {"minimum": 1, "maxLength": 255},
                                },
                            ],
                        },
                    },
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return self._client.request(
            method="post",
            path=("v1", "coupons"),
            body=body,
            model=SdkCreateCouponResponseValue201ApplicationJson,
            request_options=request_options,
        )

    def get(
        self,
        coupon: int,
        *,
        with_trashed: Optional[bool] = None,
        only_trashed: Optional[bool] = None,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkGetCouponResponseValue200ApplicationJson:
        """Retrieve a coupon

        Retrieve a coupon by its ID.

        Args:
            coupon: The coupon path parameter.
            with_trashed: Include soft-deleted resources in the result. Defaults to `false`.
            only_trashed: Return only soft-deleted resources. Ignored when with_trashed is true. Defaults to `false`.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkGetCouponResponseValue200ApplicationJson

        Raises:
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [
                        {"schemeName": "bearerAuth", "scopes": []},
                        {"schemeName": "storeAuth", "scopes": []},
                    ]
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": False,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": True,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": False,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "getCoupon",
        }
        params: Dict[str, Any] = {
            k: v
            for k, v in {
                "with_trashed": with_trashed,
                "only_trashed": only_trashed,
            }.items()
            if v is not None
        }
        return self._client.request(
            method="get",
            path=("v1", "coupons", str(coupon)),
            params=params,
            model=SdkGetCouponResponseValue200ApplicationJson,
            request_options=request_options,
        )

    def replace(
        self,
        coupon: int,
        *,
        code: Optional[str] = None,
        type: Optional[Union[SdkReplaceCouponRequestApplicationJsonType, str]] = None,
        discount: Optional[Union[float, str]] = None,
        store_wide: Optional[bool] = None,
        products: Optional[List[int]] = None,
        product_variants: Optional[List[int]] = None,
        limit: Union[int, None, NotGiven] = NOT_GIVEN,
        expires_at: Union[str, None, NotGiven] = NOT_GIVEN,
        minimum_amount: Union[Union[float, str], None, NotGiven] = NOT_GIVEN,
        with_trashed: Optional[bool] = None,
        only_trashed: Optional[bool] = None,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkReplaceCouponResponseValue200ApplicationJson:
        """Update a coupon

        Change a coupon's discount, limits, or eligible products.

        Args:
            coupon: The coupon path parameter.
            code: The code.
            type: The type.
            discount: A decimal discount value of at least 1. Percentage discounts cannot exceed 100; amount discounts use the store currency's major unit.
            store_wide: The store wide.
            products: Product IDs the coupon applies to when store_wide is false.
            product_variants: Optional variant restrictions. Omit to preserve existing restrictions or send an empty array to allow every variant of the selected products.
            limit: The limit.
            expires_at: A future date and time, or null for no expiry.
            minimum_amount: A decimal minimum order amount in the store currency's major unit, or null for no minimum.
            with_trashed: Include soft-deleted resources in the result. Defaults to `false`.
            only_trashed: Return only soft-deleted resources. Ignored when with_trashed is true. Defaults to `false`.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkReplaceCouponResponseValue200ApplicationJson

        Raises:
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [
                        {"schemeName": "bearerAuth", "scopes": []},
                        {"schemeName": "storeAuth", "scopes": []},
                    ]
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": False,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": True,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": False,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "replaceCoupon",
        }
        body: Dict[str, Any] = {
            k: v
            for k, v in {
                "code": code,
                "type": enum_value(type) if type is not None else None,
                "discount": discount,
                "store_wide": store_wide,
                "products": products,
                "product_variants": product_variants,
            }.items()
            if v is not None
        }
        if not isinstance(limit, NotGiven):
            body["limit"] = limit
        if not isinstance(expires_at, NotGiven):
            body["expires_at"] = expires_at
        if not isinstance(minimum_amount, NotGiven):
            body["minimum_amount"] = minimum_amount
        _validate_model(
            body,
            {
                "name": "SdkReplaceCouponRequestApplicationJson",
                "required": [],
                "properties": {
                    "code": {
                        "kind": "primitive",
                        "type": "string",
                        "constraints": {"maxLength": 255},
                    },
                    "type": {"kind": "enum", "values": ["PERCENTAGE", "AMOUNT"]},
                    "discount": {
                        "kind": "union",
                        "variants": [
                            {
                                "kind": "primitive",
                                "type": "number",
                                "constraints": {"minimum": 1, "maxLength": 255},
                            },
                            {
                                "kind": "primitive",
                                "type": "string",
                                "constraints": {"minimum": 1, "maxLength": 255},
                            },
                        ],
                    },
                    "store_wide": {"kind": "primitive", "type": "boolean"},
                    "products": {
                        "kind": "array",
                        "items": {"kind": "primitive", "type": "integer"},
                        "constraints": {"minItems": 1},
                    },
                    "product_variants": {
                        "kind": "array",
                        "items": {"kind": "primitive", "type": "integer"},
                    },
                    "limit": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "integer",
                            "constraints": {"minimum": 1},
                        },
                    },
                    "expires_at": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "string",
                            "format": "date-time",
                        },
                    },
                    "minimum_amount": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "union",
                            "variants": [
                                {
                                    "kind": "primitive",
                                    "type": "number",
                                    "constraints": {"minimum": 1, "maxLength": 255},
                                },
                                {
                                    "kind": "primitive",
                                    "type": "string",
                                    "constraints": {"minimum": 1, "maxLength": 255},
                                },
                            ],
                        },
                    },
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        params: Dict[str, Any] = {
            k: v
            for k, v in {
                "with_trashed": with_trashed,
                "only_trashed": only_trashed,
            }.items()
            if v is not None
        }
        return self._client.request(
            method="put",
            path=("v1", "coupons", str(coupon)),
            body=body,
            params=params,
            model=SdkReplaceCouponResponseValue200ApplicationJson,
            request_options=request_options,
        )

    def update(
        self,
        coupon: int,
        *,
        code: Optional[str] = None,
        type: Optional[Union[SdkUpdateCouponRequestApplicationJsonType, str]] = None,
        discount: Optional[Union[float, str]] = None,
        store_wide: Optional[bool] = None,
        products: Optional[List[int]] = None,
        product_variants: Optional[List[int]] = None,
        limit: Union[int, None, NotGiven] = NOT_GIVEN,
        expires_at: Union[str, None, NotGiven] = NOT_GIVEN,
        minimum_amount: Union[Union[float, str], None, NotGiven] = NOT_GIVEN,
        with_trashed: Optional[bool] = None,
        only_trashed: Optional[bool] = None,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkUpdateCouponResponseValue200ApplicationJson:
        """Update a coupon

        Change a coupon's discount, limits, or eligible products.

        Args:
            coupon: The coupon path parameter.
            code: The code.
            type: The type.
            discount: A decimal discount value of at least 1. Percentage discounts cannot exceed 100; amount discounts use the store currency's major unit.
            store_wide: The store wide.
            products: Product IDs the coupon applies to when store_wide is false.
            product_variants: Optional variant restrictions. Omit to preserve existing restrictions or send an empty array to allow every variant of the selected products.
            limit: The limit.
            expires_at: A future date and time, or null for no expiry.
            minimum_amount: A decimal minimum order amount in the store currency's major unit, or null for no minimum.
            with_trashed: Include soft-deleted resources in the result. Defaults to `false`.
            only_trashed: Return only soft-deleted resources. Ignored when with_trashed is true. Defaults to `false`.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkUpdateCouponResponseValue200ApplicationJson

        Raises:
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [
                        {"schemeName": "bearerAuth", "scopes": []},
                        {"schemeName": "storeAuth", "scopes": []},
                    ]
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": False,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": True,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": False,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "updateCoupon",
        }
        body: Dict[str, Any] = {
            k: v
            for k, v in {
                "code": code,
                "type": enum_value(type) if type is not None else None,
                "discount": discount,
                "store_wide": store_wide,
                "products": products,
                "product_variants": product_variants,
            }.items()
            if v is not None
        }
        if not isinstance(limit, NotGiven):
            body["limit"] = limit
        if not isinstance(expires_at, NotGiven):
            body["expires_at"] = expires_at
        if not isinstance(minimum_amount, NotGiven):
            body["minimum_amount"] = minimum_amount
        _validate_model(
            body,
            {
                "name": "SdkUpdateCouponRequestApplicationJson",
                "required": [],
                "properties": {
                    "code": {
                        "kind": "primitive",
                        "type": "string",
                        "constraints": {"maxLength": 255},
                    },
                    "type": {"kind": "enum", "values": ["PERCENTAGE", "AMOUNT"]},
                    "discount": {
                        "kind": "union",
                        "variants": [
                            {
                                "kind": "primitive",
                                "type": "number",
                                "constraints": {"minimum": 1, "maxLength": 255},
                            },
                            {
                                "kind": "primitive",
                                "type": "string",
                                "constraints": {"minimum": 1, "maxLength": 255},
                            },
                        ],
                    },
                    "store_wide": {"kind": "primitive", "type": "boolean"},
                    "products": {
                        "kind": "array",
                        "items": {"kind": "primitive", "type": "integer"},
                        "constraints": {"minItems": 1},
                    },
                    "product_variants": {
                        "kind": "array",
                        "items": {"kind": "primitive", "type": "integer"},
                    },
                    "limit": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "integer",
                            "constraints": {"minimum": 1},
                        },
                    },
                    "expires_at": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "string",
                            "format": "date-time",
                        },
                    },
                    "minimum_amount": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "union",
                            "variants": [
                                {
                                    "kind": "primitive",
                                    "type": "number",
                                    "constraints": {"minimum": 1, "maxLength": 255},
                                },
                                {
                                    "kind": "primitive",
                                    "type": "string",
                                    "constraints": {"minimum": 1, "maxLength": 255},
                                },
                            ],
                        },
                    },
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        params: Dict[str, Any] = {
            k: v
            for k, v in {
                "with_trashed": with_trashed,
                "only_trashed": only_trashed,
            }.items()
            if v is not None
        }
        return self._client.request(
            method="patch",
            path=("v1", "coupons", str(coupon)),
            body=body,
            params=params,
            model=SdkUpdateCouponResponseValue200ApplicationJson,
            request_options=request_options,
        )

    def delete(
        self,
        coupon: int,
        *,
        with_trashed: Optional[bool] = None,
        only_trashed: Optional[bool] = None,
        request_options: Optional[RequestOptions] = None,
    ) -> None:
        """Delete a coupon

        Soft-delete a coupon. The returned coupon includes its deleted_at timestamp; use with_trashed or only_trashed to retrieve soft-deleted coupons later.

        Args:
            coupon: The coupon path parameter.
            with_trashed: Include soft-deleted resources in the result. Defaults to `false`.
            only_trashed: Return only soft-deleted resources. Ignored when with_trashed is true. Defaults to `false`.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Raises:
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [
                        {"schemeName": "bearerAuth", "scopes": []},
                        {"schemeName": "storeAuth", "scopes": []},
                    ]
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": False,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": True,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": False,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "deleteCoupon",
        }
        params: Dict[str, Any] = {
            k: v
            for k, v in {
                "with_trashed": with_trashed,
                "only_trashed": only_trashed,
            }.items()
            if v is not None
        }
        self._client.request(
            method="delete",
            path=("v1", "coupons", str(coupon)),
            params=params,
            request_options=request_options,
        )

    def search(
        self,
        *,
        filters: Optional[
            List[SearchCouponsRequestApplicationJsonPropertyFiltersItem]
        ] = None,
        sort: Optional[
            List[SearchCouponsRequestApplicationJsonPropertySortItem]
        ] = None,
        search: Optional[SearchCouponsRequestApplicationJsonPropertySearch] = None,
        includes: Optional[
            List[SearchCouponsRequestApplicationJsonPropertyIncludesItem]
        ] = None,
        limit: Optional[int] = None,
        before: Optional[str] = None,
        after: Optional[str] = None,
        order: Optional[str] = None,
        with_trashed: Optional[bool] = None,
        only_trashed: Optional[bool] = None,
        request_options: Optional[RequestOptions] = None,
    ) -> SyncPage[SearchCouponsResponseValue200ApplicationJsonPropertyDataItem]:
        """Search coupons

        Search coupons using JSON body filters, search terms, includes, and sort instructions.

        Args:
            filters: The filters.
            sort: The sort.
            search: The search.
            includes: The includes.
            limit: The limit.
            before: The before.
            after: The after.
            order: The order.
            with_trashed: Include soft-deleted resources in the result. Defaults to `false`.
            only_trashed: Return only soft-deleted resources. Ignored when with_trashed is true. Defaults to `false`.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SyncPage[SearchCouponsResponseValue200ApplicationJsonPropertyDataItem]

        Raises:
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [
                        {"schemeName": "bearerAuth", "scopes": []},
                        {"schemeName": "storeAuth", "scopes": []},
                    ]
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": False,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": True,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": False,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "searchCoupons",
        }
        body: Dict[str, Any] = {
            k: v
            for k, v in {
                "filters": [item.to_dict() for item in filters]
                if filters is not None
                else None,
                "sort": [item.to_dict() for item in sort] if sort is not None else None,
                "search": search.to_dict() if search is not None else None,
                "includes": [item.to_dict() for item in includes]
                if includes is not None
                else None,
            }.items()
            if v is not None
        }
        params = {
            k: v
            for k, v in {
                "limit": limit,
                "before": before,
                "after": after,
                "order": order,
                "with_trashed": with_trashed,
                "only_trashed": only_trashed,
            }.items()
            if v is not None
        }
        return self._client.request_page(
            method="post",
            path=("v1", "coupons", "search"),
            model=SearchCouponsResponseValue200ApplicationJsonPropertyDataItem,
            body=body,
            params=params,
            request_options=request_options,
        )

    def batch_create(
        self,
        *,
        resources: List[BatchCreateCouponsRequestApplicationJsonPropertyResourcesItem],
        request_options: Optional[RequestOptions] = None,
    ) -> SdkBatchCreateCouponsResponseValue200ApplicationJson:
        """Batch create coupons

        Create multiple coupons in one request by sending a `resources` array.

        Args:
            resources: The resources.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkBatchCreateCouponsResponseValue200ApplicationJson

        Raises:
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [
                        {"schemeName": "bearerAuth", "scopes": []},
                        {"schemeName": "storeAuth", "scopes": []},
                    ]
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": False,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": True,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": False,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "batchCreateCoupons",
        }
        body: Dict[str, Any] = {
            "resources": [item.to_dict() for item in resources],
        }
        _validate_model(
            body,
            {
                "name": "SdkBatchCreateCouponsRequestApplicationJson",
                "required": ["resources"],
                "properties": {
                    "resources": {"kind": "array", "items": {"kind": "any"}}
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return self._client.request(
            method="post",
            path=("v1", "coupons", "batch"),
            body=body,
            model=SdkBatchCreateCouponsResponseValue200ApplicationJson,
            request_options=request_options,
        )

    def batch_update(
        self,
        *,
        resources: BatchUpdateCouponsRequestApplicationJsonPropertyResources,
        with_trashed: Optional[bool] = None,
        only_trashed: Optional[bool] = None,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkBatchUpdateCouponsResponseValue200ApplicationJson:
        """Batch update coupons

        Update multiple coupons in one request by sending a `resources` object keyed by coupon ID.

        Args:
            resources: The resources.
            with_trashed: Include soft-deleted resources in the result. Defaults to `false`.
            only_trashed: Return only soft-deleted resources. Ignored when with_trashed is true. Defaults to `false`.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkBatchUpdateCouponsResponseValue200ApplicationJson

        Raises:
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [
                        {"schemeName": "bearerAuth", "scopes": []},
                        {"schemeName": "storeAuth", "scopes": []},
                    ]
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": False,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": True,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": False,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "batchUpdateCoupons",
        }
        body: Dict[str, Any] = {
            "resources": resources.to_dict(),
        }
        _validate_model(
            body,
            {
                "name": "SdkBatchUpdateCouponsRequestApplicationJson",
                "required": ["resources"],
                "properties": {"resources": {"kind": "any"}},
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        params: Dict[str, Any] = {
            k: v
            for k, v in {
                "with_trashed": with_trashed,
                "only_trashed": only_trashed,
            }.items()
            if v is not None
        }
        return self._client.request(
            method="patch",
            path=("v1", "coupons", "batch"),
            body=body,
            params=params,
            model=SdkBatchUpdateCouponsResponseValue200ApplicationJson,
            request_options=request_options,
        )

    def batch_delete(
        self,
        *,
        resources: List[int],
        with_trashed: Optional[bool] = None,
        only_trashed: Optional[bool] = None,
        request_options: Optional[RequestOptions] = None,
    ) -> None:
        """Batch delete coupons

        Soft-delete multiple coupons by sending their IDs in `resources`.

        Args:
            resources: The resources.
            with_trashed: Include soft-deleted resources in the result. Defaults to `false`.
            only_trashed: Return only soft-deleted resources. Ignored when with_trashed is true. Defaults to `false`.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Raises:
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [
                        {"schemeName": "bearerAuth", "scopes": []},
                        {"schemeName": "storeAuth", "scopes": []},
                    ]
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": False,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": True,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": False,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "batchDeleteCoupons",
        }
        body: Dict[str, Any] = {
            "resources": resources,
        }
        _validate_model(
            body,
            {
                "name": "SdkBatchDeleteCouponsRequestApplicationJson",
                "required": ["resources"],
                "properties": {
                    "resources": {
                        "kind": "array",
                        "items": {"kind": "primitive", "type": "integer"},
                    }
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        params: Dict[str, Any] = {
            k: v
            for k, v in {
                "with_trashed": with_trashed,
                "only_trashed": only_trashed,
            }.items()
            if v is not None
        }
        self._client.request(
            method="delete",
            path=("v1", "coupons", "batch"),
            body=body,
            params=params,
            request_options=request_options,
        )

    def v2_list_coupons(
        self,
        *,
        limit: Optional[int] = None,
        before: Optional[str] = None,
        after: Optional[str] = None,
        order: Optional[str] = None,
        page: Optional[int] = None,
        with_trashed: Optional[bool] = None,
        only_trashed: Optional[bool] = None,
        request_options: Optional[RequestOptions] = None,
    ) -> SyncPage[V2ListCouponsResponseValue200ApplicationJsonPropertyDataItem]:
        """List all coupons

        List your store's coupons, 15 per page by default. This v2 route is store-scoped. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            limit: Number of items to return per page. Defaults to `15`.
            before: The before.
            after: The after.
            order: The order.
            page: Page number to return. Defaults to `1`.
            with_trashed: Include soft-deleted resources in the result. Defaults to `false`.
            only_trashed: Return only soft-deleted resources. Ignored when with_trashed is true. Defaults to `false`.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SyncPage[V2ListCouponsResponseValue200ApplicationJsonPropertyDataItem]

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "v2ListCoupons",
        }
        params = {
            k: v
            for k, v in {
                "limit": limit,
                "before": before,
                "after": after,
                "order": order,
                "page": page,
                "with_trashed": with_trashed,
                "only_trashed": only_trashed,
            }.items()
            if v is not None
        }
        return self._client.request_page(
            method="get",
            path=("v2", "coupons"),
            model=V2ListCouponsResponseValue200ApplicationJsonPropertyDataItem,
            params=params,
            request_options=request_options,
        )

    def v2_create_coupon(
        self,
        *,
        code: str,
        type: Union[SdkV2CreateCouponRequestApplicationJsonType, str],
        discount: Union[float, str],
        store_wide: bool,
        products: Optional[List[int]] = None,
        product_variants: Optional[List[int]] = None,
        limit: Union[int, None, NotGiven] = NOT_GIVEN,
        expires_at: Union[str, None, NotGiven] = NOT_GIVEN,
        minimum_amount: Union[Union[float, str], None, NotGiven] = NOT_GIVEN,
        idempotency_key: Optional[str] = None,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkV2CreateCouponResponseValue201ApplicationJson:
        """Create a coupon

        Create a discount code for your store. This v2 route is store-scoped. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            code: The code.
            type: The type.
            discount: A decimal discount value of at least 1. Percentage discounts cannot exceed 100; amount discounts use the store currency's major unit.
            store_wide: The store wide.
            products: Product IDs the coupon applies to when store_wide is false.
            product_variants: Optional variant restrictions. Every variant must belong to a selected product. Products without listed variants remain eligible on all variants.
            limit: The limit.
            expires_at: A future date and time, or null for no expiry.
            minimum_amount: A decimal minimum order amount in the store currency's major unit, or null for no minimum.
            idempotency_key: Optional idempotency key for safe retries.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkV2CreateCouponResponseValue201ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": True,
            "operation_id": "v2CreateCoupon",
        }
        body: Dict[str, Any] = {
            k: v
            for k, v in {
                "code": code,
                "type": enum_value(type),
                "discount": discount,
                "store_wide": store_wide,
                "products": products,
                "product_variants": product_variants,
            }.items()
            if v is not None
        }
        if not isinstance(limit, NotGiven):
            body["limit"] = limit
        if not isinstance(expires_at, NotGiven):
            body["expires_at"] = expires_at
        if not isinstance(minimum_amount, NotGiven):
            body["minimum_amount"] = minimum_amount
        _validate_model(
            body,
            {
                "name": "SdkV2CreateCouponRequestApplicationJson",
                "required": ["code", "type", "discount", "store_wide"],
                "properties": {
                    "code": {
                        "kind": "primitive",
                        "type": "string",
                        "constraints": {"maxLength": 255},
                    },
                    "type": {"kind": "enum", "values": ["PERCENTAGE", "AMOUNT"]},
                    "discount": {
                        "kind": "union",
                        "variants": [
                            {
                                "kind": "primitive",
                                "type": "number",
                                "constraints": {"minimum": 1, "maxLength": 255},
                            },
                            {
                                "kind": "primitive",
                                "type": "string",
                                "constraints": {"minimum": 1, "maxLength": 255},
                            },
                        ],
                    },
                    "store_wide": {"kind": "primitive", "type": "boolean"},
                    "products": {
                        "kind": "array",
                        "items": {"kind": "primitive", "type": "integer"},
                        "constraints": {"minItems": 1},
                    },
                    "product_variants": {
                        "kind": "array",
                        "items": {"kind": "primitive", "type": "integer"},
                    },
                    "limit": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "integer",
                            "constraints": {"minimum": 1},
                        },
                    },
                    "expires_at": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "string",
                            "format": "date-time",
                        },
                    },
                    "minimum_amount": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "union",
                            "variants": [
                                {
                                    "kind": "primitive",
                                    "type": "number",
                                    "constraints": {"minimum": 1, "maxLength": 255},
                                },
                                {
                                    "kind": "primitive",
                                    "type": "string",
                                    "constraints": {"minimum": 1, "maxLength": 255},
                                },
                            ],
                        },
                    },
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return self._client.request(
            method="post",
            path=("v2", "coupons"),
            body=body,
            model=SdkV2CreateCouponResponseValue201ApplicationJson,
            idempotency_key=idempotency_key,
            request_options=request_options,
        )

    def v2_search_coupons(
        self,
        *,
        filters: Optional[
            List[V2SearchCouponsRequestApplicationJsonPropertyFiltersItem]
        ] = None,
        sort: Optional[
            List[V2SearchCouponsRequestApplicationJsonPropertySortItem]
        ] = None,
        search: Optional[V2SearchCouponsRequestApplicationJsonPropertySearch] = None,
        includes: Optional[
            List[V2SearchCouponsRequestApplicationJsonPropertyIncludesItem]
        ] = None,
        limit: Optional[int] = None,
        before: Optional[str] = None,
        after: Optional[str] = None,
        order: Optional[str] = None,
        with_trashed: Optional[bool] = None,
        only_trashed: Optional[bool] = None,
        idempotency_key: Optional[str] = None,
        request_options: Optional[RequestOptions] = None,
    ) -> SyncPage[V2SearchCouponsResponseValue200ApplicationJsonPropertyDataItem]:
        """Search coupons

        Search coupons using JSON body filters, search terms, includes, and sort instructions. This v2 route is store-scoped. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            filters: The filters.
            sort: The sort.
            search: The search.
            includes: The includes.
            limit: The limit.
            before: The before.
            after: The after.
            order: The order.
            with_trashed: Include soft-deleted resources in the result. Defaults to `false`.
            only_trashed: Return only soft-deleted resources. Ignored when with_trashed is true. Defaults to `false`.
            idempotency_key: Optional idempotency key for safe retries.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SyncPage[V2SearchCouponsResponseValue200ApplicationJsonPropertyDataItem]

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": True,
            "operation_id": "v2SearchCoupons",
        }
        body: Dict[str, Any] = {
            k: v
            for k, v in {
                "filters": [item.to_dict() for item in filters]
                if filters is not None
                else None,
                "sort": [item.to_dict() for item in sort] if sort is not None else None,
                "search": search.to_dict() if search is not None else None,
                "includes": [item.to_dict() for item in includes]
                if includes is not None
                else None,
            }.items()
            if v is not None
        }
        params = {
            k: v
            for k, v in {
                "limit": limit,
                "before": before,
                "after": after,
                "order": order,
                "with_trashed": with_trashed,
                "only_trashed": only_trashed,
            }.items()
            if v is not None
        }
        return self._client.request_page(
            method="post",
            path=("v2", "coupons", "search"),
            model=V2SearchCouponsResponseValue200ApplicationJsonPropertyDataItem,
            body=body,
            params=params,
            request_options=request_options,
        )

    def v2_get_coupon(
        self,
        coupon: int,
        *,
        with_trashed: Optional[bool] = None,
        only_trashed: Optional[bool] = None,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkV2GetCouponResponseValue200ApplicationJson:
        """Retrieve a coupon

        Retrieve a coupon by its ID. This v2 route is store-scoped. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            coupon: The coupon path parameter.
            with_trashed: Include soft-deleted resources in the result. Defaults to `false`.
            only_trashed: Return only soft-deleted resources. Ignored when with_trashed is true. Defaults to `false`.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkV2GetCouponResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "v2GetCoupon",
        }
        params: Dict[str, Any] = {
            k: v
            for k, v in {
                "with_trashed": with_trashed,
                "only_trashed": only_trashed,
            }.items()
            if v is not None
        }
        return self._client.request(
            method="get",
            path=("v2", "coupons", str(coupon)),
            params=params,
            model=SdkV2GetCouponResponseValue200ApplicationJson,
            request_options=request_options,
        )

    def v2_replace_coupon(
        self,
        coupon: int,
        *,
        code: Optional[str] = None,
        type: Optional[Union[SdkV2ReplaceCouponRequestApplicationJsonType, str]] = None,
        discount: Optional[Union[float, str]] = None,
        store_wide: Optional[bool] = None,
        products: Optional[List[int]] = None,
        product_variants: Optional[List[int]] = None,
        limit: Union[int, None, NotGiven] = NOT_GIVEN,
        expires_at: Union[str, None, NotGiven] = NOT_GIVEN,
        minimum_amount: Union[Union[float, str], None, NotGiven] = NOT_GIVEN,
        with_trashed: Optional[bool] = None,
        only_trashed: Optional[bool] = None,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkV2ReplaceCouponResponseValue200ApplicationJson:
        """Update a coupon

        Change a coupon's discount, limits, or eligible products. This v2 route is store-scoped. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            coupon: The coupon path parameter.
            code: The code.
            type: The type.
            discount: A decimal discount value of at least 1. Percentage discounts cannot exceed 100; amount discounts use the store currency's major unit.
            store_wide: The store wide.
            products: Product IDs the coupon applies to when store_wide is false.
            product_variants: Optional variant restrictions. Omit to preserve existing restrictions or send an empty array to allow every variant of the selected products.
            limit: The limit.
            expires_at: A future date and time, or null for no expiry.
            minimum_amount: A decimal minimum order amount in the store currency's major unit, or null for no minimum.
            with_trashed: Include soft-deleted resources in the result. Defaults to `false`.
            only_trashed: Return only soft-deleted resources. Ignored when with_trashed is true. Defaults to `false`.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkV2ReplaceCouponResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "v2ReplaceCoupon",
        }
        body: Dict[str, Any] = {
            k: v
            for k, v in {
                "code": code,
                "type": enum_value(type) if type is not None else None,
                "discount": discount,
                "store_wide": store_wide,
                "products": products,
                "product_variants": product_variants,
            }.items()
            if v is not None
        }
        if not isinstance(limit, NotGiven):
            body["limit"] = limit
        if not isinstance(expires_at, NotGiven):
            body["expires_at"] = expires_at
        if not isinstance(minimum_amount, NotGiven):
            body["minimum_amount"] = minimum_amount
        _validate_model(
            body,
            {
                "name": "SdkV2ReplaceCouponRequestApplicationJson",
                "required": [],
                "properties": {
                    "code": {
                        "kind": "primitive",
                        "type": "string",
                        "constraints": {"maxLength": 255},
                    },
                    "type": {"kind": "enum", "values": ["PERCENTAGE", "AMOUNT"]},
                    "discount": {
                        "kind": "union",
                        "variants": [
                            {
                                "kind": "primitive",
                                "type": "number",
                                "constraints": {"minimum": 1, "maxLength": 255},
                            },
                            {
                                "kind": "primitive",
                                "type": "string",
                                "constraints": {"minimum": 1, "maxLength": 255},
                            },
                        ],
                    },
                    "store_wide": {"kind": "primitive", "type": "boolean"},
                    "products": {
                        "kind": "array",
                        "items": {"kind": "primitive", "type": "integer"},
                        "constraints": {"minItems": 1},
                    },
                    "product_variants": {
                        "kind": "array",
                        "items": {"kind": "primitive", "type": "integer"},
                    },
                    "limit": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "integer",
                            "constraints": {"minimum": 1},
                        },
                    },
                    "expires_at": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "string",
                            "format": "date-time",
                        },
                    },
                    "minimum_amount": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "union",
                            "variants": [
                                {
                                    "kind": "primitive",
                                    "type": "number",
                                    "constraints": {"minimum": 1, "maxLength": 255},
                                },
                                {
                                    "kind": "primitive",
                                    "type": "string",
                                    "constraints": {"minimum": 1, "maxLength": 255},
                                },
                            ],
                        },
                    },
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        params: Dict[str, Any] = {
            k: v
            for k, v in {
                "with_trashed": with_trashed,
                "only_trashed": only_trashed,
            }.items()
            if v is not None
        }
        return self._client.request(
            method="put",
            path=("v2", "coupons", str(coupon)),
            body=body,
            params=params,
            model=SdkV2ReplaceCouponResponseValue200ApplicationJson,
            request_options=request_options,
        )

    def v2_update_coupon(
        self,
        coupon: int,
        *,
        code: Optional[str] = None,
        type: Optional[Union[SdkV2UpdateCouponRequestApplicationJsonType, str]] = None,
        discount: Optional[Union[float, str]] = None,
        store_wide: Optional[bool] = None,
        products: Optional[List[int]] = None,
        product_variants: Optional[List[int]] = None,
        limit: Union[int, None, NotGiven] = NOT_GIVEN,
        expires_at: Union[str, None, NotGiven] = NOT_GIVEN,
        minimum_amount: Union[Union[float, str], None, NotGiven] = NOT_GIVEN,
        with_trashed: Optional[bool] = None,
        only_trashed: Optional[bool] = None,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkV2UpdateCouponResponseValue200ApplicationJson:
        """Update a coupon

        Change a coupon's discount, limits, or eligible products. This v2 route is store-scoped. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            coupon: The coupon path parameter.
            code: The code.
            type: The type.
            discount: A decimal discount value of at least 1. Percentage discounts cannot exceed 100; amount discounts use the store currency's major unit.
            store_wide: The store wide.
            products: Product IDs the coupon applies to when store_wide is false.
            product_variants: Optional variant restrictions. Omit to preserve existing restrictions or send an empty array to allow every variant of the selected products.
            limit: The limit.
            expires_at: A future date and time, or null for no expiry.
            minimum_amount: A decimal minimum order amount in the store currency's major unit, or null for no minimum.
            with_trashed: Include soft-deleted resources in the result. Defaults to `false`.
            only_trashed: Return only soft-deleted resources. Ignored when with_trashed is true. Defaults to `false`.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkV2UpdateCouponResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "v2UpdateCoupon",
        }
        body: Dict[str, Any] = {
            k: v
            for k, v in {
                "code": code,
                "type": enum_value(type) if type is not None else None,
                "discount": discount,
                "store_wide": store_wide,
                "products": products,
                "product_variants": product_variants,
            }.items()
            if v is not None
        }
        if not isinstance(limit, NotGiven):
            body["limit"] = limit
        if not isinstance(expires_at, NotGiven):
            body["expires_at"] = expires_at
        if not isinstance(minimum_amount, NotGiven):
            body["minimum_amount"] = minimum_amount
        _validate_model(
            body,
            {
                "name": "SdkV2UpdateCouponRequestApplicationJson",
                "required": [],
                "properties": {
                    "code": {
                        "kind": "primitive",
                        "type": "string",
                        "constraints": {"maxLength": 255},
                    },
                    "type": {"kind": "enum", "values": ["PERCENTAGE", "AMOUNT"]},
                    "discount": {
                        "kind": "union",
                        "variants": [
                            {
                                "kind": "primitive",
                                "type": "number",
                                "constraints": {"minimum": 1, "maxLength": 255},
                            },
                            {
                                "kind": "primitive",
                                "type": "string",
                                "constraints": {"minimum": 1, "maxLength": 255},
                            },
                        ],
                    },
                    "store_wide": {"kind": "primitive", "type": "boolean"},
                    "products": {
                        "kind": "array",
                        "items": {"kind": "primitive", "type": "integer"},
                        "constraints": {"minItems": 1},
                    },
                    "product_variants": {
                        "kind": "array",
                        "items": {"kind": "primitive", "type": "integer"},
                    },
                    "limit": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "integer",
                            "constraints": {"minimum": 1},
                        },
                    },
                    "expires_at": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "string",
                            "format": "date-time",
                        },
                    },
                    "minimum_amount": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "union",
                            "variants": [
                                {
                                    "kind": "primitive",
                                    "type": "number",
                                    "constraints": {"minimum": 1, "maxLength": 255},
                                },
                                {
                                    "kind": "primitive",
                                    "type": "string",
                                    "constraints": {"minimum": 1, "maxLength": 255},
                                },
                            ],
                        },
                    },
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        params: Dict[str, Any] = {
            k: v
            for k, v in {
                "with_trashed": with_trashed,
                "only_trashed": only_trashed,
            }.items()
            if v is not None
        }
        return self._client.request(
            method="patch",
            path=("v2", "coupons", str(coupon)),
            body=body,
            params=params,
            model=SdkV2UpdateCouponResponseValue200ApplicationJson,
            request_options=request_options,
        )

    def v2_delete_coupon(
        self,
        coupon: int,
        *,
        with_trashed: Optional[bool] = None,
        only_trashed: Optional[bool] = None,
        request_options: Optional[RequestOptions] = None,
    ) -> None:
        """Delete a coupon

        Soft-delete a coupon. The returned coupon includes its deleted_at timestamp; use with_trashed or only_trashed to retrieve soft-deleted coupons later. This v2 route is store-scoped. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            coupon: The coupon path parameter.
            with_trashed: Include soft-deleted resources in the result. Defaults to `false`.
            only_trashed: Return only soft-deleted resources. Ignored when with_trashed is true. Defaults to `false`.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "v2DeleteCoupon",
        }
        params: Dict[str, Any] = {
            k: v
            for k, v in {
                "with_trashed": with_trashed,
                "only_trashed": only_trashed,
            }.items()
            if v is not None
        }
        self._client.request(
            method="delete",
            path=("v2", "coupons", str(coupon)),
            params=params,
            request_options=request_options,
        )

    def v2_batch_create_coupons(
        self,
        *,
        resources: List[
            V2BatchCreateCouponsRequestApplicationJsonPropertyResourcesItem
        ],
        idempotency_key: Optional[str] = None,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkV2BatchCreateCouponsResponseValue200ApplicationJson:
        """Batch create coupons

        Create multiple coupons in one request by sending a `resources` array. This v2 route is store-scoped. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            resources: The resources.
            idempotency_key: Optional idempotency key for safe retries.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkV2BatchCreateCouponsResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": True,
            "operation_id": "v2BatchCreateCoupons",
        }
        body: Dict[str, Any] = {
            "resources": [item.to_dict() for item in resources],
        }
        _validate_model(
            body,
            {
                "name": "SdkV2BatchCreateCouponsRequestApplicationJson",
                "required": ["resources"],
                "properties": {
                    "resources": {"kind": "array", "items": {"kind": "any"}}
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return self._client.request(
            method="post",
            path=("v2", "coupons", "batch"),
            body=body,
            model=SdkV2BatchCreateCouponsResponseValue200ApplicationJson,
            idempotency_key=idempotency_key,
            request_options=request_options,
        )

    def v2_batch_update_coupons(
        self,
        *,
        resources: V2BatchUpdateCouponsRequestApplicationJsonPropertyResources,
        with_trashed: Optional[bool] = None,
        only_trashed: Optional[bool] = None,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkV2BatchUpdateCouponsResponseValue200ApplicationJson:
        """Batch update coupons

        Update multiple coupons in one request by sending a `resources` object keyed by coupon ID. This v2 route is store-scoped. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            resources: The resources.
            with_trashed: Include soft-deleted resources in the result. Defaults to `false`.
            only_trashed: Return only soft-deleted resources. Ignored when with_trashed is true. Defaults to `false`.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkV2BatchUpdateCouponsResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": True,
            "operation_id": "v2BatchUpdateCoupons",
        }
        body: Dict[str, Any] = {
            "resources": resources.to_dict(),
        }
        _validate_model(
            body,
            {
                "name": "SdkV2BatchUpdateCouponsRequestApplicationJson",
                "required": ["resources"],
                "properties": {"resources": {"kind": "any"}},
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        params: Dict[str, Any] = {
            k: v
            for k, v in {
                "with_trashed": with_trashed,
                "only_trashed": only_trashed,
            }.items()
            if v is not None
        }
        return self._client.request(
            method="patch",
            path=("v2", "coupons", "batch"),
            body=body,
            params=params,
            model=SdkV2BatchUpdateCouponsResponseValue200ApplicationJson,
            request_options=request_options,
        )

    def v2_batch_delete_coupons(
        self,
        *,
        resources: List[int],
        with_trashed: Optional[bool] = None,
        only_trashed: Optional[bool] = None,
        request_options: Optional[RequestOptions] = None,
    ) -> None:
        """Batch delete coupons

        Soft-delete multiple coupons by sending their IDs in `resources`. This v2 route is store-scoped. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            resources: The resources.
            with_trashed: Include soft-deleted resources in the result. Defaults to `false`.
            only_trashed: Return only soft-deleted resources. Ignored when with_trashed is true. Defaults to `false`.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "v2BatchDeleteCoupons",
        }
        body: Dict[str, Any] = {
            "resources": resources,
        }
        _validate_model(
            body,
            {
                "name": "SdkV2BatchDeleteCouponsRequestApplicationJson",
                "required": ["resources"],
                "properties": {
                    "resources": {
                        "kind": "array",
                        "items": {"kind": "primitive", "type": "integer"},
                    }
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        params: Dict[str, Any] = {
            k: v
            for k, v in {
                "with_trashed": with_trashed,
                "only_trashed": only_trashed,
            }.items()
            if v is not None
        }
        self._client.request(
            method="delete",
            path=("v2", "coupons", "batch"),
            body=body,
            params=params,
            request_options=request_options,
        )


class AsyncCoupons:
    """Coupons API resources (async)."""

    def __init__(self, client: AsyncSellAppClient) -> None:
        self._client = client
        self.with_raw_response = WithRawResponse(self)

    async def list(
        self,
        *,
        limit: Optional[int] = None,
        before: Optional[str] = None,
        after: Optional[str] = None,
        order: Optional[str] = None,
        page: Optional[int] = None,
        with_trashed: Optional[bool] = None,
        only_trashed: Optional[bool] = None,
        request_options: Optional[RequestOptions] = None,
    ) -> AsyncPage[ListCouponsResponseValue200ApplicationJsonPropertyDataItem]:
        """List all coupons

        List your store's coupons, 15 per page by default.

        Args:
            limit: Number of items to return per page. Defaults to `15`.
            before: The before.
            after: The after.
            order: The order.
            page: Page number to return. Defaults to `1`.
            with_trashed: Include soft-deleted resources in the result. Defaults to `false`.
            only_trashed: Return only soft-deleted resources. Ignored when with_trashed is true. Defaults to `false`.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            AsyncPage[ListCouponsResponseValue200ApplicationJsonPropertyDataItem]

        Raises:
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [
                        {"schemeName": "bearerAuth", "scopes": []},
                        {"schemeName": "storeAuth", "scopes": []},
                    ]
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": False,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": True,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": False,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "listCoupons",
        }
        params = {
            k: v
            for k, v in {
                "limit": limit,
                "before": before,
                "after": after,
                "order": order,
                "page": page,
                "with_trashed": with_trashed,
                "only_trashed": only_trashed,
            }.items()
            if v is not None
        }
        return await self._client.request_page(
            method="get",
            path=("v1", "coupons"),
            model=ListCouponsResponseValue200ApplicationJsonPropertyDataItem,
            params=params,
            request_options=request_options,
        )

    async def create(
        self,
        *,
        code: str,
        type: Union[SdkCreateCouponRequestApplicationJsonType, str],
        discount: Union[float, str],
        store_wide: bool,
        products: Optional[List[int]] = None,
        product_variants: Optional[List[int]] = None,
        limit: Union[int, None, NotGiven] = NOT_GIVEN,
        expires_at: Union[str, None, NotGiven] = NOT_GIVEN,
        minimum_amount: Union[Union[float, str], None, NotGiven] = NOT_GIVEN,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkCreateCouponResponseValue201ApplicationJson:
        """Create a coupon

        Create a discount code for your store.

        Args:
            code: The code.
            type: The type.
            discount: A decimal discount value of at least 1. Percentage discounts cannot exceed 100; amount discounts use the store currency's major unit.
            store_wide: The store wide.
            products: Product IDs the coupon applies to when store_wide is false.
            product_variants: Optional variant restrictions. Every variant must belong to a selected product. Products without listed variants remain eligible on all variants.
            limit: The limit.
            expires_at: A future date and time, or null for no expiry.
            minimum_amount: A decimal minimum order amount in the store currency's major unit, or null for no minimum.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkCreateCouponResponseValue201ApplicationJson

        Raises:
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [
                        {"schemeName": "bearerAuth", "scopes": []},
                        {"schemeName": "storeAuth", "scopes": []},
                    ]
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": False,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": True,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": False,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "createCoupon",
        }
        body: Dict[str, Any] = {
            k: v
            for k, v in {
                "code": code,
                "type": enum_value(type),
                "discount": discount,
                "store_wide": store_wide,
                "products": products,
                "product_variants": product_variants,
            }.items()
            if v is not None
        }
        if not isinstance(limit, NotGiven):
            body["limit"] = limit
        if not isinstance(expires_at, NotGiven):
            body["expires_at"] = expires_at
        if not isinstance(minimum_amount, NotGiven):
            body["minimum_amount"] = minimum_amount
        _validate_model(
            body,
            {
                "name": "SdkCreateCouponRequestApplicationJson",
                "required": ["code", "type", "discount", "store_wide"],
                "properties": {
                    "code": {
                        "kind": "primitive",
                        "type": "string",
                        "constraints": {"maxLength": 255},
                    },
                    "type": {"kind": "enum", "values": ["PERCENTAGE", "AMOUNT"]},
                    "discount": {
                        "kind": "union",
                        "variants": [
                            {
                                "kind": "primitive",
                                "type": "number",
                                "constraints": {"minimum": 1, "maxLength": 255},
                            },
                            {
                                "kind": "primitive",
                                "type": "string",
                                "constraints": {"minimum": 1, "maxLength": 255},
                            },
                        ],
                    },
                    "store_wide": {"kind": "primitive", "type": "boolean"},
                    "products": {
                        "kind": "array",
                        "items": {"kind": "primitive", "type": "integer"},
                        "constraints": {"minItems": 1},
                    },
                    "product_variants": {
                        "kind": "array",
                        "items": {"kind": "primitive", "type": "integer"},
                    },
                    "limit": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "integer",
                            "constraints": {"minimum": 1},
                        },
                    },
                    "expires_at": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "string",
                            "format": "date-time",
                        },
                    },
                    "minimum_amount": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "union",
                            "variants": [
                                {
                                    "kind": "primitive",
                                    "type": "number",
                                    "constraints": {"minimum": 1, "maxLength": 255},
                                },
                                {
                                    "kind": "primitive",
                                    "type": "string",
                                    "constraints": {"minimum": 1, "maxLength": 255},
                                },
                            ],
                        },
                    },
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return await self._client.request(
            method="post",
            path=("v1", "coupons"),
            body=body,
            model=SdkCreateCouponResponseValue201ApplicationJson,
            request_options=request_options,
        )

    async def get(
        self,
        coupon: int,
        *,
        with_trashed: Optional[bool] = None,
        only_trashed: Optional[bool] = None,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkGetCouponResponseValue200ApplicationJson:
        """Retrieve a coupon

        Retrieve a coupon by its ID.

        Args:
            coupon: The coupon path parameter.
            with_trashed: Include soft-deleted resources in the result. Defaults to `false`.
            only_trashed: Return only soft-deleted resources. Ignored when with_trashed is true. Defaults to `false`.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkGetCouponResponseValue200ApplicationJson

        Raises:
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [
                        {"schemeName": "bearerAuth", "scopes": []},
                        {"schemeName": "storeAuth", "scopes": []},
                    ]
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": False,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": True,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": False,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "getCoupon",
        }
        params: Dict[str, Any] = {
            k: v
            for k, v in {
                "with_trashed": with_trashed,
                "only_trashed": only_trashed,
            }.items()
            if v is not None
        }
        return await self._client.request(
            method="get",
            path=("v1", "coupons", str(coupon)),
            params=params,
            model=SdkGetCouponResponseValue200ApplicationJson,
            request_options=request_options,
        )

    async def replace(
        self,
        coupon: int,
        *,
        code: Optional[str] = None,
        type: Optional[Union[SdkReplaceCouponRequestApplicationJsonType, str]] = None,
        discount: Optional[Union[float, str]] = None,
        store_wide: Optional[bool] = None,
        products: Optional[List[int]] = None,
        product_variants: Optional[List[int]] = None,
        limit: Union[int, None, NotGiven] = NOT_GIVEN,
        expires_at: Union[str, None, NotGiven] = NOT_GIVEN,
        minimum_amount: Union[Union[float, str], None, NotGiven] = NOT_GIVEN,
        with_trashed: Optional[bool] = None,
        only_trashed: Optional[bool] = None,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkReplaceCouponResponseValue200ApplicationJson:
        """Update a coupon

        Change a coupon's discount, limits, or eligible products.

        Args:
            coupon: The coupon path parameter.
            code: The code.
            type: The type.
            discount: A decimal discount value of at least 1. Percentage discounts cannot exceed 100; amount discounts use the store currency's major unit.
            store_wide: The store wide.
            products: Product IDs the coupon applies to when store_wide is false.
            product_variants: Optional variant restrictions. Omit to preserve existing restrictions or send an empty array to allow every variant of the selected products.
            limit: The limit.
            expires_at: A future date and time, or null for no expiry.
            minimum_amount: A decimal minimum order amount in the store currency's major unit, or null for no minimum.
            with_trashed: Include soft-deleted resources in the result. Defaults to `false`.
            only_trashed: Return only soft-deleted resources. Ignored when with_trashed is true. Defaults to `false`.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkReplaceCouponResponseValue200ApplicationJson

        Raises:
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [
                        {"schemeName": "bearerAuth", "scopes": []},
                        {"schemeName": "storeAuth", "scopes": []},
                    ]
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": False,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": True,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": False,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "replaceCoupon",
        }
        body: Dict[str, Any] = {
            k: v
            for k, v in {
                "code": code,
                "type": enum_value(type) if type is not None else None,
                "discount": discount,
                "store_wide": store_wide,
                "products": products,
                "product_variants": product_variants,
            }.items()
            if v is not None
        }
        if not isinstance(limit, NotGiven):
            body["limit"] = limit
        if not isinstance(expires_at, NotGiven):
            body["expires_at"] = expires_at
        if not isinstance(minimum_amount, NotGiven):
            body["minimum_amount"] = minimum_amount
        _validate_model(
            body,
            {
                "name": "SdkReplaceCouponRequestApplicationJson",
                "required": [],
                "properties": {
                    "code": {
                        "kind": "primitive",
                        "type": "string",
                        "constraints": {"maxLength": 255},
                    },
                    "type": {"kind": "enum", "values": ["PERCENTAGE", "AMOUNT"]},
                    "discount": {
                        "kind": "union",
                        "variants": [
                            {
                                "kind": "primitive",
                                "type": "number",
                                "constraints": {"minimum": 1, "maxLength": 255},
                            },
                            {
                                "kind": "primitive",
                                "type": "string",
                                "constraints": {"minimum": 1, "maxLength": 255},
                            },
                        ],
                    },
                    "store_wide": {"kind": "primitive", "type": "boolean"},
                    "products": {
                        "kind": "array",
                        "items": {"kind": "primitive", "type": "integer"},
                        "constraints": {"minItems": 1},
                    },
                    "product_variants": {
                        "kind": "array",
                        "items": {"kind": "primitive", "type": "integer"},
                    },
                    "limit": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "integer",
                            "constraints": {"minimum": 1},
                        },
                    },
                    "expires_at": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "string",
                            "format": "date-time",
                        },
                    },
                    "minimum_amount": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "union",
                            "variants": [
                                {
                                    "kind": "primitive",
                                    "type": "number",
                                    "constraints": {"minimum": 1, "maxLength": 255},
                                },
                                {
                                    "kind": "primitive",
                                    "type": "string",
                                    "constraints": {"minimum": 1, "maxLength": 255},
                                },
                            ],
                        },
                    },
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        params: Dict[str, Any] = {
            k: v
            for k, v in {
                "with_trashed": with_trashed,
                "only_trashed": only_trashed,
            }.items()
            if v is not None
        }
        return await self._client.request(
            method="put",
            path=("v1", "coupons", str(coupon)),
            body=body,
            params=params,
            model=SdkReplaceCouponResponseValue200ApplicationJson,
            request_options=request_options,
        )

    async def update(
        self,
        coupon: int,
        *,
        code: Optional[str] = None,
        type: Optional[Union[SdkUpdateCouponRequestApplicationJsonType, str]] = None,
        discount: Optional[Union[float, str]] = None,
        store_wide: Optional[bool] = None,
        products: Optional[List[int]] = None,
        product_variants: Optional[List[int]] = None,
        limit: Union[int, None, NotGiven] = NOT_GIVEN,
        expires_at: Union[str, None, NotGiven] = NOT_GIVEN,
        minimum_amount: Union[Union[float, str], None, NotGiven] = NOT_GIVEN,
        with_trashed: Optional[bool] = None,
        only_trashed: Optional[bool] = None,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkUpdateCouponResponseValue200ApplicationJson:
        """Update a coupon

        Change a coupon's discount, limits, or eligible products.

        Args:
            coupon: The coupon path parameter.
            code: The code.
            type: The type.
            discount: A decimal discount value of at least 1. Percentage discounts cannot exceed 100; amount discounts use the store currency's major unit.
            store_wide: The store wide.
            products: Product IDs the coupon applies to when store_wide is false.
            product_variants: Optional variant restrictions. Omit to preserve existing restrictions or send an empty array to allow every variant of the selected products.
            limit: The limit.
            expires_at: A future date and time, or null for no expiry.
            minimum_amount: A decimal minimum order amount in the store currency's major unit, or null for no minimum.
            with_trashed: Include soft-deleted resources in the result. Defaults to `false`.
            only_trashed: Return only soft-deleted resources. Ignored when with_trashed is true. Defaults to `false`.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkUpdateCouponResponseValue200ApplicationJson

        Raises:
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [
                        {"schemeName": "bearerAuth", "scopes": []},
                        {"schemeName": "storeAuth", "scopes": []},
                    ]
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": False,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": True,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": False,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "updateCoupon",
        }
        body: Dict[str, Any] = {
            k: v
            for k, v in {
                "code": code,
                "type": enum_value(type) if type is not None else None,
                "discount": discount,
                "store_wide": store_wide,
                "products": products,
                "product_variants": product_variants,
            }.items()
            if v is not None
        }
        if not isinstance(limit, NotGiven):
            body["limit"] = limit
        if not isinstance(expires_at, NotGiven):
            body["expires_at"] = expires_at
        if not isinstance(minimum_amount, NotGiven):
            body["minimum_amount"] = minimum_amount
        _validate_model(
            body,
            {
                "name": "SdkUpdateCouponRequestApplicationJson",
                "required": [],
                "properties": {
                    "code": {
                        "kind": "primitive",
                        "type": "string",
                        "constraints": {"maxLength": 255},
                    },
                    "type": {"kind": "enum", "values": ["PERCENTAGE", "AMOUNT"]},
                    "discount": {
                        "kind": "union",
                        "variants": [
                            {
                                "kind": "primitive",
                                "type": "number",
                                "constraints": {"minimum": 1, "maxLength": 255},
                            },
                            {
                                "kind": "primitive",
                                "type": "string",
                                "constraints": {"minimum": 1, "maxLength": 255},
                            },
                        ],
                    },
                    "store_wide": {"kind": "primitive", "type": "boolean"},
                    "products": {
                        "kind": "array",
                        "items": {"kind": "primitive", "type": "integer"},
                        "constraints": {"minItems": 1},
                    },
                    "product_variants": {
                        "kind": "array",
                        "items": {"kind": "primitive", "type": "integer"},
                    },
                    "limit": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "integer",
                            "constraints": {"minimum": 1},
                        },
                    },
                    "expires_at": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "string",
                            "format": "date-time",
                        },
                    },
                    "minimum_amount": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "union",
                            "variants": [
                                {
                                    "kind": "primitive",
                                    "type": "number",
                                    "constraints": {"minimum": 1, "maxLength": 255},
                                },
                                {
                                    "kind": "primitive",
                                    "type": "string",
                                    "constraints": {"minimum": 1, "maxLength": 255},
                                },
                            ],
                        },
                    },
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        params: Dict[str, Any] = {
            k: v
            for k, v in {
                "with_trashed": with_trashed,
                "only_trashed": only_trashed,
            }.items()
            if v is not None
        }
        return await self._client.request(
            method="patch",
            path=("v1", "coupons", str(coupon)),
            body=body,
            params=params,
            model=SdkUpdateCouponResponseValue200ApplicationJson,
            request_options=request_options,
        )

    async def delete(
        self,
        coupon: int,
        *,
        with_trashed: Optional[bool] = None,
        only_trashed: Optional[bool] = None,
        request_options: Optional[RequestOptions] = None,
    ) -> None:
        """Delete a coupon

        Soft-delete a coupon. The returned coupon includes its deleted_at timestamp; use with_trashed or only_trashed to retrieve soft-deleted coupons later.

        Args:
            coupon: The coupon path parameter.
            with_trashed: Include soft-deleted resources in the result. Defaults to `false`.
            only_trashed: Return only soft-deleted resources. Ignored when with_trashed is true. Defaults to `false`.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Raises:
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [
                        {"schemeName": "bearerAuth", "scopes": []},
                        {"schemeName": "storeAuth", "scopes": []},
                    ]
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": False,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": True,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": False,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "deleteCoupon",
        }
        params: Dict[str, Any] = {
            k: v
            for k, v in {
                "with_trashed": with_trashed,
                "only_trashed": only_trashed,
            }.items()
            if v is not None
        }
        await self._client.request(
            method="delete",
            path=("v1", "coupons", str(coupon)),
            params=params,
            request_options=request_options,
        )

    async def search(
        self,
        *,
        filters: Optional[
            List[SearchCouponsRequestApplicationJsonPropertyFiltersItem]
        ] = None,
        sort: Optional[
            List[SearchCouponsRequestApplicationJsonPropertySortItem]
        ] = None,
        search: Optional[SearchCouponsRequestApplicationJsonPropertySearch] = None,
        includes: Optional[
            List[SearchCouponsRequestApplicationJsonPropertyIncludesItem]
        ] = None,
        limit: Optional[int] = None,
        before: Optional[str] = None,
        after: Optional[str] = None,
        order: Optional[str] = None,
        with_trashed: Optional[bool] = None,
        only_trashed: Optional[bool] = None,
        request_options: Optional[RequestOptions] = None,
    ) -> AsyncPage[SearchCouponsResponseValue200ApplicationJsonPropertyDataItem]:
        """Search coupons

        Search coupons using JSON body filters, search terms, includes, and sort instructions.

        Args:
            filters: The filters.
            sort: The sort.
            search: The search.
            includes: The includes.
            limit: The limit.
            before: The before.
            after: The after.
            order: The order.
            with_trashed: Include soft-deleted resources in the result. Defaults to `false`.
            only_trashed: Return only soft-deleted resources. Ignored when with_trashed is true. Defaults to `false`.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            AsyncPage[SearchCouponsResponseValue200ApplicationJsonPropertyDataItem]

        Raises:
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [
                        {"schemeName": "bearerAuth", "scopes": []},
                        {"schemeName": "storeAuth", "scopes": []},
                    ]
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": False,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": True,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": False,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "searchCoupons",
        }
        body: Dict[str, Any] = {
            k: v
            for k, v in {
                "filters": [item.to_dict() for item in filters]
                if filters is not None
                else None,
                "sort": [item.to_dict() for item in sort] if sort is not None else None,
                "search": search.to_dict() if search is not None else None,
                "includes": [item.to_dict() for item in includes]
                if includes is not None
                else None,
            }.items()
            if v is not None
        }
        params = {
            k: v
            for k, v in {
                "limit": limit,
                "before": before,
                "after": after,
                "order": order,
                "with_trashed": with_trashed,
                "only_trashed": only_trashed,
            }.items()
            if v is not None
        }
        return await self._client.request_page(
            method="post",
            path=("v1", "coupons", "search"),
            model=SearchCouponsResponseValue200ApplicationJsonPropertyDataItem,
            body=body,
            params=params,
            request_options=request_options,
        )

    async def batch_create(
        self,
        *,
        resources: List[BatchCreateCouponsRequestApplicationJsonPropertyResourcesItem],
        request_options: Optional[RequestOptions] = None,
    ) -> SdkBatchCreateCouponsResponseValue200ApplicationJson:
        """Batch create coupons

        Create multiple coupons in one request by sending a `resources` array.

        Args:
            resources: The resources.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkBatchCreateCouponsResponseValue200ApplicationJson

        Raises:
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [
                        {"schemeName": "bearerAuth", "scopes": []},
                        {"schemeName": "storeAuth", "scopes": []},
                    ]
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": False,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": True,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": False,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "batchCreateCoupons",
        }
        body: Dict[str, Any] = {
            "resources": [item.to_dict() for item in resources],
        }
        _validate_model(
            body,
            {
                "name": "SdkBatchCreateCouponsRequestApplicationJson",
                "required": ["resources"],
                "properties": {
                    "resources": {"kind": "array", "items": {"kind": "any"}}
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return await self._client.request(
            method="post",
            path=("v1", "coupons", "batch"),
            body=body,
            model=SdkBatchCreateCouponsResponseValue200ApplicationJson,
            request_options=request_options,
        )

    async def batch_update(
        self,
        *,
        resources: BatchUpdateCouponsRequestApplicationJsonPropertyResources,
        with_trashed: Optional[bool] = None,
        only_trashed: Optional[bool] = None,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkBatchUpdateCouponsResponseValue200ApplicationJson:
        """Batch update coupons

        Update multiple coupons in one request by sending a `resources` object keyed by coupon ID.

        Args:
            resources: The resources.
            with_trashed: Include soft-deleted resources in the result. Defaults to `false`.
            only_trashed: Return only soft-deleted resources. Ignored when with_trashed is true. Defaults to `false`.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkBatchUpdateCouponsResponseValue200ApplicationJson

        Raises:
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [
                        {"schemeName": "bearerAuth", "scopes": []},
                        {"schemeName": "storeAuth", "scopes": []},
                    ]
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": False,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": True,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": False,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "batchUpdateCoupons",
        }
        body: Dict[str, Any] = {
            "resources": resources.to_dict(),
        }
        _validate_model(
            body,
            {
                "name": "SdkBatchUpdateCouponsRequestApplicationJson",
                "required": ["resources"],
                "properties": {"resources": {"kind": "any"}},
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        params: Dict[str, Any] = {
            k: v
            for k, v in {
                "with_trashed": with_trashed,
                "only_trashed": only_trashed,
            }.items()
            if v is not None
        }
        return await self._client.request(
            method="patch",
            path=("v1", "coupons", "batch"),
            body=body,
            params=params,
            model=SdkBatchUpdateCouponsResponseValue200ApplicationJson,
            request_options=request_options,
        )

    async def batch_delete(
        self,
        *,
        resources: List[int],
        with_trashed: Optional[bool] = None,
        only_trashed: Optional[bool] = None,
        request_options: Optional[RequestOptions] = None,
    ) -> None:
        """Batch delete coupons

        Soft-delete multiple coupons by sending their IDs in `resources`.

        Args:
            resources: The resources.
            with_trashed: Include soft-deleted resources in the result. Defaults to `false`.
            only_trashed: Return only soft-deleted resources. Ignored when with_trashed is true. Defaults to `false`.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Raises:
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [
                        {"schemeName": "bearerAuth", "scopes": []},
                        {"schemeName": "storeAuth", "scopes": []},
                    ]
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": False,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": True,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": False,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "batchDeleteCoupons",
        }
        body: Dict[str, Any] = {
            "resources": resources,
        }
        _validate_model(
            body,
            {
                "name": "SdkBatchDeleteCouponsRequestApplicationJson",
                "required": ["resources"],
                "properties": {
                    "resources": {
                        "kind": "array",
                        "items": {"kind": "primitive", "type": "integer"},
                    }
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        params: Dict[str, Any] = {
            k: v
            for k, v in {
                "with_trashed": with_trashed,
                "only_trashed": only_trashed,
            }.items()
            if v is not None
        }
        await self._client.request(
            method="delete",
            path=("v1", "coupons", "batch"),
            body=body,
            params=params,
            request_options=request_options,
        )

    async def v2_list_coupons(
        self,
        *,
        limit: Optional[int] = None,
        before: Optional[str] = None,
        after: Optional[str] = None,
        order: Optional[str] = None,
        page: Optional[int] = None,
        with_trashed: Optional[bool] = None,
        only_trashed: Optional[bool] = None,
        request_options: Optional[RequestOptions] = None,
    ) -> AsyncPage[V2ListCouponsResponseValue200ApplicationJsonPropertyDataItem]:
        """List all coupons

        List your store's coupons, 15 per page by default. This v2 route is store-scoped. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            limit: Number of items to return per page. Defaults to `15`.
            before: The before.
            after: The after.
            order: The order.
            page: Page number to return. Defaults to `1`.
            with_trashed: Include soft-deleted resources in the result. Defaults to `false`.
            only_trashed: Return only soft-deleted resources. Ignored when with_trashed is true. Defaults to `false`.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            AsyncPage[V2ListCouponsResponseValue200ApplicationJsonPropertyDataItem]

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "v2ListCoupons",
        }
        params = {
            k: v
            for k, v in {
                "limit": limit,
                "before": before,
                "after": after,
                "order": order,
                "page": page,
                "with_trashed": with_trashed,
                "only_trashed": only_trashed,
            }.items()
            if v is not None
        }
        return await self._client.request_page(
            method="get",
            path=("v2", "coupons"),
            model=V2ListCouponsResponseValue200ApplicationJsonPropertyDataItem,
            params=params,
            request_options=request_options,
        )

    async def v2_create_coupon(
        self,
        *,
        code: str,
        type: Union[SdkV2CreateCouponRequestApplicationJsonType, str],
        discount: Union[float, str],
        store_wide: bool,
        products: Optional[List[int]] = None,
        product_variants: Optional[List[int]] = None,
        limit: Union[int, None, NotGiven] = NOT_GIVEN,
        expires_at: Union[str, None, NotGiven] = NOT_GIVEN,
        minimum_amount: Union[Union[float, str], None, NotGiven] = NOT_GIVEN,
        idempotency_key: Optional[str] = None,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkV2CreateCouponResponseValue201ApplicationJson:
        """Create a coupon

        Create a discount code for your store. This v2 route is store-scoped. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            code: The code.
            type: The type.
            discount: A decimal discount value of at least 1. Percentage discounts cannot exceed 100; amount discounts use the store currency's major unit.
            store_wide: The store wide.
            products: Product IDs the coupon applies to when store_wide is false.
            product_variants: Optional variant restrictions. Every variant must belong to a selected product. Products without listed variants remain eligible on all variants.
            limit: The limit.
            expires_at: A future date and time, or null for no expiry.
            minimum_amount: A decimal minimum order amount in the store currency's major unit, or null for no minimum.
            idempotency_key: Optional idempotency key for safe retries.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkV2CreateCouponResponseValue201ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": True,
            "operation_id": "v2CreateCoupon",
        }
        body: Dict[str, Any] = {
            k: v
            for k, v in {
                "code": code,
                "type": enum_value(type),
                "discount": discount,
                "store_wide": store_wide,
                "products": products,
                "product_variants": product_variants,
            }.items()
            if v is not None
        }
        if not isinstance(limit, NotGiven):
            body["limit"] = limit
        if not isinstance(expires_at, NotGiven):
            body["expires_at"] = expires_at
        if not isinstance(minimum_amount, NotGiven):
            body["minimum_amount"] = minimum_amount
        _validate_model(
            body,
            {
                "name": "SdkV2CreateCouponRequestApplicationJson",
                "required": ["code", "type", "discount", "store_wide"],
                "properties": {
                    "code": {
                        "kind": "primitive",
                        "type": "string",
                        "constraints": {"maxLength": 255},
                    },
                    "type": {"kind": "enum", "values": ["PERCENTAGE", "AMOUNT"]},
                    "discount": {
                        "kind": "union",
                        "variants": [
                            {
                                "kind": "primitive",
                                "type": "number",
                                "constraints": {"minimum": 1, "maxLength": 255},
                            },
                            {
                                "kind": "primitive",
                                "type": "string",
                                "constraints": {"minimum": 1, "maxLength": 255},
                            },
                        ],
                    },
                    "store_wide": {"kind": "primitive", "type": "boolean"},
                    "products": {
                        "kind": "array",
                        "items": {"kind": "primitive", "type": "integer"},
                        "constraints": {"minItems": 1},
                    },
                    "product_variants": {
                        "kind": "array",
                        "items": {"kind": "primitive", "type": "integer"},
                    },
                    "limit": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "integer",
                            "constraints": {"minimum": 1},
                        },
                    },
                    "expires_at": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "string",
                            "format": "date-time",
                        },
                    },
                    "minimum_amount": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "union",
                            "variants": [
                                {
                                    "kind": "primitive",
                                    "type": "number",
                                    "constraints": {"minimum": 1, "maxLength": 255},
                                },
                                {
                                    "kind": "primitive",
                                    "type": "string",
                                    "constraints": {"minimum": 1, "maxLength": 255},
                                },
                            ],
                        },
                    },
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return await self._client.request(
            method="post",
            path=("v2", "coupons"),
            body=body,
            model=SdkV2CreateCouponResponseValue201ApplicationJson,
            idempotency_key=idempotency_key,
            request_options=request_options,
        )

    async def v2_search_coupons(
        self,
        *,
        filters: Optional[
            List[V2SearchCouponsRequestApplicationJsonPropertyFiltersItem]
        ] = None,
        sort: Optional[
            List[V2SearchCouponsRequestApplicationJsonPropertySortItem]
        ] = None,
        search: Optional[V2SearchCouponsRequestApplicationJsonPropertySearch] = None,
        includes: Optional[
            List[V2SearchCouponsRequestApplicationJsonPropertyIncludesItem]
        ] = None,
        limit: Optional[int] = None,
        before: Optional[str] = None,
        after: Optional[str] = None,
        order: Optional[str] = None,
        with_trashed: Optional[bool] = None,
        only_trashed: Optional[bool] = None,
        idempotency_key: Optional[str] = None,
        request_options: Optional[RequestOptions] = None,
    ) -> AsyncPage[V2SearchCouponsResponseValue200ApplicationJsonPropertyDataItem]:
        """Search coupons

        Search coupons using JSON body filters, search terms, includes, and sort instructions. This v2 route is store-scoped. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            filters: The filters.
            sort: The sort.
            search: The search.
            includes: The includes.
            limit: The limit.
            before: The before.
            after: The after.
            order: The order.
            with_trashed: Include soft-deleted resources in the result. Defaults to `false`.
            only_trashed: Return only soft-deleted resources. Ignored when with_trashed is true. Defaults to `false`.
            idempotency_key: Optional idempotency key for safe retries.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            AsyncPage[V2SearchCouponsResponseValue200ApplicationJsonPropertyDataItem]

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": True,
            "operation_id": "v2SearchCoupons",
        }
        body: Dict[str, Any] = {
            k: v
            for k, v in {
                "filters": [item.to_dict() for item in filters]
                if filters is not None
                else None,
                "sort": [item.to_dict() for item in sort] if sort is not None else None,
                "search": search.to_dict() if search is not None else None,
                "includes": [item.to_dict() for item in includes]
                if includes is not None
                else None,
            }.items()
            if v is not None
        }
        params = {
            k: v
            for k, v in {
                "limit": limit,
                "before": before,
                "after": after,
                "order": order,
                "with_trashed": with_trashed,
                "only_trashed": only_trashed,
            }.items()
            if v is not None
        }
        return await self._client.request_page(
            method="post",
            path=("v2", "coupons", "search"),
            model=V2SearchCouponsResponseValue200ApplicationJsonPropertyDataItem,
            body=body,
            params=params,
            request_options=request_options,
        )

    async def v2_get_coupon(
        self,
        coupon: int,
        *,
        with_trashed: Optional[bool] = None,
        only_trashed: Optional[bool] = None,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkV2GetCouponResponseValue200ApplicationJson:
        """Retrieve a coupon

        Retrieve a coupon by its ID. This v2 route is store-scoped. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            coupon: The coupon path parameter.
            with_trashed: Include soft-deleted resources in the result. Defaults to `false`.
            only_trashed: Return only soft-deleted resources. Ignored when with_trashed is true. Defaults to `false`.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkV2GetCouponResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "v2GetCoupon",
        }
        params: Dict[str, Any] = {
            k: v
            for k, v in {
                "with_trashed": with_trashed,
                "only_trashed": only_trashed,
            }.items()
            if v is not None
        }
        return await self._client.request(
            method="get",
            path=("v2", "coupons", str(coupon)),
            params=params,
            model=SdkV2GetCouponResponseValue200ApplicationJson,
            request_options=request_options,
        )

    async def v2_replace_coupon(
        self,
        coupon: int,
        *,
        code: Optional[str] = None,
        type: Optional[Union[SdkV2ReplaceCouponRequestApplicationJsonType, str]] = None,
        discount: Optional[Union[float, str]] = None,
        store_wide: Optional[bool] = None,
        products: Optional[List[int]] = None,
        product_variants: Optional[List[int]] = None,
        limit: Union[int, None, NotGiven] = NOT_GIVEN,
        expires_at: Union[str, None, NotGiven] = NOT_GIVEN,
        minimum_amount: Union[Union[float, str], None, NotGiven] = NOT_GIVEN,
        with_trashed: Optional[bool] = None,
        only_trashed: Optional[bool] = None,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkV2ReplaceCouponResponseValue200ApplicationJson:
        """Update a coupon

        Change a coupon's discount, limits, or eligible products. This v2 route is store-scoped. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            coupon: The coupon path parameter.
            code: The code.
            type: The type.
            discount: A decimal discount value of at least 1. Percentage discounts cannot exceed 100; amount discounts use the store currency's major unit.
            store_wide: The store wide.
            products: Product IDs the coupon applies to when store_wide is false.
            product_variants: Optional variant restrictions. Omit to preserve existing restrictions or send an empty array to allow every variant of the selected products.
            limit: The limit.
            expires_at: A future date and time, or null for no expiry.
            minimum_amount: A decimal minimum order amount in the store currency's major unit, or null for no minimum.
            with_trashed: Include soft-deleted resources in the result. Defaults to `false`.
            only_trashed: Return only soft-deleted resources. Ignored when with_trashed is true. Defaults to `false`.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkV2ReplaceCouponResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "v2ReplaceCoupon",
        }
        body: Dict[str, Any] = {
            k: v
            for k, v in {
                "code": code,
                "type": enum_value(type) if type is not None else None,
                "discount": discount,
                "store_wide": store_wide,
                "products": products,
                "product_variants": product_variants,
            }.items()
            if v is not None
        }
        if not isinstance(limit, NotGiven):
            body["limit"] = limit
        if not isinstance(expires_at, NotGiven):
            body["expires_at"] = expires_at
        if not isinstance(minimum_amount, NotGiven):
            body["minimum_amount"] = minimum_amount
        _validate_model(
            body,
            {
                "name": "SdkV2ReplaceCouponRequestApplicationJson",
                "required": [],
                "properties": {
                    "code": {
                        "kind": "primitive",
                        "type": "string",
                        "constraints": {"maxLength": 255},
                    },
                    "type": {"kind": "enum", "values": ["PERCENTAGE", "AMOUNT"]},
                    "discount": {
                        "kind": "union",
                        "variants": [
                            {
                                "kind": "primitive",
                                "type": "number",
                                "constraints": {"minimum": 1, "maxLength": 255},
                            },
                            {
                                "kind": "primitive",
                                "type": "string",
                                "constraints": {"minimum": 1, "maxLength": 255},
                            },
                        ],
                    },
                    "store_wide": {"kind": "primitive", "type": "boolean"},
                    "products": {
                        "kind": "array",
                        "items": {"kind": "primitive", "type": "integer"},
                        "constraints": {"minItems": 1},
                    },
                    "product_variants": {
                        "kind": "array",
                        "items": {"kind": "primitive", "type": "integer"},
                    },
                    "limit": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "integer",
                            "constraints": {"minimum": 1},
                        },
                    },
                    "expires_at": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "string",
                            "format": "date-time",
                        },
                    },
                    "minimum_amount": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "union",
                            "variants": [
                                {
                                    "kind": "primitive",
                                    "type": "number",
                                    "constraints": {"minimum": 1, "maxLength": 255},
                                },
                                {
                                    "kind": "primitive",
                                    "type": "string",
                                    "constraints": {"minimum": 1, "maxLength": 255},
                                },
                            ],
                        },
                    },
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        params: Dict[str, Any] = {
            k: v
            for k, v in {
                "with_trashed": with_trashed,
                "only_trashed": only_trashed,
            }.items()
            if v is not None
        }
        return await self._client.request(
            method="put",
            path=("v2", "coupons", str(coupon)),
            body=body,
            params=params,
            model=SdkV2ReplaceCouponResponseValue200ApplicationJson,
            request_options=request_options,
        )

    async def v2_update_coupon(
        self,
        coupon: int,
        *,
        code: Optional[str] = None,
        type: Optional[Union[SdkV2UpdateCouponRequestApplicationJsonType, str]] = None,
        discount: Optional[Union[float, str]] = None,
        store_wide: Optional[bool] = None,
        products: Optional[List[int]] = None,
        product_variants: Optional[List[int]] = None,
        limit: Union[int, None, NotGiven] = NOT_GIVEN,
        expires_at: Union[str, None, NotGiven] = NOT_GIVEN,
        minimum_amount: Union[Union[float, str], None, NotGiven] = NOT_GIVEN,
        with_trashed: Optional[bool] = None,
        only_trashed: Optional[bool] = None,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkV2UpdateCouponResponseValue200ApplicationJson:
        """Update a coupon

        Change a coupon's discount, limits, or eligible products. This v2 route is store-scoped. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            coupon: The coupon path parameter.
            code: The code.
            type: The type.
            discount: A decimal discount value of at least 1. Percentage discounts cannot exceed 100; amount discounts use the store currency's major unit.
            store_wide: The store wide.
            products: Product IDs the coupon applies to when store_wide is false.
            product_variants: Optional variant restrictions. Omit to preserve existing restrictions or send an empty array to allow every variant of the selected products.
            limit: The limit.
            expires_at: A future date and time, or null for no expiry.
            minimum_amount: A decimal minimum order amount in the store currency's major unit, or null for no minimum.
            with_trashed: Include soft-deleted resources in the result. Defaults to `false`.
            only_trashed: Return only soft-deleted resources. Ignored when with_trashed is true. Defaults to `false`.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkV2UpdateCouponResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "v2UpdateCoupon",
        }
        body: Dict[str, Any] = {
            k: v
            for k, v in {
                "code": code,
                "type": enum_value(type) if type is not None else None,
                "discount": discount,
                "store_wide": store_wide,
                "products": products,
                "product_variants": product_variants,
            }.items()
            if v is not None
        }
        if not isinstance(limit, NotGiven):
            body["limit"] = limit
        if not isinstance(expires_at, NotGiven):
            body["expires_at"] = expires_at
        if not isinstance(minimum_amount, NotGiven):
            body["minimum_amount"] = minimum_amount
        _validate_model(
            body,
            {
                "name": "SdkV2UpdateCouponRequestApplicationJson",
                "required": [],
                "properties": {
                    "code": {
                        "kind": "primitive",
                        "type": "string",
                        "constraints": {"maxLength": 255},
                    },
                    "type": {"kind": "enum", "values": ["PERCENTAGE", "AMOUNT"]},
                    "discount": {
                        "kind": "union",
                        "variants": [
                            {
                                "kind": "primitive",
                                "type": "number",
                                "constraints": {"minimum": 1, "maxLength": 255},
                            },
                            {
                                "kind": "primitive",
                                "type": "string",
                                "constraints": {"minimum": 1, "maxLength": 255},
                            },
                        ],
                    },
                    "store_wide": {"kind": "primitive", "type": "boolean"},
                    "products": {
                        "kind": "array",
                        "items": {"kind": "primitive", "type": "integer"},
                        "constraints": {"minItems": 1},
                    },
                    "product_variants": {
                        "kind": "array",
                        "items": {"kind": "primitive", "type": "integer"},
                    },
                    "limit": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "integer",
                            "constraints": {"minimum": 1},
                        },
                    },
                    "expires_at": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "string",
                            "format": "date-time",
                        },
                    },
                    "minimum_amount": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "union",
                            "variants": [
                                {
                                    "kind": "primitive",
                                    "type": "number",
                                    "constraints": {"minimum": 1, "maxLength": 255},
                                },
                                {
                                    "kind": "primitive",
                                    "type": "string",
                                    "constraints": {"minimum": 1, "maxLength": 255},
                                },
                            ],
                        },
                    },
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        params: Dict[str, Any] = {
            k: v
            for k, v in {
                "with_trashed": with_trashed,
                "only_trashed": only_trashed,
            }.items()
            if v is not None
        }
        return await self._client.request(
            method="patch",
            path=("v2", "coupons", str(coupon)),
            body=body,
            params=params,
            model=SdkV2UpdateCouponResponseValue200ApplicationJson,
            request_options=request_options,
        )

    async def v2_delete_coupon(
        self,
        coupon: int,
        *,
        with_trashed: Optional[bool] = None,
        only_trashed: Optional[bool] = None,
        request_options: Optional[RequestOptions] = None,
    ) -> None:
        """Delete a coupon

        Soft-delete a coupon. The returned coupon includes its deleted_at timestamp; use with_trashed or only_trashed to retrieve soft-deleted coupons later. This v2 route is store-scoped. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            coupon: The coupon path parameter.
            with_trashed: Include soft-deleted resources in the result. Defaults to `false`.
            only_trashed: Return only soft-deleted resources. Ignored when with_trashed is true. Defaults to `false`.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "v2DeleteCoupon",
        }
        params: Dict[str, Any] = {
            k: v
            for k, v in {
                "with_trashed": with_trashed,
                "only_trashed": only_trashed,
            }.items()
            if v is not None
        }
        await self._client.request(
            method="delete",
            path=("v2", "coupons", str(coupon)),
            params=params,
            request_options=request_options,
        )

    async def v2_batch_create_coupons(
        self,
        *,
        resources: List[
            V2BatchCreateCouponsRequestApplicationJsonPropertyResourcesItem
        ],
        idempotency_key: Optional[str] = None,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkV2BatchCreateCouponsResponseValue200ApplicationJson:
        """Batch create coupons

        Create multiple coupons in one request by sending a `resources` array. This v2 route is store-scoped. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            resources: The resources.
            idempotency_key: Optional idempotency key for safe retries.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkV2BatchCreateCouponsResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": True,
            "operation_id": "v2BatchCreateCoupons",
        }
        body: Dict[str, Any] = {
            "resources": [item.to_dict() for item in resources],
        }
        _validate_model(
            body,
            {
                "name": "SdkV2BatchCreateCouponsRequestApplicationJson",
                "required": ["resources"],
                "properties": {
                    "resources": {"kind": "array", "items": {"kind": "any"}}
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return await self._client.request(
            method="post",
            path=("v2", "coupons", "batch"),
            body=body,
            model=SdkV2BatchCreateCouponsResponseValue200ApplicationJson,
            idempotency_key=idempotency_key,
            request_options=request_options,
        )

    async def v2_batch_update_coupons(
        self,
        *,
        resources: V2BatchUpdateCouponsRequestApplicationJsonPropertyResources,
        with_trashed: Optional[bool] = None,
        only_trashed: Optional[bool] = None,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkV2BatchUpdateCouponsResponseValue200ApplicationJson:
        """Batch update coupons

        Update multiple coupons in one request by sending a `resources` object keyed by coupon ID. This v2 route is store-scoped. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            resources: The resources.
            with_trashed: Include soft-deleted resources in the result. Defaults to `false`.
            only_trashed: Return only soft-deleted resources. Ignored when with_trashed is true. Defaults to `false`.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkV2BatchUpdateCouponsResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": True,
            "operation_id": "v2BatchUpdateCoupons",
        }
        body: Dict[str, Any] = {
            "resources": resources.to_dict(),
        }
        _validate_model(
            body,
            {
                "name": "SdkV2BatchUpdateCouponsRequestApplicationJson",
                "required": ["resources"],
                "properties": {"resources": {"kind": "any"}},
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        params: Dict[str, Any] = {
            k: v
            for k, v in {
                "with_trashed": with_trashed,
                "only_trashed": only_trashed,
            }.items()
            if v is not None
        }
        return await self._client.request(
            method="patch",
            path=("v2", "coupons", "batch"),
            body=body,
            params=params,
            model=SdkV2BatchUpdateCouponsResponseValue200ApplicationJson,
            request_options=request_options,
        )

    async def v2_batch_delete_coupons(
        self,
        *,
        resources: List[int],
        with_trashed: Optional[bool] = None,
        only_trashed: Optional[bool] = None,
        request_options: Optional[RequestOptions] = None,
    ) -> None:
        """Batch delete coupons

        Soft-delete multiple coupons by sending their IDs in `resources`. This v2 route is store-scoped. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            resources: The resources.
            with_trashed: Include soft-deleted resources in the result. Defaults to `false`.
            only_trashed: Return only soft-deleted resources. Ignored when with_trashed is true. Defaults to `false`.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "v2BatchDeleteCoupons",
        }
        body: Dict[str, Any] = {
            "resources": resources,
        }
        _validate_model(
            body,
            {
                "name": "SdkV2BatchDeleteCouponsRequestApplicationJson",
                "required": ["resources"],
                "properties": {
                    "resources": {
                        "kind": "array",
                        "items": {"kind": "primitive", "type": "integer"},
                    }
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        params: Dict[str, Any] = {
            k: v
            for k, v in {
                "with_trashed": with_trashed,
                "only_trashed": only_trashed,
            }.items()
            if v is not None
        }
        await self._client.request(
            method="delete",
            path=("v2", "coupons", "batch"),
            body=body,
            params=params,
            request_options=request_options,
        )
