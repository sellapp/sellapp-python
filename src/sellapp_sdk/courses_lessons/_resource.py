# This file is auto-generated by oagen. Do not edit.

from __future__ import annotations

from typing import TYPE_CHECKING, Any, Dict, List, Optional, Union

from .._base_client import WithRawResponse

if TYPE_CHECKING:
    from .._client import AsyncSellAppClient, SellAppClient

from sellapp_sdk.common.models.sdk_create_course_lesson_request_application_json_type import (
    SdkCreateCourseLessonRequestApplicationJsonType,
)
from sellapp_sdk.common.models.sdk_replace_course_lesson_request_application_json_type import (
    SdkReplaceCourseLessonRequestApplicationJsonType,
)
from sellapp_sdk.common.models.sdk_update_course_lesson_request_application_json_type import (
    SdkUpdateCourseLessonRequestApplicationJsonType,
)
from sellapp_sdk.courses.models.create_course_lesson_request_application_json_property_assignment import (
    CreateCourseLessonRequestApplicationJsonPropertyAssignment,
)
from sellapp_sdk.courses.models.create_course_lesson_request_application_json_property_questions_item import (
    CreateCourseLessonRequestApplicationJsonPropertyQuestionsItem,
)
from sellapp_sdk.courses.models.reorder_course_lessons_request_application_json_property_resources_item import (
    ReorderCourseLessonsRequestApplicationJsonPropertyResourcesItem,
)
from sellapp_sdk.courses.models.replace_course_lesson_request_application_json_property_assignment import (
    ReplaceCourseLessonRequestApplicationJsonPropertyAssignment,
)
from sellapp_sdk.courses.models.replace_course_lesson_request_application_json_property_questions_item import (
    ReplaceCourseLessonRequestApplicationJsonPropertyQuestionsItem,
)
from sellapp_sdk.courses.models.sdk_create_course_lesson_response_value_201_application_json import (
    SdkCreateCourseLessonResponseValue201ApplicationJson,
)
from sellapp_sdk.courses.models.sdk_reorder_course_lessons_response_value_200_application_json import (
    SdkReorderCourseLessonsResponseValue200ApplicationJson,
)
from sellapp_sdk.courses.models.sdk_replace_course_lesson_response_value_200_application_json import (
    SdkReplaceCourseLessonResponseValue200ApplicationJson,
)
from sellapp_sdk.courses.models.sdk_update_course_lesson_response_value_200_application_json import (
    SdkUpdateCourseLessonResponseValue200ApplicationJson,
)
from sellapp_sdk.courses.models.update_course_lesson_request_application_json_property_assignment import (
    UpdateCourseLessonRequestApplicationJsonPropertyAssignment,
)
from sellapp_sdk.courses.models.update_course_lesson_request_application_json_property_questions_item import (
    UpdateCourseLessonRequestApplicationJsonPropertyQuestionsItem,
)

from .._types import NOT_GIVEN, NotGiven, RequestOptions, _validate_model, enum_value


class CoursesLessons:
    """Courses Lessons API resources."""

    def __init__(self, client: SellAppClient) -> None:
        self._client = client
        self.with_raw_response = WithRawResponse(self)

    def create(
        self,
        course: str,
        section: int,
        *,
        title: str,
        type: Union[SdkCreateCourseLessonRequestApplicationJsonType, str],
        content: Union[str, None, NotGiven] = NOT_GIVEN,
        is_preview: Optional[bool] = None,
        is_published: Optional[bool] = None,
        assignment: Optional[
            CreateCourseLessonRequestApplicationJsonPropertyAssignment
        ] = None,
        questions: Optional[
            List[CreateCourseLessonRequestApplicationJsonPropertyQuestionsItem]
        ] = None,
        expected_updated_at: Optional[str] = None,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkCreateCourseLessonResponseValue201ApplicationJson:
        """Create a course lesson

        Append a lesson, quiz, or assignment to a section. Rich text is sanitized before storage. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            course: The course path parameter.
            section: The section path parameter.
            title: The title.
            type: The type.
            content: The content.
            is_preview: Preview is available only for video lessons.
            is_published: The is published.
            assignment: The assignment.
            questions: The questions.
            expected_updated_at: The expected updated at.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkCreateCourseLessonResponseValue201ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "createCourseLesson",
        }
        body: Dict[str, Any] = {
            k: v
            for k, v in {
                "title": title,
                "type": enum_value(type),
                "is_preview": is_preview,
                "is_published": is_published,
                "assignment": assignment.to_dict() if assignment is not None else None,
                "questions": [item.to_dict() for item in questions]
                if questions is not None
                else None,
                "expected_updated_at": expected_updated_at,
            }.items()
            if v is not None
        }
        if not isinstance(content, NotGiven):
            body["content"] = content
        _validate_model(
            body,
            {
                "name": "SdkCreateCourseLessonRequestApplicationJson",
                "required": ["title", "type"],
                "properties": {
                    "title": {
                        "kind": "primitive",
                        "type": "string",
                        "constraints": {"maxLength": 255},
                    },
                    "type": {
                        "kind": "enum",
                        "values": ["lecture", "video", "text", "quiz", "assignment"],
                    },
                    "content": {
                        "kind": "nullable",
                        "inner": {"kind": "primitive", "type": "string"},
                    },
                    "is_preview": {"kind": "primitive", "type": "boolean"},
                    "is_published": {"kind": "primitive", "type": "boolean"},
                    "assignment": {"kind": "any"},
                    "questions": {
                        "kind": "array",
                        "items": {"kind": "any"},
                        "constraints": {"maxItems": 12},
                    },
                    "expected_updated_at": {
                        "kind": "primitive",
                        "type": "string",
                        "format": "date-time",
                    },
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return self._client.request(
            method="post",
            path=("v2", "courses", str(course), "sections", str(section), "lessons"),
            body=body,
            model=SdkCreateCourseLessonResponseValue201ApplicationJson,
            request_options=request_options,
        )

    def replace(
        self,
        course: str,
        lesson: int,
        *,
        title: Optional[str] = None,
        type: Optional[
            Union[SdkReplaceCourseLessonRequestApplicationJsonType, str]
        ] = None,
        content: Union[str, None, NotGiven] = NOT_GIVEN,
        is_preview: Optional[bool] = None,
        is_published: Optional[bool] = None,
        assignment: Optional[
            ReplaceCourseLessonRequestApplicationJsonPropertyAssignment
        ] = None,
        questions: Optional[
            List[ReplaceCourseLessonRequestApplicationJsonPropertyQuestionsItem]
        ] = None,
        expected_updated_at: Optional[str] = None,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkReplaceCourseLessonResponseValue200ApplicationJson:
        """Update a course lesson

        Update lesson content, draft state, preview state, or replace the structured quiz or assignment body. Item type is immutable except while choosing video or text for a new lecture placeholder. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            course: The course path parameter.
            lesson: The lesson path parameter.
            title: The title.
            type: The type.
            content: The content.
            is_preview: Preview is available only for video lessons.
            is_published: The is published.
            assignment: The assignment.
            questions: The questions.
            expected_updated_at: The expected updated at.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkReplaceCourseLessonResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "replaceCourseLesson",
        }
        body: Dict[str, Any] = {
            k: v
            for k, v in {
                "title": title,
                "type": enum_value(type) if type is not None else None,
                "is_preview": is_preview,
                "is_published": is_published,
                "assignment": assignment.to_dict() if assignment is not None else None,
                "questions": [item.to_dict() for item in questions]
                if questions is not None
                else None,
                "expected_updated_at": expected_updated_at,
            }.items()
            if v is not None
        }
        if not isinstance(content, NotGiven):
            body["content"] = content
        _validate_model(
            body,
            {
                "name": "SdkReplaceCourseLessonRequestApplicationJson",
                "required": [],
                "properties": {
                    "title": {
                        "kind": "primitive",
                        "type": "string",
                        "constraints": {"maxLength": 255},
                    },
                    "type": {
                        "kind": "enum",
                        "values": ["lecture", "video", "text", "quiz", "assignment"],
                    },
                    "content": {
                        "kind": "nullable",
                        "inner": {"kind": "primitive", "type": "string"},
                    },
                    "is_preview": {"kind": "primitive", "type": "boolean"},
                    "is_published": {"kind": "primitive", "type": "boolean"},
                    "assignment": {"kind": "any"},
                    "questions": {
                        "kind": "array",
                        "items": {"kind": "any"},
                        "constraints": {"maxItems": 12},
                    },
                    "expected_updated_at": {
                        "kind": "primitive",
                        "type": "string",
                        "format": "date-time",
                    },
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return self._client.request(
            method="put",
            path=("v2", "courses", str(course), "lessons", str(lesson)),
            body=body,
            model=SdkReplaceCourseLessonResponseValue200ApplicationJson,
            request_options=request_options,
        )

    def update(
        self,
        course: str,
        lesson: int,
        *,
        title: Optional[str] = None,
        type: Optional[
            Union[SdkUpdateCourseLessonRequestApplicationJsonType, str]
        ] = None,
        content: Union[str, None, NotGiven] = NOT_GIVEN,
        is_preview: Optional[bool] = None,
        is_published: Optional[bool] = None,
        assignment: Optional[
            UpdateCourseLessonRequestApplicationJsonPropertyAssignment
        ] = None,
        questions: Optional[
            List[UpdateCourseLessonRequestApplicationJsonPropertyQuestionsItem]
        ] = None,
        expected_updated_at: Optional[str] = None,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkUpdateCourseLessonResponseValue200ApplicationJson:
        """Update a course lesson

        Update lesson content, draft state, preview state, or replace the structured quiz or assignment body. Item type is immutable except while choosing video or text for a new lecture placeholder. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            course: The course path parameter.
            lesson: The lesson path parameter.
            title: The title.
            type: The type.
            content: The content.
            is_preview: Preview is available only for video lessons.
            is_published: The is published.
            assignment: The assignment.
            questions: The questions.
            expected_updated_at: The expected updated at.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkUpdateCourseLessonResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "updateCourseLesson",
        }
        body: Dict[str, Any] = {
            k: v
            for k, v in {
                "title": title,
                "type": enum_value(type) if type is not None else None,
                "is_preview": is_preview,
                "is_published": is_published,
                "assignment": assignment.to_dict() if assignment is not None else None,
                "questions": [item.to_dict() for item in questions]
                if questions is not None
                else None,
                "expected_updated_at": expected_updated_at,
            }.items()
            if v is not None
        }
        if not isinstance(content, NotGiven):
            body["content"] = content
        _validate_model(
            body,
            {
                "name": "SdkUpdateCourseLessonRequestApplicationJson",
                "required": [],
                "properties": {
                    "title": {
                        "kind": "primitive",
                        "type": "string",
                        "constraints": {"maxLength": 255},
                    },
                    "type": {
                        "kind": "enum",
                        "values": ["lecture", "video", "text", "quiz", "assignment"],
                    },
                    "content": {
                        "kind": "nullable",
                        "inner": {"kind": "primitive", "type": "string"},
                    },
                    "is_preview": {"kind": "primitive", "type": "boolean"},
                    "is_published": {"kind": "primitive", "type": "boolean"},
                    "assignment": {"kind": "any"},
                    "questions": {
                        "kind": "array",
                        "items": {"kind": "any"},
                        "constraints": {"maxItems": 12},
                    },
                    "expected_updated_at": {
                        "kind": "primitive",
                        "type": "string",
                        "format": "date-time",
                    },
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return self._client.request(
            method="patch",
            path=("v2", "courses", str(course), "lessons", str(lesson)),
            body=body,
            model=SdkUpdateCourseLessonResponseValue200ApplicationJson,
            request_options=request_options,
        )

    def delete(
        self,
        course: str,
        lesson: int,
        *,
        request_options: Optional[RequestOptions] = None,
    ) -> None:
        """Delete a course lesson

        Delete a lesson and its structured questions, assignment, attachments, and video reference, then normalize lesson order. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            course: The course path parameter.
            lesson: The lesson path parameter.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "deleteCourseLesson",
        }
        self._client.request(
            method="delete",
            path=("v2", "courses", str(course), "lessons", str(lesson)),
            request_options=request_options,
        )

    def reorder(
        self,
        course: str,
        *,
        resources: List[
            ReorderCourseLessonsRequestApplicationJsonPropertyResourcesItem
        ],
        request_options: Optional[RequestOptions] = None,
    ) -> SdkReorderCourseLessonsResponseValue200ApplicationJson:
        """Reorder course lessons

        Replace the complete lesson order and section placement. Send every lesson ID exactly once with a destination section from the same course. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            course: The course path parameter.
            resources: The resources.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkReorderCourseLessonsResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "reorderCourseLessons",
        }
        body: Dict[str, Any] = {
            "resources": [item.to_dict() for item in resources],
        }
        _validate_model(
            body,
            {
                "name": "SdkReorderCourseLessonsRequestApplicationJson",
                "required": ["resources"],
                "properties": {
                    "resources": {"kind": "array", "items": {"kind": "any"}}
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return self._client.request(
            method="patch",
            path=("v2", "courses", str(course), "lessons", "reorder"),
            body=body,
            model=SdkReorderCourseLessonsResponseValue200ApplicationJson,
            request_options=request_options,
        )


class AsyncCoursesLessons:
    """Courses Lessons API resources (async)."""

    def __init__(self, client: AsyncSellAppClient) -> None:
        self._client = client
        self.with_raw_response = WithRawResponse(self)

    async def create(
        self,
        course: str,
        section: int,
        *,
        title: str,
        type: Union[SdkCreateCourseLessonRequestApplicationJsonType, str],
        content: Union[str, None, NotGiven] = NOT_GIVEN,
        is_preview: Optional[bool] = None,
        is_published: Optional[bool] = None,
        assignment: Optional[
            CreateCourseLessonRequestApplicationJsonPropertyAssignment
        ] = None,
        questions: Optional[
            List[CreateCourseLessonRequestApplicationJsonPropertyQuestionsItem]
        ] = None,
        expected_updated_at: Optional[str] = None,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkCreateCourseLessonResponseValue201ApplicationJson:
        """Create a course lesson

        Append a lesson, quiz, or assignment to a section. Rich text is sanitized before storage. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            course: The course path parameter.
            section: The section path parameter.
            title: The title.
            type: The type.
            content: The content.
            is_preview: Preview is available only for video lessons.
            is_published: The is published.
            assignment: The assignment.
            questions: The questions.
            expected_updated_at: The expected updated at.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkCreateCourseLessonResponseValue201ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "createCourseLesson",
        }
        body: Dict[str, Any] = {
            k: v
            for k, v in {
                "title": title,
                "type": enum_value(type),
                "is_preview": is_preview,
                "is_published": is_published,
                "assignment": assignment.to_dict() if assignment is not None else None,
                "questions": [item.to_dict() for item in questions]
                if questions is not None
                else None,
                "expected_updated_at": expected_updated_at,
            }.items()
            if v is not None
        }
        if not isinstance(content, NotGiven):
            body["content"] = content
        _validate_model(
            body,
            {
                "name": "SdkCreateCourseLessonRequestApplicationJson",
                "required": ["title", "type"],
                "properties": {
                    "title": {
                        "kind": "primitive",
                        "type": "string",
                        "constraints": {"maxLength": 255},
                    },
                    "type": {
                        "kind": "enum",
                        "values": ["lecture", "video", "text", "quiz", "assignment"],
                    },
                    "content": {
                        "kind": "nullable",
                        "inner": {"kind": "primitive", "type": "string"},
                    },
                    "is_preview": {"kind": "primitive", "type": "boolean"},
                    "is_published": {"kind": "primitive", "type": "boolean"},
                    "assignment": {"kind": "any"},
                    "questions": {
                        "kind": "array",
                        "items": {"kind": "any"},
                        "constraints": {"maxItems": 12},
                    },
                    "expected_updated_at": {
                        "kind": "primitive",
                        "type": "string",
                        "format": "date-time",
                    },
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return await self._client.request(
            method="post",
            path=("v2", "courses", str(course), "sections", str(section), "lessons"),
            body=body,
            model=SdkCreateCourseLessonResponseValue201ApplicationJson,
            request_options=request_options,
        )

    async def replace(
        self,
        course: str,
        lesson: int,
        *,
        title: Optional[str] = None,
        type: Optional[
            Union[SdkReplaceCourseLessonRequestApplicationJsonType, str]
        ] = None,
        content: Union[str, None, NotGiven] = NOT_GIVEN,
        is_preview: Optional[bool] = None,
        is_published: Optional[bool] = None,
        assignment: Optional[
            ReplaceCourseLessonRequestApplicationJsonPropertyAssignment
        ] = None,
        questions: Optional[
            List[ReplaceCourseLessonRequestApplicationJsonPropertyQuestionsItem]
        ] = None,
        expected_updated_at: Optional[str] = None,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkReplaceCourseLessonResponseValue200ApplicationJson:
        """Update a course lesson

        Update lesson content, draft state, preview state, or replace the structured quiz or assignment body. Item type is immutable except while choosing video or text for a new lecture placeholder. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            course: The course path parameter.
            lesson: The lesson path parameter.
            title: The title.
            type: The type.
            content: The content.
            is_preview: Preview is available only for video lessons.
            is_published: The is published.
            assignment: The assignment.
            questions: The questions.
            expected_updated_at: The expected updated at.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkReplaceCourseLessonResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "replaceCourseLesson",
        }
        body: Dict[str, Any] = {
            k: v
            for k, v in {
                "title": title,
                "type": enum_value(type) if type is not None else None,
                "is_preview": is_preview,
                "is_published": is_published,
                "assignment": assignment.to_dict() if assignment is not None else None,
                "questions": [item.to_dict() for item in questions]
                if questions is not None
                else None,
                "expected_updated_at": expected_updated_at,
            }.items()
            if v is not None
        }
        if not isinstance(content, NotGiven):
            body["content"] = content
        _validate_model(
            body,
            {
                "name": "SdkReplaceCourseLessonRequestApplicationJson",
                "required": [],
                "properties": {
                    "title": {
                        "kind": "primitive",
                        "type": "string",
                        "constraints": {"maxLength": 255},
                    },
                    "type": {
                        "kind": "enum",
                        "values": ["lecture", "video", "text", "quiz", "assignment"],
                    },
                    "content": {
                        "kind": "nullable",
                        "inner": {"kind": "primitive", "type": "string"},
                    },
                    "is_preview": {"kind": "primitive", "type": "boolean"},
                    "is_published": {"kind": "primitive", "type": "boolean"},
                    "assignment": {"kind": "any"},
                    "questions": {
                        "kind": "array",
                        "items": {"kind": "any"},
                        "constraints": {"maxItems": 12},
                    },
                    "expected_updated_at": {
                        "kind": "primitive",
                        "type": "string",
                        "format": "date-time",
                    },
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return await self._client.request(
            method="put",
            path=("v2", "courses", str(course), "lessons", str(lesson)),
            body=body,
            model=SdkReplaceCourseLessonResponseValue200ApplicationJson,
            request_options=request_options,
        )

    async def update(
        self,
        course: str,
        lesson: int,
        *,
        title: Optional[str] = None,
        type: Optional[
            Union[SdkUpdateCourseLessonRequestApplicationJsonType, str]
        ] = None,
        content: Union[str, None, NotGiven] = NOT_GIVEN,
        is_preview: Optional[bool] = None,
        is_published: Optional[bool] = None,
        assignment: Optional[
            UpdateCourseLessonRequestApplicationJsonPropertyAssignment
        ] = None,
        questions: Optional[
            List[UpdateCourseLessonRequestApplicationJsonPropertyQuestionsItem]
        ] = None,
        expected_updated_at: Optional[str] = None,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkUpdateCourseLessonResponseValue200ApplicationJson:
        """Update a course lesson

        Update lesson content, draft state, preview state, or replace the structured quiz or assignment body. Item type is immutable except while choosing video or text for a new lecture placeholder. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            course: The course path parameter.
            lesson: The lesson path parameter.
            title: The title.
            type: The type.
            content: The content.
            is_preview: Preview is available only for video lessons.
            is_published: The is published.
            assignment: The assignment.
            questions: The questions.
            expected_updated_at: The expected updated at.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkUpdateCourseLessonResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "updateCourseLesson",
        }
        body: Dict[str, Any] = {
            k: v
            for k, v in {
                "title": title,
                "type": enum_value(type) if type is not None else None,
                "is_preview": is_preview,
                "is_published": is_published,
                "assignment": assignment.to_dict() if assignment is not None else None,
                "questions": [item.to_dict() for item in questions]
                if questions is not None
                else None,
                "expected_updated_at": expected_updated_at,
            }.items()
            if v is not None
        }
        if not isinstance(content, NotGiven):
            body["content"] = content
        _validate_model(
            body,
            {
                "name": "SdkUpdateCourseLessonRequestApplicationJson",
                "required": [],
                "properties": {
                    "title": {
                        "kind": "primitive",
                        "type": "string",
                        "constraints": {"maxLength": 255},
                    },
                    "type": {
                        "kind": "enum",
                        "values": ["lecture", "video", "text", "quiz", "assignment"],
                    },
                    "content": {
                        "kind": "nullable",
                        "inner": {"kind": "primitive", "type": "string"},
                    },
                    "is_preview": {"kind": "primitive", "type": "boolean"},
                    "is_published": {"kind": "primitive", "type": "boolean"},
                    "assignment": {"kind": "any"},
                    "questions": {
                        "kind": "array",
                        "items": {"kind": "any"},
                        "constraints": {"maxItems": 12},
                    },
                    "expected_updated_at": {
                        "kind": "primitive",
                        "type": "string",
                        "format": "date-time",
                    },
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return await self._client.request(
            method="patch",
            path=("v2", "courses", str(course), "lessons", str(lesson)),
            body=body,
            model=SdkUpdateCourseLessonResponseValue200ApplicationJson,
            request_options=request_options,
        )

    async def delete(
        self,
        course: str,
        lesson: int,
        *,
        request_options: Optional[RequestOptions] = None,
    ) -> None:
        """Delete a course lesson

        Delete a lesson and its structured questions, assignment, attachments, and video reference, then normalize lesson order. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            course: The course path parameter.
            lesson: The lesson path parameter.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "deleteCourseLesson",
        }
        await self._client.request(
            method="delete",
            path=("v2", "courses", str(course), "lessons", str(lesson)),
            request_options=request_options,
        )

    async def reorder(
        self,
        course: str,
        *,
        resources: List[
            ReorderCourseLessonsRequestApplicationJsonPropertyResourcesItem
        ],
        request_options: Optional[RequestOptions] = None,
    ) -> SdkReorderCourseLessonsResponseValue200ApplicationJson:
        """Reorder course lessons

        Replace the complete lesson order and section placement. Send every lesson ID exactly once with a destination section from the same course. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            course: The course path parameter.
            resources: The resources.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkReorderCourseLessonsResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "reorderCourseLessons",
        }
        body: Dict[str, Any] = {
            "resources": [item.to_dict() for item in resources],
        }
        _validate_model(
            body,
            {
                "name": "SdkReorderCourseLessonsRequestApplicationJson",
                "required": ["resources"],
                "properties": {
                    "resources": {"kind": "array", "items": {"kind": "any"}}
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return await self._client.request(
            method="patch",
            path=("v2", "courses", str(course), "lessons", "reorder"),
            body=body,
            model=SdkReorderCourseLessonsResponseValue200ApplicationJson,
            request_options=request_options,
        )
