# This file is auto-generated by oagen. Do not edit.

from __future__ import annotations

from typing import TYPE_CHECKING, Any

from .._base_client import WithRawResponse

if TYPE_CHECKING:
    from .._client import AsyncSellAppClient, SellAppClient

from sellapp_sdk.courses.models.sdk_create_course_section_response_value_201_application_json import (
    SdkCreateCourseSectionResponseValue201ApplicationJson,
)
from sellapp_sdk.courses.models.sdk_reorder_course_sections_response_value_200_application_json import (
    SdkReorderCourseSectionsResponseValue200ApplicationJson,
)
from sellapp_sdk.courses.models.sdk_replace_course_section_response_value_200_application_json import (
    SdkReplaceCourseSectionResponseValue200ApplicationJson,
)
from sellapp_sdk.courses.models.sdk_update_course_section_response_value_200_application_json import (
    SdkUpdateCourseSectionResponseValue200ApplicationJson,
)

from .._types import NOT_GIVEN, NotGiven, RequestOptions, _validate_model


class CoursesSections:
    """Courses Sections API resources."""

    def __init__(self, client: SellAppClient) -> None:
        self._client = client
        self.with_raw_response = WithRawResponse(self)

    def create(
        self,
        course: str,
        *,
        title: str,
        description: str | None | NotGiven = NOT_GIVEN,
        request_options: RequestOptions | None = None,
    ) -> SdkCreateCourseSectionResponseValue201ApplicationJson:
        """Create a course section

        Append a section to a course curriculum. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            course: The course path parameter.
            title: The title.
            description: The description.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkCreateCourseSectionResponseValue201ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "createCourseSection",
        }
        body: dict[str, Any] = {
            "title": title,
        }
        if not isinstance(description, NotGiven):
            body["description"] = description
        _validate_model(
            body,
            {
                "name": "SdkCreateCourseSectionRequestApplicationJson",
                "required": ["title"],
                "properties": {
                    "title": {
                        "kind": "primitive",
                        "type": "string",
                        "constraints": {"maxLength": 255},
                    },
                    "description": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "string",
                            "constraints": {"maxLength": 65535},
                        },
                    },
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return self._client.request(
            method="post",
            path=("v2", "courses", str(course), "sections"),
            body=body,
            model=SdkCreateCourseSectionResponseValue201ApplicationJson,
            request_options=request_options,
        )

    def replace(
        self,
        course: str,
        section: int,
        *,
        title: str | None = None,
        description: str | None | NotGiven = NOT_GIVEN,
        expected_updated_at: str | None = None,
        request_options: RequestOptions | None = None,
    ) -> SdkReplaceCourseSectionResponseValue200ApplicationJson:
        """Update a course section

        Update a section title or sanitized rich-text description. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            course: The course path parameter.
            section: The section path parameter.
            title: The title.
            description: The description.
            expected_updated_at: The expected updated at.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkReplaceCourseSectionResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "replaceCourseSection",
        }
        body: dict[str, Any] = {
            k: v
            for k, v in {
                "title": title,
                "expected_updated_at": expected_updated_at,
            }.items()
            if v is not None
        }
        if not isinstance(description, NotGiven):
            body["description"] = description
        _validate_model(
            body,
            {
                "name": "SdkReplaceCourseSectionRequestApplicationJson",
                "required": [],
                "properties": {
                    "title": {
                        "kind": "primitive",
                        "type": "string",
                        "constraints": {"maxLength": 255},
                    },
                    "description": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "string",
                            "constraints": {"maxLength": 65535},
                        },
                    },
                    "expected_updated_at": {
                        "kind": "primitive",
                        "type": "string",
                        "format": "date-time",
                    },
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return self._client.request(
            method="put",
            path=("v2", "courses", str(course), "sections", str(section)),
            body=body,
            model=SdkReplaceCourseSectionResponseValue200ApplicationJson,
            request_options=request_options,
        )

    def update(
        self,
        course: str,
        section: int,
        *,
        title: str | None = None,
        description: str | None | NotGiven = NOT_GIVEN,
        expected_updated_at: str | None = None,
        request_options: RequestOptions | None = None,
    ) -> SdkUpdateCourseSectionResponseValue200ApplicationJson:
        """Update a course section

        Update a section title or sanitized rich-text description. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            course: The course path parameter.
            section: The section path parameter.
            title: The title.
            description: The description.
            expected_updated_at: The expected updated at.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkUpdateCourseSectionResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "updateCourseSection",
        }
        body: dict[str, Any] = {
            k: v
            for k, v in {
                "title": title,
                "expected_updated_at": expected_updated_at,
            }.items()
            if v is not None
        }
        if not isinstance(description, NotGiven):
            body["description"] = description
        _validate_model(
            body,
            {
                "name": "SdkUpdateCourseSectionRequestApplicationJson",
                "required": [],
                "properties": {
                    "title": {
                        "kind": "primitive",
                        "type": "string",
                        "constraints": {"maxLength": 255},
                    },
                    "description": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "string",
                            "constraints": {"maxLength": 65535},
                        },
                    },
                    "expected_updated_at": {
                        "kind": "primitive",
                        "type": "string",
                        "format": "date-time",
                    },
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return self._client.request(
            method="patch",
            path=("v2", "courses", str(course), "sections", str(section)),
            body=body,
            model=SdkUpdateCourseSectionResponseValue200ApplicationJson,
            request_options=request_options,
        )

    def delete(
        self,
        course: str,
        section: int,
        *,
        request_options: RequestOptions | None = None,
    ) -> None:
        """Delete a course section

        Delete a section, its lessons, structured questions, attachments, and video references, then normalize remaining section order. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            course: The course path parameter.
            section: The section path parameter.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "deleteCourseSection",
        }
        self._client.request(
            method="delete",
            path=("v2", "courses", str(course), "sections", str(section)),
            request_options=request_options,
        )

    def reorder(
        self,
        course: str,
        *,
        resources: list[int],
        request_options: RequestOptions | None = None,
    ) -> SdkReorderCourseSectionsResponseValue200ApplicationJson:
        """Reorder course sections

        Replace the complete section order. Send every section ID exactly once in the desired order. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            course: The course path parameter.
            resources: The resources.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkReorderCourseSectionsResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "reorderCourseSections",
        }
        body: dict[str, Any] = {
            "resources": resources,
        }
        _validate_model(
            body,
            {
                "name": "SdkReorderCourseSectionsRequestApplicationJson",
                "required": ["resources"],
                "properties": {
                    "resources": {
                        "kind": "array",
                        "items": {
                            "kind": "primitive",
                            "type": "integer",
                            "constraints": {"minimum": 1},
                        },
                        "constraints": {"uniqueItems": True},
                    }
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return self._client.request(
            method="patch",
            path=("v2", "courses", str(course), "sections", "reorder"),
            body=body,
            model=SdkReorderCourseSectionsResponseValue200ApplicationJson,
            request_options=request_options,
        )


class AsyncCoursesSections:
    """Courses Sections API resources (async)."""

    def __init__(self, client: AsyncSellAppClient) -> None:
        self._client = client
        self.with_raw_response = WithRawResponse(self)

    async def create(
        self,
        course: str,
        *,
        title: str,
        description: str | None | NotGiven = NOT_GIVEN,
        request_options: RequestOptions | None = None,
    ) -> SdkCreateCourseSectionResponseValue201ApplicationJson:
        """Create a course section

        Append a section to a course curriculum. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            course: The course path parameter.
            title: The title.
            description: The description.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkCreateCourseSectionResponseValue201ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "createCourseSection",
        }
        body: dict[str, Any] = {
            "title": title,
        }
        if not isinstance(description, NotGiven):
            body["description"] = description
        _validate_model(
            body,
            {
                "name": "SdkCreateCourseSectionRequestApplicationJson",
                "required": ["title"],
                "properties": {
                    "title": {
                        "kind": "primitive",
                        "type": "string",
                        "constraints": {"maxLength": 255},
                    },
                    "description": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "string",
                            "constraints": {"maxLength": 65535},
                        },
                    },
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return await self._client.request(
            method="post",
            path=("v2", "courses", str(course), "sections"),
            body=body,
            model=SdkCreateCourseSectionResponseValue201ApplicationJson,
            request_options=request_options,
        )

    async def replace(
        self,
        course: str,
        section: int,
        *,
        title: str | None = None,
        description: str | None | NotGiven = NOT_GIVEN,
        expected_updated_at: str | None = None,
        request_options: RequestOptions | None = None,
    ) -> SdkReplaceCourseSectionResponseValue200ApplicationJson:
        """Update a course section

        Update a section title or sanitized rich-text description. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            course: The course path parameter.
            section: The section path parameter.
            title: The title.
            description: The description.
            expected_updated_at: The expected updated at.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkReplaceCourseSectionResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "replaceCourseSection",
        }
        body: dict[str, Any] = {
            k: v
            for k, v in {
                "title": title,
                "expected_updated_at": expected_updated_at,
            }.items()
            if v is not None
        }
        if not isinstance(description, NotGiven):
            body["description"] = description
        _validate_model(
            body,
            {
                "name": "SdkReplaceCourseSectionRequestApplicationJson",
                "required": [],
                "properties": {
                    "title": {
                        "kind": "primitive",
                        "type": "string",
                        "constraints": {"maxLength": 255},
                    },
                    "description": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "string",
                            "constraints": {"maxLength": 65535},
                        },
                    },
                    "expected_updated_at": {
                        "kind": "primitive",
                        "type": "string",
                        "format": "date-time",
                    },
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return await self._client.request(
            method="put",
            path=("v2", "courses", str(course), "sections", str(section)),
            body=body,
            model=SdkReplaceCourseSectionResponseValue200ApplicationJson,
            request_options=request_options,
        )

    async def update(
        self,
        course: str,
        section: int,
        *,
        title: str | None = None,
        description: str | None | NotGiven = NOT_GIVEN,
        expected_updated_at: str | None = None,
        request_options: RequestOptions | None = None,
    ) -> SdkUpdateCourseSectionResponseValue200ApplicationJson:
        """Update a course section

        Update a section title or sanitized rich-text description. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            course: The course path parameter.
            section: The section path parameter.
            title: The title.
            description: The description.
            expected_updated_at: The expected updated at.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkUpdateCourseSectionResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "updateCourseSection",
        }
        body: dict[str, Any] = {
            k: v
            for k, v in {
                "title": title,
                "expected_updated_at": expected_updated_at,
            }.items()
            if v is not None
        }
        if not isinstance(description, NotGiven):
            body["description"] = description
        _validate_model(
            body,
            {
                "name": "SdkUpdateCourseSectionRequestApplicationJson",
                "required": [],
                "properties": {
                    "title": {
                        "kind": "primitive",
                        "type": "string",
                        "constraints": {"maxLength": 255},
                    },
                    "description": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "string",
                            "constraints": {"maxLength": 65535},
                        },
                    },
                    "expected_updated_at": {
                        "kind": "primitive",
                        "type": "string",
                        "format": "date-time",
                    },
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return await self._client.request(
            method="patch",
            path=("v2", "courses", str(course), "sections", str(section)),
            body=body,
            model=SdkUpdateCourseSectionResponseValue200ApplicationJson,
            request_options=request_options,
        )

    async def delete(
        self,
        course: str,
        section: int,
        *,
        request_options: RequestOptions | None = None,
    ) -> None:
        """Delete a course section

        Delete a section, its lessons, structured questions, attachments, and video references, then normalize remaining section order. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            course: The course path parameter.
            section: The section path parameter.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "deleteCourseSection",
        }
        await self._client.request(
            method="delete",
            path=("v2", "courses", str(course), "sections", str(section)),
            request_options=request_options,
        )

    async def reorder(
        self,
        course: str,
        *,
        resources: list[int],
        request_options: RequestOptions | None = None,
    ) -> SdkReorderCourseSectionsResponseValue200ApplicationJson:
        """Reorder course sections

        Replace the complete section order. Send every section ID exactly once in the desired order. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            course: The course path parameter.
            resources: The resources.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkReorderCourseSectionsResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "reorderCourseSections",
        }
        body: dict[str, Any] = {
            "resources": resources,
        }
        _validate_model(
            body,
            {
                "name": "SdkReorderCourseSectionsRequestApplicationJson",
                "required": ["resources"],
                "properties": {
                    "resources": {
                        "kind": "array",
                        "items": {
                            "kind": "primitive",
                            "type": "integer",
                            "constraints": {"minimum": 1},
                        },
                        "constraints": {"uniqueItems": True},
                    }
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return await self._client.request(
            method="patch",
            path=("v2", "courses", str(course), "sections", "reorder"),
            body=body,
            model=SdkReorderCourseSectionsResponseValue200ApplicationJson,
            request_options=request_options,
        )
