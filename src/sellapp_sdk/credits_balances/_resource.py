# This file is auto-generated by oagen. Do not edit.

from __future__ import annotations

from typing import TYPE_CHECKING

from .._base_client import WithRawResponse

if TYPE_CHECKING:
    from .._client import AsyncSellAppClient, SellAppClient

from sellapp_sdk.credits_products.models.sdk_get_credit_balance_response_value_200_application_json import (
    SdkGetCreditBalanceResponseValue200ApplicationJson,
)
from sellapp_sdk.credits_products.models.sdk_list_credit_balances_response_value_200_application_json import (
    SdkListCreditBalancesResponseValue200ApplicationJson,
)

from .._pagination import AsyncPage, SyncPage
from .._types import RequestOptions


class CreditsBalances:
    """Credits Balances API resources."""

    def __init__(self, client: SellAppClient) -> None:
        self._client = client
        self.with_raw_response = WithRawResponse(self)

    def list(
        self,
        *,
        limit: int | None = None,
        before: str | None = None,
        after: str | None = None,
        order: str | None = None,
        page: int | None = None,
        pagination: bool | None = None,
        customer_id: int | None = None,
        product_id: int | None = None,
        request_options: RequestOptions | None = None,
    ) -> SyncPage[SdkListCreditBalancesResponseValue200ApplicationJson]:
        """List credit balances

        List store-scoped customer balances for dedicated credits products. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            limit: Number of items to return per page. Defaults to `15`.
            before: The before.
            after: The after.
            order: The order.
            page: Page number to return. Defaults to `1`.
            pagination: Set to false to return at most 100 matching items in one data array without pagination links or metadata. Defaults to `true`.
            customer_id: Filter by a store customer ID.
            product_id: Filter by a credits product ID.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SyncPage[SdkListCreditBalancesResponseValue200ApplicationJson]

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "listCreditBalances",
        }
        params = {
            k: v
            for k, v in {
                "limit": limit,
                "before": before,
                "after": after,
                "order": order,
                "page": page,
                "pagination": pagination,
                "customer_id": customer_id,
                "product_id": product_id,
            }.items()
            if v is not None
        }
        return self._client.request_page(
            method="get",
            path=("v2", "credit-balances"),
            model=SdkListCreditBalancesResponseValue200ApplicationJson,
            params=params,
            request_options=request_options,
        )

    def get(
        self,
        customer: int,
        credit_product: int,
        *,
        request_options: RequestOptions | None = None,
    ) -> SdkGetCreditBalanceResponseValue200ApplicationJson:
        """Retrieve a credit balance

        Retrieve one customer and credits-product balance with up to 100 recent ledger entries. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            customer: The customer path parameter.
            credit_product: The creditProduct path parameter.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkGetCreditBalanceResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "getCreditBalance",
        }
        return self._client.request(
            method="get",
            path=("v2", "credit-balances", str(customer), str(credit_product)),
            model=SdkGetCreditBalanceResponseValue200ApplicationJson,
            request_options=request_options,
        )


class AsyncCreditsBalances:
    """Credits Balances API resources (async)."""

    def __init__(self, client: AsyncSellAppClient) -> None:
        self._client = client
        self.with_raw_response = WithRawResponse(self)

    async def list(
        self,
        *,
        limit: int | None = None,
        before: str | None = None,
        after: str | None = None,
        order: str | None = None,
        page: int | None = None,
        pagination: bool | None = None,
        customer_id: int | None = None,
        product_id: int | None = None,
        request_options: RequestOptions | None = None,
    ) -> AsyncPage[SdkListCreditBalancesResponseValue200ApplicationJson]:
        """List credit balances

        List store-scoped customer balances for dedicated credits products. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            limit: Number of items to return per page. Defaults to `15`.
            before: The before.
            after: The after.
            order: The order.
            page: Page number to return. Defaults to `1`.
            pagination: Set to false to return at most 100 matching items in one data array without pagination links or metadata. Defaults to `true`.
            customer_id: Filter by a store customer ID.
            product_id: Filter by a credits product ID.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            AsyncPage[SdkListCreditBalancesResponseValue200ApplicationJson]

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "listCreditBalances",
        }
        params = {
            k: v
            for k, v in {
                "limit": limit,
                "before": before,
                "after": after,
                "order": order,
                "page": page,
                "pagination": pagination,
                "customer_id": customer_id,
                "product_id": product_id,
            }.items()
            if v is not None
        }
        return await self._client.request_page(
            method="get",
            path=("v2", "credit-balances"),
            model=SdkListCreditBalancesResponseValue200ApplicationJson,
            params=params,
            request_options=request_options,
        )

    async def get(
        self,
        customer: int,
        credit_product: int,
        *,
        request_options: RequestOptions | None = None,
    ) -> SdkGetCreditBalanceResponseValue200ApplicationJson:
        """Retrieve a credit balance

        Retrieve one customer and credits-product balance with up to 100 recent ledger entries. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            customer: The customer path parameter.
            credit_product: The creditProduct path parameter.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkGetCreditBalanceResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "getCreditBalance",
        }
        return await self._client.request(
            method="get",
            path=("v2", "credit-balances", str(customer), str(credit_product)),
            model=SdkGetCreditBalanceResponseValue200ApplicationJson,
            request_options=request_options,
        )
