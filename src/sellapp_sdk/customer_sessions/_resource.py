# This file is auto-generated by oagen. Do not edit.

from __future__ import annotations

from typing import TYPE_CHECKING, Any

from .._base_client import WithRawResponse

if TYPE_CHECKING:
    from .._client import AsyncSellAppClient, SellAppClient

from .._types import RequestOptions
from .models import (
    CreateCustomerSessionRequestApplicationJsonOneOfValue1,
    CreateCustomerSessionRequestApplicationJsonOneOfValue2,
    SdkCreateCustomerSessionResponseValue201ApplicationJson,
)


class CustomerSessions:
    """Customer Sessions API resources."""

    def __init__(self, client: SellAppClient) -> None:
        self._client = client
        self.with_raw_response = WithRawResponse(self)

    def create_customer_session(
        self,
        *,
        body: CreateCustomerSessionRequestApplicationJsonOneOfValue1
        | CreateCustomerSessionRequestApplicationJsonOneOfValue2
        | dict[str, Any],
        request_options: RequestOptions | None = None,
    ) -> SdkCreateCustomerSessionResponseValue201ApplicationJson:
        """Create a customer session

        Return a 15-minute bearer token once plus a separate single-use hosted redemption URL. Opening the URL signs the customer into this store's customer portal only. It does not sign them into the dashboard or other stores. Browser access and wallet credentials issued from it stop working when the session expires or is revoked. This response is never cached or replayed. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            body: The request body. Accepts: CreateCustomerSessionRequestApplicationJsonOneOfValue1, CreateCustomerSessionRequestApplicationJsonOneOfValue2, or a plain dict.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkCreateCustomerSessionResponseValue201ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            ConflictError: If a conflict occurs (409).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": True,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "createCustomerSession",
        }
        _body: dict[str, Any] = body if isinstance(body, dict) else body.to_dict()
        return self._client.request(
            method="post",
            path=("v2", "customer-sessions"),
            body=_body,
            model=SdkCreateCustomerSessionResponseValue201ApplicationJson,
            request_options=request_options,
        )

    def revoke_customer_session(
        self,
        session: str,
        *,
        request_options: RequestOptions | None = None,
    ) -> None:
        """Revoke a customer session

        Revoke immediately. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            session: The session identifier.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            ConflictError: If a conflict occurs (409).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "revokeCustomerSession",
        }
        self._client.request(
            method="delete",
            path=("v2", "customer-sessions", str(session)),
            request_options=request_options,
        )


class AsyncCustomerSessions:
    """Customer Sessions API resources (async)."""

    def __init__(self, client: AsyncSellAppClient) -> None:
        self._client = client
        self.with_raw_response = WithRawResponse(self)

    async def create_customer_session(
        self,
        *,
        body: CreateCustomerSessionRequestApplicationJsonOneOfValue1
        | CreateCustomerSessionRequestApplicationJsonOneOfValue2
        | dict[str, Any],
        request_options: RequestOptions | None = None,
    ) -> SdkCreateCustomerSessionResponseValue201ApplicationJson:
        """Create a customer session

        Return a 15-minute bearer token once plus a separate single-use hosted redemption URL. Opening the URL signs the customer into this store's customer portal only. It does not sign them into the dashboard or other stores. Browser access and wallet credentials issued from it stop working when the session expires or is revoked. This response is never cached or replayed. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            body: The request body. Accepts: CreateCustomerSessionRequestApplicationJsonOneOfValue1, CreateCustomerSessionRequestApplicationJsonOneOfValue2, or a plain dict.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkCreateCustomerSessionResponseValue201ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            ConflictError: If a conflict occurs (409).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": True,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "createCustomerSession",
        }
        _body: dict[str, Any] = body if isinstance(body, dict) else body.to_dict()
        return await self._client.request(
            method="post",
            path=("v2", "customer-sessions"),
            body=_body,
            model=SdkCreateCustomerSessionResponseValue201ApplicationJson,
            request_options=request_options,
        )

    async def revoke_customer_session(
        self,
        session: str,
        *,
        request_options: RequestOptions | None = None,
    ) -> None:
        """Revoke a customer session

        Revoke immediately. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            session: The session identifier.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            ConflictError: If a conflict occurs (409).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "revokeCustomerSession",
        }
        await self._client.request(
            method="delete",
            path=("v2", "customer-sessions", str(session)),
            request_options=request_options,
        )
