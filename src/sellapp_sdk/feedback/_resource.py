# This file is auto-generated by oagen. Do not edit.

from __future__ import annotations

from typing import TYPE_CHECKING, Any, Dict, List, Optional

from .._base_client import WithRawResponse

if TYPE_CHECKING:
    from .._client import AsyncSellAppClient, SellAppClient

from sellapp_sdk.blacklists.models.list_feedback_response_value_200_application_json_property_data_item import (
    ListFeedbackResponseValue200ApplicationJsonPropertyDataItem,
)
from sellapp_sdk.blacklists.models.sdk_get_feedback_response_value_200_application_json import (
    SdkGetFeedbackResponseValue200ApplicationJson,
)
from sellapp_sdk.blacklists.models.sdk_reply_to_feedback_response_value_200_application_json import (
    SdkReplyToFeedbackResponseValue200ApplicationJson,
)
from sellapp_sdk.blacklists.models.sdk_v_2_get_feedback_response_value_200_application_json import (
    SdkV2GetFeedbackResponseValue200ApplicationJson,
)
from sellapp_sdk.blacklists.models.sdk_v_2_replace_feedback_response_value_200_application_json import (
    SdkV2ReplaceFeedbackResponseValue200ApplicationJson,
)
from sellapp_sdk.blacklists.models.sdk_v_2_reply_to_feedback_response_value_200_application_json import (
    SdkV2ReplyToFeedbackResponseValue200ApplicationJson,
)
from sellapp_sdk.blacklists.models.search_feedback_request_application_json_property_filters_item import (
    SearchFeedbackRequestApplicationJsonPropertyFiltersItem,
)
from sellapp_sdk.blacklists.models.search_feedback_request_application_json_property_includes_item import (
    SearchFeedbackRequestApplicationJsonPropertyIncludesItem,
)
from sellapp_sdk.blacklists.models.search_feedback_request_application_json_property_search import (
    SearchFeedbackRequestApplicationJsonPropertySearch,
)
from sellapp_sdk.blacklists.models.search_feedback_request_application_json_property_sort_item import (
    SearchFeedbackRequestApplicationJsonPropertySortItem,
)
from sellapp_sdk.blacklists.models.search_feedback_response_value_200_application_json_property_data_item import (
    SearchFeedbackResponseValue200ApplicationJsonPropertyDataItem,
)
from sellapp_sdk.blacklists.models.v_2_list_feedback_response_value_200_application_json_property_data_item import (
    V2ListFeedbackResponseValue200ApplicationJsonPropertyDataItem,
)
from sellapp_sdk.blacklists.models.v_2_search_feedback_request_application_json_property_filters_item import (
    V2SearchFeedbackRequestApplicationJsonPropertyFiltersItem,
)
from sellapp_sdk.blacklists.models.v_2_search_feedback_request_application_json_property_includes_item import (
    V2SearchFeedbackRequestApplicationJsonPropertyIncludesItem,
)
from sellapp_sdk.blacklists.models.v_2_search_feedback_request_application_json_property_search import (
    V2SearchFeedbackRequestApplicationJsonPropertySearch,
)
from sellapp_sdk.blacklists.models.v_2_search_feedback_request_application_json_property_sort_item import (
    V2SearchFeedbackRequestApplicationJsonPropertySortItem,
)
from sellapp_sdk.blacklists.models.v_2_search_feedback_response_value_200_application_json_property_data_item import (
    V2SearchFeedbackResponseValue200ApplicationJsonPropertyDataItem,
)

from .._pagination import AsyncPage, SyncPage
from .._types import RequestOptions, _validate_model


class Feedback:
    """Feedback API resources."""

    def __init__(self, client: SellAppClient) -> None:
        self._client = client
        self.with_raw_response = WithRawResponse(self)

    def list(
        self,
        *,
        limit: Optional[int] = None,
        before: Optional[str] = None,
        after: Optional[str] = None,
        order: Optional[str] = None,
        page: Optional[int] = None,
        request_options: Optional[RequestOptions] = None,
    ) -> SyncPage[ListFeedbackResponseValue200ApplicationJsonPropertyDataItem]:
        """List all feedback

        List customer feedback for your store, 15 entries per page by default.

        Args:
            limit: Number of items to return per page. Defaults to `15`.
            before: The before.
            after: The after.
            order: The order.
            page: Page number to return. Defaults to `1`.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SyncPage[ListFeedbackResponseValue200ApplicationJsonPropertyDataItem]

        Raises:
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [
                        {"schemeName": "bearerAuth", "scopes": []},
                        {"schemeName": "storeAuth", "scopes": []},
                    ]
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": False,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": True,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": False,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "listFeedback",
        }
        params = {
            k: v
            for k, v in {
                "limit": limit,
                "before": before,
                "after": after,
                "order": order,
                "page": page,
            }.items()
            if v is not None
        }
        return self._client.request_page(
            method="get",
            path=("v1", "feedback"),
            model=ListFeedbackResponseValue200ApplicationJsonPropertyDataItem,
            params=params,
            request_options=request_options,
        )

    def get(
        self,
        feedback: int,
        *,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkGetFeedbackResponseValue200ApplicationJson:
        """Retrieve specific feedback

        Retrieve a customer's feedback by its ID.

        Args:
            feedback: The feedback path parameter.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkGetFeedbackResponseValue200ApplicationJson

        Raises:
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [
                        {"schemeName": "bearerAuth", "scopes": []},
                        {"schemeName": "storeAuth", "scopes": []},
                    ]
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": False,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": True,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": False,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "getFeedback",
        }
        return self._client.request(
            method="get",
            path=("v1", "feedback", str(feedback)),
            model=SdkGetFeedbackResponseValue200ApplicationJson,
            request_options=request_options,
        )

    def reply(
        self,
        feedback: int,
        *,
        reply: str,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkReplyToFeedbackResponseValue200ApplicationJson:
        """Reply to feedback

        Publish a seller reply to a customer's feedback.

        Args:
            feedback: The feedback path parameter.
            reply: The reply.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkReplyToFeedbackResponseValue200ApplicationJson

        Raises:
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [
                        {"schemeName": "bearerAuth", "scopes": []},
                        {"schemeName": "storeAuth", "scopes": []},
                    ]
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": False,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": True,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": False,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "replyToFeedback",
        }
        body: Dict[str, Any] = {
            "reply": reply,
        }
        _validate_model(
            body,
            {
                "name": "SdkReplyToFeedbackRequestApplicationJson",
                "required": ["reply"],
                "properties": {"reply": {"kind": "primitive", "type": "string"}},
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return self._client.request(
            method="patch",
            path=("v1", "feedback", str(feedback)),
            body=body,
            model=SdkReplyToFeedbackResponseValue200ApplicationJson,
            request_options=request_options,
        )

    def search(
        self,
        *,
        filters: Optional[
            List[SearchFeedbackRequestApplicationJsonPropertyFiltersItem]
        ] = None,
        sort: Optional[
            List[SearchFeedbackRequestApplicationJsonPropertySortItem]
        ] = None,
        search: Optional[SearchFeedbackRequestApplicationJsonPropertySearch] = None,
        includes: Optional[
            List[SearchFeedbackRequestApplicationJsonPropertyIncludesItem]
        ] = None,
        limit: Optional[int] = None,
        before: Optional[str] = None,
        after: Optional[str] = None,
        order: Optional[str] = None,
        request_options: Optional[RequestOptions] = None,
    ) -> SyncPage[SearchFeedbackResponseValue200ApplicationJsonPropertyDataItem]:
        """Search feedback

        Search feedback using JSON body filters, search terms, includes, and sort instructions.

        Args:
            filters: The filters.
            sort: The sort.
            search: The search.
            includes: The includes.
            limit: The limit.
            before: The before.
            after: The after.
            order: The order.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SyncPage[SearchFeedbackResponseValue200ApplicationJsonPropertyDataItem]

        Raises:
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [
                        {"schemeName": "bearerAuth", "scopes": []},
                        {"schemeName": "storeAuth", "scopes": []},
                    ]
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": False,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": True,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": False,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "searchFeedback",
        }
        body: Dict[str, Any] = {
            k: v
            for k, v in {
                "filters": [item.to_dict() for item in filters]
                if filters is not None
                else None,
                "sort": [item.to_dict() for item in sort] if sort is not None else None,
                "search": search.to_dict() if search is not None else None,
                "includes": [item.to_dict() for item in includes]
                if includes is not None
                else None,
            }.items()
            if v is not None
        }
        params = {
            k: v
            for k, v in {
                "limit": limit,
                "before": before,
                "after": after,
                "order": order,
            }.items()
            if v is not None
        }
        return self._client.request_page(
            method="post",
            path=("v1", "feedback", "search"),
            model=SearchFeedbackResponseValue200ApplicationJsonPropertyDataItem,
            body=body,
            params=params,
            request_options=request_options,
        )

    def v2_list_feedback(
        self,
        *,
        limit: Optional[int] = None,
        before: Optional[str] = None,
        after: Optional[str] = None,
        order: Optional[str] = None,
        page: Optional[int] = None,
        request_options: Optional[RequestOptions] = None,
    ) -> SyncPage[V2ListFeedbackResponseValue200ApplicationJsonPropertyDataItem]:
        """List all feedback

        List customer feedback for your store, 15 entries per page by default. This v2 route is store-scoped. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            limit: Number of items to return per page. Defaults to `15`.
            before: The before.
            after: The after.
            order: The order.
            page: Page number to return. Defaults to `1`.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SyncPage[V2ListFeedbackResponseValue200ApplicationJsonPropertyDataItem]

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "v2ListFeedback",
        }
        params = {
            k: v
            for k, v in {
                "limit": limit,
                "before": before,
                "after": after,
                "order": order,
                "page": page,
            }.items()
            if v is not None
        }
        return self._client.request_page(
            method="get",
            path=("v2", "feedback"),
            model=V2ListFeedbackResponseValue200ApplicationJsonPropertyDataItem,
            params=params,
            request_options=request_options,
        )

    def v2_search_feedback(
        self,
        *,
        filters: Optional[
            List[V2SearchFeedbackRequestApplicationJsonPropertyFiltersItem]
        ] = None,
        sort: Optional[
            List[V2SearchFeedbackRequestApplicationJsonPropertySortItem]
        ] = None,
        search: Optional[V2SearchFeedbackRequestApplicationJsonPropertySearch] = None,
        includes: Optional[
            List[V2SearchFeedbackRequestApplicationJsonPropertyIncludesItem]
        ] = None,
        limit: Optional[int] = None,
        before: Optional[str] = None,
        after: Optional[str] = None,
        order: Optional[str] = None,
        request_options: Optional[RequestOptions] = None,
    ) -> SyncPage[V2SearchFeedbackResponseValue200ApplicationJsonPropertyDataItem]:
        """Search feedback

        Search feedback using JSON body filters, search terms, includes, and sort instructions. This v2 route is store-scoped. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            filters: The filters.
            sort: The sort.
            search: The search.
            includes: The includes.
            limit: The limit.
            before: The before.
            after: The after.
            order: The order.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SyncPage[V2SearchFeedbackResponseValue200ApplicationJsonPropertyDataItem]

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "v2SearchFeedback",
        }
        body: Dict[str, Any] = {
            k: v
            for k, v in {
                "filters": [item.to_dict() for item in filters]
                if filters is not None
                else None,
                "sort": [item.to_dict() for item in sort] if sort is not None else None,
                "search": search.to_dict() if search is not None else None,
                "includes": [item.to_dict() for item in includes]
                if includes is not None
                else None,
            }.items()
            if v is not None
        }
        params = {
            k: v
            for k, v in {
                "limit": limit,
                "before": before,
                "after": after,
                "order": order,
            }.items()
            if v is not None
        }
        return self._client.request_page(
            method="post",
            path=("v2", "feedback", "search"),
            model=V2SearchFeedbackResponseValue200ApplicationJsonPropertyDataItem,
            body=body,
            params=params,
            request_options=request_options,
        )

    def v2_get_feedback(
        self,
        feedback: int,
        *,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkV2GetFeedbackResponseValue200ApplicationJson:
        """Retrieve specific feedback

        Retrieve a customer's feedback by its ID. This v2 route is store-scoped. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            feedback: The feedback path parameter.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkV2GetFeedbackResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "v2GetFeedback",
        }
        return self._client.request(
            method="get",
            path=("v2", "feedback", str(feedback)),
            model=SdkV2GetFeedbackResponseValue200ApplicationJson,
            request_options=request_options,
        )

    def v2_replace_feedback(
        self,
        feedback: int,
        *,
        reply: str,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkV2ReplaceFeedbackResponseValue200ApplicationJson:
        """Reply to feedback

        Publish a seller reply to a customer's feedback. This v2 route is store-scoped. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            feedback: The feedback path parameter.
            reply: The reply.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkV2ReplaceFeedbackResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "v2ReplaceFeedback",
        }
        body: Dict[str, Any] = {
            "reply": reply,
        }
        _validate_model(
            body,
            {
                "name": "SdkV2ReplaceFeedbackRequestApplicationJson",
                "required": ["reply"],
                "properties": {"reply": {"kind": "primitive", "type": "string"}},
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return self._client.request(
            method="put",
            path=("v2", "feedback", str(feedback)),
            body=body,
            model=SdkV2ReplaceFeedbackResponseValue200ApplicationJson,
            request_options=request_options,
        )

    def v2_reply_to_feedback(
        self,
        feedback: int,
        *,
        reply: str,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkV2ReplyToFeedbackResponseValue200ApplicationJson:
        """Reply to feedback

        Publish a seller reply to a customer's feedback. This v2 route is store-scoped. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            feedback: The feedback path parameter.
            reply: The reply.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkV2ReplyToFeedbackResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "v2ReplyToFeedback",
        }
        body: Dict[str, Any] = {
            "reply": reply,
        }
        _validate_model(
            body,
            {
                "name": "SdkV2ReplyToFeedbackRequestApplicationJson",
                "required": ["reply"],
                "properties": {"reply": {"kind": "primitive", "type": "string"}},
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return self._client.request(
            method="patch",
            path=("v2", "feedback", str(feedback)),
            body=body,
            model=SdkV2ReplyToFeedbackResponseValue200ApplicationJson,
            request_options=request_options,
        )


class AsyncFeedback:
    """Feedback API resources (async)."""

    def __init__(self, client: AsyncSellAppClient) -> None:
        self._client = client
        self.with_raw_response = WithRawResponse(self)

    async def list(
        self,
        *,
        limit: Optional[int] = None,
        before: Optional[str] = None,
        after: Optional[str] = None,
        order: Optional[str] = None,
        page: Optional[int] = None,
        request_options: Optional[RequestOptions] = None,
    ) -> AsyncPage[ListFeedbackResponseValue200ApplicationJsonPropertyDataItem]:
        """List all feedback

        List customer feedback for your store, 15 entries per page by default.

        Args:
            limit: Number of items to return per page. Defaults to `15`.
            before: The before.
            after: The after.
            order: The order.
            page: Page number to return. Defaults to `1`.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            AsyncPage[ListFeedbackResponseValue200ApplicationJsonPropertyDataItem]

        Raises:
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [
                        {"schemeName": "bearerAuth", "scopes": []},
                        {"schemeName": "storeAuth", "scopes": []},
                    ]
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": False,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": True,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": False,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "listFeedback",
        }
        params = {
            k: v
            for k, v in {
                "limit": limit,
                "before": before,
                "after": after,
                "order": order,
                "page": page,
            }.items()
            if v is not None
        }
        return await self._client.request_page(
            method="get",
            path=("v1", "feedback"),
            model=ListFeedbackResponseValue200ApplicationJsonPropertyDataItem,
            params=params,
            request_options=request_options,
        )

    async def get(
        self,
        feedback: int,
        *,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkGetFeedbackResponseValue200ApplicationJson:
        """Retrieve specific feedback

        Retrieve a customer's feedback by its ID.

        Args:
            feedback: The feedback path parameter.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkGetFeedbackResponseValue200ApplicationJson

        Raises:
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [
                        {"schemeName": "bearerAuth", "scopes": []},
                        {"schemeName": "storeAuth", "scopes": []},
                    ]
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": False,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": True,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": False,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "getFeedback",
        }
        return await self._client.request(
            method="get",
            path=("v1", "feedback", str(feedback)),
            model=SdkGetFeedbackResponseValue200ApplicationJson,
            request_options=request_options,
        )

    async def reply(
        self,
        feedback: int,
        *,
        reply: str,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkReplyToFeedbackResponseValue200ApplicationJson:
        """Reply to feedback

        Publish a seller reply to a customer's feedback.

        Args:
            feedback: The feedback path parameter.
            reply: The reply.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkReplyToFeedbackResponseValue200ApplicationJson

        Raises:
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [
                        {"schemeName": "bearerAuth", "scopes": []},
                        {"schemeName": "storeAuth", "scopes": []},
                    ]
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": False,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": True,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": False,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "replyToFeedback",
        }
        body: Dict[str, Any] = {
            "reply": reply,
        }
        _validate_model(
            body,
            {
                "name": "SdkReplyToFeedbackRequestApplicationJson",
                "required": ["reply"],
                "properties": {"reply": {"kind": "primitive", "type": "string"}},
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return await self._client.request(
            method="patch",
            path=("v1", "feedback", str(feedback)),
            body=body,
            model=SdkReplyToFeedbackResponseValue200ApplicationJson,
            request_options=request_options,
        )

    async def search(
        self,
        *,
        filters: Optional[
            List[SearchFeedbackRequestApplicationJsonPropertyFiltersItem]
        ] = None,
        sort: Optional[
            List[SearchFeedbackRequestApplicationJsonPropertySortItem]
        ] = None,
        search: Optional[SearchFeedbackRequestApplicationJsonPropertySearch] = None,
        includes: Optional[
            List[SearchFeedbackRequestApplicationJsonPropertyIncludesItem]
        ] = None,
        limit: Optional[int] = None,
        before: Optional[str] = None,
        after: Optional[str] = None,
        order: Optional[str] = None,
        request_options: Optional[RequestOptions] = None,
    ) -> AsyncPage[SearchFeedbackResponseValue200ApplicationJsonPropertyDataItem]:
        """Search feedback

        Search feedback using JSON body filters, search terms, includes, and sort instructions.

        Args:
            filters: The filters.
            sort: The sort.
            search: The search.
            includes: The includes.
            limit: The limit.
            before: The before.
            after: The after.
            order: The order.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            AsyncPage[SearchFeedbackResponseValue200ApplicationJsonPropertyDataItem]

        Raises:
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [
                        {"schemeName": "bearerAuth", "scopes": []},
                        {"schemeName": "storeAuth", "scopes": []},
                    ]
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": False,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": True,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": False,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "searchFeedback",
        }
        body: Dict[str, Any] = {
            k: v
            for k, v in {
                "filters": [item.to_dict() for item in filters]
                if filters is not None
                else None,
                "sort": [item.to_dict() for item in sort] if sort is not None else None,
                "search": search.to_dict() if search is not None else None,
                "includes": [item.to_dict() for item in includes]
                if includes is not None
                else None,
            }.items()
            if v is not None
        }
        params = {
            k: v
            for k, v in {
                "limit": limit,
                "before": before,
                "after": after,
                "order": order,
            }.items()
            if v is not None
        }
        return await self._client.request_page(
            method="post",
            path=("v1", "feedback", "search"),
            model=SearchFeedbackResponseValue200ApplicationJsonPropertyDataItem,
            body=body,
            params=params,
            request_options=request_options,
        )

    async def v2_list_feedback(
        self,
        *,
        limit: Optional[int] = None,
        before: Optional[str] = None,
        after: Optional[str] = None,
        order: Optional[str] = None,
        page: Optional[int] = None,
        request_options: Optional[RequestOptions] = None,
    ) -> AsyncPage[V2ListFeedbackResponseValue200ApplicationJsonPropertyDataItem]:
        """List all feedback

        List customer feedback for your store, 15 entries per page by default. This v2 route is store-scoped. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            limit: Number of items to return per page. Defaults to `15`.
            before: The before.
            after: The after.
            order: The order.
            page: Page number to return. Defaults to `1`.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            AsyncPage[V2ListFeedbackResponseValue200ApplicationJsonPropertyDataItem]

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "v2ListFeedback",
        }
        params = {
            k: v
            for k, v in {
                "limit": limit,
                "before": before,
                "after": after,
                "order": order,
                "page": page,
            }.items()
            if v is not None
        }
        return await self._client.request_page(
            method="get",
            path=("v2", "feedback"),
            model=V2ListFeedbackResponseValue200ApplicationJsonPropertyDataItem,
            params=params,
            request_options=request_options,
        )

    async def v2_search_feedback(
        self,
        *,
        filters: Optional[
            List[V2SearchFeedbackRequestApplicationJsonPropertyFiltersItem]
        ] = None,
        sort: Optional[
            List[V2SearchFeedbackRequestApplicationJsonPropertySortItem]
        ] = None,
        search: Optional[V2SearchFeedbackRequestApplicationJsonPropertySearch] = None,
        includes: Optional[
            List[V2SearchFeedbackRequestApplicationJsonPropertyIncludesItem]
        ] = None,
        limit: Optional[int] = None,
        before: Optional[str] = None,
        after: Optional[str] = None,
        order: Optional[str] = None,
        request_options: Optional[RequestOptions] = None,
    ) -> AsyncPage[V2SearchFeedbackResponseValue200ApplicationJsonPropertyDataItem]:
        """Search feedback

        Search feedback using JSON body filters, search terms, includes, and sort instructions. This v2 route is store-scoped. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            filters: The filters.
            sort: The sort.
            search: The search.
            includes: The includes.
            limit: The limit.
            before: The before.
            after: The after.
            order: The order.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            AsyncPage[V2SearchFeedbackResponseValue200ApplicationJsonPropertyDataItem]

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "v2SearchFeedback",
        }
        body: Dict[str, Any] = {
            k: v
            for k, v in {
                "filters": [item.to_dict() for item in filters]
                if filters is not None
                else None,
                "sort": [item.to_dict() for item in sort] if sort is not None else None,
                "search": search.to_dict() if search is not None else None,
                "includes": [item.to_dict() for item in includes]
                if includes is not None
                else None,
            }.items()
            if v is not None
        }
        params = {
            k: v
            for k, v in {
                "limit": limit,
                "before": before,
                "after": after,
                "order": order,
            }.items()
            if v is not None
        }
        return await self._client.request_page(
            method="post",
            path=("v2", "feedback", "search"),
            model=V2SearchFeedbackResponseValue200ApplicationJsonPropertyDataItem,
            body=body,
            params=params,
            request_options=request_options,
        )

    async def v2_get_feedback(
        self,
        feedback: int,
        *,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkV2GetFeedbackResponseValue200ApplicationJson:
        """Retrieve specific feedback

        Retrieve a customer's feedback by its ID. This v2 route is store-scoped. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            feedback: The feedback path parameter.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkV2GetFeedbackResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "v2GetFeedback",
        }
        return await self._client.request(
            method="get",
            path=("v2", "feedback", str(feedback)),
            model=SdkV2GetFeedbackResponseValue200ApplicationJson,
            request_options=request_options,
        )

    async def v2_replace_feedback(
        self,
        feedback: int,
        *,
        reply: str,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkV2ReplaceFeedbackResponseValue200ApplicationJson:
        """Reply to feedback

        Publish a seller reply to a customer's feedback. This v2 route is store-scoped. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            feedback: The feedback path parameter.
            reply: The reply.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkV2ReplaceFeedbackResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "v2ReplaceFeedback",
        }
        body: Dict[str, Any] = {
            "reply": reply,
        }
        _validate_model(
            body,
            {
                "name": "SdkV2ReplaceFeedbackRequestApplicationJson",
                "required": ["reply"],
                "properties": {"reply": {"kind": "primitive", "type": "string"}},
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return await self._client.request(
            method="put",
            path=("v2", "feedback", str(feedback)),
            body=body,
            model=SdkV2ReplaceFeedbackResponseValue200ApplicationJson,
            request_options=request_options,
        )

    async def v2_reply_to_feedback(
        self,
        feedback: int,
        *,
        reply: str,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkV2ReplyToFeedbackResponseValue200ApplicationJson:
        """Reply to feedback

        Publish a seller reply to a customer's feedback. This v2 route is store-scoped. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            feedback: The feedback path parameter.
            reply: The reply.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkV2ReplyToFeedbackResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "v2ReplyToFeedback",
        }
        body: Dict[str, Any] = {
            "reply": reply,
        }
        _validate_model(
            body,
            {
                "name": "SdkV2ReplyToFeedbackRequestApplicationJson",
                "required": ["reply"],
                "properties": {"reply": {"kind": "primitive", "type": "string"}},
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return await self._client.request(
            method="patch",
            path=("v2", "feedback", str(feedback)),
            body=body,
            model=SdkV2ReplyToFeedbackResponseValue200ApplicationJson,
            request_options=request_options,
        )
