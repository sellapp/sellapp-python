# This file is auto-generated by oagen. Do not edit.

from __future__ import annotations

from typing import TYPE_CHECKING, Any

from .._base_client import WithRawResponse

if TYPE_CHECKING:
    from .._client import AsyncSellAppClient, SellAppClient

import builtins

from sellapp_sdk.highlights.models.sdk_add_highlight_media_response_value_201_application_json import (
    SdkAddHighlightMediaResponseValue201ApplicationJson,
)
from sellapp_sdk.highlights.models.sdk_list_highlight_media_response_value_200_application_json import (
    SdkListHighlightMediaResponseValue200ApplicationJson,
)
from sellapp_sdk.highlights.models.sdk_reorder_highlight_media_response_value_200_application_json import (
    SdkReorderHighlightMediaResponseValue200ApplicationJson,
)
from sellapp_sdk.highlights.models.sdk_replace_highlight_media_metadata_response_value_200_application_json import (
    SdkReplaceHighlightMediaMetadataResponseValue200ApplicationJson,
)
from sellapp_sdk.highlights.models.sdk_replace_highlight_media_response_value_200_application_json import (
    SdkReplaceHighlightMediaResponseValue200ApplicationJson,
)
from sellapp_sdk.highlights.models.sdk_update_highlight_media_response_value_200_application_json import (
    SdkUpdateHighlightMediaResponseValue200ApplicationJson,
)

from .._types import NOT_GIVEN, NotGiven, RequestOptions, _validate_model


class HighlightsMedia:
    """Highlights Media API resources."""

    def __init__(self, client: SellAppClient) -> None:
        self._client = client
        self.with_raw_response = WithRawResponse(self)

    def list(
        self,
        highlight: int,
        *,
        request_options: RequestOptions | None = None,
    ) -> SdkListHighlightMediaResponseValue200ApplicationJson:
        """List highlight media

        List media attached to a highlight in display order. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            highlight: The highlight path parameter.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkListHighlightMediaResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "listHighlightMedia",
        }
        return self._client.request(
            method="get",
            path=("v2", "highlights", str(highlight), "media"),
            model=SdkListHighlightMediaResponseValue200ApplicationJson,
            request_options=request_options,
        )

    def add(
        self,
        highlight: int,
        *,
        file: bytes,
        cta_title: str | None = None,
        product_id: int | None | NotGiven = NOT_GIVEN,
        request_options: RequestOptions | None = None,
    ) -> SdkAddHighlightMediaResponseValue201ApplicationJson:
        """Add highlight media

        Upload an image or video and append it to a highlight, up to 30 media items in total. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            highlight: The highlight path parameter.
            file: The file.
            cta_title: The cta title.
            product_id: The product id.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkAddHighlightMediaResponseValue201ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "form-data",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "addHighlightMedia",
        }
        body: dict[str, Any] = {
            k: v
            for k, v in {
                "file": file,
                "cta_title": cta_title,
            }.items()
            if v is not None
        }
        if not isinstance(product_id, NotGiven):
            body["product_id"] = product_id
        _validate_model(
            body,
            {
                "name": "SdkAddHighlightMediaRequestMultipartFormData",
                "required": ["file"],
                "properties": {
                    "file": {"kind": "primitive", "type": "string", "format": "binary"},
                    "cta_title": {
                        "kind": "primitive",
                        "type": "string",
                        "constraints": {"maxLength": 510},
                    },
                    "product_id": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "integer",
                            "constraints": {"minimum": 1},
                        },
                    },
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        multipart_files: dict[str, Any] = {}
        if "file" in body:
            multipart_files["file"] = body.pop("file")
        request_options = {
            **(request_options or {}),
            "files": {
                **multipart_files,
                **((request_options or {}).get("files") or {}),
            },
        }
        return self._client.request(
            method="post",
            path=("v2", "highlights", str(highlight), "media"),
            body=body,
            model=SdkAddHighlightMediaResponseValue201ApplicationJson,
            request_options=request_options,
        )

    def reorder(
        self,
        highlight: int,
        *,
        resources: builtins.list[int],
        request_options: RequestOptions | None = None,
    ) -> SdkReorderHighlightMediaResponseValue200ApplicationJson:
        """Reorder highlight media

        Replace the complete media order for a highlight with the supplied media IDs. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            highlight: The highlight path parameter.
            resources: The resources.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkReorderHighlightMediaResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "reorderHighlightMedia",
        }
        body: dict[str, Any] = {
            "resources": resources,
        }
        _validate_model(
            body,
            {
                "name": "SdkReorderHighlightsRequestApplicationJson",
                "required": ["resources"],
                "properties": {
                    "resources": {
                        "kind": "array",
                        "items": {"kind": "primitive", "type": "integer"},
                        "constraints": {"minItems": 1},
                    }
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return self._client.request(
            method="patch",
            path=("v2", "highlights", str(highlight), "media", "reorder"),
            body=body,
            model=SdkReorderHighlightMediaResponseValue200ApplicationJson,
            request_options=request_options,
        )

    def replace(
        self,
        highlight: int,
        media: int,
        *,
        file: bytes,
        cta_title: str | None = None,
        product_id: int | None | NotGiven = NOT_GIVEN,
        request_options: RequestOptions | None = None,
    ) -> SdkReplaceHighlightMediaResponseValue200ApplicationJson:
        """Replace highlight media

        Replace highlight media for a highlight. Deleting the last media item unpublishes the highlight. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            highlight: The highlight path parameter.
            media: The media path parameter.
            file: The file.
            cta_title: The cta title.
            product_id: The product id.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkReplaceHighlightMediaResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "form-data",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "replaceHighlightMedia",
        }
        body: dict[str, Any] = {
            k: v
            for k, v in {
                "file": file,
                "cta_title": cta_title,
            }.items()
            if v is not None
        }
        if not isinstance(product_id, NotGiven):
            body["product_id"] = product_id
        _validate_model(
            body,
            {
                "name": "SdkReplaceHighlightMediaRequestMultipartFormData",
                "required": ["file"],
                "properties": {
                    "file": {"kind": "primitive", "type": "string", "format": "binary"},
                    "cta_title": {
                        "kind": "primitive",
                        "type": "string",
                        "constraints": {"maxLength": 510},
                    },
                    "product_id": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "integer",
                            "constraints": {"minimum": 1},
                        },
                    },
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        multipart_files: dict[str, Any] = {}
        if "file" in body:
            multipart_files["file"] = body.pop("file")
        request_options = {
            **(request_options or {}),
            "files": {
                **multipart_files,
                **((request_options or {}).get("files") or {}),
            },
        }
        return self._client.request(
            method="post",
            path=("v2", "highlights", str(highlight), "media", str(media)),
            body=body,
            model=SdkReplaceHighlightMediaResponseValue200ApplicationJson,
            request_options=request_options,
        )

    def replace_metadata(
        self,
        highlight: int,
        media: int,
        *,
        cta_title: str | None = None,
        product_id: int | None | NotGiven = NOT_GIVEN,
        request_options: RequestOptions | None = None,
    ) -> SdkReplaceHighlightMediaMetadataResponseValue200ApplicationJson:
        """Update highlight media

        Update highlight media for a highlight. Deleting the last media item unpublishes the highlight. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            highlight: The highlight path parameter.
            media: The media path parameter.
            cta_title: The cta title.
            product_id: The product id.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkReplaceHighlightMediaMetadataResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "replaceHighlightMediaMetadata",
        }
        body: dict[str, Any] = {
            k: v
            for k, v in {
                "cta_title": cta_title,
            }.items()
            if v is not None
        }
        if not isinstance(product_id, NotGiven):
            body["product_id"] = product_id
        _validate_model(
            body,
            {
                "name": "SdkReplaceHighlightMediaMetadataRequestApplicationJson",
                "required": [],
                "properties": {
                    "cta_title": {
                        "kind": "primitive",
                        "type": "string",
                        "constraints": {"maxLength": 510},
                    },
                    "product_id": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "integer",
                            "constraints": {"minimum": 1},
                        },
                    },
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return self._client.request(
            method="put",
            path=("v2", "highlights", str(highlight), "media", str(media)),
            body=body,
            model=SdkReplaceHighlightMediaMetadataResponseValue200ApplicationJson,
            request_options=request_options,
        )

    def update(
        self,
        highlight: int,
        media: int,
        *,
        cta_title: str | None = None,
        product_id: int | None | NotGiven = NOT_GIVEN,
        request_options: RequestOptions | None = None,
    ) -> SdkUpdateHighlightMediaResponseValue200ApplicationJson:
        """Update highlight media

        Update highlight media for a highlight. Deleting the last media item unpublishes the highlight. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            highlight: The highlight path parameter.
            media: The media path parameter.
            cta_title: The cta title.
            product_id: The product id.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkUpdateHighlightMediaResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "updateHighlightMedia",
        }
        body: dict[str, Any] = {
            k: v
            for k, v in {
                "cta_title": cta_title,
            }.items()
            if v is not None
        }
        if not isinstance(product_id, NotGiven):
            body["product_id"] = product_id
        _validate_model(
            body,
            {
                "name": "SdkUpdateHighlightMediaRequestApplicationJson",
                "required": [],
                "properties": {
                    "cta_title": {
                        "kind": "primitive",
                        "type": "string",
                        "constraints": {"maxLength": 510},
                    },
                    "product_id": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "integer",
                            "constraints": {"minimum": 1},
                        },
                    },
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return self._client.request(
            method="patch",
            path=("v2", "highlights", str(highlight), "media", str(media)),
            body=body,
            model=SdkUpdateHighlightMediaResponseValue200ApplicationJson,
            request_options=request_options,
        )

    def delete(
        self,
        highlight: int,
        media: int,
        *,
        request_options: RequestOptions | None = None,
    ) -> None:
        """Delete highlight media

        Delete highlight media for a highlight. Deleting the last media item unpublishes the highlight. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            highlight: The highlight path parameter.
            media: The media path parameter.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "deleteHighlightMedia",
        }
        self._client.request(
            method="delete",
            path=("v2", "highlights", str(highlight), "media", str(media)),
            request_options=request_options,
        )


class AsyncHighlightsMedia:
    """Highlights Media API resources (async)."""

    def __init__(self, client: AsyncSellAppClient) -> None:
        self._client = client
        self.with_raw_response = WithRawResponse(self)

    async def list(
        self,
        highlight: int,
        *,
        request_options: RequestOptions | None = None,
    ) -> SdkListHighlightMediaResponseValue200ApplicationJson:
        """List highlight media

        List media attached to a highlight in display order. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            highlight: The highlight path parameter.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkListHighlightMediaResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "listHighlightMedia",
        }
        return await self._client.request(
            method="get",
            path=("v2", "highlights", str(highlight), "media"),
            model=SdkListHighlightMediaResponseValue200ApplicationJson,
            request_options=request_options,
        )

    async def add(
        self,
        highlight: int,
        *,
        file: bytes,
        cta_title: str | None = None,
        product_id: int | None | NotGiven = NOT_GIVEN,
        request_options: RequestOptions | None = None,
    ) -> SdkAddHighlightMediaResponseValue201ApplicationJson:
        """Add highlight media

        Upload an image or video and append it to a highlight, up to 30 media items in total. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            highlight: The highlight path parameter.
            file: The file.
            cta_title: The cta title.
            product_id: The product id.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkAddHighlightMediaResponseValue201ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "form-data",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "addHighlightMedia",
        }
        body: dict[str, Any] = {
            k: v
            for k, v in {
                "file": file,
                "cta_title": cta_title,
            }.items()
            if v is not None
        }
        if not isinstance(product_id, NotGiven):
            body["product_id"] = product_id
        _validate_model(
            body,
            {
                "name": "SdkAddHighlightMediaRequestMultipartFormData",
                "required": ["file"],
                "properties": {
                    "file": {"kind": "primitive", "type": "string", "format": "binary"},
                    "cta_title": {
                        "kind": "primitive",
                        "type": "string",
                        "constraints": {"maxLength": 510},
                    },
                    "product_id": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "integer",
                            "constraints": {"minimum": 1},
                        },
                    },
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        multipart_files: dict[str, Any] = {}
        if "file" in body:
            multipart_files["file"] = body.pop("file")
        request_options = {
            **(request_options or {}),
            "files": {
                **multipart_files,
                **((request_options or {}).get("files") or {}),
            },
        }
        return await self._client.request(
            method="post",
            path=("v2", "highlights", str(highlight), "media"),
            body=body,
            model=SdkAddHighlightMediaResponseValue201ApplicationJson,
            request_options=request_options,
        )

    async def reorder(
        self,
        highlight: int,
        *,
        resources: builtins.list[int],
        request_options: RequestOptions | None = None,
    ) -> SdkReorderHighlightMediaResponseValue200ApplicationJson:
        """Reorder highlight media

        Replace the complete media order for a highlight with the supplied media IDs. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            highlight: The highlight path parameter.
            resources: The resources.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkReorderHighlightMediaResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "reorderHighlightMedia",
        }
        body: dict[str, Any] = {
            "resources": resources,
        }
        _validate_model(
            body,
            {
                "name": "SdkReorderHighlightsRequestApplicationJson",
                "required": ["resources"],
                "properties": {
                    "resources": {
                        "kind": "array",
                        "items": {"kind": "primitive", "type": "integer"},
                        "constraints": {"minItems": 1},
                    }
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return await self._client.request(
            method="patch",
            path=("v2", "highlights", str(highlight), "media", "reorder"),
            body=body,
            model=SdkReorderHighlightMediaResponseValue200ApplicationJson,
            request_options=request_options,
        )

    async def replace(
        self,
        highlight: int,
        media: int,
        *,
        file: bytes,
        cta_title: str | None = None,
        product_id: int | None | NotGiven = NOT_GIVEN,
        request_options: RequestOptions | None = None,
    ) -> SdkReplaceHighlightMediaResponseValue200ApplicationJson:
        """Replace highlight media

        Replace highlight media for a highlight. Deleting the last media item unpublishes the highlight. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            highlight: The highlight path parameter.
            media: The media path parameter.
            file: The file.
            cta_title: The cta title.
            product_id: The product id.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkReplaceHighlightMediaResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "form-data",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "replaceHighlightMedia",
        }
        body: dict[str, Any] = {
            k: v
            for k, v in {
                "file": file,
                "cta_title": cta_title,
            }.items()
            if v is not None
        }
        if not isinstance(product_id, NotGiven):
            body["product_id"] = product_id
        _validate_model(
            body,
            {
                "name": "SdkReplaceHighlightMediaRequestMultipartFormData",
                "required": ["file"],
                "properties": {
                    "file": {"kind": "primitive", "type": "string", "format": "binary"},
                    "cta_title": {
                        "kind": "primitive",
                        "type": "string",
                        "constraints": {"maxLength": 510},
                    },
                    "product_id": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "integer",
                            "constraints": {"minimum": 1},
                        },
                    },
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        multipart_files: dict[str, Any] = {}
        if "file" in body:
            multipart_files["file"] = body.pop("file")
        request_options = {
            **(request_options or {}),
            "files": {
                **multipart_files,
                **((request_options or {}).get("files") or {}),
            },
        }
        return await self._client.request(
            method="post",
            path=("v2", "highlights", str(highlight), "media", str(media)),
            body=body,
            model=SdkReplaceHighlightMediaResponseValue200ApplicationJson,
            request_options=request_options,
        )

    async def replace_metadata(
        self,
        highlight: int,
        media: int,
        *,
        cta_title: str | None = None,
        product_id: int | None | NotGiven = NOT_GIVEN,
        request_options: RequestOptions | None = None,
    ) -> SdkReplaceHighlightMediaMetadataResponseValue200ApplicationJson:
        """Update highlight media

        Update highlight media for a highlight. Deleting the last media item unpublishes the highlight. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            highlight: The highlight path parameter.
            media: The media path parameter.
            cta_title: The cta title.
            product_id: The product id.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkReplaceHighlightMediaMetadataResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "replaceHighlightMediaMetadata",
        }
        body: dict[str, Any] = {
            k: v
            for k, v in {
                "cta_title": cta_title,
            }.items()
            if v is not None
        }
        if not isinstance(product_id, NotGiven):
            body["product_id"] = product_id
        _validate_model(
            body,
            {
                "name": "SdkReplaceHighlightMediaMetadataRequestApplicationJson",
                "required": [],
                "properties": {
                    "cta_title": {
                        "kind": "primitive",
                        "type": "string",
                        "constraints": {"maxLength": 510},
                    },
                    "product_id": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "integer",
                            "constraints": {"minimum": 1},
                        },
                    },
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return await self._client.request(
            method="put",
            path=("v2", "highlights", str(highlight), "media", str(media)),
            body=body,
            model=SdkReplaceHighlightMediaMetadataResponseValue200ApplicationJson,
            request_options=request_options,
        )

    async def update(
        self,
        highlight: int,
        media: int,
        *,
        cta_title: str | None = None,
        product_id: int | None | NotGiven = NOT_GIVEN,
        request_options: RequestOptions | None = None,
    ) -> SdkUpdateHighlightMediaResponseValue200ApplicationJson:
        """Update highlight media

        Update highlight media for a highlight. Deleting the last media item unpublishes the highlight. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            highlight: The highlight path parameter.
            media: The media path parameter.
            cta_title: The cta title.
            product_id: The product id.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkUpdateHighlightMediaResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "updateHighlightMedia",
        }
        body: dict[str, Any] = {
            k: v
            for k, v in {
                "cta_title": cta_title,
            }.items()
            if v is not None
        }
        if not isinstance(product_id, NotGiven):
            body["product_id"] = product_id
        _validate_model(
            body,
            {
                "name": "SdkUpdateHighlightMediaRequestApplicationJson",
                "required": [],
                "properties": {
                    "cta_title": {
                        "kind": "primitive",
                        "type": "string",
                        "constraints": {"maxLength": 510},
                    },
                    "product_id": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "integer",
                            "constraints": {"minimum": 1},
                        },
                    },
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return await self._client.request(
            method="patch",
            path=("v2", "highlights", str(highlight), "media", str(media)),
            body=body,
            model=SdkUpdateHighlightMediaResponseValue200ApplicationJson,
            request_options=request_options,
        )

    async def delete(
        self,
        highlight: int,
        media: int,
        *,
        request_options: RequestOptions | None = None,
    ) -> None:
        """Delete highlight media

        Delete highlight media for a highlight. Deleting the last media item unpublishes the highlight. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            highlight: The highlight path parameter.
            media: The media path parameter.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "deleteHighlightMedia",
        }
        await self._client.request(
            method="delete",
            path=("v2", "highlights", str(highlight), "media", str(media)),
            request_options=request_options,
        )
