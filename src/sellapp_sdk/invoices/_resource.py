# This file is auto-generated by oagen. Do not edit.

from __future__ import annotations

from typing import TYPE_CHECKING, Any, Dict, List, Optional, Union

from .._base_client import WithRawResponse

if TYPE_CHECKING:
    from .._client import AsyncSellAppClient, SellAppClient

from sellapp_sdk.common.models.create_invoice_request_application_json_property_extra import (
    CreateInvoiceRequestApplicationJsonPropertyExtra,
)
from sellapp_sdk.common.models.expected_status import ExpectedStatus
from sellapp_sdk.common.models.invoices_search_by import InvoicesSearchBy
from sellapp_sdk.common.models.invoices_sort import InvoicesSort
from sellapp_sdk.common.models.sdk_create_invoice_request_application_json_payment_method import (
    SdkCreateInvoiceRequestApplicationJsonPaymentMethod,
)
from sellapp_sdk.common.models.sdk_search_invoices_request_application_json_payment_methods import (
    SdkSearchInvoicesRequestApplicationJsonPaymentMethods,
)
from sellapp_sdk.common.models.sdk_search_invoices_request_application_json_search_by import (
    SdkSearchInvoicesRequestApplicationJsonSearchBy,
)
from sellapp_sdk.common.models.sdk_search_invoices_request_application_json_sort import (
    SdkSearchInvoicesRequestApplicationJsonSort,
)
from sellapp_sdk.common.models.sdk_search_invoices_request_application_json_status import (
    SdkSearchInvoicesRequestApplicationJsonStatus,
)
from sellapp_sdk.common.models.sdk_update_invoice_status_request_application_json_status import (
    SdkUpdateInvoiceStatusRequestApplicationJsonStatus,
)

from .._pagination import AsyncPage, SyncPage
from .._types import NOT_GIVEN, NotGiven, RequestOptions, _validate_model, enum_value
from .models import (
    CreateInvoiceRequestApplicationJsonPropertyProductVariants,
    InvoicesPaymentMethods,
    InvoicesStatus,
    IssueReplacementForCompletedInvoiceRequestApplicationJsonPropertyProductVariants,
    ListInvoicesResponseValue200ApplicationJsonPropertyDataItem,
    SdkCreateCheckoutSessionResponseValue200ApplicationJson,
    SdkCreateDynamicDeliveryRetryResponseValue200ApplicationJson,
    SdkCreateFulfillmentNotificationsResponseValue200ApplicationJson,
    SdkCreateFulfillmentRetryResponseValue200ApplicationJson,
    SdkCreateInvoiceRefundResponseValue200ApplicationJson,
    SdkCreateInvoiceResponseValue201ApplicationJson,
    SdkGetInvoiceDeliverablesResponseValue200ApplicationJson,
    SdkGetInvoiceResponseValue200ApplicationJson,
    SdkIssueReplacementForCompletedInvoiceResponseValue200ApplicationJson,
    SdkMarkPendingInvoiceCompletedResponseValue200ApplicationJson,
    SdkMarkPendingInvoiceVoidedResponseValue200ApplicationJson,
    SdkUpdateInvoiceStatusResponseValue200ApplicationJson,
    SearchInvoicesResponseValue200ApplicationJsonPropertyDataItem,
)


class Invoices:
    """Invoices API resources."""

    def __init__(self, client: SellAppClient) -> None:
        self._client = client
        self.with_raw_response = WithRawResponse(self)

    def list(
        self,
        *,
        limit: Optional[int] = None,
        before: Optional[str] = None,
        after: Optional[str] = None,
        order: Optional[str] = None,
        page: Optional[int] = None,
        search: Optional[str] = None,
        search_by: Optional[Union[InvoicesSearchBy, str]] = None,
        id: Optional[str] = None,
        email: Optional[str] = None,
        transaction_id: Optional[str] = None,
        serial_code: Optional[str] = None,
        additional_info: Optional[str] = None,
        product_name: Optional[str] = None,
        discord_data: Optional[str] = None,
        crypto_txid: Optional[str] = None,
        crypto_address: Optional[str] = None,
        coupon_code: Optional[str] = None,
        status: Optional[List[Union[InvoicesStatus, str]]] = None,
        payment_methods: Optional[List[Union[InvoicesPaymentMethods, str]]] = None,
        sort: Optional[Union[InvoicesSort, str]] = None,
        request_options: Optional[RequestOptions] = None,
    ) -> SyncPage[ListInvoicesResponseValue200ApplicationJsonPropertyDataItem]:
        """List all invoices

        List your store's invoices, 15 per page by default. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            limit: Number of items to return per page. Defaults to `15`.
            before: The before.
            after: The after.
            order: The order.
            page: Page number to return. Defaults to `1`.
            search: Free-text search term.
            search_by: Which invoice field the search term should be matched against.
            id: Filter by invoice ID.
            email: Filter by customer email.
            transaction_id: Filter by payment transaction ID.
            serial_code: Filter by delivered serial.
            additional_info: Filter by customer-provided additional information.
            product_name: Filter by product or variant title.
            discord_data: Filter by attached Discord data.
            crypto_txid: Filter by crypto TXID.
            crypto_address: Filter by crypto payment address.
            coupon_code: Filter by coupon code.
            status: Filter by one or more invoice statuses.
            payment_methods: Filter by one or more payment methods.
            sort: Sort order for the result set.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SyncPage[ListInvoicesResponseValue200ApplicationJsonPropertyDataItem]

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "listInvoices",
        }
        params = {
            k: v
            for k, v in {
                "limit": limit,
                "before": before,
                "after": after,
                "order": order,
                "page": page,
                "search": search,
                "search_by": enum_value(search_by) if search_by is not None else None,
                "id": id,
                "email": email,
                "transaction_id": transaction_id,
                "serial_code": serial_code,
                "additional_info": additional_info,
                "product_name": product_name,
                "discord_data": discord_data,
                "crypto_txid": crypto_txid,
                "crypto_address": crypto_address,
                "coupon_code": coupon_code,
                "status": status,
                "payment_methods": payment_methods,
                "sort": enum_value(sort) if sort is not None else None,
            }.items()
            if v is not None
        }
        return self._client.request_page(
            method="get",
            path=("v2", "invoices"),
            model=ListInvoicesResponseValue200ApplicationJsonPropertyDataItem,
            params=params,
            request_options=request_options,
        )

    def create(
        self,
        *,
        customer_email: str,
        payment_method: Union[SdkCreateInvoiceRequestApplicationJsonPaymentMethod, str],
        product_variants: CreateInvoiceRequestApplicationJsonPropertyProductVariants,
        customer_ip: Union[str, None, NotGiven] = NOT_GIVEN,
        coupon: Optional[str] = None,
        vat_id: Optional[str] = None,
        country: Optional[str] = None,
        affiliate: Union[str, None, NotGiven] = NOT_GIVEN,
        extra: Optional[CreateInvoiceRequestApplicationJsonPropertyExtra] = None,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkCreateInvoiceResponseValue201ApplicationJson:
        """Create an invoice

        Create an invoice for one or more product variants. Credit products are direct-only: send a credits variant by itself, use a quantity covered by its credit rate tiers, and respect the variant quantity increment rules. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            customer_email: The customer email.
            customer_ip: The customer ip.
            payment_method: The payment method.
            coupon: The coupon.
            vat_id: The vat id.
            country: The country.
            affiliate: The affiliate.
            extra: The extra.
            product_variants: The product variants.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkCreateInvoiceResponseValue201ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "createInvoice",
        }
        body: Dict[str, Any] = {
            k: v
            for k, v in {
                "customer_email": customer_email,
                "payment_method": enum_value(payment_method),
                "coupon": coupon,
                "vat_id": vat_id,
                "country": country,
                "extra": extra.to_dict() if extra is not None else None,
                "product_variants": product_variants.to_dict(),
            }.items()
            if v is not None
        }
        if not isinstance(customer_ip, NotGiven):
            body["customer_ip"] = customer_ip
        if not isinstance(affiliate, NotGiven):
            body["affiliate"] = affiliate
        _validate_model(
            body,
            {
                "name": "SdkCreateInvoiceRequestApplicationJson",
                "required": ["customer_email", "payment_method", "product_variants"],
                "properties": {
                    "customer_email": {
                        "kind": "primitive",
                        "type": "string",
                        "format": "email",
                    },
                    "customer_ip": {
                        "kind": "nullable",
                        "inner": {"kind": "primitive", "type": "string"},
                    },
                    "payment_method": {
                        "kind": "enum",
                        "values": [
                            "AUTHNET",
                            "BTCPAY",
                            "CASHAPP",
                            "COINBASE",
                            "PADDLE",
                            "PAYDASH",
                            "PAYPAL",
                            "PAYSTACK",
                            "SQUARE",
                            "STRIPE",
                            "VENMO",
                            "NMI",
                            "MERCADO_PAGO",
                            "MOLLIE",
                            "RAZORPAY",
                            "CUSTOM_PAYMENT_METHOD",
                            "LIFI",
                            "BTC",
                            "LTC",
                            "ETH",
                            "XMR",
                            "SOL",
                            "ADA",
                            "BNB",
                            "TRX",
                            "MATIC",
                            "ETH_USDT",
                            "ETH_USDC",
                            "ETH_UNI",
                            "ETH_SHIB",
                            "ETH_DAI",
                            "BNB_USDT",
                            "BNB_USDC",
                            "TRX_USDT",
                            "TRX_USDC",
                            "SOL_USDT",
                            "SOL_USDC",
                        ],
                    },
                    "coupon": {"kind": "primitive", "type": "string"},
                    "vat_id": {"kind": "primitive", "type": "string"},
                    "country": {"kind": "primitive", "type": "string"},
                    "affiliate": {
                        "kind": "nullable",
                        "inner": {"kind": "primitive", "type": "string"},
                    },
                    "extra": {"kind": "any"},
                    "product_variants": {"kind": "any"},
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return self._client.request(
            method="post",
            path=("v2", "invoices"),
            body=body,
            model=SdkCreateInvoiceResponseValue201ApplicationJson,
            request_options=request_options,
        )

    def search(
        self,
        *,
        search: Optional[str] = None,
        search_by: Optional[
            Union[SdkSearchInvoicesRequestApplicationJsonSearchBy, str]
        ] = None,
        id: Optional[str] = None,
        email: Optional[str] = None,
        transaction_id: Optional[str] = None,
        serial_code: Optional[str] = None,
        additional_info: Optional[str] = None,
        product_name: Optional[str] = None,
        discord_data: Optional[str] = None,
        crypto_txid: Optional[str] = None,
        crypto_address: Optional[str] = None,
        coupon_code: Optional[str] = None,
        status: Optional[
            List[Union[SdkSearchInvoicesRequestApplicationJsonStatus, str]]
        ] = None,
        payment_methods: Optional[
            List[Union[SdkSearchInvoicesRequestApplicationJsonPaymentMethods, str]]
        ] = None,
        sort: Optional[Union[SdkSearchInvoicesRequestApplicationJsonSort, str]] = None,
        limit: Optional[int] = None,
        before: Optional[str] = None,
        after: Optional[str] = None,
        order: Optional[str] = None,
        request_options: Optional[RequestOptions] = None,
    ) -> SyncPage[SearchInvoicesResponseValue200ApplicationJsonPropertyDataItem]:
        """Search invoices

        Search invoices with the same filters as the list endpoint, sent in a JSON body instead of query parameters. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            search: The search.
            search_by: The search by.
            id: The id.
            email: The email.
            transaction_id: The transaction id.
            serial_code: The serial code.
            additional_info: The additional info.
            product_name: The product name.
            discord_data: The discord data.
            crypto_txid: The crypto txid.
            crypto_address: The crypto address.
            coupon_code: The coupon code.
            status: The status.
            payment_methods: The payment methods.
            sort: The sort.
            limit: The limit.
            before: The before.
            after: The after.
            order: The order.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SyncPage[SearchInvoicesResponseValue200ApplicationJsonPropertyDataItem]

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "searchInvoices",
        }
        body: Dict[str, Any] = {
            k: v
            for k, v in {
                "search": search,
                "search_by": enum_value(search_by) if search_by is not None else None,
                "id": id,
                "email": email,
                "transaction_id": transaction_id,
                "serial_code": serial_code,
                "additional_info": additional_info,
                "product_name": product_name,
                "discord_data": discord_data,
                "crypto_txid": crypto_txid,
                "crypto_address": crypto_address,
                "coupon_code": coupon_code,
                "status": status,
                "payment_methods": payment_methods,
                "sort": enum_value(sort) if sort is not None else None,
            }.items()
            if v is not None
        }
        params = {
            k: v
            for k, v in {
                "limit": limit,
                "before": before,
                "after": after,
                "order": order,
            }.items()
            if v is not None
        }
        return self._client.request_page(
            method="post",
            path=("v2", "invoices", "search"),
            model=SearchInvoicesResponseValue200ApplicationJsonPropertyDataItem,
            body=body,
            params=params,
            request_options=request_options,
        )

    def get(
        self,
        invoice: int,
        *,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkGetInvoiceResponseValue200ApplicationJson:
        """Retrieve an invoice

        Retrieve an invoice by its ID to check its current payment and delivery state. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            invoice: The invoice path parameter.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkGetInvoiceResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "getInvoice",
        }
        return self._client.request(
            method="get",
            path=("v2", "invoices", str(invoice)),
            model=SdkGetInvoiceResponseValue200ApplicationJson,
            request_options=request_options,
        )

    def go_to_checkout(
        self,
        invoice: int,
        *,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkCreateCheckoutSessionResponseValue200ApplicationJson:
        """Create a checkout session

        Create or reuse a payment session for a pending invoice. Create the invoice first, then send the customer to the returned top-level `payment_url`. A zero-priced checkout can return a paid invoice without a URL; delivery may still be queued. Check for a URL before redirecting and verify payment through signed events or a fresh read. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            invoice: The invoice path parameter.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkCreateCheckoutSessionResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "createCheckoutSession",
        }
        return self._client.request(
            method="post",
            path=("v2", "invoices", str(invoice), "checkout"),
            model=SdkCreateCheckoutSessionResponseValue200ApplicationJson,
            request_options=request_options,
        )

    def get_deliverables(
        self,
        invoice: int,
        *,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkGetInvoiceDeliverablesResponseValue200ApplicationJson:
        """View invoice deliverables

        Retrieve the deliverables sent to the customer, including each product in a multi-product purchase. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            invoice: The invoice path parameter.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkGetInvoiceDeliverablesResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "getInvoiceDeliverables",
        }
        return self._client.request(
            method="get",
            path=("v2", "invoices", str(invoice), "deliverables"),
            model=SdkGetInvoiceDeliverablesResponseValue200ApplicationJson,
            request_options=request_options,
        )

    def mark_completed(
        self,
        invoice: int,
        *,
        expected_status: Union[Union[ExpectedStatus, str], None, NotGiven] = NOT_GIVEN,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkMarkPendingInvoiceCompletedResponseValue200ApplicationJson:
        """Mark pending invoice completed

        Mark a pending invoice completed and start the normal delivery flow. SellApp normally handles completion after payment is confirmed. Use this only after independently confirming payment or deliberately authorizing delivery without it; the action can release purchased products. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            invoice: The invoice path parameter.
            expected_status: Optional optimistic-concurrency guard. The mutation fails when the current order status no longer matches this value.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkMarkPendingInvoiceCompletedResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "markPendingInvoiceCompleted",
        }
        body: Dict[str, Any] = {}
        if not isinstance(expected_status, NotGiven):
            body["expected_status"] = (
                enum_value(expected_status) if expected_status is not None else None
            )
        _validate_model(
            body,
            {
                "name": "SdkMarkPendingInvoiceCompletedRequestApplicationJson",
                "required": [],
                "properties": {
                    "expected_status": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "enum",
                            "values": [
                                "PENDING",
                                "REVERSED",
                                "PARTIAL",
                                "VOIDED",
                                "REFUNDED",
                                "PAID",
                                "COMPLETED",
                                "DISPUTING",
                                "REVIEW",
                                "FAILED",
                                "null",
                            ],
                        },
                    }
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return self._client.request(
            method="patch",
            path=("v2", "invoices", str(invoice), "mark-completed"),
            body=body,
            model=SdkMarkPendingInvoiceCompletedResponseValue200ApplicationJson,
            request_options=request_options,
        )

    def mark_voided(
        self,
        invoice: int,
        *,
        expected_status: Union[Union[ExpectedStatus, str], None, NotGiven] = NOT_GIVEN,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkMarkPendingInvoiceVoidedResponseValue200ApplicationJson:
        """Mark pending invoice voided

        Void a pending invoice so it does not proceed to product delivery. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            invoice: The invoice path parameter.
            expected_status: Optional optimistic-concurrency guard. The mutation fails when the current order status no longer matches this value.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkMarkPendingInvoiceVoidedResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "markPendingInvoiceVoided",
        }
        body: Dict[str, Any] = {}
        if not isinstance(expected_status, NotGiven):
            body["expected_status"] = (
                enum_value(expected_status) if expected_status is not None else None
            )
        _validate_model(
            body,
            {
                "name": "SdkMarkPendingInvoiceVoidedRequestApplicationJson",
                "required": [],
                "properties": {
                    "expected_status": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "enum",
                            "values": [
                                "PENDING",
                                "REVERSED",
                                "PARTIAL",
                                "VOIDED",
                                "REFUNDED",
                                "PAID",
                                "COMPLETED",
                                "DISPUTING",
                                "REVIEW",
                                "FAILED",
                                "null",
                            ],
                        },
                    }
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return self._client.request(
            method="patch",
            path=("v2", "invoices", str(invoice), "mark-voided"),
            body=body,
            model=SdkMarkPendingInvoiceVoidedResponseValue200ApplicationJson,
            request_options=request_options,
        )

    def issue_replacement(
        self,
        invoice: int,
        *,
        product_variants: IssueReplacementForCompletedInvoiceRequestApplicationJsonPropertyProductVariants,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkIssueReplacementForCompletedInvoiceResponseValue200ApplicationJson:
        """Issue replacement for completed invoice

        Issue replacement deliverables for a completed purchase. This creates a new invoice and marks it completed, then starts the normal delivery and sales-notification flow. It is not just an edit to the old delivery. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            invoice: The invoice path parameter.
            product_variants: The product variants.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkIssueReplacementForCompletedInvoiceResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "issueReplacementForCompletedInvoice",
        }
        body: Dict[str, Any] = {
            "product_variants": product_variants.to_dict(),
        }
        _validate_model(
            body,
            {
                "name": "SdkIssueReplacementForCompletedInvoiceRequestApplicationJson",
                "required": ["product_variants"],
                "properties": {"product_variants": {"kind": "any"}},
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return self._client.request(
            method="patch",
            path=("v2", "invoices", str(invoice), "issue-replacement"),
            body=body,
            model=SdkIssueReplacementForCompletedInvoiceResponseValue200ApplicationJson,
            request_options=request_options,
        )

    def update_status(
        self,
        invoice: int,
        *,
        status: Union[SdkUpdateInvoiceStatusRequestApplicationJsonStatus, str],
        expected_status: Union[Union[ExpectedStatus, str], None, NotGiven] = NOT_GIVEN,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkUpdateInvoiceStatusResponseValue200ApplicationJson:
        """Update invoice status

        Change a purchase through the invoice compatibility operation. Set status to COMPLETED, VOIDED, REVIEW, or DISPUTING. Dispute transitions schedule community-access revocation and a dispute webhook. Requires the `invoice` credential ability and store ownership. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            invoice: The invoice path parameter.
            status: The status.
            expected_status: Optional optimistic-concurrency guard. The mutation fails when the current order status no longer matches this value.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkUpdateInvoiceStatusResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "updateInvoiceStatus",
        }
        body: Dict[str, Any] = {
            "status": enum_value(status),
        }
        if not isinstance(expected_status, NotGiven):
            body["expected_status"] = (
                enum_value(expected_status) if expected_status is not None else None
            )
        _validate_model(
            body,
            {
                "name": "SdkUpdateInvoiceStatusRequestApplicationJson",
                "required": ["status"],
                "properties": {
                    "status": {
                        "kind": "enum",
                        "values": ["COMPLETED", "VOIDED", "REVIEW", "DISPUTING"],
                    },
                    "expected_status": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "enum",
                            "values": [
                                "PENDING",
                                "REVERSED",
                                "PARTIAL",
                                "VOIDED",
                                "REFUNDED",
                                "PAID",
                                "COMPLETED",
                                "DISPUTING",
                                "REVIEW",
                                "FAILED",
                                "null",
                            ],
                        },
                    },
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return self._client.request(
            method="patch",
            path=("v2", "invoices", str(invoice), "status"),
            body=body,
            model=SdkUpdateInvoiceStatusResponseValue200ApplicationJson,
            request_options=request_options,
        )

    def create_refund(
        self,
        invoice: int,
        *,
        amount: Union[str, None, NotGiven] = NOT_GIVEN,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkCreateInvoiceRefundResponseValue200ApplicationJson:
        """Create invoice refund

        Request a full or partial provider refund using the same idempotent refund ledger as the dashboard. Amounts are decimal currency strings, never floating-point numbers. Requires the `invoice` token ability. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            invoice: The invoice path parameter.
            amount: Optional partial refund amount in major currency units. Omit for the full remaining refundable amount.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkCreateInvoiceRefundResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "createInvoiceRefund",
        }
        body: Dict[str, Any] = {}
        if not isinstance(amount, NotGiven):
            body["amount"] = amount
        _validate_model(
            body,
            {
                "name": "SdkCreateInvoiceRefundRequestApplicationJson",
                "required": [],
                "properties": {
                    "amount": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "string",
                            "constraints": {
                                "maxLength": 32,
                                "pattern": "^\\d+(\\.\\d+)?$",
                            },
                        },
                    }
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return self._client.request(
            method="post",
            path=("v2", "invoices", str(invoice), "refunds"),
            body=body,
            model=SdkCreateInvoiceRefundResponseValue200ApplicationJson,
            request_options=request_options,
        )

    def retry_fulfillment(
        self,
        invoice: int,
        *,
        email: Union[str, None, NotGiven] = NOT_GIVEN,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkCreateFulfillmentRetryResponseValue200ApplicationJson:
        """Create fulfillment retry

        Retry failed delivery steps for a paid or partially fulfilled purchase. Repeated requests retry the same failed steps without creating duplicate work. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            invoice: The invoice path parameter.
            email: Optional replacement delivery email.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkCreateFulfillmentRetryResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "createFulfillmentRetry",
        }
        body: Dict[str, Any] = {}
        if not isinstance(email, NotGiven):
            body["email"] = email
        _validate_model(
            body,
            {
                "name": "SdkCreateFulfillmentRetryRequestApplicationJson",
                "required": [],
                "properties": {
                    "email": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "string",
                            "format": "email",
                            "constraints": {"maxLength": 254},
                        },
                    }
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return self._client.request(
            method="post",
            path=("v2", "invoices", str(invoice), "fulfillment-retries"),
            body=body,
            model=SdkCreateFulfillmentRetryResponseValue200ApplicationJson,
            request_options=request_options,
        )

    def retry_dynamic_delivery(
        self,
        invoice: int,
        *,
        delivered_product_id: int,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkCreateDynamicDeliveryRetryResponseValue200ApplicationJson:
        """Create dynamic delivery retry

        Retry one same-store dynamic delivered product after validating order, line-item, variant, and webhook ownership. Callers should avoid concurrent retries for the same delivered product. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            invoice: The invoice path parameter.
            delivered_product_id: The delivered product id.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkCreateDynamicDeliveryRetryResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "createDynamicDeliveryRetry",
        }
        body: Dict[str, Any] = {
            "delivered_product_id": delivered_product_id,
        }
        _validate_model(
            body,
            {
                "name": "SdkCreateDynamicDeliveryRetryRequestApplicationJson",
                "required": ["delivered_product_id"],
                "properties": {
                    "delivered_product_id": {
                        "kind": "primitive",
                        "type": "integer",
                        "constraints": {"minimum": 1},
                    }
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return self._client.request(
            method="post",
            path=("v2", "invoices", str(invoice), "dynamic-delivery-retries"),
            body=body,
            model=SdkCreateDynamicDeliveryRetryResponseValue200ApplicationJson,
            request_options=request_options,
        )

    def notify_fulfillment(
        self,
        invoice: int,
        *,
        email: Union[str, None, NotGiven] = NOT_GIVEN,
        product_variant_ids: Union[List[int], None, NotGiven] = NOT_GIVEN,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkCreateFulfillmentNotificationsResponseValue200ApplicationJson:
        """Create fulfillment notifications

        Resend fulfillment notifications for all or selected same-store product variants on a completed invoice. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            invoice: The invoice path parameter.
            email: The email.
            product_variant_ids: The product variant ids.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkCreateFulfillmentNotificationsResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "createFulfillmentNotifications",
        }
        body: Dict[str, Any] = {}
        if not isinstance(email, NotGiven):
            body["email"] = email
        if not isinstance(product_variant_ids, NotGiven):
            body["product_variant_ids"] = product_variant_ids
        _validate_model(
            body,
            {
                "name": "SdkCreateFulfillmentNotificationsRequestApplicationJson",
                "required": [],
                "properties": {
                    "email": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "string",
                            "format": "email",
                            "constraints": {"maxLength": 254},
                        },
                    },
                    "product_variant_ids": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "array",
                            "items": {
                                "kind": "primitive",
                                "type": "integer",
                                "constraints": {"minimum": 1},
                            },
                            "constraints": {"minItems": 1, "uniqueItems": True},
                        },
                    },
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return self._client.request(
            method="post",
            path=("v2", "invoices", str(invoice), "fulfillment-notifications"),
            body=body,
            model=SdkCreateFulfillmentNotificationsResponseValue200ApplicationJson,
            request_options=request_options,
        )


class AsyncInvoices:
    """Invoices API resources (async)."""

    def __init__(self, client: AsyncSellAppClient) -> None:
        self._client = client
        self.with_raw_response = WithRawResponse(self)

    async def list(
        self,
        *,
        limit: Optional[int] = None,
        before: Optional[str] = None,
        after: Optional[str] = None,
        order: Optional[str] = None,
        page: Optional[int] = None,
        search: Optional[str] = None,
        search_by: Optional[Union[InvoicesSearchBy, str]] = None,
        id: Optional[str] = None,
        email: Optional[str] = None,
        transaction_id: Optional[str] = None,
        serial_code: Optional[str] = None,
        additional_info: Optional[str] = None,
        product_name: Optional[str] = None,
        discord_data: Optional[str] = None,
        crypto_txid: Optional[str] = None,
        crypto_address: Optional[str] = None,
        coupon_code: Optional[str] = None,
        status: Optional[List[Union[InvoicesStatus, str]]] = None,
        payment_methods: Optional[List[Union[InvoicesPaymentMethods, str]]] = None,
        sort: Optional[Union[InvoicesSort, str]] = None,
        request_options: Optional[RequestOptions] = None,
    ) -> AsyncPage[ListInvoicesResponseValue200ApplicationJsonPropertyDataItem]:
        """List all invoices

        List your store's invoices, 15 per page by default. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            limit: Number of items to return per page. Defaults to `15`.
            before: The before.
            after: The after.
            order: The order.
            page: Page number to return. Defaults to `1`.
            search: Free-text search term.
            search_by: Which invoice field the search term should be matched against.
            id: Filter by invoice ID.
            email: Filter by customer email.
            transaction_id: Filter by payment transaction ID.
            serial_code: Filter by delivered serial.
            additional_info: Filter by customer-provided additional information.
            product_name: Filter by product or variant title.
            discord_data: Filter by attached Discord data.
            crypto_txid: Filter by crypto TXID.
            crypto_address: Filter by crypto payment address.
            coupon_code: Filter by coupon code.
            status: Filter by one or more invoice statuses.
            payment_methods: Filter by one or more payment methods.
            sort: Sort order for the result set.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            AsyncPage[ListInvoicesResponseValue200ApplicationJsonPropertyDataItem]

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "listInvoices",
        }
        params = {
            k: v
            for k, v in {
                "limit": limit,
                "before": before,
                "after": after,
                "order": order,
                "page": page,
                "search": search,
                "search_by": enum_value(search_by) if search_by is not None else None,
                "id": id,
                "email": email,
                "transaction_id": transaction_id,
                "serial_code": serial_code,
                "additional_info": additional_info,
                "product_name": product_name,
                "discord_data": discord_data,
                "crypto_txid": crypto_txid,
                "crypto_address": crypto_address,
                "coupon_code": coupon_code,
                "status": status,
                "payment_methods": payment_methods,
                "sort": enum_value(sort) if sort is not None else None,
            }.items()
            if v is not None
        }
        return await self._client.request_page(
            method="get",
            path=("v2", "invoices"),
            model=ListInvoicesResponseValue200ApplicationJsonPropertyDataItem,
            params=params,
            request_options=request_options,
        )

    async def create(
        self,
        *,
        customer_email: str,
        payment_method: Union[SdkCreateInvoiceRequestApplicationJsonPaymentMethod, str],
        product_variants: CreateInvoiceRequestApplicationJsonPropertyProductVariants,
        customer_ip: Union[str, None, NotGiven] = NOT_GIVEN,
        coupon: Optional[str] = None,
        vat_id: Optional[str] = None,
        country: Optional[str] = None,
        affiliate: Union[str, None, NotGiven] = NOT_GIVEN,
        extra: Optional[CreateInvoiceRequestApplicationJsonPropertyExtra] = None,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkCreateInvoiceResponseValue201ApplicationJson:
        """Create an invoice

        Create an invoice for one or more product variants. Credit products are direct-only: send a credits variant by itself, use a quantity covered by its credit rate tiers, and respect the variant quantity increment rules. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            customer_email: The customer email.
            customer_ip: The customer ip.
            payment_method: The payment method.
            coupon: The coupon.
            vat_id: The vat id.
            country: The country.
            affiliate: The affiliate.
            extra: The extra.
            product_variants: The product variants.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkCreateInvoiceResponseValue201ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "createInvoice",
        }
        body: Dict[str, Any] = {
            k: v
            for k, v in {
                "customer_email": customer_email,
                "payment_method": enum_value(payment_method),
                "coupon": coupon,
                "vat_id": vat_id,
                "country": country,
                "extra": extra.to_dict() if extra is not None else None,
                "product_variants": product_variants.to_dict(),
            }.items()
            if v is not None
        }
        if not isinstance(customer_ip, NotGiven):
            body["customer_ip"] = customer_ip
        if not isinstance(affiliate, NotGiven):
            body["affiliate"] = affiliate
        _validate_model(
            body,
            {
                "name": "SdkCreateInvoiceRequestApplicationJson",
                "required": ["customer_email", "payment_method", "product_variants"],
                "properties": {
                    "customer_email": {
                        "kind": "primitive",
                        "type": "string",
                        "format": "email",
                    },
                    "customer_ip": {
                        "kind": "nullable",
                        "inner": {"kind": "primitive", "type": "string"},
                    },
                    "payment_method": {
                        "kind": "enum",
                        "values": [
                            "AUTHNET",
                            "BTCPAY",
                            "CASHAPP",
                            "COINBASE",
                            "PADDLE",
                            "PAYDASH",
                            "PAYPAL",
                            "PAYSTACK",
                            "SQUARE",
                            "STRIPE",
                            "VENMO",
                            "NMI",
                            "MERCADO_PAGO",
                            "MOLLIE",
                            "RAZORPAY",
                            "CUSTOM_PAYMENT_METHOD",
                            "LIFI",
                            "BTC",
                            "LTC",
                            "ETH",
                            "XMR",
                            "SOL",
                            "ADA",
                            "BNB",
                            "TRX",
                            "MATIC",
                            "ETH_USDT",
                            "ETH_USDC",
                            "ETH_UNI",
                            "ETH_SHIB",
                            "ETH_DAI",
                            "BNB_USDT",
                            "BNB_USDC",
                            "TRX_USDT",
                            "TRX_USDC",
                            "SOL_USDT",
                            "SOL_USDC",
                        ],
                    },
                    "coupon": {"kind": "primitive", "type": "string"},
                    "vat_id": {"kind": "primitive", "type": "string"},
                    "country": {"kind": "primitive", "type": "string"},
                    "affiliate": {
                        "kind": "nullable",
                        "inner": {"kind": "primitive", "type": "string"},
                    },
                    "extra": {"kind": "any"},
                    "product_variants": {"kind": "any"},
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return await self._client.request(
            method="post",
            path=("v2", "invoices"),
            body=body,
            model=SdkCreateInvoiceResponseValue201ApplicationJson,
            request_options=request_options,
        )

    async def search(
        self,
        *,
        search: Optional[str] = None,
        search_by: Optional[
            Union[SdkSearchInvoicesRequestApplicationJsonSearchBy, str]
        ] = None,
        id: Optional[str] = None,
        email: Optional[str] = None,
        transaction_id: Optional[str] = None,
        serial_code: Optional[str] = None,
        additional_info: Optional[str] = None,
        product_name: Optional[str] = None,
        discord_data: Optional[str] = None,
        crypto_txid: Optional[str] = None,
        crypto_address: Optional[str] = None,
        coupon_code: Optional[str] = None,
        status: Optional[
            List[Union[SdkSearchInvoicesRequestApplicationJsonStatus, str]]
        ] = None,
        payment_methods: Optional[
            List[Union[SdkSearchInvoicesRequestApplicationJsonPaymentMethods, str]]
        ] = None,
        sort: Optional[Union[SdkSearchInvoicesRequestApplicationJsonSort, str]] = None,
        limit: Optional[int] = None,
        before: Optional[str] = None,
        after: Optional[str] = None,
        order: Optional[str] = None,
        request_options: Optional[RequestOptions] = None,
    ) -> AsyncPage[SearchInvoicesResponseValue200ApplicationJsonPropertyDataItem]:
        """Search invoices

        Search invoices with the same filters as the list endpoint, sent in a JSON body instead of query parameters. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            search: The search.
            search_by: The search by.
            id: The id.
            email: The email.
            transaction_id: The transaction id.
            serial_code: The serial code.
            additional_info: The additional info.
            product_name: The product name.
            discord_data: The discord data.
            crypto_txid: The crypto txid.
            crypto_address: The crypto address.
            coupon_code: The coupon code.
            status: The status.
            payment_methods: The payment methods.
            sort: The sort.
            limit: The limit.
            before: The before.
            after: The after.
            order: The order.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            AsyncPage[SearchInvoicesResponseValue200ApplicationJsonPropertyDataItem]

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "searchInvoices",
        }
        body: Dict[str, Any] = {
            k: v
            for k, v in {
                "search": search,
                "search_by": enum_value(search_by) if search_by is not None else None,
                "id": id,
                "email": email,
                "transaction_id": transaction_id,
                "serial_code": serial_code,
                "additional_info": additional_info,
                "product_name": product_name,
                "discord_data": discord_data,
                "crypto_txid": crypto_txid,
                "crypto_address": crypto_address,
                "coupon_code": coupon_code,
                "status": status,
                "payment_methods": payment_methods,
                "sort": enum_value(sort) if sort is not None else None,
            }.items()
            if v is not None
        }
        params = {
            k: v
            for k, v in {
                "limit": limit,
                "before": before,
                "after": after,
                "order": order,
            }.items()
            if v is not None
        }
        return await self._client.request_page(
            method="post",
            path=("v2", "invoices", "search"),
            model=SearchInvoicesResponseValue200ApplicationJsonPropertyDataItem,
            body=body,
            params=params,
            request_options=request_options,
        )

    async def get(
        self,
        invoice: int,
        *,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkGetInvoiceResponseValue200ApplicationJson:
        """Retrieve an invoice

        Retrieve an invoice by its ID to check its current payment and delivery state. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            invoice: The invoice path parameter.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkGetInvoiceResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "getInvoice",
        }
        return await self._client.request(
            method="get",
            path=("v2", "invoices", str(invoice)),
            model=SdkGetInvoiceResponseValue200ApplicationJson,
            request_options=request_options,
        )

    async def go_to_checkout(
        self,
        invoice: int,
        *,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkCreateCheckoutSessionResponseValue200ApplicationJson:
        """Create a checkout session

        Create or reuse a payment session for a pending invoice. Create the invoice first, then send the customer to the returned top-level `payment_url`. A zero-priced checkout can return a paid invoice without a URL; delivery may still be queued. Check for a URL before redirecting and verify payment through signed events or a fresh read. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            invoice: The invoice path parameter.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkCreateCheckoutSessionResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "createCheckoutSession",
        }
        return await self._client.request(
            method="post",
            path=("v2", "invoices", str(invoice), "checkout"),
            model=SdkCreateCheckoutSessionResponseValue200ApplicationJson,
            request_options=request_options,
        )

    async def get_deliverables(
        self,
        invoice: int,
        *,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkGetInvoiceDeliverablesResponseValue200ApplicationJson:
        """View invoice deliverables

        Retrieve the deliverables sent to the customer, including each product in a multi-product purchase. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            invoice: The invoice path parameter.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkGetInvoiceDeliverablesResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "getInvoiceDeliverables",
        }
        return await self._client.request(
            method="get",
            path=("v2", "invoices", str(invoice), "deliverables"),
            model=SdkGetInvoiceDeliverablesResponseValue200ApplicationJson,
            request_options=request_options,
        )

    async def mark_completed(
        self,
        invoice: int,
        *,
        expected_status: Union[Union[ExpectedStatus, str], None, NotGiven] = NOT_GIVEN,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkMarkPendingInvoiceCompletedResponseValue200ApplicationJson:
        """Mark pending invoice completed

        Mark a pending invoice completed and start the normal delivery flow. SellApp normally handles completion after payment is confirmed. Use this only after independently confirming payment or deliberately authorizing delivery without it; the action can release purchased products. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            invoice: The invoice path parameter.
            expected_status: Optional optimistic-concurrency guard. The mutation fails when the current order status no longer matches this value.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkMarkPendingInvoiceCompletedResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "markPendingInvoiceCompleted",
        }
        body: Dict[str, Any] = {}
        if not isinstance(expected_status, NotGiven):
            body["expected_status"] = (
                enum_value(expected_status) if expected_status is not None else None
            )
        _validate_model(
            body,
            {
                "name": "SdkMarkPendingInvoiceCompletedRequestApplicationJson",
                "required": [],
                "properties": {
                    "expected_status": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "enum",
                            "values": [
                                "PENDING",
                                "REVERSED",
                                "PARTIAL",
                                "VOIDED",
                                "REFUNDED",
                                "PAID",
                                "COMPLETED",
                                "DISPUTING",
                                "REVIEW",
                                "FAILED",
                                "null",
                            ],
                        },
                    }
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return await self._client.request(
            method="patch",
            path=("v2", "invoices", str(invoice), "mark-completed"),
            body=body,
            model=SdkMarkPendingInvoiceCompletedResponseValue200ApplicationJson,
            request_options=request_options,
        )

    async def mark_voided(
        self,
        invoice: int,
        *,
        expected_status: Union[Union[ExpectedStatus, str], None, NotGiven] = NOT_GIVEN,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkMarkPendingInvoiceVoidedResponseValue200ApplicationJson:
        """Mark pending invoice voided

        Void a pending invoice so it does not proceed to product delivery. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            invoice: The invoice path parameter.
            expected_status: Optional optimistic-concurrency guard. The mutation fails when the current order status no longer matches this value.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkMarkPendingInvoiceVoidedResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "markPendingInvoiceVoided",
        }
        body: Dict[str, Any] = {}
        if not isinstance(expected_status, NotGiven):
            body["expected_status"] = (
                enum_value(expected_status) if expected_status is not None else None
            )
        _validate_model(
            body,
            {
                "name": "SdkMarkPendingInvoiceVoidedRequestApplicationJson",
                "required": [],
                "properties": {
                    "expected_status": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "enum",
                            "values": [
                                "PENDING",
                                "REVERSED",
                                "PARTIAL",
                                "VOIDED",
                                "REFUNDED",
                                "PAID",
                                "COMPLETED",
                                "DISPUTING",
                                "REVIEW",
                                "FAILED",
                                "null",
                            ],
                        },
                    }
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return await self._client.request(
            method="patch",
            path=("v2", "invoices", str(invoice), "mark-voided"),
            body=body,
            model=SdkMarkPendingInvoiceVoidedResponseValue200ApplicationJson,
            request_options=request_options,
        )

    async def issue_replacement(
        self,
        invoice: int,
        *,
        product_variants: IssueReplacementForCompletedInvoiceRequestApplicationJsonPropertyProductVariants,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkIssueReplacementForCompletedInvoiceResponseValue200ApplicationJson:
        """Issue replacement for completed invoice

        Issue replacement deliverables for a completed purchase. This creates a new invoice and marks it completed, then starts the normal delivery and sales-notification flow. It is not just an edit to the old delivery. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            invoice: The invoice path parameter.
            product_variants: The product variants.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkIssueReplacementForCompletedInvoiceResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "issueReplacementForCompletedInvoice",
        }
        body: Dict[str, Any] = {
            "product_variants": product_variants.to_dict(),
        }
        _validate_model(
            body,
            {
                "name": "SdkIssueReplacementForCompletedInvoiceRequestApplicationJson",
                "required": ["product_variants"],
                "properties": {"product_variants": {"kind": "any"}},
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return await self._client.request(
            method="patch",
            path=("v2", "invoices", str(invoice), "issue-replacement"),
            body=body,
            model=SdkIssueReplacementForCompletedInvoiceResponseValue200ApplicationJson,
            request_options=request_options,
        )

    async def update_status(
        self,
        invoice: int,
        *,
        status: Union[SdkUpdateInvoiceStatusRequestApplicationJsonStatus, str],
        expected_status: Union[Union[ExpectedStatus, str], None, NotGiven] = NOT_GIVEN,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkUpdateInvoiceStatusResponseValue200ApplicationJson:
        """Update invoice status

        Change a purchase through the invoice compatibility operation. Set status to COMPLETED, VOIDED, REVIEW, or DISPUTING. Dispute transitions schedule community-access revocation and a dispute webhook. Requires the `invoice` credential ability and store ownership. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            invoice: The invoice path parameter.
            status: The status.
            expected_status: Optional optimistic-concurrency guard. The mutation fails when the current order status no longer matches this value.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkUpdateInvoiceStatusResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "updateInvoiceStatus",
        }
        body: Dict[str, Any] = {
            "status": enum_value(status),
        }
        if not isinstance(expected_status, NotGiven):
            body["expected_status"] = (
                enum_value(expected_status) if expected_status is not None else None
            )
        _validate_model(
            body,
            {
                "name": "SdkUpdateInvoiceStatusRequestApplicationJson",
                "required": ["status"],
                "properties": {
                    "status": {
                        "kind": "enum",
                        "values": ["COMPLETED", "VOIDED", "REVIEW", "DISPUTING"],
                    },
                    "expected_status": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "enum",
                            "values": [
                                "PENDING",
                                "REVERSED",
                                "PARTIAL",
                                "VOIDED",
                                "REFUNDED",
                                "PAID",
                                "COMPLETED",
                                "DISPUTING",
                                "REVIEW",
                                "FAILED",
                                "null",
                            ],
                        },
                    },
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return await self._client.request(
            method="patch",
            path=("v2", "invoices", str(invoice), "status"),
            body=body,
            model=SdkUpdateInvoiceStatusResponseValue200ApplicationJson,
            request_options=request_options,
        )

    async def create_refund(
        self,
        invoice: int,
        *,
        amount: Union[str, None, NotGiven] = NOT_GIVEN,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkCreateInvoiceRefundResponseValue200ApplicationJson:
        """Create invoice refund

        Request a full or partial provider refund using the same idempotent refund ledger as the dashboard. Amounts are decimal currency strings, never floating-point numbers. Requires the `invoice` token ability. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            invoice: The invoice path parameter.
            amount: Optional partial refund amount in major currency units. Omit for the full remaining refundable amount.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkCreateInvoiceRefundResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "createInvoiceRefund",
        }
        body: Dict[str, Any] = {}
        if not isinstance(amount, NotGiven):
            body["amount"] = amount
        _validate_model(
            body,
            {
                "name": "SdkCreateInvoiceRefundRequestApplicationJson",
                "required": [],
                "properties": {
                    "amount": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "string",
                            "constraints": {
                                "maxLength": 32,
                                "pattern": "^\\d+(\\.\\d+)?$",
                            },
                        },
                    }
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return await self._client.request(
            method="post",
            path=("v2", "invoices", str(invoice), "refunds"),
            body=body,
            model=SdkCreateInvoiceRefundResponseValue200ApplicationJson,
            request_options=request_options,
        )

    async def retry_fulfillment(
        self,
        invoice: int,
        *,
        email: Union[str, None, NotGiven] = NOT_GIVEN,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkCreateFulfillmentRetryResponseValue200ApplicationJson:
        """Create fulfillment retry

        Retry failed delivery steps for a paid or partially fulfilled purchase. Repeated requests retry the same failed steps without creating duplicate work. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            invoice: The invoice path parameter.
            email: Optional replacement delivery email.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkCreateFulfillmentRetryResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "createFulfillmentRetry",
        }
        body: Dict[str, Any] = {}
        if not isinstance(email, NotGiven):
            body["email"] = email
        _validate_model(
            body,
            {
                "name": "SdkCreateFulfillmentRetryRequestApplicationJson",
                "required": [],
                "properties": {
                    "email": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "string",
                            "format": "email",
                            "constraints": {"maxLength": 254},
                        },
                    }
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return await self._client.request(
            method="post",
            path=("v2", "invoices", str(invoice), "fulfillment-retries"),
            body=body,
            model=SdkCreateFulfillmentRetryResponseValue200ApplicationJson,
            request_options=request_options,
        )

    async def retry_dynamic_delivery(
        self,
        invoice: int,
        *,
        delivered_product_id: int,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkCreateDynamicDeliveryRetryResponseValue200ApplicationJson:
        """Create dynamic delivery retry

        Retry one same-store dynamic delivered product after validating order, line-item, variant, and webhook ownership. Callers should avoid concurrent retries for the same delivered product. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            invoice: The invoice path parameter.
            delivered_product_id: The delivered product id.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkCreateDynamicDeliveryRetryResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "createDynamicDeliveryRetry",
        }
        body: Dict[str, Any] = {
            "delivered_product_id": delivered_product_id,
        }
        _validate_model(
            body,
            {
                "name": "SdkCreateDynamicDeliveryRetryRequestApplicationJson",
                "required": ["delivered_product_id"],
                "properties": {
                    "delivered_product_id": {
                        "kind": "primitive",
                        "type": "integer",
                        "constraints": {"minimum": 1},
                    }
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return await self._client.request(
            method="post",
            path=("v2", "invoices", str(invoice), "dynamic-delivery-retries"),
            body=body,
            model=SdkCreateDynamicDeliveryRetryResponseValue200ApplicationJson,
            request_options=request_options,
        )

    async def notify_fulfillment(
        self,
        invoice: int,
        *,
        email: Union[str, None, NotGiven] = NOT_GIVEN,
        product_variant_ids: Union[List[int], None, NotGiven] = NOT_GIVEN,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkCreateFulfillmentNotificationsResponseValue200ApplicationJson:
        """Create fulfillment notifications

        Resend fulfillment notifications for all or selected same-store product variants on a completed invoice. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            invoice: The invoice path parameter.
            email: The email.
            product_variant_ids: The product variant ids.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkCreateFulfillmentNotificationsResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "createFulfillmentNotifications",
        }
        body: Dict[str, Any] = {}
        if not isinstance(email, NotGiven):
            body["email"] = email
        if not isinstance(product_variant_ids, NotGiven):
            body["product_variant_ids"] = product_variant_ids
        _validate_model(
            body,
            {
                "name": "SdkCreateFulfillmentNotificationsRequestApplicationJson",
                "required": [],
                "properties": {
                    "email": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "string",
                            "format": "email",
                            "constraints": {"maxLength": 254},
                        },
                    },
                    "product_variant_ids": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "array",
                            "items": {
                                "kind": "primitive",
                                "type": "integer",
                                "constraints": {"minimum": 1},
                            },
                            "constraints": {"minItems": 1, "uniqueItems": True},
                        },
                    },
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return await self._client.request(
            method="post",
            path=("v2", "invoices", str(invoice), "fulfillment-notifications"),
            body=body,
            model=SdkCreateFulfillmentNotificationsResponseValue200ApplicationJson,
            request_options=request_options,
        )
