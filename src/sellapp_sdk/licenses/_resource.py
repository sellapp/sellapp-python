# This file is auto-generated by oagen. Do not edit.

from __future__ import annotations

from typing import TYPE_CHECKING, Any, Dict, Optional, Union

from .._base_client import WithRawResponse

if TYPE_CHECKING:
    from .._client import AsyncSellAppClient, SellAppClient

from sellapp_sdk.common.models.sdk_activate_license_key_response_value_200_application_json import (
    SdkActivateLicenseKeyResponseValue200ApplicationJson,
)

from .._pagination import AsyncPage, SyncPage
from .._types import NOT_GIVEN, NotGiven, RequestOptions, _validate_model
from .models import (
    SdkDeactivateLicenseResponseValue200ApplicationJson,
    SdkGetLicenseKeyResponseValue200ApplicationJson,
    SdkListLicenseKeysResponseValue200ApplicationJson,
    SdkUpdateLicenseKeyResponseValue200ApplicationJson,
    SdkValidateLicenseKeyResponseValue200ApplicationJson,
)


class Licenses:
    """Licenses API resources."""

    def __init__(self, client: SellAppClient) -> None:
        self._client = client
        self.with_raw_response = WithRawResponse(self)

    def activate(
        self,
        *,
        license_key: str,
        instance_name: str,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkActivateLicenseKeyResponseValue200ApplicationJson:
        """Activate a license key

        Activate a license for a named device or installation. Save the returned instance ID to validate that activation later. Activation fails if the key is missing, inactive, expired, or at its activation limit. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            license_key: The license key.
            instance_name: The instance name.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkActivateLicenseKeyResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "activateLicenseKey",
        }
        body: Dict[str, Any] = {
            "license_key": license_key,
            "instance_name": instance_name,
        }
        _validate_model(
            body,
            {
                "name": "SdkActivateLicenseKeyRequestApplicationJson",
                "required": ["license_key", "instance_name"],
                "properties": {
                    "license_key": {"kind": "primitive", "type": "string"},
                    "instance_name": {"kind": "primitive", "type": "string"},
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return self._client.request(
            method="post",
            path=("v2", "licenses", "activate"),
            body=body,
            model=SdkActivateLicenseKeyResponseValue200ApplicationJson,
            request_options=request_options,
        )

    def validate(
        self,
        *,
        license_key: str,
        instance_id: Optional[str] = None,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkValidateLicenseKeyResponseValue200ApplicationJson:
        """Validate a license key

        Checks whether a license key is currently valid. You may include `instance_id` to validate a specific activation; if omitted, the API validates the license key alone. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            license_key: The license key.
            instance_id: The instance id.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkValidateLicenseKeyResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "validateLicenseKey",
        }
        body: Dict[str, Any] = {
            k: v
            for k, v in {
                "license_key": license_key,
                "instance_id": instance_id,
            }.items()
            if v is not None
        }
        _validate_model(
            body,
            {
                "name": "SdkValidateLicenseKeyRequestApplicationJson",
                "required": ["license_key"],
                "properties": {
                    "license_key": {"kind": "primitive", "type": "string"},
                    "instance_id": {"kind": "primitive", "type": "string"},
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return self._client.request(
            method="post",
            path=("v2", "licenses", "validate"),
            body=body,
            model=SdkValidateLicenseKeyResponseValue200ApplicationJson,
            request_options=request_options,
        )

    def list(
        self,
        *,
        limit: Optional[int] = None,
        before: Optional[str] = None,
        after: Optional[str] = None,
        order: Optional[str] = None,
        page: Optional[int] = None,
        request_options: Optional[RequestOptions] = None,
    ) -> SyncPage[SdkListLicenseKeysResponseValue200ApplicationJson]:
        """List all license keys

        List your store's license keys. Use `limit` and `page` to read one page at a time. The response schema describes each license. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            limit: Number of items to return per page. Defaults to `15`.
            before: The before.
            after: The after.
            order: The order.
            page: Page number to return. Defaults to `1`.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SyncPage[SdkListLicenseKeysResponseValue200ApplicationJson]

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "listLicenseKeys",
        }
        params = {
            k: v
            for k, v in {
                "limit": limit,
                "before": before,
                "after": after,
                "order": order,
                "page": page,
            }.items()
            if v is not None
        }
        return self._client.request_page(
            method="get",
            path=("v2", "license-keys"),
            model=SdkListLicenseKeysResponseValue200ApplicationJson,
            params=params,
            request_options=request_options,
        )

    def get(
        self,
        license_key: int,
        *,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkGetLicenseKeyResponseValue200ApplicationJson:
        """Retrieve a license key

        Retrieve a license record by its ID, not the license key string a customer enters. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            license_key: The license key path parameter.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkGetLicenseKeyResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "getLicenseKey",
        }
        return self._client.request(
            method="get",
            path=("v2", "license-keys", str(license_key)),
            model=SdkGetLicenseKeyResponseValue200ApplicationJson,
            request_options=request_options,
        )

    def update(
        self,
        license_key: int,
        *,
        limit: Union[int, None, NotGiven] = NOT_GIVEN,
        expires_at: Union[str, None, NotGiven] = NOT_GIVEN,
        active: Union[bool, None, NotGiven] = NOT_GIVEN,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkUpdateLicenseKeyResponseValue200ApplicationJson:
        """Update a license key

        Change a license's activation limit, expiry date, or active status. If `limit` is lower than `instances_count`, the oldest instances are deleted until the count fits the new limit. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            license_key: The license key path parameter.
            limit: The limit.
            expires_at: The expires at.
            active: The active.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkUpdateLicenseKeyResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "updateLicenseKey",
        }
        body: Dict[str, Any] = {}
        if not isinstance(limit, NotGiven):
            body["limit"] = limit
        if not isinstance(expires_at, NotGiven):
            body["expires_at"] = expires_at
        if not isinstance(active, NotGiven):
            body["active"] = active
        _validate_model(
            body,
            {
                "name": "SdkUpdateLicenseKeyRequestApplicationJson",
                "required": [],
                "properties": {
                    "limit": {
                        "kind": "nullable",
                        "inner": {"kind": "primitive", "type": "integer"},
                    },
                    "expires_at": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "string",
                            "format": "date-time",
                        },
                    },
                    "active": {
                        "kind": "nullable",
                        "inner": {"kind": "primitive", "type": "boolean"},
                    },
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return self._client.request(
            method="patch",
            path=("v2", "license-keys", str(license_key)),
            body=body,
            model=SdkUpdateLicenseKeyResponseValue200ApplicationJson,
            request_options=request_options,
        )

    def deactivate_license(
        self,
        *,
        license_key: str,
        instance_id: str,
        idempotency_key: Optional[str] = None,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkDeactivateLicenseResponseValue200ApplicationJson:
        """Deactivate a license instance

        Deactivate one installation. The response never exposes the raw license key. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            license_key: The license key.
            instance_id: The instance id.
            idempotency_key: Optional idempotency key for safe retries.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkDeactivateLicenseResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            ConflictError: If a conflict occurs (409).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": True,
            "operation_id": "deactivateLicense",
        }
        body: Dict[str, Any] = {
            "license_key": license_key,
            "instance_id": instance_id,
        }
        _validate_model(
            body,
            {
                "name": "SdkDeactivateLicenseRequestApplicationJson",
                "required": ["license_key", "instance_id"],
                "properties": {
                    "license_key": {"kind": "primitive", "type": "string"},
                    "instance_id": {"kind": "primitive", "type": "string"},
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return self._client.request(
            method="post",
            path=("v2", "licenses", "deactivate"),
            body=body,
            model=SdkDeactivateLicenseResponseValue200ApplicationJson,
            idempotency_key=idempotency_key,
            request_options=request_options,
        )


class AsyncLicenses:
    """Licenses API resources (async)."""

    def __init__(self, client: AsyncSellAppClient) -> None:
        self._client = client
        self.with_raw_response = WithRawResponse(self)

    async def activate(
        self,
        *,
        license_key: str,
        instance_name: str,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkActivateLicenseKeyResponseValue200ApplicationJson:
        """Activate a license key

        Activate a license for a named device or installation. Save the returned instance ID to validate that activation later. Activation fails if the key is missing, inactive, expired, or at its activation limit. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            license_key: The license key.
            instance_name: The instance name.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkActivateLicenseKeyResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "activateLicenseKey",
        }
        body: Dict[str, Any] = {
            "license_key": license_key,
            "instance_name": instance_name,
        }
        _validate_model(
            body,
            {
                "name": "SdkActivateLicenseKeyRequestApplicationJson",
                "required": ["license_key", "instance_name"],
                "properties": {
                    "license_key": {"kind": "primitive", "type": "string"},
                    "instance_name": {"kind": "primitive", "type": "string"},
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return await self._client.request(
            method="post",
            path=("v2", "licenses", "activate"),
            body=body,
            model=SdkActivateLicenseKeyResponseValue200ApplicationJson,
            request_options=request_options,
        )

    async def validate(
        self,
        *,
        license_key: str,
        instance_id: Optional[str] = None,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkValidateLicenseKeyResponseValue200ApplicationJson:
        """Validate a license key

        Checks whether a license key is currently valid. You may include `instance_id` to validate a specific activation; if omitted, the API validates the license key alone. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            license_key: The license key.
            instance_id: The instance id.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkValidateLicenseKeyResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "validateLicenseKey",
        }
        body: Dict[str, Any] = {
            k: v
            for k, v in {
                "license_key": license_key,
                "instance_id": instance_id,
            }.items()
            if v is not None
        }
        _validate_model(
            body,
            {
                "name": "SdkValidateLicenseKeyRequestApplicationJson",
                "required": ["license_key"],
                "properties": {
                    "license_key": {"kind": "primitive", "type": "string"},
                    "instance_id": {"kind": "primitive", "type": "string"},
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return await self._client.request(
            method="post",
            path=("v2", "licenses", "validate"),
            body=body,
            model=SdkValidateLicenseKeyResponseValue200ApplicationJson,
            request_options=request_options,
        )

    async def list(
        self,
        *,
        limit: Optional[int] = None,
        before: Optional[str] = None,
        after: Optional[str] = None,
        order: Optional[str] = None,
        page: Optional[int] = None,
        request_options: Optional[RequestOptions] = None,
    ) -> AsyncPage[SdkListLicenseKeysResponseValue200ApplicationJson]:
        """List all license keys

        List your store's license keys. Use `limit` and `page` to read one page at a time. The response schema describes each license. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            limit: Number of items to return per page. Defaults to `15`.
            before: The before.
            after: The after.
            order: The order.
            page: Page number to return. Defaults to `1`.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            AsyncPage[SdkListLicenseKeysResponseValue200ApplicationJson]

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "listLicenseKeys",
        }
        params = {
            k: v
            for k, v in {
                "limit": limit,
                "before": before,
                "after": after,
                "order": order,
                "page": page,
            }.items()
            if v is not None
        }
        return await self._client.request_page(
            method="get",
            path=("v2", "license-keys"),
            model=SdkListLicenseKeysResponseValue200ApplicationJson,
            params=params,
            request_options=request_options,
        )

    async def get(
        self,
        license_key: int,
        *,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkGetLicenseKeyResponseValue200ApplicationJson:
        """Retrieve a license key

        Retrieve a license record by its ID, not the license key string a customer enters. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            license_key: The license key path parameter.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkGetLicenseKeyResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "getLicenseKey",
        }
        return await self._client.request(
            method="get",
            path=("v2", "license-keys", str(license_key)),
            model=SdkGetLicenseKeyResponseValue200ApplicationJson,
            request_options=request_options,
        )

    async def update(
        self,
        license_key: int,
        *,
        limit: Union[int, None, NotGiven] = NOT_GIVEN,
        expires_at: Union[str, None, NotGiven] = NOT_GIVEN,
        active: Union[bool, None, NotGiven] = NOT_GIVEN,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkUpdateLicenseKeyResponseValue200ApplicationJson:
        """Update a license key

        Change a license's activation limit, expiry date, or active status. If `limit` is lower than `instances_count`, the oldest instances are deleted until the count fits the new limit. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            license_key: The license key path parameter.
            limit: The limit.
            expires_at: The expires at.
            active: The active.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkUpdateLicenseKeyResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "updateLicenseKey",
        }
        body: Dict[str, Any] = {}
        if not isinstance(limit, NotGiven):
            body["limit"] = limit
        if not isinstance(expires_at, NotGiven):
            body["expires_at"] = expires_at
        if not isinstance(active, NotGiven):
            body["active"] = active
        _validate_model(
            body,
            {
                "name": "SdkUpdateLicenseKeyRequestApplicationJson",
                "required": [],
                "properties": {
                    "limit": {
                        "kind": "nullable",
                        "inner": {"kind": "primitive", "type": "integer"},
                    },
                    "expires_at": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "string",
                            "format": "date-time",
                        },
                    },
                    "active": {
                        "kind": "nullable",
                        "inner": {"kind": "primitive", "type": "boolean"},
                    },
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return await self._client.request(
            method="patch",
            path=("v2", "license-keys", str(license_key)),
            body=body,
            model=SdkUpdateLicenseKeyResponseValue200ApplicationJson,
            request_options=request_options,
        )

    async def deactivate_license(
        self,
        *,
        license_key: str,
        instance_id: str,
        idempotency_key: Optional[str] = None,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkDeactivateLicenseResponseValue200ApplicationJson:
        """Deactivate a license instance

        Deactivate one installation. The response never exposes the raw license key. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            license_key: The license key.
            instance_id: The instance id.
            idempotency_key: Optional idempotency key for safe retries.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkDeactivateLicenseResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            ConflictError: If a conflict occurs (409).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": True,
            "operation_id": "deactivateLicense",
        }
        body: Dict[str, Any] = {
            "license_key": license_key,
            "instance_id": instance_id,
        }
        _validate_model(
            body,
            {
                "name": "SdkDeactivateLicenseRequestApplicationJson",
                "required": ["license_key", "instance_id"],
                "properties": {
                    "license_key": {"kind": "primitive", "type": "string"},
                    "instance_id": {"kind": "primitive", "type": "string"},
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return await self._client.request(
            method="post",
            path=("v2", "licenses", "deactivate"),
            body=body,
            model=SdkDeactivateLicenseResponseValue200ApplicationJson,
            idempotency_key=idempotency_key,
            request_options=request_options,
        )
