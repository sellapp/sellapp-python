# This file is auto-generated by oagen. Do not edit.

from __future__ import annotations

from dataclasses import dataclass
from dataclasses import field as dataclass_field
from datetime import datetime
from enum import Enum
from typing import Any, cast

from sellapp_sdk._types import (
    _format_datetime,
    _parse_datetime,
    _preserve_unknown_fields,
    _raise_deserialize_error,
    _validate_model,
)
from sellapp_sdk.common.models.get_order_line_item_response_value_200_application_json_property_data_property_fulfillment_types import (
    GetOrderLineItemResponseValue200ApplicationJsonPropertyDataPropertyFulfillmentTypes,
)

_DOMAIN_RUNTIME_SCHEMA: dict[str, Any] = {
    "name": "GetOrderLineItemResponseValue200ApplicationJsonPropertyDataPropertyFulfillment",
    "required": ["deliveries_count", "delivered_quantity", "types"],
    "properties": {
        "deliveries_count": {
            "kind": "primitive",
            "type": "integer",
            "constraints": {"minimum": 0},
        },
        "delivered_quantity": {
            "kind": "primitive",
            "type": "integer",
            "constraints": {"minimum": 0},
        },
        "types": {
            "kind": "array",
            "items": {
                "kind": "enum",
                "values": [
                    "DOWNLOADABLE",
                    "TEXT",
                    "DYNAMIC",
                    "MANUAL",
                    "LICENSE_KEY",
                    "BUNDLE",
                    "BOOKING",
                    "CREDITS",
                ],
            },
        },
        "first_delivered_at": {
            "kind": "nullable",
            "inner": {"kind": "primitive", "type": "string", "format": "date-time"},
        },
        "last_delivered_at": {
            "kind": "nullable",
            "inner": {"kind": "primitive", "type": "string", "format": "date-time"},
        },
    },
    "additionalProperties": True,
}
_WIRE_RUNTIME_SCHEMA: dict[str, Any] = {
    "name": "GetOrderLineItemResponseValue200ApplicationJsonPropertyDataPropertyFulfillment",
    "required": ["deliveries_count", "delivered_quantity", "types"],
    "properties": {
        "deliveries_count": {
            "kind": "primitive",
            "type": "integer",
            "constraints": {"minimum": 0},
        },
        "delivered_quantity": {
            "kind": "primitive",
            "type": "integer",
            "constraints": {"minimum": 0},
        },
        "types": {
            "kind": "array",
            "items": {
                "kind": "enum",
                "values": [
                    "DOWNLOADABLE",
                    "TEXT",
                    "DYNAMIC",
                    "MANUAL",
                    "LICENSE_KEY",
                    "BUNDLE",
                    "BOOKING",
                    "CREDITS",
                ],
            },
        },
        "first_delivered_at": {
            "kind": "nullable",
            "inner": {"kind": "primitive", "type": "string", "format": "date-time"},
        },
        "last_delivered_at": {
            "kind": "nullable",
            "inner": {"kind": "primitive", "type": "string", "format": "date-time"},
        },
    },
    "additionalProperties": True,
}


@dataclass(slots=True)
class GetOrderLineItemResponseValue200ApplicationJsonPropertyDataPropertyFulfillment:
    """Safe fulfillment summary. Delivered file paths, serials, license keys, and dynamic-delivery payloads are never returned."""

    deliveries_count: int
    delivered_quantity: int
    types: list[
        GetOrderLineItemResponseValue200ApplicationJsonPropertyDataPropertyFulfillmentTypes
    ]
    first_delivered_at: datetime | None = None
    last_delivered_at: datetime | None = None
    additional_properties: dict[str, Any] = dataclass_field(
        default_factory=dict, repr=False
    )
    _from_response: bool = dataclass_field(default=False, repr=False, compare=False)

    @classmethod
    def from_dict(
        cls, data: dict[str, Any]
    ) -> GetOrderLineItemResponseValue200ApplicationJsonPropertyDataPropertyFulfillment:
        """Deserialize from a dictionary."""
        try:
            _validate_model(
                data,
                _WIRE_RUNTIME_SCHEMA,
                direction="response",
                allow_unknown_enums=True,
                allow_unknown_union_variants=True,
                allow_unknown_fields=True,
            )
            return cls(
                deliveries_count=data["deliveries_count"],
                delivered_quantity=data["delivered_quantity"],
                types=[
                    GetOrderLineItemResponseValue200ApplicationJsonPropertyDataPropertyFulfillmentTypes(
                        item
                    )
                    for item in cast(list[Any], data["types"])
                ],
                first_delivered_at=_parse_datetime(_v_first_delivered_at)
                if (_v_first_delivered_at := data.get("first_delivered_at")) is not None
                else None,
                last_delivered_at=_parse_datetime(_v_last_delivered_at)
                if (_v_last_delivered_at := data.get("last_delivered_at")) is not None
                else None,
                additional_properties=_preserve_unknown_fields(
                    data,
                    [
                        "deliveries_count",
                        "delivered_quantity",
                        "types",
                        "first_delivered_at",
                        "last_delivered_at",
                    ],
                ),
                _from_response=True,
            )
        except (KeyError, ValueError) as e:
            _raise_deserialize_error(
                "GetOrderLineItemResponseValue200ApplicationJsonPropertyDataPropertyFulfillment",
                e,
            )

    def to_dict(self) -> dict[str, Any]:
        """Serialize to a dictionary."""
        _domain_data: dict[str, Any] = dict(self.additional_properties)
        _domain_data["deliveries_count"] = self.deliveries_count
        _domain_data["delivered_quantity"] = self.delivered_quantity
        _domain_data["types"] = self.types
        _domain_data["first_delivered_at"] = self.first_delivered_at
        _domain_data["last_delivered_at"] = self.last_delivered_at
        _validate_model(
            _domain_data,
            _DOMAIN_RUNTIME_SCHEMA,
            direction="request",
            allow_unknown_enums=self._from_response,
            allow_unknown_union_variants=self._from_response,
            allow_unknown_fields=(self._from_response or True),
        )
        result: dict[str, Any] = dict(self.additional_properties)
        result["deliveries_count"] = self.deliveries_count
        result["delivered_quantity"] = self.delivered_quantity
        result["types"] = [
            item.value if isinstance(item, Enum) else item for item in self.types
        ]
        if self.first_delivered_at is not None:
            result["first_delivered_at"] = _format_datetime(self.first_delivered_at)
        else:
            result["first_delivered_at"] = None
        if self.last_delivered_at is not None:
            result["last_delivered_at"] = _format_datetime(self.last_delivered_at)
        else:
            result["last_delivered_at"] = None
        return result
