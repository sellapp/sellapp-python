# This file is auto-generated by oagen. Do not edit.

from __future__ import annotations

from dataclasses import dataclass
from dataclasses import field as dataclass_field
from datetime import datetime
from typing import Any, Dict, Optional

from sellapp_sdk._types import (
    _format_datetime,
    _parse_datetime,
    _preserve_unknown_fields,
    _raise_deserialize_error,
    _validate_model,
)

_DOMAIN_RUNTIME_SCHEMA: Dict[str, Any] = {
    "name": "GetOrderLineItemResponseValue200ApplicationJsonPropertyDataPropertySubscription",
    "required": ["id", "provider", "status", "cancel_at_period_end"],
    "properties": {
        "id": {"kind": "primitive", "type": "integer"},
        "provider": {
            "kind": "nullable",
            "inner": {"kind": "primitive", "type": "string"},
        },
        "status": {
            "kind": "nullable",
            "inner": {"kind": "primitive", "type": "string"},
        },
        "current_period_start": {
            "kind": "nullable",
            "inner": {"kind": "primitive", "type": "string", "format": "date-time"},
        },
        "current_period_end": {
            "kind": "nullable",
            "inner": {"kind": "primitive", "type": "string", "format": "date-time"},
        },
        "cancel_at_period_end": {"kind": "primitive", "type": "boolean"},
    },
    "additionalProperties": True,
}
_WIRE_RUNTIME_SCHEMA: Dict[str, Any] = {
    "name": "GetOrderLineItemResponseValue200ApplicationJsonPropertyDataPropertySubscription",
    "required": ["id", "provider", "status", "cancel_at_period_end"],
    "properties": {
        "id": {"kind": "primitive", "type": "integer"},
        "provider": {
            "kind": "nullable",
            "inner": {"kind": "primitive", "type": "string"},
        },
        "status": {
            "kind": "nullable",
            "inner": {"kind": "primitive", "type": "string"},
        },
        "current_period_start": {
            "kind": "nullable",
            "inner": {"kind": "primitive", "type": "string", "format": "date-time"},
        },
        "current_period_end": {
            "kind": "nullable",
            "inner": {"kind": "primitive", "type": "string", "format": "date-time"},
        },
        "cancel_at_period_end": {"kind": "primitive", "type": "boolean"},
    },
    "additionalProperties": True,
}


@dataclass(slots=True)
class GetOrderLineItemResponseValue200ApplicationJsonPropertyDataPropertySubscription:
    """Get Order Line Item Response Value200Application Json Property Data Property Subscription model."""

    id: int
    provider: Optional[str]
    status: Optional[str]
    cancel_at_period_end: bool
    current_period_start: Optional[datetime] = None
    current_period_end: Optional[datetime] = None
    additional_properties: Dict[str, Any] = dataclass_field(
        default_factory=dict, repr=False
    )
    _from_response: bool = dataclass_field(default=False, repr=False, compare=False)

    @classmethod
    def from_dict(
        cls, data: Dict[str, Any]
    ) -> (
        GetOrderLineItemResponseValue200ApplicationJsonPropertyDataPropertySubscription
    ):
        """Deserialize from a dictionary."""
        try:
            _validate_model(
                data,
                _WIRE_RUNTIME_SCHEMA,
                direction="response",
                allow_unknown_enums=True,
                allow_unknown_union_variants=True,
                allow_unknown_fields=True,
            )
            return cls(
                id=data["id"],
                provider=data["provider"],
                status=data["status"],
                cancel_at_period_end=data["cancel_at_period_end"],
                current_period_start=_parse_datetime(_v_current_period_start)
                if (_v_current_period_start := data.get("current_period_start"))
                is not None
                else None,
                current_period_end=_parse_datetime(_v_current_period_end)
                if (_v_current_period_end := data.get("current_period_end")) is not None
                else None,
                additional_properties=_preserve_unknown_fields(
                    data,
                    [
                        "id",
                        "provider",
                        "status",
                        "current_period_start",
                        "current_period_end",
                        "cancel_at_period_end",
                    ],
                ),
                _from_response=True,
            )
        except (KeyError, ValueError) as e:
            _raise_deserialize_error(
                "GetOrderLineItemResponseValue200ApplicationJsonPropertyDataPropertySubscription",
                e,
            )

    def to_dict(self) -> Dict[str, Any]:
        """Serialize to a dictionary."""
        _domain_data: Dict[str, Any] = dict(self.additional_properties)
        _domain_data["id"] = self.id
        _domain_data["provider"] = self.provider
        _domain_data["status"] = self.status
        _domain_data["cancel_at_period_end"] = self.cancel_at_period_end
        _domain_data["current_period_start"] = self.current_period_start
        _domain_data["current_period_end"] = self.current_period_end
        _validate_model(
            _domain_data,
            _DOMAIN_RUNTIME_SCHEMA,
            direction="request",
            allow_unknown_enums=self._from_response,
            allow_unknown_union_variants=self._from_response,
            allow_unknown_fields=(self._from_response or True),
        )
        result: Dict[str, Any] = dict(self.additional_properties)
        result["id"] = self.id
        if self.provider is not None:
            result["provider"] = self.provider
        else:
            result["provider"] = None
        if self.status is not None:
            result["status"] = self.status
        else:
            result["status"] = None
        result["cancel_at_period_end"] = self.cancel_at_period_end
        if self.current_period_start is not None:
            result["current_period_start"] = _format_datetime(self.current_period_start)
        else:
            result["current_period_start"] = None
        if self.current_period_end is not None:
            result["current_period_end"] = _format_datetime(self.current_period_end)
        else:
            result["current_period_end"] = None
        return result
