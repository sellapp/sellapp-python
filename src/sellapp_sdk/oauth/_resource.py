# This file is auto-generated by oagen. Do not edit.

from __future__ import annotations

from typing import TYPE_CHECKING, Any, Dict, Literal, Optional, Union

from .._base_client import WithRawResponse

if TYPE_CHECKING:
    from .._client import AsyncSellAppClient, SellAppClient

from sellapp_sdk.common.models.sdk_revoke_oauth_token_request_application_x_www_form_urlencoded_token_type_hint import (
    SdkRevokeOAuthTokenRequestApplicationXWwwFormUrlencodedTokenTypeHint,
)

from .._types import RequestOptions, _validate_model, enum_value
from .models import (
    ExchangeOAuthTokenRequestApplicationXWwwFormUrlencodedOneOfValue1,
    ExchangeOAuthTokenRequestApplicationXWwwFormUrlencodedOneOfValue2,
    SdkExchangeOAuthTokenResponseValue200ApplicationJson,
    SdkGetOAuthAuthorizationServerMetadataResponseValue200ApplicationJson,
)


class OAuth:
    """O Auth API resources."""

    def __init__(self, client: SellAppClient) -> None:
        self._client = client
        self.with_raw_response = WithRawResponse(self)

    def get_oauth_authorization_server_metadata(
        self,
        *,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkGetOAuthAuthorizationServerMetadataResponseValue200ApplicationJson:
        """Read OAuth server metadata

        Discover the official CLI authorization, token and revocation endpoints and supported scopes. The CLI uses public client authentication without a secret.

        Returns:
            SdkGetOAuthAuthorizationServerMetadataResponseValue200ApplicationJson

        Raises:
            RateLimitExceededError: If rate limited (429).
            AuthenticationError: If the API key is invalid (401).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [],
                "anonymous": True,
                "apiKey": False,
                "accessToken": False,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": False,
            },
            "server": "https://sell.app",
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "getOAuthAuthorizationServerMetadata",
        }
        return self._client.request(
            method="get",
            path=(".well-known", "oauth-authorization-server"),
            model=SdkGetOAuthAuthorizationServerMetadataResponseValue200ApplicationJson,
            request_options=request_options,
        )

    def get_oauth_authorization_request(
        self,
        *,
        response_type: Literal["code"],
        client_id: str,
        redirect_uri: str,
        scope: Optional[str] = None,
        state: str,
        code_challenge: str,
        code_challenge_method: Literal["S256"],
        request_options: Optional[RequestOptions] = None,
    ) -> Any:
        """Review CLI authorization

        Open in the user's browser to authorize the official CLI using existing store permissions. Only the configured public CLI registration is supported. PKCE S256 and non-empty state are required. The callback may vary only the port of the registered numeric loopback URL; localhost, wildcard hosts, fragments and non-loopback HTTP are rejected. Unsupported clients receive invalid_client and unsupported scopes receive invalid_scope.

        Args:
            response_type: The response type.
            client_id: The client id.
            redirect_uri: The redirect uri.
            scope: The scope.
            state: The state.
            code_challenge: The code challenge.
            code_challenge_method: The code challenge method.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            Any

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [],
                "anonymous": True,
                "apiKey": False,
                "accessToken": False,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": False,
            },
            "server": "https://sell.app",
            "encoding": "json",
            "no_retry": True,
            "text_response": True,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "getOAuthAuthorizationRequest",
        }
        params: Dict[str, Any] = {
            k: v
            for k, v in {
                "response_type": response_type,
                "client_id": client_id,
                "redirect_uri": redirect_uri,
                "scope": scope,
                "state": state,
                "code_challenge": code_challenge,
                "code_challenge_method": code_challenge_method,
            }.items()
            if v is not None
        }
        return self._client.request(
            method="get",
            path=("oauth", "authorize"),
            params=params,
            request_options=request_options,
        )

    def approve_oauth_authorization(
        self,
        *,
        auth_token: str,
        client_id: str,
        state: str,
        token: str,
        request_options: Optional[RequestOptions] = None,
    ) -> Any:
        """Approve CLI access

        Submit the official CLI consent form in the same logged-in browser session. The admin grant covers every store you can access now or later, including when you currently have no stores. It also permits store creation. Local agents connected through the CLI share this connection. Approval never adds store role permissions. Current client eligibility, membership and permissions are rechecked, including for stale consent.

        Args:
            auth_token: The auth token.
            client_id: The client id.
            state: The state.
            token: The token.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            Any

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [[{"schemeName": "oauthBrowserSession", "scopes": []}]],
                "anonymous": False,
                "apiKey": False,
                "accessToken": False,
                "customerSession": False,
                "browserSession": True,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": False,
            },
            "server": "https://sell.app",
            "encoding": "form-urlencoded",
            "no_retry": True,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "approveOAuthAuthorization",
        }
        body: Dict[str, Any] = {
            "auth_token": auth_token,
            "client_id": client_id,
            "state": state,
            "_token": token,
        }
        _validate_model(
            body,
            {
                "name": "SdkApproveOAuthAuthorizationRequestApplicationXWwwFormUrlencoded",
                "required": ["auth_token", "client_id", "state", "_token"],
                "properties": {
                    "auth_token": {"kind": "primitive", "type": "string"},
                    "client_id": {
                        "kind": "primitive",
                        "type": "string",
                        "format": "uuid",
                    },
                    "state": {
                        "kind": "primitive",
                        "type": "string",
                        "constraints": {"minLength": 1},
                    },
                    "_token": {"kind": "primitive", "type": "string"},
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return self._client.request(
            method="post",
            path=("oauth", "authorize"),
            body=body,
            request_options=request_options,
        )

    def deny_oauth_authorization(
        self,
        *,
        auth_token: str,
        token: str,
        request_options: Optional[RequestOptions] = None,
    ) -> Any:
        """Deny CLI access

        Submit the denial form from the same browser session. The callback receives access_denied and the original state.

        Args:
            auth_token: The auth token.
            token: The token.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            Any

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [[{"schemeName": "oauthBrowserSession", "scopes": []}]],
                "anonymous": False,
                "apiKey": False,
                "accessToken": False,
                "customerSession": False,
                "browserSession": True,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": False,
            },
            "server": "https://sell.app",
            "encoding": "form-urlencoded",
            "no_retry": True,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "denyOAuthAuthorization",
        }
        body: Dict[str, Any] = {
            "auth_token": auth_token,
            "_token": token,
        }
        _validate_model(
            body,
            {
                "name": "SdkDenyOAuthAuthorizationRequestApplicationXWwwFormUrlencoded",
                "required": ["auth_token", "_token"],
                "properties": {
                    "auth_token": {"kind": "primitive", "type": "string"},
                    "_token": {"kind": "primitive", "type": "string"},
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return self._client.request(
            method="delete",
            path=("oauth", "authorize"),
            body=body,
            request_options=request_options,
        )

    def exchange_oauth_token(
        self,
        *,
        body: Union[
            ExchangeOAuthTokenRequestApplicationXWwwFormUrlencodedOneOfValue1,
            ExchangeOAuthTokenRequestApplicationXWwwFormUrlencodedOneOfValue2,
            Dict[str, Any],
        ],
        request_options: Optional[RequestOptions] = None,
    ) -> SdkExchangeOAuthTokenResponseValue200ApplicationJson:
        """Exchange or refresh OAuth tokens

        Exchange an official CLI authorization code with the original redirect URI and PKCE verifier, or rotate a refresh token. Send the configured client_id without a secret. Client eligibility and current grant validity are rechecked. Access tokens last 60 minutes; refresh token families last 90 days. Atomically replace rotated refresh tokens. Reuse may revoke the family; reauthorize after invalid_grant. Fix registration or scope configuration for invalid_client or invalid_scope instead of repeating login.

        Args:
            body: The request body. Accepts: ExchangeOAuthTokenRequestApplicationXWwwFormUrlencodedOneOfValue1, ExchangeOAuthTokenRequestApplicationXWwwFormUrlencodedOneOfValue2, or a plain dict.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkExchangeOAuthTokenResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [],
                    [{"schemeName": "oauthClientBasic", "scopes": []}],
                ],
                "anonymous": True,
                "apiKey": False,
                "accessToken": False,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": True,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": False,
            },
            "server": "https://sell.app",
            "encoding": "form-urlencoded",
            "no_retry": True,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "exchangeOAuthToken",
        }
        _body: Dict[str, Any] = body if isinstance(body, dict) else body.to_dict()
        return self._client.request(
            method="post",
            path=("oauth", "token"),
            body=_body,
            model=SdkExchangeOAuthTokenResponseValue200ApplicationJson,
            request_options=request_options,
        )

    def revoke_oauth_token(
        self,
        *,
        token: str,
        client_id: Optional[str] = None,
        client_secret: Optional[str] = None,
        token_type_hint: Optional[
            Union[
                SdkRevokeOAuthTokenRequestApplicationXWwwFormUrlencodedTokenTypeHint,
                str,
            ]
        ] = None,
        request_options: Optional[RequestOptions] = None,
    ) -> Any:
        """Revoke an OAuth token

        Revoke the matching personal CLI installation and all its token families. Send the configured public client_id without a secret. Unknown tokens also succeed without disclosing whether a token existed.

        Args:
            client_id: The configured official CLI public client ID. It identifies the registration, not the executable.
            client_secret: Legacy SDK field retained for compatibility. Omit for the official CLI; it has no client secret.
            token: The token.
            token_type_hint: The token type hint.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            Any

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [],
                    [{"schemeName": "oauthClientBasic", "scopes": []}],
                ],
                "anonymous": True,
                "apiKey": False,
                "accessToken": False,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": True,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": False,
            },
            "server": "https://sell.app",
            "encoding": "form-urlencoded",
            "no_retry": True,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "revokeOAuthToken",
        }
        body: Dict[str, Any] = {
            k: v
            for k, v in {
                "client_id": client_id,
                "client_secret": client_secret,
                "token": token,
                "token_type_hint": enum_value(token_type_hint)
                if token_type_hint is not None
                else None,
            }.items()
            if v is not None
        }
        _validate_model(
            body,
            {
                "name": "SdkRevokeOAuthTokenRequestApplicationXWwwFormUrlencoded",
                "required": ["token"],
                "properties": {
                    "client_id": {
                        "kind": "primitive",
                        "type": "string",
                        "format": "uuid",
                    },
                    "client_secret": {"kind": "primitive", "type": "string"},
                    "token": {"kind": "primitive", "type": "string"},
                    "token_type_hint": {
                        "kind": "enum",
                        "values": ["access_token", "refresh_token"],
                    },
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return self._client.request(
            method="post",
            path=("oauth", "revoke"),
            body=body,
            request_options=request_options,
        )


class AsyncOAuth:
    """O Auth API resources (async)."""

    def __init__(self, client: AsyncSellAppClient) -> None:
        self._client = client
        self.with_raw_response = WithRawResponse(self)

    async def get_oauth_authorization_server_metadata(
        self,
        *,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkGetOAuthAuthorizationServerMetadataResponseValue200ApplicationJson:
        """Read OAuth server metadata

        Discover the official CLI authorization, token and revocation endpoints and supported scopes. The CLI uses public client authentication without a secret.

        Returns:
            SdkGetOAuthAuthorizationServerMetadataResponseValue200ApplicationJson

        Raises:
            RateLimitExceededError: If rate limited (429).
            AuthenticationError: If the API key is invalid (401).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [],
                "anonymous": True,
                "apiKey": False,
                "accessToken": False,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": False,
            },
            "server": "https://sell.app",
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "getOAuthAuthorizationServerMetadata",
        }
        return await self._client.request(
            method="get",
            path=(".well-known", "oauth-authorization-server"),
            model=SdkGetOAuthAuthorizationServerMetadataResponseValue200ApplicationJson,
            request_options=request_options,
        )

    async def get_oauth_authorization_request(
        self,
        *,
        response_type: Literal["code"],
        client_id: str,
        redirect_uri: str,
        scope: Optional[str] = None,
        state: str,
        code_challenge: str,
        code_challenge_method: Literal["S256"],
        request_options: Optional[RequestOptions] = None,
    ) -> Any:
        """Review CLI authorization

        Open in the user's browser to authorize the official CLI using existing store permissions. Only the configured public CLI registration is supported. PKCE S256 and non-empty state are required. The callback may vary only the port of the registered numeric loopback URL; localhost, wildcard hosts, fragments and non-loopback HTTP are rejected. Unsupported clients receive invalid_client and unsupported scopes receive invalid_scope.

        Args:
            response_type: The response type.
            client_id: The client id.
            redirect_uri: The redirect uri.
            scope: The scope.
            state: The state.
            code_challenge: The code challenge.
            code_challenge_method: The code challenge method.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            Any

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [],
                "anonymous": True,
                "apiKey": False,
                "accessToken": False,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": False,
            },
            "server": "https://sell.app",
            "encoding": "json",
            "no_retry": True,
            "text_response": True,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "getOAuthAuthorizationRequest",
        }
        params: Dict[str, Any] = {
            k: v
            for k, v in {
                "response_type": response_type,
                "client_id": client_id,
                "redirect_uri": redirect_uri,
                "scope": scope,
                "state": state,
                "code_challenge": code_challenge,
                "code_challenge_method": code_challenge_method,
            }.items()
            if v is not None
        }
        return await self._client.request(
            method="get",
            path=("oauth", "authorize"),
            params=params,
            request_options=request_options,
        )

    async def approve_oauth_authorization(
        self,
        *,
        auth_token: str,
        client_id: str,
        state: str,
        token: str,
        request_options: Optional[RequestOptions] = None,
    ) -> Any:
        """Approve CLI access

        Submit the official CLI consent form in the same logged-in browser session. The admin grant covers every store you can access now or later, including when you currently have no stores. It also permits store creation. Local agents connected through the CLI share this connection. Approval never adds store role permissions. Current client eligibility, membership and permissions are rechecked, including for stale consent.

        Args:
            auth_token: The auth token.
            client_id: The client id.
            state: The state.
            token: The token.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            Any

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [[{"schemeName": "oauthBrowserSession", "scopes": []}]],
                "anonymous": False,
                "apiKey": False,
                "accessToken": False,
                "customerSession": False,
                "browserSession": True,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": False,
            },
            "server": "https://sell.app",
            "encoding": "form-urlencoded",
            "no_retry": True,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "approveOAuthAuthorization",
        }
        body: Dict[str, Any] = {
            "auth_token": auth_token,
            "client_id": client_id,
            "state": state,
            "_token": token,
        }
        _validate_model(
            body,
            {
                "name": "SdkApproveOAuthAuthorizationRequestApplicationXWwwFormUrlencoded",
                "required": ["auth_token", "client_id", "state", "_token"],
                "properties": {
                    "auth_token": {"kind": "primitive", "type": "string"},
                    "client_id": {
                        "kind": "primitive",
                        "type": "string",
                        "format": "uuid",
                    },
                    "state": {
                        "kind": "primitive",
                        "type": "string",
                        "constraints": {"minLength": 1},
                    },
                    "_token": {"kind": "primitive", "type": "string"},
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return await self._client.request(
            method="post",
            path=("oauth", "authorize"),
            body=body,
            request_options=request_options,
        )

    async def deny_oauth_authorization(
        self,
        *,
        auth_token: str,
        token: str,
        request_options: Optional[RequestOptions] = None,
    ) -> Any:
        """Deny CLI access

        Submit the denial form from the same browser session. The callback receives access_denied and the original state.

        Args:
            auth_token: The auth token.
            token: The token.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            Any

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [[{"schemeName": "oauthBrowserSession", "scopes": []}]],
                "anonymous": False,
                "apiKey": False,
                "accessToken": False,
                "customerSession": False,
                "browserSession": True,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": False,
            },
            "server": "https://sell.app",
            "encoding": "form-urlencoded",
            "no_retry": True,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "denyOAuthAuthorization",
        }
        body: Dict[str, Any] = {
            "auth_token": auth_token,
            "_token": token,
        }
        _validate_model(
            body,
            {
                "name": "SdkDenyOAuthAuthorizationRequestApplicationXWwwFormUrlencoded",
                "required": ["auth_token", "_token"],
                "properties": {
                    "auth_token": {"kind": "primitive", "type": "string"},
                    "_token": {"kind": "primitive", "type": "string"},
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return await self._client.request(
            method="delete",
            path=("oauth", "authorize"),
            body=body,
            request_options=request_options,
        )

    async def exchange_oauth_token(
        self,
        *,
        body: Union[
            ExchangeOAuthTokenRequestApplicationXWwwFormUrlencodedOneOfValue1,
            ExchangeOAuthTokenRequestApplicationXWwwFormUrlencodedOneOfValue2,
            Dict[str, Any],
        ],
        request_options: Optional[RequestOptions] = None,
    ) -> SdkExchangeOAuthTokenResponseValue200ApplicationJson:
        """Exchange or refresh OAuth tokens

        Exchange an official CLI authorization code with the original redirect URI and PKCE verifier, or rotate a refresh token. Send the configured client_id without a secret. Client eligibility and current grant validity are rechecked. Access tokens last 60 minutes; refresh token families last 90 days. Atomically replace rotated refresh tokens. Reuse may revoke the family; reauthorize after invalid_grant. Fix registration or scope configuration for invalid_client or invalid_scope instead of repeating login.

        Args:
            body: The request body. Accepts: ExchangeOAuthTokenRequestApplicationXWwwFormUrlencodedOneOfValue1, ExchangeOAuthTokenRequestApplicationXWwwFormUrlencodedOneOfValue2, or a plain dict.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkExchangeOAuthTokenResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [],
                    [{"schemeName": "oauthClientBasic", "scopes": []}],
                ],
                "anonymous": True,
                "apiKey": False,
                "accessToken": False,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": True,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": False,
            },
            "server": "https://sell.app",
            "encoding": "form-urlencoded",
            "no_retry": True,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "exchangeOAuthToken",
        }
        _body: Dict[str, Any] = body if isinstance(body, dict) else body.to_dict()
        return await self._client.request(
            method="post",
            path=("oauth", "token"),
            body=_body,
            model=SdkExchangeOAuthTokenResponseValue200ApplicationJson,
            request_options=request_options,
        )

    async def revoke_oauth_token(
        self,
        *,
        token: str,
        client_id: Optional[str] = None,
        client_secret: Optional[str] = None,
        token_type_hint: Optional[
            Union[
                SdkRevokeOAuthTokenRequestApplicationXWwwFormUrlencodedTokenTypeHint,
                str,
            ]
        ] = None,
        request_options: Optional[RequestOptions] = None,
    ) -> Any:
        """Revoke an OAuth token

        Revoke the matching personal CLI installation and all its token families. Send the configured public client_id without a secret. Unknown tokens also succeed without disclosing whether a token existed.

        Args:
            client_id: The configured official CLI public client ID. It identifies the registration, not the executable.
            client_secret: Legacy SDK field retained for compatibility. Omit for the official CLI; it has no client secret.
            token: The token.
            token_type_hint: The token type hint.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            Any

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [],
                    [{"schemeName": "oauthClientBasic", "scopes": []}],
                ],
                "anonymous": True,
                "apiKey": False,
                "accessToken": False,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": True,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": False,
            },
            "server": "https://sell.app",
            "encoding": "form-urlencoded",
            "no_retry": True,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "revokeOAuthToken",
        }
        body: Dict[str, Any] = {
            k: v
            for k, v in {
                "client_id": client_id,
                "client_secret": client_secret,
                "token": token,
                "token_type_hint": enum_value(token_type_hint)
                if token_type_hint is not None
                else None,
            }.items()
            if v is not None
        }
        _validate_model(
            body,
            {
                "name": "SdkRevokeOAuthTokenRequestApplicationXWwwFormUrlencoded",
                "required": ["token"],
                "properties": {
                    "client_id": {
                        "kind": "primitive",
                        "type": "string",
                        "format": "uuid",
                    },
                    "client_secret": {"kind": "primitive", "type": "string"},
                    "token": {"kind": "primitive", "type": "string"},
                    "token_type_hint": {
                        "kind": "enum",
                        "values": ["access_token", "refresh_token"],
                    },
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return await self._client.request(
            method="post",
            path=("oauth", "revoke"),
            body=body,
            request_options=request_options,
        )
