# This file is auto-generated by oagen. Do not edit.

from __future__ import annotations

from typing import TYPE_CHECKING, Any

from .._base_client import WithRawResponse

if TYPE_CHECKING:
    from .._client import AsyncSellAppClient, SellAppClient

from .._types import RequestOptions, _validate_model
from .models import (
    SdkCreatePaymentConnectionHandoffResponseValue200ApplicationJson,
    SdkEnableOrDisablePaymentMethodResponseValue200ApplicationJson,
    SdkGetPaymentMethodStatusResponseValue200ApplicationJson,
    SdkListPaymentMethodsResponseValue200ApplicationJson,
    SdkValidateAndSavePaymentMethodConfigurationResponseValue200ApplicationJson,
    ValidateAndSavePaymentMethodConfigurationRequestApplicationJsonOneOfValue1,
    ValidateAndSavePaymentMethodConfigurationRequestApplicationJsonOneOfValue2,
    ValidateAndSavePaymentMethodConfigurationRequestApplicationJsonOneOfValue3,
    ValidateAndSavePaymentMethodConfigurationRequestApplicationJsonOneOfValue4,
    ValidateAndSavePaymentMethodConfigurationRequestApplicationJsonOneOfValue5,
    ValidateAndSavePaymentMethodConfigurationRequestApplicationJsonOneOfValue6,
)


class PaymentMethods:
    """Payment Methods API resources."""

    def __init__(self, client: SellAppClient) -> None:
        self._client = client
        self.with_raw_response = WithRawResponse(self)

    def list(
        self,
        *,
        request_options: RequestOptions | None = None,
    ) -> SdkListPaymentMethodsResponseValue200ApplicationJson:
        """List payment methods

        List safe, pollable setup and checkout status for every payment method available to the store. Provider credentials and wallet addresses are never serialized. Requires the `payment-method` ability or the stronger legacy `store` ability. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Returns:
            SdkListPaymentMethodsResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "listPaymentMethods",
        }
        return self._client.request(
            method="get",
            path=("v2", "payment-methods"),
            model=SdkListPaymentMethodsResponseValue200ApplicationJson,
            request_options=request_options,
        )

    def get(
        self,
        payment_method: str,
        *,
        request_options: RequestOptions | None = None,
    ) -> SdkGetPaymentMethodStatusResponseValue200ApplicationJson:
        """Retrieve payment method status

        Poll one payment method's connection, health, required-input, and checkout-enabled state. No stored secret is returned. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            payment_method: Uppercase payment method identifier returned by the collection endpoint.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkGetPaymentMethodStatusResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "getPaymentMethodStatus",
        }
        return self._client.request(
            method="get",
            path=("v2", "payment-methods", str(payment_method)),
            model=SdkGetPaymentMethodStatusResponseValue200ApplicationJson,
            request_options=request_options,
        )

    def enable(
        self,
        payment_method: str,
        *,
        enabled: bool,
        request_options: RequestOptions | None = None,
    ) -> SdkEnableOrDisablePaymentMethodResponseValue200ApplicationJson:
        """Enable or disable a payment method

        Change checkout availability after setup has succeeded. Enabling a disconnected method returns an actionable validation error. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            payment_method: Uppercase payment method identifier returned by the collection endpoint.
            enabled: The enabled.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkEnableOrDisablePaymentMethodResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "enableOrDisablePaymentMethod",
        }
        body: dict[str, Any] = {
            "enabled": enabled,
        }
        _validate_model(
            body,
            {
                "name": "SdkEnableOrDisablePaymentMethodRequestApplicationJson",
                "required": ["enabled"],
                "properties": {"enabled": {"kind": "primitive", "type": "boolean"}},
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return self._client.request(
            method="patch",
            path=("v2", "payment-methods", str(payment_method)),
            body=body,
            model=SdkEnableOrDisablePaymentMethodResponseValue200ApplicationJson,
            request_options=request_options,
        )

    def connect(
        self,
        payment_method: str,
        *,
        request_options: RequestOptions | None = None,
    ) -> SdkCreatePaymentConnectionHandoffResponseValue200ApplicationJson:
        """Create a payment connection handoff

        Return a provider-hosted connect URL and a status URL to poll. Currently supported for Stripe and PayPal; unsupported methods return a validation error. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            payment_method: Uppercase payment method identifier returned by the collection endpoint.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkCreatePaymentConnectionHandoffResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "createPaymentConnectionHandoff",
        }
        return self._client.request(
            method="post",
            path=("v2", "payment-methods", str(payment_method), "connect"),
            model=SdkCreatePaymentConnectionHandoffResponseValue200ApplicationJson,
            request_options=request_options,
        )

    def validate(
        self,
        payment_method: str,
        *,
        body: ValidateAndSavePaymentMethodConfigurationRequestApplicationJsonOneOfValue1
        | ValidateAndSavePaymentMethodConfigurationRequestApplicationJsonOneOfValue2
        | ValidateAndSavePaymentMethodConfigurationRequestApplicationJsonOneOfValue3
        | ValidateAndSavePaymentMethodConfigurationRequestApplicationJsonOneOfValue4
        | ValidateAndSavePaymentMethodConfigurationRequestApplicationJsonOneOfValue5
        | ValidateAndSavePaymentMethodConfigurationRequestApplicationJsonOneOfValue6
        | dict[str, Any],
        request_options: RequestOptions | None = None,
    ) -> SdkValidateAndSavePaymentMethodConfigurationResponseValue200ApplicationJson:
        """Validate and save payment method configuration

        Validate provider credentials or crypto wallet input with the same setup services used by the dashboard, then save the configuration. Secret fields are write-only and never appear in responses or audit payloads. Supported methods are NMI, Mercado Pago, Razorpay, Mollie, LiFi, and Bitcart-backed crypto wallets. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            payment_method: Uppercase payment method identifier returned by the collection endpoint.
            body: The request body. Accepts: ValidateAndSavePaymentMethodConfigurationRequestApplicationJsonOneOfValue1, ValidateAndSavePaymentMethodConfigurationRequestApplicationJsonOneOfValue2, ValidateAndSavePaymentMethodConfigurationRequestApplicationJsonOneOfValue3, ValidateAndSavePaymentMethodConfigurationRequestApplicationJsonOneOfValue4, ValidateAndSavePaymentMethodConfigurationRequestApplicationJsonOneOfValue5, ValidateAndSavePaymentMethodConfigurationRequestApplicationJsonOneOfValue6, or a plain dict.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkValidateAndSavePaymentMethodConfigurationResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "validateAndSavePaymentMethodConfiguration",
        }
        _body: dict[str, Any] = body if isinstance(body, dict) else body.to_dict()
        return self._client.request(
            method="put",
            path=("v2", "payment-methods", str(payment_method), "configuration"),
            body=_body,
            model=SdkValidateAndSavePaymentMethodConfigurationResponseValue200ApplicationJson,
            request_options=request_options,
        )


class AsyncPaymentMethods:
    """Payment Methods API resources (async)."""

    def __init__(self, client: AsyncSellAppClient) -> None:
        self._client = client
        self.with_raw_response = WithRawResponse(self)

    async def list(
        self,
        *,
        request_options: RequestOptions | None = None,
    ) -> SdkListPaymentMethodsResponseValue200ApplicationJson:
        """List payment methods

        List safe, pollable setup and checkout status for every payment method available to the store. Provider credentials and wallet addresses are never serialized. Requires the `payment-method` ability or the stronger legacy `store` ability. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Returns:
            SdkListPaymentMethodsResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "listPaymentMethods",
        }
        return await self._client.request(
            method="get",
            path=("v2", "payment-methods"),
            model=SdkListPaymentMethodsResponseValue200ApplicationJson,
            request_options=request_options,
        )

    async def get(
        self,
        payment_method: str,
        *,
        request_options: RequestOptions | None = None,
    ) -> SdkGetPaymentMethodStatusResponseValue200ApplicationJson:
        """Retrieve payment method status

        Poll one payment method's connection, health, required-input, and checkout-enabled state. No stored secret is returned. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            payment_method: Uppercase payment method identifier returned by the collection endpoint.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkGetPaymentMethodStatusResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "getPaymentMethodStatus",
        }
        return await self._client.request(
            method="get",
            path=("v2", "payment-methods", str(payment_method)),
            model=SdkGetPaymentMethodStatusResponseValue200ApplicationJson,
            request_options=request_options,
        )

    async def enable(
        self,
        payment_method: str,
        *,
        enabled: bool,
        request_options: RequestOptions | None = None,
    ) -> SdkEnableOrDisablePaymentMethodResponseValue200ApplicationJson:
        """Enable or disable a payment method

        Change checkout availability after setup has succeeded. Enabling a disconnected method returns an actionable validation error. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            payment_method: Uppercase payment method identifier returned by the collection endpoint.
            enabled: The enabled.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkEnableOrDisablePaymentMethodResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "enableOrDisablePaymentMethod",
        }
        body: dict[str, Any] = {
            "enabled": enabled,
        }
        _validate_model(
            body,
            {
                "name": "SdkEnableOrDisablePaymentMethodRequestApplicationJson",
                "required": ["enabled"],
                "properties": {"enabled": {"kind": "primitive", "type": "boolean"}},
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return await self._client.request(
            method="patch",
            path=("v2", "payment-methods", str(payment_method)),
            body=body,
            model=SdkEnableOrDisablePaymentMethodResponseValue200ApplicationJson,
            request_options=request_options,
        )

    async def connect(
        self,
        payment_method: str,
        *,
        request_options: RequestOptions | None = None,
    ) -> SdkCreatePaymentConnectionHandoffResponseValue200ApplicationJson:
        """Create a payment connection handoff

        Return a provider-hosted connect URL and a status URL to poll. Currently supported for Stripe and PayPal; unsupported methods return a validation error. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            payment_method: Uppercase payment method identifier returned by the collection endpoint.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkCreatePaymentConnectionHandoffResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "createPaymentConnectionHandoff",
        }
        return await self._client.request(
            method="post",
            path=("v2", "payment-methods", str(payment_method), "connect"),
            model=SdkCreatePaymentConnectionHandoffResponseValue200ApplicationJson,
            request_options=request_options,
        )

    async def validate(
        self,
        payment_method: str,
        *,
        body: ValidateAndSavePaymentMethodConfigurationRequestApplicationJsonOneOfValue1
        | ValidateAndSavePaymentMethodConfigurationRequestApplicationJsonOneOfValue2
        | ValidateAndSavePaymentMethodConfigurationRequestApplicationJsonOneOfValue3
        | ValidateAndSavePaymentMethodConfigurationRequestApplicationJsonOneOfValue4
        | ValidateAndSavePaymentMethodConfigurationRequestApplicationJsonOneOfValue5
        | ValidateAndSavePaymentMethodConfigurationRequestApplicationJsonOneOfValue6
        | dict[str, Any],
        request_options: RequestOptions | None = None,
    ) -> SdkValidateAndSavePaymentMethodConfigurationResponseValue200ApplicationJson:
        """Validate and save payment method configuration

        Validate provider credentials or crypto wallet input with the same setup services used by the dashboard, then save the configuration. Secret fields are write-only and never appear in responses or audit payloads. Supported methods are NMI, Mercado Pago, Razorpay, Mollie, LiFi, and Bitcart-backed crypto wallets. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            payment_method: Uppercase payment method identifier returned by the collection endpoint.
            body: The request body. Accepts: ValidateAndSavePaymentMethodConfigurationRequestApplicationJsonOneOfValue1, ValidateAndSavePaymentMethodConfigurationRequestApplicationJsonOneOfValue2, ValidateAndSavePaymentMethodConfigurationRequestApplicationJsonOneOfValue3, ValidateAndSavePaymentMethodConfigurationRequestApplicationJsonOneOfValue4, ValidateAndSavePaymentMethodConfigurationRequestApplicationJsonOneOfValue5, ValidateAndSavePaymentMethodConfigurationRequestApplicationJsonOneOfValue6, or a plain dict.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkValidateAndSavePaymentMethodConfigurationResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "validateAndSavePaymentMethodConfiguration",
        }
        _body: dict[str, Any] = body if isinstance(body, dict) else body.to_dict()
        return await self._client.request(
            method="put",
            path=("v2", "payment-methods", str(payment_method), "configuration"),
            body=_body,
            model=SdkValidateAndSavePaymentMethodConfigurationResponseValue200ApplicationJson,
            request_options=request_options,
        )
