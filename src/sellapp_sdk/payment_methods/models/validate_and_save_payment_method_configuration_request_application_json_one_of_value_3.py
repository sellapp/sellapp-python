# This file is auto-generated by oagen. Do not edit.

from __future__ import annotations

from dataclasses import dataclass
from dataclasses import field as dataclass_field
from typing import Any, Dict, List

from sellapp_sdk._types import (
    _preserve_unknown_fields,
    _raise_deserialize_error,
    _validate_model,
)

_DOMAIN_RUNTIME_SCHEMA: Dict[str, Any] = {
    "name": "ValidateAndSavePaymentMethodConfigurationRequestApplicationJsonOneOfValue3",
    "required": [
        "merchant_id",
        "key_id",
        "key_secret",
        "webhook_secret",
        "primary_currency",
        "international_payments_enabled",
        "supported_currencies",
    ],
    "properties": {
        "merchant_id": {"kind": "primitive", "type": "string"},
        "key_id": {"kind": "primitive", "type": "string"},
        "key_secret": {"kind": "primitive", "type": "string"},
        "webhook_secret": {"kind": "primitive", "type": "string"},
        "primary_currency": {"kind": "primitive", "type": "string"},
        "international_payments_enabled": {"kind": "primitive", "type": "boolean"},
        "supported_currencies": {
            "kind": "array",
            "items": {"kind": "primitive", "type": "string"},
            "constraints": {"minItems": 1},
        },
    },
    "additionalProperties": True,
}
_WIRE_RUNTIME_SCHEMA: Dict[str, Any] = {
    "name": "ValidateAndSavePaymentMethodConfigurationRequestApplicationJsonOneOfValue3",
    "required": [
        "merchant_id",
        "key_id",
        "key_secret",
        "webhook_secret",
        "primary_currency",
        "international_payments_enabled",
        "supported_currencies",
    ],
    "properties": {
        "merchant_id": {"kind": "primitive", "type": "string"},
        "key_id": {"kind": "primitive", "type": "string"},
        "key_secret": {"kind": "primitive", "type": "string"},
        "webhook_secret": {"kind": "primitive", "type": "string"},
        "primary_currency": {"kind": "primitive", "type": "string"},
        "international_payments_enabled": {"kind": "primitive", "type": "boolean"},
        "supported_currencies": {
            "kind": "array",
            "items": {"kind": "primitive", "type": "string"},
            "constraints": {"minItems": 1},
        },
    },
    "additionalProperties": True,
}


@dataclass(slots=True)
class ValidateAndSavePaymentMethodConfigurationRequestApplicationJsonOneOfValue3:
    """Validate And Save Payment Method Configuration Request Application Json One Of Value3 model."""

    merchant_id: str
    key_id: str
    key_secret: str
    """Write-only secret. SellApp never returns the stored value."""
    webhook_secret: str
    """Write-only secret. SellApp never returns the stored value."""
    primary_currency: str
    international_payments_enabled: bool
    supported_currencies: List[str]
    additional_properties: Dict[str, Any] = dataclass_field(
        default_factory=dict, repr=False
    )
    _from_response: bool = dataclass_field(default=False, repr=False, compare=False)

    @classmethod
    def from_dict(
        cls, data: Dict[str, Any]
    ) -> ValidateAndSavePaymentMethodConfigurationRequestApplicationJsonOneOfValue3:
        """Deserialize from a dictionary."""
        try:
            _validate_model(
                data,
                _WIRE_RUNTIME_SCHEMA,
                direction="response",
                allow_unknown_enums=True,
                allow_unknown_union_variants=True,
                allow_unknown_fields=True,
            )
            return cls(
                merchant_id=data["merchant_id"],
                key_id=data["key_id"],
                key_secret=data["key_secret"],
                webhook_secret=data["webhook_secret"],
                primary_currency=data["primary_currency"],
                international_payments_enabled=data["international_payments_enabled"],
                supported_currencies=data["supported_currencies"],
                additional_properties=_preserve_unknown_fields(
                    data,
                    [
                        "merchant_id",
                        "key_id",
                        "key_secret",
                        "webhook_secret",
                        "primary_currency",
                        "international_payments_enabled",
                        "supported_currencies",
                    ],
                ),
                _from_response=True,
            )
        except (KeyError, ValueError) as e:
            _raise_deserialize_error(
                "ValidateAndSavePaymentMethodConfigurationRequestApplicationJsonOneOfValue3",
                e,
            )

    def to_dict(self) -> Dict[str, Any]:
        """Serialize to a dictionary."""
        _domain_data: Dict[str, Any] = dict(self.additional_properties)
        _domain_data["merchant_id"] = self.merchant_id
        _domain_data["key_id"] = self.key_id
        _domain_data["key_secret"] = self.key_secret
        _domain_data["webhook_secret"] = self.webhook_secret
        _domain_data["primary_currency"] = self.primary_currency
        _domain_data["international_payments_enabled"] = (
            self.international_payments_enabled
        )
        _domain_data["supported_currencies"] = self.supported_currencies
        _validate_model(
            _domain_data,
            _DOMAIN_RUNTIME_SCHEMA,
            direction="request",
            allow_unknown_enums=self._from_response,
            allow_unknown_union_variants=self._from_response,
            allow_unknown_fields=(self._from_response or True),
        )
        result: Dict[str, Any] = dict(self.additional_properties)
        result["merchant_id"] = self.merchant_id
        result["key_id"] = self.key_id
        result["key_secret"] = self.key_secret
        result["webhook_secret"] = self.webhook_secret
        result["primary_currency"] = self.primary_currency
        result["international_payments_enabled"] = self.international_payments_enabled
        result["supported_currencies"] = self.supported_currencies
        return result
