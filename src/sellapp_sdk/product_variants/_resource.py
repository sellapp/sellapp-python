# This file is auto-generated by oagen. Do not edit.

from __future__ import annotations

from typing import TYPE_CHECKING, Any

from .._base_client import WithRawResponse

if TYPE_CHECKING:
    from .._client import AsyncSellAppClient, SellAppClient

import builtins

from sellapp_sdk.common.models.create_product_variant_request_application_json_property_bulk_discount_item import (
    CreateProductVariantRequestApplicationJsonPropertyBulkDiscountItem,
)
from sellapp_sdk.common.models.create_product_variant_request_application_json_property_deliverable import (
    CreateProductVariantRequestApplicationJsonPropertyDeliverable,
)
from sellapp_sdk.common.models.create_product_variant_request_application_json_property_other_settings import (
    CreateProductVariantRequestApplicationJsonPropertyOtherSettings,
)
from sellapp_sdk.common.models.create_product_variant_request_application_json_property_pricing import (
    CreateProductVariantRequestApplicationJsonPropertyPricing,
)
from sellapp_sdk.common.models.sdk_batch_create_product_variants_response_value_200_application_json import (
    SdkBatchCreateProductVariantsResponseValue200ApplicationJson,
)
from sellapp_sdk.common.models.sdk_create_product_variant_request_application_json_payment_methods import (
    SdkCreateProductVariantRequestApplicationJsonPaymentMethods,
)
from sellapp_sdk.common.models.sdk_replace_product_variant_with_put_request_application_json_payment_methods import (
    SdkReplaceProductVariantWithPutRequestApplicationJsonPaymentMethods,
)
from sellapp_sdk.common.models.sdk_update_product_variant_request_application_json_payment_methods import (
    SdkUpdateProductVariantRequestApplicationJsonPaymentMethods,
)

from .._pagination import AsyncPage, SyncPage
from .._types import NOT_GIVEN, NotGiven, RequestOptions, _validate_model
from .models import (
    BatchCreateProductVariantsRequestApplicationJsonPropertyResourcesItem,
    BatchUpdateProductVariantsRequestApplicationJsonPropertyResources,
    ListProductVariantsResponseValue200ApplicationJsonPropertyDataItem,
    ReplaceProductVariantWithPutRequestApplicationJsonPropertyBulkDiscountItem,
    ReplaceProductVariantWithPutRequestApplicationJsonPropertyDeliverable,
    ReplaceProductVariantWithPutRequestApplicationJsonPropertyOtherSettings,
    ReplaceProductVariantWithPutRequestApplicationJsonPropertyPricing,
    SdkBatchUpdateProductVariantsResponseValue200ApplicationJson,
    SdkCreateProductVariantResponseValue201ApplicationJson,
    SdkGetProductVariantResponseValue200ApplicationJson,
    SdkReplaceProductVariantWithPutResponseValue200ApplicationJson,
    SdkUpdateProductVariantResponseValue200ApplicationJson,
    SearchProductVariantsRequestApplicationJsonPropertyFiltersItem,
    SearchProductVariantsRequestApplicationJsonPropertyIncludesItem,
    SearchProductVariantsRequestApplicationJsonPropertySearch,
    SearchProductVariantsRequestApplicationJsonPropertySortItem,
    SearchProductVariantsResponseValue200ApplicationJsonPropertyDataItem,
    UpdateProductVariantRequestApplicationJsonPropertyBulkDiscountItem,
    UpdateProductVariantRequestApplicationJsonPropertyDeliverable,
    UpdateProductVariantRequestApplicationJsonPropertyOtherSettings,
    UpdateProductVariantRequestApplicationJsonPropertyPricing,
)


class ProductVariants:
    """Product Variants API resources."""

    def __init__(self, client: SellAppClient) -> None:
        self._client = client
        self.with_raw_response = WithRawResponse(self)

    def list(
        self,
        product: int,
        *,
        limit: int | None = None,
        before: str | None = None,
        after: str | None = None,
        order: str | None = None,
        page: int | None = None,
        with_drafts: bool | None = None,
        only_drafts: bool | None = None,
        request_options: RequestOptions | None = None,
    ) -> SyncPage[ListProductVariantsResponseValue200ApplicationJsonPropertyDataItem]:
        """List all product variants

        List a product's variants, 15 per page by default. Requires a token with the listing ability. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            product: The product path parameter.
            limit: Number of items to return per page. Defaults to `15`.
            before: The before.
            after: The after.
            order: The order.
            page: Page number to return. Defaults to `1`.
            with_drafts: Include draft variants alongside published variants.
            only_drafts: Return only draft variants.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SyncPage[ListProductVariantsResponseValue200ApplicationJsonPropertyDataItem]

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "listProductVariants",
        }
        params = {
            k: v
            for k, v in {
                "limit": limit,
                "before": before,
                "after": after,
                "order": order,
                "page": page,
                "with_drafts": with_drafts,
                "only_drafts": only_drafts,
            }.items()
            if v is not None
        }
        return self._client.request_page(
            method="get",
            path=("v2", "products", str(product), "variants"),
            model=ListProductVariantsResponseValue200ApplicationJsonPropertyDataItem,
            params=params,
            request_options=request_options,
        )

    def create(
        self,
        product: int,
        *,
        title: str,
        description: str,
        deliverable: CreateProductVariantRequestApplicationJsonPropertyDeliverable,
        pricing: CreateProductVariantRequestApplicationJsonPropertyPricing,
        payment_methods: builtins.list[
            SdkCreateProductVariantRequestApplicationJsonPaymentMethods | str
        ],
        minimum_purchase_quantity: int | None = None,
        maximum_purchase_quantity: int | None | NotGiven = NOT_GIVEN,
        bulk_discount: builtins.list[
            CreateProductVariantRequestApplicationJsonPropertyBulkDiscountItem
        ]
        | None = None,
        other_settings: CreateProductVariantRequestApplicationJsonPropertyOtherSettings
        | None = None,
        expected_updated_at: str | None = None,
        request_options: RequestOptions | None = None,
    ) -> SdkCreateProductVariantResponseValue201ApplicationJson:
        """Create a product variant

        Create a product variant for the authenticated store. Course, credits, and add-on products permit only one variant. Requires a token with the listing ability. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            product: The product path parameter.
            title: The title.
            description: The description.
            deliverable: The deliverable.
            pricing: The pricing.
            minimum_purchase_quantity: The minimum purchase quantity.
            maximum_purchase_quantity: The maximum purchase quantity.
            bulk_discount: The bulk discount.
            payment_methods: The payment methods.
            other_settings: The other settings.
            expected_updated_at: Optional optimistic-concurrency snapshot for updates. Use the variant's latest updated_at value; stale snapshots receive a 422 validation response.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkCreateProductVariantResponseValue201ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "createProductVariant",
        }
        body: dict[str, Any] = {
            k: v
            for k, v in {
                "title": title,
                "description": description,
                "deliverable": deliverable.to_dict(),
                "pricing": pricing.to_dict(),
                "minimum_purchase_quantity": minimum_purchase_quantity,
                "bulk_discount": [item.to_dict() for item in bulk_discount]
                if bulk_discount is not None
                else None,
                "payment_methods": payment_methods,
                "other_settings": other_settings.to_dict()
                if other_settings is not None
                else None,
                "expected_updated_at": expected_updated_at,
            }.items()
            if v is not None
        }
        if not isinstance(maximum_purchase_quantity, NotGiven):
            body["maximum_purchase_quantity"] = maximum_purchase_quantity
        _validate_model(
            body,
            {
                "name": "SdkCreateProductVariantRequestApplicationJson",
                "required": [
                    "title",
                    "description",
                    "deliverable",
                    "pricing",
                    "payment_methods",
                ],
                "properties": {
                    "title": {"kind": "primitive", "type": "string"},
                    "description": {"kind": "primitive", "type": "string"},
                    "deliverable": {"kind": "any"},
                    "pricing": {"kind": "any"},
                    "minimum_purchase_quantity": {
                        "kind": "primitive",
                        "type": "integer",
                    },
                    "maximum_purchase_quantity": {
                        "kind": "nullable",
                        "inner": {"kind": "primitive", "type": "integer"},
                    },
                    "bulk_discount": {"kind": "array", "items": {"kind": "any"}},
                    "payment_methods": {
                        "kind": "array",
                        "items": {
                            "kind": "enum",
                            "values": [
                                "AUTHNET",
                                "BTCPAY",
                                "CASHAPP",
                                "COINBASE",
                                "PADDLE",
                                "PAYDASH",
                                "PAYPAL",
                                "PAYSTACK",
                                "SQUARE",
                                "STRIPE",
                                "VENMO",
                                "NMI",
                                "MERCADO_PAGO",
                                "MOLLIE",
                                "RAZORPAY",
                                "CUSTOM_PAYMENT_METHOD",
                                "LIFI",
                                "BTC",
                                "LTC",
                                "ETH",
                                "XMR",
                                "SOL",
                                "ADA",
                                "BNB",
                                "TRX",
                                "MATIC",
                                "ETH_USDT",
                                "ETH_USDC",
                                "ETH_UNI",
                                "ETH_SHIB",
                                "ETH_DAI",
                                "BNB_USDT",
                                "BNB_USDC",
                                "TRX_USDT",
                                "TRX_USDC",
                                "SOL_USDT",
                                "SOL_USDC",
                            ],
                        },
                        "constraints": {"minItems": 1},
                    },
                    "other_settings": {"kind": "any"},
                    "expected_updated_at": {
                        "kind": "primitive",
                        "type": "string",
                        "format": "date-time",
                    },
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return self._client.request(
            method="post",
            path=("v2", "products", str(product), "variants"),
            body=body,
            model=SdkCreateProductVariantResponseValue201ApplicationJson,
            request_options=request_options,
        )

    def get(
        self,
        product: int,
        variant: int,
        *,
        with_drafts: bool | None = None,
        only_drafts: bool | None = None,
        request_options: RequestOptions | None = None,
    ) -> SdkGetProductVariantResponseValue200ApplicationJson:
        """Retrieve a product variant

        Retrieve a variant using its product ID and variant ID. Requires a token with the listing ability. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            product: The product path parameter.
            variant: The variant path parameter.
            with_drafts: Include draft variants alongside published variants.
            only_drafts: Return only draft variants.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkGetProductVariantResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "getProductVariant",
        }
        params: dict[str, Any] = {
            k: v
            for k, v in {
                "with_drafts": with_drafts,
                "only_drafts": only_drafts,
            }.items()
            if v is not None
        }
        return self._client.request(
            method="get",
            path=("v2", "products", str(product), "variants", str(variant)),
            params=params,
            model=SdkGetProductVariantResponseValue200ApplicationJson,
            request_options=request_options,
        )

    def replace(
        self,
        product: int,
        variant: int,
        *,
        title: str | None = None,
        description: str | None = None,
        deliverable: ReplaceProductVariantWithPutRequestApplicationJsonPropertyDeliverable
        | None = None,
        pricing: ReplaceProductVariantWithPutRequestApplicationJsonPropertyPricing
        | None = None,
        minimum_purchase_quantity: int | None = None,
        maximum_purchase_quantity: int | None | NotGiven = NOT_GIVEN,
        bulk_discount: builtins.list[
            ReplaceProductVariantWithPutRequestApplicationJsonPropertyBulkDiscountItem
        ]
        | None = None,
        payment_methods: builtins.list[
            SdkReplaceProductVariantWithPutRequestApplicationJsonPaymentMethods | str
        ]
        | None = None,
        other_settings: ReplaceProductVariantWithPutRequestApplicationJsonPropertyOtherSettings
        | None = None,
        expected_updated_at: str | None = None,
        request_options: RequestOptions | None = None,
    ) -> SdkReplaceProductVariantWithPutResponseValue200ApplicationJson:
        """Update a product variant with PUT

        Update a product variant using the PUT compatibility method. Fields remain partial for backward compatibility. Requires a token with the listing ability. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            product: The product path parameter.
            variant: The variant path parameter.
            title: The title.
            description: The description.
            deliverable: The deliverable.
            pricing: The pricing.
            minimum_purchase_quantity: The minimum purchase quantity.
            maximum_purchase_quantity: The maximum purchase quantity.
            bulk_discount: The bulk discount.
            payment_methods: The payment methods.
            other_settings: The other settings.
            expected_updated_at: Optional optimistic-concurrency snapshot for updates. Use the variant's latest updated_at value; stale snapshots receive a 422 validation response.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkReplaceProductVariantWithPutResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "replaceProductVariantWithPut",
        }
        body: dict[str, Any] = {
            k: v
            for k, v in {
                "title": title,
                "description": description,
                "deliverable": deliverable.to_dict()
                if deliverable is not None
                else None,
                "pricing": pricing.to_dict() if pricing is not None else None,
                "minimum_purchase_quantity": minimum_purchase_quantity,
                "bulk_discount": [item.to_dict() for item in bulk_discount]
                if bulk_discount is not None
                else None,
                "payment_methods": payment_methods,
                "other_settings": other_settings.to_dict()
                if other_settings is not None
                else None,
                "expected_updated_at": expected_updated_at,
            }.items()
            if v is not None
        }
        if not isinstance(maximum_purchase_quantity, NotGiven):
            body["maximum_purchase_quantity"] = maximum_purchase_quantity
        _validate_model(
            body,
            {
                "name": "SdkReplaceProductVariantWithPutRequestApplicationJson",
                "required": [],
                "properties": {
                    "title": {"kind": "primitive", "type": "string"},
                    "description": {"kind": "primitive", "type": "string"},
                    "deliverable": {"kind": "any"},
                    "pricing": {"kind": "any"},
                    "minimum_purchase_quantity": {
                        "kind": "primitive",
                        "type": "integer",
                    },
                    "maximum_purchase_quantity": {
                        "kind": "nullable",
                        "inner": {"kind": "primitive", "type": "integer"},
                    },
                    "bulk_discount": {"kind": "array", "items": {"kind": "any"}},
                    "payment_methods": {
                        "kind": "array",
                        "items": {
                            "kind": "enum",
                            "values": [
                                "AUTHNET",
                                "BTCPAY",
                                "CASHAPP",
                                "COINBASE",
                                "PADDLE",
                                "PAYDASH",
                                "PAYPAL",
                                "PAYSTACK",
                                "SQUARE",
                                "STRIPE",
                                "VENMO",
                                "NMI",
                                "MERCADO_PAGO",
                                "MOLLIE",
                                "RAZORPAY",
                                "CUSTOM_PAYMENT_METHOD",
                                "LIFI",
                                "BTC",
                                "LTC",
                                "ETH",
                                "XMR",
                                "SOL",
                                "ADA",
                                "BNB",
                                "TRX",
                                "MATIC",
                                "ETH_USDT",
                                "ETH_USDC",
                                "ETH_UNI",
                                "ETH_SHIB",
                                "ETH_DAI",
                                "BNB_USDT",
                                "BNB_USDC",
                                "TRX_USDT",
                                "TRX_USDC",
                                "SOL_USDT",
                                "SOL_USDC",
                            ],
                        },
                        "constraints": {"minItems": 1},
                    },
                    "other_settings": {"kind": "any"},
                    "expected_updated_at": {
                        "kind": "primitive",
                        "type": "string",
                        "format": "date-time",
                    },
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return self._client.request(
            method="put",
            path=("v2", "products", str(product), "variants", str(variant)),
            body=body,
            model=SdkReplaceProductVariantWithPutResponseValue200ApplicationJson,
            request_options=request_options,
        )

    def update(
        self,
        product: int,
        variant: int,
        *,
        title: str | None = None,
        description: str | None = None,
        deliverable: UpdateProductVariantRequestApplicationJsonPropertyDeliverable
        | None = None,
        pricing: UpdateProductVariantRequestApplicationJsonPropertyPricing
        | None = None,
        minimum_purchase_quantity: int | None = None,
        maximum_purchase_quantity: int | None | NotGiven = NOT_GIVEN,
        bulk_discount: builtins.list[
            UpdateProductVariantRequestApplicationJsonPropertyBulkDiscountItem
        ]
        | None = None,
        payment_methods: builtins.list[
            SdkUpdateProductVariantRequestApplicationJsonPaymentMethods | str
        ]
        | None = None,
        other_settings: UpdateProductVariantRequestApplicationJsonPropertyOtherSettings
        | None = None,
        expected_updated_at: str | None = None,
        request_options: RequestOptions | None = None,
    ) -> SdkUpdateProductVariantResponseValue200ApplicationJson:
        """Update a product variant

        Updates a product variant's details. Requires a token with the listing ability. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            product: The product path parameter.
            variant: The variant path parameter.
            title: The title.
            description: The description.
            deliverable: The deliverable.
            pricing: The pricing.
            minimum_purchase_quantity: The minimum purchase quantity.
            maximum_purchase_quantity: The maximum purchase quantity.
            bulk_discount: The bulk discount.
            payment_methods: The payment methods.
            other_settings: The other settings.
            expected_updated_at: Optional optimistic-concurrency snapshot for updates. Use the variant's latest updated_at value; stale snapshots receive a 422 validation response.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkUpdateProductVariantResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "updateProductVariant",
        }
        body: dict[str, Any] = {
            k: v
            for k, v in {
                "title": title,
                "description": description,
                "deliverable": deliverable.to_dict()
                if deliverable is not None
                else None,
                "pricing": pricing.to_dict() if pricing is not None else None,
                "minimum_purchase_quantity": minimum_purchase_quantity,
                "bulk_discount": [item.to_dict() for item in bulk_discount]
                if bulk_discount is not None
                else None,
                "payment_methods": payment_methods,
                "other_settings": other_settings.to_dict()
                if other_settings is not None
                else None,
                "expected_updated_at": expected_updated_at,
            }.items()
            if v is not None
        }
        if not isinstance(maximum_purchase_quantity, NotGiven):
            body["maximum_purchase_quantity"] = maximum_purchase_quantity
        _validate_model(
            body,
            {
                "name": "SdkUpdateProductVariantRequestApplicationJson",
                "required": [],
                "properties": {
                    "title": {"kind": "primitive", "type": "string"},
                    "description": {"kind": "primitive", "type": "string"},
                    "deliverable": {"kind": "any"},
                    "pricing": {"kind": "any"},
                    "minimum_purchase_quantity": {
                        "kind": "primitive",
                        "type": "integer",
                    },
                    "maximum_purchase_quantity": {
                        "kind": "nullable",
                        "inner": {"kind": "primitive", "type": "integer"},
                    },
                    "bulk_discount": {"kind": "array", "items": {"kind": "any"}},
                    "payment_methods": {
                        "kind": "array",
                        "items": {
                            "kind": "enum",
                            "values": [
                                "AUTHNET",
                                "BTCPAY",
                                "CASHAPP",
                                "COINBASE",
                                "PADDLE",
                                "PAYDASH",
                                "PAYPAL",
                                "PAYSTACK",
                                "SQUARE",
                                "STRIPE",
                                "VENMO",
                                "NMI",
                                "MERCADO_PAGO",
                                "MOLLIE",
                                "RAZORPAY",
                                "CUSTOM_PAYMENT_METHOD",
                                "LIFI",
                                "BTC",
                                "LTC",
                                "ETH",
                                "XMR",
                                "SOL",
                                "ADA",
                                "BNB",
                                "TRX",
                                "MATIC",
                                "ETH_USDT",
                                "ETH_USDC",
                                "ETH_UNI",
                                "ETH_SHIB",
                                "ETH_DAI",
                                "BNB_USDT",
                                "BNB_USDC",
                                "TRX_USDT",
                                "TRX_USDC",
                                "SOL_USDT",
                                "SOL_USDC",
                            ],
                        },
                        "constraints": {"minItems": 1},
                    },
                    "other_settings": {"kind": "any"},
                    "expected_updated_at": {
                        "kind": "primitive",
                        "type": "string",
                        "format": "date-time",
                    },
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return self._client.request(
            method="patch",
            path=("v2", "products", str(product), "variants", str(variant)),
            body=body,
            model=SdkUpdateProductVariantResponseValue200ApplicationJson,
            request_options=request_options,
        )

    def delete(
        self,
        product: int,
        variant: int,
        *,
        request_options: RequestOptions | None = None,
    ) -> None:
        """Delete a product variant

        Deletes a product variant. This will permanently delete the product variant and all of its details, including the sensitive deliverables, will no longer be retrievable. Requires a token with the listing ability. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            product: The product path parameter.
            variant: The variant path parameter.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "deleteProductVariant",
        }
        self._client.request(
            method="delete",
            path=("v2", "products", str(product), "variants", str(variant)),
            request_options=request_options,
        )

    def search(
        self,
        product: int,
        *,
        filters: builtins.list[
            SearchProductVariantsRequestApplicationJsonPropertyFiltersItem
        ]
        | None = None,
        sort: builtins.list[SearchProductVariantsRequestApplicationJsonPropertySortItem]
        | None = None,
        search: SearchProductVariantsRequestApplicationJsonPropertySearch | None = None,
        includes: builtins.list[
            SearchProductVariantsRequestApplicationJsonPropertyIncludesItem
        ]
        | None = None,
        limit: int | None = None,
        before: str | None = None,
        after: str | None = None,
        order: str | None = None,
        request_options: RequestOptions | None = None,
    ) -> SyncPage[SearchProductVariantsResponseValue200ApplicationJsonPropertyDataItem]:
        """Search product variants

        Search a product's variants using JSON body filters, search terms, includes, and sort instructions. Requires a token with the listing ability. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            product: The product path parameter.
            filters: The filters.
            sort: The sort.
            search: The search.
            includes: The includes.
            limit: The limit.
            before: The before.
            after: The after.
            order: The order.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SyncPage[SearchProductVariantsResponseValue200ApplicationJsonPropertyDataItem]

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "searchProductVariants",
        }
        body: dict[str, Any] = {
            k: v
            for k, v in {
                "filters": [item.to_dict() for item in filters]
                if filters is not None
                else None,
                "sort": [item.to_dict() for item in sort] if sort is not None else None,
                "search": search.to_dict() if search is not None else None,
                "includes": [item.to_dict() for item in includes]
                if includes is not None
                else None,
            }.items()
            if v is not None
        }
        params = {
            k: v
            for k, v in {
                "limit": limit,
                "before": before,
                "after": after,
                "order": order,
            }.items()
            if v is not None
        }
        return self._client.request_page(
            method="post",
            path=("v2", "products", str(product), "variants", "search"),
            model=SearchProductVariantsResponseValue200ApplicationJsonPropertyDataItem,
            body=body,
            params=params,
            request_options=request_options,
        )

    def batch_create(
        self,
        product: int,
        *,
        resources: builtins.list[
            BatchCreateProductVariantsRequestApplicationJsonPropertyResourcesItem
        ],
        request_options: RequestOptions | None = None,
    ) -> SdkBatchCreateProductVariantsResponseValue200ApplicationJson:
        """Batch create product variants

        Create multiple variants for a product in one request by sending a `resources` array. Requires a token with the listing ability. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            product: The product path parameter.
            resources: The resources.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkBatchCreateProductVariantsResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "batchCreateProductVariants",
        }
        body: dict[str, Any] = {
            "resources": [item.to_dict() for item in resources],
        }
        _validate_model(
            body,
            {
                "name": "SdkBatchCreateProductVariantsRequestApplicationJson",
                "required": ["resources"],
                "properties": {
                    "resources": {"kind": "array", "items": {"kind": "any"}}
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return self._client.request(
            method="post",
            path=("v2", "products", str(product), "variants", "batch"),
            body=body,
            model=SdkBatchCreateProductVariantsResponseValue200ApplicationJson,
            request_options=request_options,
        )

    def batch_update(
        self,
        product: int,
        *,
        resources: BatchUpdateProductVariantsRequestApplicationJsonPropertyResources,
        request_options: RequestOptions | None = None,
    ) -> SdkBatchUpdateProductVariantsResponseValue200ApplicationJson:
        """Batch update product variants

        Update multiple product variants in one request by sending a `resources` object keyed by variant ID. Requires a token with the listing ability. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            product: The product path parameter.
            resources: The resources.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkBatchUpdateProductVariantsResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "batchUpdateProductVariants",
        }
        body: dict[str, Any] = {
            "resources": resources.to_dict(),
        }
        _validate_model(
            body,
            {
                "name": "SdkBatchUpdateProductVariantsRequestApplicationJson",
                "required": ["resources"],
                "properties": {"resources": {"kind": "any"}},
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return self._client.request(
            method="patch",
            path=("v2", "products", str(product), "variants", "batch"),
            body=body,
            model=SdkBatchUpdateProductVariantsResponseValue200ApplicationJson,
            request_options=request_options,
        )

    def batch_delete(
        self,
        product: int,
        *,
        resources: builtins.list[int],
        request_options: RequestOptions | None = None,
    ) -> None:
        """Batch delete product variants

        Delete multiple product variants in one request by sending the variant IDs in `resources`. Requires a token with the listing ability. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            product: The product path parameter.
            resources: The resources.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "batchDeleteProductVariants",
        }
        body: dict[str, Any] = {
            "resources": resources,
        }
        _validate_model(
            body,
            {
                "name": "SdkBatchDeleteCouponsRequestApplicationJson",
                "required": ["resources"],
                "properties": {
                    "resources": {
                        "kind": "array",
                        "items": {"kind": "primitive", "type": "integer"},
                    }
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        self._client.request(
            method="delete",
            path=("v2", "products", str(product), "variants", "batch"),
            body=body,
            request_options=request_options,
        )


class AsyncProductVariants:
    """Product Variants API resources (async)."""

    def __init__(self, client: AsyncSellAppClient) -> None:
        self._client = client
        self.with_raw_response = WithRawResponse(self)

    async def list(
        self,
        product: int,
        *,
        limit: int | None = None,
        before: str | None = None,
        after: str | None = None,
        order: str | None = None,
        page: int | None = None,
        with_drafts: bool | None = None,
        only_drafts: bool | None = None,
        request_options: RequestOptions | None = None,
    ) -> AsyncPage[ListProductVariantsResponseValue200ApplicationJsonPropertyDataItem]:
        """List all product variants

        List a product's variants, 15 per page by default. Requires a token with the listing ability. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            product: The product path parameter.
            limit: Number of items to return per page. Defaults to `15`.
            before: The before.
            after: The after.
            order: The order.
            page: Page number to return. Defaults to `1`.
            with_drafts: Include draft variants alongside published variants.
            only_drafts: Return only draft variants.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            AsyncPage[ListProductVariantsResponseValue200ApplicationJsonPropertyDataItem]

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "listProductVariants",
        }
        params = {
            k: v
            for k, v in {
                "limit": limit,
                "before": before,
                "after": after,
                "order": order,
                "page": page,
                "with_drafts": with_drafts,
                "only_drafts": only_drafts,
            }.items()
            if v is not None
        }
        return await self._client.request_page(
            method="get",
            path=("v2", "products", str(product), "variants"),
            model=ListProductVariantsResponseValue200ApplicationJsonPropertyDataItem,
            params=params,
            request_options=request_options,
        )

    async def create(
        self,
        product: int,
        *,
        title: str,
        description: str,
        deliverable: CreateProductVariantRequestApplicationJsonPropertyDeliverable,
        pricing: CreateProductVariantRequestApplicationJsonPropertyPricing,
        payment_methods: builtins.list[
            SdkCreateProductVariantRequestApplicationJsonPaymentMethods | str
        ],
        minimum_purchase_quantity: int | None = None,
        maximum_purchase_quantity: int | None | NotGiven = NOT_GIVEN,
        bulk_discount: builtins.list[
            CreateProductVariantRequestApplicationJsonPropertyBulkDiscountItem
        ]
        | None = None,
        other_settings: CreateProductVariantRequestApplicationJsonPropertyOtherSettings
        | None = None,
        expected_updated_at: str | None = None,
        request_options: RequestOptions | None = None,
    ) -> SdkCreateProductVariantResponseValue201ApplicationJson:
        """Create a product variant

        Create a product variant for the authenticated store. Course, credits, and add-on products permit only one variant. Requires a token with the listing ability. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            product: The product path parameter.
            title: The title.
            description: The description.
            deliverable: The deliverable.
            pricing: The pricing.
            minimum_purchase_quantity: The minimum purchase quantity.
            maximum_purchase_quantity: The maximum purchase quantity.
            bulk_discount: The bulk discount.
            payment_methods: The payment methods.
            other_settings: The other settings.
            expected_updated_at: Optional optimistic-concurrency snapshot for updates. Use the variant's latest updated_at value; stale snapshots receive a 422 validation response.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkCreateProductVariantResponseValue201ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "createProductVariant",
        }
        body: dict[str, Any] = {
            k: v
            for k, v in {
                "title": title,
                "description": description,
                "deliverable": deliverable.to_dict(),
                "pricing": pricing.to_dict(),
                "minimum_purchase_quantity": minimum_purchase_quantity,
                "bulk_discount": [item.to_dict() for item in bulk_discount]
                if bulk_discount is not None
                else None,
                "payment_methods": payment_methods,
                "other_settings": other_settings.to_dict()
                if other_settings is not None
                else None,
                "expected_updated_at": expected_updated_at,
            }.items()
            if v is not None
        }
        if not isinstance(maximum_purchase_quantity, NotGiven):
            body["maximum_purchase_quantity"] = maximum_purchase_quantity
        _validate_model(
            body,
            {
                "name": "SdkCreateProductVariantRequestApplicationJson",
                "required": [
                    "title",
                    "description",
                    "deliverable",
                    "pricing",
                    "payment_methods",
                ],
                "properties": {
                    "title": {"kind": "primitive", "type": "string"},
                    "description": {"kind": "primitive", "type": "string"},
                    "deliverable": {"kind": "any"},
                    "pricing": {"kind": "any"},
                    "minimum_purchase_quantity": {
                        "kind": "primitive",
                        "type": "integer",
                    },
                    "maximum_purchase_quantity": {
                        "kind": "nullable",
                        "inner": {"kind": "primitive", "type": "integer"},
                    },
                    "bulk_discount": {"kind": "array", "items": {"kind": "any"}},
                    "payment_methods": {
                        "kind": "array",
                        "items": {
                            "kind": "enum",
                            "values": [
                                "AUTHNET",
                                "BTCPAY",
                                "CASHAPP",
                                "COINBASE",
                                "PADDLE",
                                "PAYDASH",
                                "PAYPAL",
                                "PAYSTACK",
                                "SQUARE",
                                "STRIPE",
                                "VENMO",
                                "NMI",
                                "MERCADO_PAGO",
                                "MOLLIE",
                                "RAZORPAY",
                                "CUSTOM_PAYMENT_METHOD",
                                "LIFI",
                                "BTC",
                                "LTC",
                                "ETH",
                                "XMR",
                                "SOL",
                                "ADA",
                                "BNB",
                                "TRX",
                                "MATIC",
                                "ETH_USDT",
                                "ETH_USDC",
                                "ETH_UNI",
                                "ETH_SHIB",
                                "ETH_DAI",
                                "BNB_USDT",
                                "BNB_USDC",
                                "TRX_USDT",
                                "TRX_USDC",
                                "SOL_USDT",
                                "SOL_USDC",
                            ],
                        },
                        "constraints": {"minItems": 1},
                    },
                    "other_settings": {"kind": "any"},
                    "expected_updated_at": {
                        "kind": "primitive",
                        "type": "string",
                        "format": "date-time",
                    },
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return await self._client.request(
            method="post",
            path=("v2", "products", str(product), "variants"),
            body=body,
            model=SdkCreateProductVariantResponseValue201ApplicationJson,
            request_options=request_options,
        )

    async def get(
        self,
        product: int,
        variant: int,
        *,
        with_drafts: bool | None = None,
        only_drafts: bool | None = None,
        request_options: RequestOptions | None = None,
    ) -> SdkGetProductVariantResponseValue200ApplicationJson:
        """Retrieve a product variant

        Retrieve a variant using its product ID and variant ID. Requires a token with the listing ability. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            product: The product path parameter.
            variant: The variant path parameter.
            with_drafts: Include draft variants alongside published variants.
            only_drafts: Return only draft variants.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkGetProductVariantResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "getProductVariant",
        }
        params: dict[str, Any] = {
            k: v
            for k, v in {
                "with_drafts": with_drafts,
                "only_drafts": only_drafts,
            }.items()
            if v is not None
        }
        return await self._client.request(
            method="get",
            path=("v2", "products", str(product), "variants", str(variant)),
            params=params,
            model=SdkGetProductVariantResponseValue200ApplicationJson,
            request_options=request_options,
        )

    async def replace(
        self,
        product: int,
        variant: int,
        *,
        title: str | None = None,
        description: str | None = None,
        deliverable: ReplaceProductVariantWithPutRequestApplicationJsonPropertyDeliverable
        | None = None,
        pricing: ReplaceProductVariantWithPutRequestApplicationJsonPropertyPricing
        | None = None,
        minimum_purchase_quantity: int | None = None,
        maximum_purchase_quantity: int | None | NotGiven = NOT_GIVEN,
        bulk_discount: builtins.list[
            ReplaceProductVariantWithPutRequestApplicationJsonPropertyBulkDiscountItem
        ]
        | None = None,
        payment_methods: builtins.list[
            SdkReplaceProductVariantWithPutRequestApplicationJsonPaymentMethods | str
        ]
        | None = None,
        other_settings: ReplaceProductVariantWithPutRequestApplicationJsonPropertyOtherSettings
        | None = None,
        expected_updated_at: str | None = None,
        request_options: RequestOptions | None = None,
    ) -> SdkReplaceProductVariantWithPutResponseValue200ApplicationJson:
        """Update a product variant with PUT

        Update a product variant using the PUT compatibility method. Fields remain partial for backward compatibility. Requires a token with the listing ability. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            product: The product path parameter.
            variant: The variant path parameter.
            title: The title.
            description: The description.
            deliverable: The deliverable.
            pricing: The pricing.
            minimum_purchase_quantity: The minimum purchase quantity.
            maximum_purchase_quantity: The maximum purchase quantity.
            bulk_discount: The bulk discount.
            payment_methods: The payment methods.
            other_settings: The other settings.
            expected_updated_at: Optional optimistic-concurrency snapshot for updates. Use the variant's latest updated_at value; stale snapshots receive a 422 validation response.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkReplaceProductVariantWithPutResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "replaceProductVariantWithPut",
        }
        body: dict[str, Any] = {
            k: v
            for k, v in {
                "title": title,
                "description": description,
                "deliverable": deliverable.to_dict()
                if deliverable is not None
                else None,
                "pricing": pricing.to_dict() if pricing is not None else None,
                "minimum_purchase_quantity": minimum_purchase_quantity,
                "bulk_discount": [item.to_dict() for item in bulk_discount]
                if bulk_discount is not None
                else None,
                "payment_methods": payment_methods,
                "other_settings": other_settings.to_dict()
                if other_settings is not None
                else None,
                "expected_updated_at": expected_updated_at,
            }.items()
            if v is not None
        }
        if not isinstance(maximum_purchase_quantity, NotGiven):
            body["maximum_purchase_quantity"] = maximum_purchase_quantity
        _validate_model(
            body,
            {
                "name": "SdkReplaceProductVariantWithPutRequestApplicationJson",
                "required": [],
                "properties": {
                    "title": {"kind": "primitive", "type": "string"},
                    "description": {"kind": "primitive", "type": "string"},
                    "deliverable": {"kind": "any"},
                    "pricing": {"kind": "any"},
                    "minimum_purchase_quantity": {
                        "kind": "primitive",
                        "type": "integer",
                    },
                    "maximum_purchase_quantity": {
                        "kind": "nullable",
                        "inner": {"kind": "primitive", "type": "integer"},
                    },
                    "bulk_discount": {"kind": "array", "items": {"kind": "any"}},
                    "payment_methods": {
                        "kind": "array",
                        "items": {
                            "kind": "enum",
                            "values": [
                                "AUTHNET",
                                "BTCPAY",
                                "CASHAPP",
                                "COINBASE",
                                "PADDLE",
                                "PAYDASH",
                                "PAYPAL",
                                "PAYSTACK",
                                "SQUARE",
                                "STRIPE",
                                "VENMO",
                                "NMI",
                                "MERCADO_PAGO",
                                "MOLLIE",
                                "RAZORPAY",
                                "CUSTOM_PAYMENT_METHOD",
                                "LIFI",
                                "BTC",
                                "LTC",
                                "ETH",
                                "XMR",
                                "SOL",
                                "ADA",
                                "BNB",
                                "TRX",
                                "MATIC",
                                "ETH_USDT",
                                "ETH_USDC",
                                "ETH_UNI",
                                "ETH_SHIB",
                                "ETH_DAI",
                                "BNB_USDT",
                                "BNB_USDC",
                                "TRX_USDT",
                                "TRX_USDC",
                                "SOL_USDT",
                                "SOL_USDC",
                            ],
                        },
                        "constraints": {"minItems": 1},
                    },
                    "other_settings": {"kind": "any"},
                    "expected_updated_at": {
                        "kind": "primitive",
                        "type": "string",
                        "format": "date-time",
                    },
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return await self._client.request(
            method="put",
            path=("v2", "products", str(product), "variants", str(variant)),
            body=body,
            model=SdkReplaceProductVariantWithPutResponseValue200ApplicationJson,
            request_options=request_options,
        )

    async def update(
        self,
        product: int,
        variant: int,
        *,
        title: str | None = None,
        description: str | None = None,
        deliverable: UpdateProductVariantRequestApplicationJsonPropertyDeliverable
        | None = None,
        pricing: UpdateProductVariantRequestApplicationJsonPropertyPricing
        | None = None,
        minimum_purchase_quantity: int | None = None,
        maximum_purchase_quantity: int | None | NotGiven = NOT_GIVEN,
        bulk_discount: builtins.list[
            UpdateProductVariantRequestApplicationJsonPropertyBulkDiscountItem
        ]
        | None = None,
        payment_methods: builtins.list[
            SdkUpdateProductVariantRequestApplicationJsonPaymentMethods | str
        ]
        | None = None,
        other_settings: UpdateProductVariantRequestApplicationJsonPropertyOtherSettings
        | None = None,
        expected_updated_at: str | None = None,
        request_options: RequestOptions | None = None,
    ) -> SdkUpdateProductVariantResponseValue200ApplicationJson:
        """Update a product variant

        Updates a product variant's details. Requires a token with the listing ability. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            product: The product path parameter.
            variant: The variant path parameter.
            title: The title.
            description: The description.
            deliverable: The deliverable.
            pricing: The pricing.
            minimum_purchase_quantity: The minimum purchase quantity.
            maximum_purchase_quantity: The maximum purchase quantity.
            bulk_discount: The bulk discount.
            payment_methods: The payment methods.
            other_settings: The other settings.
            expected_updated_at: Optional optimistic-concurrency snapshot for updates. Use the variant's latest updated_at value; stale snapshots receive a 422 validation response.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkUpdateProductVariantResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "updateProductVariant",
        }
        body: dict[str, Any] = {
            k: v
            for k, v in {
                "title": title,
                "description": description,
                "deliverable": deliverable.to_dict()
                if deliverable is not None
                else None,
                "pricing": pricing.to_dict() if pricing is not None else None,
                "minimum_purchase_quantity": minimum_purchase_quantity,
                "bulk_discount": [item.to_dict() for item in bulk_discount]
                if bulk_discount is not None
                else None,
                "payment_methods": payment_methods,
                "other_settings": other_settings.to_dict()
                if other_settings is not None
                else None,
                "expected_updated_at": expected_updated_at,
            }.items()
            if v is not None
        }
        if not isinstance(maximum_purchase_quantity, NotGiven):
            body["maximum_purchase_quantity"] = maximum_purchase_quantity
        _validate_model(
            body,
            {
                "name": "SdkUpdateProductVariantRequestApplicationJson",
                "required": [],
                "properties": {
                    "title": {"kind": "primitive", "type": "string"},
                    "description": {"kind": "primitive", "type": "string"},
                    "deliverable": {"kind": "any"},
                    "pricing": {"kind": "any"},
                    "minimum_purchase_quantity": {
                        "kind": "primitive",
                        "type": "integer",
                    },
                    "maximum_purchase_quantity": {
                        "kind": "nullable",
                        "inner": {"kind": "primitive", "type": "integer"},
                    },
                    "bulk_discount": {"kind": "array", "items": {"kind": "any"}},
                    "payment_methods": {
                        "kind": "array",
                        "items": {
                            "kind": "enum",
                            "values": [
                                "AUTHNET",
                                "BTCPAY",
                                "CASHAPP",
                                "COINBASE",
                                "PADDLE",
                                "PAYDASH",
                                "PAYPAL",
                                "PAYSTACK",
                                "SQUARE",
                                "STRIPE",
                                "VENMO",
                                "NMI",
                                "MERCADO_PAGO",
                                "MOLLIE",
                                "RAZORPAY",
                                "CUSTOM_PAYMENT_METHOD",
                                "LIFI",
                                "BTC",
                                "LTC",
                                "ETH",
                                "XMR",
                                "SOL",
                                "ADA",
                                "BNB",
                                "TRX",
                                "MATIC",
                                "ETH_USDT",
                                "ETH_USDC",
                                "ETH_UNI",
                                "ETH_SHIB",
                                "ETH_DAI",
                                "BNB_USDT",
                                "BNB_USDC",
                                "TRX_USDT",
                                "TRX_USDC",
                                "SOL_USDT",
                                "SOL_USDC",
                            ],
                        },
                        "constraints": {"minItems": 1},
                    },
                    "other_settings": {"kind": "any"},
                    "expected_updated_at": {
                        "kind": "primitive",
                        "type": "string",
                        "format": "date-time",
                    },
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return await self._client.request(
            method="patch",
            path=("v2", "products", str(product), "variants", str(variant)),
            body=body,
            model=SdkUpdateProductVariantResponseValue200ApplicationJson,
            request_options=request_options,
        )

    async def delete(
        self,
        product: int,
        variant: int,
        *,
        request_options: RequestOptions | None = None,
    ) -> None:
        """Delete a product variant

        Deletes a product variant. This will permanently delete the product variant and all of its details, including the sensitive deliverables, will no longer be retrievable. Requires a token with the listing ability. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            product: The product path parameter.
            variant: The variant path parameter.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "deleteProductVariant",
        }
        await self._client.request(
            method="delete",
            path=("v2", "products", str(product), "variants", str(variant)),
            request_options=request_options,
        )

    async def search(
        self,
        product: int,
        *,
        filters: builtins.list[
            SearchProductVariantsRequestApplicationJsonPropertyFiltersItem
        ]
        | None = None,
        sort: builtins.list[SearchProductVariantsRequestApplicationJsonPropertySortItem]
        | None = None,
        search: SearchProductVariantsRequestApplicationJsonPropertySearch | None = None,
        includes: builtins.list[
            SearchProductVariantsRequestApplicationJsonPropertyIncludesItem
        ]
        | None = None,
        limit: int | None = None,
        before: str | None = None,
        after: str | None = None,
        order: str | None = None,
        request_options: RequestOptions | None = None,
    ) -> AsyncPage[
        SearchProductVariantsResponseValue200ApplicationJsonPropertyDataItem
    ]:
        """Search product variants

        Search a product's variants using JSON body filters, search terms, includes, and sort instructions. Requires a token with the listing ability. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            product: The product path parameter.
            filters: The filters.
            sort: The sort.
            search: The search.
            includes: The includes.
            limit: The limit.
            before: The before.
            after: The after.
            order: The order.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            AsyncPage[SearchProductVariantsResponseValue200ApplicationJsonPropertyDataItem]

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "searchProductVariants",
        }
        body: dict[str, Any] = {
            k: v
            for k, v in {
                "filters": [item.to_dict() for item in filters]
                if filters is not None
                else None,
                "sort": [item.to_dict() for item in sort] if sort is not None else None,
                "search": search.to_dict() if search is not None else None,
                "includes": [item.to_dict() for item in includes]
                if includes is not None
                else None,
            }.items()
            if v is not None
        }
        params = {
            k: v
            for k, v in {
                "limit": limit,
                "before": before,
                "after": after,
                "order": order,
            }.items()
            if v is not None
        }
        return await self._client.request_page(
            method="post",
            path=("v2", "products", str(product), "variants", "search"),
            model=SearchProductVariantsResponseValue200ApplicationJsonPropertyDataItem,
            body=body,
            params=params,
            request_options=request_options,
        )

    async def batch_create(
        self,
        product: int,
        *,
        resources: builtins.list[
            BatchCreateProductVariantsRequestApplicationJsonPropertyResourcesItem
        ],
        request_options: RequestOptions | None = None,
    ) -> SdkBatchCreateProductVariantsResponseValue200ApplicationJson:
        """Batch create product variants

        Create multiple variants for a product in one request by sending a `resources` array. Requires a token with the listing ability. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            product: The product path parameter.
            resources: The resources.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkBatchCreateProductVariantsResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "batchCreateProductVariants",
        }
        body: dict[str, Any] = {
            "resources": [item.to_dict() for item in resources],
        }
        _validate_model(
            body,
            {
                "name": "SdkBatchCreateProductVariantsRequestApplicationJson",
                "required": ["resources"],
                "properties": {
                    "resources": {"kind": "array", "items": {"kind": "any"}}
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return await self._client.request(
            method="post",
            path=("v2", "products", str(product), "variants", "batch"),
            body=body,
            model=SdkBatchCreateProductVariantsResponseValue200ApplicationJson,
            request_options=request_options,
        )

    async def batch_update(
        self,
        product: int,
        *,
        resources: BatchUpdateProductVariantsRequestApplicationJsonPropertyResources,
        request_options: RequestOptions | None = None,
    ) -> SdkBatchUpdateProductVariantsResponseValue200ApplicationJson:
        """Batch update product variants

        Update multiple product variants in one request by sending a `resources` object keyed by variant ID. Requires a token with the listing ability. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            product: The product path parameter.
            resources: The resources.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkBatchUpdateProductVariantsResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "batchUpdateProductVariants",
        }
        body: dict[str, Any] = {
            "resources": resources.to_dict(),
        }
        _validate_model(
            body,
            {
                "name": "SdkBatchUpdateProductVariantsRequestApplicationJson",
                "required": ["resources"],
                "properties": {"resources": {"kind": "any"}},
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return await self._client.request(
            method="patch",
            path=("v2", "products", str(product), "variants", "batch"),
            body=body,
            model=SdkBatchUpdateProductVariantsResponseValue200ApplicationJson,
            request_options=request_options,
        )

    async def batch_delete(
        self,
        product: int,
        *,
        resources: builtins.list[int],
        request_options: RequestOptions | None = None,
    ) -> None:
        """Batch delete product variants

        Delete multiple product variants in one request by sending the variant IDs in `resources`. Requires a token with the listing ability. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            product: The product path parameter.
            resources: The resources.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "batchDeleteProductVariants",
        }
        body: dict[str, Any] = {
            "resources": resources,
        }
        _validate_model(
            body,
            {
                "name": "SdkBatchDeleteCouponsRequestApplicationJson",
                "required": ["resources"],
                "properties": {
                    "resources": {
                        "kind": "array",
                        "items": {"kind": "primitive", "type": "integer"},
                    }
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        await self._client.request(
            method="delete",
            path=("v2", "products", str(product), "variants", "batch"),
            body=body,
            request_options=request_options,
        )
