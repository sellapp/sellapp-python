# This file is auto-generated by oagen. Do not edit.

from __future__ import annotations

from typing import TYPE_CHECKING, Any, Dict, List, Literal, Optional, Union

from .._base_client import WithRawResponse

if TYPE_CHECKING:
    from .._client import AsyncSellAppClient, SellAppClient

from sellapp_sdk.bookings.models.sdk_get_booking_configuration_response_value_200_application_json import (
    SdkGetBookingConfigurationResponseValue200ApplicationJson,
)
from sellapp_sdk.bookings.models.sdk_replace_booking_configuration_response_value_200_application_json import (
    SdkReplaceBookingConfigurationResponseValue200ApplicationJson,
)
from sellapp_sdk.bookings.models.sdk_update_booking_configuration_response_value_200_application_json import (
    SdkUpdateBookingConfigurationResponseValue200ApplicationJson,
)
from sellapp_sdk.bookings.models.update_booking_configuration_request_application_json_property_availability_item import (
    UpdateBookingConfigurationRequestApplicationJsonPropertyAvailabilityItem,
)
from sellapp_sdk.bookings.models.update_booking_configuration_request_application_json_property_meta import (
    UpdateBookingConfigurationRequestApplicationJsonPropertyMeta,
)
from sellapp_sdk.common.models.replace_booking_configuration_request_application_json_property_availability_item import (
    ReplaceBookingConfigurationRequestApplicationJsonPropertyAvailabilityItem,
)
from sellapp_sdk.common.models.replace_booking_configuration_request_application_json_property_meta import (
    ReplaceBookingConfigurationRequestApplicationJsonPropertyMeta,
)
from sellapp_sdk.common.models.sdk_replace_booking_configuration_request_application_json_conflict_scope import (
    SdkReplaceBookingConfigurationRequestApplicationJsonConflictScope,
)
from sellapp_sdk.common.models.sdk_replace_booking_configuration_request_application_json_reminder_offset_unit import (
    SdkReplaceBookingConfigurationRequestApplicationJsonReminderOffsetUnit,
)
from sellapp_sdk.common.models.sdk_replace_booking_configuration_request_application_json_video_provider import (
    SdkReplaceBookingConfigurationRequestApplicationJsonVideoProvider,
)
from sellapp_sdk.common.models.sdk_update_booking_configuration_request_application_json_conflict_scope import (
    SdkUpdateBookingConfigurationRequestApplicationJsonConflictScope,
)
from sellapp_sdk.common.models.sdk_update_booking_configuration_request_application_json_reminder_offset_unit import (
    SdkUpdateBookingConfigurationRequestApplicationJsonReminderOffsetUnit,
)
from sellapp_sdk.common.models.sdk_update_booking_configuration_request_application_json_video_provider import (
    SdkUpdateBookingConfigurationRequestApplicationJsonVideoProvider,
)
from sellapp_sdk.product_variants.models.create_booking_hold_request_application_json_property_meta import (
    CreateBookingHoldRequestApplicationJsonPropertyMeta,
)
from sellapp_sdk.product_variants.models.sdk_create_booking_hold_response_value_201_application_json import (
    SdkCreateBookingHoldResponseValue201ApplicationJson,
)
from sellapp_sdk.product_variants.models.sdk_list_booking_availability_response_value_200_application_json import (
    SdkListBookingAvailabilityResponseValue200ApplicationJson,
)

from .._types import NOT_GIVEN, NotGiven, RequestOptions, _validate_model, enum_value


class ProductVariantsBooking:
    """Product Variants Booking API resources."""

    def __init__(self, client: SellAppClient) -> None:
        self._client = client
        self.with_raw_response = WithRawResponse(self)

    def list_availability(
        self,
        product: int,
        variant: int,
        *,
        from_: Optional[str] = None,
        to: Optional[str] = None,
        quantity: Optional[int] = None,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkListBookingAvailabilityResponseValue200ApplicationJson:
        """List booking availability

        Retrieve available appointment slots for a booking product variant. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            product: The product path parameter.
            variant: The variant path parameter.
            from_: Start date for the availability window.
            to: End date for the availability window.
            quantity: Number of seats to reserve for each returned slot.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkListBookingAvailabilityResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "listBookingAvailability",
        }
        params: Dict[str, Any] = {
            k: v
            for k, v in {
                "from": from_,
                "to": to,
                "quantity": quantity,
            }.items()
            if v is not None
        }
        return self._client.request(
            method="get",
            path=(
                "v2",
                "products",
                str(product),
                "variants",
                str(variant),
                "booking",
                "availability",
            ),
            params=params,
            model=SdkListBookingAvailabilityResponseValue200ApplicationJson,
            request_options=request_options,
        )

    def create_hold(
        self,
        product: int,
        variant: int,
        *,
        slot_start_at: str,
        quantity: Optional[int] = None,
        customer_key: Union[str, None, NotGiven] = NOT_GIVEN,
        meta: Optional[CreateBookingHoldRequestApplicationJsonPropertyMeta] = None,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkCreateBookingHoldResponseValue201ApplicationJson:
        """Create a booking hold

        Temporarily reserve a booking slot before checkout. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            product: The product path parameter.
            variant: The variant path parameter.
            slot_start_at: The slot start at.
            quantity: The quantity.
            customer_key: The customer key.
            meta: The meta.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkCreateBookingHoldResponseValue201ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "createBookingHold",
        }
        body: Dict[str, Any] = {
            k: v
            for k, v in {
                "slot_start_at": slot_start_at,
                "quantity": quantity,
                "meta": meta.to_dict() if meta is not None else None,
            }.items()
            if v is not None
        }
        if not isinstance(customer_key, NotGiven):
            body["customer_key"] = customer_key
        _validate_model(
            body,
            {
                "name": "SdkCreateBookingHoldRequestApplicationJson",
                "required": ["slot_start_at"],
                "properties": {
                    "slot_start_at": {
                        "kind": "primitive",
                        "type": "string",
                        "format": "date-time",
                    },
                    "quantity": {
                        "kind": "primitive",
                        "type": "integer",
                        "constraints": {"minimum": 1},
                    },
                    "customer_key": {
                        "kind": "nullable",
                        "inner": {"kind": "primitive", "type": "string"},
                    },
                    "meta": {"kind": "any"},
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return self._client.request(
            method="post",
            path=(
                "v2",
                "products",
                str(product),
                "variants",
                str(variant),
                "booking",
                "holds",
            ),
            body=body,
            model=SdkCreateBookingHoldResponseValue201ApplicationJson,
            request_options=request_options,
        )

    def release_hold(
        self,
        product: int,
        hold: str,
        *,
        customer_key: Union[str, None, NotGiven] = NOT_GIVEN,
        request_options: Optional[RequestOptions] = None,
    ) -> None:
        """Release a booking hold

        Release a previously created booking hold. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            product: The product path parameter.
            hold: The hold path parameter.
            customer_key: The customer key.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "releaseBookingHold",
        }
        body: Dict[str, Any] = {}
        if not isinstance(customer_key, NotGiven):
            body["customer_key"] = customer_key
        _validate_model(
            body,
            {
                "name": "SdkReleaseBookingHoldRequestApplicationJson",
                "required": [],
                "properties": {
                    "customer_key": {
                        "kind": "nullable",
                        "inner": {"kind": "primitive", "type": "string"},
                    }
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        self._client.request(
            method="delete",
            path=(
                "v2",
                "products",
                str(product),
                "variants",
                "booking",
                "holds",
                str(hold),
            ),
            body=body,
            request_options=request_options,
        )

    def get(
        self,
        product: str,
        variant: int,
        *,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkGetBookingConfigurationResponseValue200ApplicationJson:
        """Retrieve booking configuration

        Retrieve creator-facing availability, capacity, calendar, meeting, and reminder configuration for a booking variant. Requires the `listing` Sanctum ability. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            product: The booking product ID or slug.
            variant: The variant path parameter.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkGetBookingConfigurationResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "getBookingConfiguration",
        }
        return self._client.request(
            method="get",
            path=("v2", "products", str(product), "variants", str(variant), "booking"),
            model=SdkGetBookingConfigurationResponseValue200ApplicationJson,
            request_options=request_options,
        )

    def replace(
        self,
        product: str,
        variant: int,
        *,
        mode: Optional[Literal["native"]] = None,
        conflict_scope: Optional[
            Union[
                SdkReplaceBookingConfigurationRequestApplicationJsonConflictScope, str
            ]
        ] = None,
        timezone: Optional[str] = None,
        duration_minutes: Optional[int] = None,
        capacity_per_slot: Optional[int] = None,
        min_notice_minutes: Optional[int] = None,
        max_advance_days: Optional[int] = None,
        buffer_before_minutes: Optional[int] = None,
        buffer_after_minutes: Optional[int] = None,
        availability: Optional[
            List[
                ReplaceBookingConfigurationRequestApplicationJsonPropertyAvailabilityItem
            ]
        ] = None,
        provider_connection_ids: Optional[List[int]] = None,
        video_provider: Optional[
            Union[
                SdkReplaceBookingConfigurationRequestApplicationJsonVideoProvider, str
            ]
        ] = None,
        video_provider_connection_id: Union[int, None, NotGiven] = NOT_GIVEN,
        reminders_enabled: Optional[bool] = None,
        reminder_offset_value: Optional[int] = None,
        reminder_offset_unit: Optional[
            Union[
                SdkReplaceBookingConfigurationRequestApplicationJsonReminderOffsetUnit,
                str,
            ]
        ] = None,
        meta: Optional[
            ReplaceBookingConfigurationRequestApplicationJsonPropertyMeta
        ] = None,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkReplaceBookingConfigurationResponseValue200ApplicationJson:
        """Update booking configuration

        Partially update a booking variant's configuration. Provider connection IDs must belong to the selected store; secret credentials are never accepted or returned here. Requires the `listing` Sanctum ability. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            product: The booking product ID or slug.
            variant: The variant path parameter.
            mode: The mode.
            conflict_scope: The conflict scope.
            timezone: The timezone.
            duration_minutes: The duration minutes.
            capacity_per_slot: The capacity per slot.
            min_notice_minutes: The min notice minutes.
            max_advance_days: The max advance days.
            buffer_before_minutes: The buffer before minutes.
            buffer_after_minutes: The buffer after minutes.
            availability: The availability.
            provider_connection_ids: The provider connection ids.
            video_provider: The video provider.
            video_provider_connection_id: The video provider connection id.
            reminders_enabled: The reminders enabled.
            reminder_offset_value: The reminder offset value.
            reminder_offset_unit: The reminder offset unit.
            meta: The meta.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkReplaceBookingConfigurationResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "replaceBookingConfiguration",
        }
        body: Dict[str, Any] = {
            k: v
            for k, v in {
                "mode": mode,
                "conflict_scope": enum_value(conflict_scope)
                if conflict_scope is not None
                else None,
                "timezone": timezone,
                "duration_minutes": duration_minutes,
                "capacity_per_slot": capacity_per_slot,
                "min_notice_minutes": min_notice_minutes,
                "max_advance_days": max_advance_days,
                "buffer_before_minutes": buffer_before_minutes,
                "buffer_after_minutes": buffer_after_minutes,
                "availability": [item.to_dict() for item in availability]
                if availability is not None
                else None,
                "provider_connection_ids": provider_connection_ids,
                "video_provider": enum_value(video_provider)
                if video_provider is not None
                else None,
                "reminders_enabled": reminders_enabled,
                "reminder_offset_value": reminder_offset_value,
                "reminder_offset_unit": enum_value(reminder_offset_unit)
                if reminder_offset_unit is not None
                else None,
                "meta": meta.to_dict() if meta is not None else None,
            }.items()
            if v is not None
        }
        if not isinstance(video_provider_connection_id, NotGiven):
            body["video_provider_connection_id"] = video_provider_connection_id
        _validate_model(
            body,
            {
                "name": "SdkReplaceBookingConfigurationRequestApplicationJson",
                "required": [],
                "properties": {
                    "mode": {"kind": "literal", "value": "native"},
                    "conflict_scope": {
                        "kind": "enum",
                        "values": ["product", "variant"],
                    },
                    "timezone": {"kind": "primitive", "type": "string"},
                    "duration_minutes": {
                        "kind": "primitive",
                        "type": "integer",
                        "constraints": {"minimum": 30, "multipleOf": 30},
                    },
                    "capacity_per_slot": {
                        "kind": "primitive",
                        "type": "integer",
                        "constraints": {"minimum": 1, "maximum": 1000},
                    },
                    "min_notice_minutes": {
                        "kind": "primitive",
                        "type": "integer",
                        "constraints": {"minimum": 0, "maximum": 525600},
                    },
                    "max_advance_days": {
                        "kind": "primitive",
                        "type": "integer",
                        "constraints": {"minimum": 1, "maximum": 730},
                    },
                    "buffer_before_minutes": {
                        "kind": "primitive",
                        "type": "integer",
                        "constraints": {
                            "minimum": 0,
                            "maximum": 1440,
                            "multipleOf": 30,
                        },
                    },
                    "buffer_after_minutes": {
                        "kind": "primitive",
                        "type": "integer",
                        "constraints": {
                            "minimum": 0,
                            "maximum": 1440,
                            "multipleOf": 30,
                        },
                    },
                    "availability": {
                        "kind": "array",
                        "items": {"kind": "any"},
                        "constraints": {"minItems": 7, "maxItems": 7},
                    },
                    "provider_connection_ids": {
                        "kind": "array",
                        "items": {"kind": "primitive", "type": "integer"},
                    },
                    "video_provider": {
                        "kind": "enum",
                        "values": ["none", "google_meet", "zoom"],
                    },
                    "video_provider_connection_id": {
                        "kind": "nullable",
                        "inner": {"kind": "primitive", "type": "integer"},
                    },
                    "reminders_enabled": {"kind": "primitive", "type": "boolean"},
                    "reminder_offset_value": {
                        "kind": "primitive",
                        "type": "integer",
                        "constraints": {"minimum": 1, "maximum": 10080},
                    },
                    "reminder_offset_unit": {
                        "kind": "enum",
                        "values": ["minutes", "hours", "days", "weeks"],
                    },
                    "meta": {"kind": "any"},
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return self._client.request(
            method="put",
            path=("v2", "products", str(product), "variants", str(variant), "booking"),
            body=body,
            model=SdkReplaceBookingConfigurationResponseValue200ApplicationJson,
            request_options=request_options,
        )

    def update(
        self,
        product: str,
        variant: int,
        *,
        mode: Optional[Literal["native"]] = None,
        conflict_scope: Optional[
            Union[SdkUpdateBookingConfigurationRequestApplicationJsonConflictScope, str]
        ] = None,
        timezone: Optional[str] = None,
        duration_minutes: Optional[int] = None,
        capacity_per_slot: Optional[int] = None,
        min_notice_minutes: Optional[int] = None,
        max_advance_days: Optional[int] = None,
        buffer_before_minutes: Optional[int] = None,
        buffer_after_minutes: Optional[int] = None,
        availability: Optional[
            List[
                UpdateBookingConfigurationRequestApplicationJsonPropertyAvailabilityItem
            ]
        ] = None,
        provider_connection_ids: Optional[List[int]] = None,
        video_provider: Optional[
            Union[SdkUpdateBookingConfigurationRequestApplicationJsonVideoProvider, str]
        ] = None,
        video_provider_connection_id: Union[int, None, NotGiven] = NOT_GIVEN,
        reminders_enabled: Optional[bool] = None,
        reminder_offset_value: Optional[int] = None,
        reminder_offset_unit: Optional[
            Union[
                SdkUpdateBookingConfigurationRequestApplicationJsonReminderOffsetUnit,
                str,
            ]
        ] = None,
        meta: Optional[
            UpdateBookingConfigurationRequestApplicationJsonPropertyMeta
        ] = None,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkUpdateBookingConfigurationResponseValue200ApplicationJson:
        """Update booking configuration

        Partially update a booking variant's configuration. Provider connection IDs must belong to the selected store; secret credentials are never accepted or returned here. Requires the `listing` Sanctum ability. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            product: The booking product ID or slug.
            variant: The variant path parameter.
            mode: The mode.
            conflict_scope: The conflict scope.
            timezone: The timezone.
            duration_minutes: The duration minutes.
            capacity_per_slot: The capacity per slot.
            min_notice_minutes: The min notice minutes.
            max_advance_days: The max advance days.
            buffer_before_minutes: The buffer before minutes.
            buffer_after_minutes: The buffer after minutes.
            availability: The availability.
            provider_connection_ids: The provider connection ids.
            video_provider: The video provider.
            video_provider_connection_id: The video provider connection id.
            reminders_enabled: The reminders enabled.
            reminder_offset_value: The reminder offset value.
            reminder_offset_unit: The reminder offset unit.
            meta: The meta.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkUpdateBookingConfigurationResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "updateBookingConfiguration",
        }
        body: Dict[str, Any] = {
            k: v
            for k, v in {
                "mode": mode,
                "conflict_scope": enum_value(conflict_scope)
                if conflict_scope is not None
                else None,
                "timezone": timezone,
                "duration_minutes": duration_minutes,
                "capacity_per_slot": capacity_per_slot,
                "min_notice_minutes": min_notice_minutes,
                "max_advance_days": max_advance_days,
                "buffer_before_minutes": buffer_before_minutes,
                "buffer_after_minutes": buffer_after_minutes,
                "availability": [item.to_dict() for item in availability]
                if availability is not None
                else None,
                "provider_connection_ids": provider_connection_ids,
                "video_provider": enum_value(video_provider)
                if video_provider is not None
                else None,
                "reminders_enabled": reminders_enabled,
                "reminder_offset_value": reminder_offset_value,
                "reminder_offset_unit": enum_value(reminder_offset_unit)
                if reminder_offset_unit is not None
                else None,
                "meta": meta.to_dict() if meta is not None else None,
            }.items()
            if v is not None
        }
        if not isinstance(video_provider_connection_id, NotGiven):
            body["video_provider_connection_id"] = video_provider_connection_id
        _validate_model(
            body,
            {
                "name": "SdkUpdateBookingConfigurationRequestApplicationJson",
                "required": [],
                "properties": {
                    "mode": {"kind": "literal", "value": "native"},
                    "conflict_scope": {
                        "kind": "enum",
                        "values": ["product", "variant"],
                    },
                    "timezone": {"kind": "primitive", "type": "string"},
                    "duration_minutes": {
                        "kind": "primitive",
                        "type": "integer",
                        "constraints": {"minimum": 30, "multipleOf": 30},
                    },
                    "capacity_per_slot": {
                        "kind": "primitive",
                        "type": "integer",
                        "constraints": {"minimum": 1, "maximum": 1000},
                    },
                    "min_notice_minutes": {
                        "kind": "primitive",
                        "type": "integer",
                        "constraints": {"minimum": 0, "maximum": 525600},
                    },
                    "max_advance_days": {
                        "kind": "primitive",
                        "type": "integer",
                        "constraints": {"minimum": 1, "maximum": 730},
                    },
                    "buffer_before_minutes": {
                        "kind": "primitive",
                        "type": "integer",
                        "constraints": {
                            "minimum": 0,
                            "maximum": 1440,
                            "multipleOf": 30,
                        },
                    },
                    "buffer_after_minutes": {
                        "kind": "primitive",
                        "type": "integer",
                        "constraints": {
                            "minimum": 0,
                            "maximum": 1440,
                            "multipleOf": 30,
                        },
                    },
                    "availability": {
                        "kind": "array",
                        "items": {"kind": "any"},
                        "constraints": {"minItems": 7, "maxItems": 7},
                    },
                    "provider_connection_ids": {
                        "kind": "array",
                        "items": {"kind": "primitive", "type": "integer"},
                    },
                    "video_provider": {
                        "kind": "enum",
                        "values": ["none", "google_meet", "zoom"],
                    },
                    "video_provider_connection_id": {
                        "kind": "nullable",
                        "inner": {"kind": "primitive", "type": "integer"},
                    },
                    "reminders_enabled": {"kind": "primitive", "type": "boolean"},
                    "reminder_offset_value": {
                        "kind": "primitive",
                        "type": "integer",
                        "constraints": {"minimum": 1, "maximum": 10080},
                    },
                    "reminder_offset_unit": {
                        "kind": "enum",
                        "values": ["minutes", "hours", "days", "weeks"],
                    },
                    "meta": {"kind": "any"},
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return self._client.request(
            method="patch",
            path=("v2", "products", str(product), "variants", str(variant), "booking"),
            body=body,
            model=SdkUpdateBookingConfigurationResponseValue200ApplicationJson,
            request_options=request_options,
        )


class AsyncProductVariantsBooking:
    """Product Variants Booking API resources (async)."""

    def __init__(self, client: AsyncSellAppClient) -> None:
        self._client = client
        self.with_raw_response = WithRawResponse(self)

    async def list_availability(
        self,
        product: int,
        variant: int,
        *,
        from_: Optional[str] = None,
        to: Optional[str] = None,
        quantity: Optional[int] = None,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkListBookingAvailabilityResponseValue200ApplicationJson:
        """List booking availability

        Retrieve available appointment slots for a booking product variant. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            product: The product path parameter.
            variant: The variant path parameter.
            from_: Start date for the availability window.
            to: End date for the availability window.
            quantity: Number of seats to reserve for each returned slot.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkListBookingAvailabilityResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "listBookingAvailability",
        }
        params: Dict[str, Any] = {
            k: v
            for k, v in {
                "from": from_,
                "to": to,
                "quantity": quantity,
            }.items()
            if v is not None
        }
        return await self._client.request(
            method="get",
            path=(
                "v2",
                "products",
                str(product),
                "variants",
                str(variant),
                "booking",
                "availability",
            ),
            params=params,
            model=SdkListBookingAvailabilityResponseValue200ApplicationJson,
            request_options=request_options,
        )

    async def create_hold(
        self,
        product: int,
        variant: int,
        *,
        slot_start_at: str,
        quantity: Optional[int] = None,
        customer_key: Union[str, None, NotGiven] = NOT_GIVEN,
        meta: Optional[CreateBookingHoldRequestApplicationJsonPropertyMeta] = None,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkCreateBookingHoldResponseValue201ApplicationJson:
        """Create a booking hold

        Temporarily reserve a booking slot before checkout. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            product: The product path parameter.
            variant: The variant path parameter.
            slot_start_at: The slot start at.
            quantity: The quantity.
            customer_key: The customer key.
            meta: The meta.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkCreateBookingHoldResponseValue201ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "createBookingHold",
        }
        body: Dict[str, Any] = {
            k: v
            for k, v in {
                "slot_start_at": slot_start_at,
                "quantity": quantity,
                "meta": meta.to_dict() if meta is not None else None,
            }.items()
            if v is not None
        }
        if not isinstance(customer_key, NotGiven):
            body["customer_key"] = customer_key
        _validate_model(
            body,
            {
                "name": "SdkCreateBookingHoldRequestApplicationJson",
                "required": ["slot_start_at"],
                "properties": {
                    "slot_start_at": {
                        "kind": "primitive",
                        "type": "string",
                        "format": "date-time",
                    },
                    "quantity": {
                        "kind": "primitive",
                        "type": "integer",
                        "constraints": {"minimum": 1},
                    },
                    "customer_key": {
                        "kind": "nullable",
                        "inner": {"kind": "primitive", "type": "string"},
                    },
                    "meta": {"kind": "any"},
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return await self._client.request(
            method="post",
            path=(
                "v2",
                "products",
                str(product),
                "variants",
                str(variant),
                "booking",
                "holds",
            ),
            body=body,
            model=SdkCreateBookingHoldResponseValue201ApplicationJson,
            request_options=request_options,
        )

    async def release_hold(
        self,
        product: int,
        hold: str,
        *,
        customer_key: Union[str, None, NotGiven] = NOT_GIVEN,
        request_options: Optional[RequestOptions] = None,
    ) -> None:
        """Release a booking hold

        Release a previously created booking hold. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            product: The product path parameter.
            hold: The hold path parameter.
            customer_key: The customer key.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "releaseBookingHold",
        }
        body: Dict[str, Any] = {}
        if not isinstance(customer_key, NotGiven):
            body["customer_key"] = customer_key
        _validate_model(
            body,
            {
                "name": "SdkReleaseBookingHoldRequestApplicationJson",
                "required": [],
                "properties": {
                    "customer_key": {
                        "kind": "nullable",
                        "inner": {"kind": "primitive", "type": "string"},
                    }
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        await self._client.request(
            method="delete",
            path=(
                "v2",
                "products",
                str(product),
                "variants",
                "booking",
                "holds",
                str(hold),
            ),
            body=body,
            request_options=request_options,
        )

    async def get(
        self,
        product: str,
        variant: int,
        *,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkGetBookingConfigurationResponseValue200ApplicationJson:
        """Retrieve booking configuration

        Retrieve creator-facing availability, capacity, calendar, meeting, and reminder configuration for a booking variant. Requires the `listing` Sanctum ability. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            product: The booking product ID or slug.
            variant: The variant path parameter.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkGetBookingConfigurationResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "getBookingConfiguration",
        }
        return await self._client.request(
            method="get",
            path=("v2", "products", str(product), "variants", str(variant), "booking"),
            model=SdkGetBookingConfigurationResponseValue200ApplicationJson,
            request_options=request_options,
        )

    async def replace(
        self,
        product: str,
        variant: int,
        *,
        mode: Optional[Literal["native"]] = None,
        conflict_scope: Optional[
            Union[
                SdkReplaceBookingConfigurationRequestApplicationJsonConflictScope, str
            ]
        ] = None,
        timezone: Optional[str] = None,
        duration_minutes: Optional[int] = None,
        capacity_per_slot: Optional[int] = None,
        min_notice_minutes: Optional[int] = None,
        max_advance_days: Optional[int] = None,
        buffer_before_minutes: Optional[int] = None,
        buffer_after_minutes: Optional[int] = None,
        availability: Optional[
            List[
                ReplaceBookingConfigurationRequestApplicationJsonPropertyAvailabilityItem
            ]
        ] = None,
        provider_connection_ids: Optional[List[int]] = None,
        video_provider: Optional[
            Union[
                SdkReplaceBookingConfigurationRequestApplicationJsonVideoProvider, str
            ]
        ] = None,
        video_provider_connection_id: Union[int, None, NotGiven] = NOT_GIVEN,
        reminders_enabled: Optional[bool] = None,
        reminder_offset_value: Optional[int] = None,
        reminder_offset_unit: Optional[
            Union[
                SdkReplaceBookingConfigurationRequestApplicationJsonReminderOffsetUnit,
                str,
            ]
        ] = None,
        meta: Optional[
            ReplaceBookingConfigurationRequestApplicationJsonPropertyMeta
        ] = None,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkReplaceBookingConfigurationResponseValue200ApplicationJson:
        """Update booking configuration

        Partially update a booking variant's configuration. Provider connection IDs must belong to the selected store; secret credentials are never accepted or returned here. Requires the `listing` Sanctum ability. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            product: The booking product ID or slug.
            variant: The variant path parameter.
            mode: The mode.
            conflict_scope: The conflict scope.
            timezone: The timezone.
            duration_minutes: The duration minutes.
            capacity_per_slot: The capacity per slot.
            min_notice_minutes: The min notice minutes.
            max_advance_days: The max advance days.
            buffer_before_minutes: The buffer before minutes.
            buffer_after_minutes: The buffer after minutes.
            availability: The availability.
            provider_connection_ids: The provider connection ids.
            video_provider: The video provider.
            video_provider_connection_id: The video provider connection id.
            reminders_enabled: The reminders enabled.
            reminder_offset_value: The reminder offset value.
            reminder_offset_unit: The reminder offset unit.
            meta: The meta.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkReplaceBookingConfigurationResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "replaceBookingConfiguration",
        }
        body: Dict[str, Any] = {
            k: v
            for k, v in {
                "mode": mode,
                "conflict_scope": enum_value(conflict_scope)
                if conflict_scope is not None
                else None,
                "timezone": timezone,
                "duration_minutes": duration_minutes,
                "capacity_per_slot": capacity_per_slot,
                "min_notice_minutes": min_notice_minutes,
                "max_advance_days": max_advance_days,
                "buffer_before_minutes": buffer_before_minutes,
                "buffer_after_minutes": buffer_after_minutes,
                "availability": [item.to_dict() for item in availability]
                if availability is not None
                else None,
                "provider_connection_ids": provider_connection_ids,
                "video_provider": enum_value(video_provider)
                if video_provider is not None
                else None,
                "reminders_enabled": reminders_enabled,
                "reminder_offset_value": reminder_offset_value,
                "reminder_offset_unit": enum_value(reminder_offset_unit)
                if reminder_offset_unit is not None
                else None,
                "meta": meta.to_dict() if meta is not None else None,
            }.items()
            if v is not None
        }
        if not isinstance(video_provider_connection_id, NotGiven):
            body["video_provider_connection_id"] = video_provider_connection_id
        _validate_model(
            body,
            {
                "name": "SdkReplaceBookingConfigurationRequestApplicationJson",
                "required": [],
                "properties": {
                    "mode": {"kind": "literal", "value": "native"},
                    "conflict_scope": {
                        "kind": "enum",
                        "values": ["product", "variant"],
                    },
                    "timezone": {"kind": "primitive", "type": "string"},
                    "duration_minutes": {
                        "kind": "primitive",
                        "type": "integer",
                        "constraints": {"minimum": 30, "multipleOf": 30},
                    },
                    "capacity_per_slot": {
                        "kind": "primitive",
                        "type": "integer",
                        "constraints": {"minimum": 1, "maximum": 1000},
                    },
                    "min_notice_minutes": {
                        "kind": "primitive",
                        "type": "integer",
                        "constraints": {"minimum": 0, "maximum": 525600},
                    },
                    "max_advance_days": {
                        "kind": "primitive",
                        "type": "integer",
                        "constraints": {"minimum": 1, "maximum": 730},
                    },
                    "buffer_before_minutes": {
                        "kind": "primitive",
                        "type": "integer",
                        "constraints": {
                            "minimum": 0,
                            "maximum": 1440,
                            "multipleOf": 30,
                        },
                    },
                    "buffer_after_minutes": {
                        "kind": "primitive",
                        "type": "integer",
                        "constraints": {
                            "minimum": 0,
                            "maximum": 1440,
                            "multipleOf": 30,
                        },
                    },
                    "availability": {
                        "kind": "array",
                        "items": {"kind": "any"},
                        "constraints": {"minItems": 7, "maxItems": 7},
                    },
                    "provider_connection_ids": {
                        "kind": "array",
                        "items": {"kind": "primitive", "type": "integer"},
                    },
                    "video_provider": {
                        "kind": "enum",
                        "values": ["none", "google_meet", "zoom"],
                    },
                    "video_provider_connection_id": {
                        "kind": "nullable",
                        "inner": {"kind": "primitive", "type": "integer"},
                    },
                    "reminders_enabled": {"kind": "primitive", "type": "boolean"},
                    "reminder_offset_value": {
                        "kind": "primitive",
                        "type": "integer",
                        "constraints": {"minimum": 1, "maximum": 10080},
                    },
                    "reminder_offset_unit": {
                        "kind": "enum",
                        "values": ["minutes", "hours", "days", "weeks"],
                    },
                    "meta": {"kind": "any"},
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return await self._client.request(
            method="put",
            path=("v2", "products", str(product), "variants", str(variant), "booking"),
            body=body,
            model=SdkReplaceBookingConfigurationResponseValue200ApplicationJson,
            request_options=request_options,
        )

    async def update(
        self,
        product: str,
        variant: int,
        *,
        mode: Optional[Literal["native"]] = None,
        conflict_scope: Optional[
            Union[SdkUpdateBookingConfigurationRequestApplicationJsonConflictScope, str]
        ] = None,
        timezone: Optional[str] = None,
        duration_minutes: Optional[int] = None,
        capacity_per_slot: Optional[int] = None,
        min_notice_minutes: Optional[int] = None,
        max_advance_days: Optional[int] = None,
        buffer_before_minutes: Optional[int] = None,
        buffer_after_minutes: Optional[int] = None,
        availability: Optional[
            List[
                UpdateBookingConfigurationRequestApplicationJsonPropertyAvailabilityItem
            ]
        ] = None,
        provider_connection_ids: Optional[List[int]] = None,
        video_provider: Optional[
            Union[SdkUpdateBookingConfigurationRequestApplicationJsonVideoProvider, str]
        ] = None,
        video_provider_connection_id: Union[int, None, NotGiven] = NOT_GIVEN,
        reminders_enabled: Optional[bool] = None,
        reminder_offset_value: Optional[int] = None,
        reminder_offset_unit: Optional[
            Union[
                SdkUpdateBookingConfigurationRequestApplicationJsonReminderOffsetUnit,
                str,
            ]
        ] = None,
        meta: Optional[
            UpdateBookingConfigurationRequestApplicationJsonPropertyMeta
        ] = None,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkUpdateBookingConfigurationResponseValue200ApplicationJson:
        """Update booking configuration

        Partially update a booking variant's configuration. Provider connection IDs must belong to the selected store; secret credentials are never accepted or returned here. Requires the `listing` Sanctum ability. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            product: The booking product ID or slug.
            variant: The variant path parameter.
            mode: The mode.
            conflict_scope: The conflict scope.
            timezone: The timezone.
            duration_minutes: The duration minutes.
            capacity_per_slot: The capacity per slot.
            min_notice_minutes: The min notice minutes.
            max_advance_days: The max advance days.
            buffer_before_minutes: The buffer before minutes.
            buffer_after_minutes: The buffer after minutes.
            availability: The availability.
            provider_connection_ids: The provider connection ids.
            video_provider: The video provider.
            video_provider_connection_id: The video provider connection id.
            reminders_enabled: The reminders enabled.
            reminder_offset_value: The reminder offset value.
            reminder_offset_unit: The reminder offset unit.
            meta: The meta.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkUpdateBookingConfigurationResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "updateBookingConfiguration",
        }
        body: Dict[str, Any] = {
            k: v
            for k, v in {
                "mode": mode,
                "conflict_scope": enum_value(conflict_scope)
                if conflict_scope is not None
                else None,
                "timezone": timezone,
                "duration_minutes": duration_minutes,
                "capacity_per_slot": capacity_per_slot,
                "min_notice_minutes": min_notice_minutes,
                "max_advance_days": max_advance_days,
                "buffer_before_minutes": buffer_before_minutes,
                "buffer_after_minutes": buffer_after_minutes,
                "availability": [item.to_dict() for item in availability]
                if availability is not None
                else None,
                "provider_connection_ids": provider_connection_ids,
                "video_provider": enum_value(video_provider)
                if video_provider is not None
                else None,
                "reminders_enabled": reminders_enabled,
                "reminder_offset_value": reminder_offset_value,
                "reminder_offset_unit": enum_value(reminder_offset_unit)
                if reminder_offset_unit is not None
                else None,
                "meta": meta.to_dict() if meta is not None else None,
            }.items()
            if v is not None
        }
        if not isinstance(video_provider_connection_id, NotGiven):
            body["video_provider_connection_id"] = video_provider_connection_id
        _validate_model(
            body,
            {
                "name": "SdkUpdateBookingConfigurationRequestApplicationJson",
                "required": [],
                "properties": {
                    "mode": {"kind": "literal", "value": "native"},
                    "conflict_scope": {
                        "kind": "enum",
                        "values": ["product", "variant"],
                    },
                    "timezone": {"kind": "primitive", "type": "string"},
                    "duration_minutes": {
                        "kind": "primitive",
                        "type": "integer",
                        "constraints": {"minimum": 30, "multipleOf": 30},
                    },
                    "capacity_per_slot": {
                        "kind": "primitive",
                        "type": "integer",
                        "constraints": {"minimum": 1, "maximum": 1000},
                    },
                    "min_notice_minutes": {
                        "kind": "primitive",
                        "type": "integer",
                        "constraints": {"minimum": 0, "maximum": 525600},
                    },
                    "max_advance_days": {
                        "kind": "primitive",
                        "type": "integer",
                        "constraints": {"minimum": 1, "maximum": 730},
                    },
                    "buffer_before_minutes": {
                        "kind": "primitive",
                        "type": "integer",
                        "constraints": {
                            "minimum": 0,
                            "maximum": 1440,
                            "multipleOf": 30,
                        },
                    },
                    "buffer_after_minutes": {
                        "kind": "primitive",
                        "type": "integer",
                        "constraints": {
                            "minimum": 0,
                            "maximum": 1440,
                            "multipleOf": 30,
                        },
                    },
                    "availability": {
                        "kind": "array",
                        "items": {"kind": "any"},
                        "constraints": {"minItems": 7, "maxItems": 7},
                    },
                    "provider_connection_ids": {
                        "kind": "array",
                        "items": {"kind": "primitive", "type": "integer"},
                    },
                    "video_provider": {
                        "kind": "enum",
                        "values": ["none", "google_meet", "zoom"],
                    },
                    "video_provider_connection_id": {
                        "kind": "nullable",
                        "inner": {"kind": "primitive", "type": "integer"},
                    },
                    "reminders_enabled": {"kind": "primitive", "type": "boolean"},
                    "reminder_offset_value": {
                        "kind": "primitive",
                        "type": "integer",
                        "constraints": {"minimum": 1, "maximum": 10080},
                    },
                    "reminder_offset_unit": {
                        "kind": "enum",
                        "values": ["minutes", "hours", "days", "weeks"],
                    },
                    "meta": {"kind": "any"},
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return await self._client.request(
            method="patch",
            path=("v2", "products", str(product), "variants", str(variant), "booking"),
            body=body,
            model=SdkUpdateBookingConfigurationResponseValue200ApplicationJson,
            request_options=request_options,
        )
