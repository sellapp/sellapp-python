# This file is auto-generated by oagen. Do not edit.

from __future__ import annotations

from typing import TYPE_CHECKING, Any, Dict, List, Optional, Union

from .._base_client import WithRawResponse

if TYPE_CHECKING:
    from .._client import AsyncSellAppClient, SellAppClient

from sellapp_sdk.common.models.catalog_visibility import CatalogVisibility
from sellapp_sdk.common.models.list_products_response_value_200_application_json_property_data_item import (
    ListProductsResponseValue200ApplicationJsonPropertyDataItem,
)
from sellapp_sdk.common.models.sdk_batch_create_products_response_value_200_application_json import (
    SdkBatchCreateProductsResponseValue200ApplicationJson,
)
from sellapp_sdk.common.models.sdk_create_product_request_application_json_type import (
    SdkCreateProductRequestApplicationJsonType,
)

from .._pagination import AsyncPage, SyncPage
from .._types import NOT_GIVEN, NotGiven, RequestOptions, _validate_model, enum_value
from .models import (
    BatchCreateProductsRequestApplicationJsonPropertyResourcesItem,
    BatchUpdateProductsRequestApplicationJsonPropertyResources,
    CreateProductRequestApplicationJsonPropertyAdditionalInformationItem,
    CreateProductRequestApplicationJsonPropertyBundleItemsItem,
    CreateProductRequestApplicationJsonPropertyOtherSettings,
    CreateProductRequestApplicationJsonPropertyVariantsItem,
    ReplaceProductRequestApplicationJsonPropertyAdditionalInformationItem,
    ReplaceProductRequestApplicationJsonPropertyOtherSettings,
    SdkBatchUpdateProductsResponseValue200ApplicationJson,
    SdkCreateProductResponseValue201ApplicationJson,
    SdkGetProductResponseValue200ApplicationJson,
    SdkReplaceProductResponseValue200ApplicationJson,
    SdkUpdateProductResponseValue200ApplicationJson,
    SearchProductsRequestApplicationJsonPropertyFiltersItem,
    SearchProductsRequestApplicationJsonPropertyIncludesItem,
    SearchProductsRequestApplicationJsonPropertySearch,
    SearchProductsRequestApplicationJsonPropertySortItem,
    SearchProductsResponseValue200ApplicationJsonPropertyDataItem,
    UpdateProductRequestApplicationJsonPropertyAdditionalInformationItem,
    UpdateProductRequestApplicationJsonPropertyOtherSettings,
)


class Products:
    """Products API resources."""

    def __init__(self, client: SellAppClient) -> None:
        self._client = client
        self.with_raw_response = WithRawResponse(self)

    def list(
        self,
        *,
        limit: Optional[int] = None,
        before: Optional[str] = None,
        after: Optional[str] = None,
        order: Optional[str] = None,
        page: Optional[int] = None,
        with_drafts: Optional[bool] = None,
        only_drafts: Optional[bool] = None,
        request_options: Optional[RequestOptions] = None,
    ) -> SyncPage[ListProductsResponseValue200ApplicationJsonPropertyDataItem]:
        """List all products

        List your store's products, 15 per page by default. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            limit: Number of items to return per page. Defaults to `15`.
            before: The before.
            after: The after.
            order: The order.
            page: Page number to return. Defaults to `1`.
            with_drafts: Include draft products alongside published products.
            only_drafts: Return only draft products.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SyncPage[ListProductsResponseValue200ApplicationJsonPropertyDataItem]

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "listProducts",
        }
        params = {
            k: v
            for k, v in {
                "limit": limit,
                "before": before,
                "after": after,
                "order": order,
                "page": page,
                "with_drafts": with_drafts,
                "only_drafts": only_drafts,
            }.items()
            if v is not None
        }
        return self._client.request_page(
            method="get",
            path=("v2", "products"),
            model=ListProductsResponseValue200ApplicationJsonPropertyDataItem,
            params=params,
            request_options=request_options,
        )

    def create(
        self,
        *,
        title: str,
        description: str,
        visibility: Union[CatalogVisibility, str],
        slug: Optional[str] = None,
        type: Optional[Union[SdkCreateProductRequestApplicationJsonType, str]] = None,
        section: Union[int, None, NotGiven] = NOT_GIVEN,
        additional_information: Optional[
            List[CreateProductRequestApplicationJsonPropertyAdditionalInformationItem]
        ] = None,
        other_settings: Optional[
            CreateProductRequestApplicationJsonPropertyOtherSettings
        ] = None,
        variants: Optional[
            List[CreateProductRequestApplicationJsonPropertyVariantsItem]
        ] = None,
        bundle_items: Optional[
            List[CreateProductRequestApplicationJsonPropertyBundleItemsItem]
        ] = None,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkCreateProductResponseValue201ApplicationJson:
        """Create a product

        Create a product in the selected store. Requires a token with the `listing` ability. The same product rules apply as in the dashboard. Add a variant before offering the product at checkout. Include variants to create the product and its initial variants atomically. For a complete bundle, set type to bundle, supply one BUNDLE variant with empty deliverable.data, and include bundle_items. The response includes the created variant IDs in data.variants. Omit these fields to retain separate creation. These options apply to single-product creation, not batch creation. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            title: The title.
            description: The description.
            visibility: The visibility.
            slug: The slug.
            type: The type.
            section: The section.
            additional_information: The additional information.
            other_settings: The other settings.
            variants: Optional initial variants, using the same fields as variant creation. A bundle accepts exactly one variant. A validation failure rolls back the product and every variant.
            bundle_items: Only for type bundle, with exactly one initial variant. Same-store standard product variants to include. Each variant ID must be unique.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkCreateProductResponseValue201ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "createProduct",
        }
        body: Dict[str, Any] = {
            k: v
            for k, v in {
                "title": title,
                "description": description,
                "visibility": enum_value(visibility),
                "slug": slug,
                "type": enum_value(type) if type is not None else None,
                "additional_information": [
                    item.to_dict() for item in additional_information
                ]
                if additional_information is not None
                else None,
                "other_settings": other_settings.to_dict()
                if other_settings is not None
                else None,
                "variants": [item.to_dict() for item in variants]
                if variants is not None
                else None,
                "bundle_items": [item.to_dict() for item in bundle_items]
                if bundle_items is not None
                else None,
            }.items()
            if v is not None
        }
        if not isinstance(section, NotGiven):
            body["section"] = section
        _validate_model(
            body,
            {
                "name": "SdkCreateProductRequestApplicationJson",
                "required": ["title", "description", "visibility"],
                "properties": {
                    "title": {"kind": "primitive", "type": "string"},
                    "description": {"kind": "primitive", "type": "string"},
                    "visibility": {
                        "kind": "enum",
                        "values": ["PUBLIC", "ON_HOLD", "HIDDEN", "PRIVATE"],
                    },
                    "slug": {"kind": "primitive", "type": "string"},
                    "type": {"kind": "enum", "values": ["product", "bundle"]},
                    "section": {
                        "kind": "nullable",
                        "inner": {"kind": "primitive", "type": "integer"},
                    },
                    "additional_information": {
                        "kind": "array",
                        "items": {"kind": "any"},
                    },
                    "other_settings": {"kind": "any"},
                    "variants": {
                        "kind": "array",
                        "items": {"kind": "any"},
                        "constraints": {"minItems": 1, "maxItems": 100},
                    },
                    "bundle_items": {
                        "kind": "array",
                        "items": {"kind": "any"},
                        "constraints": {"minItems": 1, "maxItems": 50},
                    },
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return self._client.request(
            method="post",
            path=("v2", "products"),
            body=body,
            model=SdkCreateProductResponseValue201ApplicationJson,
            request_options=request_options,
        )

    def get(
        self,
        product: int,
        *,
        with_drafts: Optional[bool] = None,
        only_drafts: Optional[bool] = None,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkGetProductResponseValue200ApplicationJson:
        """Retrieve a product

        Retrieve a product by its ID. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            product: The product path parameter.
            with_drafts: Include draft products alongside published products.
            only_drafts: Return only draft products.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkGetProductResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "getProduct",
        }
        params: Dict[str, Any] = {
            k: v
            for k, v in {
                "with_drafts": with_drafts,
                "only_drafts": only_drafts,
            }.items()
            if v is not None
        }
        return self._client.request(
            method="get",
            path=("v2", "products", str(product)),
            params=params,
            model=SdkGetProductResponseValue200ApplicationJson,
            request_options=request_options,
        )

    def replace(
        self,
        product: int,
        *,
        title: Optional[str] = None,
        description: Optional[str] = None,
        visibility: Optional[Union[CatalogVisibility, str]] = None,
        slug: Optional[str] = None,
        section: Union[int, None, NotGiven] = NOT_GIVEN,
        additional_information: Optional[
            List[ReplaceProductRequestApplicationJsonPropertyAdditionalInformationItem]
        ] = None,
        other_settings: Optional[
            ReplaceProductRequestApplicationJsonPropertyOtherSettings
        ] = None,
        expected_updated_at: Optional[str] = None,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkReplaceProductResponseValue200ApplicationJson:
        """Update a product

        Update a product in the authenticated store. Requires a token with the `listing` ability. Send `expected_updated_at` to reject stale writes instead of overwriting a newer change. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            product: The product path parameter.
            title: The title.
            description: The description.
            visibility: The visibility.
            slug: The slug.
            section: The section.
            additional_information: The additional information.
            other_settings: The other settings.
            expected_updated_at: Optional optimistic-concurrency snapshot. Send the exact `updated_at` value from the product you last retrieved; the mutation is rejected if the product has changed.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkReplaceProductResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "replaceProduct",
        }
        body: Dict[str, Any] = {
            k: v
            for k, v in {
                "title": title,
                "description": description,
                "visibility": enum_value(visibility)
                if visibility is not None
                else None,
                "slug": slug,
                "additional_information": [
                    item.to_dict() for item in additional_information
                ]
                if additional_information is not None
                else None,
                "other_settings": other_settings.to_dict()
                if other_settings is not None
                else None,
                "expected_updated_at": expected_updated_at,
            }.items()
            if v is not None
        }
        if not isinstance(section, NotGiven):
            body["section"] = section
        _validate_model(
            body,
            {
                "name": "SdkReplaceProductRequestApplicationJson",
                "required": [],
                "properties": {
                    "title": {"kind": "primitive", "type": "string"},
                    "description": {"kind": "primitive", "type": "string"},
                    "visibility": {
                        "kind": "enum",
                        "values": ["PUBLIC", "ON_HOLD", "HIDDEN", "PRIVATE"],
                    },
                    "slug": {"kind": "primitive", "type": "string"},
                    "section": {
                        "kind": "nullable",
                        "inner": {"kind": "primitive", "type": "integer"},
                    },
                    "additional_information": {
                        "kind": "array",
                        "items": {"kind": "any"},
                    },
                    "other_settings": {"kind": "any"},
                    "expected_updated_at": {
                        "kind": "primitive",
                        "type": "string",
                        "format": "date-time",
                    },
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return self._client.request(
            method="put",
            path=("v2", "products", str(product)),
            body=body,
            model=SdkReplaceProductResponseValue200ApplicationJson,
            request_options=request_options,
        )

    def update(
        self,
        product: int,
        *,
        title: Optional[str] = None,
        description: Optional[str] = None,
        visibility: Optional[Union[CatalogVisibility, str]] = None,
        slug: Optional[str] = None,
        section: Union[int, None, NotGiven] = NOT_GIVEN,
        additional_information: Optional[
            List[UpdateProductRequestApplicationJsonPropertyAdditionalInformationItem]
        ] = None,
        other_settings: Optional[
            UpdateProductRequestApplicationJsonPropertyOtherSettings
        ] = None,
        expected_updated_at: Optional[str] = None,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkUpdateProductResponseValue200ApplicationJson:
        """Update a product

        Update a product in the authenticated store. Requires a token with the `listing` ability. Send `expected_updated_at` to reject stale writes instead of overwriting a newer change. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            product: The product path parameter.
            title: The title.
            description: The description.
            visibility: The visibility.
            slug: The slug.
            section: The section.
            additional_information: The additional information.
            other_settings: The other settings.
            expected_updated_at: Optional optimistic-concurrency snapshot. Send the exact `updated_at` value from the product you last retrieved; the mutation is rejected if the product has changed.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkUpdateProductResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "updateProduct",
        }
        body: Dict[str, Any] = {
            k: v
            for k, v in {
                "title": title,
                "description": description,
                "visibility": enum_value(visibility)
                if visibility is not None
                else None,
                "slug": slug,
                "additional_information": [
                    item.to_dict() for item in additional_information
                ]
                if additional_information is not None
                else None,
                "other_settings": other_settings.to_dict()
                if other_settings is not None
                else None,
                "expected_updated_at": expected_updated_at,
            }.items()
            if v is not None
        }
        if not isinstance(section, NotGiven):
            body["section"] = section
        _validate_model(
            body,
            {
                "name": "SdkUpdateProductRequestApplicationJson",
                "required": [],
                "properties": {
                    "title": {"kind": "primitive", "type": "string"},
                    "description": {"kind": "primitive", "type": "string"},
                    "visibility": {
                        "kind": "enum",
                        "values": ["PUBLIC", "ON_HOLD", "HIDDEN", "PRIVATE"],
                    },
                    "slug": {"kind": "primitive", "type": "string"},
                    "section": {
                        "kind": "nullable",
                        "inner": {"kind": "primitive", "type": "integer"},
                    },
                    "additional_information": {
                        "kind": "array",
                        "items": {"kind": "any"},
                    },
                    "other_settings": {"kind": "any"},
                    "expected_updated_at": {
                        "kind": "primitive",
                        "type": "string",
                        "format": "date-time",
                    },
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return self._client.request(
            method="patch",
            path=("v2", "products", str(product)),
            body=body,
            model=SdkUpdateProductResponseValue200ApplicationJson,
            request_options=request_options,
        )

    def delete(
        self,
        product: int,
        *,
        expected_updated_at: Optional[str] = None,
        request_options: Optional[RequestOptions] = None,
    ) -> None:
        """Delete a product

        Soft-delete a product in the authenticated store. Products with active or past-due subscriptions cannot be deleted. Send `expected_updated_at` to reject a stale deletion. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            product: The product path parameter.
            expected_updated_at: Optional optimistic-concurrency snapshot. Send the exact `updated_at` value from the product you last retrieved; the mutation is rejected if the product has changed.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "deleteProduct",
        }
        body: Dict[str, Any] = {
            k: v
            for k, v in {
                "expected_updated_at": expected_updated_at,
            }.items()
            if v is not None
        }
        _validate_model(
            body,
            {
                "name": "SdkDeleteProductRequestApplicationJson",
                "required": [],
                "properties": {
                    "expected_updated_at": {
                        "kind": "primitive",
                        "type": "string",
                        "format": "date-time",
                    }
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        self._client.request(
            method="delete",
            path=("v2", "products", str(product)),
            body=body,
            request_options=request_options,
        )

    def search(
        self,
        *,
        filters: Optional[
            List[SearchProductsRequestApplicationJsonPropertyFiltersItem]
        ] = None,
        sort: Optional[
            List[SearchProductsRequestApplicationJsonPropertySortItem]
        ] = None,
        search: Optional[SearchProductsRequestApplicationJsonPropertySearch] = None,
        includes: Optional[
            List[SearchProductsRequestApplicationJsonPropertyIncludesItem]
        ] = None,
        limit: Optional[int] = None,
        before: Optional[str] = None,
        after: Optional[str] = None,
        order: Optional[str] = None,
        page: Optional[int] = None,
        request_options: Optional[RequestOptions] = None,
    ) -> SyncPage[SearchProductsResponseValue200ApplicationJsonPropertyDataItem]:
        """Search products

        Search products using JSON body filters, search terms, includes, and sort instructions. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            filters: The filters.
            sort: The sort.
            search: The search.
            includes: The includes.
            limit: Number of items to return per page. Defaults to `15`.
            before: The before.
            after: The after.
            order: The order.
            page: Page number to return. Defaults to `1`.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SyncPage[SearchProductsResponseValue200ApplicationJsonPropertyDataItem]

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "searchProducts",
        }
        body: Dict[str, Any] = {
            k: v
            for k, v in {
                "filters": [item.to_dict() for item in filters]
                if filters is not None
                else None,
                "sort": [item.to_dict() for item in sort] if sort is not None else None,
                "search": search.to_dict() if search is not None else None,
                "includes": [item.to_dict() for item in includes]
                if includes is not None
                else None,
            }.items()
            if v is not None
        }
        params = {
            k: v
            for k, v in {
                "limit": limit,
                "before": before,
                "after": after,
                "order": order,
                "page": page,
            }.items()
            if v is not None
        }
        return self._client.request_page(
            method="post",
            path=("v2", "products", "search"),
            model=SearchProductsResponseValue200ApplicationJsonPropertyDataItem,
            body=body,
            params=params,
            request_options=request_options,
        )

    def batch_create(
        self,
        *,
        resources: List[BatchCreateProductsRequestApplicationJsonPropertyResourcesItem],
        request_options: Optional[RequestOptions] = None,
    ) -> SdkBatchCreateProductsResponseValue200ApplicationJson:
        """Batch create products

        Create multiple products in one request by sending a `resources` array. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            resources: The resources.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkBatchCreateProductsResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "batchCreateProducts",
        }
        body: Dict[str, Any] = {
            "resources": [item.to_dict() for item in resources],
        }
        _validate_model(
            body,
            {
                "name": "SdkBatchCreateProductsRequestApplicationJson",
                "required": ["resources"],
                "properties": {
                    "resources": {"kind": "array", "items": {"kind": "any"}}
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return self._client.request(
            method="post",
            path=("v2", "products", "batch"),
            body=body,
            model=SdkBatchCreateProductsResponseValue200ApplicationJson,
            request_options=request_options,
        )

    def batch_update(
        self,
        *,
        resources: BatchUpdateProductsRequestApplicationJsonPropertyResources,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkBatchUpdateProductsResponseValue200ApplicationJson:
        """Batch update products

        Update multiple products in one request by sending a `resources` object keyed by product ID. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            resources: The resources.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkBatchUpdateProductsResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "batchUpdateProducts",
        }
        body: Dict[str, Any] = {
            "resources": resources.to_dict(),
        }
        _validate_model(
            body,
            {
                "name": "SdkBatchUpdateProductsRequestApplicationJson",
                "required": ["resources"],
                "properties": {"resources": {"kind": "any"}},
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return self._client.request(
            method="patch",
            path=("v2", "products", "batch"),
            body=body,
            model=SdkBatchUpdateProductsResponseValue200ApplicationJson,
            request_options=request_options,
        )

    def batch_delete(
        self,
        *,
        resources: List[int],
        request_options: Optional[RequestOptions] = None,
    ) -> None:
        """Batch delete products

        Delete multiple products in one request by sending the product IDs in `resources`. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            resources: The resources.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "batchDeleteProducts",
        }
        body: Dict[str, Any] = {
            "resources": resources,
        }
        _validate_model(
            body,
            {
                "name": "SdkBatchDeleteProductsRequestApplicationJson",
                "required": ["resources"],
                "properties": {
                    "resources": {
                        "kind": "array",
                        "items": {"kind": "primitive", "type": "integer"},
                        "constraints": {"minItems": 1, "uniqueItems": True},
                    }
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        self._client.request(
            method="delete",
            path=("v2", "products", "batch"),
            body=body,
            request_options=request_options,
        )


class AsyncProducts:
    """Products API resources (async)."""

    def __init__(self, client: AsyncSellAppClient) -> None:
        self._client = client
        self.with_raw_response = WithRawResponse(self)

    async def list(
        self,
        *,
        limit: Optional[int] = None,
        before: Optional[str] = None,
        after: Optional[str] = None,
        order: Optional[str] = None,
        page: Optional[int] = None,
        with_drafts: Optional[bool] = None,
        only_drafts: Optional[bool] = None,
        request_options: Optional[RequestOptions] = None,
    ) -> AsyncPage[ListProductsResponseValue200ApplicationJsonPropertyDataItem]:
        """List all products

        List your store's products, 15 per page by default. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            limit: Number of items to return per page. Defaults to `15`.
            before: The before.
            after: The after.
            order: The order.
            page: Page number to return. Defaults to `1`.
            with_drafts: Include draft products alongside published products.
            only_drafts: Return only draft products.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            AsyncPage[ListProductsResponseValue200ApplicationJsonPropertyDataItem]

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "listProducts",
        }
        params = {
            k: v
            for k, v in {
                "limit": limit,
                "before": before,
                "after": after,
                "order": order,
                "page": page,
                "with_drafts": with_drafts,
                "only_drafts": only_drafts,
            }.items()
            if v is not None
        }
        return await self._client.request_page(
            method="get",
            path=("v2", "products"),
            model=ListProductsResponseValue200ApplicationJsonPropertyDataItem,
            params=params,
            request_options=request_options,
        )

    async def create(
        self,
        *,
        title: str,
        description: str,
        visibility: Union[CatalogVisibility, str],
        slug: Optional[str] = None,
        type: Optional[Union[SdkCreateProductRequestApplicationJsonType, str]] = None,
        section: Union[int, None, NotGiven] = NOT_GIVEN,
        additional_information: Optional[
            List[CreateProductRequestApplicationJsonPropertyAdditionalInformationItem]
        ] = None,
        other_settings: Optional[
            CreateProductRequestApplicationJsonPropertyOtherSettings
        ] = None,
        variants: Optional[
            List[CreateProductRequestApplicationJsonPropertyVariantsItem]
        ] = None,
        bundle_items: Optional[
            List[CreateProductRequestApplicationJsonPropertyBundleItemsItem]
        ] = None,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkCreateProductResponseValue201ApplicationJson:
        """Create a product

        Create a product in the selected store. Requires a token with the `listing` ability. The same product rules apply as in the dashboard. Add a variant before offering the product at checkout. Include variants to create the product and its initial variants atomically. For a complete bundle, set type to bundle, supply one BUNDLE variant with empty deliverable.data, and include bundle_items. The response includes the created variant IDs in data.variants. Omit these fields to retain separate creation. These options apply to single-product creation, not batch creation. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            title: The title.
            description: The description.
            visibility: The visibility.
            slug: The slug.
            type: The type.
            section: The section.
            additional_information: The additional information.
            other_settings: The other settings.
            variants: Optional initial variants, using the same fields as variant creation. A bundle accepts exactly one variant. A validation failure rolls back the product and every variant.
            bundle_items: Only for type bundle, with exactly one initial variant. Same-store standard product variants to include. Each variant ID must be unique.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkCreateProductResponseValue201ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "createProduct",
        }
        body: Dict[str, Any] = {
            k: v
            for k, v in {
                "title": title,
                "description": description,
                "visibility": enum_value(visibility),
                "slug": slug,
                "type": enum_value(type) if type is not None else None,
                "additional_information": [
                    item.to_dict() for item in additional_information
                ]
                if additional_information is not None
                else None,
                "other_settings": other_settings.to_dict()
                if other_settings is not None
                else None,
                "variants": [item.to_dict() for item in variants]
                if variants is not None
                else None,
                "bundle_items": [item.to_dict() for item in bundle_items]
                if bundle_items is not None
                else None,
            }.items()
            if v is not None
        }
        if not isinstance(section, NotGiven):
            body["section"] = section
        _validate_model(
            body,
            {
                "name": "SdkCreateProductRequestApplicationJson",
                "required": ["title", "description", "visibility"],
                "properties": {
                    "title": {"kind": "primitive", "type": "string"},
                    "description": {"kind": "primitive", "type": "string"},
                    "visibility": {
                        "kind": "enum",
                        "values": ["PUBLIC", "ON_HOLD", "HIDDEN", "PRIVATE"],
                    },
                    "slug": {"kind": "primitive", "type": "string"},
                    "type": {"kind": "enum", "values": ["product", "bundle"]},
                    "section": {
                        "kind": "nullable",
                        "inner": {"kind": "primitive", "type": "integer"},
                    },
                    "additional_information": {
                        "kind": "array",
                        "items": {"kind": "any"},
                    },
                    "other_settings": {"kind": "any"},
                    "variants": {
                        "kind": "array",
                        "items": {"kind": "any"},
                        "constraints": {"minItems": 1, "maxItems": 100},
                    },
                    "bundle_items": {
                        "kind": "array",
                        "items": {"kind": "any"},
                        "constraints": {"minItems": 1, "maxItems": 50},
                    },
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return await self._client.request(
            method="post",
            path=("v2", "products"),
            body=body,
            model=SdkCreateProductResponseValue201ApplicationJson,
            request_options=request_options,
        )

    async def get(
        self,
        product: int,
        *,
        with_drafts: Optional[bool] = None,
        only_drafts: Optional[bool] = None,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkGetProductResponseValue200ApplicationJson:
        """Retrieve a product

        Retrieve a product by its ID. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            product: The product path parameter.
            with_drafts: Include draft products alongside published products.
            only_drafts: Return only draft products.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkGetProductResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "getProduct",
        }
        params: Dict[str, Any] = {
            k: v
            for k, v in {
                "with_drafts": with_drafts,
                "only_drafts": only_drafts,
            }.items()
            if v is not None
        }
        return await self._client.request(
            method="get",
            path=("v2", "products", str(product)),
            params=params,
            model=SdkGetProductResponseValue200ApplicationJson,
            request_options=request_options,
        )

    async def replace(
        self,
        product: int,
        *,
        title: Optional[str] = None,
        description: Optional[str] = None,
        visibility: Optional[Union[CatalogVisibility, str]] = None,
        slug: Optional[str] = None,
        section: Union[int, None, NotGiven] = NOT_GIVEN,
        additional_information: Optional[
            List[ReplaceProductRequestApplicationJsonPropertyAdditionalInformationItem]
        ] = None,
        other_settings: Optional[
            ReplaceProductRequestApplicationJsonPropertyOtherSettings
        ] = None,
        expected_updated_at: Optional[str] = None,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkReplaceProductResponseValue200ApplicationJson:
        """Update a product

        Update a product in the authenticated store. Requires a token with the `listing` ability. Send `expected_updated_at` to reject stale writes instead of overwriting a newer change. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            product: The product path parameter.
            title: The title.
            description: The description.
            visibility: The visibility.
            slug: The slug.
            section: The section.
            additional_information: The additional information.
            other_settings: The other settings.
            expected_updated_at: Optional optimistic-concurrency snapshot. Send the exact `updated_at` value from the product you last retrieved; the mutation is rejected if the product has changed.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkReplaceProductResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "replaceProduct",
        }
        body: Dict[str, Any] = {
            k: v
            for k, v in {
                "title": title,
                "description": description,
                "visibility": enum_value(visibility)
                if visibility is not None
                else None,
                "slug": slug,
                "additional_information": [
                    item.to_dict() for item in additional_information
                ]
                if additional_information is not None
                else None,
                "other_settings": other_settings.to_dict()
                if other_settings is not None
                else None,
                "expected_updated_at": expected_updated_at,
            }.items()
            if v is not None
        }
        if not isinstance(section, NotGiven):
            body["section"] = section
        _validate_model(
            body,
            {
                "name": "SdkReplaceProductRequestApplicationJson",
                "required": [],
                "properties": {
                    "title": {"kind": "primitive", "type": "string"},
                    "description": {"kind": "primitive", "type": "string"},
                    "visibility": {
                        "kind": "enum",
                        "values": ["PUBLIC", "ON_HOLD", "HIDDEN", "PRIVATE"],
                    },
                    "slug": {"kind": "primitive", "type": "string"},
                    "section": {
                        "kind": "nullable",
                        "inner": {"kind": "primitive", "type": "integer"},
                    },
                    "additional_information": {
                        "kind": "array",
                        "items": {"kind": "any"},
                    },
                    "other_settings": {"kind": "any"},
                    "expected_updated_at": {
                        "kind": "primitive",
                        "type": "string",
                        "format": "date-time",
                    },
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return await self._client.request(
            method="put",
            path=("v2", "products", str(product)),
            body=body,
            model=SdkReplaceProductResponseValue200ApplicationJson,
            request_options=request_options,
        )

    async def update(
        self,
        product: int,
        *,
        title: Optional[str] = None,
        description: Optional[str] = None,
        visibility: Optional[Union[CatalogVisibility, str]] = None,
        slug: Optional[str] = None,
        section: Union[int, None, NotGiven] = NOT_GIVEN,
        additional_information: Optional[
            List[UpdateProductRequestApplicationJsonPropertyAdditionalInformationItem]
        ] = None,
        other_settings: Optional[
            UpdateProductRequestApplicationJsonPropertyOtherSettings
        ] = None,
        expected_updated_at: Optional[str] = None,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkUpdateProductResponseValue200ApplicationJson:
        """Update a product

        Update a product in the authenticated store. Requires a token with the `listing` ability. Send `expected_updated_at` to reject stale writes instead of overwriting a newer change. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            product: The product path parameter.
            title: The title.
            description: The description.
            visibility: The visibility.
            slug: The slug.
            section: The section.
            additional_information: The additional information.
            other_settings: The other settings.
            expected_updated_at: Optional optimistic-concurrency snapshot. Send the exact `updated_at` value from the product you last retrieved; the mutation is rejected if the product has changed.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkUpdateProductResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "updateProduct",
        }
        body: Dict[str, Any] = {
            k: v
            for k, v in {
                "title": title,
                "description": description,
                "visibility": enum_value(visibility)
                if visibility is not None
                else None,
                "slug": slug,
                "additional_information": [
                    item.to_dict() for item in additional_information
                ]
                if additional_information is not None
                else None,
                "other_settings": other_settings.to_dict()
                if other_settings is not None
                else None,
                "expected_updated_at": expected_updated_at,
            }.items()
            if v is not None
        }
        if not isinstance(section, NotGiven):
            body["section"] = section
        _validate_model(
            body,
            {
                "name": "SdkUpdateProductRequestApplicationJson",
                "required": [],
                "properties": {
                    "title": {"kind": "primitive", "type": "string"},
                    "description": {"kind": "primitive", "type": "string"},
                    "visibility": {
                        "kind": "enum",
                        "values": ["PUBLIC", "ON_HOLD", "HIDDEN", "PRIVATE"],
                    },
                    "slug": {"kind": "primitive", "type": "string"},
                    "section": {
                        "kind": "nullable",
                        "inner": {"kind": "primitive", "type": "integer"},
                    },
                    "additional_information": {
                        "kind": "array",
                        "items": {"kind": "any"},
                    },
                    "other_settings": {"kind": "any"},
                    "expected_updated_at": {
                        "kind": "primitive",
                        "type": "string",
                        "format": "date-time",
                    },
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return await self._client.request(
            method="patch",
            path=("v2", "products", str(product)),
            body=body,
            model=SdkUpdateProductResponseValue200ApplicationJson,
            request_options=request_options,
        )

    async def delete(
        self,
        product: int,
        *,
        expected_updated_at: Optional[str] = None,
        request_options: Optional[RequestOptions] = None,
    ) -> None:
        """Delete a product

        Soft-delete a product in the authenticated store. Products with active or past-due subscriptions cannot be deleted. Send `expected_updated_at` to reject a stale deletion. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            product: The product path parameter.
            expected_updated_at: Optional optimistic-concurrency snapshot. Send the exact `updated_at` value from the product you last retrieved; the mutation is rejected if the product has changed.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "deleteProduct",
        }
        body: Dict[str, Any] = {
            k: v
            for k, v in {
                "expected_updated_at": expected_updated_at,
            }.items()
            if v is not None
        }
        _validate_model(
            body,
            {
                "name": "SdkDeleteProductRequestApplicationJson",
                "required": [],
                "properties": {
                    "expected_updated_at": {
                        "kind": "primitive",
                        "type": "string",
                        "format": "date-time",
                    }
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        await self._client.request(
            method="delete",
            path=("v2", "products", str(product)),
            body=body,
            request_options=request_options,
        )

    async def search(
        self,
        *,
        filters: Optional[
            List[SearchProductsRequestApplicationJsonPropertyFiltersItem]
        ] = None,
        sort: Optional[
            List[SearchProductsRequestApplicationJsonPropertySortItem]
        ] = None,
        search: Optional[SearchProductsRequestApplicationJsonPropertySearch] = None,
        includes: Optional[
            List[SearchProductsRequestApplicationJsonPropertyIncludesItem]
        ] = None,
        limit: Optional[int] = None,
        before: Optional[str] = None,
        after: Optional[str] = None,
        order: Optional[str] = None,
        page: Optional[int] = None,
        request_options: Optional[RequestOptions] = None,
    ) -> AsyncPage[SearchProductsResponseValue200ApplicationJsonPropertyDataItem]:
        """Search products

        Search products using JSON body filters, search terms, includes, and sort instructions. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            filters: The filters.
            sort: The sort.
            search: The search.
            includes: The includes.
            limit: Number of items to return per page. Defaults to `15`.
            before: The before.
            after: The after.
            order: The order.
            page: Page number to return. Defaults to `1`.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            AsyncPage[SearchProductsResponseValue200ApplicationJsonPropertyDataItem]

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "searchProducts",
        }
        body: Dict[str, Any] = {
            k: v
            for k, v in {
                "filters": [item.to_dict() for item in filters]
                if filters is not None
                else None,
                "sort": [item.to_dict() for item in sort] if sort is not None else None,
                "search": search.to_dict() if search is not None else None,
                "includes": [item.to_dict() for item in includes]
                if includes is not None
                else None,
            }.items()
            if v is not None
        }
        params = {
            k: v
            for k, v in {
                "limit": limit,
                "before": before,
                "after": after,
                "order": order,
                "page": page,
            }.items()
            if v is not None
        }
        return await self._client.request_page(
            method="post",
            path=("v2", "products", "search"),
            model=SearchProductsResponseValue200ApplicationJsonPropertyDataItem,
            body=body,
            params=params,
            request_options=request_options,
        )

    async def batch_create(
        self,
        *,
        resources: List[BatchCreateProductsRequestApplicationJsonPropertyResourcesItem],
        request_options: Optional[RequestOptions] = None,
    ) -> SdkBatchCreateProductsResponseValue200ApplicationJson:
        """Batch create products

        Create multiple products in one request by sending a `resources` array. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            resources: The resources.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkBatchCreateProductsResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "batchCreateProducts",
        }
        body: Dict[str, Any] = {
            "resources": [item.to_dict() for item in resources],
        }
        _validate_model(
            body,
            {
                "name": "SdkBatchCreateProductsRequestApplicationJson",
                "required": ["resources"],
                "properties": {
                    "resources": {"kind": "array", "items": {"kind": "any"}}
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return await self._client.request(
            method="post",
            path=("v2", "products", "batch"),
            body=body,
            model=SdkBatchCreateProductsResponseValue200ApplicationJson,
            request_options=request_options,
        )

    async def batch_update(
        self,
        *,
        resources: BatchUpdateProductsRequestApplicationJsonPropertyResources,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkBatchUpdateProductsResponseValue200ApplicationJson:
        """Batch update products

        Update multiple products in one request by sending a `resources` object keyed by product ID. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            resources: The resources.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkBatchUpdateProductsResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "batchUpdateProducts",
        }
        body: Dict[str, Any] = {
            "resources": resources.to_dict(),
        }
        _validate_model(
            body,
            {
                "name": "SdkBatchUpdateProductsRequestApplicationJson",
                "required": ["resources"],
                "properties": {"resources": {"kind": "any"}},
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return await self._client.request(
            method="patch",
            path=("v2", "products", "batch"),
            body=body,
            model=SdkBatchUpdateProductsResponseValue200ApplicationJson,
            request_options=request_options,
        )

    async def batch_delete(
        self,
        *,
        resources: List[int],
        request_options: Optional[RequestOptions] = None,
    ) -> None:
        """Batch delete products

        Delete multiple products in one request by sending the product IDs in `resources`. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            resources: The resources.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "batchDeleteProducts",
        }
        body: Dict[str, Any] = {
            "resources": resources,
        }
        _validate_model(
            body,
            {
                "name": "SdkBatchDeleteProductsRequestApplicationJson",
                "required": ["resources"],
                "properties": {
                    "resources": {
                        "kind": "array",
                        "items": {"kind": "primitive", "type": "integer"},
                        "constraints": {"minItems": 1, "uniqueItems": True},
                    }
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        await self._client.request(
            method="delete",
            path=("v2", "products", "batch"),
            body=body,
            request_options=request_options,
        )
