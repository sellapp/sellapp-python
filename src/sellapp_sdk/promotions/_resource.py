# This file is auto-generated by oagen. Do not edit.

from __future__ import annotations

from typing import TYPE_CHECKING, Any, Dict, List, Optional, Union

from .._base_client import WithRawResponse

if TYPE_CHECKING:
    from .._client import AsyncSellAppClient, SellAppClient

from sellapp_sdk.common.models.sdk_create_promotion_request_application_json_status import (
    SdkCreatePromotionRequestApplicationJsonStatus,
)
from sellapp_sdk.common.models.sdk_replace_promotion_request_application_json_status import (
    SdkReplacePromotionRequestApplicationJsonStatus,
)
from sellapp_sdk.common.models.sdk_restore_promotion_request_application_json_status import (
    SdkRestorePromotionRequestApplicationJsonStatus,
)
from sellapp_sdk.common.models.sdk_update_promotion_request_application_json_status import (
    SdkUpdatePromotionRequestApplicationJsonStatus,
)

from .._pagination import AsyncPage, SyncPage
from .._types import NOT_GIVEN, NotGiven, RequestOptions, _validate_model, enum_value
from .models import (
    CreatePromotionRequestApplicationJsonPropertyPhasesItem,
    ReplacePromotionRequestApplicationJsonPropertyPhasesItem,
    RestorePromotionRequestApplicationJsonPropertyPhasesItem,
    SdkCreatePromotionResponseValue201ApplicationJson,
    SdkGetPromotionResponseValue200ApplicationJson,
    SdkListPromotionsResponseValue200ApplicationJson,
    SdkReplacePromotionResponseValue200ApplicationJson,
    SdkRestorePromotionResponseValue200ApplicationJson,
    SdkSearchPromotionsResponseValue200ApplicationJson,
    SdkUpdatePromotionResponseValue200ApplicationJson,
    SearchPromotionsRequestApplicationJsonPropertyFiltersItem,
    SearchPromotionsRequestApplicationJsonPropertyIncludesItem,
    SearchPromotionsRequestApplicationJsonPropertySearch,
    SearchPromotionsRequestApplicationJsonPropertySortItem,
    UpdatePromotionRequestApplicationJsonPropertyPhasesItem,
)


class Promotions:
    """Promotions API resources."""

    def __init__(self, client: SellAppClient) -> None:
        self._client = client
        self.with_raw_response = WithRawResponse(self)

    def list(
        self,
        *,
        limit: Optional[int] = None,
        before: Optional[str] = None,
        after: Optional[str] = None,
        order: Optional[str] = None,
        page: Optional[int] = None,
        pagination: Optional[bool] = None,
        request_options: Optional[RequestOptions] = None,
    ) -> SyncPage[SdkListPromotionsResponseValue200ApplicationJson]:
        """List promotions

        List promotions for the authenticated store, including their ordered phases. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            limit: Number of items to return per page. Defaults to `15`.
            before: The before.
            after: The after.
            order: The order.
            page: Page number to return. Defaults to `1`.
            pagination: Set to false to return at most 100 matching items in one data array without pagination links or metadata. Defaults to `true`.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SyncPage[SdkListPromotionsResponseValue200ApplicationJson]

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "listPromotions",
        }
        params = {
            k: v
            for k, v in {
                "limit": limit,
                "before": before,
                "after": after,
                "order": order,
                "page": page,
                "pagination": pagination,
            }.items()
            if v is not None
        }
        return self._client.request_page(
            method="get",
            path=("v2", "promotions"),
            model=SdkListPromotionsResponseValue200ApplicationJson,
            params=params,
            request_options=request_options,
        )

    def create(
        self,
        *,
        name: str,
        status: Union[SdkCreatePromotionRequestApplicationJsonStatus, str],
        priority: int,
        is_stackable: bool,
        phases: List[CreatePromotionRequestApplicationJsonPropertyPhasesItem],
        starts_at: Union[str, None, NotGiven] = NOT_GIVEN,
        ends_at: Union[str, None, NotGiven] = NOT_GIVEN,
        max_redemptions: Union[int, None, NotGiven] = NOT_GIVEN,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkCreatePromotionResponseValue201ApplicationJson:
        """Create a promotion

        Create a promotion and its initial ordered phase set. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            name: The name.
            status: The status.
            starts_at: The starts at.
            ends_at: A date and time after starts_at and after the current time, or null for no end date.
            priority: The priority.
            is_stackable: The is stackable.
            max_redemptions: The max redemptions.
            phases: The complete ordered phase set. Array order becomes phase position.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkCreatePromotionResponseValue201ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "createPromotion",
        }
        body: Dict[str, Any] = {
            "name": name,
            "status": enum_value(status),
            "priority": priority,
            "is_stackable": is_stackable,
            "phases": [item.to_dict() for item in phases],
        }
        if not isinstance(starts_at, NotGiven):
            body["starts_at"] = starts_at
        if not isinstance(ends_at, NotGiven):
            body["ends_at"] = ends_at
        if not isinstance(max_redemptions, NotGiven):
            body["max_redemptions"] = max_redemptions
        _validate_model(
            body,
            {
                "name": "SdkCreatePromotionRequestApplicationJson",
                "required": ["name", "status", "priority", "is_stackable", "phases"],
                "properties": {
                    "name": {
                        "kind": "primitive",
                        "type": "string",
                        "constraints": {"maxLength": 255},
                    },
                    "status": {"kind": "enum", "values": ["active", "disabled"]},
                    "starts_at": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "string",
                            "format": "date-time",
                        },
                    },
                    "ends_at": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "string",
                            "format": "date-time",
                        },
                    },
                    "priority": {
                        "kind": "primitive",
                        "type": "integer",
                        "constraints": {"minimum": 0, "maximum": 2147483647},
                    },
                    "is_stackable": {"kind": "primitive", "type": "boolean"},
                    "max_redemptions": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "integer",
                            "constraints": {"minimum": 1, "maximum": 4294967295},
                        },
                    },
                    "phases": {
                        "kind": "array",
                        "items": {"kind": "any"},
                        "constraints": {"minItems": 1},
                    },
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return self._client.request(
            method="post",
            path=("v2", "promotions"),
            body=body,
            model=SdkCreatePromotionResponseValue201ApplicationJson,
            request_options=request_options,
        )

    def search(
        self,
        *,
        filters: Optional[
            List[SearchPromotionsRequestApplicationJsonPropertyFiltersItem]
        ] = None,
        sort: Optional[
            List[SearchPromotionsRequestApplicationJsonPropertySortItem]
        ] = None,
        search: Optional[SearchPromotionsRequestApplicationJsonPropertySearch] = None,
        includes: Optional[
            List[SearchPromotionsRequestApplicationJsonPropertyIncludesItem]
        ] = None,
        limit: Optional[int] = None,
        before: Optional[str] = None,
        after: Optional[str] = None,
        order: Optional[str] = None,
        page: Optional[int] = None,
        pagination: Optional[bool] = None,
        request_options: Optional[RequestOptions] = None,
    ) -> SyncPage[SdkSearchPromotionsResponseValue200ApplicationJson]:
        """Search promotions

        Search promotions by name and compose supported filters and sort instructions. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            filters: The filters.
            sort: The sort.
            search: The search.
            includes: The includes.
            limit: Number of items to return per page. Defaults to `15`.
            before: The before.
            after: The after.
            order: The order.
            page: Page number to return. Defaults to `1`.
            pagination: Set to false to return at most 100 matching items in one data array without pagination links or metadata. Defaults to `true`.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SyncPage[SdkSearchPromotionsResponseValue200ApplicationJson]

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "searchPromotions",
        }
        body: Dict[str, Any] = {
            k: v
            for k, v in {
                "filters": [item.to_dict() for item in filters]
                if filters is not None
                else None,
                "sort": [item.to_dict() for item in sort] if sort is not None else None,
                "search": search.to_dict() if search is not None else None,
                "includes": [item.to_dict() for item in includes]
                if includes is not None
                else None,
            }.items()
            if v is not None
        }
        params = {
            k: v
            for k, v in {
                "limit": limit,
                "before": before,
                "after": after,
                "order": order,
                "page": page,
                "pagination": pagination,
            }.items()
            if v is not None
        }
        return self._client.request_page(
            method="post",
            path=("v2", "promotions", "search"),
            model=SdkSearchPromotionsResponseValue200ApplicationJson,
            body=body,
            params=params,
            request_options=request_options,
        )

    def get(
        self,
        promotion: int,
        *,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkGetPromotionResponseValue200ApplicationJson:
        """Retrieve a promotion

        Retrieve a promotion and its ordered phases. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            promotion: The promotion path parameter.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkGetPromotionResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "getPromotion",
        }
        return self._client.request(
            method="get",
            path=("v2", "promotions", str(promotion)),
            model=SdkGetPromotionResponseValue200ApplicationJson,
            request_options=request_options,
        )

    def replace(
        self,
        promotion: int,
        *,
        name: Optional[str] = None,
        status: Optional[
            Union[SdkReplacePromotionRequestApplicationJsonStatus, str]
        ] = None,
        starts_at: Union[str, None, NotGiven] = NOT_GIVEN,
        ends_at: Union[str, None, NotGiven] = NOT_GIVEN,
        priority: Optional[int] = None,
        is_stackable: Optional[bool] = None,
        max_redemptions: Union[int, None, NotGiven] = NOT_GIVEN,
        phases: Optional[
            List[ReplacePromotionRequestApplicationJsonPropertyPhasesItem]
        ] = None,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkReplacePromotionResponseValue200ApplicationJson:
        """Update a promotion

        Update promotion fields. Include `phases` to replace the complete ordered phase set; omit it to preserve current phases. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            promotion: The promotion path parameter.
            name: The name.
            status: The status.
            starts_at: The starts at.
            ends_at: A date and time after starts_at and after the current time, or null for no end date.
            priority: The priority.
            is_stackable: The is stackable.
            max_redemptions: The max redemptions.
            phases: The complete ordered phase set. Array order becomes phase position.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkReplacePromotionResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "replacePromotion",
        }
        body: Dict[str, Any] = {
            k: v
            for k, v in {
                "name": name,
                "status": enum_value(status) if status is not None else None,
                "priority": priority,
                "is_stackable": is_stackable,
                "phases": [item.to_dict() for item in phases]
                if phases is not None
                else None,
            }.items()
            if v is not None
        }
        if not isinstance(starts_at, NotGiven):
            body["starts_at"] = starts_at
        if not isinstance(ends_at, NotGiven):
            body["ends_at"] = ends_at
        if not isinstance(max_redemptions, NotGiven):
            body["max_redemptions"] = max_redemptions
        _validate_model(
            body,
            {
                "name": "SdkReplacePromotionRequestApplicationJson",
                "required": [],
                "properties": {
                    "name": {
                        "kind": "primitive",
                        "type": "string",
                        "constraints": {"maxLength": 255},
                    },
                    "status": {"kind": "enum", "values": ["active", "disabled"]},
                    "starts_at": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "string",
                            "format": "date-time",
                        },
                    },
                    "ends_at": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "string",
                            "format": "date-time",
                        },
                    },
                    "priority": {
                        "kind": "primitive",
                        "type": "integer",
                        "constraints": {"minimum": 0, "maximum": 2147483647},
                    },
                    "is_stackable": {"kind": "primitive", "type": "boolean"},
                    "max_redemptions": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "integer",
                            "constraints": {"minimum": 1, "maximum": 4294967295},
                        },
                    },
                    "phases": {
                        "kind": "array",
                        "items": {"kind": "any"},
                        "constraints": {"minItems": 1},
                    },
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return self._client.request(
            method="put",
            path=("v2", "promotions", str(promotion)),
            body=body,
            model=SdkReplacePromotionResponseValue200ApplicationJson,
            request_options=request_options,
        )

    def update(
        self,
        promotion: int,
        *,
        name: Optional[str] = None,
        status: Optional[
            Union[SdkUpdatePromotionRequestApplicationJsonStatus, str]
        ] = None,
        starts_at: Union[str, None, NotGiven] = NOT_GIVEN,
        ends_at: Union[str, None, NotGiven] = NOT_GIVEN,
        priority: Optional[int] = None,
        is_stackable: Optional[bool] = None,
        max_redemptions: Union[int, None, NotGiven] = NOT_GIVEN,
        phases: Optional[
            List[UpdatePromotionRequestApplicationJsonPropertyPhasesItem]
        ] = None,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkUpdatePromotionResponseValue200ApplicationJson:
        """Update a promotion

        Update promotion fields. Include `phases` to replace the complete ordered phase set; omit it to preserve current phases. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            promotion: The promotion path parameter.
            name: The name.
            status: The status.
            starts_at: The starts at.
            ends_at: A date and time after starts_at and after the current time, or null for no end date.
            priority: The priority.
            is_stackable: The is stackable.
            max_redemptions: The max redemptions.
            phases: The complete ordered phase set. Array order becomes phase position.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkUpdatePromotionResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "updatePromotion",
        }
        body: Dict[str, Any] = {
            k: v
            for k, v in {
                "name": name,
                "status": enum_value(status) if status is not None else None,
                "priority": priority,
                "is_stackable": is_stackable,
                "phases": [item.to_dict() for item in phases]
                if phases is not None
                else None,
            }.items()
            if v is not None
        }
        if not isinstance(starts_at, NotGiven):
            body["starts_at"] = starts_at
        if not isinstance(ends_at, NotGiven):
            body["ends_at"] = ends_at
        if not isinstance(max_redemptions, NotGiven):
            body["max_redemptions"] = max_redemptions
        _validate_model(
            body,
            {
                "name": "SdkUpdatePromotionRequestApplicationJson",
                "required": [],
                "properties": {
                    "name": {
                        "kind": "primitive",
                        "type": "string",
                        "constraints": {"maxLength": 255},
                    },
                    "status": {"kind": "enum", "values": ["active", "disabled"]},
                    "starts_at": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "string",
                            "format": "date-time",
                        },
                    },
                    "ends_at": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "string",
                            "format": "date-time",
                        },
                    },
                    "priority": {
                        "kind": "primitive",
                        "type": "integer",
                        "constraints": {"minimum": 0, "maximum": 2147483647},
                    },
                    "is_stackable": {"kind": "primitive", "type": "boolean"},
                    "max_redemptions": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "integer",
                            "constraints": {"minimum": 1, "maximum": 4294967295},
                        },
                    },
                    "phases": {
                        "kind": "array",
                        "items": {"kind": "any"},
                        "constraints": {"minItems": 1},
                    },
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return self._client.request(
            method="patch",
            path=("v2", "promotions", str(promotion)),
            body=body,
            model=SdkUpdatePromotionResponseValue200ApplicationJson,
            request_options=request_options,
        )

    def delete(
        self,
        promotion: int,
        *,
        request_options: Optional[RequestOptions] = None,
    ) -> None:
        """Delete a promotion

        Delete a promotion. Unreferenced phases are purged; phases referenced by order history remain soft-deleted. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            promotion: The promotion path parameter.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "deletePromotion",
        }
        self._client.request(
            method="delete",
            path=("v2", "promotions", str(promotion)),
            request_options=request_options,
        )

    def restore(
        self,
        promotion: int,
        *,
        name: str,
        status: Union[SdkRestorePromotionRequestApplicationJsonStatus, str],
        priority: int,
        is_stackable: bool,
        phases: List[RestorePromotionRequestApplicationJsonPropertyPhasesItem],
        starts_at: Union[str, None, NotGiven] = NOT_GIVEN,
        ends_at: Union[str, None, NotGiven] = NOT_GIVEN,
        max_redemptions: Union[int, None, NotGiven] = NOT_GIVEN,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkRestorePromotionResponseValue200ApplicationJson:
        """Restore a promotion

        Restore a soft-deleted promotion with a complete replacement phase set. Historical phases referenced by order history remain soft-deleted and are not reactivated. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            promotion: The promotion path parameter.
            name: The name.
            status: The status.
            starts_at: The starts at.
            ends_at: A date and time after starts_at and after the current time, or null for no end date.
            priority: The priority.
            is_stackable: The is stackable.
            max_redemptions: The max redemptions.
            phases: The complete ordered phase set. Array order becomes phase position.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkRestorePromotionResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "restorePromotion",
        }
        body: Dict[str, Any] = {
            "name": name,
            "status": enum_value(status),
            "priority": priority,
            "is_stackable": is_stackable,
            "phases": [item.to_dict() for item in phases],
        }
        if not isinstance(starts_at, NotGiven):
            body["starts_at"] = starts_at
        if not isinstance(ends_at, NotGiven):
            body["ends_at"] = ends_at
        if not isinstance(max_redemptions, NotGiven):
            body["max_redemptions"] = max_redemptions
        _validate_model(
            body,
            {
                "name": "SdkRestorePromotionRequestApplicationJson",
                "required": ["name", "status", "priority", "is_stackable", "phases"],
                "properties": {
                    "name": {
                        "kind": "primitive",
                        "type": "string",
                        "constraints": {"maxLength": 255},
                    },
                    "status": {"kind": "enum", "values": ["active", "disabled"]},
                    "starts_at": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "string",
                            "format": "date-time",
                        },
                    },
                    "ends_at": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "string",
                            "format": "date-time",
                        },
                    },
                    "priority": {
                        "kind": "primitive",
                        "type": "integer",
                        "constraints": {"minimum": 0, "maximum": 2147483647},
                    },
                    "is_stackable": {"kind": "primitive", "type": "boolean"},
                    "max_redemptions": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "integer",
                            "constraints": {"minimum": 1, "maximum": 4294967295},
                        },
                    },
                    "phases": {
                        "kind": "array",
                        "items": {"kind": "any"},
                        "constraints": {"minItems": 1},
                    },
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return self._client.request(
            method="post",
            path=("v2", "promotions", str(promotion), "restore"),
            body=body,
            model=SdkRestorePromotionResponseValue200ApplicationJson,
            request_options=request_options,
        )


class AsyncPromotions:
    """Promotions API resources (async)."""

    def __init__(self, client: AsyncSellAppClient) -> None:
        self._client = client
        self.with_raw_response = WithRawResponse(self)

    async def list(
        self,
        *,
        limit: Optional[int] = None,
        before: Optional[str] = None,
        after: Optional[str] = None,
        order: Optional[str] = None,
        page: Optional[int] = None,
        pagination: Optional[bool] = None,
        request_options: Optional[RequestOptions] = None,
    ) -> AsyncPage[SdkListPromotionsResponseValue200ApplicationJson]:
        """List promotions

        List promotions for the authenticated store, including their ordered phases. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            limit: Number of items to return per page. Defaults to `15`.
            before: The before.
            after: The after.
            order: The order.
            page: Page number to return. Defaults to `1`.
            pagination: Set to false to return at most 100 matching items in one data array without pagination links or metadata. Defaults to `true`.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            AsyncPage[SdkListPromotionsResponseValue200ApplicationJson]

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "listPromotions",
        }
        params = {
            k: v
            for k, v in {
                "limit": limit,
                "before": before,
                "after": after,
                "order": order,
                "page": page,
                "pagination": pagination,
            }.items()
            if v is not None
        }
        return await self._client.request_page(
            method="get",
            path=("v2", "promotions"),
            model=SdkListPromotionsResponseValue200ApplicationJson,
            params=params,
            request_options=request_options,
        )

    async def create(
        self,
        *,
        name: str,
        status: Union[SdkCreatePromotionRequestApplicationJsonStatus, str],
        priority: int,
        is_stackable: bool,
        phases: List[CreatePromotionRequestApplicationJsonPropertyPhasesItem],
        starts_at: Union[str, None, NotGiven] = NOT_GIVEN,
        ends_at: Union[str, None, NotGiven] = NOT_GIVEN,
        max_redemptions: Union[int, None, NotGiven] = NOT_GIVEN,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkCreatePromotionResponseValue201ApplicationJson:
        """Create a promotion

        Create a promotion and its initial ordered phase set. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            name: The name.
            status: The status.
            starts_at: The starts at.
            ends_at: A date and time after starts_at and after the current time, or null for no end date.
            priority: The priority.
            is_stackable: The is stackable.
            max_redemptions: The max redemptions.
            phases: The complete ordered phase set. Array order becomes phase position.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkCreatePromotionResponseValue201ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "createPromotion",
        }
        body: Dict[str, Any] = {
            "name": name,
            "status": enum_value(status),
            "priority": priority,
            "is_stackable": is_stackable,
            "phases": [item.to_dict() for item in phases],
        }
        if not isinstance(starts_at, NotGiven):
            body["starts_at"] = starts_at
        if not isinstance(ends_at, NotGiven):
            body["ends_at"] = ends_at
        if not isinstance(max_redemptions, NotGiven):
            body["max_redemptions"] = max_redemptions
        _validate_model(
            body,
            {
                "name": "SdkCreatePromotionRequestApplicationJson",
                "required": ["name", "status", "priority", "is_stackable", "phases"],
                "properties": {
                    "name": {
                        "kind": "primitive",
                        "type": "string",
                        "constraints": {"maxLength": 255},
                    },
                    "status": {"kind": "enum", "values": ["active", "disabled"]},
                    "starts_at": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "string",
                            "format": "date-time",
                        },
                    },
                    "ends_at": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "string",
                            "format": "date-time",
                        },
                    },
                    "priority": {
                        "kind": "primitive",
                        "type": "integer",
                        "constraints": {"minimum": 0, "maximum": 2147483647},
                    },
                    "is_stackable": {"kind": "primitive", "type": "boolean"},
                    "max_redemptions": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "integer",
                            "constraints": {"minimum": 1, "maximum": 4294967295},
                        },
                    },
                    "phases": {
                        "kind": "array",
                        "items": {"kind": "any"},
                        "constraints": {"minItems": 1},
                    },
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return await self._client.request(
            method="post",
            path=("v2", "promotions"),
            body=body,
            model=SdkCreatePromotionResponseValue201ApplicationJson,
            request_options=request_options,
        )

    async def search(
        self,
        *,
        filters: Optional[
            List[SearchPromotionsRequestApplicationJsonPropertyFiltersItem]
        ] = None,
        sort: Optional[
            List[SearchPromotionsRequestApplicationJsonPropertySortItem]
        ] = None,
        search: Optional[SearchPromotionsRequestApplicationJsonPropertySearch] = None,
        includes: Optional[
            List[SearchPromotionsRequestApplicationJsonPropertyIncludesItem]
        ] = None,
        limit: Optional[int] = None,
        before: Optional[str] = None,
        after: Optional[str] = None,
        order: Optional[str] = None,
        page: Optional[int] = None,
        pagination: Optional[bool] = None,
        request_options: Optional[RequestOptions] = None,
    ) -> AsyncPage[SdkSearchPromotionsResponseValue200ApplicationJson]:
        """Search promotions

        Search promotions by name and compose supported filters and sort instructions. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            filters: The filters.
            sort: The sort.
            search: The search.
            includes: The includes.
            limit: Number of items to return per page. Defaults to `15`.
            before: The before.
            after: The after.
            order: The order.
            page: Page number to return. Defaults to `1`.
            pagination: Set to false to return at most 100 matching items in one data array without pagination links or metadata. Defaults to `true`.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            AsyncPage[SdkSearchPromotionsResponseValue200ApplicationJson]

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "searchPromotions",
        }
        body: Dict[str, Any] = {
            k: v
            for k, v in {
                "filters": [item.to_dict() for item in filters]
                if filters is not None
                else None,
                "sort": [item.to_dict() for item in sort] if sort is not None else None,
                "search": search.to_dict() if search is not None else None,
                "includes": [item.to_dict() for item in includes]
                if includes is not None
                else None,
            }.items()
            if v is not None
        }
        params = {
            k: v
            for k, v in {
                "limit": limit,
                "before": before,
                "after": after,
                "order": order,
                "page": page,
                "pagination": pagination,
            }.items()
            if v is not None
        }
        return await self._client.request_page(
            method="post",
            path=("v2", "promotions", "search"),
            model=SdkSearchPromotionsResponseValue200ApplicationJson,
            body=body,
            params=params,
            request_options=request_options,
        )

    async def get(
        self,
        promotion: int,
        *,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkGetPromotionResponseValue200ApplicationJson:
        """Retrieve a promotion

        Retrieve a promotion and its ordered phases. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            promotion: The promotion path parameter.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkGetPromotionResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "getPromotion",
        }
        return await self._client.request(
            method="get",
            path=("v2", "promotions", str(promotion)),
            model=SdkGetPromotionResponseValue200ApplicationJson,
            request_options=request_options,
        )

    async def replace(
        self,
        promotion: int,
        *,
        name: Optional[str] = None,
        status: Optional[
            Union[SdkReplacePromotionRequestApplicationJsonStatus, str]
        ] = None,
        starts_at: Union[str, None, NotGiven] = NOT_GIVEN,
        ends_at: Union[str, None, NotGiven] = NOT_GIVEN,
        priority: Optional[int] = None,
        is_stackable: Optional[bool] = None,
        max_redemptions: Union[int, None, NotGiven] = NOT_GIVEN,
        phases: Optional[
            List[ReplacePromotionRequestApplicationJsonPropertyPhasesItem]
        ] = None,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkReplacePromotionResponseValue200ApplicationJson:
        """Update a promotion

        Update promotion fields. Include `phases` to replace the complete ordered phase set; omit it to preserve current phases. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            promotion: The promotion path parameter.
            name: The name.
            status: The status.
            starts_at: The starts at.
            ends_at: A date and time after starts_at and after the current time, or null for no end date.
            priority: The priority.
            is_stackable: The is stackable.
            max_redemptions: The max redemptions.
            phases: The complete ordered phase set. Array order becomes phase position.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkReplacePromotionResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "replacePromotion",
        }
        body: Dict[str, Any] = {
            k: v
            for k, v in {
                "name": name,
                "status": enum_value(status) if status is not None else None,
                "priority": priority,
                "is_stackable": is_stackable,
                "phases": [item.to_dict() for item in phases]
                if phases is not None
                else None,
            }.items()
            if v is not None
        }
        if not isinstance(starts_at, NotGiven):
            body["starts_at"] = starts_at
        if not isinstance(ends_at, NotGiven):
            body["ends_at"] = ends_at
        if not isinstance(max_redemptions, NotGiven):
            body["max_redemptions"] = max_redemptions
        _validate_model(
            body,
            {
                "name": "SdkReplacePromotionRequestApplicationJson",
                "required": [],
                "properties": {
                    "name": {
                        "kind": "primitive",
                        "type": "string",
                        "constraints": {"maxLength": 255},
                    },
                    "status": {"kind": "enum", "values": ["active", "disabled"]},
                    "starts_at": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "string",
                            "format": "date-time",
                        },
                    },
                    "ends_at": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "string",
                            "format": "date-time",
                        },
                    },
                    "priority": {
                        "kind": "primitive",
                        "type": "integer",
                        "constraints": {"minimum": 0, "maximum": 2147483647},
                    },
                    "is_stackable": {"kind": "primitive", "type": "boolean"},
                    "max_redemptions": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "integer",
                            "constraints": {"minimum": 1, "maximum": 4294967295},
                        },
                    },
                    "phases": {
                        "kind": "array",
                        "items": {"kind": "any"},
                        "constraints": {"minItems": 1},
                    },
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return await self._client.request(
            method="put",
            path=("v2", "promotions", str(promotion)),
            body=body,
            model=SdkReplacePromotionResponseValue200ApplicationJson,
            request_options=request_options,
        )

    async def update(
        self,
        promotion: int,
        *,
        name: Optional[str] = None,
        status: Optional[
            Union[SdkUpdatePromotionRequestApplicationJsonStatus, str]
        ] = None,
        starts_at: Union[str, None, NotGiven] = NOT_GIVEN,
        ends_at: Union[str, None, NotGiven] = NOT_GIVEN,
        priority: Optional[int] = None,
        is_stackable: Optional[bool] = None,
        max_redemptions: Union[int, None, NotGiven] = NOT_GIVEN,
        phases: Optional[
            List[UpdatePromotionRequestApplicationJsonPropertyPhasesItem]
        ] = None,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkUpdatePromotionResponseValue200ApplicationJson:
        """Update a promotion

        Update promotion fields. Include `phases` to replace the complete ordered phase set; omit it to preserve current phases. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            promotion: The promotion path parameter.
            name: The name.
            status: The status.
            starts_at: The starts at.
            ends_at: A date and time after starts_at and after the current time, or null for no end date.
            priority: The priority.
            is_stackable: The is stackable.
            max_redemptions: The max redemptions.
            phases: The complete ordered phase set. Array order becomes phase position.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkUpdatePromotionResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "updatePromotion",
        }
        body: Dict[str, Any] = {
            k: v
            for k, v in {
                "name": name,
                "status": enum_value(status) if status is not None else None,
                "priority": priority,
                "is_stackable": is_stackable,
                "phases": [item.to_dict() for item in phases]
                if phases is not None
                else None,
            }.items()
            if v is not None
        }
        if not isinstance(starts_at, NotGiven):
            body["starts_at"] = starts_at
        if not isinstance(ends_at, NotGiven):
            body["ends_at"] = ends_at
        if not isinstance(max_redemptions, NotGiven):
            body["max_redemptions"] = max_redemptions
        _validate_model(
            body,
            {
                "name": "SdkUpdatePromotionRequestApplicationJson",
                "required": [],
                "properties": {
                    "name": {
                        "kind": "primitive",
                        "type": "string",
                        "constraints": {"maxLength": 255},
                    },
                    "status": {"kind": "enum", "values": ["active", "disabled"]},
                    "starts_at": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "string",
                            "format": "date-time",
                        },
                    },
                    "ends_at": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "string",
                            "format": "date-time",
                        },
                    },
                    "priority": {
                        "kind": "primitive",
                        "type": "integer",
                        "constraints": {"minimum": 0, "maximum": 2147483647},
                    },
                    "is_stackable": {"kind": "primitive", "type": "boolean"},
                    "max_redemptions": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "integer",
                            "constraints": {"minimum": 1, "maximum": 4294967295},
                        },
                    },
                    "phases": {
                        "kind": "array",
                        "items": {"kind": "any"},
                        "constraints": {"minItems": 1},
                    },
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return await self._client.request(
            method="patch",
            path=("v2", "promotions", str(promotion)),
            body=body,
            model=SdkUpdatePromotionResponseValue200ApplicationJson,
            request_options=request_options,
        )

    async def delete(
        self,
        promotion: int,
        *,
        request_options: Optional[RequestOptions] = None,
    ) -> None:
        """Delete a promotion

        Delete a promotion. Unreferenced phases are purged; phases referenced by order history remain soft-deleted. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            promotion: The promotion path parameter.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "deletePromotion",
        }
        await self._client.request(
            method="delete",
            path=("v2", "promotions", str(promotion)),
            request_options=request_options,
        )

    async def restore(
        self,
        promotion: int,
        *,
        name: str,
        status: Union[SdkRestorePromotionRequestApplicationJsonStatus, str],
        priority: int,
        is_stackable: bool,
        phases: List[RestorePromotionRequestApplicationJsonPropertyPhasesItem],
        starts_at: Union[str, None, NotGiven] = NOT_GIVEN,
        ends_at: Union[str, None, NotGiven] = NOT_GIVEN,
        max_redemptions: Union[int, None, NotGiven] = NOT_GIVEN,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkRestorePromotionResponseValue200ApplicationJson:
        """Restore a promotion

        Restore a soft-deleted promotion with a complete replacement phase set. Historical phases referenced by order history remain soft-deleted and are not reactivated. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            promotion: The promotion path parameter.
            name: The name.
            status: The status.
            starts_at: The starts at.
            ends_at: A date and time after starts_at and after the current time, or null for no end date.
            priority: The priority.
            is_stackable: The is stackable.
            max_redemptions: The max redemptions.
            phases: The complete ordered phase set. Array order becomes phase position.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkRestorePromotionResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "restorePromotion",
        }
        body: Dict[str, Any] = {
            "name": name,
            "status": enum_value(status),
            "priority": priority,
            "is_stackable": is_stackable,
            "phases": [item.to_dict() for item in phases],
        }
        if not isinstance(starts_at, NotGiven):
            body["starts_at"] = starts_at
        if not isinstance(ends_at, NotGiven):
            body["ends_at"] = ends_at
        if not isinstance(max_redemptions, NotGiven):
            body["max_redemptions"] = max_redemptions
        _validate_model(
            body,
            {
                "name": "SdkRestorePromotionRequestApplicationJson",
                "required": ["name", "status", "priority", "is_stackable", "phases"],
                "properties": {
                    "name": {
                        "kind": "primitive",
                        "type": "string",
                        "constraints": {"maxLength": 255},
                    },
                    "status": {"kind": "enum", "values": ["active", "disabled"]},
                    "starts_at": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "string",
                            "format": "date-time",
                        },
                    },
                    "ends_at": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "string",
                            "format": "date-time",
                        },
                    },
                    "priority": {
                        "kind": "primitive",
                        "type": "integer",
                        "constraints": {"minimum": 0, "maximum": 2147483647},
                    },
                    "is_stackable": {"kind": "primitive", "type": "boolean"},
                    "max_redemptions": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "integer",
                            "constraints": {"minimum": 1, "maximum": 4294967295},
                        },
                    },
                    "phases": {
                        "kind": "array",
                        "items": {"kind": "any"},
                        "constraints": {"minItems": 1},
                    },
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return await self._client.request(
            method="post",
            path=("v2", "promotions", str(promotion), "restore"),
            body=body,
            model=SdkRestorePromotionResponseValue200ApplicationJson,
            request_options=request_options,
        )
