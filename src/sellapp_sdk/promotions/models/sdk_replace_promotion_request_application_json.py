# This file is auto-generated by oagen. Do not edit.

from __future__ import annotations

from dataclasses import dataclass
from dataclasses import field as dataclass_field
from datetime import datetime
from enum import Enum
from typing import Any, Dict, List, Optional, cast

from sellapp_sdk._types import (
    _format_datetime,
    _parse_datetime,
    _preserve_unknown_fields,
    _raise_deserialize_error,
    _validate_model,
)
from sellapp_sdk.common.models.sdk_replace_promotion_request_application_json_status import (
    SdkReplacePromotionRequestApplicationJsonStatus,
)

from .replace_promotion_request_application_json_property_phases_item import (
    ReplacePromotionRequestApplicationJsonPropertyPhasesItem,
)

_DOMAIN_RUNTIME_SCHEMA: Dict[str, Any] = {
    "name": "SdkReplacePromotionRequestApplicationJson",
    "required": [],
    "properties": {
        "name": {
            "kind": "primitive",
            "type": "string",
            "constraints": {"maxLength": 255},
        },
        "status": {"kind": "enum", "values": ["active", "disabled"]},
        "starts_at": {
            "kind": "nullable",
            "inner": {"kind": "primitive", "type": "string", "format": "date-time"},
        },
        "ends_at": {
            "kind": "nullable",
            "inner": {"kind": "primitive", "type": "string", "format": "date-time"},
        },
        "priority": {
            "kind": "primitive",
            "type": "integer",
            "constraints": {"minimum": 0, "maximum": 2147483647},
        },
        "is_stackable": {"kind": "primitive", "type": "boolean"},
        "max_redemptions": {
            "kind": "nullable",
            "inner": {
                "kind": "primitive",
                "type": "integer",
                "constraints": {"minimum": 1, "maximum": 4294967295},
            },
        },
        "phases": {
            "kind": "array",
            "items": {"kind": "any"},
            "constraints": {"minItems": 1},
        },
    },
    "additionalProperties": True,
}
_WIRE_RUNTIME_SCHEMA: Dict[str, Any] = {
    "name": "SdkReplacePromotionRequestApplicationJson",
    "required": [],
    "properties": {
        "name": {
            "kind": "primitive",
            "type": "string",
            "constraints": {"maxLength": 255},
        },
        "status": {"kind": "enum", "values": ["active", "disabled"]},
        "starts_at": {
            "kind": "nullable",
            "inner": {"kind": "primitive", "type": "string", "format": "date-time"},
        },
        "ends_at": {
            "kind": "nullable",
            "inner": {"kind": "primitive", "type": "string", "format": "date-time"},
        },
        "priority": {
            "kind": "primitive",
            "type": "integer",
            "constraints": {"minimum": 0, "maximum": 2147483647},
        },
        "is_stackable": {"kind": "primitive", "type": "boolean"},
        "max_redemptions": {
            "kind": "nullable",
            "inner": {
                "kind": "primitive",
                "type": "integer",
                "constraints": {"minimum": 1, "maximum": 4294967295},
            },
        },
        "phases": {
            "kind": "array",
            "items": {"kind": "any"},
            "constraints": {"minItems": 1},
        },
    },
    "additionalProperties": True,
}


@dataclass(slots=True)
class SdkReplacePromotionRequestApplicationJson:
    """Sdk Replace Promotion Request Application Json model."""

    name: Optional[str] = None
    status: Optional[SdkReplacePromotionRequestApplicationJsonStatus] = None
    starts_at: Optional[datetime] = None
    ends_at: Optional[datetime] = None
    """A date and time after starts_at and after the current time, or null for no end date."""
    priority: Optional[int] = None
    is_stackable: Optional[bool] = None
    max_redemptions: Optional[int] = None
    phases: Optional[List[ReplacePromotionRequestApplicationJsonPropertyPhasesItem]] = (
        None
    )
    """The complete ordered phase set. Array order becomes phase position."""
    additional_properties: Dict[str, Any] = dataclass_field(
        default_factory=dict, repr=False
    )
    _from_response: bool = dataclass_field(default=False, repr=False, compare=False)

    @classmethod
    def from_dict(
        cls, data: Dict[str, Any]
    ) -> SdkReplacePromotionRequestApplicationJson:
        """Deserialize from a dictionary."""
        try:
            _validate_model(
                data,
                _WIRE_RUNTIME_SCHEMA,
                direction="response",
                allow_unknown_enums=True,
                allow_unknown_union_variants=True,
                allow_unknown_fields=True,
            )
            return cls(
                name=data.get("name"),
                status=SdkReplacePromotionRequestApplicationJsonStatus(_v_status)
                if (_v_status := data.get("status")) is not None
                else None,
                starts_at=_parse_datetime(_v_starts_at)
                if (_v_starts_at := data.get("starts_at")) is not None
                else None,
                ends_at=_parse_datetime(_v_ends_at)
                if (_v_ends_at := data.get("ends_at")) is not None
                else None,
                priority=data.get("priority"),
                is_stackable=data.get("is_stackable"),
                max_redemptions=data.get("max_redemptions"),
                phases=[
                    ReplacePromotionRequestApplicationJsonPropertyPhasesItem.from_dict(
                        cast(Dict[str, Any], item)
                    )
                    for item in cast(list[Any], _v_phases)
                ]
                if (_v_phases := data.get("phases")) is not None
                else None,
                additional_properties=_preserve_unknown_fields(
                    data,
                    [
                        "name",
                        "status",
                        "starts_at",
                        "ends_at",
                        "priority",
                        "is_stackable",
                        "max_redemptions",
                        "phases",
                    ],
                ),
                _from_response=True,
            )
        except (KeyError, ValueError) as e:
            _raise_deserialize_error("SdkReplacePromotionRequestApplicationJson", e)

    def to_dict(self) -> Dict[str, Any]:
        """Serialize to a dictionary."""
        _domain_data: Dict[str, Any] = dict(self.additional_properties)
        if self.name is not None:
            _domain_data["name"] = self.name
        if self.status is not None:
            _domain_data["status"] = self.status
        _domain_data["starts_at"] = self.starts_at
        _domain_data["ends_at"] = self.ends_at
        if self.priority is not None:
            _domain_data["priority"] = self.priority
        if self.is_stackable is not None:
            _domain_data["is_stackable"] = self.is_stackable
        _domain_data["max_redemptions"] = self.max_redemptions
        if self.phases is not None:
            _domain_data["phases"] = self.phases
        _validate_model(
            _domain_data,
            _DOMAIN_RUNTIME_SCHEMA,
            direction="request",
            allow_unknown_enums=self._from_response,
            allow_unknown_union_variants=self._from_response,
            allow_unknown_fields=(self._from_response or True),
        )
        result: Dict[str, Any] = dict(self.additional_properties)
        if self.name is not None:
            result["name"] = self.name
        if self.status is not None:
            result["status"] = (
                self.status.value if isinstance(self.status, Enum) else self.status
            )
        if self.starts_at is not None:
            result["starts_at"] = _format_datetime(self.starts_at)
        else:
            result["starts_at"] = None
        if self.ends_at is not None:
            result["ends_at"] = _format_datetime(self.ends_at)
        else:
            result["ends_at"] = None
        if self.priority is not None:
            result["priority"] = self.priority
        if self.is_stackable is not None:
            result["is_stackable"] = self.is_stackable
        if self.max_redemptions is not None:
            result["max_redemptions"] = self.max_redemptions
        else:
            result["max_redemptions"] = None
        if self.phases is not None:
            result["phases"] = [item.to_dict() for item in self.phases]
        return result
