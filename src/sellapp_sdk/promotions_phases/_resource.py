# This file is auto-generated by oagen. Do not edit.

from __future__ import annotations

from typing import TYPE_CHECKING, Any, Dict, List, Optional

from .._base_client import WithRawResponse

if TYPE_CHECKING:
    from .._client import AsyncSellAppClient, SellAppClient

from sellapp_sdk.promotions.models.replace_promotion_phases_request_application_json_property_phases_item import (
    ReplacePromotionPhasesRequestApplicationJsonPropertyPhasesItem,
)
from sellapp_sdk.promotions.models.sdk_list_promotion_phases_response_value_200_application_json import (
    SdkListPromotionPhasesResponseValue200ApplicationJson,
)
from sellapp_sdk.promotions.models.sdk_replace_promotion_phases_response_value_200_application_json import (
    SdkReplacePromotionPhasesResponseValue200ApplicationJson,
)

from .._types import RequestOptions, _validate_model


class PromotionsPhases:
    """Promotions Phases API resources."""

    def __init__(self, client: SellAppClient) -> None:
        self._client = client
        self.with_raw_response = WithRawResponse(self)

    def list(
        self,
        promotion: int,
        *,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkListPromotionPhasesResponseValue200ApplicationJson:
        """List promotion phases

        List a promotion's active phases in evaluation order. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            promotion: The promotion path parameter.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkListPromotionPhasesResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "listPromotionPhases",
        }
        return self._client.request(
            method="get",
            path=("v2", "promotions", str(promotion), "phases"),
            model=SdkListPromotionPhasesResponseValue200ApplicationJson,
            request_options=request_options,
        )

    def replace(
        self,
        promotion: int,
        *,
        phases: List[ReplacePromotionPhasesRequestApplicationJsonPropertyPhasesItem],
        request_options: Optional[RequestOptions] = None,
    ) -> SdkReplacePromotionPhasesResponseValue200ApplicationJson:
        """Replace promotion phases

        Replace the complete phase set. Array order becomes phase position; at least one phase is required. Historical referenced phases remain soft-deleted. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            promotion: The promotion path parameter.
            phases: The phases.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkReplacePromotionPhasesResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "replacePromotionPhases",
        }
        body: Dict[str, Any] = {
            "phases": [item.to_dict() for item in phases],
        }
        _validate_model(
            body,
            {
                "name": "SdkReplacePromotionPhasesRequestApplicationJson",
                "required": ["phases"],
                "properties": {
                    "phases": {
                        "kind": "array",
                        "items": {"kind": "any"},
                        "constraints": {"minItems": 1},
                    }
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return self._client.request(
            method="put",
            path=("v2", "promotions", str(promotion), "phases"),
            body=body,
            model=SdkReplacePromotionPhasesResponseValue200ApplicationJson,
            request_options=request_options,
        )


class AsyncPromotionsPhases:
    """Promotions Phases API resources (async)."""

    def __init__(self, client: AsyncSellAppClient) -> None:
        self._client = client
        self.with_raw_response = WithRawResponse(self)

    async def list(
        self,
        promotion: int,
        *,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkListPromotionPhasesResponseValue200ApplicationJson:
        """List promotion phases

        List a promotion's active phases in evaluation order. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            promotion: The promotion path parameter.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkListPromotionPhasesResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "listPromotionPhases",
        }
        return await self._client.request(
            method="get",
            path=("v2", "promotions", str(promotion), "phases"),
            model=SdkListPromotionPhasesResponseValue200ApplicationJson,
            request_options=request_options,
        )

    async def replace(
        self,
        promotion: int,
        *,
        phases: List[ReplacePromotionPhasesRequestApplicationJsonPropertyPhasesItem],
        request_options: Optional[RequestOptions] = None,
    ) -> SdkReplacePromotionPhasesResponseValue200ApplicationJson:
        """Replace promotion phases

        Replace the complete phase set. Array order becomes phase position; at least one phase is required. Historical referenced phases remain soft-deleted. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            promotion: The promotion path parameter.
            phases: The phases.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkReplacePromotionPhasesResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "replacePromotionPhases",
        }
        body: Dict[str, Any] = {
            "phases": [item.to_dict() for item in phases],
        }
        _validate_model(
            body,
            {
                "name": "SdkReplacePromotionPhasesRequestApplicationJson",
                "required": ["phases"],
                "properties": {
                    "phases": {
                        "kind": "array",
                        "items": {"kind": "any"},
                        "constraints": {"minItems": 1},
                    }
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return await self._client.request(
            method="put",
            path=("v2", "promotions", str(promotion), "phases"),
            body=body,
            model=SdkReplacePromotionPhasesResponseValue200ApplicationJson,
            request_options=request_options,
        )
