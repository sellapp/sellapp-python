# This file is auto-generated by oagen. Do not edit.

from __future__ import annotations

from typing import TYPE_CHECKING, Any, Dict, List, Optional, Union

from .._base_client import WithRawResponse

if TYPE_CHECKING:
    from .._client import AsyncSellAppClient, SellAppClient

from sellapp_sdk.common.models.sdk_create_reward_rule_request_application_json_trigger_type import (
    SdkCreateRewardRuleRequestApplicationJsonTriggerType,
)
from sellapp_sdk.common.models.sdk_replace_reward_rule_request_application_json_trigger_type import (
    SdkReplaceRewardRuleRequestApplicationJsonTriggerType,
)
from sellapp_sdk.common.models.sdk_update_reward_rule_request_application_json_trigger_type import (
    SdkUpdateRewardRuleRequestApplicationJsonTriggerType,
)
from sellapp_sdk.common.models.search_reward_rules_request_application_json_property_filters_item import (
    SearchRewardRulesRequestApplicationJsonPropertyFiltersItem,
)
from sellapp_sdk.common.models.search_reward_rules_request_application_json_property_includes_item import (
    SearchRewardRulesRequestApplicationJsonPropertyIncludesItem,
)
from sellapp_sdk.common.models.search_reward_rules_request_application_json_property_search import (
    SearchRewardRulesRequestApplicationJsonPropertySearch,
)
from sellapp_sdk.common.models.search_reward_rules_request_application_json_property_sort_item import (
    SearchRewardRulesRequestApplicationJsonPropertySortItem,
)

from .._pagination import AsyncPage, SyncPage
from .._types import NOT_GIVEN, NotGiven, RequestOptions, _validate_model, enum_value
from .models import (
    CreateRewardRuleRequestApplicationJsonPropertyOutputsItem,
    ReplaceRewardRuleRequestApplicationJsonPropertyOutputsItem,
    SdkCreateRewardRuleResponseValue201ApplicationJson,
    SdkGetRewardRuleResponseValue200ApplicationJson,
    SdkListRewardRulesResponseValue200ApplicationJson,
    SdkReplaceRewardRuleResponseValue200ApplicationJson,
    SdkSearchRewardRulesResponseValue200ApplicationJson,
    SdkUpdateRewardRuleResponseValue200ApplicationJson,
    UpdateRewardRuleRequestApplicationJsonPropertyOutputsItem,
)


class RewardRules:
    """Reward Rules API resources."""

    def __init__(self, client: SellAppClient) -> None:
        self._client = client
        self.with_raw_response = WithRawResponse(self)

    def list(
        self,
        *,
        limit: Optional[int] = None,
        before: Optional[str] = None,
        after: Optional[str] = None,
        order: Optional[str] = None,
        page: Optional[int] = None,
        pagination: Optional[bool] = None,
        request_options: Optional[RequestOptions] = None,
    ) -> SyncPage[SdkListRewardRulesResponseValue200ApplicationJson]:
        """List reward rules

        List reward rules owned by the authenticated store. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            limit: Number of items to return per page. Defaults to `15`.
            before: The before.
            after: The after.
            order: The order.
            page: Page number to return. Defaults to `1`.
            pagination: Set to false to return at most 100 matching items in one data array without pagination links or metadata. Defaults to `true`.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SyncPage[SdkListRewardRulesResponseValue200ApplicationJson]

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "listRewardRules",
        }
        params = {
            k: v
            for k, v in {
                "limit": limit,
                "before": before,
                "after": after,
                "order": order,
                "page": page,
                "pagination": pagination,
            }.items()
            if v is not None
        }
        return self._client.request_page(
            method="get",
            path=("v2", "reward-rules"),
            model=SdkListRewardRulesResponseValue200ApplicationJson,
            params=params,
            request_options=request_options,
        )

    def create(
        self,
        *,
        name: str,
        is_active: bool,
        trigger_type: Union[SdkCreateRewardRuleRequestApplicationJsonTriggerType, str],
        trigger_threshold: int,
        outputs: List[CreateRewardRuleRequestApplicationJsonPropertyOutputsItem],
        description: Union[str, None, NotGiven] = NOT_GIVEN,
        expected_updated_at: Optional[str] = None,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkCreateRewardRuleResponseValue201ApplicationJson:
        """Create a reward rule

        Create reward rule configuration. Grant issuance remains a separate operation. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            name: The name.
            description: The description.
            is_active: The is active.
            trigger_type: The trigger type.
            trigger_threshold: The trigger threshold.
            outputs: The outputs.
            expected_updated_at: The expected updated at.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkCreateRewardRuleResponseValue201ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "createRewardRule",
        }
        body: Dict[str, Any] = {
            k: v
            for k, v in {
                "name": name,
                "is_active": is_active,
                "trigger_type": enum_value(trigger_type),
                "trigger_threshold": trigger_threshold,
                "outputs": [item.to_dict() for item in outputs],
                "expected_updated_at": expected_updated_at,
            }.items()
            if v is not None
        }
        if not isinstance(description, NotGiven):
            body["description"] = description
        _validate_model(
            body,
            {
                "name": "SdkCreateRewardRuleRequestApplicationJson",
                "required": [
                    "name",
                    "is_active",
                    "trigger_type",
                    "trigger_threshold",
                    "outputs",
                ],
                "properties": {
                    "name": {
                        "kind": "primitive",
                        "type": "string",
                        "constraints": {"minLength": 1, "maxLength": 120},
                    },
                    "description": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "string",
                            "constraints": {"maxLength": 255},
                        },
                    },
                    "is_active": {"kind": "primitive", "type": "boolean"},
                    "trigger_type": {
                        "kind": "enum",
                        "values": ["PURCHASE_COUNT", "TOTAL_SPENT"],
                    },
                    "trigger_threshold": {
                        "kind": "primitive",
                        "type": "integer",
                        "constraints": {"minimum": 1},
                    },
                    "outputs": {
                        "kind": "array",
                        "items": {"kind": "any"},
                        "constraints": {"minItems": 1},
                    },
                    "expected_updated_at": {
                        "kind": "primitive",
                        "type": "string",
                        "format": "date-time",
                    },
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return self._client.request(
            method="post",
            path=("v2", "reward-rules"),
            body=body,
            model=SdkCreateRewardRuleResponseValue201ApplicationJson,
            request_options=request_options,
        )

    def search(
        self,
        *,
        filters: Optional[
            List[SearchRewardRulesRequestApplicationJsonPropertyFiltersItem]
        ] = None,
        sort: Optional[
            List[SearchRewardRulesRequestApplicationJsonPropertySortItem]
        ] = None,
        search: Optional[SearchRewardRulesRequestApplicationJsonPropertySearch] = None,
        includes: Optional[
            List[SearchRewardRulesRequestApplicationJsonPropertyIncludesItem]
        ] = None,
        limit: Optional[int] = None,
        before: Optional[str] = None,
        after: Optional[str] = None,
        order: Optional[str] = None,
        page: Optional[int] = None,
        pagination: Optional[bool] = None,
        request_options: Optional[RequestOptions] = None,
    ) -> SyncPage[SdkSearchRewardRulesResponseValue200ApplicationJson]:
        """Search reward rules

        Search and filter reward rules owned by the authenticated store. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            filters: The filters.
            sort: The sort.
            search: The search.
            includes: The includes.
            limit: Number of items to return per page. Defaults to `15`.
            before: The before.
            after: The after.
            order: The order.
            page: Page number to return. Defaults to `1`.
            pagination: Set to false to return at most 100 matching items in one data array without pagination links or metadata. Defaults to `true`.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SyncPage[SdkSearchRewardRulesResponseValue200ApplicationJson]

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "searchRewardRules",
        }
        body: Dict[str, Any] = {
            k: v
            for k, v in {
                "filters": [item.to_dict() for item in filters]
                if filters is not None
                else None,
                "sort": [item.to_dict() for item in sort] if sort is not None else None,
                "search": search.to_dict() if search is not None else None,
                "includes": [item.to_dict() for item in includes]
                if includes is not None
                else None,
            }.items()
            if v is not None
        }
        params = {
            k: v
            for k, v in {
                "limit": limit,
                "before": before,
                "after": after,
                "order": order,
                "page": page,
                "pagination": pagination,
            }.items()
            if v is not None
        }
        return self._client.request_page(
            method="post",
            path=("v2", "reward-rules", "search"),
            model=SdkSearchRewardRulesResponseValue200ApplicationJson,
            body=body,
            params=params,
            request_options=request_options,
        )

    def get(
        self,
        reward_rule: int,
        *,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkGetRewardRuleResponseValue200ApplicationJson:
        """Retrieve a reward rule

        Retrieve one store-owned reward rule. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            reward_rule: The rewardRule path parameter.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkGetRewardRuleResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "getRewardRule",
        }
        return self._client.request(
            method="get",
            path=("v2", "reward-rules", str(reward_rule)),
            model=SdkGetRewardRuleResponseValue200ApplicationJson,
            request_options=request_options,
        )

    def replace(
        self,
        reward_rule: int,
        *,
        name: Optional[str] = None,
        description: Union[str, None, NotGiven] = NOT_GIVEN,
        is_active: Optional[bool] = None,
        trigger_type: Optional[
            Union[SdkReplaceRewardRuleRequestApplicationJsonTriggerType, str]
        ] = None,
        trigger_threshold: Optional[int] = None,
        outputs: Optional[
            List[ReplaceRewardRuleRequestApplicationJsonPropertyOutputsItem]
        ] = None,
        expected_updated_at: Optional[str] = None,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkReplaceRewardRuleResponseValue200ApplicationJson:
        """Update a reward rule

        Update reward rule configuration without directly issuing a customer grant. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            reward_rule: The rewardRule path parameter.
            name: The name.
            description: The description.
            is_active: The is active.
            trigger_type: The trigger type.
            trigger_threshold: The trigger threshold.
            outputs: The outputs.
            expected_updated_at: The expected updated at.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkReplaceRewardRuleResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "replaceRewardRule",
        }
        body: Dict[str, Any] = {
            k: v
            for k, v in {
                "name": name,
                "is_active": is_active,
                "trigger_type": enum_value(trigger_type)
                if trigger_type is not None
                else None,
                "trigger_threshold": trigger_threshold,
                "outputs": [item.to_dict() for item in outputs]
                if outputs is not None
                else None,
                "expected_updated_at": expected_updated_at,
            }.items()
            if v is not None
        }
        if not isinstance(description, NotGiven):
            body["description"] = description
        _validate_model(
            body,
            {
                "name": "SdkReplaceRewardRuleRequestApplicationJson",
                "required": [],
                "properties": {
                    "name": {
                        "kind": "primitive",
                        "type": "string",
                        "constraints": {"minLength": 1, "maxLength": 120},
                    },
                    "description": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "string",
                            "constraints": {"maxLength": 255},
                        },
                    },
                    "is_active": {"kind": "primitive", "type": "boolean"},
                    "trigger_type": {
                        "kind": "enum",
                        "values": ["PURCHASE_COUNT", "TOTAL_SPENT"],
                    },
                    "trigger_threshold": {
                        "kind": "primitive",
                        "type": "integer",
                        "constraints": {"minimum": 1},
                    },
                    "outputs": {
                        "kind": "array",
                        "items": {"kind": "any"},
                        "constraints": {"minItems": 1},
                    },
                    "expected_updated_at": {
                        "kind": "primitive",
                        "type": "string",
                        "format": "date-time",
                    },
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return self._client.request(
            method="put",
            path=("v2", "reward-rules", str(reward_rule)),
            body=body,
            model=SdkReplaceRewardRuleResponseValue200ApplicationJson,
            request_options=request_options,
        )

    def update(
        self,
        reward_rule: int,
        *,
        name: Optional[str] = None,
        description: Union[str, None, NotGiven] = NOT_GIVEN,
        is_active: Optional[bool] = None,
        trigger_type: Optional[
            Union[SdkUpdateRewardRuleRequestApplicationJsonTriggerType, str]
        ] = None,
        trigger_threshold: Optional[int] = None,
        outputs: Optional[
            List[UpdateRewardRuleRequestApplicationJsonPropertyOutputsItem]
        ] = None,
        expected_updated_at: Optional[str] = None,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkUpdateRewardRuleResponseValue200ApplicationJson:
        """Update a reward rule

        Update reward rule configuration without directly issuing a customer grant. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            reward_rule: The rewardRule path parameter.
            name: The name.
            description: The description.
            is_active: The is active.
            trigger_type: The trigger type.
            trigger_threshold: The trigger threshold.
            outputs: The outputs.
            expected_updated_at: The expected updated at.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkUpdateRewardRuleResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "updateRewardRule",
        }
        body: Dict[str, Any] = {
            k: v
            for k, v in {
                "name": name,
                "is_active": is_active,
                "trigger_type": enum_value(trigger_type)
                if trigger_type is not None
                else None,
                "trigger_threshold": trigger_threshold,
                "outputs": [item.to_dict() for item in outputs]
                if outputs is not None
                else None,
                "expected_updated_at": expected_updated_at,
            }.items()
            if v is not None
        }
        if not isinstance(description, NotGiven):
            body["description"] = description
        _validate_model(
            body,
            {
                "name": "SdkUpdateRewardRuleRequestApplicationJson",
                "required": [],
                "properties": {
                    "name": {
                        "kind": "primitive",
                        "type": "string",
                        "constraints": {"minLength": 1, "maxLength": 120},
                    },
                    "description": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "string",
                            "constraints": {"maxLength": 255},
                        },
                    },
                    "is_active": {"kind": "primitive", "type": "boolean"},
                    "trigger_type": {
                        "kind": "enum",
                        "values": ["PURCHASE_COUNT", "TOTAL_SPENT"],
                    },
                    "trigger_threshold": {
                        "kind": "primitive",
                        "type": "integer",
                        "constraints": {"minimum": 1},
                    },
                    "outputs": {
                        "kind": "array",
                        "items": {"kind": "any"},
                        "constraints": {"minItems": 1},
                    },
                    "expected_updated_at": {
                        "kind": "primitive",
                        "type": "string",
                        "format": "date-time",
                    },
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return self._client.request(
            method="patch",
            path=("v2", "reward-rules", str(reward_rule)),
            body=body,
            model=SdkUpdateRewardRuleResponseValue200ApplicationJson,
            request_options=request_options,
        )


class AsyncRewardRules:
    """Reward Rules API resources (async)."""

    def __init__(self, client: AsyncSellAppClient) -> None:
        self._client = client
        self.with_raw_response = WithRawResponse(self)

    async def list(
        self,
        *,
        limit: Optional[int] = None,
        before: Optional[str] = None,
        after: Optional[str] = None,
        order: Optional[str] = None,
        page: Optional[int] = None,
        pagination: Optional[bool] = None,
        request_options: Optional[RequestOptions] = None,
    ) -> AsyncPage[SdkListRewardRulesResponseValue200ApplicationJson]:
        """List reward rules

        List reward rules owned by the authenticated store. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            limit: Number of items to return per page. Defaults to `15`.
            before: The before.
            after: The after.
            order: The order.
            page: Page number to return. Defaults to `1`.
            pagination: Set to false to return at most 100 matching items in one data array without pagination links or metadata. Defaults to `true`.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            AsyncPage[SdkListRewardRulesResponseValue200ApplicationJson]

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "listRewardRules",
        }
        params = {
            k: v
            for k, v in {
                "limit": limit,
                "before": before,
                "after": after,
                "order": order,
                "page": page,
                "pagination": pagination,
            }.items()
            if v is not None
        }
        return await self._client.request_page(
            method="get",
            path=("v2", "reward-rules"),
            model=SdkListRewardRulesResponseValue200ApplicationJson,
            params=params,
            request_options=request_options,
        )

    async def create(
        self,
        *,
        name: str,
        is_active: bool,
        trigger_type: Union[SdkCreateRewardRuleRequestApplicationJsonTriggerType, str],
        trigger_threshold: int,
        outputs: List[CreateRewardRuleRequestApplicationJsonPropertyOutputsItem],
        description: Union[str, None, NotGiven] = NOT_GIVEN,
        expected_updated_at: Optional[str] = None,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkCreateRewardRuleResponseValue201ApplicationJson:
        """Create a reward rule

        Create reward rule configuration. Grant issuance remains a separate operation. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            name: The name.
            description: The description.
            is_active: The is active.
            trigger_type: The trigger type.
            trigger_threshold: The trigger threshold.
            outputs: The outputs.
            expected_updated_at: The expected updated at.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkCreateRewardRuleResponseValue201ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "createRewardRule",
        }
        body: Dict[str, Any] = {
            k: v
            for k, v in {
                "name": name,
                "is_active": is_active,
                "trigger_type": enum_value(trigger_type),
                "trigger_threshold": trigger_threshold,
                "outputs": [item.to_dict() for item in outputs],
                "expected_updated_at": expected_updated_at,
            }.items()
            if v is not None
        }
        if not isinstance(description, NotGiven):
            body["description"] = description
        _validate_model(
            body,
            {
                "name": "SdkCreateRewardRuleRequestApplicationJson",
                "required": [
                    "name",
                    "is_active",
                    "trigger_type",
                    "trigger_threshold",
                    "outputs",
                ],
                "properties": {
                    "name": {
                        "kind": "primitive",
                        "type": "string",
                        "constraints": {"minLength": 1, "maxLength": 120},
                    },
                    "description": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "string",
                            "constraints": {"maxLength": 255},
                        },
                    },
                    "is_active": {"kind": "primitive", "type": "boolean"},
                    "trigger_type": {
                        "kind": "enum",
                        "values": ["PURCHASE_COUNT", "TOTAL_SPENT"],
                    },
                    "trigger_threshold": {
                        "kind": "primitive",
                        "type": "integer",
                        "constraints": {"minimum": 1},
                    },
                    "outputs": {
                        "kind": "array",
                        "items": {"kind": "any"},
                        "constraints": {"minItems": 1},
                    },
                    "expected_updated_at": {
                        "kind": "primitive",
                        "type": "string",
                        "format": "date-time",
                    },
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return await self._client.request(
            method="post",
            path=("v2", "reward-rules"),
            body=body,
            model=SdkCreateRewardRuleResponseValue201ApplicationJson,
            request_options=request_options,
        )

    async def search(
        self,
        *,
        filters: Optional[
            List[SearchRewardRulesRequestApplicationJsonPropertyFiltersItem]
        ] = None,
        sort: Optional[
            List[SearchRewardRulesRequestApplicationJsonPropertySortItem]
        ] = None,
        search: Optional[SearchRewardRulesRequestApplicationJsonPropertySearch] = None,
        includes: Optional[
            List[SearchRewardRulesRequestApplicationJsonPropertyIncludesItem]
        ] = None,
        limit: Optional[int] = None,
        before: Optional[str] = None,
        after: Optional[str] = None,
        order: Optional[str] = None,
        page: Optional[int] = None,
        pagination: Optional[bool] = None,
        request_options: Optional[RequestOptions] = None,
    ) -> AsyncPage[SdkSearchRewardRulesResponseValue200ApplicationJson]:
        """Search reward rules

        Search and filter reward rules owned by the authenticated store. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            filters: The filters.
            sort: The sort.
            search: The search.
            includes: The includes.
            limit: Number of items to return per page. Defaults to `15`.
            before: The before.
            after: The after.
            order: The order.
            page: Page number to return. Defaults to `1`.
            pagination: Set to false to return at most 100 matching items in one data array without pagination links or metadata. Defaults to `true`.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            AsyncPage[SdkSearchRewardRulesResponseValue200ApplicationJson]

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "searchRewardRules",
        }
        body: Dict[str, Any] = {
            k: v
            for k, v in {
                "filters": [item.to_dict() for item in filters]
                if filters is not None
                else None,
                "sort": [item.to_dict() for item in sort] if sort is not None else None,
                "search": search.to_dict() if search is not None else None,
                "includes": [item.to_dict() for item in includes]
                if includes is not None
                else None,
            }.items()
            if v is not None
        }
        params = {
            k: v
            for k, v in {
                "limit": limit,
                "before": before,
                "after": after,
                "order": order,
                "page": page,
                "pagination": pagination,
            }.items()
            if v is not None
        }
        return await self._client.request_page(
            method="post",
            path=("v2", "reward-rules", "search"),
            model=SdkSearchRewardRulesResponseValue200ApplicationJson,
            body=body,
            params=params,
            request_options=request_options,
        )

    async def get(
        self,
        reward_rule: int,
        *,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkGetRewardRuleResponseValue200ApplicationJson:
        """Retrieve a reward rule

        Retrieve one store-owned reward rule. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            reward_rule: The rewardRule path parameter.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkGetRewardRuleResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "getRewardRule",
        }
        return await self._client.request(
            method="get",
            path=("v2", "reward-rules", str(reward_rule)),
            model=SdkGetRewardRuleResponseValue200ApplicationJson,
            request_options=request_options,
        )

    async def replace(
        self,
        reward_rule: int,
        *,
        name: Optional[str] = None,
        description: Union[str, None, NotGiven] = NOT_GIVEN,
        is_active: Optional[bool] = None,
        trigger_type: Optional[
            Union[SdkReplaceRewardRuleRequestApplicationJsonTriggerType, str]
        ] = None,
        trigger_threshold: Optional[int] = None,
        outputs: Optional[
            List[ReplaceRewardRuleRequestApplicationJsonPropertyOutputsItem]
        ] = None,
        expected_updated_at: Optional[str] = None,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkReplaceRewardRuleResponseValue200ApplicationJson:
        """Update a reward rule

        Update reward rule configuration without directly issuing a customer grant. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            reward_rule: The rewardRule path parameter.
            name: The name.
            description: The description.
            is_active: The is active.
            trigger_type: The trigger type.
            trigger_threshold: The trigger threshold.
            outputs: The outputs.
            expected_updated_at: The expected updated at.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkReplaceRewardRuleResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "replaceRewardRule",
        }
        body: Dict[str, Any] = {
            k: v
            for k, v in {
                "name": name,
                "is_active": is_active,
                "trigger_type": enum_value(trigger_type)
                if trigger_type is not None
                else None,
                "trigger_threshold": trigger_threshold,
                "outputs": [item.to_dict() for item in outputs]
                if outputs is not None
                else None,
                "expected_updated_at": expected_updated_at,
            }.items()
            if v is not None
        }
        if not isinstance(description, NotGiven):
            body["description"] = description
        _validate_model(
            body,
            {
                "name": "SdkReplaceRewardRuleRequestApplicationJson",
                "required": [],
                "properties": {
                    "name": {
                        "kind": "primitive",
                        "type": "string",
                        "constraints": {"minLength": 1, "maxLength": 120},
                    },
                    "description": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "string",
                            "constraints": {"maxLength": 255},
                        },
                    },
                    "is_active": {"kind": "primitive", "type": "boolean"},
                    "trigger_type": {
                        "kind": "enum",
                        "values": ["PURCHASE_COUNT", "TOTAL_SPENT"],
                    },
                    "trigger_threshold": {
                        "kind": "primitive",
                        "type": "integer",
                        "constraints": {"minimum": 1},
                    },
                    "outputs": {
                        "kind": "array",
                        "items": {"kind": "any"},
                        "constraints": {"minItems": 1},
                    },
                    "expected_updated_at": {
                        "kind": "primitive",
                        "type": "string",
                        "format": "date-time",
                    },
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return await self._client.request(
            method="put",
            path=("v2", "reward-rules", str(reward_rule)),
            body=body,
            model=SdkReplaceRewardRuleResponseValue200ApplicationJson,
            request_options=request_options,
        )

    async def update(
        self,
        reward_rule: int,
        *,
        name: Optional[str] = None,
        description: Union[str, None, NotGiven] = NOT_GIVEN,
        is_active: Optional[bool] = None,
        trigger_type: Optional[
            Union[SdkUpdateRewardRuleRequestApplicationJsonTriggerType, str]
        ] = None,
        trigger_threshold: Optional[int] = None,
        outputs: Optional[
            List[UpdateRewardRuleRequestApplicationJsonPropertyOutputsItem]
        ] = None,
        expected_updated_at: Optional[str] = None,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkUpdateRewardRuleResponseValue200ApplicationJson:
        """Update a reward rule

        Update reward rule configuration without directly issuing a customer grant. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            reward_rule: The rewardRule path parameter.
            name: The name.
            description: The description.
            is_active: The is active.
            trigger_type: The trigger type.
            trigger_threshold: The trigger threshold.
            outputs: The outputs.
            expected_updated_at: The expected updated at.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkUpdateRewardRuleResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "updateRewardRule",
        }
        body: Dict[str, Any] = {
            k: v
            for k, v in {
                "name": name,
                "is_active": is_active,
                "trigger_type": enum_value(trigger_type)
                if trigger_type is not None
                else None,
                "trigger_threshold": trigger_threshold,
                "outputs": [item.to_dict() for item in outputs]
                if outputs is not None
                else None,
                "expected_updated_at": expected_updated_at,
            }.items()
            if v is not None
        }
        if not isinstance(description, NotGiven):
            body["description"] = description
        _validate_model(
            body,
            {
                "name": "SdkUpdateRewardRuleRequestApplicationJson",
                "required": [],
                "properties": {
                    "name": {
                        "kind": "primitive",
                        "type": "string",
                        "constraints": {"minLength": 1, "maxLength": 120},
                    },
                    "description": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "string",
                            "constraints": {"maxLength": 255},
                        },
                    },
                    "is_active": {"kind": "primitive", "type": "boolean"},
                    "trigger_type": {
                        "kind": "enum",
                        "values": ["PURCHASE_COUNT", "TOTAL_SPENT"],
                    },
                    "trigger_threshold": {
                        "kind": "primitive",
                        "type": "integer",
                        "constraints": {"minimum": 1},
                    },
                    "outputs": {
                        "kind": "array",
                        "items": {"kind": "any"},
                        "constraints": {"minItems": 1},
                    },
                    "expected_updated_at": {
                        "kind": "primitive",
                        "type": "string",
                        "format": "date-time",
                    },
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return await self._client.request(
            method="patch",
            path=("v2", "reward-rules", str(reward_rule)),
            body=body,
            model=SdkUpdateRewardRuleResponseValue200ApplicationJson,
            request_options=request_options,
        )
