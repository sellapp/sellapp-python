# This file is auto-generated by oagen. Do not edit.

from __future__ import annotations

from typing import TYPE_CHECKING, Any, Dict, List, Optional

from .._base_client import WithRawResponse

if TYPE_CHECKING:
    from .._client import AsyncSellAppClient, SellAppClient

from sellapp_sdk.blacklists.models.batch_update_sections_request_application_json_property_resources import (
    BatchUpdateSectionsRequestApplicationJsonPropertyResources,
)
from sellapp_sdk.blacklists.models.sdk_batch_update_sections_response_value_200_application_json import (
    SdkBatchUpdateSectionsResponseValue200ApplicationJson,
)
from sellapp_sdk.blacklists.models.sdk_get_section_response_value_200_application_json import (
    SdkGetSectionResponseValue200ApplicationJson,
)
from sellapp_sdk.blacklists.models.sdk_replace_section_groups_response_value_200_application_json import (
    SdkReplaceSectionGroupsResponseValue200ApplicationJson,
)
from sellapp_sdk.blacklists.models.sdk_replace_section_order_response_value_200_application_json import (
    SdkReplaceSectionOrderResponseValue200ApplicationJson,
)
from sellapp_sdk.blacklists.models.sdk_replace_section_products_response_value_200_application_json import (
    SdkReplaceSectionProductsResponseValue200ApplicationJson,
)
from sellapp_sdk.blacklists.models.sdk_replace_section_response_value_200_application_json import (
    SdkReplaceSectionResponseValue200ApplicationJson,
)
from sellapp_sdk.blacklists.models.sdk_update_section_response_value_200_application_json import (
    SdkUpdateSectionResponseValue200ApplicationJson,
)
from sellapp_sdk.blacklists.models.sdk_v_2_batch_create_sections_response_value_200_application_json import (
    SdkV2BatchCreateSectionsResponseValue200ApplicationJson,
)
from sellapp_sdk.blacklists.models.sdk_v_2_batch_update_sections_response_value_200_application_json import (
    SdkV2BatchUpdateSectionsResponseValue200ApplicationJson,
)
from sellapp_sdk.blacklists.models.sdk_v_2_create_section_response_value_201_application_json import (
    SdkV2CreateSectionResponseValue201ApplicationJson,
)
from sellapp_sdk.blacklists.models.sdk_v_2_get_section_response_value_200_application_json import (
    SdkV2GetSectionResponseValue200ApplicationJson,
)
from sellapp_sdk.blacklists.models.sdk_v_2_replace_section_groups_response_value_200_application_json import (
    SdkV2ReplaceSectionGroupsResponseValue200ApplicationJson,
)
from sellapp_sdk.blacklists.models.sdk_v_2_replace_section_order_response_value_200_application_json import (
    SdkV2ReplaceSectionOrderResponseValue200ApplicationJson,
)
from sellapp_sdk.blacklists.models.sdk_v_2_replace_section_products_response_value_200_application_json import (
    SdkV2ReplaceSectionProductsResponseValue200ApplicationJson,
)
from sellapp_sdk.blacklists.models.sdk_v_2_replace_section_response_value_200_application_json import (
    SdkV2ReplaceSectionResponseValue200ApplicationJson,
)
from sellapp_sdk.blacklists.models.sdk_v_2_update_section_response_value_200_application_json import (
    SdkV2UpdateSectionResponseValue200ApplicationJson,
)
from sellapp_sdk.blacklists.models.search_sections_request_application_json_property_filters_item import (
    SearchSectionsRequestApplicationJsonPropertyFiltersItem,
)
from sellapp_sdk.blacklists.models.search_sections_request_application_json_property_includes_item import (
    SearchSectionsRequestApplicationJsonPropertyIncludesItem,
)
from sellapp_sdk.blacklists.models.search_sections_request_application_json_property_search import (
    SearchSectionsRequestApplicationJsonPropertySearch,
)
from sellapp_sdk.blacklists.models.search_sections_request_application_json_property_sort_item import (
    SearchSectionsRequestApplicationJsonPropertySortItem,
)
from sellapp_sdk.blacklists.models.search_sections_response_value_200_application_json_property_data_item import (
    SearchSectionsResponseValue200ApplicationJsonPropertyDataItem,
)
from sellapp_sdk.blacklists.models.v_2_batch_create_sections_request_application_json_property_resources_item import (
    V2BatchCreateSectionsRequestApplicationJsonPropertyResourcesItem,
)
from sellapp_sdk.blacklists.models.v_2_batch_update_sections_request_application_json_property_resources import (
    V2BatchUpdateSectionsRequestApplicationJsonPropertyResources,
)
from sellapp_sdk.blacklists.models.v_2_list_sections_response_value_200_application_json_property_data_item import (
    V2ListSectionsResponseValue200ApplicationJsonPropertyDataItem,
)
from sellapp_sdk.blacklists.models.v_2_search_sections_request_application_json_property_filters_item import (
    V2SearchSectionsRequestApplicationJsonPropertyFiltersItem,
)
from sellapp_sdk.blacklists.models.v_2_search_sections_request_application_json_property_includes_item import (
    V2SearchSectionsRequestApplicationJsonPropertyIncludesItem,
)
from sellapp_sdk.blacklists.models.v_2_search_sections_request_application_json_property_search import (
    V2SearchSectionsRequestApplicationJsonPropertySearch,
)
from sellapp_sdk.blacklists.models.v_2_search_sections_request_application_json_property_sort_item import (
    V2SearchSectionsRequestApplicationJsonPropertySortItem,
)
from sellapp_sdk.blacklists.models.v_2_search_sections_response_value_200_application_json_property_data_item import (
    V2SearchSectionsResponseValue200ApplicationJsonPropertyDataItem,
)
from sellapp_sdk.common.models.batch_create_sections_request_application_json_property_resources_item import (
    BatchCreateSectionsRequestApplicationJsonPropertyResourcesItem,
)
from sellapp_sdk.common.models.list_sections_response_value_200_application_json_property_data_item import (
    ListSectionsResponseValue200ApplicationJsonPropertyDataItem,
)
from sellapp_sdk.common.models.sdk_batch_create_sections_response_value_200_application_json import (
    SdkBatchCreateSectionsResponseValue200ApplicationJson,
)
from sellapp_sdk.common.models.sdk_create_section_response_value_201_application_json import (
    SdkCreateSectionResponseValue201ApplicationJson,
)

from .._pagination import AsyncPage, SyncPage
from .._types import RequestOptions, _validate_model


class Sections:
    """Sections API resources."""

    def __init__(self, client: SellAppClient) -> None:
        self._client = client
        self.with_raw_response = WithRawResponse(self)

    def list(
        self,
        *,
        limit: Optional[int] = None,
        before: Optional[str] = None,
        after: Optional[str] = None,
        order: Optional[str] = None,
        page: Optional[int] = None,
        request_options: Optional[RequestOptions] = None,
    ) -> SyncPage[ListSectionsResponseValue200ApplicationJsonPropertyDataItem]:
        """List all sections

        List your storefront sections, 15 per page by default.

        Args:
            limit: Number of items to return per page. Defaults to `15`.
            before: The before.
            after: The after.
            order: The order.
            page: Page number to return. Defaults to `1`.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SyncPage[ListSectionsResponseValue200ApplicationJsonPropertyDataItem]

        Raises:
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [
                        {"schemeName": "bearerAuth", "scopes": []},
                        {"schemeName": "storeAuth", "scopes": []},
                    ]
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": False,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": True,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": False,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "listSections",
        }
        params = {
            k: v
            for k, v in {
                "limit": limit,
                "before": before,
                "after": after,
                "order": order,
                "page": page,
            }.items()
            if v is not None
        }
        return self._client.request_page(
            method="get",
            path=("v1", "sections"),
            model=ListSectionsResponseValue200ApplicationJsonPropertyDataItem,
            params=params,
            request_options=request_options,
        )

    def create(
        self,
        *,
        title: str,
        hidden: bool,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkCreateSectionResponseValue201ApplicationJson:
        """Create a section

        Create a section to organize your storefront.

        Args:
            title: The title.
            hidden: The hidden.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkCreateSectionResponseValue201ApplicationJson

        Raises:
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [
                        {"schemeName": "bearerAuth", "scopes": []},
                        {"schemeName": "storeAuth", "scopes": []},
                    ]
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": False,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": True,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": False,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "createSection",
        }
        body: Dict[str, Any] = {
            "title": title,
            "hidden": hidden,
        }
        _validate_model(
            body,
            {
                "name": "SdkCreateSectionRequestApplicationJson",
                "required": ["title", "hidden"],
                "properties": {
                    "title": {
                        "kind": "primitive",
                        "type": "string",
                        "constraints": {"maxLength": 255},
                    },
                    "hidden": {"kind": "primitive", "type": "boolean"},
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return self._client.request(
            method="post",
            path=("v1", "sections"),
            body=body,
            model=SdkCreateSectionResponseValue201ApplicationJson,
            request_options=request_options,
        )

    def get(
        self,
        section: int,
        *,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkGetSectionResponseValue200ApplicationJson:
        """Retrieve a section

        Retrieve a section by its ID.

        Args:
            section: The section path parameter.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkGetSectionResponseValue200ApplicationJson

        Raises:
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [
                        {"schemeName": "bearerAuth", "scopes": []},
                        {"schemeName": "storeAuth", "scopes": []},
                    ]
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": False,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": True,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": False,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "getSection",
        }
        return self._client.request(
            method="get",
            path=("v1", "sections", str(section)),
            model=SdkGetSectionResponseValue200ApplicationJson,
            request_options=request_options,
        )

    def replace(
        self,
        section: int,
        *,
        title: Optional[str] = None,
        hidden: Optional[bool] = None,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkReplaceSectionResponseValue200ApplicationJson:
        """Update a section

        Update a storefront section's details.

        Args:
            section: The section path parameter.
            title: The title.
            hidden: The hidden.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkReplaceSectionResponseValue200ApplicationJson

        Raises:
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [
                        {"schemeName": "bearerAuth", "scopes": []},
                        {"schemeName": "storeAuth", "scopes": []},
                    ]
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": False,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": True,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": False,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "replaceSection",
        }
        body: Dict[str, Any] = {
            k: v
            for k, v in {
                "title": title,
                "hidden": hidden,
            }.items()
            if v is not None
        }
        _validate_model(
            body,
            {
                "name": "SdkReplaceSectionRequestApplicationJson",
                "required": [],
                "properties": {
                    "title": {
                        "kind": "primitive",
                        "type": "string",
                        "constraints": {"maxLength": 255},
                    },
                    "hidden": {"kind": "primitive", "type": "boolean"},
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return self._client.request(
            method="put",
            path=("v1", "sections", str(section)),
            body=body,
            model=SdkReplaceSectionResponseValue200ApplicationJson,
            request_options=request_options,
        )

    def update(
        self,
        section: int,
        *,
        title: Optional[str] = None,
        hidden: Optional[bool] = None,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkUpdateSectionResponseValue200ApplicationJson:
        """Update a section

        Update a storefront section's details.

        Args:
            section: The section path parameter.
            title: The title.
            hidden: The hidden.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkUpdateSectionResponseValue200ApplicationJson

        Raises:
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [
                        {"schemeName": "bearerAuth", "scopes": []},
                        {"schemeName": "storeAuth", "scopes": []},
                    ]
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": False,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": True,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": False,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "updateSection",
        }
        body: Dict[str, Any] = {
            k: v
            for k, v in {
                "title": title,
                "hidden": hidden,
            }.items()
            if v is not None
        }
        _validate_model(
            body,
            {
                "name": "SdkUpdateSectionRequestApplicationJson",
                "required": [],
                "properties": {
                    "title": {
                        "kind": "primitive",
                        "type": "string",
                        "constraints": {"maxLength": 255},
                    },
                    "hidden": {"kind": "primitive", "type": "boolean"},
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return self._client.request(
            method="patch",
            path=("v1", "sections", str(section)),
            body=body,
            model=SdkUpdateSectionResponseValue200ApplicationJson,
            request_options=request_options,
        )

    def delete(
        self,
        section: int,
        *,
        request_options: Optional[RequestOptions] = None,
    ) -> None:
        """Delete a section

        Deletes a section. This will permanently delete the section and its details.

        Args:
            section: The section path parameter.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Raises:
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [
                        {"schemeName": "bearerAuth", "scopes": []},
                        {"schemeName": "storeAuth", "scopes": []},
                    ]
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": False,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": True,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": False,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "deleteSection",
        }
        self._client.request(
            method="delete",
            path=("v1", "sections", str(section)),
            request_options=request_options,
        )

    def replace_order(
        self,
        *,
        resources: List[int],
        request_options: Optional[RequestOptions] = None,
    ) -> SdkReplaceSectionOrderResponseValue200ApplicationJson:
        """Replace section order

        Replace the storefront section order with every section ID in the store. IDs are applied in array order; incomplete lists and unknown IDs are rejected.

        Args:
            resources: The resources.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkReplaceSectionOrderResponseValue200ApplicationJson

        Raises:
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [
                        {"schemeName": "bearerAuth", "scopes": []},
                        {"schemeName": "storeAuth", "scopes": []},
                    ]
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": False,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": True,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": False,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "replaceSectionOrder",
        }
        body: Dict[str, Any] = {
            "resources": resources,
        }
        _validate_model(
            body,
            {
                "name": "SdkReplaceSectionOrderRequestApplicationJson",
                "required": ["resources"],
                "properties": {
                    "resources": {
                        "kind": "array",
                        "items": {"kind": "primitive", "type": "integer"},
                        "constraints": {"uniqueItems": True},
                    }
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return self._client.request(
            method="put",
            path=("v1", "sections", "order"),
            body=body,
            model=SdkReplaceSectionOrderResponseValue200ApplicationJson,
            request_options=request_options,
        )

    def replace_products(
        self,
        section: int,
        *,
        resources: List[int],
        request_options: Optional[RequestOptions] = None,
    ) -> SdkReplaceSectionProductsResponseValue200ApplicationJson:
        """Replace section products

        Replace the products assigned to a section. Product IDs are stored in the supplied order; omitted products are detached and unknown IDs are rejected.

        Args:
            section: The section path parameter.
            resources: The resources.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkReplaceSectionProductsResponseValue200ApplicationJson

        Raises:
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [
                        {"schemeName": "bearerAuth", "scopes": []},
                        {"schemeName": "storeAuth", "scopes": []},
                    ]
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": False,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": True,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": False,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "replaceSectionProducts",
        }
        body: Dict[str, Any] = {
            "resources": resources,
        }
        _validate_model(
            body,
            {
                "name": "SdkReplaceSectionOrderRequestApplicationJson",
                "required": ["resources"],
                "properties": {
                    "resources": {
                        "kind": "array",
                        "items": {"kind": "primitive", "type": "integer"},
                        "constraints": {"uniqueItems": True},
                    }
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return self._client.request(
            method="put",
            path=("v1", "sections", str(section), "products"),
            body=body,
            model=SdkReplaceSectionProductsResponseValue200ApplicationJson,
            request_options=request_options,
        )

    def replace_groups(
        self,
        section: int,
        *,
        resources: List[int],
        request_options: Optional[RequestOptions] = None,
    ) -> SdkReplaceSectionGroupsResponseValue200ApplicationJson:
        """Replace section groups

        Replace the groups assigned to a section. Group IDs are stored in the supplied order; omitted groups are detached and unknown IDs are rejected.

        Args:
            section: The section path parameter.
            resources: The resources.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkReplaceSectionGroupsResponseValue200ApplicationJson

        Raises:
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [
                        {"schemeName": "bearerAuth", "scopes": []},
                        {"schemeName": "storeAuth", "scopes": []},
                    ]
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": False,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": True,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": False,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "replaceSectionGroups",
        }
        body: Dict[str, Any] = {
            "resources": resources,
        }
        _validate_model(
            body,
            {
                "name": "SdkReplaceSectionOrderRequestApplicationJson",
                "required": ["resources"],
                "properties": {
                    "resources": {
                        "kind": "array",
                        "items": {"kind": "primitive", "type": "integer"},
                        "constraints": {"uniqueItems": True},
                    }
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return self._client.request(
            method="put",
            path=("v1", "sections", str(section), "groups"),
            body=body,
            model=SdkReplaceSectionGroupsResponseValue200ApplicationJson,
            request_options=request_options,
        )

    def search(
        self,
        *,
        filters: Optional[
            List[SearchSectionsRequestApplicationJsonPropertyFiltersItem]
        ] = None,
        sort: Optional[
            List[SearchSectionsRequestApplicationJsonPropertySortItem]
        ] = None,
        search: Optional[SearchSectionsRequestApplicationJsonPropertySearch] = None,
        includes: Optional[
            List[SearchSectionsRequestApplicationJsonPropertyIncludesItem]
        ] = None,
        limit: Optional[int] = None,
        before: Optional[str] = None,
        after: Optional[str] = None,
        order: Optional[str] = None,
        request_options: Optional[RequestOptions] = None,
    ) -> SyncPage[SearchSectionsResponseValue200ApplicationJsonPropertyDataItem]:
        """Search sections

        Search sections using JSON body filters, search terms, includes, and sort instructions.

        Args:
            filters: The filters.
            sort: The sort.
            search: The search.
            includes: The includes.
            limit: The limit.
            before: The before.
            after: The after.
            order: The order.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SyncPage[SearchSectionsResponseValue200ApplicationJsonPropertyDataItem]

        Raises:
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [
                        {"schemeName": "bearerAuth", "scopes": []},
                        {"schemeName": "storeAuth", "scopes": []},
                    ]
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": False,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": True,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": False,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "searchSections",
        }
        body: Dict[str, Any] = {
            k: v
            for k, v in {
                "filters": [item.to_dict() for item in filters]
                if filters is not None
                else None,
                "sort": [item.to_dict() for item in sort] if sort is not None else None,
                "search": search.to_dict() if search is not None else None,
                "includes": [item.to_dict() for item in includes]
                if includes is not None
                else None,
            }.items()
            if v is not None
        }
        params = {
            k: v
            for k, v in {
                "limit": limit,
                "before": before,
                "after": after,
                "order": order,
            }.items()
            if v is not None
        }
        return self._client.request_page(
            method="post",
            path=("v1", "sections", "search"),
            model=SearchSectionsResponseValue200ApplicationJsonPropertyDataItem,
            body=body,
            params=params,
            request_options=request_options,
        )

    def batch_create(
        self,
        *,
        resources: List[BatchCreateSectionsRequestApplicationJsonPropertyResourcesItem],
        request_options: Optional[RequestOptions] = None,
    ) -> SdkBatchCreateSectionsResponseValue200ApplicationJson:
        """Batch create sections

        Create multiple sections in one request by sending a `resources` array.

        Args:
            resources: The resources.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkBatchCreateSectionsResponseValue200ApplicationJson

        Raises:
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [
                        {"schemeName": "bearerAuth", "scopes": []},
                        {"schemeName": "storeAuth", "scopes": []},
                    ]
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": False,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": True,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": False,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "batchCreateSections",
        }
        body: Dict[str, Any] = {
            "resources": [item.to_dict() for item in resources],
        }
        _validate_model(
            body,
            {
                "name": "SdkBatchCreateSectionsRequestApplicationJson",
                "required": ["resources"],
                "properties": {
                    "resources": {"kind": "array", "items": {"kind": "any"}}
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return self._client.request(
            method="post",
            path=("v1", "sections", "batch"),
            body=body,
            model=SdkBatchCreateSectionsResponseValue200ApplicationJson,
            request_options=request_options,
        )

    def batch_update(
        self,
        *,
        resources: BatchUpdateSectionsRequestApplicationJsonPropertyResources,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkBatchUpdateSectionsResponseValue200ApplicationJson:
        """Batch update sections

        Update multiple sections in one request by sending a `resources` object keyed by section ID.

        Args:
            resources: The resources.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkBatchUpdateSectionsResponseValue200ApplicationJson

        Raises:
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [
                        {"schemeName": "bearerAuth", "scopes": []},
                        {"schemeName": "storeAuth", "scopes": []},
                    ]
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": False,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": True,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": False,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "batchUpdateSections",
        }
        body: Dict[str, Any] = {
            "resources": resources.to_dict(),
        }
        _validate_model(
            body,
            {
                "name": "SdkBatchUpdateSectionsRequestApplicationJson",
                "required": ["resources"],
                "properties": {"resources": {"kind": "any"}},
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return self._client.request(
            method="patch",
            path=("v1", "sections", "batch"),
            body=body,
            model=SdkBatchUpdateSectionsResponseValue200ApplicationJson,
            request_options=request_options,
        )

    def batch_delete(
        self,
        *,
        resources: List[int],
        request_options: Optional[RequestOptions] = None,
    ) -> None:
        """Batch delete sections

        Delete multiple sections in one request by sending the section IDs in `resources`.

        Args:
            resources: The resources.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Raises:
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [
                        {"schemeName": "bearerAuth", "scopes": []},
                        {"schemeName": "storeAuth", "scopes": []},
                    ]
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": False,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": True,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": False,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "batchDeleteSections",
        }
        body: Dict[str, Any] = {
            "resources": resources,
        }
        _validate_model(
            body,
            {
                "name": "SdkBatchDeleteCouponsRequestApplicationJson",
                "required": ["resources"],
                "properties": {
                    "resources": {
                        "kind": "array",
                        "items": {"kind": "primitive", "type": "integer"},
                    }
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        self._client.request(
            method="delete",
            path=("v1", "sections", "batch"),
            body=body,
            request_options=request_options,
        )

    def v2_list_sections(
        self,
        *,
        limit: Optional[int] = None,
        before: Optional[str] = None,
        after: Optional[str] = None,
        order: Optional[str] = None,
        page: Optional[int] = None,
        request_options: Optional[RequestOptions] = None,
    ) -> SyncPage[V2ListSectionsResponseValue200ApplicationJsonPropertyDataItem]:
        """List all sections

        List your storefront sections, 15 per page by default. This v2 route is store-scoped. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            limit: Number of items to return per page. Defaults to `15`.
            before: The before.
            after: The after.
            order: The order.
            page: Page number to return. Defaults to `1`.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SyncPage[V2ListSectionsResponseValue200ApplicationJsonPropertyDataItem]

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "v2ListSections",
        }
        params = {
            k: v
            for k, v in {
                "limit": limit,
                "before": before,
                "after": after,
                "order": order,
                "page": page,
            }.items()
            if v is not None
        }
        return self._client.request_page(
            method="get",
            path=("v2", "sections"),
            model=V2ListSectionsResponseValue200ApplicationJsonPropertyDataItem,
            params=params,
            request_options=request_options,
        )

    def v2_create_section(
        self,
        *,
        title: str,
        hidden: bool,
        idempotency_key: Optional[str] = None,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkV2CreateSectionResponseValue201ApplicationJson:
        """Create a section

        Create a section to organize your storefront. This v2 route is store-scoped. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            title: The title.
            hidden: The hidden.
            idempotency_key: Optional idempotency key for safe retries.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkV2CreateSectionResponseValue201ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": True,
            "operation_id": "v2CreateSection",
        }
        body: Dict[str, Any] = {
            "title": title,
            "hidden": hidden,
        }
        _validate_model(
            body,
            {
                "name": "SdkV2CreateSectionRequestApplicationJson",
                "required": ["title", "hidden"],
                "properties": {
                    "title": {
                        "kind": "primitive",
                        "type": "string",
                        "constraints": {"maxLength": 255},
                    },
                    "hidden": {"kind": "primitive", "type": "boolean"},
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return self._client.request(
            method="post",
            path=("v2", "sections"),
            body=body,
            model=SdkV2CreateSectionResponseValue201ApplicationJson,
            idempotency_key=idempotency_key,
            request_options=request_options,
        )

    def v2_search_sections(
        self,
        *,
        filters: Optional[
            List[V2SearchSectionsRequestApplicationJsonPropertyFiltersItem]
        ] = None,
        sort: Optional[
            List[V2SearchSectionsRequestApplicationJsonPropertySortItem]
        ] = None,
        search: Optional[V2SearchSectionsRequestApplicationJsonPropertySearch] = None,
        includes: Optional[
            List[V2SearchSectionsRequestApplicationJsonPropertyIncludesItem]
        ] = None,
        limit: Optional[int] = None,
        before: Optional[str] = None,
        after: Optional[str] = None,
        order: Optional[str] = None,
        idempotency_key: Optional[str] = None,
        request_options: Optional[RequestOptions] = None,
    ) -> SyncPage[V2SearchSectionsResponseValue200ApplicationJsonPropertyDataItem]:
        """Search sections

        Search sections using JSON body filters, search terms, includes, and sort instructions. This v2 route is store-scoped. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            filters: The filters.
            sort: The sort.
            search: The search.
            includes: The includes.
            limit: The limit.
            before: The before.
            after: The after.
            order: The order.
            idempotency_key: Optional idempotency key for safe retries.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SyncPage[V2SearchSectionsResponseValue200ApplicationJsonPropertyDataItem]

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": True,
            "operation_id": "v2SearchSections",
        }
        body: Dict[str, Any] = {
            k: v
            for k, v in {
                "filters": [item.to_dict() for item in filters]
                if filters is not None
                else None,
                "sort": [item.to_dict() for item in sort] if sort is not None else None,
                "search": search.to_dict() if search is not None else None,
                "includes": [item.to_dict() for item in includes]
                if includes is not None
                else None,
            }.items()
            if v is not None
        }
        params = {
            k: v
            for k, v in {
                "limit": limit,
                "before": before,
                "after": after,
                "order": order,
            }.items()
            if v is not None
        }
        return self._client.request_page(
            method="post",
            path=("v2", "sections", "search"),
            model=V2SearchSectionsResponseValue200ApplicationJsonPropertyDataItem,
            body=body,
            params=params,
            request_options=request_options,
        )

    def v2_get_section(
        self,
        section: int,
        *,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkV2GetSectionResponseValue200ApplicationJson:
        """Retrieve a section

        Retrieve a section by its ID. This v2 route is store-scoped. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            section: The section path parameter.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkV2GetSectionResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "v2GetSection",
        }
        return self._client.request(
            method="get",
            path=("v2", "sections", str(section)),
            model=SdkV2GetSectionResponseValue200ApplicationJson,
            request_options=request_options,
        )

    def v2_replace_section(
        self,
        section: int,
        *,
        title: Optional[str] = None,
        hidden: Optional[bool] = None,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkV2ReplaceSectionResponseValue200ApplicationJson:
        """Update a section

        Update a storefront section's details. This v2 route is store-scoped. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            section: The section path parameter.
            title: The title.
            hidden: The hidden.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkV2ReplaceSectionResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "v2ReplaceSection",
        }
        body: Dict[str, Any] = {
            k: v
            for k, v in {
                "title": title,
                "hidden": hidden,
            }.items()
            if v is not None
        }
        _validate_model(
            body,
            {
                "name": "SdkV2ReplaceSectionRequestApplicationJson",
                "required": [],
                "properties": {
                    "title": {
                        "kind": "primitive",
                        "type": "string",
                        "constraints": {"maxLength": 255},
                    },
                    "hidden": {"kind": "primitive", "type": "boolean"},
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return self._client.request(
            method="put",
            path=("v2", "sections", str(section)),
            body=body,
            model=SdkV2ReplaceSectionResponseValue200ApplicationJson,
            request_options=request_options,
        )

    def v2_update_section(
        self,
        section: int,
        *,
        title: Optional[str] = None,
        hidden: Optional[bool] = None,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkV2UpdateSectionResponseValue200ApplicationJson:
        """Update a section

        Update a storefront section's details. This v2 route is store-scoped. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            section: The section path parameter.
            title: The title.
            hidden: The hidden.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkV2UpdateSectionResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "v2UpdateSection",
        }
        body: Dict[str, Any] = {
            k: v
            for k, v in {
                "title": title,
                "hidden": hidden,
            }.items()
            if v is not None
        }
        _validate_model(
            body,
            {
                "name": "SdkV2UpdateSectionRequestApplicationJson",
                "required": [],
                "properties": {
                    "title": {
                        "kind": "primitive",
                        "type": "string",
                        "constraints": {"maxLength": 255},
                    },
                    "hidden": {"kind": "primitive", "type": "boolean"},
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return self._client.request(
            method="patch",
            path=("v2", "sections", str(section)),
            body=body,
            model=SdkV2UpdateSectionResponseValue200ApplicationJson,
            request_options=request_options,
        )

    def v2_delete_section(
        self,
        section: int,
        *,
        request_options: Optional[RequestOptions] = None,
    ) -> None:
        """Delete a section

        Deletes a section. This will permanently delete the section and its details. This v2 route is store-scoped. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            section: The section path parameter.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "v2DeleteSection",
        }
        self._client.request(
            method="delete",
            path=("v2", "sections", str(section)),
            request_options=request_options,
        )

    def v2_batch_create_sections(
        self,
        *,
        resources: List[
            V2BatchCreateSectionsRequestApplicationJsonPropertyResourcesItem
        ],
        idempotency_key: Optional[str] = None,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkV2BatchCreateSectionsResponseValue200ApplicationJson:
        """Batch create sections

        Create multiple sections in one request by sending a `resources` array. This v2 route is store-scoped. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            resources: The resources.
            idempotency_key: Optional idempotency key for safe retries.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkV2BatchCreateSectionsResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": True,
            "operation_id": "v2BatchCreateSections",
        }
        body: Dict[str, Any] = {
            "resources": [item.to_dict() for item in resources],
        }
        _validate_model(
            body,
            {
                "name": "SdkV2BatchCreateSectionsRequestApplicationJson",
                "required": ["resources"],
                "properties": {
                    "resources": {"kind": "array", "items": {"kind": "any"}}
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return self._client.request(
            method="post",
            path=("v2", "sections", "batch"),
            body=body,
            model=SdkV2BatchCreateSectionsResponseValue200ApplicationJson,
            idempotency_key=idempotency_key,
            request_options=request_options,
        )

    def v2_batch_update_sections(
        self,
        *,
        resources: V2BatchUpdateSectionsRequestApplicationJsonPropertyResources,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkV2BatchUpdateSectionsResponseValue200ApplicationJson:
        """Batch update sections

        Update multiple sections in one request by sending a `resources` object keyed by section ID. This v2 route is store-scoped. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            resources: The resources.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkV2BatchUpdateSectionsResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": True,
            "operation_id": "v2BatchUpdateSections",
        }
        body: Dict[str, Any] = {
            "resources": resources.to_dict(),
        }
        _validate_model(
            body,
            {
                "name": "SdkV2BatchUpdateSectionsRequestApplicationJson",
                "required": ["resources"],
                "properties": {"resources": {"kind": "any"}},
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return self._client.request(
            method="patch",
            path=("v2", "sections", "batch"),
            body=body,
            model=SdkV2BatchUpdateSectionsResponseValue200ApplicationJson,
            request_options=request_options,
        )

    def v2_batch_delete_sections(
        self,
        *,
        resources: List[int],
        request_options: Optional[RequestOptions] = None,
    ) -> None:
        """Batch delete sections

        Delete multiple sections in one request by sending the section IDs in `resources`. This v2 route is store-scoped. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            resources: The resources.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "v2BatchDeleteSections",
        }
        body: Dict[str, Any] = {
            "resources": resources,
        }
        _validate_model(
            body,
            {
                "name": "SdkV2BatchDeleteSectionsRequestApplicationJson",
                "required": ["resources"],
                "properties": {
                    "resources": {
                        "kind": "array",
                        "items": {"kind": "primitive", "type": "integer"},
                    }
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        self._client.request(
            method="delete",
            path=("v2", "sections", "batch"),
            body=body,
            request_options=request_options,
        )

    def v2_replace_section_order(
        self,
        *,
        resources: List[int],
        request_options: Optional[RequestOptions] = None,
    ) -> SdkV2ReplaceSectionOrderResponseValue200ApplicationJson:
        """Replace section order

        Replace the storefront section order with every section ID in the store. IDs are applied in array order; incomplete lists and unknown IDs are rejected. This v2 route is store-scoped. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            resources: The resources.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkV2ReplaceSectionOrderResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "v2ReplaceSectionOrder",
        }
        body: Dict[str, Any] = {
            "resources": resources,
        }
        _validate_model(
            body,
            {
                "name": "SdkV2ReplaceSectionOrderRequestApplicationJson",
                "required": ["resources"],
                "properties": {
                    "resources": {
                        "kind": "array",
                        "items": {"kind": "primitive", "type": "integer"},
                        "constraints": {"uniqueItems": True},
                    }
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return self._client.request(
            method="put",
            path=("v2", "sections", "order"),
            body=body,
            model=SdkV2ReplaceSectionOrderResponseValue200ApplicationJson,
            request_options=request_options,
        )

    def v2_replace_section_products(
        self,
        section: int,
        *,
        resources: List[int],
        request_options: Optional[RequestOptions] = None,
    ) -> SdkV2ReplaceSectionProductsResponseValue200ApplicationJson:
        """Replace section products

        Replace the products assigned to a section. Product IDs are stored in the supplied order; omitted products are detached and unknown IDs are rejected. This v2 route is store-scoped. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            section: The section path parameter.
            resources: The resources.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkV2ReplaceSectionProductsResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "v2ReplaceSectionProducts",
        }
        body: Dict[str, Any] = {
            "resources": resources,
        }
        _validate_model(
            body,
            {
                "name": "SdkV2ReplaceSectionProductsRequestApplicationJson",
                "required": ["resources"],
                "properties": {
                    "resources": {
                        "kind": "array",
                        "items": {"kind": "primitive", "type": "integer"},
                        "constraints": {"uniqueItems": True},
                    }
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return self._client.request(
            method="put",
            path=("v2", "sections", str(section), "products"),
            body=body,
            model=SdkV2ReplaceSectionProductsResponseValue200ApplicationJson,
            request_options=request_options,
        )

    def v2_replace_section_groups(
        self,
        section: int,
        *,
        resources: List[int],
        request_options: Optional[RequestOptions] = None,
    ) -> SdkV2ReplaceSectionGroupsResponseValue200ApplicationJson:
        """Replace section groups

        Replace the groups assigned to a section. Group IDs are stored in the supplied order; omitted groups are detached and unknown IDs are rejected. This v2 route is store-scoped. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            section: The section path parameter.
            resources: The resources.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkV2ReplaceSectionGroupsResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "v2ReplaceSectionGroups",
        }
        body: Dict[str, Any] = {
            "resources": resources,
        }
        _validate_model(
            body,
            {
                "name": "SdkV2ReplaceSectionGroupsRequestApplicationJson",
                "required": ["resources"],
                "properties": {
                    "resources": {
                        "kind": "array",
                        "items": {"kind": "primitive", "type": "integer"},
                        "constraints": {"uniqueItems": True},
                    }
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return self._client.request(
            method="put",
            path=("v2", "sections", str(section), "groups"),
            body=body,
            model=SdkV2ReplaceSectionGroupsResponseValue200ApplicationJson,
            request_options=request_options,
        )


class AsyncSections:
    """Sections API resources (async)."""

    def __init__(self, client: AsyncSellAppClient) -> None:
        self._client = client
        self.with_raw_response = WithRawResponse(self)

    async def list(
        self,
        *,
        limit: Optional[int] = None,
        before: Optional[str] = None,
        after: Optional[str] = None,
        order: Optional[str] = None,
        page: Optional[int] = None,
        request_options: Optional[RequestOptions] = None,
    ) -> AsyncPage[ListSectionsResponseValue200ApplicationJsonPropertyDataItem]:
        """List all sections

        List your storefront sections, 15 per page by default.

        Args:
            limit: Number of items to return per page. Defaults to `15`.
            before: The before.
            after: The after.
            order: The order.
            page: Page number to return. Defaults to `1`.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            AsyncPage[ListSectionsResponseValue200ApplicationJsonPropertyDataItem]

        Raises:
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [
                        {"schemeName": "bearerAuth", "scopes": []},
                        {"schemeName": "storeAuth", "scopes": []},
                    ]
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": False,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": True,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": False,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "listSections",
        }
        params = {
            k: v
            for k, v in {
                "limit": limit,
                "before": before,
                "after": after,
                "order": order,
                "page": page,
            }.items()
            if v is not None
        }
        return await self._client.request_page(
            method="get",
            path=("v1", "sections"),
            model=ListSectionsResponseValue200ApplicationJsonPropertyDataItem,
            params=params,
            request_options=request_options,
        )

    async def create(
        self,
        *,
        title: str,
        hidden: bool,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkCreateSectionResponseValue201ApplicationJson:
        """Create a section

        Create a section to organize your storefront.

        Args:
            title: The title.
            hidden: The hidden.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkCreateSectionResponseValue201ApplicationJson

        Raises:
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [
                        {"schemeName": "bearerAuth", "scopes": []},
                        {"schemeName": "storeAuth", "scopes": []},
                    ]
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": False,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": True,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": False,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "createSection",
        }
        body: Dict[str, Any] = {
            "title": title,
            "hidden": hidden,
        }
        _validate_model(
            body,
            {
                "name": "SdkCreateSectionRequestApplicationJson",
                "required": ["title", "hidden"],
                "properties": {
                    "title": {
                        "kind": "primitive",
                        "type": "string",
                        "constraints": {"maxLength": 255},
                    },
                    "hidden": {"kind": "primitive", "type": "boolean"},
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return await self._client.request(
            method="post",
            path=("v1", "sections"),
            body=body,
            model=SdkCreateSectionResponseValue201ApplicationJson,
            request_options=request_options,
        )

    async def get(
        self,
        section: int,
        *,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkGetSectionResponseValue200ApplicationJson:
        """Retrieve a section

        Retrieve a section by its ID.

        Args:
            section: The section path parameter.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkGetSectionResponseValue200ApplicationJson

        Raises:
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [
                        {"schemeName": "bearerAuth", "scopes": []},
                        {"schemeName": "storeAuth", "scopes": []},
                    ]
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": False,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": True,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": False,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "getSection",
        }
        return await self._client.request(
            method="get",
            path=("v1", "sections", str(section)),
            model=SdkGetSectionResponseValue200ApplicationJson,
            request_options=request_options,
        )

    async def replace(
        self,
        section: int,
        *,
        title: Optional[str] = None,
        hidden: Optional[bool] = None,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkReplaceSectionResponseValue200ApplicationJson:
        """Update a section

        Update a storefront section's details.

        Args:
            section: The section path parameter.
            title: The title.
            hidden: The hidden.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkReplaceSectionResponseValue200ApplicationJson

        Raises:
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [
                        {"schemeName": "bearerAuth", "scopes": []},
                        {"schemeName": "storeAuth", "scopes": []},
                    ]
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": False,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": True,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": False,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "replaceSection",
        }
        body: Dict[str, Any] = {
            k: v
            for k, v in {
                "title": title,
                "hidden": hidden,
            }.items()
            if v is not None
        }
        _validate_model(
            body,
            {
                "name": "SdkReplaceSectionRequestApplicationJson",
                "required": [],
                "properties": {
                    "title": {
                        "kind": "primitive",
                        "type": "string",
                        "constraints": {"maxLength": 255},
                    },
                    "hidden": {"kind": "primitive", "type": "boolean"},
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return await self._client.request(
            method="put",
            path=("v1", "sections", str(section)),
            body=body,
            model=SdkReplaceSectionResponseValue200ApplicationJson,
            request_options=request_options,
        )

    async def update(
        self,
        section: int,
        *,
        title: Optional[str] = None,
        hidden: Optional[bool] = None,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkUpdateSectionResponseValue200ApplicationJson:
        """Update a section

        Update a storefront section's details.

        Args:
            section: The section path parameter.
            title: The title.
            hidden: The hidden.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkUpdateSectionResponseValue200ApplicationJson

        Raises:
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [
                        {"schemeName": "bearerAuth", "scopes": []},
                        {"schemeName": "storeAuth", "scopes": []},
                    ]
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": False,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": True,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": False,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "updateSection",
        }
        body: Dict[str, Any] = {
            k: v
            for k, v in {
                "title": title,
                "hidden": hidden,
            }.items()
            if v is not None
        }
        _validate_model(
            body,
            {
                "name": "SdkUpdateSectionRequestApplicationJson",
                "required": [],
                "properties": {
                    "title": {
                        "kind": "primitive",
                        "type": "string",
                        "constraints": {"maxLength": 255},
                    },
                    "hidden": {"kind": "primitive", "type": "boolean"},
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return await self._client.request(
            method="patch",
            path=("v1", "sections", str(section)),
            body=body,
            model=SdkUpdateSectionResponseValue200ApplicationJson,
            request_options=request_options,
        )

    async def delete(
        self,
        section: int,
        *,
        request_options: Optional[RequestOptions] = None,
    ) -> None:
        """Delete a section

        Deletes a section. This will permanently delete the section and its details.

        Args:
            section: The section path parameter.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Raises:
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [
                        {"schemeName": "bearerAuth", "scopes": []},
                        {"schemeName": "storeAuth", "scopes": []},
                    ]
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": False,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": True,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": False,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "deleteSection",
        }
        await self._client.request(
            method="delete",
            path=("v1", "sections", str(section)),
            request_options=request_options,
        )

    async def replace_order(
        self,
        *,
        resources: List[int],
        request_options: Optional[RequestOptions] = None,
    ) -> SdkReplaceSectionOrderResponseValue200ApplicationJson:
        """Replace section order

        Replace the storefront section order with every section ID in the store. IDs are applied in array order; incomplete lists and unknown IDs are rejected.

        Args:
            resources: The resources.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkReplaceSectionOrderResponseValue200ApplicationJson

        Raises:
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [
                        {"schemeName": "bearerAuth", "scopes": []},
                        {"schemeName": "storeAuth", "scopes": []},
                    ]
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": False,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": True,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": False,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "replaceSectionOrder",
        }
        body: Dict[str, Any] = {
            "resources": resources,
        }
        _validate_model(
            body,
            {
                "name": "SdkReplaceSectionOrderRequestApplicationJson",
                "required": ["resources"],
                "properties": {
                    "resources": {
                        "kind": "array",
                        "items": {"kind": "primitive", "type": "integer"},
                        "constraints": {"uniqueItems": True},
                    }
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return await self._client.request(
            method="put",
            path=("v1", "sections", "order"),
            body=body,
            model=SdkReplaceSectionOrderResponseValue200ApplicationJson,
            request_options=request_options,
        )

    async def replace_products(
        self,
        section: int,
        *,
        resources: List[int],
        request_options: Optional[RequestOptions] = None,
    ) -> SdkReplaceSectionProductsResponseValue200ApplicationJson:
        """Replace section products

        Replace the products assigned to a section. Product IDs are stored in the supplied order; omitted products are detached and unknown IDs are rejected.

        Args:
            section: The section path parameter.
            resources: The resources.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkReplaceSectionProductsResponseValue200ApplicationJson

        Raises:
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [
                        {"schemeName": "bearerAuth", "scopes": []},
                        {"schemeName": "storeAuth", "scopes": []},
                    ]
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": False,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": True,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": False,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "replaceSectionProducts",
        }
        body: Dict[str, Any] = {
            "resources": resources,
        }
        _validate_model(
            body,
            {
                "name": "SdkReplaceSectionOrderRequestApplicationJson",
                "required": ["resources"],
                "properties": {
                    "resources": {
                        "kind": "array",
                        "items": {"kind": "primitive", "type": "integer"},
                        "constraints": {"uniqueItems": True},
                    }
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return await self._client.request(
            method="put",
            path=("v1", "sections", str(section), "products"),
            body=body,
            model=SdkReplaceSectionProductsResponseValue200ApplicationJson,
            request_options=request_options,
        )

    async def replace_groups(
        self,
        section: int,
        *,
        resources: List[int],
        request_options: Optional[RequestOptions] = None,
    ) -> SdkReplaceSectionGroupsResponseValue200ApplicationJson:
        """Replace section groups

        Replace the groups assigned to a section. Group IDs are stored in the supplied order; omitted groups are detached and unknown IDs are rejected.

        Args:
            section: The section path parameter.
            resources: The resources.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkReplaceSectionGroupsResponseValue200ApplicationJson

        Raises:
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [
                        {"schemeName": "bearerAuth", "scopes": []},
                        {"schemeName": "storeAuth", "scopes": []},
                    ]
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": False,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": True,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": False,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "replaceSectionGroups",
        }
        body: Dict[str, Any] = {
            "resources": resources,
        }
        _validate_model(
            body,
            {
                "name": "SdkReplaceSectionOrderRequestApplicationJson",
                "required": ["resources"],
                "properties": {
                    "resources": {
                        "kind": "array",
                        "items": {"kind": "primitive", "type": "integer"},
                        "constraints": {"uniqueItems": True},
                    }
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return await self._client.request(
            method="put",
            path=("v1", "sections", str(section), "groups"),
            body=body,
            model=SdkReplaceSectionGroupsResponseValue200ApplicationJson,
            request_options=request_options,
        )

    async def search(
        self,
        *,
        filters: Optional[
            List[SearchSectionsRequestApplicationJsonPropertyFiltersItem]
        ] = None,
        sort: Optional[
            List[SearchSectionsRequestApplicationJsonPropertySortItem]
        ] = None,
        search: Optional[SearchSectionsRequestApplicationJsonPropertySearch] = None,
        includes: Optional[
            List[SearchSectionsRequestApplicationJsonPropertyIncludesItem]
        ] = None,
        limit: Optional[int] = None,
        before: Optional[str] = None,
        after: Optional[str] = None,
        order: Optional[str] = None,
        request_options: Optional[RequestOptions] = None,
    ) -> AsyncPage[SearchSectionsResponseValue200ApplicationJsonPropertyDataItem]:
        """Search sections

        Search sections using JSON body filters, search terms, includes, and sort instructions.

        Args:
            filters: The filters.
            sort: The sort.
            search: The search.
            includes: The includes.
            limit: The limit.
            before: The before.
            after: The after.
            order: The order.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            AsyncPage[SearchSectionsResponseValue200ApplicationJsonPropertyDataItem]

        Raises:
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [
                        {"schemeName": "bearerAuth", "scopes": []},
                        {"schemeName": "storeAuth", "scopes": []},
                    ]
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": False,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": True,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": False,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "searchSections",
        }
        body: Dict[str, Any] = {
            k: v
            for k, v in {
                "filters": [item.to_dict() for item in filters]
                if filters is not None
                else None,
                "sort": [item.to_dict() for item in sort] if sort is not None else None,
                "search": search.to_dict() if search is not None else None,
                "includes": [item.to_dict() for item in includes]
                if includes is not None
                else None,
            }.items()
            if v is not None
        }
        params = {
            k: v
            for k, v in {
                "limit": limit,
                "before": before,
                "after": after,
                "order": order,
            }.items()
            if v is not None
        }
        return await self._client.request_page(
            method="post",
            path=("v1", "sections", "search"),
            model=SearchSectionsResponseValue200ApplicationJsonPropertyDataItem,
            body=body,
            params=params,
            request_options=request_options,
        )

    async def batch_create(
        self,
        *,
        resources: List[BatchCreateSectionsRequestApplicationJsonPropertyResourcesItem],
        request_options: Optional[RequestOptions] = None,
    ) -> SdkBatchCreateSectionsResponseValue200ApplicationJson:
        """Batch create sections

        Create multiple sections in one request by sending a `resources` array.

        Args:
            resources: The resources.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkBatchCreateSectionsResponseValue200ApplicationJson

        Raises:
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [
                        {"schemeName": "bearerAuth", "scopes": []},
                        {"schemeName": "storeAuth", "scopes": []},
                    ]
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": False,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": True,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": False,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "batchCreateSections",
        }
        body: Dict[str, Any] = {
            "resources": [item.to_dict() for item in resources],
        }
        _validate_model(
            body,
            {
                "name": "SdkBatchCreateSectionsRequestApplicationJson",
                "required": ["resources"],
                "properties": {
                    "resources": {"kind": "array", "items": {"kind": "any"}}
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return await self._client.request(
            method="post",
            path=("v1", "sections", "batch"),
            body=body,
            model=SdkBatchCreateSectionsResponseValue200ApplicationJson,
            request_options=request_options,
        )

    async def batch_update(
        self,
        *,
        resources: BatchUpdateSectionsRequestApplicationJsonPropertyResources,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkBatchUpdateSectionsResponseValue200ApplicationJson:
        """Batch update sections

        Update multiple sections in one request by sending a `resources` object keyed by section ID.

        Args:
            resources: The resources.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkBatchUpdateSectionsResponseValue200ApplicationJson

        Raises:
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [
                        {"schemeName": "bearerAuth", "scopes": []},
                        {"schemeName": "storeAuth", "scopes": []},
                    ]
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": False,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": True,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": False,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "batchUpdateSections",
        }
        body: Dict[str, Any] = {
            "resources": resources.to_dict(),
        }
        _validate_model(
            body,
            {
                "name": "SdkBatchUpdateSectionsRequestApplicationJson",
                "required": ["resources"],
                "properties": {"resources": {"kind": "any"}},
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return await self._client.request(
            method="patch",
            path=("v1", "sections", "batch"),
            body=body,
            model=SdkBatchUpdateSectionsResponseValue200ApplicationJson,
            request_options=request_options,
        )

    async def batch_delete(
        self,
        *,
        resources: List[int],
        request_options: Optional[RequestOptions] = None,
    ) -> None:
        """Batch delete sections

        Delete multiple sections in one request by sending the section IDs in `resources`.

        Args:
            resources: The resources.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Raises:
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [
                        {"schemeName": "bearerAuth", "scopes": []},
                        {"schemeName": "storeAuth", "scopes": []},
                    ]
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": False,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": True,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": False,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "batchDeleteSections",
        }
        body: Dict[str, Any] = {
            "resources": resources,
        }
        _validate_model(
            body,
            {
                "name": "SdkBatchDeleteCouponsRequestApplicationJson",
                "required": ["resources"],
                "properties": {
                    "resources": {
                        "kind": "array",
                        "items": {"kind": "primitive", "type": "integer"},
                    }
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        await self._client.request(
            method="delete",
            path=("v1", "sections", "batch"),
            body=body,
            request_options=request_options,
        )

    async def v2_list_sections(
        self,
        *,
        limit: Optional[int] = None,
        before: Optional[str] = None,
        after: Optional[str] = None,
        order: Optional[str] = None,
        page: Optional[int] = None,
        request_options: Optional[RequestOptions] = None,
    ) -> AsyncPage[V2ListSectionsResponseValue200ApplicationJsonPropertyDataItem]:
        """List all sections

        List your storefront sections, 15 per page by default. This v2 route is store-scoped. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            limit: Number of items to return per page. Defaults to `15`.
            before: The before.
            after: The after.
            order: The order.
            page: Page number to return. Defaults to `1`.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            AsyncPage[V2ListSectionsResponseValue200ApplicationJsonPropertyDataItem]

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "v2ListSections",
        }
        params = {
            k: v
            for k, v in {
                "limit": limit,
                "before": before,
                "after": after,
                "order": order,
                "page": page,
            }.items()
            if v is not None
        }
        return await self._client.request_page(
            method="get",
            path=("v2", "sections"),
            model=V2ListSectionsResponseValue200ApplicationJsonPropertyDataItem,
            params=params,
            request_options=request_options,
        )

    async def v2_create_section(
        self,
        *,
        title: str,
        hidden: bool,
        idempotency_key: Optional[str] = None,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkV2CreateSectionResponseValue201ApplicationJson:
        """Create a section

        Create a section to organize your storefront. This v2 route is store-scoped. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            title: The title.
            hidden: The hidden.
            idempotency_key: Optional idempotency key for safe retries.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkV2CreateSectionResponseValue201ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": True,
            "operation_id": "v2CreateSection",
        }
        body: Dict[str, Any] = {
            "title": title,
            "hidden": hidden,
        }
        _validate_model(
            body,
            {
                "name": "SdkV2CreateSectionRequestApplicationJson",
                "required": ["title", "hidden"],
                "properties": {
                    "title": {
                        "kind": "primitive",
                        "type": "string",
                        "constraints": {"maxLength": 255},
                    },
                    "hidden": {"kind": "primitive", "type": "boolean"},
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return await self._client.request(
            method="post",
            path=("v2", "sections"),
            body=body,
            model=SdkV2CreateSectionResponseValue201ApplicationJson,
            idempotency_key=idempotency_key,
            request_options=request_options,
        )

    async def v2_search_sections(
        self,
        *,
        filters: Optional[
            List[V2SearchSectionsRequestApplicationJsonPropertyFiltersItem]
        ] = None,
        sort: Optional[
            List[V2SearchSectionsRequestApplicationJsonPropertySortItem]
        ] = None,
        search: Optional[V2SearchSectionsRequestApplicationJsonPropertySearch] = None,
        includes: Optional[
            List[V2SearchSectionsRequestApplicationJsonPropertyIncludesItem]
        ] = None,
        limit: Optional[int] = None,
        before: Optional[str] = None,
        after: Optional[str] = None,
        order: Optional[str] = None,
        idempotency_key: Optional[str] = None,
        request_options: Optional[RequestOptions] = None,
    ) -> AsyncPage[V2SearchSectionsResponseValue200ApplicationJsonPropertyDataItem]:
        """Search sections

        Search sections using JSON body filters, search terms, includes, and sort instructions. This v2 route is store-scoped. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            filters: The filters.
            sort: The sort.
            search: The search.
            includes: The includes.
            limit: The limit.
            before: The before.
            after: The after.
            order: The order.
            idempotency_key: Optional idempotency key for safe retries.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            AsyncPage[V2SearchSectionsResponseValue200ApplicationJsonPropertyDataItem]

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": True,
            "operation_id": "v2SearchSections",
        }
        body: Dict[str, Any] = {
            k: v
            for k, v in {
                "filters": [item.to_dict() for item in filters]
                if filters is not None
                else None,
                "sort": [item.to_dict() for item in sort] if sort is not None else None,
                "search": search.to_dict() if search is not None else None,
                "includes": [item.to_dict() for item in includes]
                if includes is not None
                else None,
            }.items()
            if v is not None
        }
        params = {
            k: v
            for k, v in {
                "limit": limit,
                "before": before,
                "after": after,
                "order": order,
            }.items()
            if v is not None
        }
        return await self._client.request_page(
            method="post",
            path=("v2", "sections", "search"),
            model=V2SearchSectionsResponseValue200ApplicationJsonPropertyDataItem,
            body=body,
            params=params,
            request_options=request_options,
        )

    async def v2_get_section(
        self,
        section: int,
        *,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkV2GetSectionResponseValue200ApplicationJson:
        """Retrieve a section

        Retrieve a section by its ID. This v2 route is store-scoped. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            section: The section path parameter.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkV2GetSectionResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "v2GetSection",
        }
        return await self._client.request(
            method="get",
            path=("v2", "sections", str(section)),
            model=SdkV2GetSectionResponseValue200ApplicationJson,
            request_options=request_options,
        )

    async def v2_replace_section(
        self,
        section: int,
        *,
        title: Optional[str] = None,
        hidden: Optional[bool] = None,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkV2ReplaceSectionResponseValue200ApplicationJson:
        """Update a section

        Update a storefront section's details. This v2 route is store-scoped. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            section: The section path parameter.
            title: The title.
            hidden: The hidden.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkV2ReplaceSectionResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "v2ReplaceSection",
        }
        body: Dict[str, Any] = {
            k: v
            for k, v in {
                "title": title,
                "hidden": hidden,
            }.items()
            if v is not None
        }
        _validate_model(
            body,
            {
                "name": "SdkV2ReplaceSectionRequestApplicationJson",
                "required": [],
                "properties": {
                    "title": {
                        "kind": "primitive",
                        "type": "string",
                        "constraints": {"maxLength": 255},
                    },
                    "hidden": {"kind": "primitive", "type": "boolean"},
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return await self._client.request(
            method="put",
            path=("v2", "sections", str(section)),
            body=body,
            model=SdkV2ReplaceSectionResponseValue200ApplicationJson,
            request_options=request_options,
        )

    async def v2_update_section(
        self,
        section: int,
        *,
        title: Optional[str] = None,
        hidden: Optional[bool] = None,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkV2UpdateSectionResponseValue200ApplicationJson:
        """Update a section

        Update a storefront section's details. This v2 route is store-scoped. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            section: The section path parameter.
            title: The title.
            hidden: The hidden.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkV2UpdateSectionResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "v2UpdateSection",
        }
        body: Dict[str, Any] = {
            k: v
            for k, v in {
                "title": title,
                "hidden": hidden,
            }.items()
            if v is not None
        }
        _validate_model(
            body,
            {
                "name": "SdkV2UpdateSectionRequestApplicationJson",
                "required": [],
                "properties": {
                    "title": {
                        "kind": "primitive",
                        "type": "string",
                        "constraints": {"maxLength": 255},
                    },
                    "hidden": {"kind": "primitive", "type": "boolean"},
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return await self._client.request(
            method="patch",
            path=("v2", "sections", str(section)),
            body=body,
            model=SdkV2UpdateSectionResponseValue200ApplicationJson,
            request_options=request_options,
        )

    async def v2_delete_section(
        self,
        section: int,
        *,
        request_options: Optional[RequestOptions] = None,
    ) -> None:
        """Delete a section

        Deletes a section. This will permanently delete the section and its details. This v2 route is store-scoped. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            section: The section path parameter.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "v2DeleteSection",
        }
        await self._client.request(
            method="delete",
            path=("v2", "sections", str(section)),
            request_options=request_options,
        )

    async def v2_batch_create_sections(
        self,
        *,
        resources: List[
            V2BatchCreateSectionsRequestApplicationJsonPropertyResourcesItem
        ],
        idempotency_key: Optional[str] = None,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkV2BatchCreateSectionsResponseValue200ApplicationJson:
        """Batch create sections

        Create multiple sections in one request by sending a `resources` array. This v2 route is store-scoped. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            resources: The resources.
            idempotency_key: Optional idempotency key for safe retries.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkV2BatchCreateSectionsResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": True,
            "operation_id": "v2BatchCreateSections",
        }
        body: Dict[str, Any] = {
            "resources": [item.to_dict() for item in resources],
        }
        _validate_model(
            body,
            {
                "name": "SdkV2BatchCreateSectionsRequestApplicationJson",
                "required": ["resources"],
                "properties": {
                    "resources": {"kind": "array", "items": {"kind": "any"}}
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return await self._client.request(
            method="post",
            path=("v2", "sections", "batch"),
            body=body,
            model=SdkV2BatchCreateSectionsResponseValue200ApplicationJson,
            idempotency_key=idempotency_key,
            request_options=request_options,
        )

    async def v2_batch_update_sections(
        self,
        *,
        resources: V2BatchUpdateSectionsRequestApplicationJsonPropertyResources,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkV2BatchUpdateSectionsResponseValue200ApplicationJson:
        """Batch update sections

        Update multiple sections in one request by sending a `resources` object keyed by section ID. This v2 route is store-scoped. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            resources: The resources.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkV2BatchUpdateSectionsResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": True,
            "operation_id": "v2BatchUpdateSections",
        }
        body: Dict[str, Any] = {
            "resources": resources.to_dict(),
        }
        _validate_model(
            body,
            {
                "name": "SdkV2BatchUpdateSectionsRequestApplicationJson",
                "required": ["resources"],
                "properties": {"resources": {"kind": "any"}},
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return await self._client.request(
            method="patch",
            path=("v2", "sections", "batch"),
            body=body,
            model=SdkV2BatchUpdateSectionsResponseValue200ApplicationJson,
            request_options=request_options,
        )

    async def v2_batch_delete_sections(
        self,
        *,
        resources: List[int],
        request_options: Optional[RequestOptions] = None,
    ) -> None:
        """Batch delete sections

        Delete multiple sections in one request by sending the section IDs in `resources`. This v2 route is store-scoped. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            resources: The resources.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "v2BatchDeleteSections",
        }
        body: Dict[str, Any] = {
            "resources": resources,
        }
        _validate_model(
            body,
            {
                "name": "SdkV2BatchDeleteSectionsRequestApplicationJson",
                "required": ["resources"],
                "properties": {
                    "resources": {
                        "kind": "array",
                        "items": {"kind": "primitive", "type": "integer"},
                    }
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        await self._client.request(
            method="delete",
            path=("v2", "sections", "batch"),
            body=body,
            request_options=request_options,
        )

    async def v2_replace_section_order(
        self,
        *,
        resources: List[int],
        request_options: Optional[RequestOptions] = None,
    ) -> SdkV2ReplaceSectionOrderResponseValue200ApplicationJson:
        """Replace section order

        Replace the storefront section order with every section ID in the store. IDs are applied in array order; incomplete lists and unknown IDs are rejected. This v2 route is store-scoped. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            resources: The resources.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkV2ReplaceSectionOrderResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "v2ReplaceSectionOrder",
        }
        body: Dict[str, Any] = {
            "resources": resources,
        }
        _validate_model(
            body,
            {
                "name": "SdkV2ReplaceSectionOrderRequestApplicationJson",
                "required": ["resources"],
                "properties": {
                    "resources": {
                        "kind": "array",
                        "items": {"kind": "primitive", "type": "integer"},
                        "constraints": {"uniqueItems": True},
                    }
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return await self._client.request(
            method="put",
            path=("v2", "sections", "order"),
            body=body,
            model=SdkV2ReplaceSectionOrderResponseValue200ApplicationJson,
            request_options=request_options,
        )

    async def v2_replace_section_products(
        self,
        section: int,
        *,
        resources: List[int],
        request_options: Optional[RequestOptions] = None,
    ) -> SdkV2ReplaceSectionProductsResponseValue200ApplicationJson:
        """Replace section products

        Replace the products assigned to a section. Product IDs are stored in the supplied order; omitted products are detached and unknown IDs are rejected. This v2 route is store-scoped. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            section: The section path parameter.
            resources: The resources.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkV2ReplaceSectionProductsResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "v2ReplaceSectionProducts",
        }
        body: Dict[str, Any] = {
            "resources": resources,
        }
        _validate_model(
            body,
            {
                "name": "SdkV2ReplaceSectionProductsRequestApplicationJson",
                "required": ["resources"],
                "properties": {
                    "resources": {
                        "kind": "array",
                        "items": {"kind": "primitive", "type": "integer"},
                        "constraints": {"uniqueItems": True},
                    }
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return await self._client.request(
            method="put",
            path=("v2", "sections", str(section), "products"),
            body=body,
            model=SdkV2ReplaceSectionProductsResponseValue200ApplicationJson,
            request_options=request_options,
        )

    async def v2_replace_section_groups(
        self,
        section: int,
        *,
        resources: List[int],
        request_options: Optional[RequestOptions] = None,
    ) -> SdkV2ReplaceSectionGroupsResponseValue200ApplicationJson:
        """Replace section groups

        Replace the groups assigned to a section. Group IDs are stored in the supplied order; omitted groups are detached and unknown IDs are rejected. This v2 route is store-scoped. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            section: The section path parameter.
            resources: The resources.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkV2ReplaceSectionGroupsResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "v2ReplaceSectionGroups",
        }
        body: Dict[str, Any] = {
            "resources": resources,
        }
        _validate_model(
            body,
            {
                "name": "SdkV2ReplaceSectionGroupsRequestApplicationJson",
                "required": ["resources"],
                "properties": {
                    "resources": {
                        "kind": "array",
                        "items": {"kind": "primitive", "type": "integer"},
                        "constraints": {"uniqueItems": True},
                    }
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return await self._client.request(
            method="put",
            path=("v2", "sections", str(section), "groups"),
            body=body,
            model=SdkV2ReplaceSectionGroupsResponseValue200ApplicationJson,
            request_options=request_options,
        )
