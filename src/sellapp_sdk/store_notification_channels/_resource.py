# This file is auto-generated by oagen. Do not edit.

from __future__ import annotations

from typing import TYPE_CHECKING, Any, Dict, Optional

from .._base_client import WithRawResponse

if TYPE_CHECKING:
    from .._client import AsyncSellAppClient, SellAppClient

from sellapp_sdk.store_settings.models.create_notification_channel_request_application_json_property_channel import (
    CreateNotificationChannelRequestApplicationJsonPropertyChannel,
)
from sellapp_sdk.store_settings.models.replace_notification_channel_request_application_json_property_channel import (
    ReplaceNotificationChannelRequestApplicationJsonPropertyChannel,
)
from sellapp_sdk.store_settings.models.sdk_create_notification_channel_response_value_201_application_json import (
    SdkCreateNotificationChannelResponseValue201ApplicationJson,
)
from sellapp_sdk.store_settings.models.sdk_get_notification_channel_response_value_200_application_json import (
    SdkGetNotificationChannelResponseValue200ApplicationJson,
)
from sellapp_sdk.store_settings.models.sdk_list_notification_channels_response_value_200_application_json import (
    SdkListNotificationChannelsResponseValue200ApplicationJson,
)
from sellapp_sdk.store_settings.models.sdk_replace_notification_channel_response_value_200_application_json import (
    SdkReplaceNotificationChannelResponseValue200ApplicationJson,
)
from sellapp_sdk.store_settings.models.sdk_update_notification_channel_response_value_200_application_json import (
    SdkUpdateNotificationChannelResponseValue200ApplicationJson,
)
from sellapp_sdk.store_settings.models.update_notification_channel_request_application_json_property_channel import (
    UpdateNotificationChannelRequestApplicationJsonPropertyChannel,
)

from .._types import RequestOptions, _validate_model


class StoreNotificationChannels:
    """Store Notification Channels API resources."""

    def __init__(self, client: SellAppClient) -> None:
        self._client = client
        self.with_raw_response = WithRawResponse(self)

    def list(
        self,
        *,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkListNotificationChannelsResponseValue200ApplicationJson:
        """List notification channels

        List dashboard notification destinations without exposing Discord webhook URLs. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Returns:
            SdkListNotificationChannelsResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "listNotificationChannels",
        }
        return self._client.request(
            method="get",
            path=("v2", "store", "notification-channels"),
            model=SdkListNotificationChannelsResponseValue200ApplicationJson,
            request_options=request_options,
        )

    def create(
        self,
        *,
        channel: CreateNotificationChannelRequestApplicationJsonPropertyChannel,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkCreateNotificationChannelResponseValue201ApplicationJson:
        """Create a notification channel

        Create an email or Discord notification destination. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            channel: The channel.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkCreateNotificationChannelResponseValue201ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "createNotificationChannel",
        }
        body: Dict[str, Any] = {
            "channel": channel.to_dict(),
        }
        _validate_model(
            body,
            {
                "name": "SdkCreateNotificationChannelRequestApplicationJson",
                "required": ["channel"],
                "properties": {"channel": {"kind": "any"}},
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return self._client.request(
            method="post",
            path=("v2", "store", "notification-channels"),
            body=body,
            model=SdkCreateNotificationChannelResponseValue201ApplicationJson,
            request_options=request_options,
        )

    def get(
        self,
        notification_channel: str,
        *,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkGetNotificationChannelResponseValue200ApplicationJson:
        """Retrieve a notification channel

        Manage one store-scoped notification channel by its identifier. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            notification_channel: The UUID of the notification channel.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkGetNotificationChannelResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "getNotificationChannel",
        }
        return self._client.request(
            method="get",
            path=("v2", "store", "notification-channels", str(notification_channel)),
            model=SdkGetNotificationChannelResponseValue200ApplicationJson,
            request_options=request_options,
        )

    def replace(
        self,
        notification_channel: str,
        *,
        channel: ReplaceNotificationChannelRequestApplicationJsonPropertyChannel,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkReplaceNotificationChannelResponseValue200ApplicationJson:
        """Update a notification channel

        Manage one store-scoped notification channel by its identifier. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            notification_channel: The UUID of the notification channel.
            channel: The channel.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkReplaceNotificationChannelResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "replaceNotificationChannel",
        }
        body: Dict[str, Any] = {
            "channel": channel.to_dict(),
        }
        _validate_model(
            body,
            {
                "name": "SdkReplaceNotificationChannelRequestApplicationJson",
                "required": ["channel"],
                "properties": {"channel": {"kind": "any"}},
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return self._client.request(
            method="put",
            path=("v2", "store", "notification-channels", str(notification_channel)),
            body=body,
            model=SdkReplaceNotificationChannelResponseValue200ApplicationJson,
            request_options=request_options,
        )

    def update(
        self,
        notification_channel: str,
        *,
        channel: UpdateNotificationChannelRequestApplicationJsonPropertyChannel,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkUpdateNotificationChannelResponseValue200ApplicationJson:
        """Update a notification channel

        Manage one store-scoped notification channel by its identifier. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            notification_channel: The UUID of the notification channel.
            channel: The channel.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkUpdateNotificationChannelResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "updateNotificationChannel",
        }
        body: Dict[str, Any] = {
            "channel": channel.to_dict(),
        }
        _validate_model(
            body,
            {
                "name": "SdkUpdateNotificationChannelRequestApplicationJson",
                "required": ["channel"],
                "properties": {"channel": {"kind": "any"}},
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return self._client.request(
            method="patch",
            path=("v2", "store", "notification-channels", str(notification_channel)),
            body=body,
            model=SdkUpdateNotificationChannelResponseValue200ApplicationJson,
            request_options=request_options,
        )

    def delete(
        self,
        notification_channel: str,
        *,
        request_options: Optional[RequestOptions] = None,
    ) -> None:
        """Delete a notification channel

        Manage one store-scoped notification channel by its identifier. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            notification_channel: The UUID of the notification channel.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "deleteNotificationChannel",
        }
        self._client.request(
            method="delete",
            path=("v2", "store", "notification-channels", str(notification_channel)),
            request_options=request_options,
        )


class AsyncStoreNotificationChannels:
    """Store Notification Channels API resources (async)."""

    def __init__(self, client: AsyncSellAppClient) -> None:
        self._client = client
        self.with_raw_response = WithRawResponse(self)

    async def list(
        self,
        *,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkListNotificationChannelsResponseValue200ApplicationJson:
        """List notification channels

        List dashboard notification destinations without exposing Discord webhook URLs. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Returns:
            SdkListNotificationChannelsResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "listNotificationChannels",
        }
        return await self._client.request(
            method="get",
            path=("v2", "store", "notification-channels"),
            model=SdkListNotificationChannelsResponseValue200ApplicationJson,
            request_options=request_options,
        )

    async def create(
        self,
        *,
        channel: CreateNotificationChannelRequestApplicationJsonPropertyChannel,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkCreateNotificationChannelResponseValue201ApplicationJson:
        """Create a notification channel

        Create an email or Discord notification destination. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            channel: The channel.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkCreateNotificationChannelResponseValue201ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "createNotificationChannel",
        }
        body: Dict[str, Any] = {
            "channel": channel.to_dict(),
        }
        _validate_model(
            body,
            {
                "name": "SdkCreateNotificationChannelRequestApplicationJson",
                "required": ["channel"],
                "properties": {"channel": {"kind": "any"}},
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return await self._client.request(
            method="post",
            path=("v2", "store", "notification-channels"),
            body=body,
            model=SdkCreateNotificationChannelResponseValue201ApplicationJson,
            request_options=request_options,
        )

    async def get(
        self,
        notification_channel: str,
        *,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkGetNotificationChannelResponseValue200ApplicationJson:
        """Retrieve a notification channel

        Manage one store-scoped notification channel by its identifier. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            notification_channel: The UUID of the notification channel.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkGetNotificationChannelResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "getNotificationChannel",
        }
        return await self._client.request(
            method="get",
            path=("v2", "store", "notification-channels", str(notification_channel)),
            model=SdkGetNotificationChannelResponseValue200ApplicationJson,
            request_options=request_options,
        )

    async def replace(
        self,
        notification_channel: str,
        *,
        channel: ReplaceNotificationChannelRequestApplicationJsonPropertyChannel,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkReplaceNotificationChannelResponseValue200ApplicationJson:
        """Update a notification channel

        Manage one store-scoped notification channel by its identifier. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            notification_channel: The UUID of the notification channel.
            channel: The channel.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkReplaceNotificationChannelResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "replaceNotificationChannel",
        }
        body: Dict[str, Any] = {
            "channel": channel.to_dict(),
        }
        _validate_model(
            body,
            {
                "name": "SdkReplaceNotificationChannelRequestApplicationJson",
                "required": ["channel"],
                "properties": {"channel": {"kind": "any"}},
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return await self._client.request(
            method="put",
            path=("v2", "store", "notification-channels", str(notification_channel)),
            body=body,
            model=SdkReplaceNotificationChannelResponseValue200ApplicationJson,
            request_options=request_options,
        )

    async def update(
        self,
        notification_channel: str,
        *,
        channel: UpdateNotificationChannelRequestApplicationJsonPropertyChannel,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkUpdateNotificationChannelResponseValue200ApplicationJson:
        """Update a notification channel

        Manage one store-scoped notification channel by its identifier. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            notification_channel: The UUID of the notification channel.
            channel: The channel.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkUpdateNotificationChannelResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "updateNotificationChannel",
        }
        body: Dict[str, Any] = {
            "channel": channel.to_dict(),
        }
        _validate_model(
            body,
            {
                "name": "SdkUpdateNotificationChannelRequestApplicationJson",
                "required": ["channel"],
                "properties": {"channel": {"kind": "any"}},
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return await self._client.request(
            method="patch",
            path=("v2", "store", "notification-channels", str(notification_channel)),
            body=body,
            model=SdkUpdateNotificationChannelResponseValue200ApplicationJson,
            request_options=request_options,
        )

    async def delete(
        self,
        notification_channel: str,
        *,
        request_options: Optional[RequestOptions] = None,
    ) -> None:
        """Delete a notification channel

        Manage one store-scoped notification channel by its identifier. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            notification_channel: The UUID of the notification channel.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "deleteNotificationChannel",
        }
        await self._client.request(
            method="delete",
            path=("v2", "store", "notification-channels", str(notification_channel)),
            request_options=request_options,
        )
