# This file is auto-generated by oagen. Do not edit.

from __future__ import annotations

from dataclasses import dataclass
from dataclasses import field as dataclass_field
from enum import Enum
from typing import Any, Dict, Optional

from sellapp_sdk._types import (
    _preserve_unknown_fields,
    _raise_deserialize_error,
    _validate_model,
)
from sellapp_sdk.common.models.course_visibility import CourseVisibility
from sellapp_sdk.common.models.dark_mode import DarkMode

_DOMAIN_RUNTIME_SCHEMA: Dict[str, Any] = {
    "name": "SdkReplaceGeneralStoreSettingsRequestApplicationJson",
    "required": ["name", "visibility", "timezone", "currency"],
    "properties": {
        "name": {
            "kind": "primitive",
            "type": "string",
            "constraints": {"maxLength": 48},
        },
        "visibility": {"kind": "enum", "values": ["PUBLIC", "HIDDEN", "PRIVATE"]},
        "timezone": {"kind": "primitive", "type": "string"},
        "currency": {
            "kind": "primitive",
            "type": "string",
            "constraints": {"minLength": 3, "maxLength": 3},
        },
        "dark_mode": {
            "kind": "nullable",
            "inner": {"kind": "enum", "values": ["light", "dark", "system", "null"]},
        },
    },
    "additionalProperties": True,
}
_WIRE_RUNTIME_SCHEMA: Dict[str, Any] = {
    "name": "SdkReplaceGeneralStoreSettingsRequestApplicationJson",
    "required": ["name", "visibility", "timezone", "currency"],
    "properties": {
        "name": {
            "kind": "primitive",
            "type": "string",
            "constraints": {"maxLength": 48},
        },
        "visibility": {"kind": "enum", "values": ["PUBLIC", "HIDDEN", "PRIVATE"]},
        "timezone": {"kind": "primitive", "type": "string"},
        "currency": {
            "kind": "primitive",
            "type": "string",
            "constraints": {"minLength": 3, "maxLength": 3},
        },
        "dark_mode": {
            "kind": "nullable",
            "inner": {"kind": "enum", "values": ["light", "dark", "system", "null"]},
        },
    },
    "additionalProperties": True,
}


@dataclass(slots=True)
class SdkReplaceGeneralStoreSettingsRequestApplicationJson:
    """Sdk Replace General Store Settings Request Application Json model."""

    name: str
    visibility: CourseVisibility
    timezone: str
    currency: str
    dark_mode: Optional[DarkMode] = None
    additional_properties: Dict[str, Any] = dataclass_field(
        default_factory=dict, repr=False
    )
    _from_response: bool = dataclass_field(default=False, repr=False, compare=False)

    @classmethod
    def from_dict(
        cls, data: Dict[str, Any]
    ) -> SdkReplaceGeneralStoreSettingsRequestApplicationJson:
        """Deserialize from a dictionary."""
        try:
            _validate_model(
                data,
                _WIRE_RUNTIME_SCHEMA,
                direction="response",
                allow_unknown_enums=True,
                allow_unknown_union_variants=True,
                allow_unknown_fields=True,
            )
            return cls(
                name=data["name"],
                visibility=CourseVisibility(data["visibility"]),
                timezone=data["timezone"],
                currency=data["currency"],
                dark_mode=DarkMode(_v_dark_mode)
                if (_v_dark_mode := data.get("dark_mode")) is not None
                else None,
                additional_properties=_preserve_unknown_fields(
                    data, ["name", "visibility", "timezone", "currency", "dark_mode"]
                ),
                _from_response=True,
            )
        except (KeyError, ValueError) as e:
            _raise_deserialize_error(
                "SdkReplaceGeneralStoreSettingsRequestApplicationJson", e
            )

    def to_dict(self) -> Dict[str, Any]:
        """Serialize to a dictionary."""
        _domain_data: Dict[str, Any] = dict(self.additional_properties)
        _domain_data["name"] = self.name
        _domain_data["visibility"] = self.visibility
        _domain_data["timezone"] = self.timezone
        _domain_data["currency"] = self.currency
        _domain_data["dark_mode"] = self.dark_mode
        _validate_model(
            _domain_data,
            _DOMAIN_RUNTIME_SCHEMA,
            direction="request",
            allow_unknown_enums=self._from_response,
            allow_unknown_union_variants=self._from_response,
            allow_unknown_fields=(self._from_response or True),
        )
        result: Dict[str, Any] = dict(self.additional_properties)
        result["name"] = self.name
        result["visibility"] = (
            self.visibility.value
            if isinstance(self.visibility, Enum)
            else self.visibility
        )
        result["timezone"] = self.timezone
        result["currency"] = self.currency
        if self.dark_mode is not None:
            result["dark_mode"] = (
                self.dark_mode.value
                if isinstance(self.dark_mode, Enum)
                else self.dark_mode
            )
        else:
            result["dark_mode"] = None
        return result
