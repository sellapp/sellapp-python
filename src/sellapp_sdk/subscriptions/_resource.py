# This file is auto-generated by oagen. Do not edit.

from __future__ import annotations

from typing import TYPE_CHECKING, Any, Dict, Optional, Union

from .._base_client import WithRawResponse

if TYPE_CHECKING:
    from .._client import AsyncSellAppClient, SellAppClient

from sellapp_sdk.common.models.list_subscriptions_response_value_200_application_json_property_data_item import (
    ListSubscriptionsResponseValue200ApplicationJsonPropertyDataItem,
)
from sellapp_sdk.common.models.sdk_confirm_subscription_plan_change_request_application_json_effective_timing import (
    SdkConfirmSubscriptionPlanChangeRequestApplicationJsonEffectiveTiming,
)
from sellapp_sdk.common.models.sdk_confirm_subscription_plan_change_request_application_json_proration_behavior import (
    SdkConfirmSubscriptionPlanChangeRequestApplicationJsonProrationBehavior,
)
from sellapp_sdk.common.models.sdk_preview_subscription_plan_change_request_application_json_effective_timing import (
    SdkPreviewSubscriptionPlanChangeRequestApplicationJsonEffectiveTiming,
)
from sellapp_sdk.common.models.sdk_preview_subscription_plan_change_request_application_json_proration_behavior import (
    SdkPreviewSubscriptionPlanChangeRequestApplicationJsonProrationBehavior,
)

from .._pagination import AsyncPage, SyncPage
from .._types import NOT_GIVEN, NotGiven, RequestOptions, _validate_model, enum_value
from .models import (
    ConfirmSubscriptionPlanChangeRequestApplicationJsonPropertyMetadata,
    ConfirmSubscriptionRenewalDateChangeRequestApplicationJsonPropertyMetadata,
    PreviewSubscriptionPlanChangeRequestApplicationJsonPropertyMetadata,
    PreviewSubscriptionRenewalDateChangeRequestApplicationJsonPropertyMetadata,
    SdkCancelSubscriptionAtPeriodEndResponseValue200ApplicationJson,
    SdkCancelSubscriptionImmediatelyResponseValue200ApplicationJson,
    SdkCancelSubscriptionResponseValue200ApplicationJson,
    SdkConfirmSubscriptionRenewalDateChangeResponseValue200ApplicationJson,
    SdkGetSubscriptionCapabilitiesResponseValue200ApplicationJson,
    SdkGetSubscriptionResponseValue200ApplicationJson,
    SdkPauseSubscriptionResponseValue200ApplicationJson,
    SdkPreviewSubscriptionRenewalDateChangeResponseValue200ApplicationJson,
    SdkResumeSubscriptionResponseValue200ApplicationJson,
    SearchSubscriptionsResponseValue200ApplicationJsonPropertyDataItem,
)


class Subscriptions:
    """Subscriptions API resources."""

    def __init__(self, client: SellAppClient) -> None:
        self._client = client
        self.with_raw_response = WithRawResponse(self)

    def cancel(
        self,
        subscription: int,
        *,
        cancel_at_period_end: bool,
        refund_last_payment: Optional[bool] = None,
        pro_rated_refund: Optional[bool] = None,
        idempotency_key: Union[str, None, NotGiven] = NOT_GIVEN,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkCancelSubscriptionResponseValue200ApplicationJson:
        """Cancel a subscription

        Cancel an active subscription now or at period end. cancel_at_period_end is required. Refund flags are valid only when it is false; period-end cancellation does not refund the last payment. Refunds are off by default. refund_last_payment requests a real refund; pro_rated_refund requires refund_last_payment and defaults to false. Cancellation can succeed even if the requested refund fails. A 200 response or confirmed cancellation does not prove a refund, and the action response does not expose the provider refund result. Check the order and payment-provider refund records before retrying; do not replay cancellation as a refund-recovery step. Reuse an idempotency key only with identical action input and actor. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            subscription: The subscription path parameter.
            cancel_at_period_end: The cancel at period end.
            refund_last_payment: Request a real refund of the latest payment during immediate cancellation. Cancellation success does not prove refund success; verify provider/order refund records.
            pro_rated_refund: Request a prorated refund. Requires refund_last_payment=true. False requests a full latest-payment refund when refund_last_payment is enabled.
            idempotency_key: Optional idempotency key. You may also send this as the Idempotency-Key header.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkCancelSubscriptionResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": True,
            "operation_id": "cancelSubscription",
        }
        body: Dict[str, Any] = {
            k: v
            for k, v in {
                "cancel_at_period_end": cancel_at_period_end,
                "refund_last_payment": refund_last_payment,
                "pro_rated_refund": pro_rated_refund,
            }.items()
            if v is not None
        }
        if not isinstance(idempotency_key, NotGiven):
            body["idempotency_key"] = idempotency_key
        _validate_model(
            body,
            {
                "name": "SdkCancelSubscriptionRequestApplicationJson",
                "required": ["cancel_at_period_end"],
                "properties": {
                    "cancel_at_period_end": {"kind": "primitive", "type": "boolean"},
                    "refund_last_payment": {"kind": "primitive", "type": "boolean"},
                    "pro_rated_refund": {"kind": "primitive", "type": "boolean"},
                    "idempotency_key": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "string",
                            "constraints": {"maxLength": 128},
                        },
                    },
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return self._client.request(
            method="patch",
            path=("v2", "subscriptions", str(subscription), "cancel"),
            body=body,
            model=SdkCancelSubscriptionResponseValue200ApplicationJson,
            request_options=request_options,
        )

    def get_capabilities(
        self,
        product_subscription: int,
        *,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkGetSubscriptionCapabilitiesResponseValue200ApplicationJson:
        """View subscription capabilities

        Return the lifecycle actions currently supported for a subscription. Capability status controls whether an actor can pause, resume, cancel, update the payment method, change plan, or shift the billing date. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            product_subscription: The productSubscription path parameter.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkGetSubscriptionCapabilitiesResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "getSubscriptionCapabilities",
        }
        return self._client.request(
            method="get",
            path=("v2", "subscriptions", str(product_subscription), "capabilities"),
            model=SdkGetSubscriptionCapabilitiesResponseValue200ApplicationJson,
            request_options=request_options,
        )

    def cancel_at_period_end(
        self,
        product_subscription: int,
        *,
        idempotency_key: Union[str, None, NotGiven] = NOT_GIVEN,
        reason: Union[str, None, NotGiven] = NOT_GIVEN,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkCancelSubscriptionAtPeriodEndResponseValue200ApplicationJson:
        """Cancel a subscription at period end

        Schedule cancellation at the end of the current billing period without requesting a refund. Inspect capabilities first. The response records the cancellation action; retrieve the subscription to observe its current state. Reuse an idempotency key only with identical action input and actor. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            product_subscription: The productSubscription path parameter.
            idempotency_key: Optional idempotency key. You may also send this as the Idempotency-Key header.
            reason: The reason.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkCancelSubscriptionAtPeriodEndResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": True,
            "operation_id": "cancelSubscriptionAtPeriodEnd",
        }
        body: Dict[str, Any] = {}
        if not isinstance(idempotency_key, NotGiven):
            body["idempotency_key"] = idempotency_key
        if not isinstance(reason, NotGiven):
            body["reason"] = reason
        _validate_model(
            body,
            {
                "name": "SdkCancelSubscriptionAtPeriodEndRequestApplicationJson",
                "required": [],
                "properties": {
                    "idempotency_key": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "string",
                            "constraints": {"maxLength": 128},
                        },
                    },
                    "reason": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "string",
                            "constraints": {"maxLength": 500},
                        },
                    },
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return self._client.request(
            method="post",
            path=(
                "v2",
                "subscriptions",
                str(product_subscription),
                "actions",
                "cancel-period-end",
            ),
            body=body,
            model=SdkCancelSubscriptionAtPeriodEndResponseValue200ApplicationJson,
            request_options=request_options,
        )

    def cancel_immediately(
        self,
        product_subscription: int,
        *,
        idempotency_key: Union[str, None, NotGiven] = NOT_GIVEN,
        reason: Union[str, None, NotGiven] = NOT_GIVEN,
        refund_last_payment: Optional[bool] = None,
        pro_rated_refund: Optional[bool] = None,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkCancelSubscriptionImmediatelyResponseValue200ApplicationJson:
        """Cancel a subscription immediately

        Request immediate subscription cancellation. Inspect capabilities for provider support. This ends future billing and can change customer access. Refunds are off by default. refund_last_payment requests a real refund; pro_rated_refund requires refund_last_payment and defaults to false. Cancellation can succeed even if the requested refund fails. A 200 response or confirmed cancellation does not prove a refund, and the action response does not expose the provider refund result. Check the order and payment-provider refund records before retrying; do not replay cancellation as a refund-recovery step. Reuse an idempotency key only with identical action input and actor. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            product_subscription: The productSubscription path parameter.
            idempotency_key: Optional idempotency key. You may also send this as the Idempotency-Key header.
            reason: The reason.
            refund_last_payment: Request a real refund of the latest payment during immediate cancellation. Cancellation success does not prove refund success; verify provider/order refund records.
            pro_rated_refund: Request a prorated refund. Requires refund_last_payment=true. False requests a full latest-payment refund when refund_last_payment is enabled.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkCancelSubscriptionImmediatelyResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": True,
            "operation_id": "cancelSubscriptionImmediately",
        }
        body: Dict[str, Any] = {
            k: v
            for k, v in {
                "refund_last_payment": refund_last_payment,
                "pro_rated_refund": pro_rated_refund,
            }.items()
            if v is not None
        }
        if not isinstance(idempotency_key, NotGiven):
            body["idempotency_key"] = idempotency_key
        if not isinstance(reason, NotGiven):
            body["reason"] = reason
        _validate_model(
            body,
            {
                "name": "SdkCancelSubscriptionImmediatelyRequestApplicationJson",
                "required": [],
                "properties": {
                    "idempotency_key": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "string",
                            "constraints": {"maxLength": 128},
                        },
                    },
                    "reason": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "string",
                            "constraints": {"maxLength": 500},
                        },
                    },
                    "refund_last_payment": {"kind": "primitive", "type": "boolean"},
                    "pro_rated_refund": {"kind": "primitive", "type": "boolean"},
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return self._client.request(
            method="post",
            path=(
                "v2",
                "subscriptions",
                str(product_subscription),
                "actions",
                "cancel-immediately",
            ),
            body=body,
            model=SdkCancelSubscriptionImmediatelyResponseValue200ApplicationJson,
            request_options=request_options,
        )

    def pause(
        self,
        product_subscription: int,
        *,
        idempotency_key: Union[str, None, NotGiven] = NOT_GIVEN,
        resume_at: Union[str, None, NotGiven] = NOT_GIVEN,
        reason: Union[str, None, NotGiven] = NOT_GIVEN,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkPauseSubscriptionResponseValue200ApplicationJson:
        """Pause a subscription

        Pause a subscription after checking capabilities. Automatic resume dates are supported only by the current Stripe flow; use a future resume_at when supplied. Other provider pauses may need an explicit resume action. Customer-originated pauses are also subject to store pause policies. Reuse an idempotency key only with identical input and actor, and retrieve the subscription after the action. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            product_subscription: The productSubscription path parameter.
            idempotency_key: Optional idempotency key. You may also send this as the Idempotency-Key header.
            resume_at: Use a future date-time at execution time. Automatic resume dates are supported only for Stripe; the shown date is illustrative.
            reason: The reason.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkPauseSubscriptionResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": True,
            "operation_id": "pauseSubscription",
        }
        body: Dict[str, Any] = {}
        if not isinstance(idempotency_key, NotGiven):
            body["idempotency_key"] = idempotency_key
        if not isinstance(resume_at, NotGiven):
            body["resume_at"] = resume_at
        if not isinstance(reason, NotGiven):
            body["reason"] = reason
        _validate_model(
            body,
            {
                "name": "SdkPauseSubscriptionRequestApplicationJson",
                "required": [],
                "properties": {
                    "idempotency_key": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "string",
                            "constraints": {"maxLength": 128},
                        },
                    },
                    "resume_at": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "string",
                            "format": "date-time",
                        },
                    },
                    "reason": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "string",
                            "constraints": {"maxLength": 500},
                        },
                    },
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return self._client.request(
            method="post",
            path=("v2", "subscriptions", str(product_subscription), "actions", "pause"),
            body=body,
            model=SdkPauseSubscriptionResponseValue200ApplicationJson,
            request_options=request_options,
        )

    def resume(
        self,
        product_subscription: int,
        *,
        idempotency_key: Union[str, None, NotGiven] = NOT_GIVEN,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkResumeSubscriptionResponseValue200ApplicationJson:
        """Resume a subscription

        Resume a paused subscription and restore related entitlements where possible. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            product_subscription: The productSubscription path parameter.
            idempotency_key: Optional idempotency key. You may also send this as the Idempotency-Key header.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkResumeSubscriptionResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": True,
            "operation_id": "resumeSubscription",
        }
        body: Dict[str, Any] = {}
        if not isinstance(idempotency_key, NotGiven):
            body["idempotency_key"] = idempotency_key
        _validate_model(
            body,
            {
                "name": "SdkResumeSubscriptionRequestApplicationJson",
                "required": [],
                "properties": {
                    "idempotency_key": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "string",
                            "constraints": {"maxLength": 128},
                        },
                    }
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return self._client.request(
            method="post",
            path=(
                "v2",
                "subscriptions",
                str(product_subscription),
                "actions",
                "resume",
            ),
            body=body,
            model=SdkResumeSubscriptionResponseValue200ApplicationJson,
            request_options=request_options,
        )

    def update_payment_method(
        self,
        product_subscription: int,
        *,
        idempotency_key: Union[str, None, NotGiven] = NOT_GIVEN,
        request_options: Optional[RequestOptions] = None,
    ) -> Any:
        """Update a subscription payment method

        This action belongs to the customer. Seller API requests return a 422 validation error. Check subscription capabilities and use the customer flow; never expose a seller API key to work around this restriction. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            product_subscription: The productSubscription path parameter.
            idempotency_key: Optional idempotency key. You may also send this as the Idempotency-Key header.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            Any

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": True,
            "operation_id": "updateSubscriptionPaymentMethod",
        }
        body: Dict[str, Any] = {}
        if not isinstance(idempotency_key, NotGiven):
            body["idempotency_key"] = idempotency_key
        _validate_model(
            body,
            {
                "name": "SdkUpdateSubscriptionPaymentMethodRequestApplicationJson",
                "required": [],
                "properties": {
                    "idempotency_key": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "string",
                            "constraints": {"maxLength": 128},
                        },
                    }
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return self._client.request(
            method="post",
            path=(
                "v2",
                "subscriptions",
                str(product_subscription),
                "actions",
                "update-payment-method",
            ),
            body=body,
            request_options=request_options,
        )

    def preview_plan_change(
        self,
        product_subscription: int,
        *,
        target_variant_id: int,
        idempotency_key: Union[str, None, NotGiven] = NOT_GIVEN,
        effective_timing: Optional[
            Union[
                SdkPreviewSubscriptionPlanChangeRequestApplicationJsonEffectiveTiming,
                str,
            ]
        ] = None,
        proration_behavior: Optional[
            Union[
                SdkPreviewSubscriptionPlanChangeRequestApplicationJsonProrationBehavior,
                str,
            ]
        ] = None,
        metadata: Optional[
            PreviewSubscriptionPlanChangeRequestApplicationJsonPropertyMetadata
        ] = None,
        request_options: Optional[RequestOptions] = None,
    ) -> Any:
        """Preview a subscription plan change

        This action belongs to the customer. Seller API requests return a 422 validation error. Check subscription capabilities and use the customer flow; never expose a seller API key to work around this restriction. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            product_subscription: The productSubscription path parameter.
            idempotency_key: Optional idempotency key. You may also send this as the Idempotency-Key header.
            target_variant_id: The target variant id.
            effective_timing: The effective timing.
            proration_behavior: The proration behavior.
            metadata: The metadata.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            Any

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": True,
            "operation_id": "previewSubscriptionPlanChange",
        }
        body: Dict[str, Any] = {
            k: v
            for k, v in {
                "target_variant_id": target_variant_id,
                "effective_timing": enum_value(effective_timing)
                if effective_timing is not None
                else None,
                "proration_behavior": enum_value(proration_behavior)
                if proration_behavior is not None
                else None,
                "metadata": metadata.to_dict() if metadata is not None else None,
            }.items()
            if v is not None
        }
        if not isinstance(idempotency_key, NotGiven):
            body["idempotency_key"] = idempotency_key
        _validate_model(
            body,
            {
                "name": "SdkPreviewSubscriptionPlanChangeRequestApplicationJson",
                "required": ["target_variant_id"],
                "properties": {
                    "idempotency_key": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "string",
                            "constraints": {"maxLength": 128},
                        },
                    },
                    "target_variant_id": {"kind": "primitive", "type": "integer"},
                    "effective_timing": {
                        "kind": "enum",
                        "values": ["immediate", "next_renewal"],
                    },
                    "proration_behavior": {
                        "kind": "enum",
                        "values": [
                            "invoice_now",
                            "credit_next_invoice",
                            "none",
                            "provider_default",
                        ],
                    },
                    "metadata": {"kind": "any"},
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return self._client.request(
            method="post",
            path=(
                "v2",
                "subscriptions",
                str(product_subscription),
                "actions",
                "change-plan",
                "preview",
            ),
            body=body,
            request_options=request_options,
        )

    def confirm_plan_change(
        self,
        product_subscription: int,
        *,
        target_variant_id: int,
        preview_token: str,
        idempotency_key: Union[str, None, NotGiven] = NOT_GIVEN,
        effective_timing: Optional[
            Union[
                SdkConfirmSubscriptionPlanChangeRequestApplicationJsonEffectiveTiming,
                str,
            ]
        ] = None,
        proration_behavior: Optional[
            Union[
                SdkConfirmSubscriptionPlanChangeRequestApplicationJsonProrationBehavior,
                str,
            ]
        ] = None,
        metadata: Optional[
            ConfirmSubscriptionPlanChangeRequestApplicationJsonPropertyMetadata
        ] = None,
        request_options: Optional[RequestOptions] = None,
    ) -> Any:
        """Confirm a subscription plan change

        This action belongs to the customer. Seller API requests return a 422 validation error. Check subscription capabilities and use the customer flow; never expose a seller API key to work around this restriction. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            product_subscription: The productSubscription path parameter.
            idempotency_key: Optional idempotency key. You may also send this as the Idempotency-Key header.
            target_variant_id: The target variant id.
            preview_token: The preview token.
            effective_timing: The effective timing.
            proration_behavior: The proration behavior.
            metadata: The metadata.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            Any

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": True,
            "operation_id": "confirmSubscriptionPlanChange",
        }
        body: Dict[str, Any] = {
            k: v
            for k, v in {
                "target_variant_id": target_variant_id,
                "preview_token": preview_token,
                "effective_timing": enum_value(effective_timing)
                if effective_timing is not None
                else None,
                "proration_behavior": enum_value(proration_behavior)
                if proration_behavior is not None
                else None,
                "metadata": metadata.to_dict() if metadata is not None else None,
            }.items()
            if v is not None
        }
        if not isinstance(idempotency_key, NotGiven):
            body["idempotency_key"] = idempotency_key
        _validate_model(
            body,
            {
                "name": "SdkConfirmSubscriptionPlanChangeRequestApplicationJson",
                "required": ["target_variant_id", "preview_token"],
                "properties": {
                    "idempotency_key": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "string",
                            "constraints": {"maxLength": 128},
                        },
                    },
                    "target_variant_id": {"kind": "primitive", "type": "integer"},
                    "preview_token": {
                        "kind": "primitive",
                        "type": "string",
                        "constraints": {"maxLength": 128},
                    },
                    "effective_timing": {
                        "kind": "enum",
                        "values": ["immediate", "next_renewal"],
                    },
                    "proration_behavior": {
                        "kind": "enum",
                        "values": [
                            "invoice_now",
                            "credit_next_invoice",
                            "none",
                            "provider_default",
                        ],
                    },
                    "metadata": {"kind": "any"},
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return self._client.request(
            method="post",
            path=(
                "v2",
                "subscriptions",
                str(product_subscription),
                "actions",
                "change-plan",
                "confirm",
            ),
            body=body,
            request_options=request_options,
        )

    def preview_renewal_date(
        self,
        product_subscription: int,
        *,
        renewal_date: str,
        idempotency_key: Union[str, None, NotGiven] = NOT_GIVEN,
        reason: Union[str, None, NotGiven] = NOT_GIVEN,
        metadata: Optional[
            PreviewSubscriptionRenewalDateChangeRequestApplicationJsonPropertyMetadata
        ] = None,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkPreviewSubscriptionRenewalDateChangeResponseValue200ApplicationJson:
        """Preview a subscription renewal date change

        Preview a new renewal date before confirmation when the subscription's billing-date policy requires a preview. The current Stripe and PayPal provider flows do not support renewal-date changes and return 422. Check shift_billing_date capabilities before sending a request. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            product_subscription: The productSubscription path parameter.
            idempotency_key: Optional idempotency key. You may also send this as the Idempotency-Key header.
            renewal_date: The renewal date.
            reason: The reason.
            metadata: The metadata.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkPreviewSubscriptionRenewalDateChangeResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": True,
            "operation_id": "previewSubscriptionRenewalDateChange",
        }
        body: Dict[str, Any] = {
            k: v
            for k, v in {
                "renewal_date": renewal_date,
                "metadata": metadata.to_dict() if metadata is not None else None,
            }.items()
            if v is not None
        }
        if not isinstance(idempotency_key, NotGiven):
            body["idempotency_key"] = idempotency_key
        if not isinstance(reason, NotGiven):
            body["reason"] = reason
        _validate_model(
            body,
            {
                "name": "SdkPreviewSubscriptionRenewalDateChangeRequestApplicationJson",
                "required": ["renewal_date"],
                "properties": {
                    "idempotency_key": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "string",
                            "constraints": {"maxLength": 128},
                        },
                    },
                    "renewal_date": {
                        "kind": "primitive",
                        "type": "string",
                        "format": "date-time",
                    },
                    "reason": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "string",
                            "constraints": {"maxLength": 500},
                        },
                    },
                    "metadata": {"kind": "any"},
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return self._client.request(
            method="post",
            path=(
                "v2",
                "subscriptions",
                str(product_subscription),
                "actions",
                "change-renewal-date",
                "preview",
            ),
            body=body,
            model=SdkPreviewSubscriptionRenewalDateChangeResponseValue200ApplicationJson,
            request_options=request_options,
        )

    def confirm_renewal_date(
        self,
        product_subscription: int,
        *,
        renewal_date: str,
        idempotency_key: Union[str, None, NotGiven] = NOT_GIVEN,
        preview_token: Union[str, None, NotGiven] = NOT_GIVEN,
        reason: Union[str, None, NotGiven] = NOT_GIVEN,
        metadata: Optional[
            ConfirmSubscriptionRenewalDateChangeRequestApplicationJsonPropertyMetadata
        ] = None,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkConfirmSubscriptionRenewalDateChangeResponseValue200ApplicationJson:
        """Confirm a subscription renewal date change

        Confirm a subscription renewal date change. A preview token is required when the variant's billing-date policy requires previews. The current Stripe and PayPal provider flows do not support renewal-date changes and return 422. Check shift_billing_date capabilities before sending a request. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            product_subscription: The productSubscription path parameter.
            idempotency_key: Optional idempotency key. You may also send this as the Idempotency-Key header.
            renewal_date: The renewal date.
            preview_token: The preview token.
            reason: The reason.
            metadata: The metadata.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkConfirmSubscriptionRenewalDateChangeResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": True,
            "operation_id": "confirmSubscriptionRenewalDateChange",
        }
        body: Dict[str, Any] = {
            k: v
            for k, v in {
                "renewal_date": renewal_date,
                "metadata": metadata.to_dict() if metadata is not None else None,
            }.items()
            if v is not None
        }
        if not isinstance(idempotency_key, NotGiven):
            body["idempotency_key"] = idempotency_key
        if not isinstance(preview_token, NotGiven):
            body["preview_token"] = preview_token
        if not isinstance(reason, NotGiven):
            body["reason"] = reason
        _validate_model(
            body,
            {
                "name": "SdkConfirmSubscriptionRenewalDateChangeRequestApplicationJson",
                "required": ["renewal_date"],
                "properties": {
                    "idempotency_key": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "string",
                            "constraints": {"maxLength": 128},
                        },
                    },
                    "renewal_date": {
                        "kind": "primitive",
                        "type": "string",
                        "format": "date-time",
                    },
                    "preview_token": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "string",
                            "constraints": {"maxLength": 128},
                        },
                    },
                    "reason": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "string",
                            "constraints": {"maxLength": 500},
                        },
                    },
                    "metadata": {"kind": "any"},
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return self._client.request(
            method="post",
            path=(
                "v2",
                "subscriptions",
                str(product_subscription),
                "actions",
                "change-renewal-date",
                "confirm",
            ),
            body=body,
            model=SdkConfirmSubscriptionRenewalDateChangeResponseValue200ApplicationJson,
            request_options=request_options,
        )

    def list_subscriptions(
        self,
        *,
        limit: Optional[int] = None,
        before: Optional[str] = None,
        after: Optional[str] = None,
        order: Optional[str] = None,
        request_options: Optional[RequestOptions] = None,
    ) -> SyncPage[ListSubscriptionsResponseValue200ApplicationJsonPropertyDataItem]:
        """List subscriptions

        List page-based subscriptions with canonical customer and deprecated provider aliases. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            limit: The limit.
            before: The before.
            after: The after.
            order: The order.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SyncPage[ListSubscriptionsResponseValue200ApplicationJsonPropertyDataItem]

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            ConflictError: If a conflict occurs (409).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "listSubscriptions",
        }
        params = {
            k: v
            for k, v in {
                "limit": limit,
                "before": before,
                "after": after,
                "order": order,
            }.items()
            if v is not None
        }
        return self._client.request_page(
            method="get",
            path=("v2", "subscriptions"),
            model=ListSubscriptionsResponseValue200ApplicationJsonPropertyDataItem,
            params=params,
            request_options=request_options,
        )

    def search_subscriptions(
        self,
        *,
        search: Optional[str] = None,
        status: Optional[str] = None,
        limit: Optional[int] = None,
        before: Optional[str] = None,
        after: Optional[str] = None,
        order: Optional[str] = None,
        request_options: Optional[RequestOptions] = None,
    ) -> SyncPage[SearchSubscriptionsResponseValue200ApplicationJsonPropertyDataItem]:
        """Search subscriptions

        Search while preserving page-based pagination. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            search: The search.
            status: The status.
            limit: The limit.
            before: The before.
            after: The after.
            order: The order.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SyncPage[SearchSubscriptionsResponseValue200ApplicationJsonPropertyDataItem]

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            ConflictError: If a conflict occurs (409).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "searchSubscriptions",
        }
        body: Dict[str, Any] = {
            k: v
            for k, v in {
                "search": search,
                "status": status,
            }.items()
            if v is not None
        }
        params = {
            k: v
            for k, v in {
                "limit": limit,
                "before": before,
                "after": after,
                "order": order,
            }.items()
            if v is not None
        }
        return self._client.request_page(
            method="post",
            path=("v2", "subscriptions", "search"),
            model=SearchSubscriptionsResponseValue200ApplicationJsonPropertyDataItem,
            body=body,
            params=params,
            request_options=request_options,
        )

    def get_subscription(
        self,
        product_subscription: int,
        *,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkGetSubscriptionResponseValue200ApplicationJson:
        """Retrieve a subscription

        Retrieve by SellApp subscription record ID. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            product_subscription: The productSubscription identifier.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkGetSubscriptionResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            ConflictError: If a conflict occurs (409).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "getSubscription",
        }
        return self._client.request(
            method="get",
            path=("v2", "subscriptions", str(product_subscription)),
            model=SdkGetSubscriptionResponseValue200ApplicationJson,
            request_options=request_options,
        )


class AsyncSubscriptions:
    """Subscriptions API resources (async)."""

    def __init__(self, client: AsyncSellAppClient) -> None:
        self._client = client
        self.with_raw_response = WithRawResponse(self)

    async def cancel(
        self,
        subscription: int,
        *,
        cancel_at_period_end: bool,
        refund_last_payment: Optional[bool] = None,
        pro_rated_refund: Optional[bool] = None,
        idempotency_key: Union[str, None, NotGiven] = NOT_GIVEN,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkCancelSubscriptionResponseValue200ApplicationJson:
        """Cancel a subscription

        Cancel an active subscription now or at period end. cancel_at_period_end is required. Refund flags are valid only when it is false; period-end cancellation does not refund the last payment. Refunds are off by default. refund_last_payment requests a real refund; pro_rated_refund requires refund_last_payment and defaults to false. Cancellation can succeed even if the requested refund fails. A 200 response or confirmed cancellation does not prove a refund, and the action response does not expose the provider refund result. Check the order and payment-provider refund records before retrying; do not replay cancellation as a refund-recovery step. Reuse an idempotency key only with identical action input and actor. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            subscription: The subscription path parameter.
            cancel_at_period_end: The cancel at period end.
            refund_last_payment: Request a real refund of the latest payment during immediate cancellation. Cancellation success does not prove refund success; verify provider/order refund records.
            pro_rated_refund: Request a prorated refund. Requires refund_last_payment=true. False requests a full latest-payment refund when refund_last_payment is enabled.
            idempotency_key: Optional idempotency key. You may also send this as the Idempotency-Key header.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkCancelSubscriptionResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": True,
            "operation_id": "cancelSubscription",
        }
        body: Dict[str, Any] = {
            k: v
            for k, v in {
                "cancel_at_period_end": cancel_at_period_end,
                "refund_last_payment": refund_last_payment,
                "pro_rated_refund": pro_rated_refund,
            }.items()
            if v is not None
        }
        if not isinstance(idempotency_key, NotGiven):
            body["idempotency_key"] = idempotency_key
        _validate_model(
            body,
            {
                "name": "SdkCancelSubscriptionRequestApplicationJson",
                "required": ["cancel_at_period_end"],
                "properties": {
                    "cancel_at_period_end": {"kind": "primitive", "type": "boolean"},
                    "refund_last_payment": {"kind": "primitive", "type": "boolean"},
                    "pro_rated_refund": {"kind": "primitive", "type": "boolean"},
                    "idempotency_key": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "string",
                            "constraints": {"maxLength": 128},
                        },
                    },
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return await self._client.request(
            method="patch",
            path=("v2", "subscriptions", str(subscription), "cancel"),
            body=body,
            model=SdkCancelSubscriptionResponseValue200ApplicationJson,
            request_options=request_options,
        )

    async def get_capabilities(
        self,
        product_subscription: int,
        *,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkGetSubscriptionCapabilitiesResponseValue200ApplicationJson:
        """View subscription capabilities

        Return the lifecycle actions currently supported for a subscription. Capability status controls whether an actor can pause, resume, cancel, update the payment method, change plan, or shift the billing date. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            product_subscription: The productSubscription path parameter.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkGetSubscriptionCapabilitiesResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "getSubscriptionCapabilities",
        }
        return await self._client.request(
            method="get",
            path=("v2", "subscriptions", str(product_subscription), "capabilities"),
            model=SdkGetSubscriptionCapabilitiesResponseValue200ApplicationJson,
            request_options=request_options,
        )

    async def cancel_at_period_end(
        self,
        product_subscription: int,
        *,
        idempotency_key: Union[str, None, NotGiven] = NOT_GIVEN,
        reason: Union[str, None, NotGiven] = NOT_GIVEN,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkCancelSubscriptionAtPeriodEndResponseValue200ApplicationJson:
        """Cancel a subscription at period end

        Schedule cancellation at the end of the current billing period without requesting a refund. Inspect capabilities first. The response records the cancellation action; retrieve the subscription to observe its current state. Reuse an idempotency key only with identical action input and actor. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            product_subscription: The productSubscription path parameter.
            idempotency_key: Optional idempotency key. You may also send this as the Idempotency-Key header.
            reason: The reason.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkCancelSubscriptionAtPeriodEndResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": True,
            "operation_id": "cancelSubscriptionAtPeriodEnd",
        }
        body: Dict[str, Any] = {}
        if not isinstance(idempotency_key, NotGiven):
            body["idempotency_key"] = idempotency_key
        if not isinstance(reason, NotGiven):
            body["reason"] = reason
        _validate_model(
            body,
            {
                "name": "SdkCancelSubscriptionAtPeriodEndRequestApplicationJson",
                "required": [],
                "properties": {
                    "idempotency_key": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "string",
                            "constraints": {"maxLength": 128},
                        },
                    },
                    "reason": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "string",
                            "constraints": {"maxLength": 500},
                        },
                    },
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return await self._client.request(
            method="post",
            path=(
                "v2",
                "subscriptions",
                str(product_subscription),
                "actions",
                "cancel-period-end",
            ),
            body=body,
            model=SdkCancelSubscriptionAtPeriodEndResponseValue200ApplicationJson,
            request_options=request_options,
        )

    async def cancel_immediately(
        self,
        product_subscription: int,
        *,
        idempotency_key: Union[str, None, NotGiven] = NOT_GIVEN,
        reason: Union[str, None, NotGiven] = NOT_GIVEN,
        refund_last_payment: Optional[bool] = None,
        pro_rated_refund: Optional[bool] = None,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkCancelSubscriptionImmediatelyResponseValue200ApplicationJson:
        """Cancel a subscription immediately

        Request immediate subscription cancellation. Inspect capabilities for provider support. This ends future billing and can change customer access. Refunds are off by default. refund_last_payment requests a real refund; pro_rated_refund requires refund_last_payment and defaults to false. Cancellation can succeed even if the requested refund fails. A 200 response or confirmed cancellation does not prove a refund, and the action response does not expose the provider refund result. Check the order and payment-provider refund records before retrying; do not replay cancellation as a refund-recovery step. Reuse an idempotency key only with identical action input and actor. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            product_subscription: The productSubscription path parameter.
            idempotency_key: Optional idempotency key. You may also send this as the Idempotency-Key header.
            reason: The reason.
            refund_last_payment: Request a real refund of the latest payment during immediate cancellation. Cancellation success does not prove refund success; verify provider/order refund records.
            pro_rated_refund: Request a prorated refund. Requires refund_last_payment=true. False requests a full latest-payment refund when refund_last_payment is enabled.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkCancelSubscriptionImmediatelyResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": True,
            "operation_id": "cancelSubscriptionImmediately",
        }
        body: Dict[str, Any] = {
            k: v
            for k, v in {
                "refund_last_payment": refund_last_payment,
                "pro_rated_refund": pro_rated_refund,
            }.items()
            if v is not None
        }
        if not isinstance(idempotency_key, NotGiven):
            body["idempotency_key"] = idempotency_key
        if not isinstance(reason, NotGiven):
            body["reason"] = reason
        _validate_model(
            body,
            {
                "name": "SdkCancelSubscriptionImmediatelyRequestApplicationJson",
                "required": [],
                "properties": {
                    "idempotency_key": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "string",
                            "constraints": {"maxLength": 128},
                        },
                    },
                    "reason": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "string",
                            "constraints": {"maxLength": 500},
                        },
                    },
                    "refund_last_payment": {"kind": "primitive", "type": "boolean"},
                    "pro_rated_refund": {"kind": "primitive", "type": "boolean"},
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return await self._client.request(
            method="post",
            path=(
                "v2",
                "subscriptions",
                str(product_subscription),
                "actions",
                "cancel-immediately",
            ),
            body=body,
            model=SdkCancelSubscriptionImmediatelyResponseValue200ApplicationJson,
            request_options=request_options,
        )

    async def pause(
        self,
        product_subscription: int,
        *,
        idempotency_key: Union[str, None, NotGiven] = NOT_GIVEN,
        resume_at: Union[str, None, NotGiven] = NOT_GIVEN,
        reason: Union[str, None, NotGiven] = NOT_GIVEN,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkPauseSubscriptionResponseValue200ApplicationJson:
        """Pause a subscription

        Pause a subscription after checking capabilities. Automatic resume dates are supported only by the current Stripe flow; use a future resume_at when supplied. Other provider pauses may need an explicit resume action. Customer-originated pauses are also subject to store pause policies. Reuse an idempotency key only with identical input and actor, and retrieve the subscription after the action. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            product_subscription: The productSubscription path parameter.
            idempotency_key: Optional idempotency key. You may also send this as the Idempotency-Key header.
            resume_at: Use a future date-time at execution time. Automatic resume dates are supported only for Stripe; the shown date is illustrative.
            reason: The reason.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkPauseSubscriptionResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": True,
            "operation_id": "pauseSubscription",
        }
        body: Dict[str, Any] = {}
        if not isinstance(idempotency_key, NotGiven):
            body["idempotency_key"] = idempotency_key
        if not isinstance(resume_at, NotGiven):
            body["resume_at"] = resume_at
        if not isinstance(reason, NotGiven):
            body["reason"] = reason
        _validate_model(
            body,
            {
                "name": "SdkPauseSubscriptionRequestApplicationJson",
                "required": [],
                "properties": {
                    "idempotency_key": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "string",
                            "constraints": {"maxLength": 128},
                        },
                    },
                    "resume_at": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "string",
                            "format": "date-time",
                        },
                    },
                    "reason": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "string",
                            "constraints": {"maxLength": 500},
                        },
                    },
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return await self._client.request(
            method="post",
            path=("v2", "subscriptions", str(product_subscription), "actions", "pause"),
            body=body,
            model=SdkPauseSubscriptionResponseValue200ApplicationJson,
            request_options=request_options,
        )

    async def resume(
        self,
        product_subscription: int,
        *,
        idempotency_key: Union[str, None, NotGiven] = NOT_GIVEN,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkResumeSubscriptionResponseValue200ApplicationJson:
        """Resume a subscription

        Resume a paused subscription and restore related entitlements where possible. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            product_subscription: The productSubscription path parameter.
            idempotency_key: Optional idempotency key. You may also send this as the Idempotency-Key header.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkResumeSubscriptionResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": True,
            "operation_id": "resumeSubscription",
        }
        body: Dict[str, Any] = {}
        if not isinstance(idempotency_key, NotGiven):
            body["idempotency_key"] = idempotency_key
        _validate_model(
            body,
            {
                "name": "SdkResumeSubscriptionRequestApplicationJson",
                "required": [],
                "properties": {
                    "idempotency_key": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "string",
                            "constraints": {"maxLength": 128},
                        },
                    }
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return await self._client.request(
            method="post",
            path=(
                "v2",
                "subscriptions",
                str(product_subscription),
                "actions",
                "resume",
            ),
            body=body,
            model=SdkResumeSubscriptionResponseValue200ApplicationJson,
            request_options=request_options,
        )

    async def update_payment_method(
        self,
        product_subscription: int,
        *,
        idempotency_key: Union[str, None, NotGiven] = NOT_GIVEN,
        request_options: Optional[RequestOptions] = None,
    ) -> Any:
        """Update a subscription payment method

        This action belongs to the customer. Seller API requests return a 422 validation error. Check subscription capabilities and use the customer flow; never expose a seller API key to work around this restriction. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            product_subscription: The productSubscription path parameter.
            idempotency_key: Optional idempotency key. You may also send this as the Idempotency-Key header.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            Any

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": True,
            "operation_id": "updateSubscriptionPaymentMethod",
        }
        body: Dict[str, Any] = {}
        if not isinstance(idempotency_key, NotGiven):
            body["idempotency_key"] = idempotency_key
        _validate_model(
            body,
            {
                "name": "SdkUpdateSubscriptionPaymentMethodRequestApplicationJson",
                "required": [],
                "properties": {
                    "idempotency_key": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "string",
                            "constraints": {"maxLength": 128},
                        },
                    }
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return await self._client.request(
            method="post",
            path=(
                "v2",
                "subscriptions",
                str(product_subscription),
                "actions",
                "update-payment-method",
            ),
            body=body,
            request_options=request_options,
        )

    async def preview_plan_change(
        self,
        product_subscription: int,
        *,
        target_variant_id: int,
        idempotency_key: Union[str, None, NotGiven] = NOT_GIVEN,
        effective_timing: Optional[
            Union[
                SdkPreviewSubscriptionPlanChangeRequestApplicationJsonEffectiveTiming,
                str,
            ]
        ] = None,
        proration_behavior: Optional[
            Union[
                SdkPreviewSubscriptionPlanChangeRequestApplicationJsonProrationBehavior,
                str,
            ]
        ] = None,
        metadata: Optional[
            PreviewSubscriptionPlanChangeRequestApplicationJsonPropertyMetadata
        ] = None,
        request_options: Optional[RequestOptions] = None,
    ) -> Any:
        """Preview a subscription plan change

        This action belongs to the customer. Seller API requests return a 422 validation error. Check subscription capabilities and use the customer flow; never expose a seller API key to work around this restriction. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            product_subscription: The productSubscription path parameter.
            idempotency_key: Optional idempotency key. You may also send this as the Idempotency-Key header.
            target_variant_id: The target variant id.
            effective_timing: The effective timing.
            proration_behavior: The proration behavior.
            metadata: The metadata.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            Any

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": True,
            "operation_id": "previewSubscriptionPlanChange",
        }
        body: Dict[str, Any] = {
            k: v
            for k, v in {
                "target_variant_id": target_variant_id,
                "effective_timing": enum_value(effective_timing)
                if effective_timing is not None
                else None,
                "proration_behavior": enum_value(proration_behavior)
                if proration_behavior is not None
                else None,
                "metadata": metadata.to_dict() if metadata is not None else None,
            }.items()
            if v is not None
        }
        if not isinstance(idempotency_key, NotGiven):
            body["idempotency_key"] = idempotency_key
        _validate_model(
            body,
            {
                "name": "SdkPreviewSubscriptionPlanChangeRequestApplicationJson",
                "required": ["target_variant_id"],
                "properties": {
                    "idempotency_key": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "string",
                            "constraints": {"maxLength": 128},
                        },
                    },
                    "target_variant_id": {"kind": "primitive", "type": "integer"},
                    "effective_timing": {
                        "kind": "enum",
                        "values": ["immediate", "next_renewal"],
                    },
                    "proration_behavior": {
                        "kind": "enum",
                        "values": [
                            "invoice_now",
                            "credit_next_invoice",
                            "none",
                            "provider_default",
                        ],
                    },
                    "metadata": {"kind": "any"},
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return await self._client.request(
            method="post",
            path=(
                "v2",
                "subscriptions",
                str(product_subscription),
                "actions",
                "change-plan",
                "preview",
            ),
            body=body,
            request_options=request_options,
        )

    async def confirm_plan_change(
        self,
        product_subscription: int,
        *,
        target_variant_id: int,
        preview_token: str,
        idempotency_key: Union[str, None, NotGiven] = NOT_GIVEN,
        effective_timing: Optional[
            Union[
                SdkConfirmSubscriptionPlanChangeRequestApplicationJsonEffectiveTiming,
                str,
            ]
        ] = None,
        proration_behavior: Optional[
            Union[
                SdkConfirmSubscriptionPlanChangeRequestApplicationJsonProrationBehavior,
                str,
            ]
        ] = None,
        metadata: Optional[
            ConfirmSubscriptionPlanChangeRequestApplicationJsonPropertyMetadata
        ] = None,
        request_options: Optional[RequestOptions] = None,
    ) -> Any:
        """Confirm a subscription plan change

        This action belongs to the customer. Seller API requests return a 422 validation error. Check subscription capabilities and use the customer flow; never expose a seller API key to work around this restriction. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            product_subscription: The productSubscription path parameter.
            idempotency_key: Optional idempotency key. You may also send this as the Idempotency-Key header.
            target_variant_id: The target variant id.
            preview_token: The preview token.
            effective_timing: The effective timing.
            proration_behavior: The proration behavior.
            metadata: The metadata.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            Any

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": True,
            "operation_id": "confirmSubscriptionPlanChange",
        }
        body: Dict[str, Any] = {
            k: v
            for k, v in {
                "target_variant_id": target_variant_id,
                "preview_token": preview_token,
                "effective_timing": enum_value(effective_timing)
                if effective_timing is not None
                else None,
                "proration_behavior": enum_value(proration_behavior)
                if proration_behavior is not None
                else None,
                "metadata": metadata.to_dict() if metadata is not None else None,
            }.items()
            if v is not None
        }
        if not isinstance(idempotency_key, NotGiven):
            body["idempotency_key"] = idempotency_key
        _validate_model(
            body,
            {
                "name": "SdkConfirmSubscriptionPlanChangeRequestApplicationJson",
                "required": ["target_variant_id", "preview_token"],
                "properties": {
                    "idempotency_key": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "string",
                            "constraints": {"maxLength": 128},
                        },
                    },
                    "target_variant_id": {"kind": "primitive", "type": "integer"},
                    "preview_token": {
                        "kind": "primitive",
                        "type": "string",
                        "constraints": {"maxLength": 128},
                    },
                    "effective_timing": {
                        "kind": "enum",
                        "values": ["immediate", "next_renewal"],
                    },
                    "proration_behavior": {
                        "kind": "enum",
                        "values": [
                            "invoice_now",
                            "credit_next_invoice",
                            "none",
                            "provider_default",
                        ],
                    },
                    "metadata": {"kind": "any"},
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return await self._client.request(
            method="post",
            path=(
                "v2",
                "subscriptions",
                str(product_subscription),
                "actions",
                "change-plan",
                "confirm",
            ),
            body=body,
            request_options=request_options,
        )

    async def preview_renewal_date(
        self,
        product_subscription: int,
        *,
        renewal_date: str,
        idempotency_key: Union[str, None, NotGiven] = NOT_GIVEN,
        reason: Union[str, None, NotGiven] = NOT_GIVEN,
        metadata: Optional[
            PreviewSubscriptionRenewalDateChangeRequestApplicationJsonPropertyMetadata
        ] = None,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkPreviewSubscriptionRenewalDateChangeResponseValue200ApplicationJson:
        """Preview a subscription renewal date change

        Preview a new renewal date before confirmation when the subscription's billing-date policy requires a preview. The current Stripe and PayPal provider flows do not support renewal-date changes and return 422. Check shift_billing_date capabilities before sending a request. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            product_subscription: The productSubscription path parameter.
            idempotency_key: Optional idempotency key. You may also send this as the Idempotency-Key header.
            renewal_date: The renewal date.
            reason: The reason.
            metadata: The metadata.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkPreviewSubscriptionRenewalDateChangeResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": True,
            "operation_id": "previewSubscriptionRenewalDateChange",
        }
        body: Dict[str, Any] = {
            k: v
            for k, v in {
                "renewal_date": renewal_date,
                "metadata": metadata.to_dict() if metadata is not None else None,
            }.items()
            if v is not None
        }
        if not isinstance(idempotency_key, NotGiven):
            body["idempotency_key"] = idempotency_key
        if not isinstance(reason, NotGiven):
            body["reason"] = reason
        _validate_model(
            body,
            {
                "name": "SdkPreviewSubscriptionRenewalDateChangeRequestApplicationJson",
                "required": ["renewal_date"],
                "properties": {
                    "idempotency_key": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "string",
                            "constraints": {"maxLength": 128},
                        },
                    },
                    "renewal_date": {
                        "kind": "primitive",
                        "type": "string",
                        "format": "date-time",
                    },
                    "reason": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "string",
                            "constraints": {"maxLength": 500},
                        },
                    },
                    "metadata": {"kind": "any"},
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return await self._client.request(
            method="post",
            path=(
                "v2",
                "subscriptions",
                str(product_subscription),
                "actions",
                "change-renewal-date",
                "preview",
            ),
            body=body,
            model=SdkPreviewSubscriptionRenewalDateChangeResponseValue200ApplicationJson,
            request_options=request_options,
        )

    async def confirm_renewal_date(
        self,
        product_subscription: int,
        *,
        renewal_date: str,
        idempotency_key: Union[str, None, NotGiven] = NOT_GIVEN,
        preview_token: Union[str, None, NotGiven] = NOT_GIVEN,
        reason: Union[str, None, NotGiven] = NOT_GIVEN,
        metadata: Optional[
            ConfirmSubscriptionRenewalDateChangeRequestApplicationJsonPropertyMetadata
        ] = None,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkConfirmSubscriptionRenewalDateChangeResponseValue200ApplicationJson:
        """Confirm a subscription renewal date change

        Confirm a subscription renewal date change. A preview token is required when the variant's billing-date policy requires previews. The current Stripe and PayPal provider flows do not support renewal-date changes and return 422. Check shift_billing_date capabilities before sending a request. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            product_subscription: The productSubscription path parameter.
            idempotency_key: Optional idempotency key. You may also send this as the Idempotency-Key header.
            renewal_date: The renewal date.
            preview_token: The preview token.
            reason: The reason.
            metadata: The metadata.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkConfirmSubscriptionRenewalDateChangeResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": True,
            "operation_id": "confirmSubscriptionRenewalDateChange",
        }
        body: Dict[str, Any] = {
            k: v
            for k, v in {
                "renewal_date": renewal_date,
                "metadata": metadata.to_dict() if metadata is not None else None,
            }.items()
            if v is not None
        }
        if not isinstance(idempotency_key, NotGiven):
            body["idempotency_key"] = idempotency_key
        if not isinstance(preview_token, NotGiven):
            body["preview_token"] = preview_token
        if not isinstance(reason, NotGiven):
            body["reason"] = reason
        _validate_model(
            body,
            {
                "name": "SdkConfirmSubscriptionRenewalDateChangeRequestApplicationJson",
                "required": ["renewal_date"],
                "properties": {
                    "idempotency_key": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "string",
                            "constraints": {"maxLength": 128},
                        },
                    },
                    "renewal_date": {
                        "kind": "primitive",
                        "type": "string",
                        "format": "date-time",
                    },
                    "preview_token": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "string",
                            "constraints": {"maxLength": 128},
                        },
                    },
                    "reason": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "string",
                            "constraints": {"maxLength": 500},
                        },
                    },
                    "metadata": {"kind": "any"},
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return await self._client.request(
            method="post",
            path=(
                "v2",
                "subscriptions",
                str(product_subscription),
                "actions",
                "change-renewal-date",
                "confirm",
            ),
            body=body,
            model=SdkConfirmSubscriptionRenewalDateChangeResponseValue200ApplicationJson,
            request_options=request_options,
        )

    async def list_subscriptions(
        self,
        *,
        limit: Optional[int] = None,
        before: Optional[str] = None,
        after: Optional[str] = None,
        order: Optional[str] = None,
        request_options: Optional[RequestOptions] = None,
    ) -> AsyncPage[ListSubscriptionsResponseValue200ApplicationJsonPropertyDataItem]:
        """List subscriptions

        List page-based subscriptions with canonical customer and deprecated provider aliases. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            limit: The limit.
            before: The before.
            after: The after.
            order: The order.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            AsyncPage[ListSubscriptionsResponseValue200ApplicationJsonPropertyDataItem]

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            ConflictError: If a conflict occurs (409).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "listSubscriptions",
        }
        params = {
            k: v
            for k, v in {
                "limit": limit,
                "before": before,
                "after": after,
                "order": order,
            }.items()
            if v is not None
        }
        return await self._client.request_page(
            method="get",
            path=("v2", "subscriptions"),
            model=ListSubscriptionsResponseValue200ApplicationJsonPropertyDataItem,
            params=params,
            request_options=request_options,
        )

    async def search_subscriptions(
        self,
        *,
        search: Optional[str] = None,
        status: Optional[str] = None,
        limit: Optional[int] = None,
        before: Optional[str] = None,
        after: Optional[str] = None,
        order: Optional[str] = None,
        request_options: Optional[RequestOptions] = None,
    ) -> AsyncPage[SearchSubscriptionsResponseValue200ApplicationJsonPropertyDataItem]:
        """Search subscriptions

        Search while preserving page-based pagination. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            search: The search.
            status: The status.
            limit: The limit.
            before: The before.
            after: The after.
            order: The order.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            AsyncPage[SearchSubscriptionsResponseValue200ApplicationJsonPropertyDataItem]

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            ConflictError: If a conflict occurs (409).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "searchSubscriptions",
        }
        body: Dict[str, Any] = {
            k: v
            for k, v in {
                "search": search,
                "status": status,
            }.items()
            if v is not None
        }
        params = {
            k: v
            for k, v in {
                "limit": limit,
                "before": before,
                "after": after,
                "order": order,
            }.items()
            if v is not None
        }
        return await self._client.request_page(
            method="post",
            path=("v2", "subscriptions", "search"),
            model=SearchSubscriptionsResponseValue200ApplicationJsonPropertyDataItem,
            body=body,
            params=params,
            request_options=request_options,
        )

    async def get_subscription(
        self,
        product_subscription: int,
        *,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkGetSubscriptionResponseValue200ApplicationJson:
        """Retrieve a subscription

        Retrieve by SellApp subscription record ID. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            product_subscription: The productSubscription identifier.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkGetSubscriptionResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            ConflictError: If a conflict occurs (409).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "getSubscription",
        }
        return await self._client.request(
            method="get",
            path=("v2", "subscriptions", str(product_subscription)),
            model=SdkGetSubscriptionResponseValue200ApplicationJson,
            request_options=request_options,
        )
