# This file is auto-generated by oagen. Do not edit.

from __future__ import annotations

from typing import TYPE_CHECKING, Any, Dict, List, Optional, Union

from .._base_client import WithRawResponse

if TYPE_CHECKING:
    from .._client import AsyncSellAppClient, SellAppClient

from sellapp_sdk.blacklists.models.list_tickets_response_value_200_application_json_property_data_item import (
    ListTicketsResponseValue200ApplicationJsonPropertyDataItem,
)
from sellapp_sdk.blacklists.models.sdk_get_ticket_response_value_200_application_json import (
    SdkGetTicketResponseValue200ApplicationJson,
)
from sellapp_sdk.blacklists.models.sdk_update_ticket_response_value_200_application_json import (
    SdkUpdateTicketResponseValue200ApplicationJson,
)
from sellapp_sdk.blacklists.models.sdk_v_2_get_ticket_response_value_200_application_json import (
    SdkV2GetTicketResponseValue200ApplicationJson,
)
from sellapp_sdk.blacklists.models.search_tickets_request_application_json_property_filters_item import (
    SearchTicketsRequestApplicationJsonPropertyFiltersItem,
)
from sellapp_sdk.blacklists.models.search_tickets_request_application_json_property_includes_item import (
    SearchTicketsRequestApplicationJsonPropertyIncludesItem,
)
from sellapp_sdk.blacklists.models.search_tickets_request_application_json_property_search import (
    SearchTicketsRequestApplicationJsonPropertySearch,
)
from sellapp_sdk.blacklists.models.search_tickets_request_application_json_property_sort_item import (
    SearchTicketsRequestApplicationJsonPropertySortItem,
)
from sellapp_sdk.blacklists.models.search_tickets_response_value_200_application_json_property_data_item import (
    SearchTicketsResponseValue200ApplicationJsonPropertyDataItem,
)
from sellapp_sdk.blacklists.models.v_2_list_tickets_response_value_200_application_json_property_data_item import (
    V2ListTicketsResponseValue200ApplicationJsonPropertyDataItem,
)
from sellapp_sdk.blacklists.models.v_2_search_tickets_request_application_json_property_filters_item import (
    V2SearchTicketsRequestApplicationJsonPropertyFiltersItem,
)
from sellapp_sdk.blacklists.models.v_2_search_tickets_request_application_json_property_includes_item import (
    V2SearchTicketsRequestApplicationJsonPropertyIncludesItem,
)
from sellapp_sdk.blacklists.models.v_2_search_tickets_request_application_json_property_search import (
    V2SearchTicketsRequestApplicationJsonPropertySearch,
)
from sellapp_sdk.blacklists.models.v_2_search_tickets_request_application_json_property_sort_item import (
    V2SearchTicketsRequestApplicationJsonPropertySortItem,
)
from sellapp_sdk.blacklists.models.v_2_search_tickets_response_value_200_application_json_property_data_item import (
    V2SearchTicketsResponseValue200ApplicationJsonPropertyDataItem,
)
from sellapp_sdk.common.models.sdk_update_ticket_request_application_json_status import (
    SdkUpdateTicketRequestApplicationJsonStatus,
)

from .._pagination import AsyncPage, SyncPage
from .._types import RequestOptions, _validate_model, enum_value


class Tickets:
    """Tickets API resources."""

    def __init__(self, client: SellAppClient) -> None:
        self._client = client
        self.with_raw_response = WithRawResponse(self)

    def list(
        self,
        *,
        limit: Optional[int] = None,
        before: Optional[str] = None,
        after: Optional[str] = None,
        order: Optional[str] = None,
        page: Optional[int] = None,
        request_options: Optional[RequestOptions] = None,
    ) -> SyncPage[ListTicketsResponseValue200ApplicationJsonPropertyDataItem]:
        """List all tickets

        List your store's support tickets, 15 per page by default.

        Args:
            limit: Number of items to return per page. Defaults to `15`.
            before: The before.
            after: The after.
            order: The order.
            page: Page number to return. Defaults to `1`.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SyncPage[ListTicketsResponseValue200ApplicationJsonPropertyDataItem]

        Raises:
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [
                        {"schemeName": "bearerAuth", "scopes": []},
                        {"schemeName": "storeAuth", "scopes": []},
                    ]
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": False,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": True,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": False,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "listTickets",
        }
        params = {
            k: v
            for k, v in {
                "limit": limit,
                "before": before,
                "after": after,
                "order": order,
                "page": page,
            }.items()
            if v is not None
        }
        return self._client.request_page(
            method="get",
            path=("v1", "tickets"),
            model=ListTicketsResponseValue200ApplicationJsonPropertyDataItem,
            params=params,
            request_options=request_options,
        )

    def get(
        self,
        ticket: int,
        *,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkGetTicketResponseValue200ApplicationJson:
        """Retrieve specific ticket

        Retrieve a support ticket by its ID.

        Args:
            ticket: The ticket path parameter.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkGetTicketResponseValue200ApplicationJson

        Raises:
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [
                        {"schemeName": "bearerAuth", "scopes": []},
                        {"schemeName": "storeAuth", "scopes": []},
                    ]
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": False,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": True,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": False,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "getTicket",
        }
        return self._client.request(
            method="get",
            path=("v1", "tickets", str(ticket)),
            model=SdkGetTicketResponseValue200ApplicationJson,
            request_options=request_options,
        )

    def search(
        self,
        *,
        filters: Optional[
            List[SearchTicketsRequestApplicationJsonPropertyFiltersItem]
        ] = None,
        sort: Optional[
            List[SearchTicketsRequestApplicationJsonPropertySortItem]
        ] = None,
        search: Optional[SearchTicketsRequestApplicationJsonPropertySearch] = None,
        includes: Optional[
            List[SearchTicketsRequestApplicationJsonPropertyIncludesItem]
        ] = None,
        limit: Optional[int] = None,
        before: Optional[str] = None,
        after: Optional[str] = None,
        order: Optional[str] = None,
        request_options: Optional[RequestOptions] = None,
    ) -> SyncPage[SearchTicketsResponseValue200ApplicationJsonPropertyDataItem]:
        """Search tickets

        Search tickets using JSON body filters, search terms, includes, and sort instructions.

        Args:
            filters: The filters.
            sort: The sort.
            search: The search.
            includes: The includes.
            limit: The limit.
            before: The before.
            after: The after.
            order: The order.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SyncPage[SearchTicketsResponseValue200ApplicationJsonPropertyDataItem]

        Raises:
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [
                        {"schemeName": "bearerAuth", "scopes": []},
                        {"schemeName": "storeAuth", "scopes": []},
                    ]
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": False,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": True,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": False,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "searchTickets",
        }
        body: Dict[str, Any] = {
            k: v
            for k, v in {
                "filters": [item.to_dict() for item in filters]
                if filters is not None
                else None,
                "sort": [item.to_dict() for item in sort] if sort is not None else None,
                "search": search.to_dict() if search is not None else None,
                "includes": [item.to_dict() for item in includes]
                if includes is not None
                else None,
            }.items()
            if v is not None
        }
        params = {
            k: v
            for k, v in {
                "limit": limit,
                "before": before,
                "after": after,
                "order": order,
            }.items()
            if v is not None
        }
        return self._client.request_page(
            method="post",
            path=("v1", "tickets", "search"),
            model=SearchTicketsResponseValue200ApplicationJsonPropertyDataItem,
            body=body,
            params=params,
            request_options=request_options,
        )

    def v2_list_tickets(
        self,
        *,
        limit: Optional[int] = None,
        before: Optional[str] = None,
        after: Optional[str] = None,
        order: Optional[str] = None,
        page: Optional[int] = None,
        request_options: Optional[RequestOptions] = None,
    ) -> SyncPage[V2ListTicketsResponseValue200ApplicationJsonPropertyDataItem]:
        """List all tickets

        List your store's support tickets, 15 per page by default. This v2 route is store-scoped. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            limit: Number of items to return per page. Defaults to `15`.
            before: The before.
            after: The after.
            order: The order.
            page: Page number to return. Defaults to `1`.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SyncPage[V2ListTicketsResponseValue200ApplicationJsonPropertyDataItem]

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "v2ListTickets",
        }
        params = {
            k: v
            for k, v in {
                "limit": limit,
                "before": before,
                "after": after,
                "order": order,
                "page": page,
            }.items()
            if v is not None
        }
        return self._client.request_page(
            method="get",
            path=("v2", "tickets"),
            model=V2ListTicketsResponseValue200ApplicationJsonPropertyDataItem,
            params=params,
            request_options=request_options,
        )

    def v2_search_tickets(
        self,
        *,
        filters: Optional[
            List[V2SearchTicketsRequestApplicationJsonPropertyFiltersItem]
        ] = None,
        sort: Optional[
            List[V2SearchTicketsRequestApplicationJsonPropertySortItem]
        ] = None,
        search: Optional[V2SearchTicketsRequestApplicationJsonPropertySearch] = None,
        includes: Optional[
            List[V2SearchTicketsRequestApplicationJsonPropertyIncludesItem]
        ] = None,
        limit: Optional[int] = None,
        before: Optional[str] = None,
        after: Optional[str] = None,
        order: Optional[str] = None,
        request_options: Optional[RequestOptions] = None,
    ) -> SyncPage[V2SearchTicketsResponseValue200ApplicationJsonPropertyDataItem]:
        """Search tickets

        Search tickets using JSON body filters, search terms, includes, and sort instructions. This v2 route is store-scoped. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            filters: The filters.
            sort: The sort.
            search: The search.
            includes: The includes.
            limit: The limit.
            before: The before.
            after: The after.
            order: The order.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SyncPage[V2SearchTicketsResponseValue200ApplicationJsonPropertyDataItem]

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "v2SearchTickets",
        }
        body: Dict[str, Any] = {
            k: v
            for k, v in {
                "filters": [item.to_dict() for item in filters]
                if filters is not None
                else None,
                "sort": [item.to_dict() for item in sort] if sort is not None else None,
                "search": search.to_dict() if search is not None else None,
                "includes": [item.to_dict() for item in includes]
                if includes is not None
                else None,
            }.items()
            if v is not None
        }
        params = {
            k: v
            for k, v in {
                "limit": limit,
                "before": before,
                "after": after,
                "order": order,
            }.items()
            if v is not None
        }
        return self._client.request_page(
            method="post",
            path=("v2", "tickets", "search"),
            model=V2SearchTicketsResponseValue200ApplicationJsonPropertyDataItem,
            body=body,
            params=params,
            request_options=request_options,
        )

    def v2_get_ticket(
        self,
        ticket: int,
        *,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkV2GetTicketResponseValue200ApplicationJson:
        """Retrieve specific ticket

        Retrieve a support ticket by its ID. This v2 route is store-scoped. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            ticket: The ticket path parameter.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkV2GetTicketResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "v2GetTicket",
        }
        return self._client.request(
            method="get",
            path=("v2", "tickets", str(ticket)),
            model=SdkV2GetTicketResponseValue200ApplicationJson,
            request_options=request_options,
        )

    def update(
        self,
        ticket: int,
        *,
        status: Optional[
            Union[SdkUpdateTicketRequestApplicationJsonStatus, str]
        ] = None,
        archived: Optional[bool] = None,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkUpdateTicketResponseValue200ApplicationJson:
        """Close, reopen, or archive a ticket

        Change ticket status or archive state in the selected store. Requires the ticket API-key ability and store ticket permission. Send status OPEN to reopen or CLOSED to close; closing sends the existing customer notification only when the status changes. Send archived true to archive or false to restore. You may send both fields together; omitted fields stay unchanged. Use the same optional Idempotency-Key and identical body after a lost response. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            ticket: The ticket.
            status: The status.
            archived: The archived.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkUpdateTicketResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": True,
            "operation_id": "updateTicket",
        }
        body: Dict[str, Any] = {
            k: v
            for k, v in {
                "status": enum_value(status) if status is not None else None,
                "archived": archived,
            }.items()
            if v is not None
        }
        _validate_model(
            body,
            {
                "name": "SdkUpdateTicketRequestApplicationJson",
                "required": [],
                "properties": {
                    "status": {"kind": "enum", "values": ["OPEN", "CLOSED"]},
                    "archived": {"kind": "primitive", "type": "boolean"},
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return self._client.request(
            method="patch",
            path=("v2", "tickets", str(ticket)),
            body=body,
            model=SdkUpdateTicketResponseValue200ApplicationJson,
            request_options=request_options,
        )


class AsyncTickets:
    """Tickets API resources (async)."""

    def __init__(self, client: AsyncSellAppClient) -> None:
        self._client = client
        self.with_raw_response = WithRawResponse(self)

    async def list(
        self,
        *,
        limit: Optional[int] = None,
        before: Optional[str] = None,
        after: Optional[str] = None,
        order: Optional[str] = None,
        page: Optional[int] = None,
        request_options: Optional[RequestOptions] = None,
    ) -> AsyncPage[ListTicketsResponseValue200ApplicationJsonPropertyDataItem]:
        """List all tickets

        List your store's support tickets, 15 per page by default.

        Args:
            limit: Number of items to return per page. Defaults to `15`.
            before: The before.
            after: The after.
            order: The order.
            page: Page number to return. Defaults to `1`.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            AsyncPage[ListTicketsResponseValue200ApplicationJsonPropertyDataItem]

        Raises:
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [
                        {"schemeName": "bearerAuth", "scopes": []},
                        {"schemeName": "storeAuth", "scopes": []},
                    ]
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": False,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": True,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": False,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "listTickets",
        }
        params = {
            k: v
            for k, v in {
                "limit": limit,
                "before": before,
                "after": after,
                "order": order,
                "page": page,
            }.items()
            if v is not None
        }
        return await self._client.request_page(
            method="get",
            path=("v1", "tickets"),
            model=ListTicketsResponseValue200ApplicationJsonPropertyDataItem,
            params=params,
            request_options=request_options,
        )

    async def get(
        self,
        ticket: int,
        *,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkGetTicketResponseValue200ApplicationJson:
        """Retrieve specific ticket

        Retrieve a support ticket by its ID.

        Args:
            ticket: The ticket path parameter.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkGetTicketResponseValue200ApplicationJson

        Raises:
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [
                        {"schemeName": "bearerAuth", "scopes": []},
                        {"schemeName": "storeAuth", "scopes": []},
                    ]
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": False,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": True,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": False,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "getTicket",
        }
        return await self._client.request(
            method="get",
            path=("v1", "tickets", str(ticket)),
            model=SdkGetTicketResponseValue200ApplicationJson,
            request_options=request_options,
        )

    async def search(
        self,
        *,
        filters: Optional[
            List[SearchTicketsRequestApplicationJsonPropertyFiltersItem]
        ] = None,
        sort: Optional[
            List[SearchTicketsRequestApplicationJsonPropertySortItem]
        ] = None,
        search: Optional[SearchTicketsRequestApplicationJsonPropertySearch] = None,
        includes: Optional[
            List[SearchTicketsRequestApplicationJsonPropertyIncludesItem]
        ] = None,
        limit: Optional[int] = None,
        before: Optional[str] = None,
        after: Optional[str] = None,
        order: Optional[str] = None,
        request_options: Optional[RequestOptions] = None,
    ) -> AsyncPage[SearchTicketsResponseValue200ApplicationJsonPropertyDataItem]:
        """Search tickets

        Search tickets using JSON body filters, search terms, includes, and sort instructions.

        Args:
            filters: The filters.
            sort: The sort.
            search: The search.
            includes: The includes.
            limit: The limit.
            before: The before.
            after: The after.
            order: The order.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            AsyncPage[SearchTicketsResponseValue200ApplicationJsonPropertyDataItem]

        Raises:
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [
                        {"schemeName": "bearerAuth", "scopes": []},
                        {"schemeName": "storeAuth", "scopes": []},
                    ]
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": False,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": True,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": False,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "searchTickets",
        }
        body: Dict[str, Any] = {
            k: v
            for k, v in {
                "filters": [item.to_dict() for item in filters]
                if filters is not None
                else None,
                "sort": [item.to_dict() for item in sort] if sort is not None else None,
                "search": search.to_dict() if search is not None else None,
                "includes": [item.to_dict() for item in includes]
                if includes is not None
                else None,
            }.items()
            if v is not None
        }
        params = {
            k: v
            for k, v in {
                "limit": limit,
                "before": before,
                "after": after,
                "order": order,
            }.items()
            if v is not None
        }
        return await self._client.request_page(
            method="post",
            path=("v1", "tickets", "search"),
            model=SearchTicketsResponseValue200ApplicationJsonPropertyDataItem,
            body=body,
            params=params,
            request_options=request_options,
        )

    async def v2_list_tickets(
        self,
        *,
        limit: Optional[int] = None,
        before: Optional[str] = None,
        after: Optional[str] = None,
        order: Optional[str] = None,
        page: Optional[int] = None,
        request_options: Optional[RequestOptions] = None,
    ) -> AsyncPage[V2ListTicketsResponseValue200ApplicationJsonPropertyDataItem]:
        """List all tickets

        List your store's support tickets, 15 per page by default. This v2 route is store-scoped. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            limit: Number of items to return per page. Defaults to `15`.
            before: The before.
            after: The after.
            order: The order.
            page: Page number to return. Defaults to `1`.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            AsyncPage[V2ListTicketsResponseValue200ApplicationJsonPropertyDataItem]

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "v2ListTickets",
        }
        params = {
            k: v
            for k, v in {
                "limit": limit,
                "before": before,
                "after": after,
                "order": order,
                "page": page,
            }.items()
            if v is not None
        }
        return await self._client.request_page(
            method="get",
            path=("v2", "tickets"),
            model=V2ListTicketsResponseValue200ApplicationJsonPropertyDataItem,
            params=params,
            request_options=request_options,
        )

    async def v2_search_tickets(
        self,
        *,
        filters: Optional[
            List[V2SearchTicketsRequestApplicationJsonPropertyFiltersItem]
        ] = None,
        sort: Optional[
            List[V2SearchTicketsRequestApplicationJsonPropertySortItem]
        ] = None,
        search: Optional[V2SearchTicketsRequestApplicationJsonPropertySearch] = None,
        includes: Optional[
            List[V2SearchTicketsRequestApplicationJsonPropertyIncludesItem]
        ] = None,
        limit: Optional[int] = None,
        before: Optional[str] = None,
        after: Optional[str] = None,
        order: Optional[str] = None,
        request_options: Optional[RequestOptions] = None,
    ) -> AsyncPage[V2SearchTicketsResponseValue200ApplicationJsonPropertyDataItem]:
        """Search tickets

        Search tickets using JSON body filters, search terms, includes, and sort instructions. This v2 route is store-scoped. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            filters: The filters.
            sort: The sort.
            search: The search.
            includes: The includes.
            limit: The limit.
            before: The before.
            after: The after.
            order: The order.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            AsyncPage[V2SearchTicketsResponseValue200ApplicationJsonPropertyDataItem]

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "v2SearchTickets",
        }
        body: Dict[str, Any] = {
            k: v
            for k, v in {
                "filters": [item.to_dict() for item in filters]
                if filters is not None
                else None,
                "sort": [item.to_dict() for item in sort] if sort is not None else None,
                "search": search.to_dict() if search is not None else None,
                "includes": [item.to_dict() for item in includes]
                if includes is not None
                else None,
            }.items()
            if v is not None
        }
        params = {
            k: v
            for k, v in {
                "limit": limit,
                "before": before,
                "after": after,
                "order": order,
            }.items()
            if v is not None
        }
        return await self._client.request_page(
            method="post",
            path=("v2", "tickets", "search"),
            model=V2SearchTicketsResponseValue200ApplicationJsonPropertyDataItem,
            body=body,
            params=params,
            request_options=request_options,
        )

    async def v2_get_ticket(
        self,
        ticket: int,
        *,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkV2GetTicketResponseValue200ApplicationJson:
        """Retrieve specific ticket

        Retrieve a support ticket by its ID. This v2 route is store-scoped. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            ticket: The ticket path parameter.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkV2GetTicketResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "v2GetTicket",
        }
        return await self._client.request(
            method="get",
            path=("v2", "tickets", str(ticket)),
            model=SdkV2GetTicketResponseValue200ApplicationJson,
            request_options=request_options,
        )

    async def update(
        self,
        ticket: int,
        *,
        status: Optional[
            Union[SdkUpdateTicketRequestApplicationJsonStatus, str]
        ] = None,
        archived: Optional[bool] = None,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkUpdateTicketResponseValue200ApplicationJson:
        """Close, reopen, or archive a ticket

        Change ticket status or archive state in the selected store. Requires the ticket API-key ability and store ticket permission. Send status OPEN to reopen or CLOSED to close; closing sends the existing customer notification only when the status changes. Send archived true to archive or false to restore. You may send both fields together; omitted fields stay unchanged. Use the same optional Idempotency-Key and identical body after a lost response. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            ticket: The ticket.
            status: The status.
            archived: The archived.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkUpdateTicketResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": True,
            "operation_id": "updateTicket",
        }
        body: Dict[str, Any] = {
            k: v
            for k, v in {
                "status": enum_value(status) if status is not None else None,
                "archived": archived,
            }.items()
            if v is not None
        }
        _validate_model(
            body,
            {
                "name": "SdkUpdateTicketRequestApplicationJson",
                "required": [],
                "properties": {
                    "status": {"kind": "enum", "values": ["OPEN", "CLOSED"]},
                    "archived": {"kind": "primitive", "type": "boolean"},
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return await self._client.request(
            method="patch",
            path=("v2", "tickets", str(ticket)),
            body=body,
            model=SdkUpdateTicketResponseValue200ApplicationJson,
            request_options=request_options,
        )
