# This file is auto-generated by oagen. Do not edit.

from __future__ import annotations

from typing import TYPE_CHECKING, Any, Dict, List, Optional

from .._base_client import WithRawResponse

if TYPE_CHECKING:
    from .._client import AsyncSellAppClient, SellAppClient

from sellapp_sdk.blacklists.models.sdk_reply_to_ticket_response_value_201_application_json import (
    SdkReplyToTicketResponseValue201ApplicationJson,
)
from sellapp_sdk.blacklists.models.sdk_v_2_get_ticket_message_response_value_200_application_json import (
    SdkV2GetTicketMessageResponseValue200ApplicationJson,
)
from sellapp_sdk.blacklists.models.sdk_v_2_reply_to_ticket_response_value_201_application_json import (
    SdkV2ReplyToTicketResponseValue201ApplicationJson,
)
from sellapp_sdk.blacklists.models.search_ticket_messages_request_application_json_property_filters_item import (
    SearchTicketMessagesRequestApplicationJsonPropertyFiltersItem,
)
from sellapp_sdk.blacklists.models.search_ticket_messages_request_application_json_property_includes_item import (
    SearchTicketMessagesRequestApplicationJsonPropertyIncludesItem,
)
from sellapp_sdk.blacklists.models.search_ticket_messages_request_application_json_property_search import (
    SearchTicketMessagesRequestApplicationJsonPropertySearch,
)
from sellapp_sdk.blacklists.models.search_ticket_messages_request_application_json_property_sort_item import (
    SearchTicketMessagesRequestApplicationJsonPropertySortItem,
)
from sellapp_sdk.blacklists.models.search_ticket_messages_response_value_200_application_json_property_data_item import (
    SearchTicketMessagesResponseValue200ApplicationJsonPropertyDataItem,
)
from sellapp_sdk.blacklists.models.v_2_list_ticket_messages_response_value_200_application_json_property_data_item import (
    V2ListTicketMessagesResponseValue200ApplicationJsonPropertyDataItem,
)
from sellapp_sdk.blacklists.models.v_2_search_ticket_messages_request_application_json_property_filters_item import (
    V2SearchTicketMessagesRequestApplicationJsonPropertyFiltersItem,
)
from sellapp_sdk.blacklists.models.v_2_search_ticket_messages_request_application_json_property_includes_item import (
    V2SearchTicketMessagesRequestApplicationJsonPropertyIncludesItem,
)
from sellapp_sdk.blacklists.models.v_2_search_ticket_messages_request_application_json_property_search import (
    V2SearchTicketMessagesRequestApplicationJsonPropertySearch,
)
from sellapp_sdk.blacklists.models.v_2_search_ticket_messages_request_application_json_property_sort_item import (
    V2SearchTicketMessagesRequestApplicationJsonPropertySortItem,
)
from sellapp_sdk.blacklists.models.v_2_search_ticket_messages_response_value_200_application_json_property_data_item import (
    V2SearchTicketMessagesResponseValue200ApplicationJsonPropertyDataItem,
)
from sellapp_sdk.common.models.list_ticket_messages_response_value_200_application_json_property_data_item import (
    ListTicketMessagesResponseValue200ApplicationJsonPropertyDataItem,
)
from sellapp_sdk.common.models.sdk_get_ticket_message_response_value_200_application_json import (
    SdkGetTicketMessageResponseValue200ApplicationJson,
)

from .._pagination import AsyncPage, SyncPage
from .._types import RequestOptions, _validate_model


class TicketsMessages:
    """Tickets Messages API resources."""

    def __init__(self, client: SellAppClient) -> None:
        self._client = client
        self.with_raw_response = WithRawResponse(self)

    def list(
        self,
        ticket: int,
        *,
        limit: Optional[int] = None,
        before: Optional[str] = None,
        after: Optional[str] = None,
        order: Optional[str] = None,
        page: Optional[int] = None,
        request_options: Optional[RequestOptions] = None,
    ) -> SyncPage[ListTicketMessagesResponseValue200ApplicationJsonPropertyDataItem]:
        """List all ticket messages

        Read a ticket's conversation, 15 messages per page by default.

        Args:
            ticket: The ticket path parameter.
            limit: Number of items to return per page. Defaults to `15`.
            before: The before.
            after: The after.
            order: The order.
            page: Page number to return. Defaults to `1`.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SyncPage[ListTicketMessagesResponseValue200ApplicationJsonPropertyDataItem]

        Raises:
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [
                        {"schemeName": "bearerAuth", "scopes": []},
                        {"schemeName": "storeAuth", "scopes": []},
                    ]
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": False,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": True,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": False,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "listTicketMessages",
        }
        params = {
            k: v
            for k, v in {
                "limit": limit,
                "before": before,
                "after": after,
                "order": order,
                "page": page,
            }.items()
            if v is not None
        }
        return self._client.request_page(
            method="get",
            path=("v1", "tickets", str(ticket), "messages"),
            model=ListTicketMessagesResponseValue200ApplicationJsonPropertyDataItem,
            params=params,
            request_options=request_options,
        )

    def reply(
        self,
        ticket: int,
        *,
        content: str,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkReplyToTicketResponseValue201ApplicationJson:
        """Reply to ticket

        Add a seller reply to a support conversation.

        Args:
            ticket: The ticket path parameter.
            content: The content.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkReplyToTicketResponseValue201ApplicationJson

        Raises:
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [
                        {"schemeName": "bearerAuth", "scopes": []},
                        {"schemeName": "storeAuth", "scopes": []},
                    ]
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": False,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": True,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": False,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "replyToTicket",
        }
        body: Dict[str, Any] = {
            "content": content,
        }
        _validate_model(
            body,
            {
                "name": "SdkReplyToTicketRequestApplicationJson",
                "required": ["content"],
                "properties": {"content": {"kind": "primitive", "type": "string"}},
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return self._client.request(
            method="post",
            path=("v1", "tickets", str(ticket), "messages"),
            body=body,
            model=SdkReplyToTicketResponseValue201ApplicationJson,
            request_options=request_options,
        )

    def get(
        self,
        ticket: int,
        message: int,
        *,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkGetTicketMessageResponseValue200ApplicationJson:
        """Retrieve specific ticket message

        Retrieve one message using its ticket ID and message ID.

        Args:
            ticket: The ticket path parameter.
            message: The message path parameter.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkGetTicketMessageResponseValue200ApplicationJson

        Raises:
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [
                        {"schemeName": "bearerAuth", "scopes": []},
                        {"schemeName": "storeAuth", "scopes": []},
                    ]
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": False,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": True,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": False,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "getTicketMessage",
        }
        return self._client.request(
            method="get",
            path=("v1", "tickets", str(ticket), "messages", str(message)),
            model=SdkGetTicketMessageResponseValue200ApplicationJson,
            request_options=request_options,
        )

    def search(
        self,
        ticket: int,
        *,
        filters: Optional[
            List[SearchTicketMessagesRequestApplicationJsonPropertyFiltersItem]
        ] = None,
        sort: Optional[
            List[SearchTicketMessagesRequestApplicationJsonPropertySortItem]
        ] = None,
        search: Optional[
            SearchTicketMessagesRequestApplicationJsonPropertySearch
        ] = None,
        includes: Optional[
            List[SearchTicketMessagesRequestApplicationJsonPropertyIncludesItem]
        ] = None,
        limit: Optional[int] = None,
        before: Optional[str] = None,
        after: Optional[str] = None,
        order: Optional[str] = None,
        request_options: Optional[RequestOptions] = None,
    ) -> SyncPage[SearchTicketMessagesResponseValue200ApplicationJsonPropertyDataItem]:
        """Search ticket messages

        Search a ticket's messages using JSON body filters, search terms, includes, and sort instructions.

        Args:
            ticket: The ticket path parameter.
            filters: The filters.
            sort: The sort.
            search: The search.
            includes: The includes.
            limit: The limit.
            before: The before.
            after: The after.
            order: The order.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SyncPage[SearchTicketMessagesResponseValue200ApplicationJsonPropertyDataItem]

        Raises:
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [
                        {"schemeName": "bearerAuth", "scopes": []},
                        {"schemeName": "storeAuth", "scopes": []},
                    ]
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": False,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": True,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": False,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "searchTicketMessages",
        }
        body: Dict[str, Any] = {
            k: v
            for k, v in {
                "filters": [item.to_dict() for item in filters]
                if filters is not None
                else None,
                "sort": [item.to_dict() for item in sort] if sort is not None else None,
                "search": search.to_dict() if search is not None else None,
                "includes": [item.to_dict() for item in includes]
                if includes is not None
                else None,
            }.items()
            if v is not None
        }
        params = {
            k: v
            for k, v in {
                "limit": limit,
                "before": before,
                "after": after,
                "order": order,
            }.items()
            if v is not None
        }
        return self._client.request_page(
            method="post",
            path=("v1", "tickets", str(ticket), "messages", "search"),
            model=SearchTicketMessagesResponseValue200ApplicationJsonPropertyDataItem,
            body=body,
            params=params,
            request_options=request_options,
        )

    def v2_list_ticket_messages(
        self,
        ticket: int,
        *,
        limit: Optional[int] = None,
        before: Optional[str] = None,
        after: Optional[str] = None,
        order: Optional[str] = None,
        page: Optional[int] = None,
        request_options: Optional[RequestOptions] = None,
    ) -> SyncPage[V2ListTicketMessagesResponseValue200ApplicationJsonPropertyDataItem]:
        """List all ticket messages

        Read a ticket's conversation, 15 messages per page by default. This v2 route is store-scoped. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            ticket: The ticket path parameter.
            limit: Number of items to return per page. Defaults to `15`.
            before: The before.
            after: The after.
            order: The order.
            page: Page number to return. Defaults to `1`.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SyncPage[V2ListTicketMessagesResponseValue200ApplicationJsonPropertyDataItem]

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "v2ListTicketMessages",
        }
        params = {
            k: v
            for k, v in {
                "limit": limit,
                "before": before,
                "after": after,
                "order": order,
                "page": page,
            }.items()
            if v is not None
        }
        return self._client.request_page(
            method="get",
            path=("v2", "tickets", str(ticket), "messages"),
            model=V2ListTicketMessagesResponseValue200ApplicationJsonPropertyDataItem,
            params=params,
            request_options=request_options,
        )

    def v2_reply_to_ticket(
        self,
        ticket: int,
        *,
        content: str,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkV2ReplyToTicketResponseValue201ApplicationJson:
        """Reply to ticket

        Add a seller reply to a support conversation. This v2 route is store-scoped. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            ticket: The ticket path parameter.
            content: The content.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkV2ReplyToTicketResponseValue201ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "v2ReplyToTicket",
        }
        body: Dict[str, Any] = {
            "content": content,
        }
        _validate_model(
            body,
            {
                "name": "SdkV2ReplyToTicketRequestApplicationJson",
                "required": ["content"],
                "properties": {"content": {"kind": "primitive", "type": "string"}},
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return self._client.request(
            method="post",
            path=("v2", "tickets", str(ticket), "messages"),
            body=body,
            model=SdkV2ReplyToTicketResponseValue201ApplicationJson,
            request_options=request_options,
        )

    def v2_search_ticket_messages(
        self,
        ticket: int,
        *,
        filters: Optional[
            List[V2SearchTicketMessagesRequestApplicationJsonPropertyFiltersItem]
        ] = None,
        sort: Optional[
            List[V2SearchTicketMessagesRequestApplicationJsonPropertySortItem]
        ] = None,
        search: Optional[
            V2SearchTicketMessagesRequestApplicationJsonPropertySearch
        ] = None,
        includes: Optional[
            List[V2SearchTicketMessagesRequestApplicationJsonPropertyIncludesItem]
        ] = None,
        limit: Optional[int] = None,
        before: Optional[str] = None,
        after: Optional[str] = None,
        order: Optional[str] = None,
        request_options: Optional[RequestOptions] = None,
    ) -> SyncPage[
        V2SearchTicketMessagesResponseValue200ApplicationJsonPropertyDataItem
    ]:
        """Search ticket messages

        Search a ticket's messages using JSON body filters, search terms, includes, and sort instructions. This v2 route is store-scoped. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            ticket: The ticket path parameter.
            filters: The filters.
            sort: The sort.
            search: The search.
            includes: The includes.
            limit: The limit.
            before: The before.
            after: The after.
            order: The order.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SyncPage[V2SearchTicketMessagesResponseValue200ApplicationJsonPropertyDataItem]

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "v2SearchTicketMessages",
        }
        body: Dict[str, Any] = {
            k: v
            for k, v in {
                "filters": [item.to_dict() for item in filters]
                if filters is not None
                else None,
                "sort": [item.to_dict() for item in sort] if sort is not None else None,
                "search": search.to_dict() if search is not None else None,
                "includes": [item.to_dict() for item in includes]
                if includes is not None
                else None,
            }.items()
            if v is not None
        }
        params = {
            k: v
            for k, v in {
                "limit": limit,
                "before": before,
                "after": after,
                "order": order,
            }.items()
            if v is not None
        }
        return self._client.request_page(
            method="post",
            path=("v2", "tickets", str(ticket), "messages", "search"),
            model=V2SearchTicketMessagesResponseValue200ApplicationJsonPropertyDataItem,
            body=body,
            params=params,
            request_options=request_options,
        )

    def v2_get_ticket_message(
        self,
        ticket: int,
        message: int,
        *,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkV2GetTicketMessageResponseValue200ApplicationJson:
        """Retrieve specific ticket message

        Retrieve one message using its ticket ID and message ID. This v2 route is store-scoped. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            ticket: The ticket path parameter.
            message: The message path parameter.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkV2GetTicketMessageResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "v2GetTicketMessage",
        }
        return self._client.request(
            method="get",
            path=("v2", "tickets", str(ticket), "messages", str(message)),
            model=SdkV2GetTicketMessageResponseValue200ApplicationJson,
            request_options=request_options,
        )


class AsyncTicketsMessages:
    """Tickets Messages API resources (async)."""

    def __init__(self, client: AsyncSellAppClient) -> None:
        self._client = client
        self.with_raw_response = WithRawResponse(self)

    async def list(
        self,
        ticket: int,
        *,
        limit: Optional[int] = None,
        before: Optional[str] = None,
        after: Optional[str] = None,
        order: Optional[str] = None,
        page: Optional[int] = None,
        request_options: Optional[RequestOptions] = None,
    ) -> AsyncPage[ListTicketMessagesResponseValue200ApplicationJsonPropertyDataItem]:
        """List all ticket messages

        Read a ticket's conversation, 15 messages per page by default.

        Args:
            ticket: The ticket path parameter.
            limit: Number of items to return per page. Defaults to `15`.
            before: The before.
            after: The after.
            order: The order.
            page: Page number to return. Defaults to `1`.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            AsyncPage[ListTicketMessagesResponseValue200ApplicationJsonPropertyDataItem]

        Raises:
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [
                        {"schemeName": "bearerAuth", "scopes": []},
                        {"schemeName": "storeAuth", "scopes": []},
                    ]
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": False,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": True,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": False,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "listTicketMessages",
        }
        params = {
            k: v
            for k, v in {
                "limit": limit,
                "before": before,
                "after": after,
                "order": order,
                "page": page,
            }.items()
            if v is not None
        }
        return await self._client.request_page(
            method="get",
            path=("v1", "tickets", str(ticket), "messages"),
            model=ListTicketMessagesResponseValue200ApplicationJsonPropertyDataItem,
            params=params,
            request_options=request_options,
        )

    async def reply(
        self,
        ticket: int,
        *,
        content: str,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkReplyToTicketResponseValue201ApplicationJson:
        """Reply to ticket

        Add a seller reply to a support conversation.

        Args:
            ticket: The ticket path parameter.
            content: The content.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkReplyToTicketResponseValue201ApplicationJson

        Raises:
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [
                        {"schemeName": "bearerAuth", "scopes": []},
                        {"schemeName": "storeAuth", "scopes": []},
                    ]
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": False,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": True,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": False,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "replyToTicket",
        }
        body: Dict[str, Any] = {
            "content": content,
        }
        _validate_model(
            body,
            {
                "name": "SdkReplyToTicketRequestApplicationJson",
                "required": ["content"],
                "properties": {"content": {"kind": "primitive", "type": "string"}},
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return await self._client.request(
            method="post",
            path=("v1", "tickets", str(ticket), "messages"),
            body=body,
            model=SdkReplyToTicketResponseValue201ApplicationJson,
            request_options=request_options,
        )

    async def get(
        self,
        ticket: int,
        message: int,
        *,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkGetTicketMessageResponseValue200ApplicationJson:
        """Retrieve specific ticket message

        Retrieve one message using its ticket ID and message ID.

        Args:
            ticket: The ticket path parameter.
            message: The message path parameter.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkGetTicketMessageResponseValue200ApplicationJson

        Raises:
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [
                        {"schemeName": "bearerAuth", "scopes": []},
                        {"schemeName": "storeAuth", "scopes": []},
                    ]
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": False,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": True,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": False,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "getTicketMessage",
        }
        return await self._client.request(
            method="get",
            path=("v1", "tickets", str(ticket), "messages", str(message)),
            model=SdkGetTicketMessageResponseValue200ApplicationJson,
            request_options=request_options,
        )

    async def search(
        self,
        ticket: int,
        *,
        filters: Optional[
            List[SearchTicketMessagesRequestApplicationJsonPropertyFiltersItem]
        ] = None,
        sort: Optional[
            List[SearchTicketMessagesRequestApplicationJsonPropertySortItem]
        ] = None,
        search: Optional[
            SearchTicketMessagesRequestApplicationJsonPropertySearch
        ] = None,
        includes: Optional[
            List[SearchTicketMessagesRequestApplicationJsonPropertyIncludesItem]
        ] = None,
        limit: Optional[int] = None,
        before: Optional[str] = None,
        after: Optional[str] = None,
        order: Optional[str] = None,
        request_options: Optional[RequestOptions] = None,
    ) -> AsyncPage[SearchTicketMessagesResponseValue200ApplicationJsonPropertyDataItem]:
        """Search ticket messages

        Search a ticket's messages using JSON body filters, search terms, includes, and sort instructions.

        Args:
            ticket: The ticket path parameter.
            filters: The filters.
            sort: The sort.
            search: The search.
            includes: The includes.
            limit: The limit.
            before: The before.
            after: The after.
            order: The order.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            AsyncPage[SearchTicketMessagesResponseValue200ApplicationJsonPropertyDataItem]

        Raises:
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [
                        {"schemeName": "bearerAuth", "scopes": []},
                        {"schemeName": "storeAuth", "scopes": []},
                    ]
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": False,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": True,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": False,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "searchTicketMessages",
        }
        body: Dict[str, Any] = {
            k: v
            for k, v in {
                "filters": [item.to_dict() for item in filters]
                if filters is not None
                else None,
                "sort": [item.to_dict() for item in sort] if sort is not None else None,
                "search": search.to_dict() if search is not None else None,
                "includes": [item.to_dict() for item in includes]
                if includes is not None
                else None,
            }.items()
            if v is not None
        }
        params = {
            k: v
            for k, v in {
                "limit": limit,
                "before": before,
                "after": after,
                "order": order,
            }.items()
            if v is not None
        }
        return await self._client.request_page(
            method="post",
            path=("v1", "tickets", str(ticket), "messages", "search"),
            model=SearchTicketMessagesResponseValue200ApplicationJsonPropertyDataItem,
            body=body,
            params=params,
            request_options=request_options,
        )

    async def v2_list_ticket_messages(
        self,
        ticket: int,
        *,
        limit: Optional[int] = None,
        before: Optional[str] = None,
        after: Optional[str] = None,
        order: Optional[str] = None,
        page: Optional[int] = None,
        request_options: Optional[RequestOptions] = None,
    ) -> AsyncPage[V2ListTicketMessagesResponseValue200ApplicationJsonPropertyDataItem]:
        """List all ticket messages

        Read a ticket's conversation, 15 messages per page by default. This v2 route is store-scoped. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            ticket: The ticket path parameter.
            limit: Number of items to return per page. Defaults to `15`.
            before: The before.
            after: The after.
            order: The order.
            page: Page number to return. Defaults to `1`.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            AsyncPage[V2ListTicketMessagesResponseValue200ApplicationJsonPropertyDataItem]

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "v2ListTicketMessages",
        }
        params = {
            k: v
            for k, v in {
                "limit": limit,
                "before": before,
                "after": after,
                "order": order,
                "page": page,
            }.items()
            if v is not None
        }
        return await self._client.request_page(
            method="get",
            path=("v2", "tickets", str(ticket), "messages"),
            model=V2ListTicketMessagesResponseValue200ApplicationJsonPropertyDataItem,
            params=params,
            request_options=request_options,
        )

    async def v2_reply_to_ticket(
        self,
        ticket: int,
        *,
        content: str,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkV2ReplyToTicketResponseValue201ApplicationJson:
        """Reply to ticket

        Add a seller reply to a support conversation. This v2 route is store-scoped. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            ticket: The ticket path parameter.
            content: The content.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkV2ReplyToTicketResponseValue201ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "v2ReplyToTicket",
        }
        body: Dict[str, Any] = {
            "content": content,
        }
        _validate_model(
            body,
            {
                "name": "SdkV2ReplyToTicketRequestApplicationJson",
                "required": ["content"],
                "properties": {"content": {"kind": "primitive", "type": "string"}},
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return await self._client.request(
            method="post",
            path=("v2", "tickets", str(ticket), "messages"),
            body=body,
            model=SdkV2ReplyToTicketResponseValue201ApplicationJson,
            request_options=request_options,
        )

    async def v2_search_ticket_messages(
        self,
        ticket: int,
        *,
        filters: Optional[
            List[V2SearchTicketMessagesRequestApplicationJsonPropertyFiltersItem]
        ] = None,
        sort: Optional[
            List[V2SearchTicketMessagesRequestApplicationJsonPropertySortItem]
        ] = None,
        search: Optional[
            V2SearchTicketMessagesRequestApplicationJsonPropertySearch
        ] = None,
        includes: Optional[
            List[V2SearchTicketMessagesRequestApplicationJsonPropertyIncludesItem]
        ] = None,
        limit: Optional[int] = None,
        before: Optional[str] = None,
        after: Optional[str] = None,
        order: Optional[str] = None,
        request_options: Optional[RequestOptions] = None,
    ) -> AsyncPage[
        V2SearchTicketMessagesResponseValue200ApplicationJsonPropertyDataItem
    ]:
        """Search ticket messages

        Search a ticket's messages using JSON body filters, search terms, includes, and sort instructions. This v2 route is store-scoped. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            ticket: The ticket path parameter.
            filters: The filters.
            sort: The sort.
            search: The search.
            includes: The includes.
            limit: The limit.
            before: The before.
            after: The after.
            order: The order.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            AsyncPage[V2SearchTicketMessagesResponseValue200ApplicationJsonPropertyDataItem]

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "v2SearchTicketMessages",
        }
        body: Dict[str, Any] = {
            k: v
            for k, v in {
                "filters": [item.to_dict() for item in filters]
                if filters is not None
                else None,
                "sort": [item.to_dict() for item in sort] if sort is not None else None,
                "search": search.to_dict() if search is not None else None,
                "includes": [item.to_dict() for item in includes]
                if includes is not None
                else None,
            }.items()
            if v is not None
        }
        params = {
            k: v
            for k, v in {
                "limit": limit,
                "before": before,
                "after": after,
                "order": order,
            }.items()
            if v is not None
        }
        return await self._client.request_page(
            method="post",
            path=("v2", "tickets", str(ticket), "messages", "search"),
            model=V2SearchTicketMessagesResponseValue200ApplicationJsonPropertyDataItem,
            body=body,
            params=params,
            request_options=request_options,
        )

    async def v2_get_ticket_message(
        self,
        ticket: int,
        message: int,
        *,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkV2GetTicketMessageResponseValue200ApplicationJson:
        """Retrieve specific ticket message

        Retrieve one message using its ticket ID and message ID. This v2 route is store-scoped. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            ticket: The ticket path parameter.
            message: The message path parameter.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkV2GetTicketMessageResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "v2GetTicketMessage",
        }
        return await self._client.request(
            method="get",
            path=("v2", "tickets", str(ticket), "messages", str(message)),
            model=SdkV2GetTicketMessageResponseValue200ApplicationJson,
            request_options=request_options,
        )
