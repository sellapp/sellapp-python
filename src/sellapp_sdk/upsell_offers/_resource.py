# This file is auto-generated by oagen. Do not edit.

from __future__ import annotations

from typing import TYPE_CHECKING, Any

from .._base_client import WithRawResponse

if TYPE_CHECKING:
    from .._client import AsyncSellAppClient, SellAppClient

import builtins

from .._pagination import AsyncPage, SyncPage
from .._types import NOT_GIVEN, NotGiven, RequestOptions, _validate_model
from .models import (
    CreateUpsellOfferRequestApplicationJsonPropertyItemsItem,
    ReplaceUpsellOfferRequestApplicationJsonPropertyItemsItem,
    SdkCreateUpsellOfferResponseValue201ApplicationJson,
    SdkGetUpsellOfferResponseValue200ApplicationJson,
    SdkListUpsellOffersResponseValue200ApplicationJson,
    SdkReplaceUpsellOfferResponseValue200ApplicationJson,
    SdkSearchUpsellOffersResponseValue200ApplicationJson,
    SdkUpdateUpsellOfferResponseValue200ApplicationJson,
    SearchUpsellOffersRequestApplicationJsonPropertyFiltersItem,
    SearchUpsellOffersRequestApplicationJsonPropertyIncludesItem,
    SearchUpsellOffersRequestApplicationJsonPropertySearch,
    SearchUpsellOffersRequestApplicationJsonPropertySortItem,
    UpdateUpsellOfferRequestApplicationJsonPropertyItemsItem,
)


class UpsellOffers:
    """Upsell Offers API resources."""

    def __init__(self, client: SellAppClient) -> None:
        self._client = client
        self.with_raw_response = WithRawResponse(self)

    def list(
        self,
        *,
        limit: int | None = None,
        before: str | None = None,
        after: str | None = None,
        order: str | None = None,
        page: int | None = None,
        pagination: bool | None = None,
        request_options: RequestOptions | None = None,
    ) -> SyncPage[SdkListUpsellOffersResponseValue200ApplicationJson]:
        """List upsell offers

        List the authenticated store's ordered post-purchase offers. Requires a Sanctum token with the `upsell` ability and the Upsells role for the store selected by `X-STORE`. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            limit: Number of items to return per page. Defaults to `15`.
            before: The before.
            after: The after.
            order: The order.
            page: Page number to return. Defaults to `1`.
            pagination: Set to false to return at most 100 matching items in one data array without pagination links or metadata. Defaults to `true`.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SyncPage[SdkListUpsellOffersResponseValue200ApplicationJson]

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "listUpsellOffers",
        }
        params = {
            k: v
            for k, v in {
                "limit": limit,
                "before": before,
                "after": after,
                "order": order,
                "page": page,
                "pagination": pagination,
            }.items()
            if v is not None
        }
        return self._client.request_page(
            method="get",
            path=("v2", "upsell-offers"),
            model=SdkListUpsellOffersResponseValue200ApplicationJson,
            params=params,
            request_options=request_options,
        )

    def create(
        self,
        *,
        name: str,
        is_active: bool,
        source_listing_id: int,
        items: builtins.list[CreateUpsellOfferRequestApplicationJsonPropertyItemsItem],
        description: str | None | NotGiven = NOT_GIVEN,
        source_variant_id: int | None | NotGiven = NOT_GIVEN,
        minimum_order_total_usd_cents: int | None | NotGiven = NOT_GIVEN,
        maximum_order_total_usd_cents: int | None | NotGiven = NOT_GIVEN,
        starts_at: str | None | NotGiven = NOT_GIVEN,
        ends_at: str | None | NotGiven = NOT_GIVEN,
        available_for_days: int | None | NotGiven = NOT_GIVEN,
        max_accepts_per_customer: int | None | NotGiven = NOT_GIVEN,
        expected_version: int | None = None,
        request_options: RequestOptions | None = None,
    ) -> SdkCreateUpsellOfferResponseValue201ApplicationJson:
        """Create an upsell offer

        Create an ordered post-purchase offer. Every product and variant ID is checked against the selected store and expected parent product. Requires a Sanctum token with the `upsell` ability and the Upsells role for the store selected by `X-STORE`. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            name: The name.
            description: The description.
            is_active: The is active.
            source_listing_id: The source listing id.
            source_variant_id: The source variant id.
            minimum_order_total_usd_cents: Integer USD cents, for example 1000 for USD 10.00.
            maximum_order_total_usd_cents: Integer USD cents. When present, must be greater than or equal to minimum_order_total_usd_cents.
            starts_at: The starts at.
            ends_at: Must be after or equal to starts_at.
            available_for_days: The available for days.
            max_accepts_per_customer: The max accepts per customer.
            expected_version: Required optimistic concurrency token for updates. Use the version from the latest response; stale versions are rejected with 422.
            items: Complete ordered target-item set. Target variant IDs must be unique and belong to their target product in this store.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkCreateUpsellOfferResponseValue201ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "createUpsellOffer",
        }
        body: dict[str, Any] = {
            k: v
            for k, v in {
                "name": name,
                "is_active": is_active,
                "source_listing_id": source_listing_id,
                "expected_version": expected_version,
                "items": [item.to_dict() for item in items],
            }.items()
            if v is not None
        }
        if not isinstance(description, NotGiven):
            body["description"] = description
        if not isinstance(source_variant_id, NotGiven):
            body["source_variant_id"] = source_variant_id
        if not isinstance(minimum_order_total_usd_cents, NotGiven):
            body["minimum_order_total_usd_cents"] = minimum_order_total_usd_cents
        if not isinstance(maximum_order_total_usd_cents, NotGiven):
            body["maximum_order_total_usd_cents"] = maximum_order_total_usd_cents
        if not isinstance(starts_at, NotGiven):
            body["starts_at"] = starts_at
        if not isinstance(ends_at, NotGiven):
            body["ends_at"] = ends_at
        if not isinstance(available_for_days, NotGiven):
            body["available_for_days"] = available_for_days
        if not isinstance(max_accepts_per_customer, NotGiven):
            body["max_accepts_per_customer"] = max_accepts_per_customer
        _validate_model(
            body,
            {
                "name": "SdkCreateUpsellOfferRequestApplicationJson",
                "required": ["name", "is_active", "source_listing_id", "items"],
                "properties": {
                    "name": {
                        "kind": "primitive",
                        "type": "string",
                        "constraints": {"minLength": 1, "maxLength": 120},
                    },
                    "description": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "string",
                            "constraints": {"maxLength": 255},
                        },
                    },
                    "is_active": {"kind": "primitive", "type": "boolean"},
                    "source_listing_id": {"kind": "primitive", "type": "integer"},
                    "source_variant_id": {
                        "kind": "nullable",
                        "inner": {"kind": "primitive", "type": "integer"},
                    },
                    "minimum_order_total_usd_cents": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "integer",
                            "constraints": {"minimum": 1},
                        },
                    },
                    "maximum_order_total_usd_cents": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "integer",
                            "constraints": {"minimum": 1},
                        },
                    },
                    "starts_at": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "string",
                            "format": "date-time",
                        },
                    },
                    "ends_at": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "string",
                            "format": "date-time",
                        },
                    },
                    "available_for_days": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "integer",
                            "constraints": {"minimum": 1, "maximum": 4294967295},
                        },
                    },
                    "max_accepts_per_customer": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "integer",
                            "constraints": {"minimum": 1, "maximum": 4294967295},
                        },
                    },
                    "expected_version": {
                        "kind": "primitive",
                        "type": "integer",
                        "constraints": {"minimum": 1},
                    },
                    "items": {
                        "kind": "array",
                        "items": {"kind": "any"},
                        "constraints": {"minItems": 1, "uniqueItems": False},
                    },
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return self._client.request(
            method="post",
            path=("v2", "upsell-offers"),
            body=body,
            model=SdkCreateUpsellOfferResponseValue201ApplicationJson,
            request_options=request_options,
        )

    def search(
        self,
        *,
        filters: builtins.list[
            SearchUpsellOffersRequestApplicationJsonPropertyFiltersItem
        ]
        | None = None,
        sort: builtins.list[SearchUpsellOffersRequestApplicationJsonPropertySortItem]
        | None = None,
        search: SearchUpsellOffersRequestApplicationJsonPropertySearch | None = None,
        includes: builtins.list[
            SearchUpsellOffersRequestApplicationJsonPropertyIncludesItem
        ]
        | None = None,
        limit: int | None = None,
        before: str | None = None,
        after: str | None = None,
        order: str | None = None,
        page: int | None = None,
        pagination: bool | None = None,
        request_options: RequestOptions | None = None,
    ) -> SyncPage[SdkSearchUpsellOffersResponseValue200ApplicationJson]:
        """Search upsell offers

        Search offer names and descriptions and compose supported filters and sort instructions. Requires a Sanctum token with the `upsell` ability and the Upsells role for the store selected by `X-STORE`. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            filters: The filters.
            sort: The sort.
            search: The search.
            includes: The includes.
            limit: Number of items to return per page. Defaults to `15`.
            before: The before.
            after: The after.
            order: The order.
            page: Page number to return. Defaults to `1`.
            pagination: Set to false to return at most 100 matching items in one data array without pagination links or metadata. Defaults to `true`.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SyncPage[SdkSearchUpsellOffersResponseValue200ApplicationJson]

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "searchUpsellOffers",
        }
        body: dict[str, Any] = {
            k: v
            for k, v in {
                "filters": [item.to_dict() for item in filters]
                if filters is not None
                else None,
                "sort": [item.to_dict() for item in sort] if sort is not None else None,
                "search": search.to_dict() if search is not None else None,
                "includes": [item.to_dict() for item in includes]
                if includes is not None
                else None,
            }.items()
            if v is not None
        }
        params = {
            k: v
            for k, v in {
                "limit": limit,
                "before": before,
                "after": after,
                "order": order,
                "page": page,
                "pagination": pagination,
            }.items()
            if v is not None
        }
        return self._client.request_page(
            method="post",
            path=("v2", "upsell-offers", "search"),
            model=SdkSearchUpsellOffersResponseValue200ApplicationJson,
            body=body,
            params=params,
            request_options=request_options,
        )

    def get(
        self,
        upsell_offer: int,
        *,
        request_options: RequestOptions | None = None,
    ) -> SdkGetUpsellOfferResponseValue200ApplicationJson:
        """Retrieve an upsell offer

        Retrieve one store-scoped upsell offer and its ordered target items. Requires a Sanctum token with the `upsell` ability and the Upsells role for the store selected by `X-STORE`. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            upsell_offer: The upsellOffer path parameter.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkGetUpsellOfferResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "getUpsellOffer",
        }
        return self._client.request(
            method="get",
            path=("v2", "upsell-offers", str(upsell_offer)),
            model=SdkGetUpsellOfferResponseValue200ApplicationJson,
            request_options=request_options,
        )

    def replace(
        self,
        upsell_offer: int,
        *,
        expected_version: int,
        name: str | None = None,
        description: str | None | NotGiven = NOT_GIVEN,
        is_active: bool | None = None,
        source_listing_id: int | None = None,
        source_variant_id: int | None | NotGiven = NOT_GIVEN,
        minimum_order_total_usd_cents: int | None | NotGiven = NOT_GIVEN,
        maximum_order_total_usd_cents: int | None | NotGiven = NOT_GIVEN,
        starts_at: str | None | NotGiven = NOT_GIVEN,
        ends_at: str | None | NotGiven = NOT_GIVEN,
        available_for_days: int | None | NotGiven = NOT_GIVEN,
        max_accepts_per_customer: int | None | NotGiven = NOT_GIVEN,
        items: builtins.list[ReplaceUpsellOfferRequestApplicationJsonPropertyItemsItem]
        | None = None,
        request_options: RequestOptions | None = None,
    ) -> SdkReplaceUpsellOfferResponseValue200ApplicationJson:
        """Update an upsell offer

        Update supplied fields. When items are supplied, they replace the complete ordered target-item set. Requires a Sanctum token with the `upsell` ability and the Upsells role for the store selected by `X-STORE`. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            upsell_offer: The upsellOffer path parameter.
            name: The name.
            description: The description.
            is_active: The is active.
            source_listing_id: The source listing id.
            source_variant_id: The source variant id.
            minimum_order_total_usd_cents: Integer USD cents, for example 1000 for USD 10.00.
            maximum_order_total_usd_cents: Integer USD cents. When present, must be greater than or equal to minimum_order_total_usd_cents.
            starts_at: The starts at.
            ends_at: Must be after or equal to starts_at.
            available_for_days: The available for days.
            max_accepts_per_customer: The max accepts per customer.
            expected_version: Required optimistic concurrency token for updates. Use the version from the latest response; stale versions are rejected with 422.
            items: Complete ordered target-item set. Target variant IDs must be unique and belong to their target product in this store.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkReplaceUpsellOfferResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "replaceUpsellOffer",
        }
        body: dict[str, Any] = {
            k: v
            for k, v in {
                "name": name,
                "is_active": is_active,
                "source_listing_id": source_listing_id,
                "expected_version": expected_version,
                "items": [item.to_dict() for item in items]
                if items is not None
                else None,
            }.items()
            if v is not None
        }
        if not isinstance(description, NotGiven):
            body["description"] = description
        if not isinstance(source_variant_id, NotGiven):
            body["source_variant_id"] = source_variant_id
        if not isinstance(minimum_order_total_usd_cents, NotGiven):
            body["minimum_order_total_usd_cents"] = minimum_order_total_usd_cents
        if not isinstance(maximum_order_total_usd_cents, NotGiven):
            body["maximum_order_total_usd_cents"] = maximum_order_total_usd_cents
        if not isinstance(starts_at, NotGiven):
            body["starts_at"] = starts_at
        if not isinstance(ends_at, NotGiven):
            body["ends_at"] = ends_at
        if not isinstance(available_for_days, NotGiven):
            body["available_for_days"] = available_for_days
        if not isinstance(max_accepts_per_customer, NotGiven):
            body["max_accepts_per_customer"] = max_accepts_per_customer
        _validate_model(
            body,
            {
                "name": "SdkReplaceUpsellOfferRequestApplicationJson",
                "required": ["expected_version"],
                "properties": {
                    "name": {
                        "kind": "primitive",
                        "type": "string",
                        "constraints": {"minLength": 1, "maxLength": 120},
                    },
                    "description": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "string",
                            "constraints": {"maxLength": 255},
                        },
                    },
                    "is_active": {"kind": "primitive", "type": "boolean"},
                    "source_listing_id": {"kind": "primitive", "type": "integer"},
                    "source_variant_id": {
                        "kind": "nullable",
                        "inner": {"kind": "primitive", "type": "integer"},
                    },
                    "minimum_order_total_usd_cents": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "integer",
                            "constraints": {"minimum": 1},
                        },
                    },
                    "maximum_order_total_usd_cents": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "integer",
                            "constraints": {"minimum": 1},
                        },
                    },
                    "starts_at": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "string",
                            "format": "date-time",
                        },
                    },
                    "ends_at": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "string",
                            "format": "date-time",
                        },
                    },
                    "available_for_days": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "integer",
                            "constraints": {"minimum": 1, "maximum": 4294967295},
                        },
                    },
                    "max_accepts_per_customer": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "integer",
                            "constraints": {"minimum": 1, "maximum": 4294967295},
                        },
                    },
                    "expected_version": {
                        "kind": "primitive",
                        "type": "integer",
                        "constraints": {"minimum": 1},
                    },
                    "items": {
                        "kind": "array",
                        "items": {"kind": "any"},
                        "constraints": {"minItems": 1, "uniqueItems": False},
                    },
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return self._client.request(
            method="put",
            path=("v2", "upsell-offers", str(upsell_offer)),
            body=body,
            model=SdkReplaceUpsellOfferResponseValue200ApplicationJson,
            request_options=request_options,
        )

    def update(
        self,
        upsell_offer: int,
        *,
        expected_version: int,
        name: str | None = None,
        description: str | None | NotGiven = NOT_GIVEN,
        is_active: bool | None = None,
        source_listing_id: int | None = None,
        source_variant_id: int | None | NotGiven = NOT_GIVEN,
        minimum_order_total_usd_cents: int | None | NotGiven = NOT_GIVEN,
        maximum_order_total_usd_cents: int | None | NotGiven = NOT_GIVEN,
        starts_at: str | None | NotGiven = NOT_GIVEN,
        ends_at: str | None | NotGiven = NOT_GIVEN,
        available_for_days: int | None | NotGiven = NOT_GIVEN,
        max_accepts_per_customer: int | None | NotGiven = NOT_GIVEN,
        items: builtins.list[UpdateUpsellOfferRequestApplicationJsonPropertyItemsItem]
        | None = None,
        request_options: RequestOptions | None = None,
    ) -> SdkUpdateUpsellOfferResponseValue200ApplicationJson:
        """Update an upsell offer

        Update supplied fields. When items are supplied, they replace the complete ordered target-item set. Requires a Sanctum token with the `upsell` ability and the Upsells role for the store selected by `X-STORE`. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            upsell_offer: The upsellOffer path parameter.
            name: The name.
            description: The description.
            is_active: The is active.
            source_listing_id: The source listing id.
            source_variant_id: The source variant id.
            minimum_order_total_usd_cents: Integer USD cents, for example 1000 for USD 10.00.
            maximum_order_total_usd_cents: Integer USD cents. When present, must be greater than or equal to minimum_order_total_usd_cents.
            starts_at: The starts at.
            ends_at: Must be after or equal to starts_at.
            available_for_days: The available for days.
            max_accepts_per_customer: The max accepts per customer.
            expected_version: Required optimistic concurrency token for updates. Use the version from the latest response; stale versions are rejected with 422.
            items: Complete ordered target-item set. Target variant IDs must be unique and belong to their target product in this store.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkUpdateUpsellOfferResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "updateUpsellOffer",
        }
        body: dict[str, Any] = {
            k: v
            for k, v in {
                "name": name,
                "is_active": is_active,
                "source_listing_id": source_listing_id,
                "expected_version": expected_version,
                "items": [item.to_dict() for item in items]
                if items is not None
                else None,
            }.items()
            if v is not None
        }
        if not isinstance(description, NotGiven):
            body["description"] = description
        if not isinstance(source_variant_id, NotGiven):
            body["source_variant_id"] = source_variant_id
        if not isinstance(minimum_order_total_usd_cents, NotGiven):
            body["minimum_order_total_usd_cents"] = minimum_order_total_usd_cents
        if not isinstance(maximum_order_total_usd_cents, NotGiven):
            body["maximum_order_total_usd_cents"] = maximum_order_total_usd_cents
        if not isinstance(starts_at, NotGiven):
            body["starts_at"] = starts_at
        if not isinstance(ends_at, NotGiven):
            body["ends_at"] = ends_at
        if not isinstance(available_for_days, NotGiven):
            body["available_for_days"] = available_for_days
        if not isinstance(max_accepts_per_customer, NotGiven):
            body["max_accepts_per_customer"] = max_accepts_per_customer
        _validate_model(
            body,
            {
                "name": "SdkUpdateUpsellOfferRequestApplicationJson",
                "required": ["expected_version"],
                "properties": {
                    "name": {
                        "kind": "primitive",
                        "type": "string",
                        "constraints": {"minLength": 1, "maxLength": 120},
                    },
                    "description": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "string",
                            "constraints": {"maxLength": 255},
                        },
                    },
                    "is_active": {"kind": "primitive", "type": "boolean"},
                    "source_listing_id": {"kind": "primitive", "type": "integer"},
                    "source_variant_id": {
                        "kind": "nullable",
                        "inner": {"kind": "primitive", "type": "integer"},
                    },
                    "minimum_order_total_usd_cents": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "integer",
                            "constraints": {"minimum": 1},
                        },
                    },
                    "maximum_order_total_usd_cents": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "integer",
                            "constraints": {"minimum": 1},
                        },
                    },
                    "starts_at": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "string",
                            "format": "date-time",
                        },
                    },
                    "ends_at": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "string",
                            "format": "date-time",
                        },
                    },
                    "available_for_days": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "integer",
                            "constraints": {"minimum": 1, "maximum": 4294967295},
                        },
                    },
                    "max_accepts_per_customer": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "integer",
                            "constraints": {"minimum": 1, "maximum": 4294967295},
                        },
                    },
                    "expected_version": {
                        "kind": "primitive",
                        "type": "integer",
                        "constraints": {"minimum": 1},
                    },
                    "items": {
                        "kind": "array",
                        "items": {"kind": "any"},
                        "constraints": {"minItems": 1, "uniqueItems": False},
                    },
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return self._client.request(
            method="patch",
            path=("v2", "upsell-offers", str(upsell_offer)),
            body=body,
            model=SdkUpdateUpsellOfferResponseValue200ApplicationJson,
            request_options=request_options,
        )

    def delete(
        self,
        upsell_offer: int,
        *,
        request_options: RequestOptions | None = None,
    ) -> None:
        """Delete an upsell offer

        Soft-delete an upsell offer while preserving checkout and reporting attribution. Requires a Sanctum token with the `upsell` ability and the Upsells role for the store selected by `X-STORE`. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            upsell_offer: The upsellOffer path parameter.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "deleteUpsellOffer",
        }
        self._client.request(
            method="delete",
            path=("v2", "upsell-offers", str(upsell_offer)),
            request_options=request_options,
        )


class AsyncUpsellOffers:
    """Upsell Offers API resources (async)."""

    def __init__(self, client: AsyncSellAppClient) -> None:
        self._client = client
        self.with_raw_response = WithRawResponse(self)

    async def list(
        self,
        *,
        limit: int | None = None,
        before: str | None = None,
        after: str | None = None,
        order: str | None = None,
        page: int | None = None,
        pagination: bool | None = None,
        request_options: RequestOptions | None = None,
    ) -> AsyncPage[SdkListUpsellOffersResponseValue200ApplicationJson]:
        """List upsell offers

        List the authenticated store's ordered post-purchase offers. Requires a Sanctum token with the `upsell` ability and the Upsells role for the store selected by `X-STORE`. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            limit: Number of items to return per page. Defaults to `15`.
            before: The before.
            after: The after.
            order: The order.
            page: Page number to return. Defaults to `1`.
            pagination: Set to false to return at most 100 matching items in one data array without pagination links or metadata. Defaults to `true`.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            AsyncPage[SdkListUpsellOffersResponseValue200ApplicationJson]

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "listUpsellOffers",
        }
        params = {
            k: v
            for k, v in {
                "limit": limit,
                "before": before,
                "after": after,
                "order": order,
                "page": page,
                "pagination": pagination,
            }.items()
            if v is not None
        }
        return await self._client.request_page(
            method="get",
            path=("v2", "upsell-offers"),
            model=SdkListUpsellOffersResponseValue200ApplicationJson,
            params=params,
            request_options=request_options,
        )

    async def create(
        self,
        *,
        name: str,
        is_active: bool,
        source_listing_id: int,
        items: builtins.list[CreateUpsellOfferRequestApplicationJsonPropertyItemsItem],
        description: str | None | NotGiven = NOT_GIVEN,
        source_variant_id: int | None | NotGiven = NOT_GIVEN,
        minimum_order_total_usd_cents: int | None | NotGiven = NOT_GIVEN,
        maximum_order_total_usd_cents: int | None | NotGiven = NOT_GIVEN,
        starts_at: str | None | NotGiven = NOT_GIVEN,
        ends_at: str | None | NotGiven = NOT_GIVEN,
        available_for_days: int | None | NotGiven = NOT_GIVEN,
        max_accepts_per_customer: int | None | NotGiven = NOT_GIVEN,
        expected_version: int | None = None,
        request_options: RequestOptions | None = None,
    ) -> SdkCreateUpsellOfferResponseValue201ApplicationJson:
        """Create an upsell offer

        Create an ordered post-purchase offer. Every product and variant ID is checked against the selected store and expected parent product. Requires a Sanctum token with the `upsell` ability and the Upsells role for the store selected by `X-STORE`. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            name: The name.
            description: The description.
            is_active: The is active.
            source_listing_id: The source listing id.
            source_variant_id: The source variant id.
            minimum_order_total_usd_cents: Integer USD cents, for example 1000 for USD 10.00.
            maximum_order_total_usd_cents: Integer USD cents. When present, must be greater than or equal to minimum_order_total_usd_cents.
            starts_at: The starts at.
            ends_at: Must be after or equal to starts_at.
            available_for_days: The available for days.
            max_accepts_per_customer: The max accepts per customer.
            expected_version: Required optimistic concurrency token for updates. Use the version from the latest response; stale versions are rejected with 422.
            items: Complete ordered target-item set. Target variant IDs must be unique and belong to their target product in this store.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkCreateUpsellOfferResponseValue201ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "createUpsellOffer",
        }
        body: dict[str, Any] = {
            k: v
            for k, v in {
                "name": name,
                "is_active": is_active,
                "source_listing_id": source_listing_id,
                "expected_version": expected_version,
                "items": [item.to_dict() for item in items],
            }.items()
            if v is not None
        }
        if not isinstance(description, NotGiven):
            body["description"] = description
        if not isinstance(source_variant_id, NotGiven):
            body["source_variant_id"] = source_variant_id
        if not isinstance(minimum_order_total_usd_cents, NotGiven):
            body["minimum_order_total_usd_cents"] = minimum_order_total_usd_cents
        if not isinstance(maximum_order_total_usd_cents, NotGiven):
            body["maximum_order_total_usd_cents"] = maximum_order_total_usd_cents
        if not isinstance(starts_at, NotGiven):
            body["starts_at"] = starts_at
        if not isinstance(ends_at, NotGiven):
            body["ends_at"] = ends_at
        if not isinstance(available_for_days, NotGiven):
            body["available_for_days"] = available_for_days
        if not isinstance(max_accepts_per_customer, NotGiven):
            body["max_accepts_per_customer"] = max_accepts_per_customer
        _validate_model(
            body,
            {
                "name": "SdkCreateUpsellOfferRequestApplicationJson",
                "required": ["name", "is_active", "source_listing_id", "items"],
                "properties": {
                    "name": {
                        "kind": "primitive",
                        "type": "string",
                        "constraints": {"minLength": 1, "maxLength": 120},
                    },
                    "description": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "string",
                            "constraints": {"maxLength": 255},
                        },
                    },
                    "is_active": {"kind": "primitive", "type": "boolean"},
                    "source_listing_id": {"kind": "primitive", "type": "integer"},
                    "source_variant_id": {
                        "kind": "nullable",
                        "inner": {"kind": "primitive", "type": "integer"},
                    },
                    "minimum_order_total_usd_cents": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "integer",
                            "constraints": {"minimum": 1},
                        },
                    },
                    "maximum_order_total_usd_cents": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "integer",
                            "constraints": {"minimum": 1},
                        },
                    },
                    "starts_at": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "string",
                            "format": "date-time",
                        },
                    },
                    "ends_at": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "string",
                            "format": "date-time",
                        },
                    },
                    "available_for_days": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "integer",
                            "constraints": {"minimum": 1, "maximum": 4294967295},
                        },
                    },
                    "max_accepts_per_customer": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "integer",
                            "constraints": {"minimum": 1, "maximum": 4294967295},
                        },
                    },
                    "expected_version": {
                        "kind": "primitive",
                        "type": "integer",
                        "constraints": {"minimum": 1},
                    },
                    "items": {
                        "kind": "array",
                        "items": {"kind": "any"},
                        "constraints": {"minItems": 1, "uniqueItems": False},
                    },
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return await self._client.request(
            method="post",
            path=("v2", "upsell-offers"),
            body=body,
            model=SdkCreateUpsellOfferResponseValue201ApplicationJson,
            request_options=request_options,
        )

    async def search(
        self,
        *,
        filters: builtins.list[
            SearchUpsellOffersRequestApplicationJsonPropertyFiltersItem
        ]
        | None = None,
        sort: builtins.list[SearchUpsellOffersRequestApplicationJsonPropertySortItem]
        | None = None,
        search: SearchUpsellOffersRequestApplicationJsonPropertySearch | None = None,
        includes: builtins.list[
            SearchUpsellOffersRequestApplicationJsonPropertyIncludesItem
        ]
        | None = None,
        limit: int | None = None,
        before: str | None = None,
        after: str | None = None,
        order: str | None = None,
        page: int | None = None,
        pagination: bool | None = None,
        request_options: RequestOptions | None = None,
    ) -> AsyncPage[SdkSearchUpsellOffersResponseValue200ApplicationJson]:
        """Search upsell offers

        Search offer names and descriptions and compose supported filters and sort instructions. Requires a Sanctum token with the `upsell` ability and the Upsells role for the store selected by `X-STORE`. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            filters: The filters.
            sort: The sort.
            search: The search.
            includes: The includes.
            limit: Number of items to return per page. Defaults to `15`.
            before: The before.
            after: The after.
            order: The order.
            page: Page number to return. Defaults to `1`.
            pagination: Set to false to return at most 100 matching items in one data array without pagination links or metadata. Defaults to `true`.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            AsyncPage[SdkSearchUpsellOffersResponseValue200ApplicationJson]

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "searchUpsellOffers",
        }
        body: dict[str, Any] = {
            k: v
            for k, v in {
                "filters": [item.to_dict() for item in filters]
                if filters is not None
                else None,
                "sort": [item.to_dict() for item in sort] if sort is not None else None,
                "search": search.to_dict() if search is not None else None,
                "includes": [item.to_dict() for item in includes]
                if includes is not None
                else None,
            }.items()
            if v is not None
        }
        params = {
            k: v
            for k, v in {
                "limit": limit,
                "before": before,
                "after": after,
                "order": order,
                "page": page,
                "pagination": pagination,
            }.items()
            if v is not None
        }
        return await self._client.request_page(
            method="post",
            path=("v2", "upsell-offers", "search"),
            model=SdkSearchUpsellOffersResponseValue200ApplicationJson,
            body=body,
            params=params,
            request_options=request_options,
        )

    async def get(
        self,
        upsell_offer: int,
        *,
        request_options: RequestOptions | None = None,
    ) -> SdkGetUpsellOfferResponseValue200ApplicationJson:
        """Retrieve an upsell offer

        Retrieve one store-scoped upsell offer and its ordered target items. Requires a Sanctum token with the `upsell` ability and the Upsells role for the store selected by `X-STORE`. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            upsell_offer: The upsellOffer path parameter.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkGetUpsellOfferResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "getUpsellOffer",
        }
        return await self._client.request(
            method="get",
            path=("v2", "upsell-offers", str(upsell_offer)),
            model=SdkGetUpsellOfferResponseValue200ApplicationJson,
            request_options=request_options,
        )

    async def replace(
        self,
        upsell_offer: int,
        *,
        expected_version: int,
        name: str | None = None,
        description: str | None | NotGiven = NOT_GIVEN,
        is_active: bool | None = None,
        source_listing_id: int | None = None,
        source_variant_id: int | None | NotGiven = NOT_GIVEN,
        minimum_order_total_usd_cents: int | None | NotGiven = NOT_GIVEN,
        maximum_order_total_usd_cents: int | None | NotGiven = NOT_GIVEN,
        starts_at: str | None | NotGiven = NOT_GIVEN,
        ends_at: str | None | NotGiven = NOT_GIVEN,
        available_for_days: int | None | NotGiven = NOT_GIVEN,
        max_accepts_per_customer: int | None | NotGiven = NOT_GIVEN,
        items: builtins.list[ReplaceUpsellOfferRequestApplicationJsonPropertyItemsItem]
        | None = None,
        request_options: RequestOptions | None = None,
    ) -> SdkReplaceUpsellOfferResponseValue200ApplicationJson:
        """Update an upsell offer

        Update supplied fields. When items are supplied, they replace the complete ordered target-item set. Requires a Sanctum token with the `upsell` ability and the Upsells role for the store selected by `X-STORE`. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            upsell_offer: The upsellOffer path parameter.
            name: The name.
            description: The description.
            is_active: The is active.
            source_listing_id: The source listing id.
            source_variant_id: The source variant id.
            minimum_order_total_usd_cents: Integer USD cents, for example 1000 for USD 10.00.
            maximum_order_total_usd_cents: Integer USD cents. When present, must be greater than or equal to minimum_order_total_usd_cents.
            starts_at: The starts at.
            ends_at: Must be after or equal to starts_at.
            available_for_days: The available for days.
            max_accepts_per_customer: The max accepts per customer.
            expected_version: Required optimistic concurrency token for updates. Use the version from the latest response; stale versions are rejected with 422.
            items: Complete ordered target-item set. Target variant IDs must be unique and belong to their target product in this store.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkReplaceUpsellOfferResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "replaceUpsellOffer",
        }
        body: dict[str, Any] = {
            k: v
            for k, v in {
                "name": name,
                "is_active": is_active,
                "source_listing_id": source_listing_id,
                "expected_version": expected_version,
                "items": [item.to_dict() for item in items]
                if items is not None
                else None,
            }.items()
            if v is not None
        }
        if not isinstance(description, NotGiven):
            body["description"] = description
        if not isinstance(source_variant_id, NotGiven):
            body["source_variant_id"] = source_variant_id
        if not isinstance(minimum_order_total_usd_cents, NotGiven):
            body["minimum_order_total_usd_cents"] = minimum_order_total_usd_cents
        if not isinstance(maximum_order_total_usd_cents, NotGiven):
            body["maximum_order_total_usd_cents"] = maximum_order_total_usd_cents
        if not isinstance(starts_at, NotGiven):
            body["starts_at"] = starts_at
        if not isinstance(ends_at, NotGiven):
            body["ends_at"] = ends_at
        if not isinstance(available_for_days, NotGiven):
            body["available_for_days"] = available_for_days
        if not isinstance(max_accepts_per_customer, NotGiven):
            body["max_accepts_per_customer"] = max_accepts_per_customer
        _validate_model(
            body,
            {
                "name": "SdkReplaceUpsellOfferRequestApplicationJson",
                "required": ["expected_version"],
                "properties": {
                    "name": {
                        "kind": "primitive",
                        "type": "string",
                        "constraints": {"minLength": 1, "maxLength": 120},
                    },
                    "description": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "string",
                            "constraints": {"maxLength": 255},
                        },
                    },
                    "is_active": {"kind": "primitive", "type": "boolean"},
                    "source_listing_id": {"kind": "primitive", "type": "integer"},
                    "source_variant_id": {
                        "kind": "nullable",
                        "inner": {"kind": "primitive", "type": "integer"},
                    },
                    "minimum_order_total_usd_cents": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "integer",
                            "constraints": {"minimum": 1},
                        },
                    },
                    "maximum_order_total_usd_cents": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "integer",
                            "constraints": {"minimum": 1},
                        },
                    },
                    "starts_at": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "string",
                            "format": "date-time",
                        },
                    },
                    "ends_at": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "string",
                            "format": "date-time",
                        },
                    },
                    "available_for_days": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "integer",
                            "constraints": {"minimum": 1, "maximum": 4294967295},
                        },
                    },
                    "max_accepts_per_customer": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "integer",
                            "constraints": {"minimum": 1, "maximum": 4294967295},
                        },
                    },
                    "expected_version": {
                        "kind": "primitive",
                        "type": "integer",
                        "constraints": {"minimum": 1},
                    },
                    "items": {
                        "kind": "array",
                        "items": {"kind": "any"},
                        "constraints": {"minItems": 1, "uniqueItems": False},
                    },
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return await self._client.request(
            method="put",
            path=("v2", "upsell-offers", str(upsell_offer)),
            body=body,
            model=SdkReplaceUpsellOfferResponseValue200ApplicationJson,
            request_options=request_options,
        )

    async def update(
        self,
        upsell_offer: int,
        *,
        expected_version: int,
        name: str | None = None,
        description: str | None | NotGiven = NOT_GIVEN,
        is_active: bool | None = None,
        source_listing_id: int | None = None,
        source_variant_id: int | None | NotGiven = NOT_GIVEN,
        minimum_order_total_usd_cents: int | None | NotGiven = NOT_GIVEN,
        maximum_order_total_usd_cents: int | None | NotGiven = NOT_GIVEN,
        starts_at: str | None | NotGiven = NOT_GIVEN,
        ends_at: str | None | NotGiven = NOT_GIVEN,
        available_for_days: int | None | NotGiven = NOT_GIVEN,
        max_accepts_per_customer: int | None | NotGiven = NOT_GIVEN,
        items: builtins.list[UpdateUpsellOfferRequestApplicationJsonPropertyItemsItem]
        | None = None,
        request_options: RequestOptions | None = None,
    ) -> SdkUpdateUpsellOfferResponseValue200ApplicationJson:
        """Update an upsell offer

        Update supplied fields. When items are supplied, they replace the complete ordered target-item set. Requires a Sanctum token with the `upsell` ability and the Upsells role for the store selected by `X-STORE`. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            upsell_offer: The upsellOffer path parameter.
            name: The name.
            description: The description.
            is_active: The is active.
            source_listing_id: The source listing id.
            source_variant_id: The source variant id.
            minimum_order_total_usd_cents: Integer USD cents, for example 1000 for USD 10.00.
            maximum_order_total_usd_cents: Integer USD cents. When present, must be greater than or equal to minimum_order_total_usd_cents.
            starts_at: The starts at.
            ends_at: Must be after or equal to starts_at.
            available_for_days: The available for days.
            max_accepts_per_customer: The max accepts per customer.
            expected_version: Required optimistic concurrency token for updates. Use the version from the latest response; stale versions are rejected with 422.
            items: Complete ordered target-item set. Target variant IDs must be unique and belong to their target product in this store.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkUpdateUpsellOfferResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "updateUpsellOffer",
        }
        body: dict[str, Any] = {
            k: v
            for k, v in {
                "name": name,
                "is_active": is_active,
                "source_listing_id": source_listing_id,
                "expected_version": expected_version,
                "items": [item.to_dict() for item in items]
                if items is not None
                else None,
            }.items()
            if v is not None
        }
        if not isinstance(description, NotGiven):
            body["description"] = description
        if not isinstance(source_variant_id, NotGiven):
            body["source_variant_id"] = source_variant_id
        if not isinstance(minimum_order_total_usd_cents, NotGiven):
            body["minimum_order_total_usd_cents"] = minimum_order_total_usd_cents
        if not isinstance(maximum_order_total_usd_cents, NotGiven):
            body["maximum_order_total_usd_cents"] = maximum_order_total_usd_cents
        if not isinstance(starts_at, NotGiven):
            body["starts_at"] = starts_at
        if not isinstance(ends_at, NotGiven):
            body["ends_at"] = ends_at
        if not isinstance(available_for_days, NotGiven):
            body["available_for_days"] = available_for_days
        if not isinstance(max_accepts_per_customer, NotGiven):
            body["max_accepts_per_customer"] = max_accepts_per_customer
        _validate_model(
            body,
            {
                "name": "SdkUpdateUpsellOfferRequestApplicationJson",
                "required": ["expected_version"],
                "properties": {
                    "name": {
                        "kind": "primitive",
                        "type": "string",
                        "constraints": {"minLength": 1, "maxLength": 120},
                    },
                    "description": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "string",
                            "constraints": {"maxLength": 255},
                        },
                    },
                    "is_active": {"kind": "primitive", "type": "boolean"},
                    "source_listing_id": {"kind": "primitive", "type": "integer"},
                    "source_variant_id": {
                        "kind": "nullable",
                        "inner": {"kind": "primitive", "type": "integer"},
                    },
                    "minimum_order_total_usd_cents": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "integer",
                            "constraints": {"minimum": 1},
                        },
                    },
                    "maximum_order_total_usd_cents": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "integer",
                            "constraints": {"minimum": 1},
                        },
                    },
                    "starts_at": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "string",
                            "format": "date-time",
                        },
                    },
                    "ends_at": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "string",
                            "format": "date-time",
                        },
                    },
                    "available_for_days": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "integer",
                            "constraints": {"minimum": 1, "maximum": 4294967295},
                        },
                    },
                    "max_accepts_per_customer": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "integer",
                            "constraints": {"minimum": 1, "maximum": 4294967295},
                        },
                    },
                    "expected_version": {
                        "kind": "primitive",
                        "type": "integer",
                        "constraints": {"minimum": 1},
                    },
                    "items": {
                        "kind": "array",
                        "items": {"kind": "any"},
                        "constraints": {"minItems": 1, "uniqueItems": False},
                    },
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return await self._client.request(
            method="patch",
            path=("v2", "upsell-offers", str(upsell_offer)),
            body=body,
            model=SdkUpdateUpsellOfferResponseValue200ApplicationJson,
            request_options=request_options,
        )

    async def delete(
        self,
        upsell_offer: int,
        *,
        request_options: RequestOptions | None = None,
    ) -> None:
        """Delete an upsell offer

        Soft-delete an upsell offer while preserving checkout and reporting attribution. Requires a Sanctum token with the `upsell` ability and the Upsells role for the store selected by `X-STORE`. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            upsell_offer: The upsellOffer path parameter.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "deleteUpsellOffer",
        }
        await self._client.request(
            method="delete",
            path=("v2", "upsell-offers", str(upsell_offer)),
            request_options=request_options,
        )
