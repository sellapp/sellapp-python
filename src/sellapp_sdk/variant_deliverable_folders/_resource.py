# This file is auto-generated by oagen. Do not edit.

from __future__ import annotations

from typing import TYPE_CHECKING, Any, Dict, Optional, Union

from .._base_client import WithRawResponse

if TYPE_CHECKING:
    from .._client import AsyncSellAppClient, SellAppClient

from .._types import NOT_GIVEN, NotGiven, RequestOptions, _validate_model
from .models import (
    SdkCreateVariantDeliverableFolderResponseValue201ApplicationJson,
    SdkGetVariantDeliverableFolderResponseValue200ApplicationJson,
    SdkListVariantDeliverableFoldersResponseValue200ApplicationJson,
    SdkReplaceVariantDeliverableFolderSettingsResponseValue200ApplicationJson,
    SdkUpdateVariantDeliverableFolderResponseValue200ApplicationJson,
)


class VariantDeliverableFolders:
    """Variant Deliverable Folders API resources."""

    def __init__(self, client: SellAppClient) -> None:
        self._client = client
        self.with_raw_response = WithRawResponse(self)

    def list(
        self,
        product: str,
        variant: int,
        *,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkListVariantDeliverableFoldersResponseValue200ApplicationJson:
        """List variant deliverable folders

        List variant deliverable folders. Requires the listing Sanctum ability. Returns folders ordered by sort order. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            product: The product identifier or slug. The product must belong to the authenticated store.
            variant: The product variant identifier.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkListVariantDeliverableFoldersResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "listVariantDeliverableFolders",
        }
        return self._client.request(
            method="get",
            path=(
                "v2",
                "products",
                str(product),
                "variants",
                str(variant),
                "deliverable",
                "folders",
            ),
            model=SdkListVariantDeliverableFoldersResponseValue200ApplicationJson,
            request_options=request_options,
        )

    def create(
        self,
        product: str,
        variant: int,
        *,
        name: str,
        description: Union[str, None, NotGiven] = NOT_GIVEN,
        parent_id: Union[int, None, NotGiven] = NOT_GIVEN,
        sort_order: Optional[int] = None,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkCreateVariantDeliverableFolderResponseValue201ApplicationJson:
        """Create a variant deliverable folder

        Create a variant deliverable folder. Requires the listing Sanctum ability. Parent folders are validated against the same variant. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            product: The product identifier or slug. The product must belong to the authenticated store.
            variant: The product variant identifier.
            name: The name.
            description: The description.
            parent_id: The parent id.
            sort_order: The sort order.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkCreateVariantDeliverableFolderResponseValue201ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "createVariantDeliverableFolder",
        }
        body: Dict[str, Any] = {
            k: v
            for k, v in {
                "name": name,
                "sort_order": sort_order,
            }.items()
            if v is not None
        }
        if not isinstance(description, NotGiven):
            body["description"] = description
        if not isinstance(parent_id, NotGiven):
            body["parent_id"] = parent_id
        _validate_model(
            body,
            {
                "name": "SdkCreateVariantDeliverableFolderRequestApplicationJson",
                "required": ["name"],
                "properties": {
                    "name": {
                        "kind": "primitive",
                        "type": "string",
                        "constraints": {"maxLength": 255},
                    },
                    "description": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "string",
                            "constraints": {"maxLength": 255},
                        },
                    },
                    "parent_id": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "integer",
                            "constraints": {"minimum": 1},
                        },
                    },
                    "sort_order": {
                        "kind": "primitive",
                        "type": "integer",
                        "constraints": {"minimum": 0},
                    },
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return self._client.request(
            method="post",
            path=(
                "v2",
                "products",
                str(product),
                "variants",
                str(variant),
                "deliverable",
                "folders",
            ),
            body=body,
            model=SdkCreateVariantDeliverableFolderResponseValue201ApplicationJson,
            request_options=request_options,
        )

    def get(
        self,
        product: str,
        variant: int,
        folder: int,
        *,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkGetVariantDeliverableFolderResponseValue200ApplicationJson:
        """Retrieve a variant deliverable folder

        Retrieve a variant deliverable folder. Requires the listing Sanctum ability. Folder and parent IDs are scoped through the authenticated store, product, and variant; cycles are rejected. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            product: The product identifier or slug. The product must belong to the authenticated store.
            variant: The product variant identifier.
            folder: The deliverable folder identifier.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkGetVariantDeliverableFolderResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "getVariantDeliverableFolder",
        }
        return self._client.request(
            method="get",
            path=(
                "v2",
                "products",
                str(product),
                "variants",
                str(variant),
                "deliverable",
                "folders",
                str(folder),
            ),
            model=SdkGetVariantDeliverableFolderResponseValue200ApplicationJson,
            request_options=request_options,
        )

    def replace(
        self,
        product: str,
        variant: int,
        folder: int,
        *,
        name: Optional[str] = None,
        description: Union[str, None, NotGiven] = NOT_GIVEN,
        parent_id: Union[int, None, NotGiven] = NOT_GIVEN,
        sort_order: Optional[int] = None,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkReplaceVariantDeliverableFolderSettingsResponseValue200ApplicationJson:
        """Replace variant deliverable folder settings

        Replace variant deliverable folder settings. Requires the listing Sanctum ability. Folder and parent IDs are scoped through the authenticated store, product, and variant; cycles are rejected. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            product: The product identifier or slug. The product must belong to the authenticated store.
            variant: The product variant identifier.
            folder: The deliverable folder identifier.
            name: The name.
            description: The description.
            parent_id: The parent id.
            sort_order: The sort order.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkReplaceVariantDeliverableFolderSettingsResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "replaceVariantDeliverableFolderSettings",
        }
        body: Dict[str, Any] = {
            k: v
            for k, v in {
                "name": name,
                "sort_order": sort_order,
            }.items()
            if v is not None
        }
        if not isinstance(description, NotGiven):
            body["description"] = description
        if not isinstance(parent_id, NotGiven):
            body["parent_id"] = parent_id
        _validate_model(
            body,
            {
                "name": "SdkReplaceVariantDeliverableFolderSettingsRequestApplicationJson",
                "required": [],
                "properties": {
                    "name": {
                        "kind": "primitive",
                        "type": "string",
                        "constraints": {"maxLength": 255},
                    },
                    "description": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "string",
                            "constraints": {"maxLength": 255},
                        },
                    },
                    "parent_id": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "integer",
                            "constraints": {"minimum": 1},
                        },
                    },
                    "sort_order": {
                        "kind": "primitive",
                        "type": "integer",
                        "constraints": {"minimum": 0},
                    },
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return self._client.request(
            method="put",
            path=(
                "v2",
                "products",
                str(product),
                "variants",
                str(variant),
                "deliverable",
                "folders",
                str(folder),
            ),
            body=body,
            model=SdkReplaceVariantDeliverableFolderSettingsResponseValue200ApplicationJson,
            request_options=request_options,
        )

    def update(
        self,
        product: str,
        variant: int,
        folder: int,
        *,
        name: Optional[str] = None,
        description: Union[str, None, NotGiven] = NOT_GIVEN,
        parent_id: Union[int, None, NotGiven] = NOT_GIVEN,
        sort_order: Optional[int] = None,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkUpdateVariantDeliverableFolderResponseValue200ApplicationJson:
        """Update a variant deliverable folder

        Update a variant deliverable folder. Requires the listing Sanctum ability. Folder and parent IDs are scoped through the authenticated store, product, and variant; cycles are rejected. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            product: The product identifier or slug. The product must belong to the authenticated store.
            variant: The product variant identifier.
            folder: The deliverable folder identifier.
            name: The name.
            description: The description.
            parent_id: The parent id.
            sort_order: The sort order.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkUpdateVariantDeliverableFolderResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "updateVariantDeliverableFolder",
        }
        body: Dict[str, Any] = {
            k: v
            for k, v in {
                "name": name,
                "sort_order": sort_order,
            }.items()
            if v is not None
        }
        if not isinstance(description, NotGiven):
            body["description"] = description
        if not isinstance(parent_id, NotGiven):
            body["parent_id"] = parent_id
        _validate_model(
            body,
            {
                "name": "SdkUpdateVariantDeliverableFolderRequestApplicationJson",
                "required": [],
                "properties": {
                    "name": {
                        "kind": "primitive",
                        "type": "string",
                        "constraints": {"maxLength": 255},
                    },
                    "description": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "string",
                            "constraints": {"maxLength": 255},
                        },
                    },
                    "parent_id": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "integer",
                            "constraints": {"minimum": 1},
                        },
                    },
                    "sort_order": {
                        "kind": "primitive",
                        "type": "integer",
                        "constraints": {"minimum": 0},
                    },
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return self._client.request(
            method="patch",
            path=(
                "v2",
                "products",
                str(product),
                "variants",
                str(variant),
                "deliverable",
                "folders",
                str(folder),
            ),
            body=body,
            model=SdkUpdateVariantDeliverableFolderResponseValue200ApplicationJson,
            request_options=request_options,
        )

    def delete(
        self,
        product: str,
        variant: int,
        folder: int,
        *,
        request_options: Optional[RequestOptions] = None,
    ) -> None:
        """Delete a variant deliverable folder

        Delete a variant deliverable folder. Requires the listing Sanctum ability. Folder and parent IDs are scoped through the authenticated store, product, and variant; cycles are rejected. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            product: The product identifier or slug. The product must belong to the authenticated store.
            variant: The product variant identifier.
            folder: The deliverable folder identifier.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "deleteVariantDeliverableFolder",
        }
        self._client.request(
            method="delete",
            path=(
                "v2",
                "products",
                str(product),
                "variants",
                str(variant),
                "deliverable",
                "folders",
                str(folder),
            ),
            request_options=request_options,
        )


class AsyncVariantDeliverableFolders:
    """Variant Deliverable Folders API resources (async)."""

    def __init__(self, client: AsyncSellAppClient) -> None:
        self._client = client
        self.with_raw_response = WithRawResponse(self)

    async def list(
        self,
        product: str,
        variant: int,
        *,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkListVariantDeliverableFoldersResponseValue200ApplicationJson:
        """List variant deliverable folders

        List variant deliverable folders. Requires the listing Sanctum ability. Returns folders ordered by sort order. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            product: The product identifier or slug. The product must belong to the authenticated store.
            variant: The product variant identifier.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkListVariantDeliverableFoldersResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "listVariantDeliverableFolders",
        }
        return await self._client.request(
            method="get",
            path=(
                "v2",
                "products",
                str(product),
                "variants",
                str(variant),
                "deliverable",
                "folders",
            ),
            model=SdkListVariantDeliverableFoldersResponseValue200ApplicationJson,
            request_options=request_options,
        )

    async def create(
        self,
        product: str,
        variant: int,
        *,
        name: str,
        description: Union[str, None, NotGiven] = NOT_GIVEN,
        parent_id: Union[int, None, NotGiven] = NOT_GIVEN,
        sort_order: Optional[int] = None,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkCreateVariantDeliverableFolderResponseValue201ApplicationJson:
        """Create a variant deliverable folder

        Create a variant deliverable folder. Requires the listing Sanctum ability. Parent folders are validated against the same variant. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            product: The product identifier or slug. The product must belong to the authenticated store.
            variant: The product variant identifier.
            name: The name.
            description: The description.
            parent_id: The parent id.
            sort_order: The sort order.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkCreateVariantDeliverableFolderResponseValue201ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "createVariantDeliverableFolder",
        }
        body: Dict[str, Any] = {
            k: v
            for k, v in {
                "name": name,
                "sort_order": sort_order,
            }.items()
            if v is not None
        }
        if not isinstance(description, NotGiven):
            body["description"] = description
        if not isinstance(parent_id, NotGiven):
            body["parent_id"] = parent_id
        _validate_model(
            body,
            {
                "name": "SdkCreateVariantDeliverableFolderRequestApplicationJson",
                "required": ["name"],
                "properties": {
                    "name": {
                        "kind": "primitive",
                        "type": "string",
                        "constraints": {"maxLength": 255},
                    },
                    "description": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "string",
                            "constraints": {"maxLength": 255},
                        },
                    },
                    "parent_id": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "integer",
                            "constraints": {"minimum": 1},
                        },
                    },
                    "sort_order": {
                        "kind": "primitive",
                        "type": "integer",
                        "constraints": {"minimum": 0},
                    },
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return await self._client.request(
            method="post",
            path=(
                "v2",
                "products",
                str(product),
                "variants",
                str(variant),
                "deliverable",
                "folders",
            ),
            body=body,
            model=SdkCreateVariantDeliverableFolderResponseValue201ApplicationJson,
            request_options=request_options,
        )

    async def get(
        self,
        product: str,
        variant: int,
        folder: int,
        *,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkGetVariantDeliverableFolderResponseValue200ApplicationJson:
        """Retrieve a variant deliverable folder

        Retrieve a variant deliverable folder. Requires the listing Sanctum ability. Folder and parent IDs are scoped through the authenticated store, product, and variant; cycles are rejected. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            product: The product identifier or slug. The product must belong to the authenticated store.
            variant: The product variant identifier.
            folder: The deliverable folder identifier.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkGetVariantDeliverableFolderResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "getVariantDeliverableFolder",
        }
        return await self._client.request(
            method="get",
            path=(
                "v2",
                "products",
                str(product),
                "variants",
                str(variant),
                "deliverable",
                "folders",
                str(folder),
            ),
            model=SdkGetVariantDeliverableFolderResponseValue200ApplicationJson,
            request_options=request_options,
        )

    async def replace(
        self,
        product: str,
        variant: int,
        folder: int,
        *,
        name: Optional[str] = None,
        description: Union[str, None, NotGiven] = NOT_GIVEN,
        parent_id: Union[int, None, NotGiven] = NOT_GIVEN,
        sort_order: Optional[int] = None,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkReplaceVariantDeliverableFolderSettingsResponseValue200ApplicationJson:
        """Replace variant deliverable folder settings

        Replace variant deliverable folder settings. Requires the listing Sanctum ability. Folder and parent IDs are scoped through the authenticated store, product, and variant; cycles are rejected. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            product: The product identifier or slug. The product must belong to the authenticated store.
            variant: The product variant identifier.
            folder: The deliverable folder identifier.
            name: The name.
            description: The description.
            parent_id: The parent id.
            sort_order: The sort order.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkReplaceVariantDeliverableFolderSettingsResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "replaceVariantDeliverableFolderSettings",
        }
        body: Dict[str, Any] = {
            k: v
            for k, v in {
                "name": name,
                "sort_order": sort_order,
            }.items()
            if v is not None
        }
        if not isinstance(description, NotGiven):
            body["description"] = description
        if not isinstance(parent_id, NotGiven):
            body["parent_id"] = parent_id
        _validate_model(
            body,
            {
                "name": "SdkReplaceVariantDeliverableFolderSettingsRequestApplicationJson",
                "required": [],
                "properties": {
                    "name": {
                        "kind": "primitive",
                        "type": "string",
                        "constraints": {"maxLength": 255},
                    },
                    "description": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "string",
                            "constraints": {"maxLength": 255},
                        },
                    },
                    "parent_id": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "integer",
                            "constraints": {"minimum": 1},
                        },
                    },
                    "sort_order": {
                        "kind": "primitive",
                        "type": "integer",
                        "constraints": {"minimum": 0},
                    },
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return await self._client.request(
            method="put",
            path=(
                "v2",
                "products",
                str(product),
                "variants",
                str(variant),
                "deliverable",
                "folders",
                str(folder),
            ),
            body=body,
            model=SdkReplaceVariantDeliverableFolderSettingsResponseValue200ApplicationJson,
            request_options=request_options,
        )

    async def update(
        self,
        product: str,
        variant: int,
        folder: int,
        *,
        name: Optional[str] = None,
        description: Union[str, None, NotGiven] = NOT_GIVEN,
        parent_id: Union[int, None, NotGiven] = NOT_GIVEN,
        sort_order: Optional[int] = None,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkUpdateVariantDeliverableFolderResponseValue200ApplicationJson:
        """Update a variant deliverable folder

        Update a variant deliverable folder. Requires the listing Sanctum ability. Folder and parent IDs are scoped through the authenticated store, product, and variant; cycles are rejected. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            product: The product identifier or slug. The product must belong to the authenticated store.
            variant: The product variant identifier.
            folder: The deliverable folder identifier.
            name: The name.
            description: The description.
            parent_id: The parent id.
            sort_order: The sort order.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkUpdateVariantDeliverableFolderResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "updateVariantDeliverableFolder",
        }
        body: Dict[str, Any] = {
            k: v
            for k, v in {
                "name": name,
                "sort_order": sort_order,
            }.items()
            if v is not None
        }
        if not isinstance(description, NotGiven):
            body["description"] = description
        if not isinstance(parent_id, NotGiven):
            body["parent_id"] = parent_id
        _validate_model(
            body,
            {
                "name": "SdkUpdateVariantDeliverableFolderRequestApplicationJson",
                "required": [],
                "properties": {
                    "name": {
                        "kind": "primitive",
                        "type": "string",
                        "constraints": {"maxLength": 255},
                    },
                    "description": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "string",
                            "constraints": {"maxLength": 255},
                        },
                    },
                    "parent_id": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "integer",
                            "constraints": {"minimum": 1},
                        },
                    },
                    "sort_order": {
                        "kind": "primitive",
                        "type": "integer",
                        "constraints": {"minimum": 0},
                    },
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return await self._client.request(
            method="patch",
            path=(
                "v2",
                "products",
                str(product),
                "variants",
                str(variant),
                "deliverable",
                "folders",
                str(folder),
            ),
            body=body,
            model=SdkUpdateVariantDeliverableFolderResponseValue200ApplicationJson,
            request_options=request_options,
        )

    async def delete(
        self,
        product: str,
        variant: int,
        folder: int,
        *,
        request_options: Optional[RequestOptions] = None,
    ) -> None:
        """Delete a variant deliverable folder

        Delete a variant deliverable folder. Requires the listing Sanctum ability. Folder and parent IDs are scoped through the authenticated store, product, and variant; cycles are rejected. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            product: The product identifier or slug. The product must belong to the authenticated store.
            variant: The product variant identifier.
            folder: The deliverable folder identifier.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "deleteVariantDeliverableFolder",
        }
        await self._client.request(
            method="delete",
            path=(
                "v2",
                "products",
                str(product),
                "variants",
                str(variant),
                "deliverable",
                "folders",
                str(folder),
            ),
            request_options=request_options,
        )
