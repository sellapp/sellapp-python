# This file is auto-generated by oagen. Do not edit.

from __future__ import annotations

from typing import TYPE_CHECKING, Any, Dict, List, Optional, Union

from .._base_client import WithRawResponse

if TYPE_CHECKING:
    from .._client import AsyncSellAppClient, SellAppClient

from sellapp_sdk.common.models.sdk_replace_variant_deliverable_configuration_request_application_json_types import (
    SdkReplaceVariantDeliverableConfigurationRequestApplicationJsonTypes,
)

from .._types import RequestOptions, _validate_model
from .models import (
    ReplaceVariantDeliverableConfigurationRequestApplicationJsonPropertyData,
    SdkGetVariantDeliverableConfigurationResponseValue200ApplicationJson,
    SdkReplaceVariantDeliverableConfigurationResponseValue200ApplicationJson,
)


class VariantDeliverables:
    """Variant Deliverables API resources."""

    def __init__(self, client: SellAppClient) -> None:
        self._client = client
        self.with_raw_response = WithRawResponse(self)

    def get(
        self,
        product: str,
        variant: int,
        *,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkGetVariantDeliverableConfigurationResponseValue200ApplicationJson:
        """Retrieve variant deliverable configuration

        Retrieve variant deliverable configuration. Requires the listing Sanctum ability. Returns delivery types and safe configuration for a variant in the authenticated store. Dynamic endpoint URLs and inventory data are not returned. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            product: The product identifier or slug. The product must belong to the authenticated store.
            variant: The product variant identifier.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkGetVariantDeliverableConfigurationResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "getVariantDeliverableConfiguration",
        }
        return self._client.request(
            method="get",
            path=(
                "v2",
                "products",
                str(product),
                "variants",
                str(variant),
                "deliverable",
            ),
            model=SdkGetVariantDeliverableConfigurationResponseValue200ApplicationJson,
            request_options=request_options,
        )

    def replace(
        self,
        product: str,
        variant: int,
        *,
        types: List[
            Union[
                SdkReplaceVariantDeliverableConfigurationRequestApplicationJsonTypes,
                str,
            ]
        ],
        data: ReplaceVariantDeliverableConfigurationRequestApplicationJsonPropertyData,
        expected_updated_at: Optional[str] = None,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkReplaceVariantDeliverableConfigurationResponseValue200ApplicationJson:
        """Replace variant deliverable configuration

        Replace variant deliverable configuration. Requires the listing Sanctum ability. Replaces this resource's delivery types atomically. Pass expected_updated_at from the product variant resource to reject stale updates with 422. TEXT, BOOKING, CREDITS, and BUNDLE must remain unchanged. Removing DOWNLOADABLE deletes managed files and folders after commit; removing a type clears its private configuration. Dynamic endpoints are write-only. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            product: The product identifier or slug. The product must belong to the authenticated store.
            variant: The product variant identifier.
            types: The complete type list. TEXT, BOOKING, CREDITS, and BUNDLE are owned by their dedicated resources and must remain unchanged here.
            expected_updated_at: Optional optimistic-concurrency snapshot from the deliverable resource's updated_at field. The request returns 422 if the variant changed after this timestamp was loaded.
            data: The data.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkReplaceVariantDeliverableConfigurationResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "replaceVariantDeliverableConfiguration",
        }
        body: Dict[str, Any] = {
            k: v
            for k, v in {
                "types": types,
                "expected_updated_at": expected_updated_at,
                "data": data.to_dict(),
            }.items()
            if v is not None
        }
        _validate_model(
            body,
            {
                "name": "SdkReplaceVariantDeliverableConfigurationRequestApplicationJson",
                "required": ["types", "data"],
                "properties": {
                    "types": {
                        "kind": "array",
                        "items": {
                            "kind": "enum",
                            "values": [
                                "DOWNLOADABLE",
                                "TEXT",
                                "DYNAMIC",
                                "MANUAL",
                                "LICENSE_KEY",
                                "BUNDLE",
                                "BOOKING",
                                "CREDITS",
                            ],
                        },
                        "constraints": {"minItems": 1, "uniqueItems": True},
                    },
                    "expected_updated_at": {
                        "kind": "primitive",
                        "type": "string",
                        "format": "date-time",
                    },
                    "data": {"kind": "any"},
                },
                "additionalProperties": False,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=False,
        )
        return self._client.request(
            method="put",
            path=(
                "v2",
                "products",
                str(product),
                "variants",
                str(variant),
                "deliverable",
            ),
            body=body,
            model=SdkReplaceVariantDeliverableConfigurationResponseValue200ApplicationJson,
            request_options=request_options,
        )


class AsyncVariantDeliverables:
    """Variant Deliverables API resources (async)."""

    def __init__(self, client: AsyncSellAppClient) -> None:
        self._client = client
        self.with_raw_response = WithRawResponse(self)

    async def get(
        self,
        product: str,
        variant: int,
        *,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkGetVariantDeliverableConfigurationResponseValue200ApplicationJson:
        """Retrieve variant deliverable configuration

        Retrieve variant deliverable configuration. Requires the listing Sanctum ability. Returns delivery types and safe configuration for a variant in the authenticated store. Dynamic endpoint URLs and inventory data are not returned. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            product: The product identifier or slug. The product must belong to the authenticated store.
            variant: The product variant identifier.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkGetVariantDeliverableConfigurationResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "getVariantDeliverableConfiguration",
        }
        return await self._client.request(
            method="get",
            path=(
                "v2",
                "products",
                str(product),
                "variants",
                str(variant),
                "deliverable",
            ),
            model=SdkGetVariantDeliverableConfigurationResponseValue200ApplicationJson,
            request_options=request_options,
        )

    async def replace(
        self,
        product: str,
        variant: int,
        *,
        types: List[
            Union[
                SdkReplaceVariantDeliverableConfigurationRequestApplicationJsonTypes,
                str,
            ]
        ],
        data: ReplaceVariantDeliverableConfigurationRequestApplicationJsonPropertyData,
        expected_updated_at: Optional[str] = None,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkReplaceVariantDeliverableConfigurationResponseValue200ApplicationJson:
        """Replace variant deliverable configuration

        Replace variant deliverable configuration. Requires the listing Sanctum ability. Replaces this resource's delivery types atomically. Pass expected_updated_at from the product variant resource to reject stale updates with 422. TEXT, BOOKING, CREDITS, and BUNDLE must remain unchanged. Removing DOWNLOADABLE deletes managed files and folders after commit; removing a type clears its private configuration. Dynamic endpoints are write-only. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            product: The product identifier or slug. The product must belong to the authenticated store.
            variant: The product variant identifier.
            types: The complete type list. TEXT, BOOKING, CREDITS, and BUNDLE are owned by their dedicated resources and must remain unchanged here.
            expected_updated_at: Optional optimistic-concurrency snapshot from the deliverable resource's updated_at field. The request returns 422 if the variant changed after this timestamp was loaded.
            data: The data.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkReplaceVariantDeliverableConfigurationResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "replaceVariantDeliverableConfiguration",
        }
        body: Dict[str, Any] = {
            k: v
            for k, v in {
                "types": types,
                "expected_updated_at": expected_updated_at,
                "data": data.to_dict(),
            }.items()
            if v is not None
        }
        _validate_model(
            body,
            {
                "name": "SdkReplaceVariantDeliverableConfigurationRequestApplicationJson",
                "required": ["types", "data"],
                "properties": {
                    "types": {
                        "kind": "array",
                        "items": {
                            "kind": "enum",
                            "values": [
                                "DOWNLOADABLE",
                                "TEXT",
                                "DYNAMIC",
                                "MANUAL",
                                "LICENSE_KEY",
                                "BUNDLE",
                                "BOOKING",
                                "CREDITS",
                            ],
                        },
                        "constraints": {"minItems": 1, "uniqueItems": True},
                    },
                    "expected_updated_at": {
                        "kind": "primitive",
                        "type": "string",
                        "format": "date-time",
                    },
                    "data": {"kind": "any"},
                },
                "additionalProperties": False,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=False,
        )
        return await self._client.request(
            method="put",
            path=(
                "v2",
                "products",
                str(product),
                "variants",
                str(variant),
                "deliverable",
            ),
            body=body,
            model=SdkReplaceVariantDeliverableConfigurationResponseValue200ApplicationJson,
            request_options=request_options,
        )
