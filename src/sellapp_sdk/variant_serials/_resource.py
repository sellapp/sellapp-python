# This file is auto-generated by oagen. Do not edit.

from __future__ import annotations

from typing import TYPE_CHECKING, Any, Dict, List, Optional, Union

from .._base_client import WithRawResponse

if TYPE_CHECKING:
    from .._client import AsyncSellAppClient, SellAppClient

from sellapp_sdk.common.models.sdk_queue_variant_serial_import_request_multipart_form_data_mode import (
    SdkQueueVariantSerialImportRequestMultipartFormDataMode,
)
from sellapp_sdk.common.models.sdk_queue_variant_serial_import_request_multipart_form_data_parsing_mode import (
    SdkQueueVariantSerialImportRequestMultipartFormDataParsingMode,
)

from .._pagination import AsyncPage, SyncPage
from .._types import NOT_GIVEN, NotGiven, RequestOptions, _validate_model, enum_value
from .models import (
    ListVariantSerialInventoryResponseValue200ApplicationJsonPropertyDataItem,
    SdkAppendVariantSerialInventoryResponseValue201ApplicationJson,
    SdkQueueVariantSerialImportResponseValue202ApplicationJson,
    SdkReplaceVariantSerialInventoryResponseValue200ApplicationJson,
)


class VariantSerials:
    """Variant Serials API resources."""

    def __init__(self, client: SellAppClient) -> None:
        self._client = client
        self.with_raw_response = WithRawResponse(self)

    def list(
        self,
        product: int,
        variant: int,
        *,
        limit: Optional[int] = None,
        before: Optional[str] = None,
        after: Optional[str] = None,
        order: Optional[str] = None,
        search: Optional[str] = None,
        page: Optional[int] = None,
        request_options: Optional[RequestOptions] = None,
    ) -> SyncPage[
        ListVariantSerialInventoryResponseValue200ApplicationJsonPropertyDataItem
    ]:
        """List variant serial inventory

        Lists unsold serial inventory for a variant in the current store. Requires the `listing` Sanctum ability. Serial metadata is not exposed. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            product: The product path parameter.
            variant: The variant path parameter.
            limit: Number of inventory entries per page. Defaults to `50`.
            before: The before.
            after: The after.
            order: The order.
            search: Filter inventory by a partial serial value.
            page: Page number to return. Defaults to `1`.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SyncPage[ListVariantSerialInventoryResponseValue200ApplicationJsonPropertyDataItem]

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "listVariantSerialInventory",
        }
        params = {
            k: v
            for k, v in {
                "limit": limit,
                "before": before,
                "after": after,
                "order": order,
                "search": search,
                "page": page,
            }.items()
            if v is not None
        }
        return self._client.request_page(
            method="get",
            path=("v2", "products", str(product), "variants", str(variant), "serials"),
            model=ListVariantSerialInventoryResponseValue200ApplicationJsonPropertyDataItem,
            params=params,
            request_options=request_options,
        )

    def append(
        self,
        product: int,
        variant: int,
        *,
        serials: List[str],
        remove_duplicates: Optional[bool] = None,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkAppendVariantSerialInventoryResponseValue201ApplicationJson:
        """Append variant serial inventory

        Atomically appends unsold serials and recalculates variant stock. Requires the `listing` Sanctum ability. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            product: The product path parameter.
            variant: The variant path parameter.
            serials: Serial values to append or use as the replacement inventory.
            remove_duplicates: Remove duplicate values from this submitted payload.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkAppendVariantSerialInventoryResponseValue201ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "appendVariantSerialInventory",
        }
        body: Dict[str, Any] = {
            k: v
            for k, v in {
                "serials": serials,
                "remove_duplicates": remove_duplicates,
            }.items()
            if v is not None
        }
        _validate_model(
            body,
            {
                "name": "SdkAppendVariantSerialInventoryRequestApplicationJson",
                "required": ["serials"],
                "properties": {
                    "serials": {
                        "kind": "array",
                        "items": {
                            "kind": "primitive",
                            "type": "string",
                            "constraints": {"maxLength": 2048},
                        },
                        "constraints": {"minItems": 1, "maxItems": 5000},
                    },
                    "remove_duplicates": {"kind": "primitive", "type": "boolean"},
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return self._client.request(
            method="post",
            path=("v2", "products", str(product), "variants", str(variant), "serials"),
            body=body,
            model=SdkAppendVariantSerialInventoryResponseValue201ApplicationJson,
            request_options=request_options,
        )

    def replace(
        self,
        product: int,
        variant: int,
        *,
        serials: List[str],
        remove_duplicates: Optional[bool] = None,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkReplaceVariantSerialInventoryResponseValue200ApplicationJson:
        """Replace variant serial inventory

        Atomically replaces all unsold serials and recalculates variant stock. Concurrent inventory mutations are serialized. Requires the `listing` Sanctum ability. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            product: The product path parameter.
            variant: The variant path parameter.
            serials: Serial values to append or use as the replacement inventory.
            remove_duplicates: Remove duplicate values from this submitted payload.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkReplaceVariantSerialInventoryResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "replaceVariantSerialInventory",
        }
        body: Dict[str, Any] = {
            k: v
            for k, v in {
                "serials": serials,
                "remove_duplicates": remove_duplicates,
            }.items()
            if v is not None
        }
        _validate_model(
            body,
            {
                "name": "SdkReplaceVariantSerialInventoryRequestApplicationJson",
                "required": ["serials"],
                "properties": {
                    "serials": {
                        "kind": "array",
                        "items": {
                            "kind": "primitive",
                            "type": "string",
                            "constraints": {"maxLength": 2048},
                        },
                        "constraints": {"minItems": 1, "maxItems": 5000},
                    },
                    "remove_duplicates": {"kind": "primitive", "type": "boolean"},
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return self._client.request(
            method="put",
            path=("v2", "products", str(product), "variants", str(variant), "serials"),
            body=body,
            model=SdkReplaceVariantSerialInventoryResponseValue200ApplicationJson,
            request_options=request_options,
        )

    def queue(
        self,
        product: int,
        variant: int,
        *,
        file: bytes,
        parsing_mode: Union[
            SdkQueueVariantSerialImportRequestMultipartFormDataParsingMode, str
        ],
        custom_delimiter: Union[str, None, NotGiven] = NOT_GIVEN,
        remove_duplicates: Optional[bool] = None,
        mode: Optional[
            Union[SdkQueueVariantSerialImportRequestMultipartFormDataMode, str]
        ] = None,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkQueueVariantSerialImportResponseValue202ApplicationJson:
        """Queue a variant serial import

        Uploads a text file to application-owned temporary storage and queues an atomic serial import. Use this endpoint for large imports. Requires the `listing` Sanctum ability. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            product: The product path parameter.
            variant: The variant path parameter.
            file: A text file up to 100 MiB.
            parsing_mode: How the uploaded file is split into serials. CUSTOM requires custom_delimiter; sending CUSTOM without it fails validation with a 422.
            custom_delimiter: The delimiter to split on. Required when parsing_mode is CUSTOM (omitting it fails validation with a 422); ignored otherwise.
            remove_duplicates: The remove duplicates.
            mode: The mode.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkQueueVariantSerialImportResponseValue202ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "form-data",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "queueVariantSerialImport",
        }
        body: Dict[str, Any] = {
            k: v
            for k, v in {
                "file": file,
                "parsing_mode": enum_value(parsing_mode),
                "remove_duplicates": remove_duplicates,
                "mode": enum_value(mode) if mode is not None else None,
            }.items()
            if v is not None
        }
        if not isinstance(custom_delimiter, NotGiven):
            body["custom_delimiter"] = custom_delimiter
        _validate_model(
            body,
            {
                "name": "SdkQueueVariantSerialImportRequestMultipartFormData",
                "required": ["file", "parsing_mode"],
                "properties": {
                    "file": {
                        "kind": "primitive",
                        "type": "string",
                        "format": "binary",
                        "constraints": {"minLength": 1},
                    },
                    "parsing_mode": {
                        "kind": "enum",
                        "values": ["COMMA", "NEW_LINE", "SPACE", "CUSTOM"],
                    },
                    "custom_delimiter": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "string",
                            "constraints": {"maxLength": 32},
                        },
                    },
                    "remove_duplicates": {"kind": "primitive", "type": "boolean"},
                    "mode": {"kind": "enum", "values": ["append", "replace"]},
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        multipart_files: Dict[str, Any] = {}
        if "file" in body:
            multipart_files["file"] = body.pop("file")
        request_options = {
            **(request_options or {}),
            "files": {
                **multipart_files,
                **((request_options or {}).get("files") or {}),
            },
        }
        return self._client.request(
            method="post",
            path=(
                "v2",
                "products",
                str(product),
                "variants",
                str(variant),
                "serials",
                "import",
            ),
            body=body,
            model=SdkQueueVariantSerialImportResponseValue202ApplicationJson,
            request_options=request_options,
        )

    def delete(
        self,
        product: int,
        variant: int,
        serial: str,
        *,
        request_options: Optional[RequestOptions] = None,
    ) -> None:
        """Delete a variant serial

        Deletes one unsold serial scoped to the current store, product, and variant, then recalculates stock. Requires the `listing` Sanctum ability. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            product: The product path parameter.
            variant: The variant path parameter.
            serial: The unsold serial entry ULID.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "deleteVariantSerial",
        }
        self._client.request(
            method="delete",
            path=(
                "v2",
                "products",
                str(product),
                "variants",
                str(variant),
                "serials",
                str(serial),
            ),
            request_options=request_options,
        )


class AsyncVariantSerials:
    """Variant Serials API resources (async)."""

    def __init__(self, client: AsyncSellAppClient) -> None:
        self._client = client
        self.with_raw_response = WithRawResponse(self)

    async def list(
        self,
        product: int,
        variant: int,
        *,
        limit: Optional[int] = None,
        before: Optional[str] = None,
        after: Optional[str] = None,
        order: Optional[str] = None,
        search: Optional[str] = None,
        page: Optional[int] = None,
        request_options: Optional[RequestOptions] = None,
    ) -> AsyncPage[
        ListVariantSerialInventoryResponseValue200ApplicationJsonPropertyDataItem
    ]:
        """List variant serial inventory

        Lists unsold serial inventory for a variant in the current store. Requires the `listing` Sanctum ability. Serial metadata is not exposed. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            product: The product path parameter.
            variant: The variant path parameter.
            limit: Number of inventory entries per page. Defaults to `50`.
            before: The before.
            after: The after.
            order: The order.
            search: Filter inventory by a partial serial value.
            page: Page number to return. Defaults to `1`.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            AsyncPage[ListVariantSerialInventoryResponseValue200ApplicationJsonPropertyDataItem]

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "listVariantSerialInventory",
        }
        params = {
            k: v
            for k, v in {
                "limit": limit,
                "before": before,
                "after": after,
                "order": order,
                "search": search,
                "page": page,
            }.items()
            if v is not None
        }
        return await self._client.request_page(
            method="get",
            path=("v2", "products", str(product), "variants", str(variant), "serials"),
            model=ListVariantSerialInventoryResponseValue200ApplicationJsonPropertyDataItem,
            params=params,
            request_options=request_options,
        )

    async def append(
        self,
        product: int,
        variant: int,
        *,
        serials: List[str],
        remove_duplicates: Optional[bool] = None,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkAppendVariantSerialInventoryResponseValue201ApplicationJson:
        """Append variant serial inventory

        Atomically appends unsold serials and recalculates variant stock. Requires the `listing` Sanctum ability. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            product: The product path parameter.
            variant: The variant path parameter.
            serials: Serial values to append or use as the replacement inventory.
            remove_duplicates: Remove duplicate values from this submitted payload.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkAppendVariantSerialInventoryResponseValue201ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "appendVariantSerialInventory",
        }
        body: Dict[str, Any] = {
            k: v
            for k, v in {
                "serials": serials,
                "remove_duplicates": remove_duplicates,
            }.items()
            if v is not None
        }
        _validate_model(
            body,
            {
                "name": "SdkAppendVariantSerialInventoryRequestApplicationJson",
                "required": ["serials"],
                "properties": {
                    "serials": {
                        "kind": "array",
                        "items": {
                            "kind": "primitive",
                            "type": "string",
                            "constraints": {"maxLength": 2048},
                        },
                        "constraints": {"minItems": 1, "maxItems": 5000},
                    },
                    "remove_duplicates": {"kind": "primitive", "type": "boolean"},
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return await self._client.request(
            method="post",
            path=("v2", "products", str(product), "variants", str(variant), "serials"),
            body=body,
            model=SdkAppendVariantSerialInventoryResponseValue201ApplicationJson,
            request_options=request_options,
        )

    async def replace(
        self,
        product: int,
        variant: int,
        *,
        serials: List[str],
        remove_duplicates: Optional[bool] = None,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkReplaceVariantSerialInventoryResponseValue200ApplicationJson:
        """Replace variant serial inventory

        Atomically replaces all unsold serials and recalculates variant stock. Concurrent inventory mutations are serialized. Requires the `listing` Sanctum ability. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            product: The product path parameter.
            variant: The variant path parameter.
            serials: Serial values to append or use as the replacement inventory.
            remove_duplicates: Remove duplicate values from this submitted payload.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkReplaceVariantSerialInventoryResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "replaceVariantSerialInventory",
        }
        body: Dict[str, Any] = {
            k: v
            for k, v in {
                "serials": serials,
                "remove_duplicates": remove_duplicates,
            }.items()
            if v is not None
        }
        _validate_model(
            body,
            {
                "name": "SdkReplaceVariantSerialInventoryRequestApplicationJson",
                "required": ["serials"],
                "properties": {
                    "serials": {
                        "kind": "array",
                        "items": {
                            "kind": "primitive",
                            "type": "string",
                            "constraints": {"maxLength": 2048},
                        },
                        "constraints": {"minItems": 1, "maxItems": 5000},
                    },
                    "remove_duplicates": {"kind": "primitive", "type": "boolean"},
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return await self._client.request(
            method="put",
            path=("v2", "products", str(product), "variants", str(variant), "serials"),
            body=body,
            model=SdkReplaceVariantSerialInventoryResponseValue200ApplicationJson,
            request_options=request_options,
        )

    async def queue(
        self,
        product: int,
        variant: int,
        *,
        file: bytes,
        parsing_mode: Union[
            SdkQueueVariantSerialImportRequestMultipartFormDataParsingMode, str
        ],
        custom_delimiter: Union[str, None, NotGiven] = NOT_GIVEN,
        remove_duplicates: Optional[bool] = None,
        mode: Optional[
            Union[SdkQueueVariantSerialImportRequestMultipartFormDataMode, str]
        ] = None,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkQueueVariantSerialImportResponseValue202ApplicationJson:
        """Queue a variant serial import

        Uploads a text file to application-owned temporary storage and queues an atomic serial import. Use this endpoint for large imports. Requires the `listing` Sanctum ability. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            product: The product path parameter.
            variant: The variant path parameter.
            file: A text file up to 100 MiB.
            parsing_mode: How the uploaded file is split into serials. CUSTOM requires custom_delimiter; sending CUSTOM without it fails validation with a 422.
            custom_delimiter: The delimiter to split on. Required when parsing_mode is CUSTOM (omitting it fails validation with a 422); ignored otherwise.
            remove_duplicates: The remove duplicates.
            mode: The mode.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkQueueVariantSerialImportResponseValue202ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "form-data",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "queueVariantSerialImport",
        }
        body: Dict[str, Any] = {
            k: v
            for k, v in {
                "file": file,
                "parsing_mode": enum_value(parsing_mode),
                "remove_duplicates": remove_duplicates,
                "mode": enum_value(mode) if mode is not None else None,
            }.items()
            if v is not None
        }
        if not isinstance(custom_delimiter, NotGiven):
            body["custom_delimiter"] = custom_delimiter
        _validate_model(
            body,
            {
                "name": "SdkQueueVariantSerialImportRequestMultipartFormData",
                "required": ["file", "parsing_mode"],
                "properties": {
                    "file": {
                        "kind": "primitive",
                        "type": "string",
                        "format": "binary",
                        "constraints": {"minLength": 1},
                    },
                    "parsing_mode": {
                        "kind": "enum",
                        "values": ["COMMA", "NEW_LINE", "SPACE", "CUSTOM"],
                    },
                    "custom_delimiter": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "string",
                            "constraints": {"maxLength": 32},
                        },
                    },
                    "remove_duplicates": {"kind": "primitive", "type": "boolean"},
                    "mode": {"kind": "enum", "values": ["append", "replace"]},
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        multipart_files: Dict[str, Any] = {}
        if "file" in body:
            multipart_files["file"] = body.pop("file")
        request_options = {
            **(request_options or {}),
            "files": {
                **multipart_files,
                **((request_options or {}).get("files") or {}),
            },
        }
        return await self._client.request(
            method="post",
            path=(
                "v2",
                "products",
                str(product),
                "variants",
                str(variant),
                "serials",
                "import",
            ),
            body=body,
            model=SdkQueueVariantSerialImportResponseValue202ApplicationJson,
            request_options=request_options,
        )

    async def delete(
        self,
        product: int,
        variant: int,
        serial: str,
        *,
        request_options: Optional[RequestOptions] = None,
    ) -> None:
        """Delete a variant serial

        Deletes one unsold serial scoped to the current store, product, and variant, then recalculates stock. Requires the `listing` Sanctum ability. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            product: The product path parameter.
            variant: The variant path parameter.
            serial: The unsold serial entry ULID.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "deleteVariantSerial",
        }
        await self._client.request(
            method="delete",
            path=(
                "v2",
                "products",
                str(product),
                "variants",
                str(variant),
                "serials",
                str(serial),
            ),
            request_options=request_options,
        )
