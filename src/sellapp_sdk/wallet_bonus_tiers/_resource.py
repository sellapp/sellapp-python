# This file is auto-generated by oagen. Do not edit.

from __future__ import annotations

from typing import TYPE_CHECKING, Any

from .._base_client import WithRawResponse

if TYPE_CHECKING:
    from .._client import AsyncSellAppClient, SellAppClient

from sellapp_sdk.common.models.sdk_create_wallet_bonus_tier_request_application_json_bonus_kind import (
    SdkCreateWalletBonusTierRequestApplicationJsonBonusKind,
)
from sellapp_sdk.common.models.sdk_create_wallet_bonus_tier_response_value_201_application_json import (
    SdkCreateWalletBonusTierResponseValue201ApplicationJson,
)
from sellapp_sdk.common.models.sdk_replace_wallet_bonus_tier_request_application_json_bonus_kind import (
    SdkReplaceWalletBonusTierRequestApplicationJsonBonusKind,
)
from sellapp_sdk.common.models.sdk_update_wallet_bonus_tier_request_application_json_bonus_kind import (
    SdkUpdateWalletBonusTierRequestApplicationJsonBonusKind,
)

from .._types import NOT_GIVEN, NotGiven, RequestOptions, _validate_model, enum_value
from .models import (
    SdkListWalletBonusTiersResponseValue200ApplicationJson,
    SdkReplaceWalletBonusTierResponseValue200ApplicationJson,
    SdkRestoreWalletBonusTierResponseValue200ApplicationJson,
    SdkUpdateWalletBonusTierResponseValue200ApplicationJson,
)


class WalletBonusTiers:
    """Wallet Bonus Tiers API resources."""

    def __init__(self, client: SellAppClient) -> None:
        self._client = client
        self.with_raw_response = WithRawResponse(self)

    def list(
        self,
        *,
        request_options: RequestOptions | None = None,
    ) -> SdkListWalletBonusTiersResponseValue200ApplicationJson:
        """List wallet bonus tiers

        List current and archived store records. Requires the `wallet` or `wallet:write` API ability. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Returns:
            SdkListWalletBonusTiersResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "listWalletBonusTiers",
        }
        return self._client.request(
            method="get",
            path=("v2", "wallet-bonus-tiers"),
            model=SdkListWalletBonusTiersResponseValue200ApplicationJson,
            request_options=request_options,
        )

    def create(
        self,
        *,
        minimum_top_up_cents: int,
        bonus_kind: SdkCreateWalletBonusTierRequestApplicationJsonBonusKind | str,
        fixed_bonus_cents: int | None,
        percent_basis: int | None,
        maximum_bonus_cents: int | None,
        priority: int,
        is_active: bool,
        request_options: RequestOptions | None = None,
    ) -> SdkCreateWalletBonusTierResponseValue201ApplicationJson:
        """Create a wallet bonus tier

        Create a wallet bonus tier. Requires the `wallet:write` API ability and the store wallet permission. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            minimum_top_up_cents: The minimum top up cents.
            bonus_kind: The bonus kind.
            fixed_bonus_cents: The fixed bonus cents.
            percent_basis: The percent basis.
            maximum_bonus_cents: The maximum bonus cents.
            priority: The priority.
            is_active: The is active.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkCreateWalletBonusTierResponseValue201ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "createWalletBonusTier",
        }
        body: dict[str, Any] = {
            "minimum_top_up_cents": minimum_top_up_cents,
            "bonus_kind": enum_value(bonus_kind),
            "fixed_bonus_cents": fixed_bonus_cents,
            "percent_basis": percent_basis,
            "maximum_bonus_cents": maximum_bonus_cents,
            "priority": priority,
            "is_active": is_active,
        }
        _validate_model(
            body,
            {
                "name": "SdkCreateWalletBonusTierRequestApplicationJson",
                "required": [
                    "minimum_top_up_cents",
                    "bonus_kind",
                    "fixed_bonus_cents",
                    "percent_basis",
                    "maximum_bonus_cents",
                    "priority",
                    "is_active",
                ],
                "properties": {
                    "minimum_top_up_cents": {
                        "kind": "primitive",
                        "type": "integer",
                        "constraints": {"minimum": 1, "maximum": 999999999999},
                    },
                    "bonus_kind": {"kind": "enum", "values": ["fixed", "percent"]},
                    "fixed_bonus_cents": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "integer",
                            "constraints": {"minimum": 1, "maximum": 999999999999},
                        },
                    },
                    "percent_basis": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "integer",
                            "constraints": {"minimum": 1, "maximum": 10000},
                        },
                    },
                    "maximum_bonus_cents": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "integer",
                            "constraints": {"minimum": 1, "maximum": 999999999999},
                        },
                    },
                    "priority": {
                        "kind": "primitive",
                        "type": "integer",
                        "constraints": {"minimum": 0, "maximum": 65535},
                    },
                    "is_active": {"kind": "primitive", "type": "boolean"},
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return self._client.request(
            method="post",
            path=("v2", "wallet-bonus-tiers"),
            body=body,
            model=SdkCreateWalletBonusTierResponseValue201ApplicationJson,
            request_options=request_options,
        )

    def replace(
        self,
        bonus_tier: int,
        *,
        minimum_top_up_cents: int,
        bonus_kind: SdkReplaceWalletBonusTierRequestApplicationJsonBonusKind | str,
        fixed_bonus_cents: int | None,
        percent_basis: int | None,
        maximum_bonus_cents: int | None,
        priority: int,
        is_active: bool,
        request_options: RequestOptions | None = None,
    ) -> SdkReplaceWalletBonusTierResponseValue200ApplicationJson:
        """Update a wallet bonus tier

        Update a wallet bonus tier. Requires the `wallet:write` API ability and the store wallet permission. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            bonus_tier: The bonusTier path parameter.
            minimum_top_up_cents: The minimum top up cents.
            bonus_kind: The bonus kind.
            fixed_bonus_cents: The fixed bonus cents.
            percent_basis: The percent basis.
            maximum_bonus_cents: The maximum bonus cents.
            priority: The priority.
            is_active: The is active.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkReplaceWalletBonusTierResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "replaceWalletBonusTier",
        }
        body: dict[str, Any] = {
            "minimum_top_up_cents": minimum_top_up_cents,
            "bonus_kind": enum_value(bonus_kind),
            "fixed_bonus_cents": fixed_bonus_cents,
            "percent_basis": percent_basis,
            "maximum_bonus_cents": maximum_bonus_cents,
            "priority": priority,
            "is_active": is_active,
        }
        _validate_model(
            body,
            {
                "name": "SdkReplaceWalletBonusTierRequestApplicationJson",
                "required": [
                    "minimum_top_up_cents",
                    "bonus_kind",
                    "fixed_bonus_cents",
                    "percent_basis",
                    "maximum_bonus_cents",
                    "priority",
                    "is_active",
                ],
                "properties": {
                    "minimum_top_up_cents": {
                        "kind": "primitive",
                        "type": "integer",
                        "constraints": {"minimum": 1, "maximum": 999999999999},
                    },
                    "bonus_kind": {"kind": "enum", "values": ["fixed", "percent"]},
                    "fixed_bonus_cents": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "integer",
                            "constraints": {"minimum": 1, "maximum": 999999999999},
                        },
                    },
                    "percent_basis": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "integer",
                            "constraints": {"minimum": 1, "maximum": 10000},
                        },
                    },
                    "maximum_bonus_cents": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "integer",
                            "constraints": {"minimum": 1, "maximum": 999999999999},
                        },
                    },
                    "priority": {
                        "kind": "primitive",
                        "type": "integer",
                        "constraints": {"minimum": 0, "maximum": 65535},
                    },
                    "is_active": {"kind": "primitive", "type": "boolean"},
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return self._client.request(
            method="put",
            path=("v2", "wallet-bonus-tiers", str(bonus_tier)),
            body=body,
            model=SdkReplaceWalletBonusTierResponseValue200ApplicationJson,
            request_options=request_options,
        )

    def update(
        self,
        bonus_tier: int,
        *,
        minimum_top_up_cents: int | None = None,
        bonus_kind: SdkUpdateWalletBonusTierRequestApplicationJsonBonusKind
        | str
        | None = None,
        fixed_bonus_cents: int | None | NotGiven = NOT_GIVEN,
        percent_basis: int | None | NotGiven = NOT_GIVEN,
        maximum_bonus_cents: int | None | NotGiven = NOT_GIVEN,
        priority: int | None = None,
        is_active: bool | None = None,
        request_options: RequestOptions | None = None,
    ) -> SdkUpdateWalletBonusTierResponseValue200ApplicationJson:
        """Update a wallet bonus tier

        Update a wallet bonus tier. Requires the `wallet:write` API ability and the store wallet permission. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            bonus_tier: The bonusTier path parameter.
            minimum_top_up_cents: The minimum top up cents.
            bonus_kind: The bonus kind.
            fixed_bonus_cents: The fixed bonus cents.
            percent_basis: The percent basis.
            maximum_bonus_cents: The maximum bonus cents.
            priority: The priority.
            is_active: The is active.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkUpdateWalletBonusTierResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "updateWalletBonusTier",
        }
        body: dict[str, Any] = {
            k: v
            for k, v in {
                "minimum_top_up_cents": minimum_top_up_cents,
                "bonus_kind": enum_value(bonus_kind)
                if bonus_kind is not None
                else None,
                "priority": priority,
                "is_active": is_active,
            }.items()
            if v is not None
        }
        if not isinstance(fixed_bonus_cents, NotGiven):
            body["fixed_bonus_cents"] = fixed_bonus_cents
        if not isinstance(percent_basis, NotGiven):
            body["percent_basis"] = percent_basis
        if not isinstance(maximum_bonus_cents, NotGiven):
            body["maximum_bonus_cents"] = maximum_bonus_cents
        _validate_model(
            body,
            {
                "name": "SdkUpdateWalletBonusTierRequestApplicationJson",
                "required": [],
                "properties": {
                    "minimum_top_up_cents": {
                        "kind": "primitive",
                        "type": "integer",
                        "constraints": {"minimum": 1, "maximum": 999999999999},
                    },
                    "bonus_kind": {"kind": "enum", "values": ["fixed", "percent"]},
                    "fixed_bonus_cents": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "integer",
                            "constraints": {"minimum": 1, "maximum": 999999999999},
                        },
                    },
                    "percent_basis": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "integer",
                            "constraints": {"minimum": 1, "maximum": 10000},
                        },
                    },
                    "maximum_bonus_cents": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "integer",
                            "constraints": {"minimum": 1, "maximum": 999999999999},
                        },
                    },
                    "priority": {
                        "kind": "primitive",
                        "type": "integer",
                        "constraints": {"minimum": 0, "maximum": 65535},
                    },
                    "is_active": {"kind": "primitive", "type": "boolean"},
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return self._client.request(
            method="patch",
            path=("v2", "wallet-bonus-tiers", str(bonus_tier)),
            body=body,
            model=SdkUpdateWalletBonusTierResponseValue200ApplicationJson,
            request_options=request_options,
        )

    def archive(
        self,
        bonus_tier: int,
        *,
        request_options: RequestOptions | None = None,
    ) -> None:
        """Archive a wallet bonus tier

        Archive a wallet bonus tier. Requires the `wallet:write` API ability and the store wallet permission. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            bonus_tier: The bonusTier path parameter.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "archiveWalletBonusTier",
        }
        self._client.request(
            method="delete",
            path=("v2", "wallet-bonus-tiers", str(bonus_tier)),
            request_options=request_options,
        )

    def restore(
        self,
        bonus_tier: int,
        *,
        request_options: RequestOptions | None = None,
    ) -> SdkRestoreWalletBonusTierResponseValue200ApplicationJson:
        """Restore a wallet bonus tier

        Restore a wallet bonus tier. Requires the `wallet:write` API ability and the store wallet permission. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            bonus_tier: The bonusTier path parameter.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkRestoreWalletBonusTierResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "restoreWalletBonusTier",
        }
        return self._client.request(
            method="post",
            path=("v2", "wallet-bonus-tiers", str(bonus_tier), "restore"),
            model=SdkRestoreWalletBonusTierResponseValue200ApplicationJson,
            request_options=request_options,
        )


class AsyncWalletBonusTiers:
    """Wallet Bonus Tiers API resources (async)."""

    def __init__(self, client: AsyncSellAppClient) -> None:
        self._client = client
        self.with_raw_response = WithRawResponse(self)

    async def list(
        self,
        *,
        request_options: RequestOptions | None = None,
    ) -> SdkListWalletBonusTiersResponseValue200ApplicationJson:
        """List wallet bonus tiers

        List current and archived store records. Requires the `wallet` or `wallet:write` API ability. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Returns:
            SdkListWalletBonusTiersResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "listWalletBonusTiers",
        }
        return await self._client.request(
            method="get",
            path=("v2", "wallet-bonus-tiers"),
            model=SdkListWalletBonusTiersResponseValue200ApplicationJson,
            request_options=request_options,
        )

    async def create(
        self,
        *,
        minimum_top_up_cents: int,
        bonus_kind: SdkCreateWalletBonusTierRequestApplicationJsonBonusKind | str,
        fixed_bonus_cents: int | None,
        percent_basis: int | None,
        maximum_bonus_cents: int | None,
        priority: int,
        is_active: bool,
        request_options: RequestOptions | None = None,
    ) -> SdkCreateWalletBonusTierResponseValue201ApplicationJson:
        """Create a wallet bonus tier

        Create a wallet bonus tier. Requires the `wallet:write` API ability and the store wallet permission. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            minimum_top_up_cents: The minimum top up cents.
            bonus_kind: The bonus kind.
            fixed_bonus_cents: The fixed bonus cents.
            percent_basis: The percent basis.
            maximum_bonus_cents: The maximum bonus cents.
            priority: The priority.
            is_active: The is active.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkCreateWalletBonusTierResponseValue201ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "createWalletBonusTier",
        }
        body: dict[str, Any] = {
            "minimum_top_up_cents": minimum_top_up_cents,
            "bonus_kind": enum_value(bonus_kind),
            "fixed_bonus_cents": fixed_bonus_cents,
            "percent_basis": percent_basis,
            "maximum_bonus_cents": maximum_bonus_cents,
            "priority": priority,
            "is_active": is_active,
        }
        _validate_model(
            body,
            {
                "name": "SdkCreateWalletBonusTierRequestApplicationJson",
                "required": [
                    "minimum_top_up_cents",
                    "bonus_kind",
                    "fixed_bonus_cents",
                    "percent_basis",
                    "maximum_bonus_cents",
                    "priority",
                    "is_active",
                ],
                "properties": {
                    "minimum_top_up_cents": {
                        "kind": "primitive",
                        "type": "integer",
                        "constraints": {"minimum": 1, "maximum": 999999999999},
                    },
                    "bonus_kind": {"kind": "enum", "values": ["fixed", "percent"]},
                    "fixed_bonus_cents": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "integer",
                            "constraints": {"minimum": 1, "maximum": 999999999999},
                        },
                    },
                    "percent_basis": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "integer",
                            "constraints": {"minimum": 1, "maximum": 10000},
                        },
                    },
                    "maximum_bonus_cents": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "integer",
                            "constraints": {"minimum": 1, "maximum": 999999999999},
                        },
                    },
                    "priority": {
                        "kind": "primitive",
                        "type": "integer",
                        "constraints": {"minimum": 0, "maximum": 65535},
                    },
                    "is_active": {"kind": "primitive", "type": "boolean"},
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return await self._client.request(
            method="post",
            path=("v2", "wallet-bonus-tiers"),
            body=body,
            model=SdkCreateWalletBonusTierResponseValue201ApplicationJson,
            request_options=request_options,
        )

    async def replace(
        self,
        bonus_tier: int,
        *,
        minimum_top_up_cents: int,
        bonus_kind: SdkReplaceWalletBonusTierRequestApplicationJsonBonusKind | str,
        fixed_bonus_cents: int | None,
        percent_basis: int | None,
        maximum_bonus_cents: int | None,
        priority: int,
        is_active: bool,
        request_options: RequestOptions | None = None,
    ) -> SdkReplaceWalletBonusTierResponseValue200ApplicationJson:
        """Update a wallet bonus tier

        Update a wallet bonus tier. Requires the `wallet:write` API ability and the store wallet permission. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            bonus_tier: The bonusTier path parameter.
            minimum_top_up_cents: The minimum top up cents.
            bonus_kind: The bonus kind.
            fixed_bonus_cents: The fixed bonus cents.
            percent_basis: The percent basis.
            maximum_bonus_cents: The maximum bonus cents.
            priority: The priority.
            is_active: The is active.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkReplaceWalletBonusTierResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "replaceWalletBonusTier",
        }
        body: dict[str, Any] = {
            "minimum_top_up_cents": minimum_top_up_cents,
            "bonus_kind": enum_value(bonus_kind),
            "fixed_bonus_cents": fixed_bonus_cents,
            "percent_basis": percent_basis,
            "maximum_bonus_cents": maximum_bonus_cents,
            "priority": priority,
            "is_active": is_active,
        }
        _validate_model(
            body,
            {
                "name": "SdkReplaceWalletBonusTierRequestApplicationJson",
                "required": [
                    "minimum_top_up_cents",
                    "bonus_kind",
                    "fixed_bonus_cents",
                    "percent_basis",
                    "maximum_bonus_cents",
                    "priority",
                    "is_active",
                ],
                "properties": {
                    "minimum_top_up_cents": {
                        "kind": "primitive",
                        "type": "integer",
                        "constraints": {"minimum": 1, "maximum": 999999999999},
                    },
                    "bonus_kind": {"kind": "enum", "values": ["fixed", "percent"]},
                    "fixed_bonus_cents": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "integer",
                            "constraints": {"minimum": 1, "maximum": 999999999999},
                        },
                    },
                    "percent_basis": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "integer",
                            "constraints": {"minimum": 1, "maximum": 10000},
                        },
                    },
                    "maximum_bonus_cents": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "integer",
                            "constraints": {"minimum": 1, "maximum": 999999999999},
                        },
                    },
                    "priority": {
                        "kind": "primitive",
                        "type": "integer",
                        "constraints": {"minimum": 0, "maximum": 65535},
                    },
                    "is_active": {"kind": "primitive", "type": "boolean"},
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return await self._client.request(
            method="put",
            path=("v2", "wallet-bonus-tiers", str(bonus_tier)),
            body=body,
            model=SdkReplaceWalletBonusTierResponseValue200ApplicationJson,
            request_options=request_options,
        )

    async def update(
        self,
        bonus_tier: int,
        *,
        minimum_top_up_cents: int | None = None,
        bonus_kind: SdkUpdateWalletBonusTierRequestApplicationJsonBonusKind
        | str
        | None = None,
        fixed_bonus_cents: int | None | NotGiven = NOT_GIVEN,
        percent_basis: int | None | NotGiven = NOT_GIVEN,
        maximum_bonus_cents: int | None | NotGiven = NOT_GIVEN,
        priority: int | None = None,
        is_active: bool | None = None,
        request_options: RequestOptions | None = None,
    ) -> SdkUpdateWalletBonusTierResponseValue200ApplicationJson:
        """Update a wallet bonus tier

        Update a wallet bonus tier. Requires the `wallet:write` API ability and the store wallet permission. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            bonus_tier: The bonusTier path parameter.
            minimum_top_up_cents: The minimum top up cents.
            bonus_kind: The bonus kind.
            fixed_bonus_cents: The fixed bonus cents.
            percent_basis: The percent basis.
            maximum_bonus_cents: The maximum bonus cents.
            priority: The priority.
            is_active: The is active.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkUpdateWalletBonusTierResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "updateWalletBonusTier",
        }
        body: dict[str, Any] = {
            k: v
            for k, v in {
                "minimum_top_up_cents": minimum_top_up_cents,
                "bonus_kind": enum_value(bonus_kind)
                if bonus_kind is not None
                else None,
                "priority": priority,
                "is_active": is_active,
            }.items()
            if v is not None
        }
        if not isinstance(fixed_bonus_cents, NotGiven):
            body["fixed_bonus_cents"] = fixed_bonus_cents
        if not isinstance(percent_basis, NotGiven):
            body["percent_basis"] = percent_basis
        if not isinstance(maximum_bonus_cents, NotGiven):
            body["maximum_bonus_cents"] = maximum_bonus_cents
        _validate_model(
            body,
            {
                "name": "SdkUpdateWalletBonusTierRequestApplicationJson",
                "required": [],
                "properties": {
                    "minimum_top_up_cents": {
                        "kind": "primitive",
                        "type": "integer",
                        "constraints": {"minimum": 1, "maximum": 999999999999},
                    },
                    "bonus_kind": {"kind": "enum", "values": ["fixed", "percent"]},
                    "fixed_bonus_cents": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "integer",
                            "constraints": {"minimum": 1, "maximum": 999999999999},
                        },
                    },
                    "percent_basis": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "integer",
                            "constraints": {"minimum": 1, "maximum": 10000},
                        },
                    },
                    "maximum_bonus_cents": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "integer",
                            "constraints": {"minimum": 1, "maximum": 999999999999},
                        },
                    },
                    "priority": {
                        "kind": "primitive",
                        "type": "integer",
                        "constraints": {"minimum": 0, "maximum": 65535},
                    },
                    "is_active": {"kind": "primitive", "type": "boolean"},
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return await self._client.request(
            method="patch",
            path=("v2", "wallet-bonus-tiers", str(bonus_tier)),
            body=body,
            model=SdkUpdateWalletBonusTierResponseValue200ApplicationJson,
            request_options=request_options,
        )

    async def archive(
        self,
        bonus_tier: int,
        *,
        request_options: RequestOptions | None = None,
    ) -> None:
        """Archive a wallet bonus tier

        Archive a wallet bonus tier. Requires the `wallet:write` API ability and the store wallet permission. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            bonus_tier: The bonusTier path parameter.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "archiveWalletBonusTier",
        }
        await self._client.request(
            method="delete",
            path=("v2", "wallet-bonus-tiers", str(bonus_tier)),
            request_options=request_options,
        )

    async def restore(
        self,
        bonus_tier: int,
        *,
        request_options: RequestOptions | None = None,
    ) -> SdkRestoreWalletBonusTierResponseValue200ApplicationJson:
        """Restore a wallet bonus tier

        Restore a wallet bonus tier. Requires the `wallet:write` API ability and the store wallet permission. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            bonus_tier: The bonusTier path parameter.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkRestoreWalletBonusTierResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "restoreWalletBonusTier",
        }
        return await self._client.request(
            method="post",
            path=("v2", "wallet-bonus-tiers", str(bonus_tier), "restore"),
            model=SdkRestoreWalletBonusTierResponseValue200ApplicationJson,
            request_options=request_options,
        )
