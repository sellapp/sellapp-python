# This file is auto-generated by oagen. Do not edit.

from __future__ import annotations

from typing import TYPE_CHECKING, Any, Dict, List, Optional

from .._base_client import WithRawResponse

if TYPE_CHECKING:
    from .._client import AsyncSellAppClient, SellAppClient

from .._types import RequestOptions, _validate_model
from .models import (
    SdkGetWalletSettingsResponseValue200ApplicationJson,
    SdkReplaceWalletSettingsResponseValue200ApplicationJson,
)


class WalletSettings:
    """Wallet Settings API resources."""

    def __init__(self, client: SellAppClient) -> None:
        self._client = client
        self.with_raw_response = WithRawResponse(self)

    def get(
        self,
        *,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkGetWalletSettingsResponseValue200ApplicationJson:
        """Retrieve wallet settings

        Retrieve wallet settings for the authenticated store. Requires the `wallet` or `wallet:write` API ability. Payment methods are returned as their effective configurable values when no explicit restriction is stored. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Returns:
            SdkGetWalletSettingsResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "getWalletSettings",
        }
        return self._client.request(
            method="get",
            path=("v2", "wallet", "settings"),
            model=SdkGetWalletSettingsResponseValue200ApplicationJson,
            request_options=request_options,
        )

    def replace(
        self,
        *,
        enabled: bool,
        minimum_top_up_cents: Optional[int],
        maximum_top_up_cents: Optional[int],
        expiration_days: Optional[int],
        payment_methods: List[str],
        request_options: Optional[RequestOptions] = None,
    ) -> SdkReplaceWalletSettingsResponseValue200ApplicationJson:
        """Update wallet settings

        Update wallet settings. Requires the `wallet:write` API ability and the store wallet permission. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            enabled: The enabled.
            minimum_top_up_cents: The minimum top up cents.
            maximum_top_up_cents: The maximum top up cents.
            expiration_days: The expiration days.
            payment_methods: Effective top-up gateway groups. CUSTOM_PAYMENT_METHOD enables all usable custom methods; customers choose an individual method in the portal. With no saved restriction, every currently configurable group is returned, including custom methods. An empty array disables all methods. Custom top-ups charge and credit the exact USD deposit amount, plus any wallet bonus, without custom-method discounts or fees.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkReplaceWalletSettingsResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "replaceWalletSettings",
        }
        body: Dict[str, Any] = {
            "enabled": enabled,
            "minimum_top_up_cents": minimum_top_up_cents,
            "maximum_top_up_cents": maximum_top_up_cents,
            "expiration_days": expiration_days,
            "payment_methods": payment_methods,
        }
        _validate_model(
            body,
            {
                "name": "SdkReplaceWalletSettingsRequestApplicationJson",
                "required": [
                    "enabled",
                    "minimum_top_up_cents",
                    "maximum_top_up_cents",
                    "expiration_days",
                    "payment_methods",
                ],
                "properties": {
                    "enabled": {"kind": "primitive", "type": "boolean"},
                    "minimum_top_up_cents": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "integer",
                            "constraints": {"minimum": 1, "maximum": 999999999999},
                        },
                    },
                    "maximum_top_up_cents": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "integer",
                            "constraints": {"minimum": 1, "maximum": 999999999999},
                        },
                    },
                    "expiration_days": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "integer",
                            "constraints": {"minimum": 1, "maximum": 3650},
                        },
                    },
                    "payment_methods": {
                        "kind": "array",
                        "items": {"kind": "primitive", "type": "string"},
                    },
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return self._client.request(
            method="put",
            path=("v2", "wallet", "settings"),
            body=body,
            model=SdkReplaceWalletSettingsResponseValue200ApplicationJson,
            request_options=request_options,
        )


class AsyncWalletSettings:
    """Wallet Settings API resources (async)."""

    def __init__(self, client: AsyncSellAppClient) -> None:
        self._client = client
        self.with_raw_response = WithRawResponse(self)

    async def get(
        self,
        *,
        request_options: Optional[RequestOptions] = None,
    ) -> SdkGetWalletSettingsResponseValue200ApplicationJson:
        """Retrieve wallet settings

        Retrieve wallet settings for the authenticated store. Requires the `wallet` or `wallet:write` API ability. Payment methods are returned as their effective configurable values when no explicit restriction is stored. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Returns:
            SdkGetWalletSettingsResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "getWalletSettings",
        }
        return await self._client.request(
            method="get",
            path=("v2", "wallet", "settings"),
            model=SdkGetWalletSettingsResponseValue200ApplicationJson,
            request_options=request_options,
        )

    async def replace(
        self,
        *,
        enabled: bool,
        minimum_top_up_cents: Optional[int],
        maximum_top_up_cents: Optional[int],
        expiration_days: Optional[int],
        payment_methods: List[str],
        request_options: Optional[RequestOptions] = None,
    ) -> SdkReplaceWalletSettingsResponseValue200ApplicationJson:
        """Update wallet settings

        Update wallet settings. Requires the `wallet:write` API ability and the store wallet permission. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            enabled: The enabled.
            minimum_top_up_cents: The minimum top up cents.
            maximum_top_up_cents: The maximum top up cents.
            expiration_days: The expiration days.
            payment_methods: Effective top-up gateway groups. CUSTOM_PAYMENT_METHOD enables all usable custom methods; customers choose an individual method in the portal. With no saved restriction, every currently configurable group is returned, including custom methods. An empty array disables all methods. Custom top-ups charge and credit the exact USD deposit amount, plus any wallet bonus, without custom-method discounts or fees.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkReplaceWalletSettingsResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "replaceWalletSettings",
        }
        body: Dict[str, Any] = {
            "enabled": enabled,
            "minimum_top_up_cents": minimum_top_up_cents,
            "maximum_top_up_cents": maximum_top_up_cents,
            "expiration_days": expiration_days,
            "payment_methods": payment_methods,
        }
        _validate_model(
            body,
            {
                "name": "SdkReplaceWalletSettingsRequestApplicationJson",
                "required": [
                    "enabled",
                    "minimum_top_up_cents",
                    "maximum_top_up_cents",
                    "expiration_days",
                    "payment_methods",
                ],
                "properties": {
                    "enabled": {"kind": "primitive", "type": "boolean"},
                    "minimum_top_up_cents": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "integer",
                            "constraints": {"minimum": 1, "maximum": 999999999999},
                        },
                    },
                    "maximum_top_up_cents": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "integer",
                            "constraints": {"minimum": 1, "maximum": 999999999999},
                        },
                    },
                    "expiration_days": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "integer",
                            "constraints": {"minimum": 1, "maximum": 3650},
                        },
                    },
                    "payment_methods": {
                        "kind": "array",
                        "items": {"kind": "primitive", "type": "string"},
                    },
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return await self._client.request(
            method="put",
            path=("v2", "wallet", "settings"),
            body=body,
            model=SdkReplaceWalletSettingsResponseValue200ApplicationJson,
            request_options=request_options,
        )
