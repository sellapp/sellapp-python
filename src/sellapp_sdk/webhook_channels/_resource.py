# This file is auto-generated by oagen. Do not edit.

from __future__ import annotations

from typing import TYPE_CHECKING, Any

from .._base_client import WithRawResponse

if TYPE_CHECKING:
    from .._client import AsyncSellAppClient, SellAppClient

import builtins

from sellapp_sdk.common.models.event import Event
from sellapp_sdk.common.models.sdk_create_webhook_channel_request_application_json_allowed_notifications import (
    SdkCreateWebhookChannelRequestApplicationJsonAllowedNotifications,
)
from sellapp_sdk.common.models.sdk_replace_webhook_channel_request_application_json_allowed_notifications import (
    SdkReplaceWebhookChannelRequestApplicationJsonAllowedNotifications,
)
from sellapp_sdk.common.models.sdk_send_test_webhook_request_application_json_event import (
    SdkSendTestWebhookRequestApplicationJsonEvent,
)
from sellapp_sdk.common.models.sdk_update_webhook_channel_request_application_json_allowed_notifications import (
    SdkUpdateWebhookChannelRequestApplicationJsonAllowedNotifications,
)

from .._pagination import AsyncPage, SyncPage
from .._types import NOT_GIVEN, NotGiven, RequestOptions, _validate_model, enum_value
from .models import (
    ListWebhookChannelsResponseValue200ApplicationJsonPropertyDataItem,
    SdkCreateWebhookChannelResponseValue201ApplicationJson,
    SdkGetWebhookChannelResponseValue200ApplicationJson,
    SdkReplaceWebhookChannelResponseValue200ApplicationJson,
    SdkRotateWebhookSigningSecretResponseValue200ApplicationJson,
    SdkSendTestWebhookResponseValue200ApplicationJson,
    SdkUpdateWebhookChannelResponseValue200ApplicationJson,
    SearchWebhookChannelsRequestApplicationJsonPropertySearch,
    SearchWebhookChannelsResponseValue200ApplicationJsonPropertyDataItem,
)


class WebhookChannels:
    """Webhook Channels API resources."""

    def __init__(self, client: SellAppClient) -> None:
        self._client = client
        self.with_raw_response = WithRawResponse(self)

    def list(
        self,
        *,
        limit: int | None = None,
        before: str | None = None,
        after: str | None = None,
        order: str | None = None,
        page: int | None = None,
        request_options: RequestOptions | None = None,
    ) -> SyncPage[ListWebhookChannelsResponseValue200ApplicationJsonPropertyDataItem]:
        """List webhook channels

        Retrieve the authenticated store's outbound developer webhook destinations. Signing secrets and delivery headers are never included. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            limit: Number of webhook channels to return per page. Defaults to `15`.
            before: The before.
            after: The after.
            order: The order.
            page: Page number. Defaults to `1`.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SyncPage[ListWebhookChannelsResponseValue200ApplicationJsonPropertyDataItem]

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "listWebhookChannels",
        }
        params = {
            k: v
            for k, v in {
                "limit": limit,
                "before": before,
                "after": after,
                "order": order,
                "page": page,
            }.items()
            if v is not None
        }
        return self._client.request_page(
            method="get",
            path=("v2", "webhook-channels"),
            model=ListWebhookChannelsResponseValue200ApplicationJsonPropertyDataItem,
            params=params,
            request_options=request_options,
        )

    def create(
        self,
        *,
        url: str,
        allowed_notifications: builtins.list[
            SdkCreateWebhookChannelRequestApplicationJsonAllowedNotifications | str
        ],
        name: str | None | NotGiven = NOT_GIVEN,
        request_options: RequestOptions | None = None,
    ) -> SdkCreateWebhookChannelResponseValue201ApplicationJson:
        """Create a webhook channel

        Create an outbound webhook destination with a stable UUID and an explicit event filter. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            name: Optional seller-facing reference name.
            url: Public HTTP or HTTPS destination. The same canonical URL cannot be configured twice for one store.
            allowed_notifications: Complete set of public webhook event names enabled for this destination.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkCreateWebhookChannelResponseValue201ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "createWebhookChannel",
        }
        body: dict[str, Any] = {
            "url": url,
            "allowed_notifications": allowed_notifications,
        }
        if not isinstance(name, NotGiven):
            body["name"] = name
        _validate_model(
            body,
            {
                "name": "SdkCreateWebhookChannelRequestApplicationJson",
                "required": ["url", "allowed_notifications"],
                "properties": {
                    "name": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "string",
                            "constraints": {"maxLength": 100},
                        },
                    },
                    "url": {
                        "kind": "primitive",
                        "type": "string",
                        "format": "uri",
                        "constraints": {"maxLength": 2048},
                    },
                    "allowed_notifications": {
                        "kind": "array",
                        "items": {
                            "kind": "enum",
                            "values": [
                                "booking.cancelled",
                                "booking.rescheduled",
                                "cash_app.payment_requires_attention",
                                "charge.completed",
                                "charge.created",
                                "charge.disputed",
                                "charge.partial_paid",
                                "charge.review",
                                "feedback.created",
                                "order.completed",
                                "order.created",
                                "order.disputed",
                                "order.manual_payment_completed",
                                "order.paid",
                                "order.partial_paid",
                                "product.created",
                                "product.trashed",
                                "product.updated",
                                "subscription.cancelled",
                                "subscription.created",
                                "subscription.renewed",
                                "ticket_message.created",
                                "variant.created",
                                "variant.sold_out",
                                "variant.updated",
                            ],
                        },
                        "constraints": {"uniqueItems": True},
                    },
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return self._client.request(
            method="post",
            path=("v2", "webhook-channels"),
            body=body,
            model=SdkCreateWebhookChannelResponseValue201ApplicationJson,
            request_options=request_options,
        )

    def search(
        self,
        *,
        search: SearchWebhookChannelsRequestApplicationJsonPropertySearch | None = None,
        event: Event | str | None | NotGiven = NOT_GIVEN,
        page: int | None = None,
        limit: int | None = None,
        before: str | None = None,
        after: str | None = None,
        order: str | None = None,
        request_options: RequestOptions | None = None,
    ) -> SyncPage[SearchWebhookChannelsResponseValue200ApplicationJsonPropertyDataItem]:
        """Search webhook channels

        Search destinations by name or URL and optionally filter by a subscribed event. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            search: The search.
            event: Public event name delivered in webhook payloads. SellApp maps it to the corresponding allowed_notifications filter.
            page: The page.
            limit: The limit.
            limit: The limit.
            before: The before.
            after: The after.
            order: The order.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SyncPage[SearchWebhookChannelsResponseValue200ApplicationJsonPropertyDataItem]

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "searchWebhookChannels",
        }
        body: dict[str, Any] = {
            k: v
            for k, v in {
                "search": search.to_dict() if search is not None else None,
                "page": page,
                "limit": limit,
            }.items()
            if v is not None
        }
        if not isinstance(event, NotGiven):
            body["event"] = enum_value(event) if event is not None else None
        params = {
            k: v
            for k, v in {
                "limit": limit,
                "before": before,
                "after": after,
                "order": order,
            }.items()
            if v is not None
        }
        return self._client.request_page(
            method="post",
            path=("v2", "webhook-channels", "search"),
            model=SearchWebhookChannelsResponseValue200ApplicationJsonPropertyDataItem,
            body=body,
            params=params,
            request_options=request_options,
        )

    def rotate(
        self,
        *,
        signing_secret: str,
        request_options: RequestOptions | None = None,
    ) -> SdkRotateWebhookSigningSecretResponseValue200ApplicationJson:
        """Rotate the webhook signing secret

        Replace the store-wide outbound webhook signing secret. The value is write-only and is never returned or written to delivery logs. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            signing_secret: The signing secret.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkRotateWebhookSigningSecretResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "rotateWebhookSigningSecret",
        }
        body: dict[str, Any] = {
            "signing_secret": signing_secret,
        }
        _validate_model(
            body,
            {
                "name": "SdkRotateWebhookSigningSecretRequestApplicationJson",
                "required": ["signing_secret"],
                "properties": {
                    "signing_secret": {
                        "kind": "primitive",
                        "type": "string",
                        "constraints": {"minLength": 32, "maxLength": 255},
                    }
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return self._client.request(
            method="put",
            path=("v2", "webhook-channels", "signing-secret"),
            body=body,
            model=SdkRotateWebhookSigningSecretResponseValue200ApplicationJson,
            request_options=request_options,
        )

    def get(
        self,
        webhook_channel: str,
        *,
        request_options: RequestOptions | None = None,
    ) -> SdkGetWebhookChannelResponseValue200ApplicationJson:
        """Retrieve a webhook channel

        Retrieve one outbound webhook destination by its stable UUID. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            webhook_channel: The stable webhook channel UUID.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkGetWebhookChannelResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "getWebhookChannel",
        }
        return self._client.request(
            method="get",
            path=("v2", "webhook-channels", str(webhook_channel)),
            model=SdkGetWebhookChannelResponseValue200ApplicationJson,
            request_options=request_options,
        )

    def replace(
        self,
        webhook_channel: str,
        *,
        name: str | None,
        url: str,
        allowed_notifications: builtins.list[
            SdkReplaceWebhookChannelRequestApplicationJsonAllowedNotifications | str
        ],
        request_options: RequestOptions | None = None,
    ) -> SdkReplaceWebhookChannelResponseValue200ApplicationJson:
        """Replace a webhook channel

        Replace a destination with a complete representation. name, url, and allowed_notifications must all be present. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            webhook_channel: The stable webhook channel UUID.
            name: Optional seller-facing reference name.
            url: Public HTTP or HTTPS destination. The same canonical URL cannot be configured twice for one store.
            allowed_notifications: Complete set of public webhook event names enabled for this destination.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkReplaceWebhookChannelResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "replaceWebhookChannel",
        }
        body: dict[str, Any] = {
            "name": name,
            "url": url,
            "allowed_notifications": allowed_notifications,
        }
        _validate_model(
            body,
            {
                "name": "SdkReplaceWebhookChannelRequestApplicationJson",
                "required": ["name", "url", "allowed_notifications"],
                "properties": {
                    "name": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "string",
                            "constraints": {"maxLength": 100},
                        },
                    },
                    "url": {
                        "kind": "primitive",
                        "type": "string",
                        "format": "uri",
                        "constraints": {"maxLength": 2048},
                    },
                    "allowed_notifications": {
                        "kind": "array",
                        "items": {
                            "kind": "enum",
                            "values": [
                                "booking.cancelled",
                                "booking.rescheduled",
                                "cash_app.payment_requires_attention",
                                "charge.completed",
                                "charge.created",
                                "charge.disputed",
                                "charge.partial_paid",
                                "charge.review",
                                "feedback.created",
                                "order.completed",
                                "order.created",
                                "order.disputed",
                                "order.manual_payment_completed",
                                "order.paid",
                                "order.partial_paid",
                                "product.created",
                                "product.trashed",
                                "product.updated",
                                "subscription.cancelled",
                                "subscription.created",
                                "subscription.renewed",
                                "ticket_message.created",
                                "variant.created",
                                "variant.sold_out",
                                "variant.updated",
                            ],
                        },
                        "constraints": {"uniqueItems": True},
                    },
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return self._client.request(
            method="put",
            path=("v2", "webhook-channels", str(webhook_channel)),
            body=body,
            model=SdkReplaceWebhookChannelResponseValue200ApplicationJson,
            request_options=request_options,
        )

    def update(
        self,
        webhook_channel: str,
        *,
        name: str | None | NotGiven = NOT_GIVEN,
        url: str | None = None,
        allowed_notifications: builtins.list[
            SdkUpdateWebhookChannelRequestApplicationJsonAllowedNotifications | str
        ]
        | None = None,
        request_options: RequestOptions | None = None,
    ) -> SdkUpdateWebhookChannelResponseValue200ApplicationJson:
        """Update a webhook channel

        Partially update a destination. Supplying allowed_notifications replaces the complete event filter; omitted fields retain their current values. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            webhook_channel: The stable webhook channel UUID.
            name: Optional seller-facing reference name.
            url: Public HTTP or HTTPS destination. The same canonical URL cannot be configured twice for one store.
            allowed_notifications: Complete set of public webhook event names enabled for this destination.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkUpdateWebhookChannelResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "updateWebhookChannel",
        }
        body: dict[str, Any] = {
            k: v
            for k, v in {
                "url": url,
                "allowed_notifications": allowed_notifications,
            }.items()
            if v is not None
        }
        if not isinstance(name, NotGiven):
            body["name"] = name
        _validate_model(
            body,
            {
                "name": "SdkUpdateWebhookChannelRequestApplicationJson",
                "required": [],
                "properties": {
                    "name": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "string",
                            "constraints": {"maxLength": 100},
                        },
                    },
                    "url": {
                        "kind": "primitive",
                        "type": "string",
                        "format": "uri",
                        "constraints": {"maxLength": 2048},
                    },
                    "allowed_notifications": {
                        "kind": "array",
                        "items": {
                            "kind": "enum",
                            "values": [
                                "booking.cancelled",
                                "booking.rescheduled",
                                "cash_app.payment_requires_attention",
                                "charge.completed",
                                "charge.created",
                                "charge.disputed",
                                "charge.partial_paid",
                                "charge.review",
                                "feedback.created",
                                "order.completed",
                                "order.created",
                                "order.disputed",
                                "order.manual_payment_completed",
                                "order.paid",
                                "order.partial_paid",
                                "product.created",
                                "product.trashed",
                                "product.updated",
                                "subscription.cancelled",
                                "subscription.created",
                                "subscription.renewed",
                                "ticket_message.created",
                                "variant.created",
                                "variant.sold_out",
                                "variant.updated",
                            ],
                        },
                        "constraints": {"uniqueItems": True},
                    },
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return self._client.request(
            method="patch",
            path=("v2", "webhook-channels", str(webhook_channel)),
            body=body,
            model=SdkUpdateWebhookChannelResponseValue200ApplicationJson,
            request_options=request_options,
        )

    def delete(
        self,
        webhook_channel: str,
        *,
        request_options: RequestOptions | None = None,
    ) -> None:
        """Delete a webhook channel

        Delete a destination. Other channel UUIDs remain unchanged and the deleted UUID is not reassigned. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            webhook_channel: The stable webhook channel UUID.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "deleteWebhookChannel",
        }
        self._client.request(
            method="delete",
            path=("v2", "webhook-channels", str(webhook_channel)),
            request_options=request_options,
        )

    def send(
        self,
        webhook_channel: str,
        *,
        event: SdkSendTestWebhookRequestApplicationJsonEvent | str,
        request_options: RequestOptions | None = None,
    ) -> SdkSendTestWebhookResponseValue200ApplicationJson:
        """Send a test webhook

        Synchronously send one mock delivery to the stored destination. `event` is the public delivery event name and the same name must already be enabled in `allowed_notifications`. The test payload uses the same top-level event/version contract as production. Test deliveries do not retry or follow redirects and never return response bodies or signing headers. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            webhook_channel: The stable webhook channel UUID.
            event: Public event name used by channel configuration, search, tests, and delivered webhook payloads.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkSendTestWebhookResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "sendTestWebhook",
        }
        body: dict[str, Any] = {
            "event": enum_value(event),
        }
        _validate_model(
            body,
            {
                "name": "SdkSendTestWebhookRequestApplicationJson",
                "required": ["event"],
                "properties": {
                    "event": {
                        "kind": "enum",
                        "values": [
                            "booking.cancelled",
                            "booking.rescheduled",
                            "cash_app.payment_requires_attention",
                            "charge.completed",
                            "charge.created",
                            "charge.disputed",
                            "charge.partial_paid",
                            "charge.review",
                            "feedback.created",
                            "order.completed",
                            "order.created",
                            "order.disputed",
                            "order.manual_payment_completed",
                            "order.paid",
                            "order.partial_paid",
                            "product.created",
                            "product.trashed",
                            "product.updated",
                            "subscription.cancelled",
                            "subscription.created",
                            "subscription.renewed",
                            "ticket_message.created",
                            "variant.created",
                            "variant.sold_out",
                            "variant.updated",
                        ],
                    }
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return self._client.request(
            method="post",
            path=("v2", "webhook-channels", str(webhook_channel), "test"),
            body=body,
            model=SdkSendTestWebhookResponseValue200ApplicationJson,
            request_options=request_options,
        )


class AsyncWebhookChannels:
    """Webhook Channels API resources (async)."""

    def __init__(self, client: AsyncSellAppClient) -> None:
        self._client = client
        self.with_raw_response = WithRawResponse(self)

    async def list(
        self,
        *,
        limit: int | None = None,
        before: str | None = None,
        after: str | None = None,
        order: str | None = None,
        page: int | None = None,
        request_options: RequestOptions | None = None,
    ) -> AsyncPage[ListWebhookChannelsResponseValue200ApplicationJsonPropertyDataItem]:
        """List webhook channels

        Retrieve the authenticated store's outbound developer webhook destinations. Signing secrets and delivery headers are never included. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            limit: Number of webhook channels to return per page. Defaults to `15`.
            before: The before.
            after: The after.
            order: The order.
            page: Page number. Defaults to `1`.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            AsyncPage[ListWebhookChannelsResponseValue200ApplicationJsonPropertyDataItem]

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "listWebhookChannels",
        }
        params = {
            k: v
            for k, v in {
                "limit": limit,
                "before": before,
                "after": after,
                "order": order,
                "page": page,
            }.items()
            if v is not None
        }
        return await self._client.request_page(
            method="get",
            path=("v2", "webhook-channels"),
            model=ListWebhookChannelsResponseValue200ApplicationJsonPropertyDataItem,
            params=params,
            request_options=request_options,
        )

    async def create(
        self,
        *,
        url: str,
        allowed_notifications: builtins.list[
            SdkCreateWebhookChannelRequestApplicationJsonAllowedNotifications | str
        ],
        name: str | None | NotGiven = NOT_GIVEN,
        request_options: RequestOptions | None = None,
    ) -> SdkCreateWebhookChannelResponseValue201ApplicationJson:
        """Create a webhook channel

        Create an outbound webhook destination with a stable UUID and an explicit event filter. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            name: Optional seller-facing reference name.
            url: Public HTTP or HTTPS destination. The same canonical URL cannot be configured twice for one store.
            allowed_notifications: Complete set of public webhook event names enabled for this destination.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkCreateWebhookChannelResponseValue201ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "createWebhookChannel",
        }
        body: dict[str, Any] = {
            "url": url,
            "allowed_notifications": allowed_notifications,
        }
        if not isinstance(name, NotGiven):
            body["name"] = name
        _validate_model(
            body,
            {
                "name": "SdkCreateWebhookChannelRequestApplicationJson",
                "required": ["url", "allowed_notifications"],
                "properties": {
                    "name": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "string",
                            "constraints": {"maxLength": 100},
                        },
                    },
                    "url": {
                        "kind": "primitive",
                        "type": "string",
                        "format": "uri",
                        "constraints": {"maxLength": 2048},
                    },
                    "allowed_notifications": {
                        "kind": "array",
                        "items": {
                            "kind": "enum",
                            "values": [
                                "booking.cancelled",
                                "booking.rescheduled",
                                "cash_app.payment_requires_attention",
                                "charge.completed",
                                "charge.created",
                                "charge.disputed",
                                "charge.partial_paid",
                                "charge.review",
                                "feedback.created",
                                "order.completed",
                                "order.created",
                                "order.disputed",
                                "order.manual_payment_completed",
                                "order.paid",
                                "order.partial_paid",
                                "product.created",
                                "product.trashed",
                                "product.updated",
                                "subscription.cancelled",
                                "subscription.created",
                                "subscription.renewed",
                                "ticket_message.created",
                                "variant.created",
                                "variant.sold_out",
                                "variant.updated",
                            ],
                        },
                        "constraints": {"uniqueItems": True},
                    },
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return await self._client.request(
            method="post",
            path=("v2", "webhook-channels"),
            body=body,
            model=SdkCreateWebhookChannelResponseValue201ApplicationJson,
            request_options=request_options,
        )

    async def search(
        self,
        *,
        search: SearchWebhookChannelsRequestApplicationJsonPropertySearch | None = None,
        event: Event | str | None | NotGiven = NOT_GIVEN,
        page: int | None = None,
        limit: int | None = None,
        before: str | None = None,
        after: str | None = None,
        order: str | None = None,
        request_options: RequestOptions | None = None,
    ) -> AsyncPage[
        SearchWebhookChannelsResponseValue200ApplicationJsonPropertyDataItem
    ]:
        """Search webhook channels

        Search destinations by name or URL and optionally filter by a subscribed event. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            search: The search.
            event: Public event name delivered in webhook payloads. SellApp maps it to the corresponding allowed_notifications filter.
            page: The page.
            limit: The limit.
            limit: The limit.
            before: The before.
            after: The after.
            order: The order.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            AsyncPage[SearchWebhookChannelsResponseValue200ApplicationJsonPropertyDataItem]

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "searchWebhookChannels",
        }
        body: dict[str, Any] = {
            k: v
            for k, v in {
                "search": search.to_dict() if search is not None else None,
                "page": page,
                "limit": limit,
            }.items()
            if v is not None
        }
        if not isinstance(event, NotGiven):
            body["event"] = enum_value(event) if event is not None else None
        params = {
            k: v
            for k, v in {
                "limit": limit,
                "before": before,
                "after": after,
                "order": order,
            }.items()
            if v is not None
        }
        return await self._client.request_page(
            method="post",
            path=("v2", "webhook-channels", "search"),
            model=SearchWebhookChannelsResponseValue200ApplicationJsonPropertyDataItem,
            body=body,
            params=params,
            request_options=request_options,
        )

    async def rotate(
        self,
        *,
        signing_secret: str,
        request_options: RequestOptions | None = None,
    ) -> SdkRotateWebhookSigningSecretResponseValue200ApplicationJson:
        """Rotate the webhook signing secret

        Replace the store-wide outbound webhook signing secret. The value is write-only and is never returned or written to delivery logs. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            signing_secret: The signing secret.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkRotateWebhookSigningSecretResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "rotateWebhookSigningSecret",
        }
        body: dict[str, Any] = {
            "signing_secret": signing_secret,
        }
        _validate_model(
            body,
            {
                "name": "SdkRotateWebhookSigningSecretRequestApplicationJson",
                "required": ["signing_secret"],
                "properties": {
                    "signing_secret": {
                        "kind": "primitive",
                        "type": "string",
                        "constraints": {"minLength": 32, "maxLength": 255},
                    }
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return await self._client.request(
            method="put",
            path=("v2", "webhook-channels", "signing-secret"),
            body=body,
            model=SdkRotateWebhookSigningSecretResponseValue200ApplicationJson,
            request_options=request_options,
        )

    async def get(
        self,
        webhook_channel: str,
        *,
        request_options: RequestOptions | None = None,
    ) -> SdkGetWebhookChannelResponseValue200ApplicationJson:
        """Retrieve a webhook channel

        Retrieve one outbound webhook destination by its stable UUID. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            webhook_channel: The stable webhook channel UUID.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkGetWebhookChannelResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "getWebhookChannel",
        }
        return await self._client.request(
            method="get",
            path=("v2", "webhook-channels", str(webhook_channel)),
            model=SdkGetWebhookChannelResponseValue200ApplicationJson,
            request_options=request_options,
        )

    async def replace(
        self,
        webhook_channel: str,
        *,
        name: str | None,
        url: str,
        allowed_notifications: builtins.list[
            SdkReplaceWebhookChannelRequestApplicationJsonAllowedNotifications | str
        ],
        request_options: RequestOptions | None = None,
    ) -> SdkReplaceWebhookChannelResponseValue200ApplicationJson:
        """Replace a webhook channel

        Replace a destination with a complete representation. name, url, and allowed_notifications must all be present. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            webhook_channel: The stable webhook channel UUID.
            name: Optional seller-facing reference name.
            url: Public HTTP or HTTPS destination. The same canonical URL cannot be configured twice for one store.
            allowed_notifications: Complete set of public webhook event names enabled for this destination.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkReplaceWebhookChannelResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "replaceWebhookChannel",
        }
        body: dict[str, Any] = {
            "name": name,
            "url": url,
            "allowed_notifications": allowed_notifications,
        }
        _validate_model(
            body,
            {
                "name": "SdkReplaceWebhookChannelRequestApplicationJson",
                "required": ["name", "url", "allowed_notifications"],
                "properties": {
                    "name": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "string",
                            "constraints": {"maxLength": 100},
                        },
                    },
                    "url": {
                        "kind": "primitive",
                        "type": "string",
                        "format": "uri",
                        "constraints": {"maxLength": 2048},
                    },
                    "allowed_notifications": {
                        "kind": "array",
                        "items": {
                            "kind": "enum",
                            "values": [
                                "booking.cancelled",
                                "booking.rescheduled",
                                "cash_app.payment_requires_attention",
                                "charge.completed",
                                "charge.created",
                                "charge.disputed",
                                "charge.partial_paid",
                                "charge.review",
                                "feedback.created",
                                "order.completed",
                                "order.created",
                                "order.disputed",
                                "order.manual_payment_completed",
                                "order.paid",
                                "order.partial_paid",
                                "product.created",
                                "product.trashed",
                                "product.updated",
                                "subscription.cancelled",
                                "subscription.created",
                                "subscription.renewed",
                                "ticket_message.created",
                                "variant.created",
                                "variant.sold_out",
                                "variant.updated",
                            ],
                        },
                        "constraints": {"uniqueItems": True},
                    },
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return await self._client.request(
            method="put",
            path=("v2", "webhook-channels", str(webhook_channel)),
            body=body,
            model=SdkReplaceWebhookChannelResponseValue200ApplicationJson,
            request_options=request_options,
        )

    async def update(
        self,
        webhook_channel: str,
        *,
        name: str | None | NotGiven = NOT_GIVEN,
        url: str | None = None,
        allowed_notifications: builtins.list[
            SdkUpdateWebhookChannelRequestApplicationJsonAllowedNotifications | str
        ]
        | None = None,
        request_options: RequestOptions | None = None,
    ) -> SdkUpdateWebhookChannelResponseValue200ApplicationJson:
        """Update a webhook channel

        Partially update a destination. Supplying allowed_notifications replaces the complete event filter; omitted fields retain their current values. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            webhook_channel: The stable webhook channel UUID.
            name: Optional seller-facing reference name.
            url: Public HTTP or HTTPS destination. The same canonical URL cannot be configured twice for one store.
            allowed_notifications: Complete set of public webhook event names enabled for this destination.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkUpdateWebhookChannelResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "updateWebhookChannel",
        }
        body: dict[str, Any] = {
            k: v
            for k, v in {
                "url": url,
                "allowed_notifications": allowed_notifications,
            }.items()
            if v is not None
        }
        if not isinstance(name, NotGiven):
            body["name"] = name
        _validate_model(
            body,
            {
                "name": "SdkUpdateWebhookChannelRequestApplicationJson",
                "required": [],
                "properties": {
                    "name": {
                        "kind": "nullable",
                        "inner": {
                            "kind": "primitive",
                            "type": "string",
                            "constraints": {"maxLength": 100},
                        },
                    },
                    "url": {
                        "kind": "primitive",
                        "type": "string",
                        "format": "uri",
                        "constraints": {"maxLength": 2048},
                    },
                    "allowed_notifications": {
                        "kind": "array",
                        "items": {
                            "kind": "enum",
                            "values": [
                                "booking.cancelled",
                                "booking.rescheduled",
                                "cash_app.payment_requires_attention",
                                "charge.completed",
                                "charge.created",
                                "charge.disputed",
                                "charge.partial_paid",
                                "charge.review",
                                "feedback.created",
                                "order.completed",
                                "order.created",
                                "order.disputed",
                                "order.manual_payment_completed",
                                "order.paid",
                                "order.partial_paid",
                                "product.created",
                                "product.trashed",
                                "product.updated",
                                "subscription.cancelled",
                                "subscription.created",
                                "subscription.renewed",
                                "ticket_message.created",
                                "variant.created",
                                "variant.sold_out",
                                "variant.updated",
                            ],
                        },
                        "constraints": {"uniqueItems": True},
                    },
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return await self._client.request(
            method="patch",
            path=("v2", "webhook-channels", str(webhook_channel)),
            body=body,
            model=SdkUpdateWebhookChannelResponseValue200ApplicationJson,
            request_options=request_options,
        )

    async def delete(
        self,
        webhook_channel: str,
        *,
        request_options: RequestOptions | None = None,
    ) -> None:
        """Delete a webhook channel

        Delete a destination. Other channel UUIDs remain unchanged and the deleted UUID is not reassigned. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            webhook_channel: The stable webhook channel UUID.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "deleteWebhookChannel",
        }
        await self._client.request(
            method="delete",
            path=("v2", "webhook-channels", str(webhook_channel)),
            request_options=request_options,
        )

    async def send(
        self,
        webhook_channel: str,
        *,
        event: SdkSendTestWebhookRequestApplicationJsonEvent | str,
        request_options: RequestOptions | None = None,
    ) -> SdkSendTestWebhookResponseValue200ApplicationJson:
        """Send a test webhook

        Synchronously send one mock delivery to the stored destination. `event` is the public delivery event name and the same name must already be enabled in `allowed_notifications`. The test payload uses the same top-level event/version contract as production. Test deliveries do not retry or follow redirects and never return response bodies or signing headers. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

        Args:
            webhook_channel: The stable webhook channel UUID.
            event: Public event name used by channel configuration, search, tests, and delivered webhook payloads.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SdkSendTestWebhookResponseValue200ApplicationJson

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            AuthorizationError: If the request is forbidden (403).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        request_options = {
            **(request_options or {}),
            "auth": {
                "alternatives": [
                    [{"schemeName": "bearerAuth", "scopes": []}],
                    [
                        {"schemeName": "oauthAccessToken", "scopes": ["admin"]},
                        {"schemeName": "storeAuth", "scopes": []},
                    ],
                ],
                "anonymous": False,
                "apiKey": True,
                "accessToken": True,
                "customerSession": False,
                "browserSession": False,
                "clientBasic": False,
                "apiKeyRequiresStore": False,
                "apiKeyUsesStore": True,
                "accessTokenRequiresStore": True,
            },
            "server": None,
            "encoding": "json",
            "no_retry": False,
            "text_response": False,
        }
        request_options = {
            **(request_options or {}),
            "idempotency_supported": False,
            "operation_id": "sendTestWebhook",
        }
        body: dict[str, Any] = {
            "event": enum_value(event),
        }
        _validate_model(
            body,
            {
                "name": "SdkSendTestWebhookRequestApplicationJson",
                "required": ["event"],
                "properties": {
                    "event": {
                        "kind": "enum",
                        "values": [
                            "booking.cancelled",
                            "booking.rescheduled",
                            "cash_app.payment_requires_attention",
                            "charge.completed",
                            "charge.created",
                            "charge.disputed",
                            "charge.partial_paid",
                            "charge.review",
                            "feedback.created",
                            "order.completed",
                            "order.created",
                            "order.disputed",
                            "order.manual_payment_completed",
                            "order.paid",
                            "order.partial_paid",
                            "product.created",
                            "product.trashed",
                            "product.updated",
                            "subscription.cancelled",
                            "subscription.created",
                            "subscription.renewed",
                            "ticket_message.created",
                            "variant.created",
                            "variant.sold_out",
                            "variant.updated",
                        ],
                    }
                },
                "additionalProperties": True,
            },
            direction="request",
            allow_unknown_enums=False,
            allow_unknown_union_variants=False,
            allow_unknown_fields=True,
        )
        return await self._client.request(
            method="post",
            path=("v2", "webhook-channels", str(webhook_channel), "test"),
            body=body,
            model=SdkSendTestWebhookResponseValue200ApplicationJson,
            request_options=request_options,
        )
