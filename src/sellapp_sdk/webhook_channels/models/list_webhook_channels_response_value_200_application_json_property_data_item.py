# This file is auto-generated by oagen. Do not edit.

from __future__ import annotations

from dataclasses import dataclass
from dataclasses import field as dataclass_field
from enum import Enum
from typing import Any, Dict, List, Optional, cast

from sellapp_sdk._types import (
    _preserve_unknown_fields,
    _raise_deserialize_error,
    _validate_model,
)
from sellapp_sdk.common.models.list_webhook_channels_response_value_200_application_json_property_data_item_allowed_notifications import (
    ListWebhookChannelsResponseValue200ApplicationJsonPropertyDataItemAllowedNotifications,
)

_DOMAIN_RUNTIME_SCHEMA: Dict[str, Any] = {
    "name": "ListWebhookChannelsResponseValue200ApplicationJsonPropertyDataItem",
    "required": [
        "id",
        "name",
        "url",
        "allowed_notifications",
        "signing_secret_configured",
    ],
    "properties": {
        "id": {"kind": "primitive", "type": "string", "format": "uuid"},
        "name": {"kind": "nullable", "inner": {"kind": "primitive", "type": "string"}},
        "url": {"kind": "primitive", "type": "string", "format": "uri"},
        "allowed_notifications": {
            "kind": "array",
            "items": {
                "kind": "enum",
                "values": [
                    "booking.cancelled",
                    "booking.rescheduled",
                    "cash_app.payment_requires_attention",
                    "charge.completed",
                    "charge.created",
                    "charge.disputed",
                    "charge.partial_paid",
                    "charge.review",
                    "feedback.created",
                    "order.completed",
                    "order.created",
                    "order.disputed",
                    "order.manual_payment_completed",
                    "order.paid",
                    "order.partial_paid",
                    "product.created",
                    "product.trashed",
                    "product.updated",
                    "subscription.cancelled",
                    "subscription.created",
                    "subscription.renewed",
                    "ticket_message.created",
                    "variant.created",
                    "variant.sold_out",
                    "variant.updated",
                ],
            },
            "constraints": {"uniqueItems": True},
        },
        "signing_secret_configured": {"kind": "primitive", "type": "boolean"},
    },
    "additionalProperties": True,
}
_WIRE_RUNTIME_SCHEMA: Dict[str, Any] = {
    "name": "ListWebhookChannelsResponseValue200ApplicationJsonPropertyDataItem",
    "required": [
        "id",
        "name",
        "url",
        "allowed_notifications",
        "signing_secret_configured",
    ],
    "properties": {
        "id": {"kind": "primitive", "type": "string", "format": "uuid"},
        "name": {"kind": "nullable", "inner": {"kind": "primitive", "type": "string"}},
        "url": {"kind": "primitive", "type": "string", "format": "uri"},
        "allowed_notifications": {
            "kind": "array",
            "items": {
                "kind": "enum",
                "values": [
                    "booking.cancelled",
                    "booking.rescheduled",
                    "cash_app.payment_requires_attention",
                    "charge.completed",
                    "charge.created",
                    "charge.disputed",
                    "charge.partial_paid",
                    "charge.review",
                    "feedback.created",
                    "order.completed",
                    "order.created",
                    "order.disputed",
                    "order.manual_payment_completed",
                    "order.paid",
                    "order.partial_paid",
                    "product.created",
                    "product.trashed",
                    "product.updated",
                    "subscription.cancelled",
                    "subscription.created",
                    "subscription.renewed",
                    "ticket_message.created",
                    "variant.created",
                    "variant.sold_out",
                    "variant.updated",
                ],
            },
            "constraints": {"uniqueItems": True},
        },
        "signing_secret_configured": {"kind": "primitive", "type": "boolean"},
    },
    "additionalProperties": True,
}


@dataclass(slots=True)
class ListWebhookChannelsResponseValue200ApplicationJsonPropertyDataItem:
    """A seller-configured outbound webhook destination. IDs are stable UUIDs and are not reused when another channel is deleted."""

    id: str
    """Stable identifier for this webhook channel."""
    name: Optional[str]
    """Optional seller-facing reference name."""
    url: str
    """Public HTTP or HTTPS destination. Local, private, reserved, link-local, metadata, credential-bearing, and Discord webhook URLs are rejected. Delivery resolves and pins a public address and does not follow redirects."""
    allowed_notifications: List[
        ListWebhookChannelsResponseValue200ApplicationJsonPropertyDataItemAllowedNotifications
    ]
    """Complete set of public webhook event names enabled for this destination. Updating this field replaces the previous filter."""
    signing_secret_configured: bool
    """Whether the store has an outbound webhook signing secret. The stored secret is never returned."""
    additional_properties: Dict[str, Any] = dataclass_field(
        default_factory=dict, repr=False
    )
    _from_response: bool = dataclass_field(default=False, repr=False, compare=False)

    @classmethod
    def from_dict(
        cls, data: Dict[str, Any]
    ) -> ListWebhookChannelsResponseValue200ApplicationJsonPropertyDataItem:
        """Deserialize from a dictionary."""
        try:
            _validate_model(
                data,
                _WIRE_RUNTIME_SCHEMA,
                direction="response",
                allow_unknown_enums=True,
                allow_unknown_union_variants=True,
                allow_unknown_fields=True,
            )
            return cls(
                id=data["id"],
                name=data["name"],
                url=data["url"],
                allowed_notifications=[
                    ListWebhookChannelsResponseValue200ApplicationJsonPropertyDataItemAllowedNotifications(
                        item
                    )
                    for item in cast(list[Any], data["allowed_notifications"])
                ],
                signing_secret_configured=data["signing_secret_configured"],
                additional_properties=_preserve_unknown_fields(
                    data,
                    [
                        "id",
                        "name",
                        "url",
                        "allowed_notifications",
                        "signing_secret_configured",
                    ],
                ),
                _from_response=True,
            )
        except (KeyError, ValueError) as e:
            _raise_deserialize_error(
                "ListWebhookChannelsResponseValue200ApplicationJsonPropertyDataItem", e
            )

    def to_dict(self) -> Dict[str, Any]:
        """Serialize to a dictionary."""
        _domain_data: Dict[str, Any] = dict(self.additional_properties)
        _domain_data["id"] = self.id
        _domain_data["name"] = self.name
        _domain_data["url"] = self.url
        _domain_data["allowed_notifications"] = self.allowed_notifications
        _domain_data["signing_secret_configured"] = self.signing_secret_configured
        _validate_model(
            _domain_data,
            _DOMAIN_RUNTIME_SCHEMA,
            direction="request",
            allow_unknown_enums=self._from_response,
            allow_unknown_union_variants=self._from_response,
            allow_unknown_fields=(self._from_response or True),
        )
        result: Dict[str, Any] = dict(self.additional_properties)
        result["id"] = self.id
        if self.name is not None:
            result["name"] = self.name
        else:
            result["name"] = None
        result["url"] = self.url
        result["allowed_notifications"] = [
            item.value if isinstance(item, Enum) else item
            for item in self.allowed_notifications
        ]
        result["signing_secret_configured"] = self.signing_secret_configured
        return result
