# This file is auto-generated by oagen. Do not edit.

from __future__ import annotations

from dataclasses import dataclass
from dataclasses import field as dataclass_field
from enum import Enum
from typing import Any, Dict, List, Optional, cast

from sellapp_sdk._types import (
    _preserve_unknown_fields,
    _raise_deserialize_error,
    _validate_model,
)
from sellapp_sdk.common.models.sdk_create_webhook_channel_request_application_json_allowed_notifications import (
    SdkCreateWebhookChannelRequestApplicationJsonAllowedNotifications,
)

_DOMAIN_RUNTIME_SCHEMA: Dict[str, Any] = {
    "name": "SdkCreateWebhookChannelRequestApplicationJson",
    "required": ["url", "allowed_notifications"],
    "properties": {
        "name": {
            "kind": "nullable",
            "inner": {
                "kind": "primitive",
                "type": "string",
                "constraints": {"maxLength": 100},
            },
        },
        "url": {
            "kind": "primitive",
            "type": "string",
            "format": "uri",
            "constraints": {"maxLength": 2048},
        },
        "allowed_notifications": {
            "kind": "array",
            "items": {
                "kind": "enum",
                "values": [
                    "booking.cancelled",
                    "booking.rescheduled",
                    "cash_app.payment_requires_attention",
                    "charge.completed",
                    "charge.created",
                    "charge.disputed",
                    "charge.partial_paid",
                    "charge.review",
                    "feedback.created",
                    "order.completed",
                    "order.created",
                    "order.disputed",
                    "order.manual_payment_completed",
                    "order.paid",
                    "order.partial_paid",
                    "product.created",
                    "product.trashed",
                    "product.updated",
                    "subscription.cancelled",
                    "subscription.created",
                    "subscription.renewed",
                    "ticket_message.created",
                    "variant.created",
                    "variant.sold_out",
                    "variant.updated",
                ],
            },
            "constraints": {"uniqueItems": True},
        },
    },
    "additionalProperties": True,
}
_WIRE_RUNTIME_SCHEMA: Dict[str, Any] = {
    "name": "SdkCreateWebhookChannelRequestApplicationJson",
    "required": ["url", "allowed_notifications"],
    "properties": {
        "name": {
            "kind": "nullable",
            "inner": {
                "kind": "primitive",
                "type": "string",
                "constraints": {"maxLength": 100},
            },
        },
        "url": {
            "kind": "primitive",
            "type": "string",
            "format": "uri",
            "constraints": {"maxLength": 2048},
        },
        "allowed_notifications": {
            "kind": "array",
            "items": {
                "kind": "enum",
                "values": [
                    "booking.cancelled",
                    "booking.rescheduled",
                    "cash_app.payment_requires_attention",
                    "charge.completed",
                    "charge.created",
                    "charge.disputed",
                    "charge.partial_paid",
                    "charge.review",
                    "feedback.created",
                    "order.completed",
                    "order.created",
                    "order.disputed",
                    "order.manual_payment_completed",
                    "order.paid",
                    "order.partial_paid",
                    "product.created",
                    "product.trashed",
                    "product.updated",
                    "subscription.cancelled",
                    "subscription.created",
                    "subscription.renewed",
                    "ticket_message.created",
                    "variant.created",
                    "variant.sold_out",
                    "variant.updated",
                ],
            },
            "constraints": {"uniqueItems": True},
        },
    },
    "additionalProperties": True,
}


@dataclass(slots=True)
class SdkCreateWebhookChannelRequestApplicationJson:
    """Sdk Create Webhook Channel Request Application Json model."""

    url: str
    """Public HTTP or HTTPS destination. The same canonical URL cannot be configured twice for one store."""
    allowed_notifications: List[
        SdkCreateWebhookChannelRequestApplicationJsonAllowedNotifications
    ]
    """Complete set of public webhook event names enabled for this destination."""
    name: Optional[str] = None
    """Optional seller-facing reference name."""
    additional_properties: Dict[str, Any] = dataclass_field(
        default_factory=dict, repr=False
    )
    _from_response: bool = dataclass_field(default=False, repr=False, compare=False)

    @classmethod
    def from_dict(
        cls, data: Dict[str, Any]
    ) -> SdkCreateWebhookChannelRequestApplicationJson:
        """Deserialize from a dictionary."""
        try:
            _validate_model(
                data,
                _WIRE_RUNTIME_SCHEMA,
                direction="response",
                allow_unknown_enums=True,
                allow_unknown_union_variants=True,
                allow_unknown_fields=True,
            )
            return cls(
                url=data["url"],
                allowed_notifications=[
                    SdkCreateWebhookChannelRequestApplicationJsonAllowedNotifications(
                        item
                    )
                    for item in cast(list[Any], data["allowed_notifications"])
                ],
                name=data.get("name"),
                additional_properties=_preserve_unknown_fields(
                    data, ["name", "url", "allowed_notifications"]
                ),
                _from_response=True,
            )
        except (KeyError, ValueError) as e:
            _raise_deserialize_error("SdkCreateWebhookChannelRequestApplicationJson", e)

    def to_dict(self) -> Dict[str, Any]:
        """Serialize to a dictionary."""
        _domain_data: Dict[str, Any] = dict(self.additional_properties)
        _domain_data["url"] = self.url
        _domain_data["allowed_notifications"] = self.allowed_notifications
        _domain_data["name"] = self.name
        _validate_model(
            _domain_data,
            _DOMAIN_RUNTIME_SCHEMA,
            direction="request",
            allow_unknown_enums=self._from_response,
            allow_unknown_union_variants=self._from_response,
            allow_unknown_fields=(self._from_response or True),
        )
        result: Dict[str, Any] = dict(self.additional_properties)
        result["url"] = self.url
        result["allowed_notifications"] = [
            item.value if isinstance(item, Enum) else item
            for item in self.allowed_notifications
        ]
        if self.name is not None:
            result["name"] = self.name
        else:
            result["name"] = None
        return result
