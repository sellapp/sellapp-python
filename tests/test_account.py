# This file is auto-generated by oagen. Do not edit.

import json

import pytest

from sellapp_sdk import AsyncSellAppClient, SellAppClient
from sellapp_sdk._errors import (
    AuthenticationError,
    BadRequestError,
    NotFoundError,
    RateLimitExceededError,
    ServerError,
    UnprocessableEntityError,
)
from sellapp_sdk.account.models import (
    SdkCreateStoreResponseValue201ApplicationJson,
    SdkGetAccountResponseValue200ApplicationJson,
    SdkGetStorePermissionsResponseValue200ApplicationJson,
    SdkGetStoreResponseValue200ApplicationJson,
    SdkListStoresResponseValue200ApplicationJson,
)
from tests.generated_helpers import load_fixture


class TestAccount:
    def test_get_account(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_get_account_response_value_200_application_json.json"
            ),
        )
        result = sellapp_sdk.account.get_account()
        assert isinstance(result, SdkGetAccountResponseValue200ApplicationJson)
        request = httpx_mock.get_request()
        assert request.method == "GET"
        assert request.url.path.endswith("/v2/me")

    def test_list_stores(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_list_stores_response_value_200_application_json.json"
            ),
        )
        result = sellapp_sdk.account.list_stores()
        assert isinstance(result, SdkListStoresResponseValue200ApplicationJson)
        request = httpx_mock.get_request()
        assert request.method == "GET"
        assert request.url.path.endswith("/v2/stores")

    def test_create_store(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_create_store_response_value_201_application_json.json"
            ),
        )
        result = sellapp_sdk.account.create_store(name="enx", slug="enx")
        assert isinstance(result, SdkCreateStoreResponseValue201ApplicationJson)
        request = httpx_mock.get_request()
        assert request.method == "POST"
        assert request.url.path.endswith("/v2/stores")
        body = json.loads(request.content)
        assert body["name"] == "enx"
        assert body["slug"] == "enx"

    def test_get_store(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture("sdk_get_store_response_value_200_application_json.json"),
        )
        result = sellapp_sdk.account.get_store("test_store")
        assert isinstance(result, SdkGetStoreResponseValue200ApplicationJson)
        request = httpx_mock.get_request()
        assert request.method == "GET"
        assert request.url.path.endswith("/v2/stores/test_store")

    def test_get_store_permissions(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_get_store_permissions_response_value_200_application_json.json"
            ),
        )
        result = sellapp_sdk.account.get_store_permissions()
        assert isinstance(result, SdkGetStorePermissionsResponseValue200ApplicationJson)
        request = httpx_mock.get_request()
        assert request.method == "GET"
        assert request.url.path.endswith("/v2/permissions")

    def test_get_account_with_request_options(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_get_account_response_value_200_application_json.json"
            )
        )
        sellapp_sdk.account.get_account(
            request_options={"headers": {"X-Custom": "value"}}
        )
        request = httpx_mock.get_request()
        assert request.headers["X-Custom"] == "value"

    def test_get_account_unauthorized(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            status_code=401,
            json={"message": "Unauthorized"},
        )
        with pytest.raises(AuthenticationError):
            sellapp_sdk.account.get_account()

    def test_get_account_not_found(self, httpx_mock):
        sellapp_sdk = SellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=404, json={"message": "Not found"})
            with pytest.raises(NotFoundError):
                sellapp_sdk.account.get_account()
        finally:
            sellapp_sdk.close()

    def test_get_account_rate_limited(self, httpx_mock):
        sellapp_sdk = SellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(
                status_code=429,
                headers={"Retry-After": "0"},
                json={"message": "Slow down"},
            )
            with pytest.raises(RateLimitExceededError):
                sellapp_sdk.account.get_account()
        finally:
            sellapp_sdk.close()

    def test_get_account_server_error(self, httpx_mock):
        sellapp_sdk = SellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=500, json={"message": "Server error"})
            with pytest.raises(ServerError):
                sellapp_sdk.account.get_account()
        finally:
            sellapp_sdk.close()

    def test_get_account_bad_request(self, httpx_mock):
        sellapp_sdk = SellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=400, json={"message": "Bad request"})
            with pytest.raises(BadRequestError):
                sellapp_sdk.account.get_account()
        finally:
            sellapp_sdk.close()

    def test_get_account_unprocessable(self, httpx_mock):
        sellapp_sdk = SellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=422, json={"message": "Unprocessable"})
            with pytest.raises(UnprocessableEntityError):
                sellapp_sdk.account.get_account()
        finally:
            sellapp_sdk.close()


class TestAsyncAccount:
    @pytest.mark.asyncio
    async def test_get_account(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_get_account_response_value_200_application_json.json"
            )
        )
        result = await async_sellapp_sdk.account.get_account()
        assert isinstance(result, SdkGetAccountResponseValue200ApplicationJson)
        request = httpx_mock.get_request()
        assert request.method == "GET"
        assert request.url.path.endswith("/v2/me")

    @pytest.mark.asyncio
    async def test_list_stores(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_list_stores_response_value_200_application_json.json"
            )
        )
        result = await async_sellapp_sdk.account.list_stores()
        assert isinstance(result, SdkListStoresResponseValue200ApplicationJson)
        request = httpx_mock.get_request()
        assert request.method == "GET"
        assert request.url.path.endswith("/v2/stores")

    @pytest.mark.asyncio
    async def test_create_store(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_create_store_response_value_201_application_json.json"
            )
        )
        result = await async_sellapp_sdk.account.create_store(name="enx", slug="enx")
        assert isinstance(result, SdkCreateStoreResponseValue201ApplicationJson)
        request = httpx_mock.get_request()
        assert request.method == "POST"
        assert request.url.path.endswith("/v2/stores")

    @pytest.mark.asyncio
    async def test_get_store(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture("sdk_get_store_response_value_200_application_json.json")
        )
        result = await async_sellapp_sdk.account.get_store("test_store")
        assert isinstance(result, SdkGetStoreResponseValue200ApplicationJson)
        request = httpx_mock.get_request()
        assert request.method == "GET"
        assert request.url.path.endswith("/v2/stores/test_store")

    @pytest.mark.asyncio
    async def test_get_store_permissions(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_get_store_permissions_response_value_200_application_json.json"
            )
        )
        result = await async_sellapp_sdk.account.get_store_permissions()
        assert isinstance(result, SdkGetStorePermissionsResponseValue200ApplicationJson)
        request = httpx_mock.get_request()
        assert request.method == "GET"
        assert request.url.path.endswith("/v2/permissions")

    @pytest.mark.asyncio
    async def test_get_account_with_request_options(
        self, async_sellapp_sdk, httpx_mock
    ):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_get_account_response_value_200_application_json.json"
            )
        )
        await async_sellapp_sdk.account.get_account(
            request_options={"headers": {"X-Custom": "value"}}
        )
        request = httpx_mock.get_request()
        assert request.headers["X-Custom"] == "value"

    @pytest.mark.asyncio
    async def test_get_account_unauthorized(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(status_code=401, json={"message": "Unauthorized"})
        with pytest.raises(AuthenticationError):
            await async_sellapp_sdk.account.get_account()

    @pytest.mark.asyncio
    async def test_get_account_not_found(self, httpx_mock):
        sellapp_sdk = AsyncSellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=404, json={"message": "Not found"})
            with pytest.raises(NotFoundError):
                await sellapp_sdk.account.get_account()
        finally:
            await sellapp_sdk.close()

    @pytest.mark.asyncio
    async def test_get_account_rate_limited(self, httpx_mock):
        sellapp_sdk = AsyncSellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(
                status_code=429,
                headers={"Retry-After": "0"},
                json={"message": "Slow down"},
            )
            with pytest.raises(RateLimitExceededError):
                await sellapp_sdk.account.get_account()
        finally:
            await sellapp_sdk.close()

    @pytest.mark.asyncio
    async def test_get_account_server_error(self, httpx_mock):
        sellapp_sdk = AsyncSellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=500, json={"message": "Server error"})
            with pytest.raises(ServerError):
                await sellapp_sdk.account.get_account()
        finally:
            await sellapp_sdk.close()

    @pytest.mark.asyncio
    async def test_get_account_bad_request(self, httpx_mock):
        sellapp_sdk = AsyncSellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=400, json={"message": "Bad request"})
            with pytest.raises(BadRequestError):
                await sellapp_sdk.account.get_account()
        finally:
            await sellapp_sdk.close()

    @pytest.mark.asyncio
    async def test_get_account_unprocessable(self, httpx_mock):
        sellapp_sdk = AsyncSellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=422, json={"message": "Unprocessable"})
            with pytest.raises(UnprocessableEntityError):
                await sellapp_sdk.account.get_account()
        finally:
            await sellapp_sdk.close()
