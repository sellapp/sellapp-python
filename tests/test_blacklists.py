# This file is auto-generated by oagen. Do not edit.

import json

import pytest

from sellapp_sdk import AsyncSellAppClient, SellAppClient
from sellapp_sdk._errors import (
    AuthenticationError,
    BadRequestError,
    NotFoundError,
    RateLimitExceededError,
    ServerError,
    UnprocessableEntityError,
)
from sellapp_sdk._pagination import AsyncPage, SyncPage
from sellapp_sdk.common.models import (
    Blacklist,
    BlacklistResponse,
    BlacklistType,
)
from tests.generated_helpers import load_fixture


class TestBlacklists:
    def test_list(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture("list_blacklist.json"),
        )
        page = sellapp_sdk.blacklists.list()
        assert isinstance(page, SyncPage)
        assert len(page.data) == 1
        assert isinstance(page.data[0], Blacklist)

    def test_list_empty_page(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(json={"data": [], "list_metadata": {}})
        page = sellapp_sdk.blacklists.list()
        assert isinstance(page, SyncPage)
        assert page.data == []

    def test_list_encodes_query_params(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(json={"data": [], "list_metadata": {}})
        sellapp_sdk.blacklists.list(
            limit=10, before="cursor before", after="cursor/after", page=7
        )
        request = httpx_mock.get_request()
        assert request.url.params["limit"] == "10"
        assert request.url.params["before"] == "cursor before"
        assert request.url.params["after"] == "cursor/after"
        assert request.url.params["page"] == "7"

    def test_create(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture("blacklist_response.json"),
        )
        result = sellapp_sdk.blacklists.create(
            type=BlacklistType("ASN"), data="test_data", description="test_description"
        )
        assert isinstance(result, BlacklistResponse)
        request = httpx_mock.get_request()
        assert request.method == "POST"
        assert request.url.path.endswith("/v1/blacklists")
        body = json.loads(request.content)
        assert body["type"] == BlacklistType("ASN")
        assert body["data"] == "test_data"
        assert body["description"] == "test_description"

    def test_get(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture("blacklist_response.json"),
        )
        result = sellapp_sdk.blacklists.get("test_blacklist")
        assert isinstance(result, BlacklistResponse)
        request = httpx_mock.get_request()
        assert request.method == "GET"
        assert request.url.path.endswith("/v1/blacklists/test_blacklist")

    def test_update(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture("blacklist_response.json"),
        )
        result = sellapp_sdk.blacklists.update("test_blacklist")
        assert isinstance(result, BlacklistResponse)
        request = httpx_mock.get_request()
        assert request.method == "PATCH"
        assert request.url.path.endswith("/v1/blacklists/test_blacklist")

    def test_delete(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(status_code=200, content=b"")
        result = sellapp_sdk.blacklists.delete("test_blacklist")
        assert result is None
        request = httpx_mock.get_request()
        assert request.method == "DELETE"
        assert request.url.path.endswith("/v1/blacklists/test_blacklist")

    def test_v2_list_blacklists(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture("list_blacklist.json"),
        )
        page = sellapp_sdk.blacklists.v2_list_blacklists()
        assert isinstance(page, SyncPage)
        assert len(page.data) == 1
        assert isinstance(page.data[0], Blacklist)

    def test_v2_list_blacklists_empty_page(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(json={"data": [], "list_metadata": {}})
        page = sellapp_sdk.blacklists.v2_list_blacklists()
        assert isinstance(page, SyncPage)
        assert page.data == []

    def test_v2_create_blacklist(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture("blacklist_response.json"),
        )
        result = sellapp_sdk.blacklists.v2_create_blacklist(
            type=BlacklistType("ASN"), data="test_data", description="test_description"
        )
        assert isinstance(result, BlacklistResponse)
        request = httpx_mock.get_request()
        assert request.method == "POST"
        assert request.url.path.endswith("/v2/blacklists")
        body = json.loads(request.content)
        assert body["type"] == BlacklistType("ASN")
        assert body["data"] == "test_data"
        assert body["description"] == "test_description"

    def test_v2_get_blacklist(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture("blacklist_response.json"),
        )
        result = sellapp_sdk.blacklists.v2_get_blacklist("test_blacklist")
        assert isinstance(result, BlacklistResponse)
        request = httpx_mock.get_request()
        assert request.method == "GET"
        assert request.url.path.endswith("/v2/blacklists/test_blacklist")

    def test_v2_replace_blacklist(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture("blacklist_response.json"),
        )
        result = sellapp_sdk.blacklists.v2_replace_blacklist("test_blacklist")
        assert isinstance(result, BlacklistResponse)
        request = httpx_mock.get_request()
        assert request.method == "PUT"
        assert request.url.path.endswith("/v2/blacklists/test_blacklist")

    def test_v2_update_blacklist(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture("blacklist_response.json"),
        )
        result = sellapp_sdk.blacklists.v2_update_blacklist("test_blacklist")
        assert isinstance(result, BlacklistResponse)
        request = httpx_mock.get_request()
        assert request.method == "PATCH"
        assert request.url.path.endswith("/v2/blacklists/test_blacklist")

    def test_v2_delete_blacklist(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(status_code=200, content=b"")
        result = sellapp_sdk.blacklists.v2_delete_blacklist("test_blacklist")
        assert result is None
        request = httpx_mock.get_request()
        assert request.method == "DELETE"
        assert request.url.path.endswith("/v2/blacklists/test_blacklist")

    def test_list_with_request_options(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(json={"data": [], "list_metadata": {}})
        sellapp_sdk.blacklists.list(request_options={"headers": {"X-Custom": "value"}})
        request = httpx_mock.get_request()
        assert request.headers["X-Custom"] == "value"

    def test_list_unauthorized(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            status_code=401,
            json={"message": "Unauthorized"},
        )
        with pytest.raises(AuthenticationError):
            sellapp_sdk.blacklists.list()

    def test_list_not_found(self, httpx_mock):
        sellapp_sdk = SellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=404, json={"message": "Not found"})
            with pytest.raises(NotFoundError):
                sellapp_sdk.blacklists.list()
        finally:
            sellapp_sdk.close()

    def test_list_rate_limited(self, httpx_mock):
        sellapp_sdk = SellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(
                status_code=429,
                headers={"Retry-After": "0"},
                json={"message": "Slow down"},
            )
            with pytest.raises(RateLimitExceededError):
                sellapp_sdk.blacklists.list()
        finally:
            sellapp_sdk.close()

    def test_list_server_error(self, httpx_mock):
        sellapp_sdk = SellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=500, json={"message": "Server error"})
            with pytest.raises(ServerError):
                sellapp_sdk.blacklists.list()
        finally:
            sellapp_sdk.close()

    def test_list_bad_request(self, httpx_mock):
        sellapp_sdk = SellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=400, json={"message": "Bad request"})
            with pytest.raises(BadRequestError):
                sellapp_sdk.blacklists.list()
        finally:
            sellapp_sdk.close()

    def test_list_unprocessable(self, httpx_mock):
        sellapp_sdk = SellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=422, json={"message": "Unprocessable"})
            with pytest.raises(UnprocessableEntityError):
                sellapp_sdk.blacklists.list()
        finally:
            sellapp_sdk.close()


class TestAsyncBlacklists:
    @pytest.mark.asyncio
    async def test_list(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(json=load_fixture("list_blacklist.json"))
        page = await async_sellapp_sdk.blacklists.list()
        assert isinstance(page, AsyncPage)
        assert len(page.data) == 1
        assert isinstance(page.data[0], Blacklist)

    @pytest.mark.asyncio
    async def test_list_empty_page(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(json={"data": [], "list_metadata": {}})
        page = await async_sellapp_sdk.blacklists.list()
        assert isinstance(page, AsyncPage)
        assert page.data == []

    @pytest.mark.asyncio
    async def test_list_encodes_query_params(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(json={"data": [], "list_metadata": {}})
        await async_sellapp_sdk.blacklists.list(
            limit=10, before="cursor before", after="cursor/after", page=7
        )
        request = httpx_mock.get_request()
        assert request.url.params["limit"] == "10"
        assert request.url.params["before"] == "cursor before"
        assert request.url.params["after"] == "cursor/after"
        assert request.url.params["page"] == "7"

    @pytest.mark.asyncio
    async def test_create(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(json=load_fixture("blacklist_response.json"))
        result = await async_sellapp_sdk.blacklists.create(
            type=BlacklistType("ASN"), data="test_data", description="test_description"
        )
        assert isinstance(result, BlacklistResponse)
        request = httpx_mock.get_request()
        assert request.method == "POST"
        assert request.url.path.endswith("/v1/blacklists")

    @pytest.mark.asyncio
    async def test_get(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(json=load_fixture("blacklist_response.json"))
        result = await async_sellapp_sdk.blacklists.get("test_blacklist")
        assert isinstance(result, BlacklistResponse)
        request = httpx_mock.get_request()
        assert request.method == "GET"
        assert request.url.path.endswith("/v1/blacklists/test_blacklist")

    @pytest.mark.asyncio
    async def test_update(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(json=load_fixture("blacklist_response.json"))
        result = await async_sellapp_sdk.blacklists.update("test_blacklist")
        assert isinstance(result, BlacklistResponse)
        request = httpx_mock.get_request()
        assert request.method == "PATCH"
        assert request.url.path.endswith("/v1/blacklists/test_blacklist")

    @pytest.mark.asyncio
    async def test_delete(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(status_code=200, content=b"")
        result = await async_sellapp_sdk.blacklists.delete("test_blacklist")
        assert result is None
        request = httpx_mock.get_request()
        assert request.method == "DELETE"
        assert request.url.path.endswith("/v1/blacklists/test_blacklist")

    @pytest.mark.asyncio
    async def test_v2_list_blacklists(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(json=load_fixture("list_blacklist.json"))
        page = await async_sellapp_sdk.blacklists.v2_list_blacklists()
        assert isinstance(page, AsyncPage)
        assert len(page.data) == 1
        assert isinstance(page.data[0], Blacklist)

    @pytest.mark.asyncio
    async def test_v2_list_blacklists_empty_page(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(json={"data": [], "list_metadata": {}})
        page = await async_sellapp_sdk.blacklists.v2_list_blacklists()
        assert isinstance(page, AsyncPage)
        assert page.data == []

    @pytest.mark.asyncio
    async def test_v2_create_blacklist(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(json=load_fixture("blacklist_response.json"))
        result = await async_sellapp_sdk.blacklists.v2_create_blacklist(
            type=BlacklistType("ASN"), data="test_data", description="test_description"
        )
        assert isinstance(result, BlacklistResponse)
        request = httpx_mock.get_request()
        assert request.method == "POST"
        assert request.url.path.endswith("/v2/blacklists")

    @pytest.mark.asyncio
    async def test_v2_get_blacklist(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(json=load_fixture("blacklist_response.json"))
        result = await async_sellapp_sdk.blacklists.v2_get_blacklist("test_blacklist")
        assert isinstance(result, BlacklistResponse)
        request = httpx_mock.get_request()
        assert request.method == "GET"
        assert request.url.path.endswith("/v2/blacklists/test_blacklist")

    @pytest.mark.asyncio
    async def test_v2_replace_blacklist(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(json=load_fixture("blacklist_response.json"))
        result = await async_sellapp_sdk.blacklists.v2_replace_blacklist(
            "test_blacklist"
        )
        assert isinstance(result, BlacklistResponse)
        request = httpx_mock.get_request()
        assert request.method == "PUT"
        assert request.url.path.endswith("/v2/blacklists/test_blacklist")

    @pytest.mark.asyncio
    async def test_v2_update_blacklist(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(json=load_fixture("blacklist_response.json"))
        result = await async_sellapp_sdk.blacklists.v2_update_blacklist(
            "test_blacklist"
        )
        assert isinstance(result, BlacklistResponse)
        request = httpx_mock.get_request()
        assert request.method == "PATCH"
        assert request.url.path.endswith("/v2/blacklists/test_blacklist")

    @pytest.mark.asyncio
    async def test_v2_delete_blacklist(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(status_code=200, content=b"")
        result = await async_sellapp_sdk.blacklists.v2_delete_blacklist(
            "test_blacklist"
        )
        assert result is None
        request = httpx_mock.get_request()
        assert request.method == "DELETE"
        assert request.url.path.endswith("/v2/blacklists/test_blacklist")

    @pytest.mark.asyncio
    async def test_list_with_request_options(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(json={"data": [], "list_metadata": {}})
        await async_sellapp_sdk.blacklists.list(
            request_options={"headers": {"X-Custom": "value"}}
        )
        request = httpx_mock.get_request()
        assert request.headers["X-Custom"] == "value"

    @pytest.mark.asyncio
    async def test_list_unauthorized(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(status_code=401, json={"message": "Unauthorized"})
        with pytest.raises(AuthenticationError):
            await async_sellapp_sdk.blacklists.list()

    @pytest.mark.asyncio
    async def test_list_not_found(self, httpx_mock):
        sellapp_sdk = AsyncSellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=404, json={"message": "Not found"})
            with pytest.raises(NotFoundError):
                await sellapp_sdk.blacklists.list()
        finally:
            await sellapp_sdk.close()

    @pytest.mark.asyncio
    async def test_list_rate_limited(self, httpx_mock):
        sellapp_sdk = AsyncSellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(
                status_code=429,
                headers={"Retry-After": "0"},
                json={"message": "Slow down"},
            )
            with pytest.raises(RateLimitExceededError):
                await sellapp_sdk.blacklists.list()
        finally:
            await sellapp_sdk.close()

    @pytest.mark.asyncio
    async def test_list_server_error(self, httpx_mock):
        sellapp_sdk = AsyncSellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=500, json={"message": "Server error"})
            with pytest.raises(ServerError):
                await sellapp_sdk.blacklists.list()
        finally:
            await sellapp_sdk.close()

    @pytest.mark.asyncio
    async def test_list_bad_request(self, httpx_mock):
        sellapp_sdk = AsyncSellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=400, json={"message": "Bad request"})
            with pytest.raises(BadRequestError):
                await sellapp_sdk.blacklists.list()
        finally:
            await sellapp_sdk.close()

    @pytest.mark.asyncio
    async def test_list_unprocessable(self, httpx_mock):
        sellapp_sdk = AsyncSellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=422, json={"message": "Unprocessable"})
            with pytest.raises(UnprocessableEntityError):
                await sellapp_sdk.blacklists.list()
        finally:
            await sellapp_sdk.close()
