# This file is auto-generated by oagen. Do not edit.

import json

import pytest

from sellapp_sdk import AsyncSellAppClient, SellAppClient
from sellapp_sdk._errors import (
    AuthenticationError,
    BadRequestError,
    NotFoundError,
    RateLimitExceededError,
    ServerError,
    UnprocessableEntityError,
)
from sellapp_sdk._pagination import AsyncPage, SyncPage
from sellapp_sdk.bundle_items.models import (
    ListBundleItemsResponseValue200ApplicationJsonPropertyDataItem,
    SdkAttachBundleItemsResponseValue200ApplicationJson,
    SdkGetBundleItemResponseValue200ApplicationJson,
)
from sellapp_sdk.common.models import (
    AttachBundleItemsRequestApplicationJsonPropertyResources,
)
from tests.generated_helpers import load_fixture


class TestBundleItems:
    def test_get(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_get_bundle_item_response_value_200_application_json.json"
            ),
        )
        result = sellapp_sdk.bundle_items.get("test_bundle", "test_item")
        assert isinstance(result, SdkGetBundleItemResponseValue200ApplicationJson)
        request = httpx_mock.get_request()
        assert request.method == "GET"
        assert request.url.path.endswith("/v2/bundles/test_bundle/items/test_item")

    def test_list(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "list_list_bundle_items_response_value_200_application_json_property_data_item.json"
            ),
        )
        page = sellapp_sdk.bundle_items.list("test_bundle")
        assert isinstance(page, SyncPage)
        assert len(page.data) == 1
        assert isinstance(
            page.data[0], ListBundleItemsResponseValue200ApplicationJsonPropertyDataItem
        )

    def test_list_empty_page(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(json={"data": [], "list_metadata": {}})
        page = sellapp_sdk.bundle_items.list("test_bundle")
        assert isinstance(page, SyncPage)
        assert page.data == []

    def test_list_encodes_query_params(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(json={"data": [], "list_metadata": {}})
        sellapp_sdk.bundle_items.list(
            "test_bundle",
            limit=10,
            before="cursor before",
            after="cursor/after",
            page=7,
        )
        request = httpx_mock.get_request()
        assert request.url.params["limit"] == "10"
        assert request.url.params["before"] == "cursor before"
        assert request.url.params["after"] == "cursor/after"
        assert request.url.params["page"] == "7"

    def test_attach(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_attach_bundle_items_response_value_200_application_json.json"
            ),
        )
        result = sellapp_sdk.bundle_items.attach(
            "test_bundle",
            resources=AttachBundleItemsRequestApplicationJsonPropertyResources.from_dict(
                load_fixture(
                    "attach_bundle_items_request_application_json_property_resources.json"
                )
            ),
        )
        assert isinstance(result, SdkAttachBundleItemsResponseValue200ApplicationJson)
        request = httpx_mock.get_request()
        assert request.method == "POST"
        assert request.url.path.endswith("/v2/bundles/test_bundle/items/attach")
        body = json.loads(request.content)
        assert "resources" in body

    def test_detach(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(status_code=200, content=b"")
        result = sellapp_sdk.bundle_items.detach("test_bundle", resources=[1])
        assert result is None
        request = httpx_mock.get_request()
        assert request.method == "DELETE"
        assert request.url.path.endswith("/v2/bundles/test_bundle/items/detach")

    def test_get_with_request_options(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_get_bundle_item_response_value_200_application_json.json"
            )
        )
        sellapp_sdk.bundle_items.get(
            "test_bundle",
            "test_item",
            request_options={"headers": {"X-Custom": "value"}},
        )
        request = httpx_mock.get_request()
        assert request.headers["X-Custom"] == "value"

    def test_get_unauthorized(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            status_code=401,
            json={"message": "Unauthorized"},
        )
        with pytest.raises(AuthenticationError):
            sellapp_sdk.bundle_items.get("test_bundle", "test_item")

    def test_get_not_found(self, httpx_mock):
        sellapp_sdk = SellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=404, json={"message": "Not found"})
            with pytest.raises(NotFoundError):
                sellapp_sdk.bundle_items.get("test_bundle", "test_item")
        finally:
            sellapp_sdk.close()

    def test_get_rate_limited(self, httpx_mock):
        sellapp_sdk = SellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(
                status_code=429,
                headers={"Retry-After": "0"},
                json={"message": "Slow down"},
            )
            with pytest.raises(RateLimitExceededError):
                sellapp_sdk.bundle_items.get("test_bundle", "test_item")
        finally:
            sellapp_sdk.close()

    def test_get_server_error(self, httpx_mock):
        sellapp_sdk = SellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=500, json={"message": "Server error"})
            with pytest.raises(ServerError):
                sellapp_sdk.bundle_items.get("test_bundle", "test_item")
        finally:
            sellapp_sdk.close()

    def test_get_bad_request(self, httpx_mock):
        sellapp_sdk = SellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=400, json={"message": "Bad request"})
            with pytest.raises(BadRequestError):
                sellapp_sdk.bundle_items.get("test_bundle", "test_item")
        finally:
            sellapp_sdk.close()

    def test_get_unprocessable(self, httpx_mock):
        sellapp_sdk = SellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=422, json={"message": "Unprocessable"})
            with pytest.raises(UnprocessableEntityError):
                sellapp_sdk.bundle_items.get("test_bundle", "test_item")
        finally:
            sellapp_sdk.close()


class TestAsyncBundleItems:
    @pytest.mark.asyncio
    async def test_get(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_get_bundle_item_response_value_200_application_json.json"
            )
        )
        result = await async_sellapp_sdk.bundle_items.get("test_bundle", "test_item")
        assert isinstance(result, SdkGetBundleItemResponseValue200ApplicationJson)
        request = httpx_mock.get_request()
        assert request.method == "GET"
        assert request.url.path.endswith("/v2/bundles/test_bundle/items/test_item")

    @pytest.mark.asyncio
    async def test_list(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "list_list_bundle_items_response_value_200_application_json_property_data_item.json"
            )
        )
        page = await async_sellapp_sdk.bundle_items.list("test_bundle")
        assert isinstance(page, AsyncPage)
        assert len(page.data) == 1
        assert isinstance(
            page.data[0], ListBundleItemsResponseValue200ApplicationJsonPropertyDataItem
        )

    @pytest.mark.asyncio
    async def test_list_empty_page(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(json={"data": [], "list_metadata": {}})
        page = await async_sellapp_sdk.bundle_items.list("test_bundle")
        assert isinstance(page, AsyncPage)
        assert page.data == []

    @pytest.mark.asyncio
    async def test_list_encodes_query_params(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(json={"data": [], "list_metadata": {}})
        await async_sellapp_sdk.bundle_items.list(
            "test_bundle",
            limit=10,
            before="cursor before",
            after="cursor/after",
            page=7,
        )
        request = httpx_mock.get_request()
        assert request.url.params["limit"] == "10"
        assert request.url.params["before"] == "cursor before"
        assert request.url.params["after"] == "cursor/after"
        assert request.url.params["page"] == "7"

    @pytest.mark.asyncio
    async def test_attach(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_attach_bundle_items_response_value_200_application_json.json"
            )
        )
        result = await async_sellapp_sdk.bundle_items.attach(
            "test_bundle",
            resources=AttachBundleItemsRequestApplicationJsonPropertyResources.from_dict(
                load_fixture(
                    "attach_bundle_items_request_application_json_property_resources.json"
                )
            ),
        )
        assert isinstance(result, SdkAttachBundleItemsResponseValue200ApplicationJson)
        request = httpx_mock.get_request()
        assert request.method == "POST"
        assert request.url.path.endswith("/v2/bundles/test_bundle/items/attach")

    @pytest.mark.asyncio
    async def test_detach(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(status_code=200, content=b"")
        result = await async_sellapp_sdk.bundle_items.detach(
            "test_bundle", resources=[1]
        )
        assert result is None
        request = httpx_mock.get_request()
        assert request.method == "DELETE"
        assert request.url.path.endswith("/v2/bundles/test_bundle/items/detach")

    @pytest.mark.asyncio
    async def test_get_with_request_options(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_get_bundle_item_response_value_200_application_json.json"
            )
        )
        await async_sellapp_sdk.bundle_items.get(
            "test_bundle",
            "test_item",
            request_options={"headers": {"X-Custom": "value"}},
        )
        request = httpx_mock.get_request()
        assert request.headers["X-Custom"] == "value"

    @pytest.mark.asyncio
    async def test_get_unauthorized(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(status_code=401, json={"message": "Unauthorized"})
        with pytest.raises(AuthenticationError):
            await async_sellapp_sdk.bundle_items.get("test_bundle", "test_item")

    @pytest.mark.asyncio
    async def test_get_not_found(self, httpx_mock):
        sellapp_sdk = AsyncSellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=404, json={"message": "Not found"})
            with pytest.raises(NotFoundError):
                await sellapp_sdk.bundle_items.get("test_bundle", "test_item")
        finally:
            await sellapp_sdk.close()

    @pytest.mark.asyncio
    async def test_get_rate_limited(self, httpx_mock):
        sellapp_sdk = AsyncSellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(
                status_code=429,
                headers={"Retry-After": "0"},
                json={"message": "Slow down"},
            )
            with pytest.raises(RateLimitExceededError):
                await sellapp_sdk.bundle_items.get("test_bundle", "test_item")
        finally:
            await sellapp_sdk.close()

    @pytest.mark.asyncio
    async def test_get_server_error(self, httpx_mock):
        sellapp_sdk = AsyncSellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=500, json={"message": "Server error"})
            with pytest.raises(ServerError):
                await sellapp_sdk.bundle_items.get("test_bundle", "test_item")
        finally:
            await sellapp_sdk.close()

    @pytest.mark.asyncio
    async def test_get_bad_request(self, httpx_mock):
        sellapp_sdk = AsyncSellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=400, json={"message": "Bad request"})
            with pytest.raises(BadRequestError):
                await sellapp_sdk.bundle_items.get("test_bundle", "test_item")
        finally:
            await sellapp_sdk.close()

    @pytest.mark.asyncio
    async def test_get_unprocessable(self, httpx_mock):
        sellapp_sdk = AsyncSellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=422, json={"message": "Unprocessable"})
            with pytest.raises(UnprocessableEntityError):
                await sellapp_sdk.bundle_items.get("test_bundle", "test_item")
        finally:
            await sellapp_sdk.close()
