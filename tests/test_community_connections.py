# This file is auto-generated by oagen. Do not edit.

import json

import pytest

from sellapp_sdk import AsyncSellAppClient, SellAppClient
from sellapp_sdk._errors import (
    AuthenticationError,
    BadRequestError,
    NotFoundError,
    RateLimitExceededError,
    ServerError,
    UnprocessableEntityError,
)
from sellapp_sdk.community_connections.models import (
    SdkCompleteCommunityConnectionResponseValue200ApplicationJson,
    SdkListCommunityConnectionsResponseValue200ApplicationJson,
    SdkPollCommunityConnectionResponseValue200ApplicationJson,
    SdkStartCommunityConnectionResponseValue200ApplicationJson,
    SdkVerifyCommunityConnectionResponseValue200ApplicationJson,
)
from tests.generated_helpers import load_fixture


class TestCommunityConnections:
    def test_list(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_list_community_connections_response_value_200_application_json.json"
            ),
        )
        result = sellapp_sdk.community_connections.list()
        assert isinstance(
            result, SdkListCommunityConnectionsResponseValue200ApplicationJson
        )
        request = httpx_mock.get_request()
        assert request.method == "GET"
        assert request.url.path.endswith("/v2/community-connections")

    def test_start(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_start_community_connection_response_value_200_application_json.json"
            ),
        )
        result = sellapp_sdk.community_connections.start("test_platform")
        assert isinstance(
            result, SdkStartCommunityConnectionResponseValue200ApplicationJson
        )
        request = httpx_mock.get_request()
        assert request.method == "POST"
        assert request.url.path.endswith(
            "/v2/community-connections/test_platform/connect"
        )

    def test_poll(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_poll_community_connection_response_value_200_application_json.json"
            ),
        )
        result = sellapp_sdk.community_connections.poll(
            "test_platform", status_token="test_status_token"
        )
        assert isinstance(
            result, SdkPollCommunityConnectionResponseValue200ApplicationJson
        )
        request = httpx_mock.get_request()
        assert request.method == "GET"
        assert request.url.path.endswith(
            "/v2/community-connections/test_platform/status"
        )

    def test_poll_encodes_query_params(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_poll_community_connection_response_value_200_application_json.json"
            )
        )
        sellapp_sdk.community_connections.poll(
            "test_platform", status_token="value status_token/test"
        )
        request = httpx_mock.get_request()
        assert request.url.params["status_token"] == "value status_token/test"

    def test_complete(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_complete_community_connection_response_value_200_application_json.json"
            ),
        )
        result = sellapp_sdk.community_connections.complete(
            "test_platform",
            status_token="test_status_token",
            server_id="test_server_id",
        )
        assert isinstance(
            result, SdkCompleteCommunityConnectionResponseValue200ApplicationJson
        )
        request = httpx_mock.get_request()
        assert request.method == "POST"
        assert request.url.path.endswith(
            "/v2/community-connections/test_platform/complete"
        )
        body = json.loads(request.content)
        assert body["status_token"] == "test_status_token"
        assert body["server_id"] == "test_server_id"

    def test_verify(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_verify_community_connection_response_value_200_application_json.json"
            ),
        )
        result = sellapp_sdk.community_connections.verify("test_platform")
        assert isinstance(
            result, SdkVerifyCommunityConnectionResponseValue200ApplicationJson
        )
        request = httpx_mock.get_request()
        assert request.method == "POST"
        assert request.url.path.endswith(
            "/v2/community-connections/test_platform/verify"
        )

    def test_disconnect(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(status_code=200, content=b"")
        result = sellapp_sdk.community_connections.disconnect("test_platform")
        assert result is None
        request = httpx_mock.get_request()
        assert request.method == "DELETE"
        assert request.url.path.endswith("/v2/community-connections/test_platform")

    def test_list_with_request_options(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_list_community_connections_response_value_200_application_json.json"
            )
        )
        sellapp_sdk.community_connections.list(
            request_options={"headers": {"X-Custom": "value"}}
        )
        request = httpx_mock.get_request()
        assert request.headers["X-Custom"] == "value"

    def test_list_unauthorized(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            status_code=401,
            json={"message": "Unauthorized"},
        )
        with pytest.raises(AuthenticationError):
            sellapp_sdk.community_connections.list()

    def test_list_not_found(self, httpx_mock):
        sellapp_sdk = SellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=404, json={"message": "Not found"})
            with pytest.raises(NotFoundError):
                sellapp_sdk.community_connections.list()
        finally:
            sellapp_sdk.close()

    def test_list_rate_limited(self, httpx_mock):
        sellapp_sdk = SellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(
                status_code=429,
                headers={"Retry-After": "0"},
                json={"message": "Slow down"},
            )
            with pytest.raises(RateLimitExceededError):
                sellapp_sdk.community_connections.list()
        finally:
            sellapp_sdk.close()

    def test_list_server_error(self, httpx_mock):
        sellapp_sdk = SellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=500, json={"message": "Server error"})
            with pytest.raises(ServerError):
                sellapp_sdk.community_connections.list()
        finally:
            sellapp_sdk.close()

    def test_list_bad_request(self, httpx_mock):
        sellapp_sdk = SellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=400, json={"message": "Bad request"})
            with pytest.raises(BadRequestError):
                sellapp_sdk.community_connections.list()
        finally:
            sellapp_sdk.close()

    def test_list_unprocessable(self, httpx_mock):
        sellapp_sdk = SellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=422, json={"message": "Unprocessable"})
            with pytest.raises(UnprocessableEntityError):
                sellapp_sdk.community_connections.list()
        finally:
            sellapp_sdk.close()


class TestAsyncCommunityConnections:
    @pytest.mark.asyncio
    async def test_list(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_list_community_connections_response_value_200_application_json.json"
            )
        )
        result = await async_sellapp_sdk.community_connections.list()
        assert isinstance(
            result, SdkListCommunityConnectionsResponseValue200ApplicationJson
        )
        request = httpx_mock.get_request()
        assert request.method == "GET"
        assert request.url.path.endswith("/v2/community-connections")

    @pytest.mark.asyncio
    async def test_start(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_start_community_connection_response_value_200_application_json.json"
            )
        )
        result = await async_sellapp_sdk.community_connections.start("test_platform")
        assert isinstance(
            result, SdkStartCommunityConnectionResponseValue200ApplicationJson
        )
        request = httpx_mock.get_request()
        assert request.method == "POST"
        assert request.url.path.endswith(
            "/v2/community-connections/test_platform/connect"
        )

    @pytest.mark.asyncio
    async def test_poll(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_poll_community_connection_response_value_200_application_json.json"
            )
        )
        result = await async_sellapp_sdk.community_connections.poll(
            "test_platform", status_token="test_status_token"
        )
        assert isinstance(
            result, SdkPollCommunityConnectionResponseValue200ApplicationJson
        )
        request = httpx_mock.get_request()
        assert request.method == "GET"
        assert request.url.path.endswith(
            "/v2/community-connections/test_platform/status"
        )

    @pytest.mark.asyncio
    async def test_poll_encodes_query_params(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_poll_community_connection_response_value_200_application_json.json"
            )
        )
        await async_sellapp_sdk.community_connections.poll(
            "test_platform", status_token="value status_token/test"
        )
        request = httpx_mock.get_request()
        assert request.url.params["status_token"] == "value status_token/test"

    @pytest.mark.asyncio
    async def test_complete(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_complete_community_connection_response_value_200_application_json.json"
            )
        )
        result = await async_sellapp_sdk.community_connections.complete(
            "test_platform",
            status_token="test_status_token",
            server_id="test_server_id",
        )
        assert isinstance(
            result, SdkCompleteCommunityConnectionResponseValue200ApplicationJson
        )
        request = httpx_mock.get_request()
        assert request.method == "POST"
        assert request.url.path.endswith(
            "/v2/community-connections/test_platform/complete"
        )

    @pytest.mark.asyncio
    async def test_verify(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_verify_community_connection_response_value_200_application_json.json"
            )
        )
        result = await async_sellapp_sdk.community_connections.verify("test_platform")
        assert isinstance(
            result, SdkVerifyCommunityConnectionResponseValue200ApplicationJson
        )
        request = httpx_mock.get_request()
        assert request.method == "POST"
        assert request.url.path.endswith(
            "/v2/community-connections/test_platform/verify"
        )

    @pytest.mark.asyncio
    async def test_disconnect(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(status_code=200, content=b"")
        result = await async_sellapp_sdk.community_connections.disconnect(
            "test_platform"
        )
        assert result is None
        request = httpx_mock.get_request()
        assert request.method == "DELETE"
        assert request.url.path.endswith("/v2/community-connections/test_platform")

    @pytest.mark.asyncio
    async def test_list_with_request_options(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_list_community_connections_response_value_200_application_json.json"
            )
        )
        await async_sellapp_sdk.community_connections.list(
            request_options={"headers": {"X-Custom": "value"}}
        )
        request = httpx_mock.get_request()
        assert request.headers["X-Custom"] == "value"

    @pytest.mark.asyncio
    async def test_list_unauthorized(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(status_code=401, json={"message": "Unauthorized"})
        with pytest.raises(AuthenticationError):
            await async_sellapp_sdk.community_connections.list()

    @pytest.mark.asyncio
    async def test_list_not_found(self, httpx_mock):
        sellapp_sdk = AsyncSellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=404, json={"message": "Not found"})
            with pytest.raises(NotFoundError):
                await sellapp_sdk.community_connections.list()
        finally:
            await sellapp_sdk.close()

    @pytest.mark.asyncio
    async def test_list_rate_limited(self, httpx_mock):
        sellapp_sdk = AsyncSellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(
                status_code=429,
                headers={"Retry-After": "0"},
                json={"message": "Slow down"},
            )
            with pytest.raises(RateLimitExceededError):
                await sellapp_sdk.community_connections.list()
        finally:
            await sellapp_sdk.close()

    @pytest.mark.asyncio
    async def test_list_server_error(self, httpx_mock):
        sellapp_sdk = AsyncSellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=500, json={"message": "Server error"})
            with pytest.raises(ServerError):
                await sellapp_sdk.community_connections.list()
        finally:
            await sellapp_sdk.close()

    @pytest.mark.asyncio
    async def test_list_bad_request(self, httpx_mock):
        sellapp_sdk = AsyncSellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=400, json={"message": "Bad request"})
            with pytest.raises(BadRequestError):
                await sellapp_sdk.community_connections.list()
        finally:
            await sellapp_sdk.close()

    @pytest.mark.asyncio
    async def test_list_unprocessable(self, httpx_mock):
        sellapp_sdk = AsyncSellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=422, json={"message": "Unprocessable"})
            with pytest.raises(UnprocessableEntityError):
                await sellapp_sdk.community_connections.list()
        finally:
            await sellapp_sdk.close()
