# This file is auto-generated by oagen. Do not edit.

import json

import pytest

from sellapp_sdk import AsyncSellAppClient, SellAppClient
from sellapp_sdk._errors import (
    AuthenticationError,
    BadRequestError,
    NotFoundError,
    RateLimitExceededError,
    ServerError,
    UnprocessableEntityError,
)
from sellapp_sdk._pagination import AsyncPage, SyncPage
from sellapp_sdk.blacklists.models import (
    BatchUpdateCouponsRequestApplicationJsonPropertyResources,
    SdkBatchUpdateCouponsResponseValue200ApplicationJson,
    SdkGetCouponResponseValue200ApplicationJson,
    SdkReplaceCouponResponseValue200ApplicationJson,
    SdkUpdateCouponResponseValue200ApplicationJson,
    SdkV2BatchCreateCouponsResponseValue200ApplicationJson,
    SdkV2BatchUpdateCouponsResponseValue200ApplicationJson,
    SdkV2CreateCouponResponseValue201ApplicationJson,
    SdkV2GetCouponResponseValue200ApplicationJson,
    SdkV2ReplaceCouponResponseValue200ApplicationJson,
    SdkV2UpdateCouponResponseValue200ApplicationJson,
    SearchCouponsResponseValue200ApplicationJsonPropertyDataItem,
    V2BatchCreateCouponsRequestApplicationJsonPropertyResourcesItem,
    V2BatchUpdateCouponsRequestApplicationJsonPropertyResources,
    V2ListCouponsResponseValue200ApplicationJsonPropertyDataItem,
    V2SearchCouponsResponseValue200ApplicationJsonPropertyDataItem,
)
from sellapp_sdk.common.models import (
    BatchCreateCouponsRequestApplicationJsonPropertyResourcesItem,
    ListCouponsResponseValue200ApplicationJsonPropertyDataItem,
    SdkBatchCreateCouponsResponseValue200ApplicationJson,
    SdkCreateCouponRequestApplicationJsonType,
    SdkCreateCouponResponseValue201ApplicationJson,
    SdkV2CreateCouponRequestApplicationJsonType,
)
from tests.generated_helpers import load_fixture


class TestCoupons:
    def test_list(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "list_list_coupons_response_value_200_application_json_property_data_item.json"
            ),
        )
        page = sellapp_sdk.coupons.list()
        assert isinstance(page, SyncPage)
        assert len(page.data) == 1
        assert isinstance(
            page.data[0], ListCouponsResponseValue200ApplicationJsonPropertyDataItem
        )

    def test_list_empty_page(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(json={"data": [], "list_metadata": {}})
        page = sellapp_sdk.coupons.list()
        assert isinstance(page, SyncPage)
        assert page.data == []

    def test_list_encodes_query_params(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(json={"data": [], "list_metadata": {}})
        sellapp_sdk.coupons.list(
            limit=10,
            before="cursor before",
            after="cursor/after",
            page=7,
            with_trashed=True,
            only_trashed=True,
        )
        request = httpx_mock.get_request()
        assert request.url.params["limit"] == "10"
        assert request.url.params["before"] == "cursor before"
        assert request.url.params["after"] == "cursor/after"
        assert request.url.params["page"] == "7"
        assert request.url.params["with_trashed"] == "true"
        assert request.url.params["only_trashed"] == "true"

    def test_create(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_create_coupon_response_value_201_application_json.json"
            ),
        )
        result = sellapp_sdk.coupons.create(
            code="test_code",
            type=SdkCreateCouponRequestApplicationJsonType("PERCENTAGE"),
            discount=1,
            store_wide=True,
        )
        assert isinstance(result, SdkCreateCouponResponseValue201ApplicationJson)
        request = httpx_mock.get_request()
        assert request.method == "POST"
        assert request.url.path.endswith("/v1/coupons")
        body = json.loads(request.content)
        assert body["code"] == "test_code"
        assert body["type"] == SdkCreateCouponRequestApplicationJsonType("PERCENTAGE")
        assert "discount" in body
        assert body["store_wide"] == True

    def test_get(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_get_coupon_response_value_200_application_json.json"
            ),
        )
        result = sellapp_sdk.coupons.get("test_coupon")
        assert isinstance(result, SdkGetCouponResponseValue200ApplicationJson)
        request = httpx_mock.get_request()
        assert request.method == "GET"
        assert request.url.path.endswith("/v1/coupons/test_coupon")

    def test_get_encodes_query_params(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture("sdk_get_coupon_response_value_200_application_json.json")
        )
        sellapp_sdk.coupons.get("test_coupon", with_trashed=True, only_trashed=True)
        request = httpx_mock.get_request()
        assert request.url.params["with_trashed"] == "true"
        assert request.url.params["only_trashed"] == "true"

    def test_replace(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_replace_coupon_response_value_200_application_json.json"
            ),
        )
        result = sellapp_sdk.coupons.replace("test_coupon")
        assert isinstance(result, SdkReplaceCouponResponseValue200ApplicationJson)
        request = httpx_mock.get_request()
        assert request.method == "PUT"
        assert request.url.path.endswith("/v1/coupons/test_coupon")

    def test_replace_encodes_query_params(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_replace_coupon_response_value_200_application_json.json"
            )
        )
        sellapp_sdk.coupons.replace("test_coupon", with_trashed=True, only_trashed=True)
        request = httpx_mock.get_request()
        assert request.url.params["with_trashed"] == "true"
        assert request.url.params["only_trashed"] == "true"

    def test_update(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_update_coupon_response_value_200_application_json.json"
            ),
        )
        result = sellapp_sdk.coupons.update("test_coupon")
        assert isinstance(result, SdkUpdateCouponResponseValue200ApplicationJson)
        request = httpx_mock.get_request()
        assert request.method == "PATCH"
        assert request.url.path.endswith("/v1/coupons/test_coupon")

    def test_update_encodes_query_params(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_update_coupon_response_value_200_application_json.json"
            )
        )
        sellapp_sdk.coupons.update("test_coupon", with_trashed=True, only_trashed=True)
        request = httpx_mock.get_request()
        assert request.url.params["with_trashed"] == "true"
        assert request.url.params["only_trashed"] == "true"

    def test_delete(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(status_code=200, content=b"")
        result = sellapp_sdk.coupons.delete("test_coupon")
        assert result is None
        request = httpx_mock.get_request()
        assert request.method == "DELETE"
        assert request.url.path.endswith("/v1/coupons/test_coupon")

    def test_delete_encodes_query_params(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(status_code=200, content=b"")
        sellapp_sdk.coupons.delete("test_coupon", with_trashed=True, only_trashed=True)
        request = httpx_mock.get_request()
        assert request.url.params["with_trashed"] == "true"
        assert request.url.params["only_trashed"] == "true"

    def test_search(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "list_search_coupons_response_value_200_application_json_property_data_item.json"
            ),
        )
        page = sellapp_sdk.coupons.search()
        assert isinstance(page, SyncPage)
        assert len(page.data) == 1
        assert isinstance(
            page.data[0], SearchCouponsResponseValue200ApplicationJsonPropertyDataItem
        )

    def test_search_empty_page(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(json={"data": [], "list_metadata": {}})
        page = sellapp_sdk.coupons.search()
        assert isinstance(page, SyncPage)
        assert page.data == []

    def test_search_encodes_query_params(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(json={"data": [], "list_metadata": {}})
        sellapp_sdk.coupons.search(
            limit=10,
            before="cursor before",
            after="cursor/after",
            with_trashed=True,
            only_trashed=True,
        )
        request = httpx_mock.get_request()
        assert request.url.params["limit"] == "10"
        assert request.url.params["before"] == "cursor before"
        assert request.url.params["after"] == "cursor/after"
        assert request.url.params["with_trashed"] == "true"
        assert request.url.params["only_trashed"] == "true"

    def test_batch_create(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_batch_create_coupons_response_value_200_application_json.json"
            ),
        )
        result = sellapp_sdk.coupons.batch_create(
            resources=[
                BatchCreateCouponsRequestApplicationJsonPropertyResourcesItem.from_dict(
                    load_fixture(
                        "batch_create_coupons_request_application_json_property_resources_item.json"
                    )
                )
            ]
        )
        assert isinstance(result, SdkBatchCreateCouponsResponseValue200ApplicationJson)
        request = httpx_mock.get_request()
        assert request.method == "POST"
        assert request.url.path.endswith("/v1/coupons/batch")
        body = json.loads(request.content)
        assert "resources" in body

    def test_batch_update(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_batch_update_coupons_response_value_200_application_json.json"
            ),
        )
        result = sellapp_sdk.coupons.batch_update(
            resources=BatchUpdateCouponsRequestApplicationJsonPropertyResources.from_dict(
                load_fixture(
                    "batch_update_coupons_request_application_json_property_resources.json"
                )
            )
        )
        assert isinstance(result, SdkBatchUpdateCouponsResponseValue200ApplicationJson)
        request = httpx_mock.get_request()
        assert request.method == "PATCH"
        assert request.url.path.endswith("/v1/coupons/batch")
        body = json.loads(request.content)
        assert "resources" in body

    def test_batch_update_encodes_query_params(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_batch_update_coupons_response_value_200_application_json.json"
            )
        )
        sellapp_sdk.coupons.batch_update(
            resources=BatchUpdateCouponsRequestApplicationJsonPropertyResources.from_dict(
                load_fixture(
                    "batch_update_coupons_request_application_json_property_resources.json"
                )
            ),
            with_trashed=True,
            only_trashed=True,
        )
        request = httpx_mock.get_request()
        assert request.url.params["with_trashed"] == "true"
        assert request.url.params["only_trashed"] == "true"

    def test_batch_delete(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(status_code=200, content=b"")
        result = sellapp_sdk.coupons.batch_delete(resources=[1])
        assert result is None
        request = httpx_mock.get_request()
        assert request.method == "DELETE"
        assert request.url.path.endswith("/v1/coupons/batch")

    def test_batch_delete_encodes_query_params(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(status_code=200, content=b"")
        sellapp_sdk.coupons.batch_delete(
            resources=[1], with_trashed=True, only_trashed=True
        )
        request = httpx_mock.get_request()
        assert request.url.params["with_trashed"] == "true"
        assert request.url.params["only_trashed"] == "true"

    def test_v2_list_coupons(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "list_v_2_list_coupons_response_value_200_application_json_property_data_item.json"
            ),
        )
        page = sellapp_sdk.coupons.v2_list_coupons()
        assert isinstance(page, SyncPage)
        assert len(page.data) == 1
        assert isinstance(
            page.data[0], V2ListCouponsResponseValue200ApplicationJsonPropertyDataItem
        )

    def test_v2_list_coupons_empty_page(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(json={"data": [], "list_metadata": {}})
        page = sellapp_sdk.coupons.v2_list_coupons()
        assert isinstance(page, SyncPage)
        assert page.data == []

    def test_v2_list_coupons_encodes_query_params(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(json={"data": [], "list_metadata": {}})
        sellapp_sdk.coupons.v2_list_coupons(
            limit=10,
            before="cursor before",
            after="cursor/after",
            page=7,
            with_trashed=True,
            only_trashed=True,
        )
        request = httpx_mock.get_request()
        assert request.url.params["limit"] == "10"
        assert request.url.params["before"] == "cursor before"
        assert request.url.params["after"] == "cursor/after"
        assert request.url.params["page"] == "7"
        assert request.url.params["with_trashed"] == "true"
        assert request.url.params["only_trashed"] == "true"

    def test_v2_create_coupon(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_v_2_create_coupon_response_value_201_application_json.json"
            ),
        )
        result = sellapp_sdk.coupons.v2_create_coupon(
            code="test_code",
            type=SdkV2CreateCouponRequestApplicationJsonType("PERCENTAGE"),
            discount=1,
            store_wide=True,
        )
        assert isinstance(result, SdkV2CreateCouponResponseValue201ApplicationJson)
        request = httpx_mock.get_request()
        assert request.method == "POST"
        assert request.url.path.endswith("/v2/coupons")
        body = json.loads(request.content)
        assert body["code"] == "test_code"
        assert body["type"] == SdkV2CreateCouponRequestApplicationJsonType("PERCENTAGE")
        assert "discount" in body
        assert body["store_wide"] == True

    def test_v2_search_coupons(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "list_v_2_search_coupons_response_value_200_application_json_property_data_item.json"
            ),
        )
        page = sellapp_sdk.coupons.v2_search_coupons()
        assert isinstance(page, SyncPage)
        assert len(page.data) == 1
        assert isinstance(
            page.data[0], V2SearchCouponsResponseValue200ApplicationJsonPropertyDataItem
        )

    def test_v2_search_coupons_empty_page(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(json={"data": [], "list_metadata": {}})
        page = sellapp_sdk.coupons.v2_search_coupons()
        assert isinstance(page, SyncPage)
        assert page.data == []

    def test_v2_search_coupons_encodes_query_params(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(json={"data": [], "list_metadata": {}})
        sellapp_sdk.coupons.v2_search_coupons(
            limit=10,
            before="cursor before",
            after="cursor/after",
            with_trashed=True,
            only_trashed=True,
        )
        request = httpx_mock.get_request()
        assert request.url.params["limit"] == "10"
        assert request.url.params["before"] == "cursor before"
        assert request.url.params["after"] == "cursor/after"
        assert request.url.params["with_trashed"] == "true"
        assert request.url.params["only_trashed"] == "true"

    def test_v2_get_coupon(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_v_2_get_coupon_response_value_200_application_json.json"
            ),
        )
        result = sellapp_sdk.coupons.v2_get_coupon("test_coupon")
        assert isinstance(result, SdkV2GetCouponResponseValue200ApplicationJson)
        request = httpx_mock.get_request()
        assert request.method == "GET"
        assert request.url.path.endswith("/v2/coupons/test_coupon")

    def test_v2_get_coupon_encodes_query_params(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_v_2_get_coupon_response_value_200_application_json.json"
            )
        )
        sellapp_sdk.coupons.v2_get_coupon(
            "test_coupon", with_trashed=True, only_trashed=True
        )
        request = httpx_mock.get_request()
        assert request.url.params["with_trashed"] == "true"
        assert request.url.params["only_trashed"] == "true"

    def test_v2_replace_coupon(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_v_2_replace_coupon_response_value_200_application_json.json"
            ),
        )
        result = sellapp_sdk.coupons.v2_replace_coupon("test_coupon")
        assert isinstance(result, SdkV2ReplaceCouponResponseValue200ApplicationJson)
        request = httpx_mock.get_request()
        assert request.method == "PUT"
        assert request.url.path.endswith("/v2/coupons/test_coupon")

    def test_v2_replace_coupon_encodes_query_params(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_v_2_replace_coupon_response_value_200_application_json.json"
            )
        )
        sellapp_sdk.coupons.v2_replace_coupon(
            "test_coupon", with_trashed=True, only_trashed=True
        )
        request = httpx_mock.get_request()
        assert request.url.params["with_trashed"] == "true"
        assert request.url.params["only_trashed"] == "true"

    def test_v2_update_coupon(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_v_2_update_coupon_response_value_200_application_json.json"
            ),
        )
        result = sellapp_sdk.coupons.v2_update_coupon("test_coupon")
        assert isinstance(result, SdkV2UpdateCouponResponseValue200ApplicationJson)
        request = httpx_mock.get_request()
        assert request.method == "PATCH"
        assert request.url.path.endswith("/v2/coupons/test_coupon")

    def test_v2_update_coupon_encodes_query_params(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_v_2_update_coupon_response_value_200_application_json.json"
            )
        )
        sellapp_sdk.coupons.v2_update_coupon(
            "test_coupon", with_trashed=True, only_trashed=True
        )
        request = httpx_mock.get_request()
        assert request.url.params["with_trashed"] == "true"
        assert request.url.params["only_trashed"] == "true"

    def test_v2_delete_coupon(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(status_code=200, content=b"")
        result = sellapp_sdk.coupons.v2_delete_coupon("test_coupon")
        assert result is None
        request = httpx_mock.get_request()
        assert request.method == "DELETE"
        assert request.url.path.endswith("/v2/coupons/test_coupon")

    def test_v2_delete_coupon_encodes_query_params(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(status_code=200, content=b"")
        sellapp_sdk.coupons.v2_delete_coupon(
            "test_coupon", with_trashed=True, only_trashed=True
        )
        request = httpx_mock.get_request()
        assert request.url.params["with_trashed"] == "true"
        assert request.url.params["only_trashed"] == "true"

    def test_v2_batch_create_coupons(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_v_2_batch_create_coupons_response_value_200_application_json.json"
            ),
        )
        result = sellapp_sdk.coupons.v2_batch_create_coupons(
            resources=[
                V2BatchCreateCouponsRequestApplicationJsonPropertyResourcesItem.from_dict(
                    load_fixture(
                        "v_2_batch_create_coupons_request_application_json_property_resources_item.json"
                    )
                )
            ]
        )
        assert isinstance(
            result, SdkV2BatchCreateCouponsResponseValue200ApplicationJson
        )
        request = httpx_mock.get_request()
        assert request.method == "POST"
        assert request.url.path.endswith("/v2/coupons/batch")
        body = json.loads(request.content)
        assert "resources" in body

    def test_v2_batch_update_coupons(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_v_2_batch_update_coupons_response_value_200_application_json.json"
            ),
        )
        result = sellapp_sdk.coupons.v2_batch_update_coupons(
            resources=V2BatchUpdateCouponsRequestApplicationJsonPropertyResources.from_dict(
                load_fixture(
                    "v_2_batch_update_coupons_request_application_json_property_resources.json"
                )
            )
        )
        assert isinstance(
            result, SdkV2BatchUpdateCouponsResponseValue200ApplicationJson
        )
        request = httpx_mock.get_request()
        assert request.method == "PATCH"
        assert request.url.path.endswith("/v2/coupons/batch")
        body = json.loads(request.content)
        assert "resources" in body

    def test_v2_batch_update_coupons_encodes_query_params(
        self, sellapp_sdk, httpx_mock
    ):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_v_2_batch_update_coupons_response_value_200_application_json.json"
            )
        )
        sellapp_sdk.coupons.v2_batch_update_coupons(
            resources=V2BatchUpdateCouponsRequestApplicationJsonPropertyResources.from_dict(
                load_fixture(
                    "v_2_batch_update_coupons_request_application_json_property_resources.json"
                )
            ),
            with_trashed=True,
            only_trashed=True,
        )
        request = httpx_mock.get_request()
        assert request.url.params["with_trashed"] == "true"
        assert request.url.params["only_trashed"] == "true"

    def test_v2_batch_delete_coupons(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(status_code=200, content=b"")
        result = sellapp_sdk.coupons.v2_batch_delete_coupons(resources=[1])
        assert result is None
        request = httpx_mock.get_request()
        assert request.method == "DELETE"
        assert request.url.path.endswith("/v2/coupons/batch")

    def test_v2_batch_delete_coupons_encodes_query_params(
        self, sellapp_sdk, httpx_mock
    ):
        httpx_mock.add_response(status_code=200, content=b"")
        sellapp_sdk.coupons.v2_batch_delete_coupons(
            resources=[1], with_trashed=True, only_trashed=True
        )
        request = httpx_mock.get_request()
        assert request.url.params["with_trashed"] == "true"
        assert request.url.params["only_trashed"] == "true"

    def test_list_with_request_options(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(json={"data": [], "list_metadata": {}})
        sellapp_sdk.coupons.list(request_options={"headers": {"X-Custom": "value"}})
        request = httpx_mock.get_request()
        assert request.headers["X-Custom"] == "value"

    def test_list_unauthorized(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            status_code=401,
            json={"message": "Unauthorized"},
        )
        with pytest.raises(AuthenticationError):
            sellapp_sdk.coupons.list()

    def test_list_not_found(self, httpx_mock):
        sellapp_sdk = SellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=404, json={"message": "Not found"})
            with pytest.raises(NotFoundError):
                sellapp_sdk.coupons.list()
        finally:
            sellapp_sdk.close()

    def test_list_rate_limited(self, httpx_mock):
        sellapp_sdk = SellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(
                status_code=429,
                headers={"Retry-After": "0"},
                json={"message": "Slow down"},
            )
            with pytest.raises(RateLimitExceededError):
                sellapp_sdk.coupons.list()
        finally:
            sellapp_sdk.close()

    def test_list_server_error(self, httpx_mock):
        sellapp_sdk = SellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=500, json={"message": "Server error"})
            with pytest.raises(ServerError):
                sellapp_sdk.coupons.list()
        finally:
            sellapp_sdk.close()

    def test_list_bad_request(self, httpx_mock):
        sellapp_sdk = SellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=400, json={"message": "Bad request"})
            with pytest.raises(BadRequestError):
                sellapp_sdk.coupons.list()
        finally:
            sellapp_sdk.close()

    def test_list_unprocessable(self, httpx_mock):
        sellapp_sdk = SellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=422, json={"message": "Unprocessable"})
            with pytest.raises(UnprocessableEntityError):
                sellapp_sdk.coupons.list()
        finally:
            sellapp_sdk.close()


class TestAsyncCoupons:
    @pytest.mark.asyncio
    async def test_list(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "list_list_coupons_response_value_200_application_json_property_data_item.json"
            )
        )
        page = await async_sellapp_sdk.coupons.list()
        assert isinstance(page, AsyncPage)
        assert len(page.data) == 1
        assert isinstance(
            page.data[0], ListCouponsResponseValue200ApplicationJsonPropertyDataItem
        )

    @pytest.mark.asyncio
    async def test_list_empty_page(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(json={"data": [], "list_metadata": {}})
        page = await async_sellapp_sdk.coupons.list()
        assert isinstance(page, AsyncPage)
        assert page.data == []

    @pytest.mark.asyncio
    async def test_list_encodes_query_params(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(json={"data": [], "list_metadata": {}})
        await async_sellapp_sdk.coupons.list(
            limit=10,
            before="cursor before",
            after="cursor/after",
            page=7,
            with_trashed=True,
            only_trashed=True,
        )
        request = httpx_mock.get_request()
        assert request.url.params["limit"] == "10"
        assert request.url.params["before"] == "cursor before"
        assert request.url.params["after"] == "cursor/after"
        assert request.url.params["page"] == "7"
        assert request.url.params["with_trashed"] == "true"
        assert request.url.params["only_trashed"] == "true"

    @pytest.mark.asyncio
    async def test_create(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_create_coupon_response_value_201_application_json.json"
            )
        )
        result = await async_sellapp_sdk.coupons.create(
            code="test_code",
            type=SdkCreateCouponRequestApplicationJsonType("PERCENTAGE"),
            discount=1,
            store_wide=True,
        )
        assert isinstance(result, SdkCreateCouponResponseValue201ApplicationJson)
        request = httpx_mock.get_request()
        assert request.method == "POST"
        assert request.url.path.endswith("/v1/coupons")

    @pytest.mark.asyncio
    async def test_get(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture("sdk_get_coupon_response_value_200_application_json.json")
        )
        result = await async_sellapp_sdk.coupons.get("test_coupon")
        assert isinstance(result, SdkGetCouponResponseValue200ApplicationJson)
        request = httpx_mock.get_request()
        assert request.method == "GET"
        assert request.url.path.endswith("/v1/coupons/test_coupon")

    @pytest.mark.asyncio
    async def test_get_encodes_query_params(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture("sdk_get_coupon_response_value_200_application_json.json")
        )
        await async_sellapp_sdk.coupons.get(
            "test_coupon", with_trashed=True, only_trashed=True
        )
        request = httpx_mock.get_request()
        assert request.url.params["with_trashed"] == "true"
        assert request.url.params["only_trashed"] == "true"

    @pytest.mark.asyncio
    async def test_replace(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_replace_coupon_response_value_200_application_json.json"
            )
        )
        result = await async_sellapp_sdk.coupons.replace("test_coupon")
        assert isinstance(result, SdkReplaceCouponResponseValue200ApplicationJson)
        request = httpx_mock.get_request()
        assert request.method == "PUT"
        assert request.url.path.endswith("/v1/coupons/test_coupon")

    @pytest.mark.asyncio
    async def test_replace_encodes_query_params(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_replace_coupon_response_value_200_application_json.json"
            )
        )
        await async_sellapp_sdk.coupons.replace(
            "test_coupon", with_trashed=True, only_trashed=True
        )
        request = httpx_mock.get_request()
        assert request.url.params["with_trashed"] == "true"
        assert request.url.params["only_trashed"] == "true"

    @pytest.mark.asyncio
    async def test_update(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_update_coupon_response_value_200_application_json.json"
            )
        )
        result = await async_sellapp_sdk.coupons.update("test_coupon")
        assert isinstance(result, SdkUpdateCouponResponseValue200ApplicationJson)
        request = httpx_mock.get_request()
        assert request.method == "PATCH"
        assert request.url.path.endswith("/v1/coupons/test_coupon")

    @pytest.mark.asyncio
    async def test_update_encodes_query_params(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_update_coupon_response_value_200_application_json.json"
            )
        )
        await async_sellapp_sdk.coupons.update(
            "test_coupon", with_trashed=True, only_trashed=True
        )
        request = httpx_mock.get_request()
        assert request.url.params["with_trashed"] == "true"
        assert request.url.params["only_trashed"] == "true"

    @pytest.mark.asyncio
    async def test_delete(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(status_code=200, content=b"")
        result = await async_sellapp_sdk.coupons.delete("test_coupon")
        assert result is None
        request = httpx_mock.get_request()
        assert request.method == "DELETE"
        assert request.url.path.endswith("/v1/coupons/test_coupon")

    @pytest.mark.asyncio
    async def test_delete_encodes_query_params(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(status_code=200, content=b"")
        await async_sellapp_sdk.coupons.delete(
            "test_coupon", with_trashed=True, only_trashed=True
        )
        request = httpx_mock.get_request()
        assert request.url.params["with_trashed"] == "true"
        assert request.url.params["only_trashed"] == "true"

    @pytest.mark.asyncio
    async def test_search(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "list_search_coupons_response_value_200_application_json_property_data_item.json"
            )
        )
        page = await async_sellapp_sdk.coupons.search()
        assert isinstance(page, AsyncPage)
        assert len(page.data) == 1
        assert isinstance(
            page.data[0], SearchCouponsResponseValue200ApplicationJsonPropertyDataItem
        )

    @pytest.mark.asyncio
    async def test_search_empty_page(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(json={"data": [], "list_metadata": {}})
        page = await async_sellapp_sdk.coupons.search()
        assert isinstance(page, AsyncPage)
        assert page.data == []

    @pytest.mark.asyncio
    async def test_search_encodes_query_params(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(json={"data": [], "list_metadata": {}})
        await async_sellapp_sdk.coupons.search(
            limit=10,
            before="cursor before",
            after="cursor/after",
            with_trashed=True,
            only_trashed=True,
        )
        request = httpx_mock.get_request()
        assert request.url.params["limit"] == "10"
        assert request.url.params["before"] == "cursor before"
        assert request.url.params["after"] == "cursor/after"
        assert request.url.params["with_trashed"] == "true"
        assert request.url.params["only_trashed"] == "true"

    @pytest.mark.asyncio
    async def test_batch_create(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_batch_create_coupons_response_value_200_application_json.json"
            )
        )
        result = await async_sellapp_sdk.coupons.batch_create(
            resources=[
                BatchCreateCouponsRequestApplicationJsonPropertyResourcesItem.from_dict(
                    load_fixture(
                        "batch_create_coupons_request_application_json_property_resources_item.json"
                    )
                )
            ]
        )
        assert isinstance(result, SdkBatchCreateCouponsResponseValue200ApplicationJson)
        request = httpx_mock.get_request()
        assert request.method == "POST"
        assert request.url.path.endswith("/v1/coupons/batch")

    @pytest.mark.asyncio
    async def test_batch_update(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_batch_update_coupons_response_value_200_application_json.json"
            )
        )
        result = await async_sellapp_sdk.coupons.batch_update(
            resources=BatchUpdateCouponsRequestApplicationJsonPropertyResources.from_dict(
                load_fixture(
                    "batch_update_coupons_request_application_json_property_resources.json"
                )
            )
        )
        assert isinstance(result, SdkBatchUpdateCouponsResponseValue200ApplicationJson)
        request = httpx_mock.get_request()
        assert request.method == "PATCH"
        assert request.url.path.endswith("/v1/coupons/batch")

    @pytest.mark.asyncio
    async def test_batch_update_encodes_query_params(
        self, async_sellapp_sdk, httpx_mock
    ):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_batch_update_coupons_response_value_200_application_json.json"
            )
        )
        await async_sellapp_sdk.coupons.batch_update(
            resources=BatchUpdateCouponsRequestApplicationJsonPropertyResources.from_dict(
                load_fixture(
                    "batch_update_coupons_request_application_json_property_resources.json"
                )
            ),
            with_trashed=True,
            only_trashed=True,
        )
        request = httpx_mock.get_request()
        assert request.url.params["with_trashed"] == "true"
        assert request.url.params["only_trashed"] == "true"

    @pytest.mark.asyncio
    async def test_batch_delete(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(status_code=200, content=b"")
        result = await async_sellapp_sdk.coupons.batch_delete(resources=[1])
        assert result is None
        request = httpx_mock.get_request()
        assert request.method == "DELETE"
        assert request.url.path.endswith("/v1/coupons/batch")

    @pytest.mark.asyncio
    async def test_batch_delete_encodes_query_params(
        self, async_sellapp_sdk, httpx_mock
    ):
        httpx_mock.add_response(status_code=200, content=b"")
        await async_sellapp_sdk.coupons.batch_delete(
            resources=[1], with_trashed=True, only_trashed=True
        )
        request = httpx_mock.get_request()
        assert request.url.params["with_trashed"] == "true"
        assert request.url.params["only_trashed"] == "true"

    @pytest.mark.asyncio
    async def test_v2_list_coupons(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "list_v_2_list_coupons_response_value_200_application_json_property_data_item.json"
            )
        )
        page = await async_sellapp_sdk.coupons.v2_list_coupons()
        assert isinstance(page, AsyncPage)
        assert len(page.data) == 1
        assert isinstance(
            page.data[0], V2ListCouponsResponseValue200ApplicationJsonPropertyDataItem
        )

    @pytest.mark.asyncio
    async def test_v2_list_coupons_empty_page(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(json={"data": [], "list_metadata": {}})
        page = await async_sellapp_sdk.coupons.v2_list_coupons()
        assert isinstance(page, AsyncPage)
        assert page.data == []

    @pytest.mark.asyncio
    async def test_v2_list_coupons_encodes_query_params(
        self, async_sellapp_sdk, httpx_mock
    ):
        httpx_mock.add_response(json={"data": [], "list_metadata": {}})
        await async_sellapp_sdk.coupons.v2_list_coupons(
            limit=10,
            before="cursor before",
            after="cursor/after",
            page=7,
            with_trashed=True,
            only_trashed=True,
        )
        request = httpx_mock.get_request()
        assert request.url.params["limit"] == "10"
        assert request.url.params["before"] == "cursor before"
        assert request.url.params["after"] == "cursor/after"
        assert request.url.params["page"] == "7"
        assert request.url.params["with_trashed"] == "true"
        assert request.url.params["only_trashed"] == "true"

    @pytest.mark.asyncio
    async def test_v2_create_coupon(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_v_2_create_coupon_response_value_201_application_json.json"
            )
        )
        result = await async_sellapp_sdk.coupons.v2_create_coupon(
            code="test_code",
            type=SdkV2CreateCouponRequestApplicationJsonType("PERCENTAGE"),
            discount=1,
            store_wide=True,
        )
        assert isinstance(result, SdkV2CreateCouponResponseValue201ApplicationJson)
        request = httpx_mock.get_request()
        assert request.method == "POST"
        assert request.url.path.endswith("/v2/coupons")

    @pytest.mark.asyncio
    async def test_v2_search_coupons(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "list_v_2_search_coupons_response_value_200_application_json_property_data_item.json"
            )
        )
        page = await async_sellapp_sdk.coupons.v2_search_coupons()
        assert isinstance(page, AsyncPage)
        assert len(page.data) == 1
        assert isinstance(
            page.data[0], V2SearchCouponsResponseValue200ApplicationJsonPropertyDataItem
        )

    @pytest.mark.asyncio
    async def test_v2_search_coupons_empty_page(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(json={"data": [], "list_metadata": {}})
        page = await async_sellapp_sdk.coupons.v2_search_coupons()
        assert isinstance(page, AsyncPage)
        assert page.data == []

    @pytest.mark.asyncio
    async def test_v2_search_coupons_encodes_query_params(
        self, async_sellapp_sdk, httpx_mock
    ):
        httpx_mock.add_response(json={"data": [], "list_metadata": {}})
        await async_sellapp_sdk.coupons.v2_search_coupons(
            limit=10,
            before="cursor before",
            after="cursor/after",
            with_trashed=True,
            only_trashed=True,
        )
        request = httpx_mock.get_request()
        assert request.url.params["limit"] == "10"
        assert request.url.params["before"] == "cursor before"
        assert request.url.params["after"] == "cursor/after"
        assert request.url.params["with_trashed"] == "true"
        assert request.url.params["only_trashed"] == "true"

    @pytest.mark.asyncio
    async def test_v2_get_coupon(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_v_2_get_coupon_response_value_200_application_json.json"
            )
        )
        result = await async_sellapp_sdk.coupons.v2_get_coupon("test_coupon")
        assert isinstance(result, SdkV2GetCouponResponseValue200ApplicationJson)
        request = httpx_mock.get_request()
        assert request.method == "GET"
        assert request.url.path.endswith("/v2/coupons/test_coupon")

    @pytest.mark.asyncio
    async def test_v2_get_coupon_encodes_query_params(
        self, async_sellapp_sdk, httpx_mock
    ):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_v_2_get_coupon_response_value_200_application_json.json"
            )
        )
        await async_sellapp_sdk.coupons.v2_get_coupon(
            "test_coupon", with_trashed=True, only_trashed=True
        )
        request = httpx_mock.get_request()
        assert request.url.params["with_trashed"] == "true"
        assert request.url.params["only_trashed"] == "true"

    @pytest.mark.asyncio
    async def test_v2_replace_coupon(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_v_2_replace_coupon_response_value_200_application_json.json"
            )
        )
        result = await async_sellapp_sdk.coupons.v2_replace_coupon("test_coupon")
        assert isinstance(result, SdkV2ReplaceCouponResponseValue200ApplicationJson)
        request = httpx_mock.get_request()
        assert request.method == "PUT"
        assert request.url.path.endswith("/v2/coupons/test_coupon")

    @pytest.mark.asyncio
    async def test_v2_replace_coupon_encodes_query_params(
        self, async_sellapp_sdk, httpx_mock
    ):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_v_2_replace_coupon_response_value_200_application_json.json"
            )
        )
        await async_sellapp_sdk.coupons.v2_replace_coupon(
            "test_coupon", with_trashed=True, only_trashed=True
        )
        request = httpx_mock.get_request()
        assert request.url.params["with_trashed"] == "true"
        assert request.url.params["only_trashed"] == "true"

    @pytest.mark.asyncio
    async def test_v2_update_coupon(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_v_2_update_coupon_response_value_200_application_json.json"
            )
        )
        result = await async_sellapp_sdk.coupons.v2_update_coupon("test_coupon")
        assert isinstance(result, SdkV2UpdateCouponResponseValue200ApplicationJson)
        request = httpx_mock.get_request()
        assert request.method == "PATCH"
        assert request.url.path.endswith("/v2/coupons/test_coupon")

    @pytest.mark.asyncio
    async def test_v2_update_coupon_encodes_query_params(
        self, async_sellapp_sdk, httpx_mock
    ):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_v_2_update_coupon_response_value_200_application_json.json"
            )
        )
        await async_sellapp_sdk.coupons.v2_update_coupon(
            "test_coupon", with_trashed=True, only_trashed=True
        )
        request = httpx_mock.get_request()
        assert request.url.params["with_trashed"] == "true"
        assert request.url.params["only_trashed"] == "true"

    @pytest.mark.asyncio
    async def test_v2_delete_coupon(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(status_code=200, content=b"")
        result = await async_sellapp_sdk.coupons.v2_delete_coupon("test_coupon")
        assert result is None
        request = httpx_mock.get_request()
        assert request.method == "DELETE"
        assert request.url.path.endswith("/v2/coupons/test_coupon")

    @pytest.mark.asyncio
    async def test_v2_delete_coupon_encodes_query_params(
        self, async_sellapp_sdk, httpx_mock
    ):
        httpx_mock.add_response(status_code=200, content=b"")
        await async_sellapp_sdk.coupons.v2_delete_coupon(
            "test_coupon", with_trashed=True, only_trashed=True
        )
        request = httpx_mock.get_request()
        assert request.url.params["with_trashed"] == "true"
        assert request.url.params["only_trashed"] == "true"

    @pytest.mark.asyncio
    async def test_v2_batch_create_coupons(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_v_2_batch_create_coupons_response_value_200_application_json.json"
            )
        )
        result = await async_sellapp_sdk.coupons.v2_batch_create_coupons(
            resources=[
                V2BatchCreateCouponsRequestApplicationJsonPropertyResourcesItem.from_dict(
                    load_fixture(
                        "v_2_batch_create_coupons_request_application_json_property_resources_item.json"
                    )
                )
            ]
        )
        assert isinstance(
            result, SdkV2BatchCreateCouponsResponseValue200ApplicationJson
        )
        request = httpx_mock.get_request()
        assert request.method == "POST"
        assert request.url.path.endswith("/v2/coupons/batch")

    @pytest.mark.asyncio
    async def test_v2_batch_update_coupons(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_v_2_batch_update_coupons_response_value_200_application_json.json"
            )
        )
        result = await async_sellapp_sdk.coupons.v2_batch_update_coupons(
            resources=V2BatchUpdateCouponsRequestApplicationJsonPropertyResources.from_dict(
                load_fixture(
                    "v_2_batch_update_coupons_request_application_json_property_resources.json"
                )
            )
        )
        assert isinstance(
            result, SdkV2BatchUpdateCouponsResponseValue200ApplicationJson
        )
        request = httpx_mock.get_request()
        assert request.method == "PATCH"
        assert request.url.path.endswith("/v2/coupons/batch")

    @pytest.mark.asyncio
    async def test_v2_batch_update_coupons_encodes_query_params(
        self, async_sellapp_sdk, httpx_mock
    ):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_v_2_batch_update_coupons_response_value_200_application_json.json"
            )
        )
        await async_sellapp_sdk.coupons.v2_batch_update_coupons(
            resources=V2BatchUpdateCouponsRequestApplicationJsonPropertyResources.from_dict(
                load_fixture(
                    "v_2_batch_update_coupons_request_application_json_property_resources.json"
                )
            ),
            with_trashed=True,
            only_trashed=True,
        )
        request = httpx_mock.get_request()
        assert request.url.params["with_trashed"] == "true"
        assert request.url.params["only_trashed"] == "true"

    @pytest.mark.asyncio
    async def test_v2_batch_delete_coupons(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(status_code=200, content=b"")
        result = await async_sellapp_sdk.coupons.v2_batch_delete_coupons(resources=[1])
        assert result is None
        request = httpx_mock.get_request()
        assert request.method == "DELETE"
        assert request.url.path.endswith("/v2/coupons/batch")

    @pytest.mark.asyncio
    async def test_v2_batch_delete_coupons_encodes_query_params(
        self, async_sellapp_sdk, httpx_mock
    ):
        httpx_mock.add_response(status_code=200, content=b"")
        await async_sellapp_sdk.coupons.v2_batch_delete_coupons(
            resources=[1], with_trashed=True, only_trashed=True
        )
        request = httpx_mock.get_request()
        assert request.url.params["with_trashed"] == "true"
        assert request.url.params["only_trashed"] == "true"

    @pytest.mark.asyncio
    async def test_list_with_request_options(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(json={"data": [], "list_metadata": {}})
        await async_sellapp_sdk.coupons.list(
            request_options={"headers": {"X-Custom": "value"}}
        )
        request = httpx_mock.get_request()
        assert request.headers["X-Custom"] == "value"

    @pytest.mark.asyncio
    async def test_list_unauthorized(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(status_code=401, json={"message": "Unauthorized"})
        with pytest.raises(AuthenticationError):
            await async_sellapp_sdk.coupons.list()

    @pytest.mark.asyncio
    async def test_list_not_found(self, httpx_mock):
        sellapp_sdk = AsyncSellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=404, json={"message": "Not found"})
            with pytest.raises(NotFoundError):
                await sellapp_sdk.coupons.list()
        finally:
            await sellapp_sdk.close()

    @pytest.mark.asyncio
    async def test_list_rate_limited(self, httpx_mock):
        sellapp_sdk = AsyncSellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(
                status_code=429,
                headers={"Retry-After": "0"},
                json={"message": "Slow down"},
            )
            with pytest.raises(RateLimitExceededError):
                await sellapp_sdk.coupons.list()
        finally:
            await sellapp_sdk.close()

    @pytest.mark.asyncio
    async def test_list_server_error(self, httpx_mock):
        sellapp_sdk = AsyncSellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=500, json={"message": "Server error"})
            with pytest.raises(ServerError):
                await sellapp_sdk.coupons.list()
        finally:
            await sellapp_sdk.close()

    @pytest.mark.asyncio
    async def test_list_bad_request(self, httpx_mock):
        sellapp_sdk = AsyncSellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=400, json={"message": "Bad request"})
            with pytest.raises(BadRequestError):
                await sellapp_sdk.coupons.list()
        finally:
            await sellapp_sdk.close()

    @pytest.mark.asyncio
    async def test_list_unprocessable(self, httpx_mock):
        sellapp_sdk = AsyncSellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=422, json={"message": "Unprocessable"})
            with pytest.raises(UnprocessableEntityError):
                await sellapp_sdk.coupons.list()
        finally:
            await sellapp_sdk.close()
