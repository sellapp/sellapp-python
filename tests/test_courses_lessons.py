# This file is auto-generated by oagen. Do not edit.

import json

import pytest

from sellapp_sdk import AsyncSellAppClient, SellAppClient
from sellapp_sdk._errors import (
    AuthenticationError,
    BadRequestError,
    NotFoundError,
    RateLimitExceededError,
    ServerError,
    UnprocessableEntityError,
)
from sellapp_sdk.common.models import (
    SdkCreateCourseLessonRequestApplicationJsonType,
)
from sellapp_sdk.courses.models import (
    ReorderCourseLessonsRequestApplicationJsonPropertyResourcesItem,
    SdkCreateCourseLessonResponseValue201ApplicationJson,
    SdkReorderCourseLessonsResponseValue200ApplicationJson,
    SdkReplaceCourseLessonResponseValue200ApplicationJson,
    SdkUpdateCourseLessonResponseValue200ApplicationJson,
)
from tests.generated_helpers import load_fixture


class TestCoursesLessons:
    def test_create(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_create_course_lesson_response_value_201_application_json.json"
            ),
        )
        result = sellapp_sdk.courses_lessons.create(
            "test_course",
            "test_section",
            title="test_title",
            type=SdkCreateCourseLessonRequestApplicationJsonType("lecture"),
        )
        assert isinstance(result, SdkCreateCourseLessonResponseValue201ApplicationJson)
        request = httpx_mock.get_request()
        assert request.method == "POST"
        assert request.url.path.endswith(
            "/v2/courses/test_course/sections/test_section/lessons"
        )
        body = json.loads(request.content)
        assert body["title"] == "test_title"
        assert body["type"] == SdkCreateCourseLessonRequestApplicationJsonType(
            "lecture"
        )

    def test_replace(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_replace_course_lesson_response_value_200_application_json.json"
            ),
        )
        result = sellapp_sdk.courses_lessons.replace("test_course", "test_lesson")
        assert isinstance(result, SdkReplaceCourseLessonResponseValue200ApplicationJson)
        request = httpx_mock.get_request()
        assert request.method == "PUT"
        assert request.url.path.endswith("/v2/courses/test_course/lessons/test_lesson")

    def test_update(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_update_course_lesson_response_value_200_application_json.json"
            ),
        )
        result = sellapp_sdk.courses_lessons.update("test_course", "test_lesson")
        assert isinstance(result, SdkUpdateCourseLessonResponseValue200ApplicationJson)
        request = httpx_mock.get_request()
        assert request.method == "PATCH"
        assert request.url.path.endswith("/v2/courses/test_course/lessons/test_lesson")

    def test_delete(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(status_code=200, content=b"")
        result = sellapp_sdk.courses_lessons.delete("test_course", "test_lesson")
        assert result is None
        request = httpx_mock.get_request()
        assert request.method == "DELETE"
        assert request.url.path.endswith("/v2/courses/test_course/lessons/test_lesson")

    def test_reorder(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_reorder_course_lessons_response_value_200_application_json.json"
            ),
        )
        result = sellapp_sdk.courses_lessons.reorder(
            "test_course",
            resources=[
                ReorderCourseLessonsRequestApplicationJsonPropertyResourcesItem.from_dict(
                    load_fixture(
                        "reorder_course_lessons_request_application_json_property_resources_item.json"
                    )
                )
            ],
        )
        assert isinstance(
            result, SdkReorderCourseLessonsResponseValue200ApplicationJson
        )
        request = httpx_mock.get_request()
        assert request.method == "PATCH"
        assert request.url.path.endswith("/v2/courses/test_course/lessons/reorder")
        body = json.loads(request.content)
        assert "resources" in body

    def test_create_with_request_options(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_create_course_lesson_response_value_201_application_json.json"
            )
        )
        sellapp_sdk.courses_lessons.create(
            "test_course",
            "test_section",
            title="test_title",
            type=SdkCreateCourseLessonRequestApplicationJsonType("lecture"),
            request_options={"headers": {"X-Custom": "value"}},
        )
        request = httpx_mock.get_request()
        assert request.headers["X-Custom"] == "value"

    def test_create_unauthorized(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            status_code=401,
            json={"message": "Unauthorized"},
        )
        with pytest.raises(AuthenticationError):
            sellapp_sdk.courses_lessons.create(
                "test_course",
                "test_section",
                title="test_title",
                type=SdkCreateCourseLessonRequestApplicationJsonType("lecture"),
            )

    def test_create_not_found(self, httpx_mock):
        sellapp_sdk = SellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=404, json={"message": "Not found"})
            with pytest.raises(NotFoundError):
                sellapp_sdk.courses_lessons.create(
                    "test_course",
                    "test_section",
                    title="test_title",
                    type=SdkCreateCourseLessonRequestApplicationJsonType("lecture"),
                )
        finally:
            sellapp_sdk.close()

    def test_create_rate_limited(self, httpx_mock):
        sellapp_sdk = SellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(
                status_code=429,
                headers={"Retry-After": "0"},
                json={"message": "Slow down"},
            )
            with pytest.raises(RateLimitExceededError):
                sellapp_sdk.courses_lessons.create(
                    "test_course",
                    "test_section",
                    title="test_title",
                    type=SdkCreateCourseLessonRequestApplicationJsonType("lecture"),
                )
        finally:
            sellapp_sdk.close()

    def test_create_server_error(self, httpx_mock):
        sellapp_sdk = SellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=500, json={"message": "Server error"})
            with pytest.raises(ServerError):
                sellapp_sdk.courses_lessons.create(
                    "test_course",
                    "test_section",
                    title="test_title",
                    type=SdkCreateCourseLessonRequestApplicationJsonType("lecture"),
                )
        finally:
            sellapp_sdk.close()

    def test_create_bad_request(self, httpx_mock):
        sellapp_sdk = SellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=400, json={"message": "Bad request"})
            with pytest.raises(BadRequestError):
                sellapp_sdk.courses_lessons.create(
                    "test_course",
                    "test_section",
                    title="test_title",
                    type=SdkCreateCourseLessonRequestApplicationJsonType("lecture"),
                )
        finally:
            sellapp_sdk.close()

    def test_create_unprocessable(self, httpx_mock):
        sellapp_sdk = SellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=422, json={"message": "Unprocessable"})
            with pytest.raises(UnprocessableEntityError):
                sellapp_sdk.courses_lessons.create(
                    "test_course",
                    "test_section",
                    title="test_title",
                    type=SdkCreateCourseLessonRequestApplicationJsonType("lecture"),
                )
        finally:
            sellapp_sdk.close()


class TestAsyncCoursesLessons:
    @pytest.mark.asyncio
    async def test_create(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_create_course_lesson_response_value_201_application_json.json"
            )
        )
        result = await async_sellapp_sdk.courses_lessons.create(
            "test_course",
            "test_section",
            title="test_title",
            type=SdkCreateCourseLessonRequestApplicationJsonType("lecture"),
        )
        assert isinstance(result, SdkCreateCourseLessonResponseValue201ApplicationJson)
        request = httpx_mock.get_request()
        assert request.method == "POST"
        assert request.url.path.endswith(
            "/v2/courses/test_course/sections/test_section/lessons"
        )

    @pytest.mark.asyncio
    async def test_replace(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_replace_course_lesson_response_value_200_application_json.json"
            )
        )
        result = await async_sellapp_sdk.courses_lessons.replace(
            "test_course", "test_lesson"
        )
        assert isinstance(result, SdkReplaceCourseLessonResponseValue200ApplicationJson)
        request = httpx_mock.get_request()
        assert request.method == "PUT"
        assert request.url.path.endswith("/v2/courses/test_course/lessons/test_lesson")

    @pytest.mark.asyncio
    async def test_update(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_update_course_lesson_response_value_200_application_json.json"
            )
        )
        result = await async_sellapp_sdk.courses_lessons.update(
            "test_course", "test_lesson"
        )
        assert isinstance(result, SdkUpdateCourseLessonResponseValue200ApplicationJson)
        request = httpx_mock.get_request()
        assert request.method == "PATCH"
        assert request.url.path.endswith("/v2/courses/test_course/lessons/test_lesson")

    @pytest.mark.asyncio
    async def test_delete(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(status_code=200, content=b"")
        result = await async_sellapp_sdk.courses_lessons.delete(
            "test_course", "test_lesson"
        )
        assert result is None
        request = httpx_mock.get_request()
        assert request.method == "DELETE"
        assert request.url.path.endswith("/v2/courses/test_course/lessons/test_lesson")

    @pytest.mark.asyncio
    async def test_reorder(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_reorder_course_lessons_response_value_200_application_json.json"
            )
        )
        result = await async_sellapp_sdk.courses_lessons.reorder(
            "test_course",
            resources=[
                ReorderCourseLessonsRequestApplicationJsonPropertyResourcesItem.from_dict(
                    load_fixture(
                        "reorder_course_lessons_request_application_json_property_resources_item.json"
                    )
                )
            ],
        )
        assert isinstance(
            result, SdkReorderCourseLessonsResponseValue200ApplicationJson
        )
        request = httpx_mock.get_request()
        assert request.method == "PATCH"
        assert request.url.path.endswith("/v2/courses/test_course/lessons/reorder")

    @pytest.mark.asyncio
    async def test_create_with_request_options(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_create_course_lesson_response_value_201_application_json.json"
            )
        )
        await async_sellapp_sdk.courses_lessons.create(
            "test_course",
            "test_section",
            title="test_title",
            type=SdkCreateCourseLessonRequestApplicationJsonType("lecture"),
            request_options={"headers": {"X-Custom": "value"}},
        )
        request = httpx_mock.get_request()
        assert request.headers["X-Custom"] == "value"

    @pytest.mark.asyncio
    async def test_create_unauthorized(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(status_code=401, json={"message": "Unauthorized"})
        with pytest.raises(AuthenticationError):
            await async_sellapp_sdk.courses_lessons.create(
                "test_course",
                "test_section",
                title="test_title",
                type=SdkCreateCourseLessonRequestApplicationJsonType("lecture"),
            )

    @pytest.mark.asyncio
    async def test_create_not_found(self, httpx_mock):
        sellapp_sdk = AsyncSellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=404, json={"message": "Not found"})
            with pytest.raises(NotFoundError):
                await sellapp_sdk.courses_lessons.create(
                    "test_course",
                    "test_section",
                    title="test_title",
                    type=SdkCreateCourseLessonRequestApplicationJsonType("lecture"),
                )
        finally:
            await sellapp_sdk.close()

    @pytest.mark.asyncio
    async def test_create_rate_limited(self, httpx_mock):
        sellapp_sdk = AsyncSellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(
                status_code=429,
                headers={"Retry-After": "0"},
                json={"message": "Slow down"},
            )
            with pytest.raises(RateLimitExceededError):
                await sellapp_sdk.courses_lessons.create(
                    "test_course",
                    "test_section",
                    title="test_title",
                    type=SdkCreateCourseLessonRequestApplicationJsonType("lecture"),
                )
        finally:
            await sellapp_sdk.close()

    @pytest.mark.asyncio
    async def test_create_server_error(self, httpx_mock):
        sellapp_sdk = AsyncSellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=500, json={"message": "Server error"})
            with pytest.raises(ServerError):
                await sellapp_sdk.courses_lessons.create(
                    "test_course",
                    "test_section",
                    title="test_title",
                    type=SdkCreateCourseLessonRequestApplicationJsonType("lecture"),
                )
        finally:
            await sellapp_sdk.close()

    @pytest.mark.asyncio
    async def test_create_bad_request(self, httpx_mock):
        sellapp_sdk = AsyncSellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=400, json={"message": "Bad request"})
            with pytest.raises(BadRequestError):
                await sellapp_sdk.courses_lessons.create(
                    "test_course",
                    "test_section",
                    title="test_title",
                    type=SdkCreateCourseLessonRequestApplicationJsonType("lecture"),
                )
        finally:
            await sellapp_sdk.close()

    @pytest.mark.asyncio
    async def test_create_unprocessable(self, httpx_mock):
        sellapp_sdk = AsyncSellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=422, json={"message": "Unprocessable"})
            with pytest.raises(UnprocessableEntityError):
                await sellapp_sdk.courses_lessons.create(
                    "test_course",
                    "test_section",
                    title="test_title",
                    type=SdkCreateCourseLessonRequestApplicationJsonType("lecture"),
                )
        finally:
            await sellapp_sdk.close()
