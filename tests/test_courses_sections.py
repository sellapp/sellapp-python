# This file is auto-generated by oagen. Do not edit.

import json

import pytest

from sellapp_sdk import AsyncSellAppClient, SellAppClient
from sellapp_sdk._errors import (
    AuthenticationError,
    BadRequestError,
    NotFoundError,
    RateLimitExceededError,
    ServerError,
    UnprocessableEntityError,
)
from sellapp_sdk.courses.models import (
    SdkCreateCourseSectionResponseValue201ApplicationJson,
    SdkReorderCourseSectionsResponseValue200ApplicationJson,
    SdkReplaceCourseSectionResponseValue200ApplicationJson,
    SdkUpdateCourseSectionResponseValue200ApplicationJson,
)
from tests.generated_helpers import load_fixture


class TestCoursesSections:
    def test_create(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_create_course_section_response_value_201_application_json.json"
            ),
        )
        result = sellapp_sdk.courses_sections.create("test_course", title="test_title")
        assert isinstance(result, SdkCreateCourseSectionResponseValue201ApplicationJson)
        request = httpx_mock.get_request()
        assert request.method == "POST"
        assert request.url.path.endswith("/v2/courses/test_course/sections")
        body = json.loads(request.content)
        assert body["title"] == "test_title"

    def test_replace(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_replace_course_section_response_value_200_application_json.json"
            ),
        )
        result = sellapp_sdk.courses_sections.replace("test_course", "test_section")
        assert isinstance(
            result, SdkReplaceCourseSectionResponseValue200ApplicationJson
        )
        request = httpx_mock.get_request()
        assert request.method == "PUT"
        assert request.url.path.endswith(
            "/v2/courses/test_course/sections/test_section"
        )

    def test_update(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_update_course_section_response_value_200_application_json.json"
            ),
        )
        result = sellapp_sdk.courses_sections.update("test_course", "test_section")
        assert isinstance(result, SdkUpdateCourseSectionResponseValue200ApplicationJson)
        request = httpx_mock.get_request()
        assert request.method == "PATCH"
        assert request.url.path.endswith(
            "/v2/courses/test_course/sections/test_section"
        )

    def test_delete(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(status_code=200, content=b"")
        result = sellapp_sdk.courses_sections.delete("test_course", "test_section")
        assert result is None
        request = httpx_mock.get_request()
        assert request.method == "DELETE"
        assert request.url.path.endswith(
            "/v2/courses/test_course/sections/test_section"
        )

    def test_reorder(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_reorder_course_sections_response_value_200_application_json.json"
            ),
        )
        result = sellapp_sdk.courses_sections.reorder("test_course", resources=[1])
        assert isinstance(
            result, SdkReorderCourseSectionsResponseValue200ApplicationJson
        )
        request = httpx_mock.get_request()
        assert request.method == "PATCH"
        assert request.url.path.endswith("/v2/courses/test_course/sections/reorder")
        body = json.loads(request.content)
        assert "resources" in body

    def test_create_with_request_options(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_create_course_section_response_value_201_application_json.json"
            )
        )
        sellapp_sdk.courses_sections.create(
            "test_course",
            title="test_title",
            request_options={"headers": {"X-Custom": "value"}},
        )
        request = httpx_mock.get_request()
        assert request.headers["X-Custom"] == "value"

    def test_create_unauthorized(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            status_code=401,
            json={"message": "Unauthorized"},
        )
        with pytest.raises(AuthenticationError):
            sellapp_sdk.courses_sections.create("test_course", title="test_title")

    def test_create_not_found(self, httpx_mock):
        sellapp_sdk = SellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=404, json={"message": "Not found"})
            with pytest.raises(NotFoundError):
                sellapp_sdk.courses_sections.create("test_course", title="test_title")
        finally:
            sellapp_sdk.close()

    def test_create_rate_limited(self, httpx_mock):
        sellapp_sdk = SellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(
                status_code=429,
                headers={"Retry-After": "0"},
                json={"message": "Slow down"},
            )
            with pytest.raises(RateLimitExceededError):
                sellapp_sdk.courses_sections.create("test_course", title="test_title")
        finally:
            sellapp_sdk.close()

    def test_create_server_error(self, httpx_mock):
        sellapp_sdk = SellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=500, json={"message": "Server error"})
            with pytest.raises(ServerError):
                sellapp_sdk.courses_sections.create("test_course", title="test_title")
        finally:
            sellapp_sdk.close()

    def test_create_bad_request(self, httpx_mock):
        sellapp_sdk = SellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=400, json={"message": "Bad request"})
            with pytest.raises(BadRequestError):
                sellapp_sdk.courses_sections.create("test_course", title="test_title")
        finally:
            sellapp_sdk.close()

    def test_create_unprocessable(self, httpx_mock):
        sellapp_sdk = SellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=422, json={"message": "Unprocessable"})
            with pytest.raises(UnprocessableEntityError):
                sellapp_sdk.courses_sections.create("test_course", title="test_title")
        finally:
            sellapp_sdk.close()


class TestAsyncCoursesSections:
    @pytest.mark.asyncio
    async def test_create(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_create_course_section_response_value_201_application_json.json"
            )
        )
        result = await async_sellapp_sdk.courses_sections.create(
            "test_course", title="test_title"
        )
        assert isinstance(result, SdkCreateCourseSectionResponseValue201ApplicationJson)
        request = httpx_mock.get_request()
        assert request.method == "POST"
        assert request.url.path.endswith("/v2/courses/test_course/sections")

    @pytest.mark.asyncio
    async def test_replace(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_replace_course_section_response_value_200_application_json.json"
            )
        )
        result = await async_sellapp_sdk.courses_sections.replace(
            "test_course", "test_section"
        )
        assert isinstance(
            result, SdkReplaceCourseSectionResponseValue200ApplicationJson
        )
        request = httpx_mock.get_request()
        assert request.method == "PUT"
        assert request.url.path.endswith(
            "/v2/courses/test_course/sections/test_section"
        )

    @pytest.mark.asyncio
    async def test_update(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_update_course_section_response_value_200_application_json.json"
            )
        )
        result = await async_sellapp_sdk.courses_sections.update(
            "test_course", "test_section"
        )
        assert isinstance(result, SdkUpdateCourseSectionResponseValue200ApplicationJson)
        request = httpx_mock.get_request()
        assert request.method == "PATCH"
        assert request.url.path.endswith(
            "/v2/courses/test_course/sections/test_section"
        )

    @pytest.mark.asyncio
    async def test_delete(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(status_code=200, content=b"")
        result = await async_sellapp_sdk.courses_sections.delete(
            "test_course", "test_section"
        )
        assert result is None
        request = httpx_mock.get_request()
        assert request.method == "DELETE"
        assert request.url.path.endswith(
            "/v2/courses/test_course/sections/test_section"
        )

    @pytest.mark.asyncio
    async def test_reorder(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_reorder_course_sections_response_value_200_application_json.json"
            )
        )
        result = await async_sellapp_sdk.courses_sections.reorder(
            "test_course", resources=[1]
        )
        assert isinstance(
            result, SdkReorderCourseSectionsResponseValue200ApplicationJson
        )
        request = httpx_mock.get_request()
        assert request.method == "PATCH"
        assert request.url.path.endswith("/v2/courses/test_course/sections/reorder")

    @pytest.mark.asyncio
    async def test_create_with_request_options(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_create_course_section_response_value_201_application_json.json"
            )
        )
        await async_sellapp_sdk.courses_sections.create(
            "test_course",
            title="test_title",
            request_options={"headers": {"X-Custom": "value"}},
        )
        request = httpx_mock.get_request()
        assert request.headers["X-Custom"] == "value"

    @pytest.mark.asyncio
    async def test_create_unauthorized(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(status_code=401, json={"message": "Unauthorized"})
        with pytest.raises(AuthenticationError):
            await async_sellapp_sdk.courses_sections.create(
                "test_course", title="test_title"
            )

    @pytest.mark.asyncio
    async def test_create_not_found(self, httpx_mock):
        sellapp_sdk = AsyncSellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=404, json={"message": "Not found"})
            with pytest.raises(NotFoundError):
                await sellapp_sdk.courses_sections.create(
                    "test_course", title="test_title"
                )
        finally:
            await sellapp_sdk.close()

    @pytest.mark.asyncio
    async def test_create_rate_limited(self, httpx_mock):
        sellapp_sdk = AsyncSellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(
                status_code=429,
                headers={"Retry-After": "0"},
                json={"message": "Slow down"},
            )
            with pytest.raises(RateLimitExceededError):
                await sellapp_sdk.courses_sections.create(
                    "test_course", title="test_title"
                )
        finally:
            await sellapp_sdk.close()

    @pytest.mark.asyncio
    async def test_create_server_error(self, httpx_mock):
        sellapp_sdk = AsyncSellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=500, json={"message": "Server error"})
            with pytest.raises(ServerError):
                await sellapp_sdk.courses_sections.create(
                    "test_course", title="test_title"
                )
        finally:
            await sellapp_sdk.close()

    @pytest.mark.asyncio
    async def test_create_bad_request(self, httpx_mock):
        sellapp_sdk = AsyncSellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=400, json={"message": "Bad request"})
            with pytest.raises(BadRequestError):
                await sellapp_sdk.courses_sections.create(
                    "test_course", title="test_title"
                )
        finally:
            await sellapp_sdk.close()

    @pytest.mark.asyncio
    async def test_create_unprocessable(self, httpx_mock):
        sellapp_sdk = AsyncSellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=422, json={"message": "Unprocessable"})
            with pytest.raises(UnprocessableEntityError):
                await sellapp_sdk.courses_sections.create(
                    "test_course", title="test_title"
                )
        finally:
            await sellapp_sdk.close()
