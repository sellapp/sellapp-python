# This file is auto-generated by oagen. Do not edit.

import json

import pytest

from sellapp_sdk import AsyncSellAppClient, SellAppClient
from sellapp_sdk._errors import (
    AuthenticationError,
    BadRequestError,
    NotFoundError,
    RateLimitExceededError,
    ServerError,
    UnprocessableEntityError,
)
from sellapp_sdk.common.models import (
    SdkRecordCreditTransactionRequestApplicationJsonKind,
)
from sellapp_sdk.credits_products.models import (
    SdkRecordCreditTransactionResponseValue200ApplicationJson,
)
from tests.generated_helpers import load_fixture


class TestCredits:
    def test_record(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_record_credit_transaction_response_value_200_application_json.json"
            ),
        )
        result = sellapp_sdk.credits.record(
            customer_id=1,
            product_id=1,
            kind=SdkRecordCreditTransactionRequestApplicationJsonKind("grant"),
            amount_units=1,
            idempotency_key="test_idempotency_key",
        )
        assert isinstance(
            result, SdkRecordCreditTransactionResponseValue200ApplicationJson
        )
        request = httpx_mock.get_request()
        assert request.method == "POST"
        assert request.url.path.endswith("/v2/credit-transactions")
        body = json.loads(request.content)
        assert body["customer_id"] == 1
        assert body["product_id"] == 1
        assert body["kind"] == SdkRecordCreditTransactionRequestApplicationJsonKind(
            "grant"
        )
        assert body["amount_units"] == 1
        assert body["idempotency_key"] == "test_idempotency_key"

    def test_record_with_request_options(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_record_credit_transaction_response_value_200_application_json.json"
            )
        )
        sellapp_sdk.credits.record(
            customer_id=1,
            product_id=1,
            kind=SdkRecordCreditTransactionRequestApplicationJsonKind("grant"),
            amount_units=1,
            idempotency_key="test_idempotency_key",
            request_options={"headers": {"X-Custom": "value"}},
        )
        request = httpx_mock.get_request()
        assert request.headers["X-Custom"] == "value"

    def test_record_unauthorized(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            status_code=401,
            json={"message": "Unauthorized"},
        )
        with pytest.raises(AuthenticationError):
            sellapp_sdk.credits.record(
                customer_id=1,
                product_id=1,
                kind=SdkRecordCreditTransactionRequestApplicationJsonKind("grant"),
                amount_units=1,
                idempotency_key="test_idempotency_key",
            )

    def test_record_not_found(self, httpx_mock):
        sellapp_sdk = SellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=404, json={"message": "Not found"})
            with pytest.raises(NotFoundError):
                sellapp_sdk.credits.record(
                    customer_id=1,
                    product_id=1,
                    kind=SdkRecordCreditTransactionRequestApplicationJsonKind("grant"),
                    amount_units=1,
                    idempotency_key="test_idempotency_key",
                )
        finally:
            sellapp_sdk.close()

    def test_record_rate_limited(self, httpx_mock):
        sellapp_sdk = SellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(
                status_code=429,
                headers={"Retry-After": "0"},
                json={"message": "Slow down"},
            )
            with pytest.raises(RateLimitExceededError):
                sellapp_sdk.credits.record(
                    customer_id=1,
                    product_id=1,
                    kind=SdkRecordCreditTransactionRequestApplicationJsonKind("grant"),
                    amount_units=1,
                    idempotency_key="test_idempotency_key",
                )
        finally:
            sellapp_sdk.close()

    def test_record_server_error(self, httpx_mock):
        sellapp_sdk = SellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=500, json={"message": "Server error"})
            with pytest.raises(ServerError):
                sellapp_sdk.credits.record(
                    customer_id=1,
                    product_id=1,
                    kind=SdkRecordCreditTransactionRequestApplicationJsonKind("grant"),
                    amount_units=1,
                    idempotency_key="test_idempotency_key",
                )
        finally:
            sellapp_sdk.close()

    def test_record_bad_request(self, httpx_mock):
        sellapp_sdk = SellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=400, json={"message": "Bad request"})
            with pytest.raises(BadRequestError):
                sellapp_sdk.credits.record(
                    customer_id=1,
                    product_id=1,
                    kind=SdkRecordCreditTransactionRequestApplicationJsonKind("grant"),
                    amount_units=1,
                    idempotency_key="test_idempotency_key",
                )
        finally:
            sellapp_sdk.close()

    def test_record_unprocessable(self, httpx_mock):
        sellapp_sdk = SellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=422, json={"message": "Unprocessable"})
            with pytest.raises(UnprocessableEntityError):
                sellapp_sdk.credits.record(
                    customer_id=1,
                    product_id=1,
                    kind=SdkRecordCreditTransactionRequestApplicationJsonKind("grant"),
                    amount_units=1,
                    idempotency_key="test_idempotency_key",
                )
        finally:
            sellapp_sdk.close()


class TestAsyncCredits:
    @pytest.mark.asyncio
    async def test_record(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_record_credit_transaction_response_value_200_application_json.json"
            )
        )
        result = await async_sellapp_sdk.credits.record(
            customer_id=1,
            product_id=1,
            kind=SdkRecordCreditTransactionRequestApplicationJsonKind("grant"),
            amount_units=1,
            idempotency_key="test_idempotency_key",
        )
        assert isinstance(
            result, SdkRecordCreditTransactionResponseValue200ApplicationJson
        )
        request = httpx_mock.get_request()
        assert request.method == "POST"
        assert request.url.path.endswith("/v2/credit-transactions")

    @pytest.mark.asyncio
    async def test_record_with_request_options(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_record_credit_transaction_response_value_200_application_json.json"
            )
        )
        await async_sellapp_sdk.credits.record(
            customer_id=1,
            product_id=1,
            kind=SdkRecordCreditTransactionRequestApplicationJsonKind("grant"),
            amount_units=1,
            idempotency_key="test_idempotency_key",
            request_options={"headers": {"X-Custom": "value"}},
        )
        request = httpx_mock.get_request()
        assert request.headers["X-Custom"] == "value"

    @pytest.mark.asyncio
    async def test_record_unauthorized(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(status_code=401, json={"message": "Unauthorized"})
        with pytest.raises(AuthenticationError):
            await async_sellapp_sdk.credits.record(
                customer_id=1,
                product_id=1,
                kind=SdkRecordCreditTransactionRequestApplicationJsonKind("grant"),
                amount_units=1,
                idempotency_key="test_idempotency_key",
            )

    @pytest.mark.asyncio
    async def test_record_not_found(self, httpx_mock):
        sellapp_sdk = AsyncSellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=404, json={"message": "Not found"})
            with pytest.raises(NotFoundError):
                await sellapp_sdk.credits.record(
                    customer_id=1,
                    product_id=1,
                    kind=SdkRecordCreditTransactionRequestApplicationJsonKind("grant"),
                    amount_units=1,
                    idempotency_key="test_idempotency_key",
                )
        finally:
            await sellapp_sdk.close()

    @pytest.mark.asyncio
    async def test_record_rate_limited(self, httpx_mock):
        sellapp_sdk = AsyncSellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(
                status_code=429,
                headers={"Retry-After": "0"},
                json={"message": "Slow down"},
            )
            with pytest.raises(RateLimitExceededError):
                await sellapp_sdk.credits.record(
                    customer_id=1,
                    product_id=1,
                    kind=SdkRecordCreditTransactionRequestApplicationJsonKind("grant"),
                    amount_units=1,
                    idempotency_key="test_idempotency_key",
                )
        finally:
            await sellapp_sdk.close()

    @pytest.mark.asyncio
    async def test_record_server_error(self, httpx_mock):
        sellapp_sdk = AsyncSellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=500, json={"message": "Server error"})
            with pytest.raises(ServerError):
                await sellapp_sdk.credits.record(
                    customer_id=1,
                    product_id=1,
                    kind=SdkRecordCreditTransactionRequestApplicationJsonKind("grant"),
                    amount_units=1,
                    idempotency_key="test_idempotency_key",
                )
        finally:
            await sellapp_sdk.close()

    @pytest.mark.asyncio
    async def test_record_bad_request(self, httpx_mock):
        sellapp_sdk = AsyncSellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=400, json={"message": "Bad request"})
            with pytest.raises(BadRequestError):
                await sellapp_sdk.credits.record(
                    customer_id=1,
                    product_id=1,
                    kind=SdkRecordCreditTransactionRequestApplicationJsonKind("grant"),
                    amount_units=1,
                    idempotency_key="test_idempotency_key",
                )
        finally:
            await sellapp_sdk.close()

    @pytest.mark.asyncio
    async def test_record_unprocessable(self, httpx_mock):
        sellapp_sdk = AsyncSellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=422, json={"message": "Unprocessable"})
            with pytest.raises(UnprocessableEntityError):
                await sellapp_sdk.credits.record(
                    customer_id=1,
                    product_id=1,
                    kind=SdkRecordCreditTransactionRequestApplicationJsonKind("grant"),
                    amount_units=1,
                    idempotency_key="test_idempotency_key",
                )
        finally:
            await sellapp_sdk.close()
