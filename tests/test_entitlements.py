# This file is auto-generated by oagen. Do not edit.


import pytest

from sellapp_sdk import AsyncSellAppClient, SellAppClient
from sellapp_sdk._errors import (
    AuthenticationError,
    BadRequestError,
    NotFoundError,
    RateLimitExceededError,
    ServerError,
    UnprocessableEntityError,
)
from sellapp_sdk.common.models import (
    SdkListCustomerEntitlementsResponseValue200ApplicationJson,
)
from tests.generated_helpers import load_fixture


class TestEntitlements:
    def test_list_customer_entitlements(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_list_customer_entitlements_response_value_200_application_json.json"
            ),
        )
        result = sellapp_sdk.entitlements.list_customer_entitlements("test_customer")
        assert isinstance(
            result, SdkListCustomerEntitlementsResponseValue200ApplicationJson
        )
        request = httpx_mock.get_request()
        assert request.method == "GET"
        assert request.url.path.endswith("/v2/customers/test_customer/entitlements")

    def test_list_customer_entitlements_by_external_id(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_list_customer_entitlements_response_value_200_application_json.json"
            ),
        )
        result = sellapp_sdk.entitlements.list_customer_entitlements_by_external_id(
            "test_externalId"
        )
        assert isinstance(
            result, SdkListCustomerEntitlementsResponseValue200ApplicationJson
        )
        request = httpx_mock.get_request()
        assert request.method == "GET"
        assert request.url.path.endswith(
            "/v2/customers/external/test_externalId/entitlements"
        )

    def test_list_customer_entitlements_with_request_options(
        self, sellapp_sdk, httpx_mock
    ):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_list_customer_entitlements_response_value_200_application_json.json"
            )
        )
        sellapp_sdk.entitlements.list_customer_entitlements(
            "test_customer", request_options={"headers": {"X-Custom": "value"}}
        )
        request = httpx_mock.get_request()
        assert request.headers["X-Custom"] == "value"

    def test_list_customer_entitlements_unauthorized(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            status_code=401,
            json={"message": "Unauthorized"},
        )
        with pytest.raises(AuthenticationError):
            sellapp_sdk.entitlements.list_customer_entitlements("test_customer")

    def test_list_customer_entitlements_not_found(self, httpx_mock):
        sellapp_sdk = SellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=404, json={"message": "Not found"})
            with pytest.raises(NotFoundError):
                sellapp_sdk.entitlements.list_customer_entitlements("test_customer")
        finally:
            sellapp_sdk.close()

    def test_list_customer_entitlements_rate_limited(self, httpx_mock):
        sellapp_sdk = SellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(
                status_code=429,
                headers={"Retry-After": "0"},
                json={"message": "Slow down"},
            )
            with pytest.raises(RateLimitExceededError):
                sellapp_sdk.entitlements.list_customer_entitlements("test_customer")
        finally:
            sellapp_sdk.close()

    def test_list_customer_entitlements_server_error(self, httpx_mock):
        sellapp_sdk = SellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=500, json={"message": "Server error"})
            with pytest.raises(ServerError):
                sellapp_sdk.entitlements.list_customer_entitlements("test_customer")
        finally:
            sellapp_sdk.close()

    def test_list_customer_entitlements_bad_request(self, httpx_mock):
        sellapp_sdk = SellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=400, json={"message": "Bad request"})
            with pytest.raises(BadRequestError):
                sellapp_sdk.entitlements.list_customer_entitlements("test_customer")
        finally:
            sellapp_sdk.close()

    def test_list_customer_entitlements_unprocessable(self, httpx_mock):
        sellapp_sdk = SellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=422, json={"message": "Unprocessable"})
            with pytest.raises(UnprocessableEntityError):
                sellapp_sdk.entitlements.list_customer_entitlements("test_customer")
        finally:
            sellapp_sdk.close()


class TestAsyncEntitlements:
    @pytest.mark.asyncio
    async def test_list_customer_entitlements(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_list_customer_entitlements_response_value_200_application_json.json"
            )
        )
        result = await async_sellapp_sdk.entitlements.list_customer_entitlements(
            "test_customer"
        )
        assert isinstance(
            result, SdkListCustomerEntitlementsResponseValue200ApplicationJson
        )
        request = httpx_mock.get_request()
        assert request.method == "GET"
        assert request.url.path.endswith("/v2/customers/test_customer/entitlements")

    @pytest.mark.asyncio
    async def test_list_customer_entitlements_by_external_id(
        self, async_sellapp_sdk, httpx_mock
    ):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_list_customer_entitlements_response_value_200_application_json.json"
            )
        )
        result = await async_sellapp_sdk.entitlements.list_customer_entitlements_by_external_id(
            "test_externalId"
        )
        assert isinstance(
            result, SdkListCustomerEntitlementsResponseValue200ApplicationJson
        )
        request = httpx_mock.get_request()
        assert request.method == "GET"
        assert request.url.path.endswith(
            "/v2/customers/external/test_externalId/entitlements"
        )

    @pytest.mark.asyncio
    async def test_list_customer_entitlements_with_request_options(
        self, async_sellapp_sdk, httpx_mock
    ):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_list_customer_entitlements_response_value_200_application_json.json"
            )
        )
        await async_sellapp_sdk.entitlements.list_customer_entitlements(
            "test_customer", request_options={"headers": {"X-Custom": "value"}}
        )
        request = httpx_mock.get_request()
        assert request.headers["X-Custom"] == "value"

    @pytest.mark.asyncio
    async def test_list_customer_entitlements_unauthorized(
        self, async_sellapp_sdk, httpx_mock
    ):
        httpx_mock.add_response(status_code=401, json={"message": "Unauthorized"})
        with pytest.raises(AuthenticationError):
            await async_sellapp_sdk.entitlements.list_customer_entitlements(
                "test_customer"
            )

    @pytest.mark.asyncio
    async def test_list_customer_entitlements_not_found(self, httpx_mock):
        sellapp_sdk = AsyncSellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=404, json={"message": "Not found"})
            with pytest.raises(NotFoundError):
                await sellapp_sdk.entitlements.list_customer_entitlements(
                    "test_customer"
                )
        finally:
            await sellapp_sdk.close()

    @pytest.mark.asyncio
    async def test_list_customer_entitlements_rate_limited(self, httpx_mock):
        sellapp_sdk = AsyncSellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(
                status_code=429,
                headers={"Retry-After": "0"},
                json={"message": "Slow down"},
            )
            with pytest.raises(RateLimitExceededError):
                await sellapp_sdk.entitlements.list_customer_entitlements(
                    "test_customer"
                )
        finally:
            await sellapp_sdk.close()

    @pytest.mark.asyncio
    async def test_list_customer_entitlements_server_error(self, httpx_mock):
        sellapp_sdk = AsyncSellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=500, json={"message": "Server error"})
            with pytest.raises(ServerError):
                await sellapp_sdk.entitlements.list_customer_entitlements(
                    "test_customer"
                )
        finally:
            await sellapp_sdk.close()

    @pytest.mark.asyncio
    async def test_list_customer_entitlements_bad_request(self, httpx_mock):
        sellapp_sdk = AsyncSellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=400, json={"message": "Bad request"})
            with pytest.raises(BadRequestError):
                await sellapp_sdk.entitlements.list_customer_entitlements(
                    "test_customer"
                )
        finally:
            await sellapp_sdk.close()

    @pytest.mark.asyncio
    async def test_list_customer_entitlements_unprocessable(self, httpx_mock):
        sellapp_sdk = AsyncSellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=422, json={"message": "Unprocessable"})
            with pytest.raises(UnprocessableEntityError):
                await sellapp_sdk.entitlements.list_customer_entitlements(
                    "test_customer"
                )
        finally:
            await sellapp_sdk.close()
