# This file is auto-generated by oagen. Do not edit.


import pytest

from sellapp_sdk import AsyncSellAppClient, SellAppClient
from sellapp_sdk._errors import (
    AuthenticationError,
    BadRequestError,
    NotFoundError,
    RateLimitExceededError,
    ServerError,
    UnprocessableEntityError,
)
from sellapp_sdk._pagination import AsyncPage, SyncPage
from sellapp_sdk.events.models import (
    ListIntegrationEventsResponseValue200ApplicationJsonPropertyDataItem,
    ListOrderEventsResponseValue200ApplicationJsonPropertyDataItem,
)
from tests.generated_helpers import load_fixture


class TestEvents:
    def test_list_integration_events(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "list_list_integration_events_response_value_200_application_json_property_data_item.json"
            ),
        )
        page = sellapp_sdk.events.list_integration_events()
        assert isinstance(page, SyncPage)
        assert len(page.data) == 1
        assert isinstance(
            page.data[0],
            ListIntegrationEventsResponseValue200ApplicationJsonPropertyDataItem,
        )

    def test_list_integration_events_empty_page(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(json={"data": [], "list_metadata": {}})
        page = sellapp_sdk.events.list_integration_events()
        assert isinstance(page, SyncPage)
        assert page.data == []

    def test_list_integration_events_encodes_query_params(
        self, sellapp_sdk, httpx_mock
    ):
        httpx_mock.add_response(json={"data": [], "list_metadata": {}})
        sellapp_sdk.events.list_integration_events(
            limit=10,
            before="cursor before",
            after="cursor/after",
            cursor="value cursor/test",
            type="value type/test",
            subject_type="order",
            subject_id="value subject_id/test",
        )
        request = httpx_mock.get_request()
        assert request.url.params["limit"] == "10"
        assert request.url.params["before"] == "cursor before"
        assert request.url.params["after"] == "cursor/after"
        assert request.url.params["cursor"] == "value cursor/test"
        assert request.url.params["type"] == "value type/test"
        assert request.url.params["subject_type"] == "order"
        assert request.url.params["subject_id"] == "value subject_id/test"

    def test_list_order_events(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "list_list_order_events_response_value_200_application_json_property_data_item.json"
            ),
        )
        page = sellapp_sdk.events.list_order_events("test_order")
        assert isinstance(page, SyncPage)
        assert len(page.data) == 1
        assert isinstance(
            page.data[0], ListOrderEventsResponseValue200ApplicationJsonPropertyDataItem
        )

    def test_list_order_events_empty_page(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(json={"data": [], "list_metadata": {}})
        page = sellapp_sdk.events.list_order_events("test_order")
        assert isinstance(page, SyncPage)
        assert page.data == []

    def test_list_order_events_encodes_query_params(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(json={"data": [], "list_metadata": {}})
        sellapp_sdk.events.list_order_events(
            "test_order",
            limit=10,
            before="cursor before",
            after="cursor/after",
            cursor="value cursor/test",
        )
        request = httpx_mock.get_request()
        assert request.url.params["limit"] == "10"
        assert request.url.params["before"] == "cursor before"
        assert request.url.params["after"] == "cursor/after"
        assert request.url.params["cursor"] == "value cursor/test"

    def test_list_integration_events_with_request_options(
        self, sellapp_sdk, httpx_mock
    ):
        httpx_mock.add_response(json={"data": [], "list_metadata": {}})
        sellapp_sdk.events.list_integration_events(
            request_options={"headers": {"X-Custom": "value"}}
        )
        request = httpx_mock.get_request()
        assert request.headers["X-Custom"] == "value"

    def test_list_integration_events_unauthorized(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            status_code=401,
            json={"message": "Unauthorized"},
        )
        with pytest.raises(AuthenticationError):
            sellapp_sdk.events.list_integration_events()

    def test_list_integration_events_not_found(self, httpx_mock):
        sellapp_sdk = SellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=404, json={"message": "Not found"})
            with pytest.raises(NotFoundError):
                sellapp_sdk.events.list_integration_events()
        finally:
            sellapp_sdk.close()

    def test_list_integration_events_rate_limited(self, httpx_mock):
        sellapp_sdk = SellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(
                status_code=429,
                headers={"Retry-After": "0"},
                json={"message": "Slow down"},
            )
            with pytest.raises(RateLimitExceededError):
                sellapp_sdk.events.list_integration_events()
        finally:
            sellapp_sdk.close()

    def test_list_integration_events_server_error(self, httpx_mock):
        sellapp_sdk = SellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=500, json={"message": "Server error"})
            with pytest.raises(ServerError):
                sellapp_sdk.events.list_integration_events()
        finally:
            sellapp_sdk.close()

    def test_list_integration_events_bad_request(self, httpx_mock):
        sellapp_sdk = SellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=400, json={"message": "Bad request"})
            with pytest.raises(BadRequestError):
                sellapp_sdk.events.list_integration_events()
        finally:
            sellapp_sdk.close()

    def test_list_integration_events_unprocessable(self, httpx_mock):
        sellapp_sdk = SellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=422, json={"message": "Unprocessable"})
            with pytest.raises(UnprocessableEntityError):
                sellapp_sdk.events.list_integration_events()
        finally:
            sellapp_sdk.close()


class TestAsyncEvents:
    @pytest.mark.asyncio
    async def test_list_integration_events(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "list_list_integration_events_response_value_200_application_json_property_data_item.json"
            )
        )
        page = await async_sellapp_sdk.events.list_integration_events()
        assert isinstance(page, AsyncPage)
        assert len(page.data) == 1
        assert isinstance(
            page.data[0],
            ListIntegrationEventsResponseValue200ApplicationJsonPropertyDataItem,
        )

    @pytest.mark.asyncio
    async def test_list_integration_events_empty_page(
        self, async_sellapp_sdk, httpx_mock
    ):
        httpx_mock.add_response(json={"data": [], "list_metadata": {}})
        page = await async_sellapp_sdk.events.list_integration_events()
        assert isinstance(page, AsyncPage)
        assert page.data == []

    @pytest.mark.asyncio
    async def test_list_integration_events_encodes_query_params(
        self, async_sellapp_sdk, httpx_mock
    ):
        httpx_mock.add_response(json={"data": [], "list_metadata": {}})
        await async_sellapp_sdk.events.list_integration_events(
            limit=10,
            before="cursor before",
            after="cursor/after",
            cursor="value cursor/test",
            type="value type/test",
            subject_type="order",
            subject_id="value subject_id/test",
        )
        request = httpx_mock.get_request()
        assert request.url.params["limit"] == "10"
        assert request.url.params["before"] == "cursor before"
        assert request.url.params["after"] == "cursor/after"
        assert request.url.params["cursor"] == "value cursor/test"
        assert request.url.params["type"] == "value type/test"
        assert request.url.params["subject_type"] == "order"
        assert request.url.params["subject_id"] == "value subject_id/test"

    @pytest.mark.asyncio
    async def test_list_order_events(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "list_list_order_events_response_value_200_application_json_property_data_item.json"
            )
        )
        page = await async_sellapp_sdk.events.list_order_events("test_order")
        assert isinstance(page, AsyncPage)
        assert len(page.data) == 1
        assert isinstance(
            page.data[0], ListOrderEventsResponseValue200ApplicationJsonPropertyDataItem
        )

    @pytest.mark.asyncio
    async def test_list_order_events_empty_page(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(json={"data": [], "list_metadata": {}})
        page = await async_sellapp_sdk.events.list_order_events("test_order")
        assert isinstance(page, AsyncPage)
        assert page.data == []

    @pytest.mark.asyncio
    async def test_list_order_events_encodes_query_params(
        self, async_sellapp_sdk, httpx_mock
    ):
        httpx_mock.add_response(json={"data": [], "list_metadata": {}})
        await async_sellapp_sdk.events.list_order_events(
            "test_order",
            limit=10,
            before="cursor before",
            after="cursor/after",
            cursor="value cursor/test",
        )
        request = httpx_mock.get_request()
        assert request.url.params["limit"] == "10"
        assert request.url.params["before"] == "cursor before"
        assert request.url.params["after"] == "cursor/after"
        assert request.url.params["cursor"] == "value cursor/test"

    @pytest.mark.asyncio
    async def test_list_integration_events_with_request_options(
        self, async_sellapp_sdk, httpx_mock
    ):
        httpx_mock.add_response(json={"data": [], "list_metadata": {}})
        await async_sellapp_sdk.events.list_integration_events(
            request_options={"headers": {"X-Custom": "value"}}
        )
        request = httpx_mock.get_request()
        assert request.headers["X-Custom"] == "value"

    @pytest.mark.asyncio
    async def test_list_integration_events_unauthorized(
        self, async_sellapp_sdk, httpx_mock
    ):
        httpx_mock.add_response(status_code=401, json={"message": "Unauthorized"})
        with pytest.raises(AuthenticationError):
            await async_sellapp_sdk.events.list_integration_events()

    @pytest.mark.asyncio
    async def test_list_integration_events_not_found(self, httpx_mock):
        sellapp_sdk = AsyncSellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=404, json={"message": "Not found"})
            with pytest.raises(NotFoundError):
                await sellapp_sdk.events.list_integration_events()
        finally:
            await sellapp_sdk.close()

    @pytest.mark.asyncio
    async def test_list_integration_events_rate_limited(self, httpx_mock):
        sellapp_sdk = AsyncSellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(
                status_code=429,
                headers={"Retry-After": "0"},
                json={"message": "Slow down"},
            )
            with pytest.raises(RateLimitExceededError):
                await sellapp_sdk.events.list_integration_events()
        finally:
            await sellapp_sdk.close()

    @pytest.mark.asyncio
    async def test_list_integration_events_server_error(self, httpx_mock):
        sellapp_sdk = AsyncSellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=500, json={"message": "Server error"})
            with pytest.raises(ServerError):
                await sellapp_sdk.events.list_integration_events()
        finally:
            await sellapp_sdk.close()

    @pytest.mark.asyncio
    async def test_list_integration_events_bad_request(self, httpx_mock):
        sellapp_sdk = AsyncSellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=400, json={"message": "Bad request"})
            with pytest.raises(BadRequestError):
                await sellapp_sdk.events.list_integration_events()
        finally:
            await sellapp_sdk.close()

    @pytest.mark.asyncio
    async def test_list_integration_events_unprocessable(self, httpx_mock):
        sellapp_sdk = AsyncSellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=422, json={"message": "Unprocessable"})
            with pytest.raises(UnprocessableEntityError):
                await sellapp_sdk.events.list_integration_events()
        finally:
            await sellapp_sdk.close()
