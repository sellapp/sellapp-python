# This file is auto-generated by oagen. Do not edit.

import json

import pytest

from sellapp_sdk import AsyncSellAppClient, SellAppClient
from sellapp_sdk._errors import (
    AuthenticationError,
    BadRequestError,
    NotFoundError,
    RateLimitExceededError,
    ServerError,
    UnprocessableEntityError,
)
from sellapp_sdk._pagination import AsyncPage, SyncPage
from sellapp_sdk.common.models import (
    SdkActivateLicenseKeyResponseValue200ApplicationJson,
)
from sellapp_sdk.licenses.models import (
    SdkDeactivateLicenseResponseValue200ApplicationJson,
    SdkGetLicenseKeyResponseValue200ApplicationJson,
    SdkListLicenseKeysResponseValue200ApplicationJson,
    SdkUpdateLicenseKeyResponseValue200ApplicationJson,
    SdkValidateLicenseKeyResponseValue200ApplicationJson,
)
from tests.generated_helpers import load_fixture


class TestLicenses:
    def test_activate(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_activate_license_key_response_value_200_application_json.json"
            ),
        )
        result = sellapp_sdk.licenses.activate(
            license_key="test_license_key", instance_name="test_instance_name"
        )
        assert isinstance(result, SdkActivateLicenseKeyResponseValue200ApplicationJson)
        request = httpx_mock.get_request()
        assert request.method == "POST"
        assert request.url.path.endswith("/v2/licenses/activate")
        body = json.loads(request.content)
        assert body["license_key"] == "test_license_key"
        assert body["instance_name"] == "test_instance_name"

    def test_validate(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_validate_license_key_response_value_200_application_json.json"
            ),
        )
        result = sellapp_sdk.licenses.validate(license_key="test_license_key")
        assert isinstance(result, SdkValidateLicenseKeyResponseValue200ApplicationJson)
        request = httpx_mock.get_request()
        assert request.method == "POST"
        assert request.url.path.endswith("/v2/licenses/validate")
        body = json.loads(request.content)
        assert body["license_key"] == "test_license_key"

    def test_list(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "list_sdk_list_license_keys_response_value_200_application_json.json"
            ),
        )
        page = sellapp_sdk.licenses.list()
        assert isinstance(page, SyncPage)
        assert len(page.data) == 1
        assert isinstance(
            page.data[0], SdkListLicenseKeysResponseValue200ApplicationJson
        )

    def test_list_empty_page(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(json={"data": [], "list_metadata": {}})
        page = sellapp_sdk.licenses.list()
        assert isinstance(page, SyncPage)
        assert page.data == []

    def test_list_encodes_query_params(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(json={"data": [], "list_metadata": {}})
        sellapp_sdk.licenses.list(
            limit=10, before="cursor before", after="cursor/after", page=7
        )
        request = httpx_mock.get_request()
        assert request.url.params["limit"] == "10"
        assert request.url.params["before"] == "cursor before"
        assert request.url.params["after"] == "cursor/after"
        assert request.url.params["page"] == "7"

    def test_get(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_get_license_key_response_value_200_application_json.json"
            ),
        )
        result = sellapp_sdk.licenses.get("test_license_key")
        assert isinstance(result, SdkGetLicenseKeyResponseValue200ApplicationJson)
        request = httpx_mock.get_request()
        assert request.method == "GET"
        assert request.url.path.endswith("/v2/license-keys/test_license_key")

    def test_update(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_update_license_key_response_value_200_application_json.json"
            ),
        )
        result = sellapp_sdk.licenses.update("test_license_key")
        assert isinstance(result, SdkUpdateLicenseKeyResponseValue200ApplicationJson)
        request = httpx_mock.get_request()
        assert request.method == "PATCH"
        assert request.url.path.endswith("/v2/license-keys/test_license_key")

    def test_deactivate_license(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_deactivate_license_response_value_200_application_json.json"
            ),
        )
        result = sellapp_sdk.licenses.deactivate_license(
            license_key="test_license_key", instance_id="test_instance_id"
        )
        assert isinstance(result, SdkDeactivateLicenseResponseValue200ApplicationJson)
        request = httpx_mock.get_request()
        assert request.method == "POST"
        assert request.url.path.endswith("/v2/licenses/deactivate")
        body = json.loads(request.content)
        assert body["license_key"] == "test_license_key"
        assert body["instance_id"] == "test_instance_id"

    def test_activate_with_request_options(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_activate_license_key_response_value_200_application_json.json"
            )
        )
        sellapp_sdk.licenses.activate(
            license_key="test_license_key",
            instance_name="test_instance_name",
            request_options={"headers": {"X-Custom": "value"}},
        )
        request = httpx_mock.get_request()
        assert request.headers["X-Custom"] == "value"

    def test_activate_unauthorized(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            status_code=401,
            json={"message": "Unauthorized"},
        )
        with pytest.raises(AuthenticationError):
            sellapp_sdk.licenses.activate(
                license_key="test_license_key", instance_name="test_instance_name"
            )

    def test_activate_not_found(self, httpx_mock):
        sellapp_sdk = SellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=404, json={"message": "Not found"})
            with pytest.raises(NotFoundError):
                sellapp_sdk.licenses.activate(
                    license_key="test_license_key", instance_name="test_instance_name"
                )
        finally:
            sellapp_sdk.close()

    def test_activate_rate_limited(self, httpx_mock):
        sellapp_sdk = SellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(
                status_code=429,
                headers={"Retry-After": "0"},
                json={"message": "Slow down"},
            )
            with pytest.raises(RateLimitExceededError):
                sellapp_sdk.licenses.activate(
                    license_key="test_license_key", instance_name="test_instance_name"
                )
        finally:
            sellapp_sdk.close()

    def test_activate_server_error(self, httpx_mock):
        sellapp_sdk = SellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=500, json={"message": "Server error"})
            with pytest.raises(ServerError):
                sellapp_sdk.licenses.activate(
                    license_key="test_license_key", instance_name="test_instance_name"
                )
        finally:
            sellapp_sdk.close()

    def test_activate_bad_request(self, httpx_mock):
        sellapp_sdk = SellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=400, json={"message": "Bad request"})
            with pytest.raises(BadRequestError):
                sellapp_sdk.licenses.activate(
                    license_key="test_license_key", instance_name="test_instance_name"
                )
        finally:
            sellapp_sdk.close()

    def test_activate_unprocessable(self, httpx_mock):
        sellapp_sdk = SellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=422, json={"message": "Unprocessable"})
            with pytest.raises(UnprocessableEntityError):
                sellapp_sdk.licenses.activate(
                    license_key="test_license_key", instance_name="test_instance_name"
                )
        finally:
            sellapp_sdk.close()


class TestAsyncLicenses:
    @pytest.mark.asyncio
    async def test_activate(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_activate_license_key_response_value_200_application_json.json"
            )
        )
        result = await async_sellapp_sdk.licenses.activate(
            license_key="test_license_key", instance_name="test_instance_name"
        )
        assert isinstance(result, SdkActivateLicenseKeyResponseValue200ApplicationJson)
        request = httpx_mock.get_request()
        assert request.method == "POST"
        assert request.url.path.endswith("/v2/licenses/activate")

    @pytest.mark.asyncio
    async def test_validate(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_validate_license_key_response_value_200_application_json.json"
            )
        )
        result = await async_sellapp_sdk.licenses.validate(
            license_key="test_license_key"
        )
        assert isinstance(result, SdkValidateLicenseKeyResponseValue200ApplicationJson)
        request = httpx_mock.get_request()
        assert request.method == "POST"
        assert request.url.path.endswith("/v2/licenses/validate")

    @pytest.mark.asyncio
    async def test_list(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "list_sdk_list_license_keys_response_value_200_application_json.json"
            )
        )
        page = await async_sellapp_sdk.licenses.list()
        assert isinstance(page, AsyncPage)
        assert len(page.data) == 1
        assert isinstance(
            page.data[0], SdkListLicenseKeysResponseValue200ApplicationJson
        )

    @pytest.mark.asyncio
    async def test_list_empty_page(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(json={"data": [], "list_metadata": {}})
        page = await async_sellapp_sdk.licenses.list()
        assert isinstance(page, AsyncPage)
        assert page.data == []

    @pytest.mark.asyncio
    async def test_list_encodes_query_params(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(json={"data": [], "list_metadata": {}})
        await async_sellapp_sdk.licenses.list(
            limit=10, before="cursor before", after="cursor/after", page=7
        )
        request = httpx_mock.get_request()
        assert request.url.params["limit"] == "10"
        assert request.url.params["before"] == "cursor before"
        assert request.url.params["after"] == "cursor/after"
        assert request.url.params["page"] == "7"

    @pytest.mark.asyncio
    async def test_get(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_get_license_key_response_value_200_application_json.json"
            )
        )
        result = await async_sellapp_sdk.licenses.get("test_license_key")
        assert isinstance(result, SdkGetLicenseKeyResponseValue200ApplicationJson)
        request = httpx_mock.get_request()
        assert request.method == "GET"
        assert request.url.path.endswith("/v2/license-keys/test_license_key")

    @pytest.mark.asyncio
    async def test_update(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_update_license_key_response_value_200_application_json.json"
            )
        )
        result = await async_sellapp_sdk.licenses.update("test_license_key")
        assert isinstance(result, SdkUpdateLicenseKeyResponseValue200ApplicationJson)
        request = httpx_mock.get_request()
        assert request.method == "PATCH"
        assert request.url.path.endswith("/v2/license-keys/test_license_key")

    @pytest.mark.asyncio
    async def test_deactivate_license(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_deactivate_license_response_value_200_application_json.json"
            )
        )
        result = await async_sellapp_sdk.licenses.deactivate_license(
            license_key="test_license_key", instance_id="test_instance_id"
        )
        assert isinstance(result, SdkDeactivateLicenseResponseValue200ApplicationJson)
        request = httpx_mock.get_request()
        assert request.method == "POST"
        assert request.url.path.endswith("/v2/licenses/deactivate")

    @pytest.mark.asyncio
    async def test_activate_with_request_options(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_activate_license_key_response_value_200_application_json.json"
            )
        )
        await async_sellapp_sdk.licenses.activate(
            license_key="test_license_key",
            instance_name="test_instance_name",
            request_options={"headers": {"X-Custom": "value"}},
        )
        request = httpx_mock.get_request()
        assert request.headers["X-Custom"] == "value"

    @pytest.mark.asyncio
    async def test_activate_unauthorized(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(status_code=401, json={"message": "Unauthorized"})
        with pytest.raises(AuthenticationError):
            await async_sellapp_sdk.licenses.activate(
                license_key="test_license_key", instance_name="test_instance_name"
            )

    @pytest.mark.asyncio
    async def test_activate_not_found(self, httpx_mock):
        sellapp_sdk = AsyncSellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=404, json={"message": "Not found"})
            with pytest.raises(NotFoundError):
                await sellapp_sdk.licenses.activate(
                    license_key="test_license_key", instance_name="test_instance_name"
                )
        finally:
            await sellapp_sdk.close()

    @pytest.mark.asyncio
    async def test_activate_rate_limited(self, httpx_mock):
        sellapp_sdk = AsyncSellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(
                status_code=429,
                headers={"Retry-After": "0"},
                json={"message": "Slow down"},
            )
            with pytest.raises(RateLimitExceededError):
                await sellapp_sdk.licenses.activate(
                    license_key="test_license_key", instance_name="test_instance_name"
                )
        finally:
            await sellapp_sdk.close()

    @pytest.mark.asyncio
    async def test_activate_server_error(self, httpx_mock):
        sellapp_sdk = AsyncSellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=500, json={"message": "Server error"})
            with pytest.raises(ServerError):
                await sellapp_sdk.licenses.activate(
                    license_key="test_license_key", instance_name="test_instance_name"
                )
        finally:
            await sellapp_sdk.close()

    @pytest.mark.asyncio
    async def test_activate_bad_request(self, httpx_mock):
        sellapp_sdk = AsyncSellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=400, json={"message": "Bad request"})
            with pytest.raises(BadRequestError):
                await sellapp_sdk.licenses.activate(
                    license_key="test_license_key", instance_name="test_instance_name"
                )
        finally:
            await sellapp_sdk.close()

    @pytest.mark.asyncio
    async def test_activate_unprocessable(self, httpx_mock):
        sellapp_sdk = AsyncSellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=422, json={"message": "Unprocessable"})
            with pytest.raises(UnprocessableEntityError):
                await sellapp_sdk.licenses.activate(
                    license_key="test_license_key", instance_name="test_instance_name"
                )
        finally:
            await sellapp_sdk.close()
