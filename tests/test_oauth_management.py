# This file is auto-generated by oagen. Do not edit.


import pytest

from sellapp_sdk import AsyncSellAppClient, SellAppClient
from sellapp_sdk._errors import (
    AuthenticationError,
    BadRequestError,
    NotFoundError,
    RateLimitExceededError,
    ServerError,
    UnprocessableEntityError,
)
from sellapp_sdk.oauth_management.models import (
    SdkGetOAuthInstallationResponseValue200ApplicationJson,
)
from tests.generated_helpers import load_fixture


class TestOauthManagement:
    def test_get_oauth_installation(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_get_oauth_installation_response_value_200_application_json.json"
            ),
        )
        result = sellapp_sdk.oauth_management.get_oauth_installation()
        assert isinstance(
            result, SdkGetOAuthInstallationResponseValue200ApplicationJson
        )
        request = httpx_mock.get_request()
        assert request.method == "GET"
        assert request.url.path.endswith("/v2/oauth/installation")

    def test_delete_oauth_installation(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(status_code=204)
        result = sellapp_sdk.oauth_management.delete_oauth_installation()
        assert result is None
        request = httpx_mock.get_request()
        assert request.method == "DELETE"
        assert request.url.path.endswith("/v2/oauth/installation")

    def test_get_oauth_installation_with_request_options(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_get_oauth_installation_response_value_200_application_json.json"
            )
        )
        sellapp_sdk.oauth_management.get_oauth_installation(
            request_options={"headers": {"X-Custom": "value"}}
        )
        request = httpx_mock.get_request()
        assert request.headers["X-Custom"] == "value"

    def test_get_oauth_installation_unauthorized(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            status_code=401,
            json={"message": "Unauthorized"},
        )
        with pytest.raises(AuthenticationError):
            sellapp_sdk.oauth_management.get_oauth_installation()

    def test_get_oauth_installation_not_found(self, httpx_mock):
        sellapp_sdk = SellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=404, json={"message": "Not found"})
            with pytest.raises(NotFoundError):
                sellapp_sdk.oauth_management.get_oauth_installation()
        finally:
            sellapp_sdk.close()

    def test_get_oauth_installation_rate_limited(self, httpx_mock):
        sellapp_sdk = SellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(
                status_code=429,
                headers={"Retry-After": "0"},
                json={"message": "Slow down"},
            )
            with pytest.raises(RateLimitExceededError):
                sellapp_sdk.oauth_management.get_oauth_installation()
        finally:
            sellapp_sdk.close()

    def test_get_oauth_installation_server_error(self, httpx_mock):
        sellapp_sdk = SellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=500, json={"message": "Server error"})
            with pytest.raises(ServerError):
                sellapp_sdk.oauth_management.get_oauth_installation()
        finally:
            sellapp_sdk.close()

    def test_get_oauth_installation_bad_request(self, httpx_mock):
        sellapp_sdk = SellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=400, json={"message": "Bad request"})
            with pytest.raises(BadRequestError):
                sellapp_sdk.oauth_management.get_oauth_installation()
        finally:
            sellapp_sdk.close()

    def test_get_oauth_installation_unprocessable(self, httpx_mock):
        sellapp_sdk = SellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=422, json={"message": "Unprocessable"})
            with pytest.raises(UnprocessableEntityError):
                sellapp_sdk.oauth_management.get_oauth_installation()
        finally:
            sellapp_sdk.close()


class TestAsyncOauthManagement:
    @pytest.mark.asyncio
    async def test_get_oauth_installation(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_get_oauth_installation_response_value_200_application_json.json"
            )
        )
        result = await async_sellapp_sdk.oauth_management.get_oauth_installation()
        assert isinstance(
            result, SdkGetOAuthInstallationResponseValue200ApplicationJson
        )
        request = httpx_mock.get_request()
        assert request.method == "GET"
        assert request.url.path.endswith("/v2/oauth/installation")

    @pytest.mark.asyncio
    async def test_delete_oauth_installation(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(status_code=204)
        result = await async_sellapp_sdk.oauth_management.delete_oauth_installation()
        assert result is None
        request = httpx_mock.get_request()
        assert request.method == "DELETE"
        assert request.url.path.endswith("/v2/oauth/installation")

    @pytest.mark.asyncio
    async def test_get_oauth_installation_with_request_options(
        self, async_sellapp_sdk, httpx_mock
    ):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_get_oauth_installation_response_value_200_application_json.json"
            )
        )
        await async_sellapp_sdk.oauth_management.get_oauth_installation(
            request_options={"headers": {"X-Custom": "value"}}
        )
        request = httpx_mock.get_request()
        assert request.headers["X-Custom"] == "value"

    @pytest.mark.asyncio
    async def test_get_oauth_installation_unauthorized(
        self, async_sellapp_sdk, httpx_mock
    ):
        httpx_mock.add_response(status_code=401, json={"message": "Unauthorized"})
        with pytest.raises(AuthenticationError):
            await async_sellapp_sdk.oauth_management.get_oauth_installation()

    @pytest.mark.asyncio
    async def test_get_oauth_installation_not_found(self, httpx_mock):
        sellapp_sdk = AsyncSellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=404, json={"message": "Not found"})
            with pytest.raises(NotFoundError):
                await sellapp_sdk.oauth_management.get_oauth_installation()
        finally:
            await sellapp_sdk.close()

    @pytest.mark.asyncio
    async def test_get_oauth_installation_rate_limited(self, httpx_mock):
        sellapp_sdk = AsyncSellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(
                status_code=429,
                headers={"Retry-After": "0"},
                json={"message": "Slow down"},
            )
            with pytest.raises(RateLimitExceededError):
                await sellapp_sdk.oauth_management.get_oauth_installation()
        finally:
            await sellapp_sdk.close()

    @pytest.mark.asyncio
    async def test_get_oauth_installation_server_error(self, httpx_mock):
        sellapp_sdk = AsyncSellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=500, json={"message": "Server error"})
            with pytest.raises(ServerError):
                await sellapp_sdk.oauth_management.get_oauth_installation()
        finally:
            await sellapp_sdk.close()

    @pytest.mark.asyncio
    async def test_get_oauth_installation_bad_request(self, httpx_mock):
        sellapp_sdk = AsyncSellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=400, json={"message": "Bad request"})
            with pytest.raises(BadRequestError):
                await sellapp_sdk.oauth_management.get_oauth_installation()
        finally:
            await sellapp_sdk.close()

    @pytest.mark.asyncio
    async def test_get_oauth_installation_unprocessable(self, httpx_mock):
        sellapp_sdk = AsyncSellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=422, json={"message": "Unprocessable"})
            with pytest.raises(UnprocessableEntityError):
                await sellapp_sdk.oauth_management.get_oauth_installation()
        finally:
            await sellapp_sdk.close()
