# This file is auto-generated by oagen. Do not edit.

import json

import pytest

from sellapp_sdk import AsyncSellAppClient, SellAppClient
from sellapp_sdk._errors import (
    AuthenticationError,
    BadRequestError,
    NotFoundError,
    RateLimitExceededError,
    ServerError,
    UnprocessableEntityError,
)
from sellapp_sdk.payment_methods.models import (
    SdkCreatePaymentConnectionHandoffResponseValue200ApplicationJson,
    SdkEnableOrDisablePaymentMethodResponseValue200ApplicationJson,
    SdkGetPaymentMethodStatusResponseValue200ApplicationJson,
    SdkListPaymentMethodsResponseValue200ApplicationJson,
    SdkValidateAndSavePaymentMethodConfigurationResponseValue200ApplicationJson,
)
from tests.generated_helpers import load_fixture


class TestPaymentMethods:
    def test_list(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_list_payment_methods_response_value_200_application_json.json"
            ),
        )
        result = sellapp_sdk.payment_methods.list()
        assert isinstance(result, SdkListPaymentMethodsResponseValue200ApplicationJson)
        request = httpx_mock.get_request()
        assert request.method == "GET"
        assert request.url.path.endswith("/v2/payment-methods")

    def test_get(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_get_payment_method_status_response_value_200_application_json.json"
            ),
        )
        result = sellapp_sdk.payment_methods.get("test_paymentMethod")
        assert isinstance(
            result, SdkGetPaymentMethodStatusResponseValue200ApplicationJson
        )
        request = httpx_mock.get_request()
        assert request.method == "GET"
        assert request.url.path.endswith("/v2/payment-methods/test_paymentMethod")

    def test_enable(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_enable_or_disable_payment_method_response_value_200_application_json.json"
            ),
        )
        result = sellapp_sdk.payment_methods.enable("test_paymentMethod", enabled=True)
        assert isinstance(
            result, SdkEnableOrDisablePaymentMethodResponseValue200ApplicationJson
        )
        request = httpx_mock.get_request()
        assert request.method == "PATCH"
        assert request.url.path.endswith("/v2/payment-methods/test_paymentMethod")
        body = json.loads(request.content)
        assert body["enabled"] == True

    def test_connect(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_create_payment_connection_handoff_response_value_200_application_json.json"
            ),
        )
        result = sellapp_sdk.payment_methods.connect("test_paymentMethod")
        assert isinstance(
            result, SdkCreatePaymentConnectionHandoffResponseValue200ApplicationJson
        )
        request = httpx_mock.get_request()
        assert request.method == "POST"
        assert request.url.path.endswith(
            "/v2/payment-methods/test_paymentMethod/connect"
        )

    def test_validate(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_validate_and_save_payment_method_configuration_response_value_200_application_json.json"
            ),
        )
        result = sellapp_sdk.payment_methods.validate(
            "test_paymentMethod",
            body=load_fixture(
                "validate_and_save_payment_method_configuration_request_application_json_one_of_value_1.json"
            ),
        )
        assert isinstance(
            result,
            SdkValidateAndSavePaymentMethodConfigurationResponseValue200ApplicationJson,
        )
        request = httpx_mock.get_request()
        assert request.method == "PUT"
        assert request.url.path.endswith(
            "/v2/payment-methods/test_paymentMethod/configuration"
        )

    def test_list_with_request_options(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_list_payment_methods_response_value_200_application_json.json"
            )
        )
        sellapp_sdk.payment_methods.list(
            request_options={"headers": {"X-Custom": "value"}}
        )
        request = httpx_mock.get_request()
        assert request.headers["X-Custom"] == "value"

    def test_list_unauthorized(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            status_code=401,
            json={"message": "Unauthorized"},
        )
        with pytest.raises(AuthenticationError):
            sellapp_sdk.payment_methods.list()

    def test_list_not_found(self, httpx_mock):
        sellapp_sdk = SellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=404, json={"message": "Not found"})
            with pytest.raises(NotFoundError):
                sellapp_sdk.payment_methods.list()
        finally:
            sellapp_sdk.close()

    def test_list_rate_limited(self, httpx_mock):
        sellapp_sdk = SellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(
                status_code=429,
                headers={"Retry-After": "0"},
                json={"message": "Slow down"},
            )
            with pytest.raises(RateLimitExceededError):
                sellapp_sdk.payment_methods.list()
        finally:
            sellapp_sdk.close()

    def test_list_server_error(self, httpx_mock):
        sellapp_sdk = SellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=500, json={"message": "Server error"})
            with pytest.raises(ServerError):
                sellapp_sdk.payment_methods.list()
        finally:
            sellapp_sdk.close()

    def test_list_bad_request(self, httpx_mock):
        sellapp_sdk = SellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=400, json={"message": "Bad request"})
            with pytest.raises(BadRequestError):
                sellapp_sdk.payment_methods.list()
        finally:
            sellapp_sdk.close()

    def test_list_unprocessable(self, httpx_mock):
        sellapp_sdk = SellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=422, json={"message": "Unprocessable"})
            with pytest.raises(UnprocessableEntityError):
                sellapp_sdk.payment_methods.list()
        finally:
            sellapp_sdk.close()


class TestAsyncPaymentMethods:
    @pytest.mark.asyncio
    async def test_list(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_list_payment_methods_response_value_200_application_json.json"
            )
        )
        result = await async_sellapp_sdk.payment_methods.list()
        assert isinstance(result, SdkListPaymentMethodsResponseValue200ApplicationJson)
        request = httpx_mock.get_request()
        assert request.method == "GET"
        assert request.url.path.endswith("/v2/payment-methods")

    @pytest.mark.asyncio
    async def test_get(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_get_payment_method_status_response_value_200_application_json.json"
            )
        )
        result = await async_sellapp_sdk.payment_methods.get("test_paymentMethod")
        assert isinstance(
            result, SdkGetPaymentMethodStatusResponseValue200ApplicationJson
        )
        request = httpx_mock.get_request()
        assert request.method == "GET"
        assert request.url.path.endswith("/v2/payment-methods/test_paymentMethod")

    @pytest.mark.asyncio
    async def test_enable(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_enable_or_disable_payment_method_response_value_200_application_json.json"
            )
        )
        result = await async_sellapp_sdk.payment_methods.enable(
            "test_paymentMethod", enabled=True
        )
        assert isinstance(
            result, SdkEnableOrDisablePaymentMethodResponseValue200ApplicationJson
        )
        request = httpx_mock.get_request()
        assert request.method == "PATCH"
        assert request.url.path.endswith("/v2/payment-methods/test_paymentMethod")

    @pytest.mark.asyncio
    async def test_connect(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_create_payment_connection_handoff_response_value_200_application_json.json"
            )
        )
        result = await async_sellapp_sdk.payment_methods.connect("test_paymentMethod")
        assert isinstance(
            result, SdkCreatePaymentConnectionHandoffResponseValue200ApplicationJson
        )
        request = httpx_mock.get_request()
        assert request.method == "POST"
        assert request.url.path.endswith(
            "/v2/payment-methods/test_paymentMethod/connect"
        )

    @pytest.mark.asyncio
    async def test_validate(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_validate_and_save_payment_method_configuration_response_value_200_application_json.json"
            )
        )
        result = await async_sellapp_sdk.payment_methods.validate(
            "test_paymentMethod",
            body=load_fixture(
                "validate_and_save_payment_method_configuration_request_application_json_one_of_value_1.json"
            ),
        )
        assert isinstance(
            result,
            SdkValidateAndSavePaymentMethodConfigurationResponseValue200ApplicationJson,
        )
        request = httpx_mock.get_request()
        assert request.method == "PUT"
        assert request.url.path.endswith(
            "/v2/payment-methods/test_paymentMethod/configuration"
        )

    @pytest.mark.asyncio
    async def test_list_with_request_options(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_list_payment_methods_response_value_200_application_json.json"
            )
        )
        await async_sellapp_sdk.payment_methods.list(
            request_options={"headers": {"X-Custom": "value"}}
        )
        request = httpx_mock.get_request()
        assert request.headers["X-Custom"] == "value"

    @pytest.mark.asyncio
    async def test_list_unauthorized(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(status_code=401, json={"message": "Unauthorized"})
        with pytest.raises(AuthenticationError):
            await async_sellapp_sdk.payment_methods.list()

    @pytest.mark.asyncio
    async def test_list_not_found(self, httpx_mock):
        sellapp_sdk = AsyncSellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=404, json={"message": "Not found"})
            with pytest.raises(NotFoundError):
                await sellapp_sdk.payment_methods.list()
        finally:
            await sellapp_sdk.close()

    @pytest.mark.asyncio
    async def test_list_rate_limited(self, httpx_mock):
        sellapp_sdk = AsyncSellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(
                status_code=429,
                headers={"Retry-After": "0"},
                json={"message": "Slow down"},
            )
            with pytest.raises(RateLimitExceededError):
                await sellapp_sdk.payment_methods.list()
        finally:
            await sellapp_sdk.close()

    @pytest.mark.asyncio
    async def test_list_server_error(self, httpx_mock):
        sellapp_sdk = AsyncSellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=500, json={"message": "Server error"})
            with pytest.raises(ServerError):
                await sellapp_sdk.payment_methods.list()
        finally:
            await sellapp_sdk.close()

    @pytest.mark.asyncio
    async def test_list_bad_request(self, httpx_mock):
        sellapp_sdk = AsyncSellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=400, json={"message": "Bad request"})
            with pytest.raises(BadRequestError):
                await sellapp_sdk.payment_methods.list()
        finally:
            await sellapp_sdk.close()

    @pytest.mark.asyncio
    async def test_list_unprocessable(self, httpx_mock):
        sellapp_sdk = AsyncSellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=422, json={"message": "Unprocessable"})
            with pytest.raises(UnprocessableEntityError):
                await sellapp_sdk.payment_methods.list()
        finally:
            await sellapp_sdk.close()
