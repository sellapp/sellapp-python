# This file is auto-generated by oagen. Do not edit.

import json

import pytest

from sellapp_sdk import AsyncSellAppClient, SellAppClient
from sellapp_sdk._errors import (
    AuthenticationError,
    BadRequestError,
    NotFoundError,
    RateLimitExceededError,
    ServerError,
    UnprocessableEntityError,
)
from sellapp_sdk._pagination import AsyncPage, SyncPage
from sellapp_sdk.common.models import (
    CreateProductVariantRequestApplicationJsonPropertyDeliverable,
    CreateProductVariantRequestApplicationJsonPropertyPricing,
    SdkBatchCreateProductVariantsResponseValue200ApplicationJson,
    SdkCreateProductVariantRequestApplicationJsonPaymentMethods,
)
from sellapp_sdk.product_variants.models import (
    BatchCreateProductVariantsRequestApplicationJsonPropertyResourcesItem,
    BatchUpdateProductVariantsRequestApplicationJsonPropertyResources,
    ListProductVariantsResponseValue200ApplicationJsonPropertyDataItem,
    SdkBatchUpdateProductVariantsResponseValue200ApplicationJson,
    SdkCreateProductVariantResponseValue201ApplicationJson,
    SdkGetProductVariantResponseValue200ApplicationJson,
    SdkReplaceProductVariantWithPutResponseValue200ApplicationJson,
    SdkUpdateProductVariantResponseValue200ApplicationJson,
    SearchProductVariantsResponseValue200ApplicationJsonPropertyDataItem,
)
from tests.generated_helpers import load_fixture


class TestProductVariants:
    def test_list(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "list_list_product_variants_response_value_200_application_json_property_data_item.json"
            ),
        )
        page = sellapp_sdk.product_variants.list("test_product")
        assert isinstance(page, SyncPage)
        assert len(page.data) == 1
        assert isinstance(
            page.data[0],
            ListProductVariantsResponseValue200ApplicationJsonPropertyDataItem,
        )

    def test_list_empty_page(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(json={"data": [], "list_metadata": {}})
        page = sellapp_sdk.product_variants.list("test_product")
        assert isinstance(page, SyncPage)
        assert page.data == []

    def test_list_encodes_query_params(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(json={"data": [], "list_metadata": {}})
        sellapp_sdk.product_variants.list(
            "test_product",
            limit=10,
            before="cursor before",
            after="cursor/after",
            page=7,
            with_drafts=True,
            only_drafts=True,
        )
        request = httpx_mock.get_request()
        assert request.url.params["limit"] == "10"
        assert request.url.params["before"] == "cursor before"
        assert request.url.params["after"] == "cursor/after"
        assert request.url.params["page"] == "7"
        assert request.url.params["with_drafts"] == "true"
        assert request.url.params["only_drafts"] == "true"

    def test_create(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_create_product_variant_response_value_201_application_json.json"
            ),
        )
        result = sellapp_sdk.product_variants.create(
            "test_product",
            title="test_title",
            description="test_description",
            deliverable=CreateProductVariantRequestApplicationJsonPropertyDeliverable.from_dict(
                load_fixture(
                    "create_product_variant_request_application_json_property_deliverable.json"
                )
            ),
            pricing=CreateProductVariantRequestApplicationJsonPropertyPricing.from_dict(
                load_fixture(
                    "create_product_variant_request_application_json_property_pricing.json"
                )
            ),
            payment_methods=[
                SdkCreateProductVariantRequestApplicationJsonPaymentMethods("AUTHNET")
            ],
        )
        assert isinstance(
            result, SdkCreateProductVariantResponseValue201ApplicationJson
        )
        request = httpx_mock.get_request()
        assert request.method == "POST"
        assert request.url.path.endswith("/v2/products/test_product/variants")
        body = json.loads(request.content)
        assert body["title"] == "test_title"
        assert body["description"] == "test_description"
        assert "deliverable" in body
        assert "pricing" in body
        assert "payment_methods" in body

    def test_get(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_get_product_variant_response_value_200_application_json.json"
            ),
        )
        result = sellapp_sdk.product_variants.get("test_product", "test_variant")
        assert isinstance(result, SdkGetProductVariantResponseValue200ApplicationJson)
        request = httpx_mock.get_request()
        assert request.method == "GET"
        assert request.url.path.endswith(
            "/v2/products/test_product/variants/test_variant"
        )

    def test_get_encodes_query_params(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_get_product_variant_response_value_200_application_json.json"
            )
        )
        sellapp_sdk.product_variants.get(
            "test_product", "test_variant", with_drafts=True, only_drafts=True
        )
        request = httpx_mock.get_request()
        assert request.url.params["with_drafts"] == "true"
        assert request.url.params["only_drafts"] == "true"

    def test_replace(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_replace_product_variant_with_put_response_value_200_application_json.json"
            ),
        )
        result = sellapp_sdk.product_variants.replace("test_product", "test_variant")
        assert isinstance(
            result, SdkReplaceProductVariantWithPutResponseValue200ApplicationJson
        )
        request = httpx_mock.get_request()
        assert request.method == "PUT"
        assert request.url.path.endswith(
            "/v2/products/test_product/variants/test_variant"
        )

    def test_update(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_update_product_variant_response_value_200_application_json.json"
            ),
        )
        result = sellapp_sdk.product_variants.update("test_product", "test_variant")
        assert isinstance(
            result, SdkUpdateProductVariantResponseValue200ApplicationJson
        )
        request = httpx_mock.get_request()
        assert request.method == "PATCH"
        assert request.url.path.endswith(
            "/v2/products/test_product/variants/test_variant"
        )

    def test_delete(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(status_code=200, content=b"")
        result = sellapp_sdk.product_variants.delete("test_product", "test_variant")
        assert result is None
        request = httpx_mock.get_request()
        assert request.method == "DELETE"
        assert request.url.path.endswith(
            "/v2/products/test_product/variants/test_variant"
        )

    def test_search(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "list_search_product_variants_response_value_200_application_json_property_data_item.json"
            ),
        )
        page = sellapp_sdk.product_variants.search("test_product")
        assert isinstance(page, SyncPage)
        assert len(page.data) == 1
        assert isinstance(
            page.data[0],
            SearchProductVariantsResponseValue200ApplicationJsonPropertyDataItem,
        )

    def test_search_empty_page(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(json={"data": [], "list_metadata": {}})
        page = sellapp_sdk.product_variants.search("test_product")
        assert isinstance(page, SyncPage)
        assert page.data == []

    def test_batch_create(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_batch_create_product_variants_response_value_200_application_json.json"
            ),
        )
        result = sellapp_sdk.product_variants.batch_create(
            "test_product",
            resources=[
                BatchCreateProductVariantsRequestApplicationJsonPropertyResourcesItem.from_dict(
                    load_fixture(
                        "batch_create_product_variants_request_application_json_property_resources_item.json"
                    )
                )
            ],
        )
        assert isinstance(
            result, SdkBatchCreateProductVariantsResponseValue200ApplicationJson
        )
        request = httpx_mock.get_request()
        assert request.method == "POST"
        assert request.url.path.endswith("/v2/products/test_product/variants/batch")
        body = json.loads(request.content)
        assert "resources" in body

    def test_batch_update(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_batch_update_product_variants_response_value_200_application_json.json"
            ),
        )
        result = sellapp_sdk.product_variants.batch_update(
            "test_product",
            resources=BatchUpdateProductVariantsRequestApplicationJsonPropertyResources.from_dict(
                load_fixture(
                    "batch_update_product_variants_request_application_json_property_resources.json"
                )
            ),
        )
        assert isinstance(
            result, SdkBatchUpdateProductVariantsResponseValue200ApplicationJson
        )
        request = httpx_mock.get_request()
        assert request.method == "PATCH"
        assert request.url.path.endswith("/v2/products/test_product/variants/batch")
        body = json.loads(request.content)
        assert "resources" in body

    def test_batch_delete(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(status_code=200, content=b"")
        result = sellapp_sdk.product_variants.batch_delete(
            "test_product", resources=[1]
        )
        assert result is None
        request = httpx_mock.get_request()
        assert request.method == "DELETE"
        assert request.url.path.endswith("/v2/products/test_product/variants/batch")

    def test_list_with_request_options(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(json={"data": [], "list_metadata": {}})
        sellapp_sdk.product_variants.list(
            "test_product", request_options={"headers": {"X-Custom": "value"}}
        )
        request = httpx_mock.get_request()
        assert request.headers["X-Custom"] == "value"

    def test_list_unauthorized(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            status_code=401,
            json={"message": "Unauthorized"},
        )
        with pytest.raises(AuthenticationError):
            sellapp_sdk.product_variants.list("test_product")

    def test_list_not_found(self, httpx_mock):
        sellapp_sdk = SellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=404, json={"message": "Not found"})
            with pytest.raises(NotFoundError):
                sellapp_sdk.product_variants.list("test_product")
        finally:
            sellapp_sdk.close()

    def test_list_rate_limited(self, httpx_mock):
        sellapp_sdk = SellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(
                status_code=429,
                headers={"Retry-After": "0"},
                json={"message": "Slow down"},
            )
            with pytest.raises(RateLimitExceededError):
                sellapp_sdk.product_variants.list("test_product")
        finally:
            sellapp_sdk.close()

    def test_list_server_error(self, httpx_mock):
        sellapp_sdk = SellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=500, json={"message": "Server error"})
            with pytest.raises(ServerError):
                sellapp_sdk.product_variants.list("test_product")
        finally:
            sellapp_sdk.close()

    def test_list_bad_request(self, httpx_mock):
        sellapp_sdk = SellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=400, json={"message": "Bad request"})
            with pytest.raises(BadRequestError):
                sellapp_sdk.product_variants.list("test_product")
        finally:
            sellapp_sdk.close()

    def test_list_unprocessable(self, httpx_mock):
        sellapp_sdk = SellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=422, json={"message": "Unprocessable"})
            with pytest.raises(UnprocessableEntityError):
                sellapp_sdk.product_variants.list("test_product")
        finally:
            sellapp_sdk.close()


class TestAsyncProductVariants:
    @pytest.mark.asyncio
    async def test_list(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "list_list_product_variants_response_value_200_application_json_property_data_item.json"
            )
        )
        page = await async_sellapp_sdk.product_variants.list("test_product")
        assert isinstance(page, AsyncPage)
        assert len(page.data) == 1
        assert isinstance(
            page.data[0],
            ListProductVariantsResponseValue200ApplicationJsonPropertyDataItem,
        )

    @pytest.mark.asyncio
    async def test_list_empty_page(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(json={"data": [], "list_metadata": {}})
        page = await async_sellapp_sdk.product_variants.list("test_product")
        assert isinstance(page, AsyncPage)
        assert page.data == []

    @pytest.mark.asyncio
    async def test_list_encodes_query_params(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(json={"data": [], "list_metadata": {}})
        await async_sellapp_sdk.product_variants.list(
            "test_product",
            limit=10,
            before="cursor before",
            after="cursor/after",
            page=7,
            with_drafts=True,
            only_drafts=True,
        )
        request = httpx_mock.get_request()
        assert request.url.params["limit"] == "10"
        assert request.url.params["before"] == "cursor before"
        assert request.url.params["after"] == "cursor/after"
        assert request.url.params["page"] == "7"
        assert request.url.params["with_drafts"] == "true"
        assert request.url.params["only_drafts"] == "true"

    @pytest.mark.asyncio
    async def test_create(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_create_product_variant_response_value_201_application_json.json"
            )
        )
        result = await async_sellapp_sdk.product_variants.create(
            "test_product",
            title="test_title",
            description="test_description",
            deliverable=CreateProductVariantRequestApplicationJsonPropertyDeliverable.from_dict(
                load_fixture(
                    "create_product_variant_request_application_json_property_deliverable.json"
                )
            ),
            pricing=CreateProductVariantRequestApplicationJsonPropertyPricing.from_dict(
                load_fixture(
                    "create_product_variant_request_application_json_property_pricing.json"
                )
            ),
            payment_methods=[
                SdkCreateProductVariantRequestApplicationJsonPaymentMethods("AUTHNET")
            ],
        )
        assert isinstance(
            result, SdkCreateProductVariantResponseValue201ApplicationJson
        )
        request = httpx_mock.get_request()
        assert request.method == "POST"
        assert request.url.path.endswith("/v2/products/test_product/variants")

    @pytest.mark.asyncio
    async def test_get(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_get_product_variant_response_value_200_application_json.json"
            )
        )
        result = await async_sellapp_sdk.product_variants.get(
            "test_product", "test_variant"
        )
        assert isinstance(result, SdkGetProductVariantResponseValue200ApplicationJson)
        request = httpx_mock.get_request()
        assert request.method == "GET"
        assert request.url.path.endswith(
            "/v2/products/test_product/variants/test_variant"
        )

    @pytest.mark.asyncio
    async def test_get_encodes_query_params(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_get_product_variant_response_value_200_application_json.json"
            )
        )
        await async_sellapp_sdk.product_variants.get(
            "test_product", "test_variant", with_drafts=True, only_drafts=True
        )
        request = httpx_mock.get_request()
        assert request.url.params["with_drafts"] == "true"
        assert request.url.params["only_drafts"] == "true"

    @pytest.mark.asyncio
    async def test_replace(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_replace_product_variant_with_put_response_value_200_application_json.json"
            )
        )
        result = await async_sellapp_sdk.product_variants.replace(
            "test_product", "test_variant"
        )
        assert isinstance(
            result, SdkReplaceProductVariantWithPutResponseValue200ApplicationJson
        )
        request = httpx_mock.get_request()
        assert request.method == "PUT"
        assert request.url.path.endswith(
            "/v2/products/test_product/variants/test_variant"
        )

    @pytest.mark.asyncio
    async def test_update(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_update_product_variant_response_value_200_application_json.json"
            )
        )
        result = await async_sellapp_sdk.product_variants.update(
            "test_product", "test_variant"
        )
        assert isinstance(
            result, SdkUpdateProductVariantResponseValue200ApplicationJson
        )
        request = httpx_mock.get_request()
        assert request.method == "PATCH"
        assert request.url.path.endswith(
            "/v2/products/test_product/variants/test_variant"
        )

    @pytest.mark.asyncio
    async def test_delete(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(status_code=200, content=b"")
        result = await async_sellapp_sdk.product_variants.delete(
            "test_product", "test_variant"
        )
        assert result is None
        request = httpx_mock.get_request()
        assert request.method == "DELETE"
        assert request.url.path.endswith(
            "/v2/products/test_product/variants/test_variant"
        )

    @pytest.mark.asyncio
    async def test_search(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "list_search_product_variants_response_value_200_application_json_property_data_item.json"
            )
        )
        page = await async_sellapp_sdk.product_variants.search("test_product")
        assert isinstance(page, AsyncPage)
        assert len(page.data) == 1
        assert isinstance(
            page.data[0],
            SearchProductVariantsResponseValue200ApplicationJsonPropertyDataItem,
        )

    @pytest.mark.asyncio
    async def test_search_empty_page(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(json={"data": [], "list_metadata": {}})
        page = await async_sellapp_sdk.product_variants.search("test_product")
        assert isinstance(page, AsyncPage)
        assert page.data == []

    @pytest.mark.asyncio
    async def test_batch_create(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_batch_create_product_variants_response_value_200_application_json.json"
            )
        )
        result = await async_sellapp_sdk.product_variants.batch_create(
            "test_product",
            resources=[
                BatchCreateProductVariantsRequestApplicationJsonPropertyResourcesItem.from_dict(
                    load_fixture(
                        "batch_create_product_variants_request_application_json_property_resources_item.json"
                    )
                )
            ],
        )
        assert isinstance(
            result, SdkBatchCreateProductVariantsResponseValue200ApplicationJson
        )
        request = httpx_mock.get_request()
        assert request.method == "POST"
        assert request.url.path.endswith("/v2/products/test_product/variants/batch")

    @pytest.mark.asyncio
    async def test_batch_update(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_batch_update_product_variants_response_value_200_application_json.json"
            )
        )
        result = await async_sellapp_sdk.product_variants.batch_update(
            "test_product",
            resources=BatchUpdateProductVariantsRequestApplicationJsonPropertyResources.from_dict(
                load_fixture(
                    "batch_update_product_variants_request_application_json_property_resources.json"
                )
            ),
        )
        assert isinstance(
            result, SdkBatchUpdateProductVariantsResponseValue200ApplicationJson
        )
        request = httpx_mock.get_request()
        assert request.method == "PATCH"
        assert request.url.path.endswith("/v2/products/test_product/variants/batch")

    @pytest.mark.asyncio
    async def test_batch_delete(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(status_code=200, content=b"")
        result = await async_sellapp_sdk.product_variants.batch_delete(
            "test_product", resources=[1]
        )
        assert result is None
        request = httpx_mock.get_request()
        assert request.method == "DELETE"
        assert request.url.path.endswith("/v2/products/test_product/variants/batch")

    @pytest.mark.asyncio
    async def test_list_with_request_options(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(json={"data": [], "list_metadata": {}})
        await async_sellapp_sdk.product_variants.list(
            "test_product", request_options={"headers": {"X-Custom": "value"}}
        )
        request = httpx_mock.get_request()
        assert request.headers["X-Custom"] == "value"

    @pytest.mark.asyncio
    async def test_list_unauthorized(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(status_code=401, json={"message": "Unauthorized"})
        with pytest.raises(AuthenticationError):
            await async_sellapp_sdk.product_variants.list("test_product")

    @pytest.mark.asyncio
    async def test_list_not_found(self, httpx_mock):
        sellapp_sdk = AsyncSellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=404, json={"message": "Not found"})
            with pytest.raises(NotFoundError):
                await sellapp_sdk.product_variants.list("test_product")
        finally:
            await sellapp_sdk.close()

    @pytest.mark.asyncio
    async def test_list_rate_limited(self, httpx_mock):
        sellapp_sdk = AsyncSellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(
                status_code=429,
                headers={"Retry-After": "0"},
                json={"message": "Slow down"},
            )
            with pytest.raises(RateLimitExceededError):
                await sellapp_sdk.product_variants.list("test_product")
        finally:
            await sellapp_sdk.close()

    @pytest.mark.asyncio
    async def test_list_server_error(self, httpx_mock):
        sellapp_sdk = AsyncSellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=500, json={"message": "Server error"})
            with pytest.raises(ServerError):
                await sellapp_sdk.product_variants.list("test_product")
        finally:
            await sellapp_sdk.close()

    @pytest.mark.asyncio
    async def test_list_bad_request(self, httpx_mock):
        sellapp_sdk = AsyncSellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=400, json={"message": "Bad request"})
            with pytest.raises(BadRequestError):
                await sellapp_sdk.product_variants.list("test_product")
        finally:
            await sellapp_sdk.close()

    @pytest.mark.asyncio
    async def test_list_unprocessable(self, httpx_mock):
        sellapp_sdk = AsyncSellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=422, json={"message": "Unprocessable"})
            with pytest.raises(UnprocessableEntityError):
                await sellapp_sdk.product_variants.list("test_product")
        finally:
            await sellapp_sdk.close()
