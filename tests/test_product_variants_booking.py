# This file is auto-generated by oagen. Do not edit.

import json

import pytest

from sellapp_sdk import AsyncSellAppClient, SellAppClient
from sellapp_sdk._errors import (
    AuthenticationError,
    BadRequestError,
    NotFoundError,
    RateLimitExceededError,
    ServerError,
    UnprocessableEntityError,
)
from sellapp_sdk.bookings.models import (
    SdkGetBookingConfigurationResponseValue200ApplicationJson,
    SdkReplaceBookingConfigurationResponseValue200ApplicationJson,
    SdkUpdateBookingConfigurationResponseValue200ApplicationJson,
)
from sellapp_sdk.product_variants.models import (
    SdkCreateBookingHoldResponseValue201ApplicationJson,
    SdkListBookingAvailabilityResponseValue200ApplicationJson,
)
from tests.generated_helpers import load_fixture


class TestProductVariantsBooking:
    def test_list_availability(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_list_booking_availability_response_value_200_application_json.json"
            ),
        )
        result = sellapp_sdk.product_variants_booking.list_availability(
            "test_product", "test_variant"
        )
        assert isinstance(
            result, SdkListBookingAvailabilityResponseValue200ApplicationJson
        )
        request = httpx_mock.get_request()
        assert request.method == "GET"
        assert request.url.path.endswith(
            "/v2/products/test_product/variants/test_variant/booking/availability"
        )

    def test_list_availability_encodes_query_params(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_list_booking_availability_response_value_200_application_json.json"
            )
        )
        sellapp_sdk.product_variants_booking.list_availability(
            "test_product",
            "test_variant",
            from_="value from/test",
            to="value to/test",
            quantity=7,
        )
        request = httpx_mock.get_request()
        assert request.url.params["from"] == "value from/test"
        assert request.url.params["to"] == "value to/test"
        assert request.url.params["quantity"] == "7"

    def test_create_hold(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_create_booking_hold_response_value_201_application_json.json"
            ),
        )
        result = sellapp_sdk.product_variants_booking.create_hold(
            "test_product", "test_variant", slot_start_at="2023-01-01T00:00:00.000Z"
        )
        assert isinstance(result, SdkCreateBookingHoldResponseValue201ApplicationJson)
        request = httpx_mock.get_request()
        assert request.method == "POST"
        assert request.url.path.endswith(
            "/v2/products/test_product/variants/test_variant/booking/holds"
        )
        body = json.loads(request.content)
        assert body["slot_start_at"] == "2023-01-01T00:00:00.000Z"

    def test_release_hold(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(status_code=200, content=b"")
        result = sellapp_sdk.product_variants_booking.release_hold(
            "test_product", "test_hold"
        )
        assert result is None
        request = httpx_mock.get_request()
        assert request.method == "DELETE"
        assert request.url.path.endswith(
            "/v2/products/test_product/variants/booking/holds/test_hold"
        )

    def test_get(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_get_booking_configuration_response_value_200_application_json.json"
            ),
        )
        result = sellapp_sdk.product_variants_booking.get(
            "test_product", "test_variant"
        )
        assert isinstance(
            result, SdkGetBookingConfigurationResponseValue200ApplicationJson
        )
        request = httpx_mock.get_request()
        assert request.method == "GET"
        assert request.url.path.endswith(
            "/v2/products/test_product/variants/test_variant/booking"
        )

    def test_replace(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_replace_booking_configuration_response_value_200_application_json.json"
            ),
        )
        result = sellapp_sdk.product_variants_booking.replace(
            "test_product", "test_variant"
        )
        assert isinstance(
            result, SdkReplaceBookingConfigurationResponseValue200ApplicationJson
        )
        request = httpx_mock.get_request()
        assert request.method == "PUT"
        assert request.url.path.endswith(
            "/v2/products/test_product/variants/test_variant/booking"
        )

    def test_update(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_update_booking_configuration_response_value_200_application_json.json"
            ),
        )
        result = sellapp_sdk.product_variants_booking.update(
            "test_product", "test_variant"
        )
        assert isinstance(
            result, SdkUpdateBookingConfigurationResponseValue200ApplicationJson
        )
        request = httpx_mock.get_request()
        assert request.method == "PATCH"
        assert request.url.path.endswith(
            "/v2/products/test_product/variants/test_variant/booking"
        )

    def test_list_availability_with_request_options(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_list_booking_availability_response_value_200_application_json.json"
            )
        )
        sellapp_sdk.product_variants_booking.list_availability(
            "test_product",
            "test_variant",
            request_options={"headers": {"X-Custom": "value"}},
        )
        request = httpx_mock.get_request()
        assert request.headers["X-Custom"] == "value"

    def test_list_availability_unauthorized(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            status_code=401,
            json={"message": "Unauthorized"},
        )
        with pytest.raises(AuthenticationError):
            sellapp_sdk.product_variants_booking.list_availability(
                "test_product", "test_variant"
            )

    def test_list_availability_not_found(self, httpx_mock):
        sellapp_sdk = SellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=404, json={"message": "Not found"})
            with pytest.raises(NotFoundError):
                sellapp_sdk.product_variants_booking.list_availability(
                    "test_product", "test_variant"
                )
        finally:
            sellapp_sdk.close()

    def test_list_availability_rate_limited(self, httpx_mock):
        sellapp_sdk = SellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(
                status_code=429,
                headers={"Retry-After": "0"},
                json={"message": "Slow down"},
            )
            with pytest.raises(RateLimitExceededError):
                sellapp_sdk.product_variants_booking.list_availability(
                    "test_product", "test_variant"
                )
        finally:
            sellapp_sdk.close()

    def test_list_availability_server_error(self, httpx_mock):
        sellapp_sdk = SellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=500, json={"message": "Server error"})
            with pytest.raises(ServerError):
                sellapp_sdk.product_variants_booking.list_availability(
                    "test_product", "test_variant"
                )
        finally:
            sellapp_sdk.close()

    def test_list_availability_bad_request(self, httpx_mock):
        sellapp_sdk = SellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=400, json={"message": "Bad request"})
            with pytest.raises(BadRequestError):
                sellapp_sdk.product_variants_booking.list_availability(
                    "test_product", "test_variant"
                )
        finally:
            sellapp_sdk.close()

    def test_list_availability_unprocessable(self, httpx_mock):
        sellapp_sdk = SellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=422, json={"message": "Unprocessable"})
            with pytest.raises(UnprocessableEntityError):
                sellapp_sdk.product_variants_booking.list_availability(
                    "test_product", "test_variant"
                )
        finally:
            sellapp_sdk.close()


class TestAsyncProductVariantsBooking:
    @pytest.mark.asyncio
    async def test_list_availability(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_list_booking_availability_response_value_200_application_json.json"
            )
        )
        result = await async_sellapp_sdk.product_variants_booking.list_availability(
            "test_product", "test_variant"
        )
        assert isinstance(
            result, SdkListBookingAvailabilityResponseValue200ApplicationJson
        )
        request = httpx_mock.get_request()
        assert request.method == "GET"
        assert request.url.path.endswith(
            "/v2/products/test_product/variants/test_variant/booking/availability"
        )

    @pytest.mark.asyncio
    async def test_list_availability_encodes_query_params(
        self, async_sellapp_sdk, httpx_mock
    ):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_list_booking_availability_response_value_200_application_json.json"
            )
        )
        await async_sellapp_sdk.product_variants_booking.list_availability(
            "test_product",
            "test_variant",
            from_="value from/test",
            to="value to/test",
            quantity=7,
        )
        request = httpx_mock.get_request()
        assert request.url.params["from"] == "value from/test"
        assert request.url.params["to"] == "value to/test"
        assert request.url.params["quantity"] == "7"

    @pytest.mark.asyncio
    async def test_create_hold(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_create_booking_hold_response_value_201_application_json.json"
            )
        )
        result = await async_sellapp_sdk.product_variants_booking.create_hold(
            "test_product", "test_variant", slot_start_at="2023-01-01T00:00:00.000Z"
        )
        assert isinstance(result, SdkCreateBookingHoldResponseValue201ApplicationJson)
        request = httpx_mock.get_request()
        assert request.method == "POST"
        assert request.url.path.endswith(
            "/v2/products/test_product/variants/test_variant/booking/holds"
        )

    @pytest.mark.asyncio
    async def test_release_hold(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(status_code=200, content=b"")
        result = await async_sellapp_sdk.product_variants_booking.release_hold(
            "test_product", "test_hold"
        )
        assert result is None
        request = httpx_mock.get_request()
        assert request.method == "DELETE"
        assert request.url.path.endswith(
            "/v2/products/test_product/variants/booking/holds/test_hold"
        )

    @pytest.mark.asyncio
    async def test_get(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_get_booking_configuration_response_value_200_application_json.json"
            )
        )
        result = await async_sellapp_sdk.product_variants_booking.get(
            "test_product", "test_variant"
        )
        assert isinstance(
            result, SdkGetBookingConfigurationResponseValue200ApplicationJson
        )
        request = httpx_mock.get_request()
        assert request.method == "GET"
        assert request.url.path.endswith(
            "/v2/products/test_product/variants/test_variant/booking"
        )

    @pytest.mark.asyncio
    async def test_replace(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_replace_booking_configuration_response_value_200_application_json.json"
            )
        )
        result = await async_sellapp_sdk.product_variants_booking.replace(
            "test_product", "test_variant"
        )
        assert isinstance(
            result, SdkReplaceBookingConfigurationResponseValue200ApplicationJson
        )
        request = httpx_mock.get_request()
        assert request.method == "PUT"
        assert request.url.path.endswith(
            "/v2/products/test_product/variants/test_variant/booking"
        )

    @pytest.mark.asyncio
    async def test_update(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_update_booking_configuration_response_value_200_application_json.json"
            )
        )
        result = await async_sellapp_sdk.product_variants_booking.update(
            "test_product", "test_variant"
        )
        assert isinstance(
            result, SdkUpdateBookingConfigurationResponseValue200ApplicationJson
        )
        request = httpx_mock.get_request()
        assert request.method == "PATCH"
        assert request.url.path.endswith(
            "/v2/products/test_product/variants/test_variant/booking"
        )

    @pytest.mark.asyncio
    async def test_list_availability_with_request_options(
        self, async_sellapp_sdk, httpx_mock
    ):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_list_booking_availability_response_value_200_application_json.json"
            )
        )
        await async_sellapp_sdk.product_variants_booking.list_availability(
            "test_product",
            "test_variant",
            request_options={"headers": {"X-Custom": "value"}},
        )
        request = httpx_mock.get_request()
        assert request.headers["X-Custom"] == "value"

    @pytest.mark.asyncio
    async def test_list_availability_unauthorized(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(status_code=401, json={"message": "Unauthorized"})
        with pytest.raises(AuthenticationError):
            await async_sellapp_sdk.product_variants_booking.list_availability(
                "test_product", "test_variant"
            )

    @pytest.mark.asyncio
    async def test_list_availability_not_found(self, httpx_mock):
        sellapp_sdk = AsyncSellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=404, json={"message": "Not found"})
            with pytest.raises(NotFoundError):
                await sellapp_sdk.product_variants_booking.list_availability(
                    "test_product", "test_variant"
                )
        finally:
            await sellapp_sdk.close()

    @pytest.mark.asyncio
    async def test_list_availability_rate_limited(self, httpx_mock):
        sellapp_sdk = AsyncSellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(
                status_code=429,
                headers={"Retry-After": "0"},
                json={"message": "Slow down"},
            )
            with pytest.raises(RateLimitExceededError):
                await sellapp_sdk.product_variants_booking.list_availability(
                    "test_product", "test_variant"
                )
        finally:
            await sellapp_sdk.close()

    @pytest.mark.asyncio
    async def test_list_availability_server_error(self, httpx_mock):
        sellapp_sdk = AsyncSellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=500, json={"message": "Server error"})
            with pytest.raises(ServerError):
                await sellapp_sdk.product_variants_booking.list_availability(
                    "test_product", "test_variant"
                )
        finally:
            await sellapp_sdk.close()

    @pytest.mark.asyncio
    async def test_list_availability_bad_request(self, httpx_mock):
        sellapp_sdk = AsyncSellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=400, json={"message": "Bad request"})
            with pytest.raises(BadRequestError):
                await sellapp_sdk.product_variants_booking.list_availability(
                    "test_product", "test_variant"
                )
        finally:
            await sellapp_sdk.close()

    @pytest.mark.asyncio
    async def test_list_availability_unprocessable(self, httpx_mock):
        sellapp_sdk = AsyncSellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=422, json={"message": "Unprocessable"})
            with pytest.raises(UnprocessableEntityError):
                await sellapp_sdk.product_variants_booking.list_availability(
                    "test_product", "test_variant"
                )
        finally:
            await sellapp_sdk.close()
