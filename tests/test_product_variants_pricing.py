# This file is auto-generated by oagen. Do not edit.

import json

import pytest

from sellapp_sdk import AsyncSellAppClient, SellAppClient
from sellapp_sdk._errors import (
    AuthenticationError,
    BadRequestError,
    NotFoundError,
    RateLimitExceededError,
    ServerError,
    UnprocessableEntityError,
)
from sellapp_sdk.common.models import (
    SdkReplaceProductVariantPricingRequestApplicationJsonPaymentMethods,
)
from sellapp_sdk.product_variants.models import (
    ReplaceProductVariantPricingRequestApplicationJsonPropertyPricing,
    SdkReplaceProductVariantPricingResponseValue200ApplicationJson,
    SdkUpdateProductVariantPricingResponseValue200ApplicationJson,
)
from tests.generated_helpers import load_fixture


class TestProductVariantsPricing:
    def test_replace(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_replace_product_variant_pricing_response_value_200_application_json.json"
            ),
        )
        result = sellapp_sdk.product_variants_pricing.replace(
            "test_product",
            "test_variant",
            pricing=ReplaceProductVariantPricingRequestApplicationJsonPropertyPricing.from_dict(
                load_fixture(
                    "replace_product_variant_pricing_request_application_json_property_pricing.json"
                )
            ),
            payment_methods=[
                SdkReplaceProductVariantPricingRequestApplicationJsonPaymentMethods(
                    "AUTHNET"
                )
            ],
        )
        assert isinstance(
            result, SdkReplaceProductVariantPricingResponseValue200ApplicationJson
        )
        request = httpx_mock.get_request()
        assert request.method == "PUT"
        assert request.url.path.endswith(
            "/v2/products/test_product/variants/test_variant/pricing"
        )
        body = json.loads(request.content)
        assert "pricing" in body
        assert "payment_methods" in body

    def test_update(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_update_product_variant_pricing_response_value_200_application_json.json"
            ),
        )
        result = sellapp_sdk.product_variants_pricing.update(
            "test_product", "test_variant"
        )
        assert isinstance(
            result, SdkUpdateProductVariantPricingResponseValue200ApplicationJson
        )
        request = httpx_mock.get_request()
        assert request.method == "PATCH"
        assert request.url.path.endswith(
            "/v2/products/test_product/variants/test_variant/pricing"
        )

    def test_replace_with_request_options(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_replace_product_variant_pricing_response_value_200_application_json.json"
            )
        )
        sellapp_sdk.product_variants_pricing.replace(
            "test_product",
            "test_variant",
            pricing=ReplaceProductVariantPricingRequestApplicationJsonPropertyPricing.from_dict(
                load_fixture(
                    "replace_product_variant_pricing_request_application_json_property_pricing.json"
                )
            ),
            payment_methods=[
                SdkReplaceProductVariantPricingRequestApplicationJsonPaymentMethods(
                    "AUTHNET"
                )
            ],
            request_options={"headers": {"X-Custom": "value"}},
        )
        request = httpx_mock.get_request()
        assert request.headers["X-Custom"] == "value"

    def test_replace_unauthorized(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            status_code=401,
            json={"message": "Unauthorized"},
        )
        with pytest.raises(AuthenticationError):
            sellapp_sdk.product_variants_pricing.replace(
                "test_product",
                "test_variant",
                pricing=ReplaceProductVariantPricingRequestApplicationJsonPropertyPricing.from_dict(
                    load_fixture(
                        "replace_product_variant_pricing_request_application_json_property_pricing.json"
                    )
                ),
                payment_methods=[
                    SdkReplaceProductVariantPricingRequestApplicationJsonPaymentMethods(
                        "AUTHNET"
                    )
                ],
            )

    def test_replace_not_found(self, httpx_mock):
        sellapp_sdk = SellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=404, json={"message": "Not found"})
            with pytest.raises(NotFoundError):
                sellapp_sdk.product_variants_pricing.replace(
                    "test_product",
                    "test_variant",
                    pricing=ReplaceProductVariantPricingRequestApplicationJsonPropertyPricing.from_dict(
                        load_fixture(
                            "replace_product_variant_pricing_request_application_json_property_pricing.json"
                        )
                    ),
                    payment_methods=[
                        SdkReplaceProductVariantPricingRequestApplicationJsonPaymentMethods(
                            "AUTHNET"
                        )
                    ],
                )
        finally:
            sellapp_sdk.close()

    def test_replace_rate_limited(self, httpx_mock):
        sellapp_sdk = SellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(
                status_code=429,
                headers={"Retry-After": "0"},
                json={"message": "Slow down"},
            )
            with pytest.raises(RateLimitExceededError):
                sellapp_sdk.product_variants_pricing.replace(
                    "test_product",
                    "test_variant",
                    pricing=ReplaceProductVariantPricingRequestApplicationJsonPropertyPricing.from_dict(
                        load_fixture(
                            "replace_product_variant_pricing_request_application_json_property_pricing.json"
                        )
                    ),
                    payment_methods=[
                        SdkReplaceProductVariantPricingRequestApplicationJsonPaymentMethods(
                            "AUTHNET"
                        )
                    ],
                )
        finally:
            sellapp_sdk.close()

    def test_replace_server_error(self, httpx_mock):
        sellapp_sdk = SellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=500, json={"message": "Server error"})
            with pytest.raises(ServerError):
                sellapp_sdk.product_variants_pricing.replace(
                    "test_product",
                    "test_variant",
                    pricing=ReplaceProductVariantPricingRequestApplicationJsonPropertyPricing.from_dict(
                        load_fixture(
                            "replace_product_variant_pricing_request_application_json_property_pricing.json"
                        )
                    ),
                    payment_methods=[
                        SdkReplaceProductVariantPricingRequestApplicationJsonPaymentMethods(
                            "AUTHNET"
                        )
                    ],
                )
        finally:
            sellapp_sdk.close()

    def test_replace_bad_request(self, httpx_mock):
        sellapp_sdk = SellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=400, json={"message": "Bad request"})
            with pytest.raises(BadRequestError):
                sellapp_sdk.product_variants_pricing.replace(
                    "test_product",
                    "test_variant",
                    pricing=ReplaceProductVariantPricingRequestApplicationJsonPropertyPricing.from_dict(
                        load_fixture(
                            "replace_product_variant_pricing_request_application_json_property_pricing.json"
                        )
                    ),
                    payment_methods=[
                        SdkReplaceProductVariantPricingRequestApplicationJsonPaymentMethods(
                            "AUTHNET"
                        )
                    ],
                )
        finally:
            sellapp_sdk.close()

    def test_replace_unprocessable(self, httpx_mock):
        sellapp_sdk = SellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=422, json={"message": "Unprocessable"})
            with pytest.raises(UnprocessableEntityError):
                sellapp_sdk.product_variants_pricing.replace(
                    "test_product",
                    "test_variant",
                    pricing=ReplaceProductVariantPricingRequestApplicationJsonPropertyPricing.from_dict(
                        load_fixture(
                            "replace_product_variant_pricing_request_application_json_property_pricing.json"
                        )
                    ),
                    payment_methods=[
                        SdkReplaceProductVariantPricingRequestApplicationJsonPaymentMethods(
                            "AUTHNET"
                        )
                    ],
                )
        finally:
            sellapp_sdk.close()


class TestAsyncProductVariantsPricing:
    @pytest.mark.asyncio
    async def test_replace(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_replace_product_variant_pricing_response_value_200_application_json.json"
            )
        )
        result = await async_sellapp_sdk.product_variants_pricing.replace(
            "test_product",
            "test_variant",
            pricing=ReplaceProductVariantPricingRequestApplicationJsonPropertyPricing.from_dict(
                load_fixture(
                    "replace_product_variant_pricing_request_application_json_property_pricing.json"
                )
            ),
            payment_methods=[
                SdkReplaceProductVariantPricingRequestApplicationJsonPaymentMethods(
                    "AUTHNET"
                )
            ],
        )
        assert isinstance(
            result, SdkReplaceProductVariantPricingResponseValue200ApplicationJson
        )
        request = httpx_mock.get_request()
        assert request.method == "PUT"
        assert request.url.path.endswith(
            "/v2/products/test_product/variants/test_variant/pricing"
        )

    @pytest.mark.asyncio
    async def test_update(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_update_product_variant_pricing_response_value_200_application_json.json"
            )
        )
        result = await async_sellapp_sdk.product_variants_pricing.update(
            "test_product", "test_variant"
        )
        assert isinstance(
            result, SdkUpdateProductVariantPricingResponseValue200ApplicationJson
        )
        request = httpx_mock.get_request()
        assert request.method == "PATCH"
        assert request.url.path.endswith(
            "/v2/products/test_product/variants/test_variant/pricing"
        )

    @pytest.mark.asyncio
    async def test_replace_with_request_options(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_replace_product_variant_pricing_response_value_200_application_json.json"
            )
        )
        await async_sellapp_sdk.product_variants_pricing.replace(
            "test_product",
            "test_variant",
            pricing=ReplaceProductVariantPricingRequestApplicationJsonPropertyPricing.from_dict(
                load_fixture(
                    "replace_product_variant_pricing_request_application_json_property_pricing.json"
                )
            ),
            payment_methods=[
                SdkReplaceProductVariantPricingRequestApplicationJsonPaymentMethods(
                    "AUTHNET"
                )
            ],
            request_options={"headers": {"X-Custom": "value"}},
        )
        request = httpx_mock.get_request()
        assert request.headers["X-Custom"] == "value"

    @pytest.mark.asyncio
    async def test_replace_unauthorized(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(status_code=401, json={"message": "Unauthorized"})
        with pytest.raises(AuthenticationError):
            await async_sellapp_sdk.product_variants_pricing.replace(
                "test_product",
                "test_variant",
                pricing=ReplaceProductVariantPricingRequestApplicationJsonPropertyPricing.from_dict(
                    load_fixture(
                        "replace_product_variant_pricing_request_application_json_property_pricing.json"
                    )
                ),
                payment_methods=[
                    SdkReplaceProductVariantPricingRequestApplicationJsonPaymentMethods(
                        "AUTHNET"
                    )
                ],
            )

    @pytest.mark.asyncio
    async def test_replace_not_found(self, httpx_mock):
        sellapp_sdk = AsyncSellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=404, json={"message": "Not found"})
            with pytest.raises(NotFoundError):
                await sellapp_sdk.product_variants_pricing.replace(
                    "test_product",
                    "test_variant",
                    pricing=ReplaceProductVariantPricingRequestApplicationJsonPropertyPricing.from_dict(
                        load_fixture(
                            "replace_product_variant_pricing_request_application_json_property_pricing.json"
                        )
                    ),
                    payment_methods=[
                        SdkReplaceProductVariantPricingRequestApplicationJsonPaymentMethods(
                            "AUTHNET"
                        )
                    ],
                )
        finally:
            await sellapp_sdk.close()

    @pytest.mark.asyncio
    async def test_replace_rate_limited(self, httpx_mock):
        sellapp_sdk = AsyncSellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(
                status_code=429,
                headers={"Retry-After": "0"},
                json={"message": "Slow down"},
            )
            with pytest.raises(RateLimitExceededError):
                await sellapp_sdk.product_variants_pricing.replace(
                    "test_product",
                    "test_variant",
                    pricing=ReplaceProductVariantPricingRequestApplicationJsonPropertyPricing.from_dict(
                        load_fixture(
                            "replace_product_variant_pricing_request_application_json_property_pricing.json"
                        )
                    ),
                    payment_methods=[
                        SdkReplaceProductVariantPricingRequestApplicationJsonPaymentMethods(
                            "AUTHNET"
                        )
                    ],
                )
        finally:
            await sellapp_sdk.close()

    @pytest.mark.asyncio
    async def test_replace_server_error(self, httpx_mock):
        sellapp_sdk = AsyncSellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=500, json={"message": "Server error"})
            with pytest.raises(ServerError):
                await sellapp_sdk.product_variants_pricing.replace(
                    "test_product",
                    "test_variant",
                    pricing=ReplaceProductVariantPricingRequestApplicationJsonPropertyPricing.from_dict(
                        load_fixture(
                            "replace_product_variant_pricing_request_application_json_property_pricing.json"
                        )
                    ),
                    payment_methods=[
                        SdkReplaceProductVariantPricingRequestApplicationJsonPaymentMethods(
                            "AUTHNET"
                        )
                    ],
                )
        finally:
            await sellapp_sdk.close()

    @pytest.mark.asyncio
    async def test_replace_bad_request(self, httpx_mock):
        sellapp_sdk = AsyncSellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=400, json={"message": "Bad request"})
            with pytest.raises(BadRequestError):
                await sellapp_sdk.product_variants_pricing.replace(
                    "test_product",
                    "test_variant",
                    pricing=ReplaceProductVariantPricingRequestApplicationJsonPropertyPricing.from_dict(
                        load_fixture(
                            "replace_product_variant_pricing_request_application_json_property_pricing.json"
                        )
                    ),
                    payment_methods=[
                        SdkReplaceProductVariantPricingRequestApplicationJsonPaymentMethods(
                            "AUTHNET"
                        )
                    ],
                )
        finally:
            await sellapp_sdk.close()

    @pytest.mark.asyncio
    async def test_replace_unprocessable(self, httpx_mock):
        sellapp_sdk = AsyncSellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=422, json={"message": "Unprocessable"})
            with pytest.raises(UnprocessableEntityError):
                await sellapp_sdk.product_variants_pricing.replace(
                    "test_product",
                    "test_variant",
                    pricing=ReplaceProductVariantPricingRequestApplicationJsonPropertyPricing.from_dict(
                        load_fixture(
                            "replace_product_variant_pricing_request_application_json_property_pricing.json"
                        )
                    ),
                    payment_methods=[
                        SdkReplaceProductVariantPricingRequestApplicationJsonPaymentMethods(
                            "AUTHNET"
                        )
                    ],
                )
        finally:
            await sellapp_sdk.close()
