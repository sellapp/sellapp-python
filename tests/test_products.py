# This file is auto-generated by oagen. Do not edit.

import json

import pytest

from sellapp_sdk import AsyncSellAppClient, SellAppClient
from sellapp_sdk._errors import (
    AuthenticationError,
    BadRequestError,
    NotFoundError,
    RateLimitExceededError,
    ServerError,
    UnprocessableEntityError,
)
from sellapp_sdk._pagination import AsyncPage, SyncPage
from sellapp_sdk.common.models import (
    CatalogVisibility,
    ListProductsResponseValue200ApplicationJsonPropertyDataItem,
    SdkBatchCreateProductsResponseValue200ApplicationJson,
)
from sellapp_sdk.products.models import (
    BatchCreateProductsRequestApplicationJsonPropertyResourcesItem,
    BatchUpdateProductsRequestApplicationJsonPropertyResources,
    SdkBatchUpdateProductsResponseValue200ApplicationJson,
    SdkCreateProductResponseValue201ApplicationJson,
    SdkGetProductResponseValue200ApplicationJson,
    SdkReplaceProductResponseValue200ApplicationJson,
    SdkUpdateProductResponseValue200ApplicationJson,
    SearchProductsResponseValue200ApplicationJsonPropertyDataItem,
)
from tests.generated_helpers import load_fixture


class TestProducts:
    def test_list(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "list_list_products_response_value_200_application_json_property_data_item.json"
            ),
        )
        page = sellapp_sdk.products.list()
        assert isinstance(page, SyncPage)
        assert len(page.data) == 1
        assert isinstance(
            page.data[0], ListProductsResponseValue200ApplicationJsonPropertyDataItem
        )

    def test_list_empty_page(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(json={"data": [], "list_metadata": {}})
        page = sellapp_sdk.products.list()
        assert isinstance(page, SyncPage)
        assert page.data == []

    def test_list_encodes_query_params(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(json={"data": [], "list_metadata": {}})
        sellapp_sdk.products.list(
            limit=10,
            before="cursor before",
            after="cursor/after",
            page=7,
            with_drafts=True,
            only_drafts=True,
        )
        request = httpx_mock.get_request()
        assert request.url.params["limit"] == "10"
        assert request.url.params["before"] == "cursor before"
        assert request.url.params["after"] == "cursor/after"
        assert request.url.params["page"] == "7"
        assert request.url.params["with_drafts"] == "true"
        assert request.url.params["only_drafts"] == "true"

    def test_create(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_create_product_response_value_201_application_json.json"
            ),
        )
        result = sellapp_sdk.products.create(
            title="test_title",
            description="test_description",
            visibility=CatalogVisibility("PUBLIC"),
        )
        assert isinstance(result, SdkCreateProductResponseValue201ApplicationJson)
        request = httpx_mock.get_request()
        assert request.method == "POST"
        assert request.url.path.endswith("/v2/products")
        body = json.loads(request.content)
        assert body["title"] == "test_title"
        assert body["description"] == "test_description"
        assert body["visibility"] == CatalogVisibility("PUBLIC")

    def test_get(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_get_product_response_value_200_application_json.json"
            ),
        )
        result = sellapp_sdk.products.get("test_product")
        assert isinstance(result, SdkGetProductResponseValue200ApplicationJson)
        request = httpx_mock.get_request()
        assert request.method == "GET"
        assert request.url.path.endswith("/v2/products/test_product")

    def test_get_encodes_query_params(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_get_product_response_value_200_application_json.json"
            )
        )
        sellapp_sdk.products.get("test_product", with_drafts=True, only_drafts=True)
        request = httpx_mock.get_request()
        assert request.url.params["with_drafts"] == "true"
        assert request.url.params["only_drafts"] == "true"

    def test_replace(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_replace_product_response_value_200_application_json.json"
            ),
        )
        result = sellapp_sdk.products.replace("test_product")
        assert isinstance(result, SdkReplaceProductResponseValue200ApplicationJson)
        request = httpx_mock.get_request()
        assert request.method == "PUT"
        assert request.url.path.endswith("/v2/products/test_product")

    def test_update(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_update_product_response_value_200_application_json.json"
            ),
        )
        result = sellapp_sdk.products.update("test_product")
        assert isinstance(result, SdkUpdateProductResponseValue200ApplicationJson)
        request = httpx_mock.get_request()
        assert request.method == "PATCH"
        assert request.url.path.endswith("/v2/products/test_product")

    def test_delete(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(status_code=200, content=b"")
        result = sellapp_sdk.products.delete("test_product")
        assert result is None
        request = httpx_mock.get_request()
        assert request.method == "DELETE"
        assert request.url.path.endswith("/v2/products/test_product")

    def test_search(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "list_search_products_response_value_200_application_json_property_data_item.json"
            ),
        )
        page = sellapp_sdk.products.search()
        assert isinstance(page, SyncPage)
        assert len(page.data) == 1
        assert isinstance(
            page.data[0], SearchProductsResponseValue200ApplicationJsonPropertyDataItem
        )

    def test_search_empty_page(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(json={"data": [], "list_metadata": {}})
        page = sellapp_sdk.products.search()
        assert isinstance(page, SyncPage)
        assert page.data == []

    def test_search_encodes_query_params(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(json={"data": [], "list_metadata": {}})
        sellapp_sdk.products.search(
            limit=10, before="cursor before", after="cursor/after", page=7
        )
        request = httpx_mock.get_request()
        assert request.url.params["limit"] == "10"
        assert request.url.params["before"] == "cursor before"
        assert request.url.params["after"] == "cursor/after"
        assert request.url.params["page"] == "7"

    def test_batch_create(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_batch_create_products_response_value_200_application_json.json"
            ),
        )
        result = sellapp_sdk.products.batch_create(
            resources=[
                BatchCreateProductsRequestApplicationJsonPropertyResourcesItem.from_dict(
                    load_fixture(
                        "batch_create_products_request_application_json_property_resources_item.json"
                    )
                )
            ]
        )
        assert isinstance(result, SdkBatchCreateProductsResponseValue200ApplicationJson)
        request = httpx_mock.get_request()
        assert request.method == "POST"
        assert request.url.path.endswith("/v2/products/batch")
        body = json.loads(request.content)
        assert "resources" in body

    def test_batch_update(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_batch_update_products_response_value_200_application_json.json"
            ),
        )
        result = sellapp_sdk.products.batch_update(
            resources=BatchUpdateProductsRequestApplicationJsonPropertyResources.from_dict(
                load_fixture(
                    "batch_update_products_request_application_json_property_resources.json"
                )
            )
        )
        assert isinstance(result, SdkBatchUpdateProductsResponseValue200ApplicationJson)
        request = httpx_mock.get_request()
        assert request.method == "PATCH"
        assert request.url.path.endswith("/v2/products/batch")
        body = json.loads(request.content)
        assert "resources" in body

    def test_batch_delete(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(status_code=200, content=b"")
        result = sellapp_sdk.products.batch_delete(resources=[1])
        assert result is None
        request = httpx_mock.get_request()
        assert request.method == "DELETE"
        assert request.url.path.endswith("/v2/products/batch")

    def test_list_with_request_options(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(json={"data": [], "list_metadata": {}})
        sellapp_sdk.products.list(request_options={"headers": {"X-Custom": "value"}})
        request = httpx_mock.get_request()
        assert request.headers["X-Custom"] == "value"

    def test_list_unauthorized(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            status_code=401,
            json={"message": "Unauthorized"},
        )
        with pytest.raises(AuthenticationError):
            sellapp_sdk.products.list()

    def test_list_not_found(self, httpx_mock):
        sellapp_sdk = SellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=404, json={"message": "Not found"})
            with pytest.raises(NotFoundError):
                sellapp_sdk.products.list()
        finally:
            sellapp_sdk.close()

    def test_list_rate_limited(self, httpx_mock):
        sellapp_sdk = SellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(
                status_code=429,
                headers={"Retry-After": "0"},
                json={"message": "Slow down"},
            )
            with pytest.raises(RateLimitExceededError):
                sellapp_sdk.products.list()
        finally:
            sellapp_sdk.close()

    def test_list_server_error(self, httpx_mock):
        sellapp_sdk = SellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=500, json={"message": "Server error"})
            with pytest.raises(ServerError):
                sellapp_sdk.products.list()
        finally:
            sellapp_sdk.close()

    def test_list_bad_request(self, httpx_mock):
        sellapp_sdk = SellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=400, json={"message": "Bad request"})
            with pytest.raises(BadRequestError):
                sellapp_sdk.products.list()
        finally:
            sellapp_sdk.close()

    def test_list_unprocessable(self, httpx_mock):
        sellapp_sdk = SellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=422, json={"message": "Unprocessable"})
            with pytest.raises(UnprocessableEntityError):
                sellapp_sdk.products.list()
        finally:
            sellapp_sdk.close()


class TestAsyncProducts:
    @pytest.mark.asyncio
    async def test_list(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "list_list_products_response_value_200_application_json_property_data_item.json"
            )
        )
        page = await async_sellapp_sdk.products.list()
        assert isinstance(page, AsyncPage)
        assert len(page.data) == 1
        assert isinstance(
            page.data[0], ListProductsResponseValue200ApplicationJsonPropertyDataItem
        )

    @pytest.mark.asyncio
    async def test_list_empty_page(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(json={"data": [], "list_metadata": {}})
        page = await async_sellapp_sdk.products.list()
        assert isinstance(page, AsyncPage)
        assert page.data == []

    @pytest.mark.asyncio
    async def test_list_encodes_query_params(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(json={"data": [], "list_metadata": {}})
        await async_sellapp_sdk.products.list(
            limit=10,
            before="cursor before",
            after="cursor/after",
            page=7,
            with_drafts=True,
            only_drafts=True,
        )
        request = httpx_mock.get_request()
        assert request.url.params["limit"] == "10"
        assert request.url.params["before"] == "cursor before"
        assert request.url.params["after"] == "cursor/after"
        assert request.url.params["page"] == "7"
        assert request.url.params["with_drafts"] == "true"
        assert request.url.params["only_drafts"] == "true"

    @pytest.mark.asyncio
    async def test_create(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_create_product_response_value_201_application_json.json"
            )
        )
        result = await async_sellapp_sdk.products.create(
            title="test_title",
            description="test_description",
            visibility=CatalogVisibility("PUBLIC"),
        )
        assert isinstance(result, SdkCreateProductResponseValue201ApplicationJson)
        request = httpx_mock.get_request()
        assert request.method == "POST"
        assert request.url.path.endswith("/v2/products")

    @pytest.mark.asyncio
    async def test_get(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_get_product_response_value_200_application_json.json"
            )
        )
        result = await async_sellapp_sdk.products.get("test_product")
        assert isinstance(result, SdkGetProductResponseValue200ApplicationJson)
        request = httpx_mock.get_request()
        assert request.method == "GET"
        assert request.url.path.endswith("/v2/products/test_product")

    @pytest.mark.asyncio
    async def test_get_encodes_query_params(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_get_product_response_value_200_application_json.json"
            )
        )
        await async_sellapp_sdk.products.get(
            "test_product", with_drafts=True, only_drafts=True
        )
        request = httpx_mock.get_request()
        assert request.url.params["with_drafts"] == "true"
        assert request.url.params["only_drafts"] == "true"

    @pytest.mark.asyncio
    async def test_replace(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_replace_product_response_value_200_application_json.json"
            )
        )
        result = await async_sellapp_sdk.products.replace("test_product")
        assert isinstance(result, SdkReplaceProductResponseValue200ApplicationJson)
        request = httpx_mock.get_request()
        assert request.method == "PUT"
        assert request.url.path.endswith("/v2/products/test_product")

    @pytest.mark.asyncio
    async def test_update(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_update_product_response_value_200_application_json.json"
            )
        )
        result = await async_sellapp_sdk.products.update("test_product")
        assert isinstance(result, SdkUpdateProductResponseValue200ApplicationJson)
        request = httpx_mock.get_request()
        assert request.method == "PATCH"
        assert request.url.path.endswith("/v2/products/test_product")

    @pytest.mark.asyncio
    async def test_delete(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(status_code=200, content=b"")
        result = await async_sellapp_sdk.products.delete("test_product")
        assert result is None
        request = httpx_mock.get_request()
        assert request.method == "DELETE"
        assert request.url.path.endswith("/v2/products/test_product")

    @pytest.mark.asyncio
    async def test_search(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "list_search_products_response_value_200_application_json_property_data_item.json"
            )
        )
        page = await async_sellapp_sdk.products.search()
        assert isinstance(page, AsyncPage)
        assert len(page.data) == 1
        assert isinstance(
            page.data[0], SearchProductsResponseValue200ApplicationJsonPropertyDataItem
        )

    @pytest.mark.asyncio
    async def test_search_empty_page(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(json={"data": [], "list_metadata": {}})
        page = await async_sellapp_sdk.products.search()
        assert isinstance(page, AsyncPage)
        assert page.data == []

    @pytest.mark.asyncio
    async def test_search_encodes_query_params(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(json={"data": [], "list_metadata": {}})
        await async_sellapp_sdk.products.search(
            limit=10, before="cursor before", after="cursor/after", page=7
        )
        request = httpx_mock.get_request()
        assert request.url.params["limit"] == "10"
        assert request.url.params["before"] == "cursor before"
        assert request.url.params["after"] == "cursor/after"
        assert request.url.params["page"] == "7"

    @pytest.mark.asyncio
    async def test_batch_create(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_batch_create_products_response_value_200_application_json.json"
            )
        )
        result = await async_sellapp_sdk.products.batch_create(
            resources=[
                BatchCreateProductsRequestApplicationJsonPropertyResourcesItem.from_dict(
                    load_fixture(
                        "batch_create_products_request_application_json_property_resources_item.json"
                    )
                )
            ]
        )
        assert isinstance(result, SdkBatchCreateProductsResponseValue200ApplicationJson)
        request = httpx_mock.get_request()
        assert request.method == "POST"
        assert request.url.path.endswith("/v2/products/batch")

    @pytest.mark.asyncio
    async def test_batch_update(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_batch_update_products_response_value_200_application_json.json"
            )
        )
        result = await async_sellapp_sdk.products.batch_update(
            resources=BatchUpdateProductsRequestApplicationJsonPropertyResources.from_dict(
                load_fixture(
                    "batch_update_products_request_application_json_property_resources.json"
                )
            )
        )
        assert isinstance(result, SdkBatchUpdateProductsResponseValue200ApplicationJson)
        request = httpx_mock.get_request()
        assert request.method == "PATCH"
        assert request.url.path.endswith("/v2/products/batch")

    @pytest.mark.asyncio
    async def test_batch_delete(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(status_code=200, content=b"")
        result = await async_sellapp_sdk.products.batch_delete(resources=[1])
        assert result is None
        request = httpx_mock.get_request()
        assert request.method == "DELETE"
        assert request.url.path.endswith("/v2/products/batch")

    @pytest.mark.asyncio
    async def test_list_with_request_options(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(json={"data": [], "list_metadata": {}})
        await async_sellapp_sdk.products.list(
            request_options={"headers": {"X-Custom": "value"}}
        )
        request = httpx_mock.get_request()
        assert request.headers["X-Custom"] == "value"

    @pytest.mark.asyncio
    async def test_list_unauthorized(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(status_code=401, json={"message": "Unauthorized"})
        with pytest.raises(AuthenticationError):
            await async_sellapp_sdk.products.list()

    @pytest.mark.asyncio
    async def test_list_not_found(self, httpx_mock):
        sellapp_sdk = AsyncSellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=404, json={"message": "Not found"})
            with pytest.raises(NotFoundError):
                await sellapp_sdk.products.list()
        finally:
            await sellapp_sdk.close()

    @pytest.mark.asyncio
    async def test_list_rate_limited(self, httpx_mock):
        sellapp_sdk = AsyncSellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(
                status_code=429,
                headers={"Retry-After": "0"},
                json={"message": "Slow down"},
            )
            with pytest.raises(RateLimitExceededError):
                await sellapp_sdk.products.list()
        finally:
            await sellapp_sdk.close()

    @pytest.mark.asyncio
    async def test_list_server_error(self, httpx_mock):
        sellapp_sdk = AsyncSellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=500, json={"message": "Server error"})
            with pytest.raises(ServerError):
                await sellapp_sdk.products.list()
        finally:
            await sellapp_sdk.close()

    @pytest.mark.asyncio
    async def test_list_bad_request(self, httpx_mock):
        sellapp_sdk = AsyncSellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=400, json={"message": "Bad request"})
            with pytest.raises(BadRequestError):
                await sellapp_sdk.products.list()
        finally:
            await sellapp_sdk.close()

    @pytest.mark.asyncio
    async def test_list_unprocessable(self, httpx_mock):
        sellapp_sdk = AsyncSellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=422, json={"message": "Unprocessable"})
            with pytest.raises(UnprocessableEntityError):
                await sellapp_sdk.products.list()
        finally:
            await sellapp_sdk.close()
