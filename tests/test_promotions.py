# This file is auto-generated by oagen. Do not edit.

import json

import pytest

from sellapp_sdk import AsyncSellAppClient, SellAppClient
from sellapp_sdk._errors import (
    AuthenticationError,
    BadRequestError,
    NotFoundError,
    RateLimitExceededError,
    ServerError,
    UnprocessableEntityError,
)
from sellapp_sdk._pagination import AsyncPage, SyncPage
from sellapp_sdk.common.models import (
    SdkCreatePromotionRequestApplicationJsonStatus,
    SdkRestorePromotionRequestApplicationJsonStatus,
)
from sellapp_sdk.promotions.models import (
    CreatePromotionRequestApplicationJsonPropertyPhasesItem,
    RestorePromotionRequestApplicationJsonPropertyPhasesItem,
    SdkCreatePromotionResponseValue201ApplicationJson,
    SdkGetPromotionResponseValue200ApplicationJson,
    SdkReplacePromotionResponseValue200ApplicationJson,
    SdkRestorePromotionResponseValue200ApplicationJson,
    SdkUpdatePromotionResponseValue200ApplicationJson,
)
from tests.generated_helpers import load_fixture


class TestPromotions:
    def test_list(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(json={"data": [], "list_metadata": {}})
        page = sellapp_sdk.promotions.list()
        assert isinstance(page, SyncPage)

    def test_list_encodes_query_params(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(json={"data": [], "list_metadata": {}})
        sellapp_sdk.promotions.list(
            limit=10,
            before="cursor before",
            after="cursor/after",
            page=7,
            pagination=True,
        )
        request = httpx_mock.get_request()
        assert request.url.params["limit"] == "10"
        assert request.url.params["before"] == "cursor before"
        assert request.url.params["after"] == "cursor/after"
        assert request.url.params["page"] == "7"
        assert request.url.params["pagination"] == "true"

    def test_create(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_create_promotion_response_value_201_application_json.json"
            ),
        )
        result = sellapp_sdk.promotions.create(
            name="test_name",
            status=SdkCreatePromotionRequestApplicationJsonStatus("active"),
            priority=1,
            is_stackable=True,
            phases=[
                CreatePromotionRequestApplicationJsonPropertyPhasesItem.from_dict(
                    load_fixture(
                        "create_promotion_request_application_json_property_phases_item.json"
                    )
                )
            ],
        )
        assert isinstance(result, SdkCreatePromotionResponseValue201ApplicationJson)
        request = httpx_mock.get_request()
        assert request.method == "POST"
        assert request.url.path.endswith("/v2/promotions")
        body = json.loads(request.content)
        assert body["name"] == "test_name"
        assert body["status"] == SdkCreatePromotionRequestApplicationJsonStatus(
            "active"
        )
        assert body["priority"] == 1
        assert body["is_stackable"] == True
        assert "phases" in body

    def test_search(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(json={"data": [], "list_metadata": {}})
        page = sellapp_sdk.promotions.search()
        assert isinstance(page, SyncPage)

    def test_search_encodes_query_params(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(json={"data": [], "list_metadata": {}})
        sellapp_sdk.promotions.search(
            limit=10,
            before="cursor before",
            after="cursor/after",
            page=7,
            pagination=True,
        )
        request = httpx_mock.get_request()
        assert request.url.params["limit"] == "10"
        assert request.url.params["before"] == "cursor before"
        assert request.url.params["after"] == "cursor/after"
        assert request.url.params["page"] == "7"
        assert request.url.params["pagination"] == "true"

    def test_get(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_get_promotion_response_value_200_application_json.json"
            ),
        )
        result = sellapp_sdk.promotions.get("test_promotion")
        assert isinstance(result, SdkGetPromotionResponseValue200ApplicationJson)
        request = httpx_mock.get_request()
        assert request.method == "GET"
        assert request.url.path.endswith("/v2/promotions/test_promotion")

    def test_replace(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_replace_promotion_response_value_200_application_json.json"
            ),
        )
        result = sellapp_sdk.promotions.replace("test_promotion")
        assert isinstance(result, SdkReplacePromotionResponseValue200ApplicationJson)
        request = httpx_mock.get_request()
        assert request.method == "PUT"
        assert request.url.path.endswith("/v2/promotions/test_promotion")

    def test_update(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_update_promotion_response_value_200_application_json.json"
            ),
        )
        result = sellapp_sdk.promotions.update("test_promotion")
        assert isinstance(result, SdkUpdatePromotionResponseValue200ApplicationJson)
        request = httpx_mock.get_request()
        assert request.method == "PATCH"
        assert request.url.path.endswith("/v2/promotions/test_promotion")

    def test_delete(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(status_code=200, content=b"")
        result = sellapp_sdk.promotions.delete("test_promotion")
        assert result is None
        request = httpx_mock.get_request()
        assert request.method == "DELETE"
        assert request.url.path.endswith("/v2/promotions/test_promotion")

    def test_restore(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_restore_promotion_response_value_200_application_json.json"
            ),
        )
        result = sellapp_sdk.promotions.restore(
            "test_promotion",
            name="test_name",
            status=SdkRestorePromotionRequestApplicationJsonStatus("active"),
            priority=1,
            is_stackable=True,
            phases=[
                RestorePromotionRequestApplicationJsonPropertyPhasesItem.from_dict(
                    load_fixture(
                        "restore_promotion_request_application_json_property_phases_item.json"
                    )
                )
            ],
        )
        assert isinstance(result, SdkRestorePromotionResponseValue200ApplicationJson)
        request = httpx_mock.get_request()
        assert request.method == "POST"
        assert request.url.path.endswith("/v2/promotions/test_promotion/restore")
        body = json.loads(request.content)
        assert body["name"] == "test_name"
        assert body["status"] == SdkRestorePromotionRequestApplicationJsonStatus(
            "active"
        )
        assert body["priority"] == 1
        assert body["is_stackable"] == True
        assert "phases" in body

    def test_list_with_request_options(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(json={"data": [], "list_metadata": {}})
        sellapp_sdk.promotions.list(request_options={"headers": {"X-Custom": "value"}})
        request = httpx_mock.get_request()
        assert request.headers["X-Custom"] == "value"

    def test_list_unauthorized(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            status_code=401,
            json={"message": "Unauthorized"},
        )
        with pytest.raises(AuthenticationError):
            sellapp_sdk.promotions.list()

    def test_list_not_found(self, httpx_mock):
        sellapp_sdk = SellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=404, json={"message": "Not found"})
            with pytest.raises(NotFoundError):
                sellapp_sdk.promotions.list()
        finally:
            sellapp_sdk.close()

    def test_list_rate_limited(self, httpx_mock):
        sellapp_sdk = SellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(
                status_code=429,
                headers={"Retry-After": "0"},
                json={"message": "Slow down"},
            )
            with pytest.raises(RateLimitExceededError):
                sellapp_sdk.promotions.list()
        finally:
            sellapp_sdk.close()

    def test_list_server_error(self, httpx_mock):
        sellapp_sdk = SellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=500, json={"message": "Server error"})
            with pytest.raises(ServerError):
                sellapp_sdk.promotions.list()
        finally:
            sellapp_sdk.close()

    def test_list_bad_request(self, httpx_mock):
        sellapp_sdk = SellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=400, json={"message": "Bad request"})
            with pytest.raises(BadRequestError):
                sellapp_sdk.promotions.list()
        finally:
            sellapp_sdk.close()

    def test_list_unprocessable(self, httpx_mock):
        sellapp_sdk = SellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=422, json={"message": "Unprocessable"})
            with pytest.raises(UnprocessableEntityError):
                sellapp_sdk.promotions.list()
        finally:
            sellapp_sdk.close()


class TestAsyncPromotions:
    @pytest.mark.asyncio
    async def test_list(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(json={"data": [], "list_metadata": {}})
        page = await async_sellapp_sdk.promotions.list()
        assert isinstance(page, AsyncPage)

    @pytest.mark.asyncio
    async def test_list_encodes_query_params(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(json={"data": [], "list_metadata": {}})
        await async_sellapp_sdk.promotions.list(
            limit=10,
            before="cursor before",
            after="cursor/after",
            page=7,
            pagination=True,
        )
        request = httpx_mock.get_request()
        assert request.url.params["limit"] == "10"
        assert request.url.params["before"] == "cursor before"
        assert request.url.params["after"] == "cursor/after"
        assert request.url.params["page"] == "7"
        assert request.url.params["pagination"] == "true"

    @pytest.mark.asyncio
    async def test_create(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_create_promotion_response_value_201_application_json.json"
            )
        )
        result = await async_sellapp_sdk.promotions.create(
            name="test_name",
            status=SdkCreatePromotionRequestApplicationJsonStatus("active"),
            priority=1,
            is_stackable=True,
            phases=[
                CreatePromotionRequestApplicationJsonPropertyPhasesItem.from_dict(
                    load_fixture(
                        "create_promotion_request_application_json_property_phases_item.json"
                    )
                )
            ],
        )
        assert isinstance(result, SdkCreatePromotionResponseValue201ApplicationJson)
        request = httpx_mock.get_request()
        assert request.method == "POST"
        assert request.url.path.endswith("/v2/promotions")

    @pytest.mark.asyncio
    async def test_search(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(json={"data": [], "list_metadata": {}})
        page = await async_sellapp_sdk.promotions.search()
        assert isinstance(page, AsyncPage)

    @pytest.mark.asyncio
    async def test_search_encodes_query_params(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(json={"data": [], "list_metadata": {}})
        await async_sellapp_sdk.promotions.search(
            limit=10,
            before="cursor before",
            after="cursor/after",
            page=7,
            pagination=True,
        )
        request = httpx_mock.get_request()
        assert request.url.params["limit"] == "10"
        assert request.url.params["before"] == "cursor before"
        assert request.url.params["after"] == "cursor/after"
        assert request.url.params["page"] == "7"
        assert request.url.params["pagination"] == "true"

    @pytest.mark.asyncio
    async def test_get(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_get_promotion_response_value_200_application_json.json"
            )
        )
        result = await async_sellapp_sdk.promotions.get("test_promotion")
        assert isinstance(result, SdkGetPromotionResponseValue200ApplicationJson)
        request = httpx_mock.get_request()
        assert request.method == "GET"
        assert request.url.path.endswith("/v2/promotions/test_promotion")

    @pytest.mark.asyncio
    async def test_replace(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_replace_promotion_response_value_200_application_json.json"
            )
        )
        result = await async_sellapp_sdk.promotions.replace("test_promotion")
        assert isinstance(result, SdkReplacePromotionResponseValue200ApplicationJson)
        request = httpx_mock.get_request()
        assert request.method == "PUT"
        assert request.url.path.endswith("/v2/promotions/test_promotion")

    @pytest.mark.asyncio
    async def test_update(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_update_promotion_response_value_200_application_json.json"
            )
        )
        result = await async_sellapp_sdk.promotions.update("test_promotion")
        assert isinstance(result, SdkUpdatePromotionResponseValue200ApplicationJson)
        request = httpx_mock.get_request()
        assert request.method == "PATCH"
        assert request.url.path.endswith("/v2/promotions/test_promotion")

    @pytest.mark.asyncio
    async def test_delete(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(status_code=200, content=b"")
        result = await async_sellapp_sdk.promotions.delete("test_promotion")
        assert result is None
        request = httpx_mock.get_request()
        assert request.method == "DELETE"
        assert request.url.path.endswith("/v2/promotions/test_promotion")

    @pytest.mark.asyncio
    async def test_restore(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_restore_promotion_response_value_200_application_json.json"
            )
        )
        result = await async_sellapp_sdk.promotions.restore(
            "test_promotion",
            name="test_name",
            status=SdkRestorePromotionRequestApplicationJsonStatus("active"),
            priority=1,
            is_stackable=True,
            phases=[
                RestorePromotionRequestApplicationJsonPropertyPhasesItem.from_dict(
                    load_fixture(
                        "restore_promotion_request_application_json_property_phases_item.json"
                    )
                )
            ],
        )
        assert isinstance(result, SdkRestorePromotionResponseValue200ApplicationJson)
        request = httpx_mock.get_request()
        assert request.method == "POST"
        assert request.url.path.endswith("/v2/promotions/test_promotion/restore")

    @pytest.mark.asyncio
    async def test_list_with_request_options(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(json={"data": [], "list_metadata": {}})
        await async_sellapp_sdk.promotions.list(
            request_options={"headers": {"X-Custom": "value"}}
        )
        request = httpx_mock.get_request()
        assert request.headers["X-Custom"] == "value"

    @pytest.mark.asyncio
    async def test_list_unauthorized(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(status_code=401, json={"message": "Unauthorized"})
        with pytest.raises(AuthenticationError):
            await async_sellapp_sdk.promotions.list()

    @pytest.mark.asyncio
    async def test_list_not_found(self, httpx_mock):
        sellapp_sdk = AsyncSellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=404, json={"message": "Not found"})
            with pytest.raises(NotFoundError):
                await sellapp_sdk.promotions.list()
        finally:
            await sellapp_sdk.close()

    @pytest.mark.asyncio
    async def test_list_rate_limited(self, httpx_mock):
        sellapp_sdk = AsyncSellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(
                status_code=429,
                headers={"Retry-After": "0"},
                json={"message": "Slow down"},
            )
            with pytest.raises(RateLimitExceededError):
                await sellapp_sdk.promotions.list()
        finally:
            await sellapp_sdk.close()

    @pytest.mark.asyncio
    async def test_list_server_error(self, httpx_mock):
        sellapp_sdk = AsyncSellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=500, json={"message": "Server error"})
            with pytest.raises(ServerError):
                await sellapp_sdk.promotions.list()
        finally:
            await sellapp_sdk.close()

    @pytest.mark.asyncio
    async def test_list_bad_request(self, httpx_mock):
        sellapp_sdk = AsyncSellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=400, json={"message": "Bad request"})
            with pytest.raises(BadRequestError):
                await sellapp_sdk.promotions.list()
        finally:
            await sellapp_sdk.close()

    @pytest.mark.asyncio
    async def test_list_unprocessable(self, httpx_mock):
        sellapp_sdk = AsyncSellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=422, json={"message": "Unprocessable"})
            with pytest.raises(UnprocessableEntityError):
                await sellapp_sdk.promotions.list()
        finally:
            await sellapp_sdk.close()
