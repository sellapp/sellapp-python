# This file is auto-generated by oagen. Do not edit.

import json

import pytest

from sellapp_sdk import AsyncSellAppClient, SellAppClient
from sellapp_sdk._errors import (
    AuthenticationError,
    BadRequestError,
    NotFoundError,
    RateLimitExceededError,
    ServerError,
    UnprocessableEntityError,
)
from sellapp_sdk._pagination import AsyncPage, SyncPage
from sellapp_sdk.blacklists.models import (
    BatchUpdateSectionsRequestApplicationJsonPropertyResources,
    SdkBatchUpdateSectionsResponseValue200ApplicationJson,
    SdkGetSectionResponseValue200ApplicationJson,
    SdkReplaceSectionGroupsResponseValue200ApplicationJson,
    SdkReplaceSectionOrderResponseValue200ApplicationJson,
    SdkReplaceSectionProductsResponseValue200ApplicationJson,
    SdkReplaceSectionResponseValue200ApplicationJson,
    SdkUpdateSectionResponseValue200ApplicationJson,
    SdkV2BatchCreateSectionsResponseValue200ApplicationJson,
    SdkV2BatchUpdateSectionsResponseValue200ApplicationJson,
    SdkV2CreateSectionResponseValue201ApplicationJson,
    SdkV2GetSectionResponseValue200ApplicationJson,
    SdkV2ReplaceSectionGroupsResponseValue200ApplicationJson,
    SdkV2ReplaceSectionOrderResponseValue200ApplicationJson,
    SdkV2ReplaceSectionProductsResponseValue200ApplicationJson,
    SdkV2ReplaceSectionResponseValue200ApplicationJson,
    SdkV2UpdateSectionResponseValue200ApplicationJson,
    SearchSectionsResponseValue200ApplicationJsonPropertyDataItem,
    V2BatchCreateSectionsRequestApplicationJsonPropertyResourcesItem,
    V2BatchUpdateSectionsRequestApplicationJsonPropertyResources,
    V2ListSectionsResponseValue200ApplicationJsonPropertyDataItem,
    V2SearchSectionsResponseValue200ApplicationJsonPropertyDataItem,
)
from sellapp_sdk.common.models import (
    BatchCreateSectionsRequestApplicationJsonPropertyResourcesItem,
    ListSectionsResponseValue200ApplicationJsonPropertyDataItem,
    SdkBatchCreateSectionsResponseValue200ApplicationJson,
    SdkCreateSectionResponseValue201ApplicationJson,
)
from tests.generated_helpers import load_fixture


class TestSections:
    def test_list(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "list_list_sections_response_value_200_application_json_property_data_item.json"
            ),
        )
        page = sellapp_sdk.sections.list()
        assert isinstance(page, SyncPage)
        assert len(page.data) == 1
        assert isinstance(
            page.data[0], ListSectionsResponseValue200ApplicationJsonPropertyDataItem
        )

    def test_list_empty_page(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(json={"data": [], "list_metadata": {}})
        page = sellapp_sdk.sections.list()
        assert isinstance(page, SyncPage)
        assert page.data == []

    def test_list_encodes_query_params(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(json={"data": [], "list_metadata": {}})
        sellapp_sdk.sections.list(
            limit=10, before="cursor before", after="cursor/after", page=7
        )
        request = httpx_mock.get_request()
        assert request.url.params["limit"] == "10"
        assert request.url.params["before"] == "cursor before"
        assert request.url.params["after"] == "cursor/after"
        assert request.url.params["page"] == "7"

    def test_create(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_create_section_response_value_201_application_json.json"
            ),
        )
        result = sellapp_sdk.sections.create(title="test_title", hidden=True)
        assert isinstance(result, SdkCreateSectionResponseValue201ApplicationJson)
        request = httpx_mock.get_request()
        assert request.method == "POST"
        assert request.url.path.endswith("/v1/sections")
        body = json.loads(request.content)
        assert body["title"] == "test_title"
        assert body["hidden"] == True

    def test_get(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_get_section_response_value_200_application_json.json"
            ),
        )
        result = sellapp_sdk.sections.get("test_section")
        assert isinstance(result, SdkGetSectionResponseValue200ApplicationJson)
        request = httpx_mock.get_request()
        assert request.method == "GET"
        assert request.url.path.endswith("/v1/sections/test_section")

    def test_replace(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_replace_section_response_value_200_application_json.json"
            ),
        )
        result = sellapp_sdk.sections.replace("test_section")
        assert isinstance(result, SdkReplaceSectionResponseValue200ApplicationJson)
        request = httpx_mock.get_request()
        assert request.method == "PUT"
        assert request.url.path.endswith("/v1/sections/test_section")

    def test_update(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_update_section_response_value_200_application_json.json"
            ),
        )
        result = sellapp_sdk.sections.update("test_section")
        assert isinstance(result, SdkUpdateSectionResponseValue200ApplicationJson)
        request = httpx_mock.get_request()
        assert request.method == "PATCH"
        assert request.url.path.endswith("/v1/sections/test_section")

    def test_delete(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(status_code=200, content=b"")
        result = sellapp_sdk.sections.delete("test_section")
        assert result is None
        request = httpx_mock.get_request()
        assert request.method == "DELETE"
        assert request.url.path.endswith("/v1/sections/test_section")

    def test_replace_order(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_replace_section_order_response_value_200_application_json.json"
            ),
        )
        result = sellapp_sdk.sections.replace_order(resources=[1])
        assert isinstance(result, SdkReplaceSectionOrderResponseValue200ApplicationJson)
        request = httpx_mock.get_request()
        assert request.method == "PUT"
        assert request.url.path.endswith("/v1/sections/order")
        body = json.loads(request.content)
        assert "resources" in body

    def test_replace_products(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_replace_section_products_response_value_200_application_json.json"
            ),
        )
        result = sellapp_sdk.sections.replace_products("test_section", resources=[1])
        assert isinstance(
            result, SdkReplaceSectionProductsResponseValue200ApplicationJson
        )
        request = httpx_mock.get_request()
        assert request.method == "PUT"
        assert request.url.path.endswith("/v1/sections/test_section/products")
        body = json.loads(request.content)
        assert "resources" in body

    def test_replace_groups(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_replace_section_groups_response_value_200_application_json.json"
            ),
        )
        result = sellapp_sdk.sections.replace_groups("test_section", resources=[1])
        assert isinstance(
            result, SdkReplaceSectionGroupsResponseValue200ApplicationJson
        )
        request = httpx_mock.get_request()
        assert request.method == "PUT"
        assert request.url.path.endswith("/v1/sections/test_section/groups")
        body = json.loads(request.content)
        assert "resources" in body

    def test_search(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "list_search_sections_response_value_200_application_json_property_data_item.json"
            ),
        )
        page = sellapp_sdk.sections.search()
        assert isinstance(page, SyncPage)
        assert len(page.data) == 1
        assert isinstance(
            page.data[0], SearchSectionsResponseValue200ApplicationJsonPropertyDataItem
        )

    def test_search_empty_page(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(json={"data": [], "list_metadata": {}})
        page = sellapp_sdk.sections.search()
        assert isinstance(page, SyncPage)
        assert page.data == []

    def test_batch_create(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_batch_create_sections_response_value_200_application_json.json"
            ),
        )
        result = sellapp_sdk.sections.batch_create(
            resources=[
                BatchCreateSectionsRequestApplicationJsonPropertyResourcesItem.from_dict(
                    load_fixture(
                        "batch_create_sections_request_application_json_property_resources_item.json"
                    )
                )
            ]
        )
        assert isinstance(result, SdkBatchCreateSectionsResponseValue200ApplicationJson)
        request = httpx_mock.get_request()
        assert request.method == "POST"
        assert request.url.path.endswith("/v1/sections/batch")
        body = json.loads(request.content)
        assert "resources" in body

    def test_batch_update(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_batch_update_sections_response_value_200_application_json.json"
            ),
        )
        result = sellapp_sdk.sections.batch_update(
            resources=BatchUpdateSectionsRequestApplicationJsonPropertyResources.from_dict(
                load_fixture(
                    "batch_update_sections_request_application_json_property_resources.json"
                )
            )
        )
        assert isinstance(result, SdkBatchUpdateSectionsResponseValue200ApplicationJson)
        request = httpx_mock.get_request()
        assert request.method == "PATCH"
        assert request.url.path.endswith("/v1/sections/batch")
        body = json.loads(request.content)
        assert "resources" in body

    def test_batch_delete(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(status_code=200, content=b"")
        result = sellapp_sdk.sections.batch_delete(resources=[1])
        assert result is None
        request = httpx_mock.get_request()
        assert request.method == "DELETE"
        assert request.url.path.endswith("/v1/sections/batch")

    def test_v2_list_sections(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "list_v_2_list_sections_response_value_200_application_json_property_data_item.json"
            ),
        )
        page = sellapp_sdk.sections.v2_list_sections()
        assert isinstance(page, SyncPage)
        assert len(page.data) == 1
        assert isinstance(
            page.data[0], V2ListSectionsResponseValue200ApplicationJsonPropertyDataItem
        )

    def test_v2_list_sections_empty_page(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(json={"data": [], "list_metadata": {}})
        page = sellapp_sdk.sections.v2_list_sections()
        assert isinstance(page, SyncPage)
        assert page.data == []

    def test_v2_list_sections_encodes_query_params(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(json={"data": [], "list_metadata": {}})
        sellapp_sdk.sections.v2_list_sections(
            limit=10, before="cursor before", after="cursor/after", page=7
        )
        request = httpx_mock.get_request()
        assert request.url.params["limit"] == "10"
        assert request.url.params["before"] == "cursor before"
        assert request.url.params["after"] == "cursor/after"
        assert request.url.params["page"] == "7"

    def test_v2_create_section(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_v_2_create_section_response_value_201_application_json.json"
            ),
        )
        result = sellapp_sdk.sections.v2_create_section(title="test_title", hidden=True)
        assert isinstance(result, SdkV2CreateSectionResponseValue201ApplicationJson)
        request = httpx_mock.get_request()
        assert request.method == "POST"
        assert request.url.path.endswith("/v2/sections")
        body = json.loads(request.content)
        assert body["title"] == "test_title"
        assert body["hidden"] == True

    def test_v2_search_sections(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "list_v_2_search_sections_response_value_200_application_json_property_data_item.json"
            ),
        )
        page = sellapp_sdk.sections.v2_search_sections()
        assert isinstance(page, SyncPage)
        assert len(page.data) == 1
        assert isinstance(
            page.data[0],
            V2SearchSectionsResponseValue200ApplicationJsonPropertyDataItem,
        )

    def test_v2_search_sections_empty_page(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(json={"data": [], "list_metadata": {}})
        page = sellapp_sdk.sections.v2_search_sections()
        assert isinstance(page, SyncPage)
        assert page.data == []

    def test_v2_get_section(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_v_2_get_section_response_value_200_application_json.json"
            ),
        )
        result = sellapp_sdk.sections.v2_get_section("test_section")
        assert isinstance(result, SdkV2GetSectionResponseValue200ApplicationJson)
        request = httpx_mock.get_request()
        assert request.method == "GET"
        assert request.url.path.endswith("/v2/sections/test_section")

    def test_v2_replace_section(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_v_2_replace_section_response_value_200_application_json.json"
            ),
        )
        result = sellapp_sdk.sections.v2_replace_section("test_section")
        assert isinstance(result, SdkV2ReplaceSectionResponseValue200ApplicationJson)
        request = httpx_mock.get_request()
        assert request.method == "PUT"
        assert request.url.path.endswith("/v2/sections/test_section")

    def test_v2_update_section(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_v_2_update_section_response_value_200_application_json.json"
            ),
        )
        result = sellapp_sdk.sections.v2_update_section("test_section")
        assert isinstance(result, SdkV2UpdateSectionResponseValue200ApplicationJson)
        request = httpx_mock.get_request()
        assert request.method == "PATCH"
        assert request.url.path.endswith("/v2/sections/test_section")

    def test_v2_delete_section(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(status_code=200, content=b"")
        result = sellapp_sdk.sections.v2_delete_section("test_section")
        assert result is None
        request = httpx_mock.get_request()
        assert request.method == "DELETE"
        assert request.url.path.endswith("/v2/sections/test_section")

    def test_v2_batch_create_sections(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_v_2_batch_create_sections_response_value_200_application_json.json"
            ),
        )
        result = sellapp_sdk.sections.v2_batch_create_sections(
            resources=[
                V2BatchCreateSectionsRequestApplicationJsonPropertyResourcesItem.from_dict(
                    load_fixture(
                        "v_2_batch_create_sections_request_application_json_property_resources_item.json"
                    )
                )
            ]
        )
        assert isinstance(
            result, SdkV2BatchCreateSectionsResponseValue200ApplicationJson
        )
        request = httpx_mock.get_request()
        assert request.method == "POST"
        assert request.url.path.endswith("/v2/sections/batch")
        body = json.loads(request.content)
        assert "resources" in body

    def test_v2_batch_update_sections(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_v_2_batch_update_sections_response_value_200_application_json.json"
            ),
        )
        result = sellapp_sdk.sections.v2_batch_update_sections(
            resources=V2BatchUpdateSectionsRequestApplicationJsonPropertyResources.from_dict(
                load_fixture(
                    "v_2_batch_update_sections_request_application_json_property_resources.json"
                )
            )
        )
        assert isinstance(
            result, SdkV2BatchUpdateSectionsResponseValue200ApplicationJson
        )
        request = httpx_mock.get_request()
        assert request.method == "PATCH"
        assert request.url.path.endswith("/v2/sections/batch")
        body = json.loads(request.content)
        assert "resources" in body

    def test_v2_batch_delete_sections(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(status_code=200, content=b"")
        result = sellapp_sdk.sections.v2_batch_delete_sections(resources=[1])
        assert result is None
        request = httpx_mock.get_request()
        assert request.method == "DELETE"
        assert request.url.path.endswith("/v2/sections/batch")

    def test_v2_replace_section_order(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_v_2_replace_section_order_response_value_200_application_json.json"
            ),
        )
        result = sellapp_sdk.sections.v2_replace_section_order(resources=[1])
        assert isinstance(
            result, SdkV2ReplaceSectionOrderResponseValue200ApplicationJson
        )
        request = httpx_mock.get_request()
        assert request.method == "PUT"
        assert request.url.path.endswith("/v2/sections/order")
        body = json.loads(request.content)
        assert "resources" in body

    def test_v2_replace_section_products(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_v_2_replace_section_products_response_value_200_application_json.json"
            ),
        )
        result = sellapp_sdk.sections.v2_replace_section_products(
            "test_section", resources=[1]
        )
        assert isinstance(
            result, SdkV2ReplaceSectionProductsResponseValue200ApplicationJson
        )
        request = httpx_mock.get_request()
        assert request.method == "PUT"
        assert request.url.path.endswith("/v2/sections/test_section/products")
        body = json.loads(request.content)
        assert "resources" in body

    def test_v2_replace_section_groups(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_v_2_replace_section_groups_response_value_200_application_json.json"
            ),
        )
        result = sellapp_sdk.sections.v2_replace_section_groups(
            "test_section", resources=[1]
        )
        assert isinstance(
            result, SdkV2ReplaceSectionGroupsResponseValue200ApplicationJson
        )
        request = httpx_mock.get_request()
        assert request.method == "PUT"
        assert request.url.path.endswith("/v2/sections/test_section/groups")
        body = json.loads(request.content)
        assert "resources" in body

    def test_list_with_request_options(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(json={"data": [], "list_metadata": {}})
        sellapp_sdk.sections.list(request_options={"headers": {"X-Custom": "value"}})
        request = httpx_mock.get_request()
        assert request.headers["X-Custom"] == "value"

    def test_list_unauthorized(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            status_code=401,
            json={"message": "Unauthorized"},
        )
        with pytest.raises(AuthenticationError):
            sellapp_sdk.sections.list()

    def test_list_not_found(self, httpx_mock):
        sellapp_sdk = SellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=404, json={"message": "Not found"})
            with pytest.raises(NotFoundError):
                sellapp_sdk.sections.list()
        finally:
            sellapp_sdk.close()

    def test_list_rate_limited(self, httpx_mock):
        sellapp_sdk = SellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(
                status_code=429,
                headers={"Retry-After": "0"},
                json={"message": "Slow down"},
            )
            with pytest.raises(RateLimitExceededError):
                sellapp_sdk.sections.list()
        finally:
            sellapp_sdk.close()

    def test_list_server_error(self, httpx_mock):
        sellapp_sdk = SellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=500, json={"message": "Server error"})
            with pytest.raises(ServerError):
                sellapp_sdk.sections.list()
        finally:
            sellapp_sdk.close()

    def test_list_bad_request(self, httpx_mock):
        sellapp_sdk = SellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=400, json={"message": "Bad request"})
            with pytest.raises(BadRequestError):
                sellapp_sdk.sections.list()
        finally:
            sellapp_sdk.close()

    def test_list_unprocessable(self, httpx_mock):
        sellapp_sdk = SellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=422, json={"message": "Unprocessable"})
            with pytest.raises(UnprocessableEntityError):
                sellapp_sdk.sections.list()
        finally:
            sellapp_sdk.close()


class TestAsyncSections:
    @pytest.mark.asyncio
    async def test_list(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "list_list_sections_response_value_200_application_json_property_data_item.json"
            )
        )
        page = await async_sellapp_sdk.sections.list()
        assert isinstance(page, AsyncPage)
        assert len(page.data) == 1
        assert isinstance(
            page.data[0], ListSectionsResponseValue200ApplicationJsonPropertyDataItem
        )

    @pytest.mark.asyncio
    async def test_list_empty_page(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(json={"data": [], "list_metadata": {}})
        page = await async_sellapp_sdk.sections.list()
        assert isinstance(page, AsyncPage)
        assert page.data == []

    @pytest.mark.asyncio
    async def test_list_encodes_query_params(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(json={"data": [], "list_metadata": {}})
        await async_sellapp_sdk.sections.list(
            limit=10, before="cursor before", after="cursor/after", page=7
        )
        request = httpx_mock.get_request()
        assert request.url.params["limit"] == "10"
        assert request.url.params["before"] == "cursor before"
        assert request.url.params["after"] == "cursor/after"
        assert request.url.params["page"] == "7"

    @pytest.mark.asyncio
    async def test_create(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_create_section_response_value_201_application_json.json"
            )
        )
        result = await async_sellapp_sdk.sections.create(
            title="test_title", hidden=True
        )
        assert isinstance(result, SdkCreateSectionResponseValue201ApplicationJson)
        request = httpx_mock.get_request()
        assert request.method == "POST"
        assert request.url.path.endswith("/v1/sections")

    @pytest.mark.asyncio
    async def test_get(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_get_section_response_value_200_application_json.json"
            )
        )
        result = await async_sellapp_sdk.sections.get("test_section")
        assert isinstance(result, SdkGetSectionResponseValue200ApplicationJson)
        request = httpx_mock.get_request()
        assert request.method == "GET"
        assert request.url.path.endswith("/v1/sections/test_section")

    @pytest.mark.asyncio
    async def test_replace(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_replace_section_response_value_200_application_json.json"
            )
        )
        result = await async_sellapp_sdk.sections.replace("test_section")
        assert isinstance(result, SdkReplaceSectionResponseValue200ApplicationJson)
        request = httpx_mock.get_request()
        assert request.method == "PUT"
        assert request.url.path.endswith("/v1/sections/test_section")

    @pytest.mark.asyncio
    async def test_update(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_update_section_response_value_200_application_json.json"
            )
        )
        result = await async_sellapp_sdk.sections.update("test_section")
        assert isinstance(result, SdkUpdateSectionResponseValue200ApplicationJson)
        request = httpx_mock.get_request()
        assert request.method == "PATCH"
        assert request.url.path.endswith("/v1/sections/test_section")

    @pytest.mark.asyncio
    async def test_delete(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(status_code=200, content=b"")
        result = await async_sellapp_sdk.sections.delete("test_section")
        assert result is None
        request = httpx_mock.get_request()
        assert request.method == "DELETE"
        assert request.url.path.endswith("/v1/sections/test_section")

    @pytest.mark.asyncio
    async def test_replace_order(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_replace_section_order_response_value_200_application_json.json"
            )
        )
        result = await async_sellapp_sdk.sections.replace_order(resources=[1])
        assert isinstance(result, SdkReplaceSectionOrderResponseValue200ApplicationJson)
        request = httpx_mock.get_request()
        assert request.method == "PUT"
        assert request.url.path.endswith("/v1/sections/order")

    @pytest.mark.asyncio
    async def test_replace_products(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_replace_section_products_response_value_200_application_json.json"
            )
        )
        result = await async_sellapp_sdk.sections.replace_products(
            "test_section", resources=[1]
        )
        assert isinstance(
            result, SdkReplaceSectionProductsResponseValue200ApplicationJson
        )
        request = httpx_mock.get_request()
        assert request.method == "PUT"
        assert request.url.path.endswith("/v1/sections/test_section/products")

    @pytest.mark.asyncio
    async def test_replace_groups(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_replace_section_groups_response_value_200_application_json.json"
            )
        )
        result = await async_sellapp_sdk.sections.replace_groups(
            "test_section", resources=[1]
        )
        assert isinstance(
            result, SdkReplaceSectionGroupsResponseValue200ApplicationJson
        )
        request = httpx_mock.get_request()
        assert request.method == "PUT"
        assert request.url.path.endswith("/v1/sections/test_section/groups")

    @pytest.mark.asyncio
    async def test_search(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "list_search_sections_response_value_200_application_json_property_data_item.json"
            )
        )
        page = await async_sellapp_sdk.sections.search()
        assert isinstance(page, AsyncPage)
        assert len(page.data) == 1
        assert isinstance(
            page.data[0], SearchSectionsResponseValue200ApplicationJsonPropertyDataItem
        )

    @pytest.mark.asyncio
    async def test_search_empty_page(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(json={"data": [], "list_metadata": {}})
        page = await async_sellapp_sdk.sections.search()
        assert isinstance(page, AsyncPage)
        assert page.data == []

    @pytest.mark.asyncio
    async def test_batch_create(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_batch_create_sections_response_value_200_application_json.json"
            )
        )
        result = await async_sellapp_sdk.sections.batch_create(
            resources=[
                BatchCreateSectionsRequestApplicationJsonPropertyResourcesItem.from_dict(
                    load_fixture(
                        "batch_create_sections_request_application_json_property_resources_item.json"
                    )
                )
            ]
        )
        assert isinstance(result, SdkBatchCreateSectionsResponseValue200ApplicationJson)
        request = httpx_mock.get_request()
        assert request.method == "POST"
        assert request.url.path.endswith("/v1/sections/batch")

    @pytest.mark.asyncio
    async def test_batch_update(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_batch_update_sections_response_value_200_application_json.json"
            )
        )
        result = await async_sellapp_sdk.sections.batch_update(
            resources=BatchUpdateSectionsRequestApplicationJsonPropertyResources.from_dict(
                load_fixture(
                    "batch_update_sections_request_application_json_property_resources.json"
                )
            )
        )
        assert isinstance(result, SdkBatchUpdateSectionsResponseValue200ApplicationJson)
        request = httpx_mock.get_request()
        assert request.method == "PATCH"
        assert request.url.path.endswith("/v1/sections/batch")

    @pytest.mark.asyncio
    async def test_batch_delete(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(status_code=200, content=b"")
        result = await async_sellapp_sdk.sections.batch_delete(resources=[1])
        assert result is None
        request = httpx_mock.get_request()
        assert request.method == "DELETE"
        assert request.url.path.endswith("/v1/sections/batch")

    @pytest.mark.asyncio
    async def test_v2_list_sections(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "list_v_2_list_sections_response_value_200_application_json_property_data_item.json"
            )
        )
        page = await async_sellapp_sdk.sections.v2_list_sections()
        assert isinstance(page, AsyncPage)
        assert len(page.data) == 1
        assert isinstance(
            page.data[0], V2ListSectionsResponseValue200ApplicationJsonPropertyDataItem
        )

    @pytest.mark.asyncio
    async def test_v2_list_sections_empty_page(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(json={"data": [], "list_metadata": {}})
        page = await async_sellapp_sdk.sections.v2_list_sections()
        assert isinstance(page, AsyncPage)
        assert page.data == []

    @pytest.mark.asyncio
    async def test_v2_list_sections_encodes_query_params(
        self, async_sellapp_sdk, httpx_mock
    ):
        httpx_mock.add_response(json={"data": [], "list_metadata": {}})
        await async_sellapp_sdk.sections.v2_list_sections(
            limit=10, before="cursor before", after="cursor/after", page=7
        )
        request = httpx_mock.get_request()
        assert request.url.params["limit"] == "10"
        assert request.url.params["before"] == "cursor before"
        assert request.url.params["after"] == "cursor/after"
        assert request.url.params["page"] == "7"

    @pytest.mark.asyncio
    async def test_v2_create_section(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_v_2_create_section_response_value_201_application_json.json"
            )
        )
        result = await async_sellapp_sdk.sections.v2_create_section(
            title="test_title", hidden=True
        )
        assert isinstance(result, SdkV2CreateSectionResponseValue201ApplicationJson)
        request = httpx_mock.get_request()
        assert request.method == "POST"
        assert request.url.path.endswith("/v2/sections")

    @pytest.mark.asyncio
    async def test_v2_search_sections(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "list_v_2_search_sections_response_value_200_application_json_property_data_item.json"
            )
        )
        page = await async_sellapp_sdk.sections.v2_search_sections()
        assert isinstance(page, AsyncPage)
        assert len(page.data) == 1
        assert isinstance(
            page.data[0],
            V2SearchSectionsResponseValue200ApplicationJsonPropertyDataItem,
        )

    @pytest.mark.asyncio
    async def test_v2_search_sections_empty_page(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(json={"data": [], "list_metadata": {}})
        page = await async_sellapp_sdk.sections.v2_search_sections()
        assert isinstance(page, AsyncPage)
        assert page.data == []

    @pytest.mark.asyncio
    async def test_v2_get_section(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_v_2_get_section_response_value_200_application_json.json"
            )
        )
        result = await async_sellapp_sdk.sections.v2_get_section("test_section")
        assert isinstance(result, SdkV2GetSectionResponseValue200ApplicationJson)
        request = httpx_mock.get_request()
        assert request.method == "GET"
        assert request.url.path.endswith("/v2/sections/test_section")

    @pytest.mark.asyncio
    async def test_v2_replace_section(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_v_2_replace_section_response_value_200_application_json.json"
            )
        )
        result = await async_sellapp_sdk.sections.v2_replace_section("test_section")
        assert isinstance(result, SdkV2ReplaceSectionResponseValue200ApplicationJson)
        request = httpx_mock.get_request()
        assert request.method == "PUT"
        assert request.url.path.endswith("/v2/sections/test_section")

    @pytest.mark.asyncio
    async def test_v2_update_section(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_v_2_update_section_response_value_200_application_json.json"
            )
        )
        result = await async_sellapp_sdk.sections.v2_update_section("test_section")
        assert isinstance(result, SdkV2UpdateSectionResponseValue200ApplicationJson)
        request = httpx_mock.get_request()
        assert request.method == "PATCH"
        assert request.url.path.endswith("/v2/sections/test_section")

    @pytest.mark.asyncio
    async def test_v2_delete_section(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(status_code=200, content=b"")
        result = await async_sellapp_sdk.sections.v2_delete_section("test_section")
        assert result is None
        request = httpx_mock.get_request()
        assert request.method == "DELETE"
        assert request.url.path.endswith("/v2/sections/test_section")

    @pytest.mark.asyncio
    async def test_v2_batch_create_sections(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_v_2_batch_create_sections_response_value_200_application_json.json"
            )
        )
        result = await async_sellapp_sdk.sections.v2_batch_create_sections(
            resources=[
                V2BatchCreateSectionsRequestApplicationJsonPropertyResourcesItem.from_dict(
                    load_fixture(
                        "v_2_batch_create_sections_request_application_json_property_resources_item.json"
                    )
                )
            ]
        )
        assert isinstance(
            result, SdkV2BatchCreateSectionsResponseValue200ApplicationJson
        )
        request = httpx_mock.get_request()
        assert request.method == "POST"
        assert request.url.path.endswith("/v2/sections/batch")

    @pytest.mark.asyncio
    async def test_v2_batch_update_sections(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_v_2_batch_update_sections_response_value_200_application_json.json"
            )
        )
        result = await async_sellapp_sdk.sections.v2_batch_update_sections(
            resources=V2BatchUpdateSectionsRequestApplicationJsonPropertyResources.from_dict(
                load_fixture(
                    "v_2_batch_update_sections_request_application_json_property_resources.json"
                )
            )
        )
        assert isinstance(
            result, SdkV2BatchUpdateSectionsResponseValue200ApplicationJson
        )
        request = httpx_mock.get_request()
        assert request.method == "PATCH"
        assert request.url.path.endswith("/v2/sections/batch")

    @pytest.mark.asyncio
    async def test_v2_batch_delete_sections(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(status_code=200, content=b"")
        result = await async_sellapp_sdk.sections.v2_batch_delete_sections(
            resources=[1]
        )
        assert result is None
        request = httpx_mock.get_request()
        assert request.method == "DELETE"
        assert request.url.path.endswith("/v2/sections/batch")

    @pytest.mark.asyncio
    async def test_v2_replace_section_order(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_v_2_replace_section_order_response_value_200_application_json.json"
            )
        )
        result = await async_sellapp_sdk.sections.v2_replace_section_order(
            resources=[1]
        )
        assert isinstance(
            result, SdkV2ReplaceSectionOrderResponseValue200ApplicationJson
        )
        request = httpx_mock.get_request()
        assert request.method == "PUT"
        assert request.url.path.endswith("/v2/sections/order")

    @pytest.mark.asyncio
    async def test_v2_replace_section_products(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_v_2_replace_section_products_response_value_200_application_json.json"
            )
        )
        result = await async_sellapp_sdk.sections.v2_replace_section_products(
            "test_section", resources=[1]
        )
        assert isinstance(
            result, SdkV2ReplaceSectionProductsResponseValue200ApplicationJson
        )
        request = httpx_mock.get_request()
        assert request.method == "PUT"
        assert request.url.path.endswith("/v2/sections/test_section/products")

    @pytest.mark.asyncio
    async def test_v2_replace_section_groups(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_v_2_replace_section_groups_response_value_200_application_json.json"
            )
        )
        result = await async_sellapp_sdk.sections.v2_replace_section_groups(
            "test_section", resources=[1]
        )
        assert isinstance(
            result, SdkV2ReplaceSectionGroupsResponseValue200ApplicationJson
        )
        request = httpx_mock.get_request()
        assert request.method == "PUT"
        assert request.url.path.endswith("/v2/sections/test_section/groups")

    @pytest.mark.asyncio
    async def test_list_with_request_options(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(json={"data": [], "list_metadata": {}})
        await async_sellapp_sdk.sections.list(
            request_options={"headers": {"X-Custom": "value"}}
        )
        request = httpx_mock.get_request()
        assert request.headers["X-Custom"] == "value"

    @pytest.mark.asyncio
    async def test_list_unauthorized(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(status_code=401, json={"message": "Unauthorized"})
        with pytest.raises(AuthenticationError):
            await async_sellapp_sdk.sections.list()

    @pytest.mark.asyncio
    async def test_list_not_found(self, httpx_mock):
        sellapp_sdk = AsyncSellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=404, json={"message": "Not found"})
            with pytest.raises(NotFoundError):
                await sellapp_sdk.sections.list()
        finally:
            await sellapp_sdk.close()

    @pytest.mark.asyncio
    async def test_list_rate_limited(self, httpx_mock):
        sellapp_sdk = AsyncSellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(
                status_code=429,
                headers={"Retry-After": "0"},
                json={"message": "Slow down"},
            )
            with pytest.raises(RateLimitExceededError):
                await sellapp_sdk.sections.list()
        finally:
            await sellapp_sdk.close()

    @pytest.mark.asyncio
    async def test_list_server_error(self, httpx_mock):
        sellapp_sdk = AsyncSellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=500, json={"message": "Server error"})
            with pytest.raises(ServerError):
                await sellapp_sdk.sections.list()
        finally:
            await sellapp_sdk.close()

    @pytest.mark.asyncio
    async def test_list_bad_request(self, httpx_mock):
        sellapp_sdk = AsyncSellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=400, json={"message": "Bad request"})
            with pytest.raises(BadRequestError):
                await sellapp_sdk.sections.list()
        finally:
            await sellapp_sdk.close()

    @pytest.mark.asyncio
    async def test_list_unprocessable(self, httpx_mock):
        sellapp_sdk = AsyncSellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=422, json={"message": "Unprocessable"})
            with pytest.raises(UnprocessableEntityError):
                await sellapp_sdk.sections.list()
        finally:
            await sellapp_sdk.close()
