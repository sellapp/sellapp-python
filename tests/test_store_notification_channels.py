# This file is auto-generated by oagen. Do not edit.

import json

import pytest

from sellapp_sdk import AsyncSellAppClient, SellAppClient
from sellapp_sdk._errors import (
    AuthenticationError,
    BadRequestError,
    NotFoundError,
    RateLimitExceededError,
    ServerError,
    UnprocessableEntityError,
)
from sellapp_sdk.store_settings.models import (
    CreateNotificationChannelRequestApplicationJsonPropertyChannel,
    ReplaceNotificationChannelRequestApplicationJsonPropertyChannel,
    SdkCreateNotificationChannelResponseValue201ApplicationJson,
    SdkGetNotificationChannelResponseValue200ApplicationJson,
    SdkListNotificationChannelsResponseValue200ApplicationJson,
    SdkReplaceNotificationChannelResponseValue200ApplicationJson,
    SdkUpdateNotificationChannelResponseValue200ApplicationJson,
    UpdateNotificationChannelRequestApplicationJsonPropertyChannel,
)
from tests.generated_helpers import load_fixture


class TestStoreNotificationChannels:
    def test_list(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_list_notification_channels_response_value_200_application_json.json"
            ),
        )
        result = sellapp_sdk.store_notification_channels.list()
        assert isinstance(
            result, SdkListNotificationChannelsResponseValue200ApplicationJson
        )
        request = httpx_mock.get_request()
        assert request.method == "GET"
        assert request.url.path.endswith("/v2/store/notification-channels")

    def test_create(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_create_notification_channel_response_value_201_application_json.json"
            ),
        )
        result = sellapp_sdk.store_notification_channels.create(
            channel=CreateNotificationChannelRequestApplicationJsonPropertyChannel.from_dict(
                load_fixture(
                    "create_notification_channel_request_application_json_property_channel.json"
                )
            )
        )
        assert isinstance(
            result, SdkCreateNotificationChannelResponseValue201ApplicationJson
        )
        request = httpx_mock.get_request()
        assert request.method == "POST"
        assert request.url.path.endswith("/v2/store/notification-channels")
        body = json.loads(request.content)
        assert "channel" in body

    def test_get(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_get_notification_channel_response_value_200_application_json.json"
            ),
        )
        result = sellapp_sdk.store_notification_channels.get("test_notificationChannel")
        assert isinstance(
            result, SdkGetNotificationChannelResponseValue200ApplicationJson
        )
        request = httpx_mock.get_request()
        assert request.method == "GET"
        assert request.url.path.endswith(
            "/v2/store/notification-channels/test_notificationChannel"
        )

    def test_replace(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_replace_notification_channel_response_value_200_application_json.json"
            ),
        )
        result = sellapp_sdk.store_notification_channels.replace(
            "test_notificationChannel",
            channel=ReplaceNotificationChannelRequestApplicationJsonPropertyChannel.from_dict(
                load_fixture(
                    "replace_notification_channel_request_application_json_property_channel.json"
                )
            ),
        )
        assert isinstance(
            result, SdkReplaceNotificationChannelResponseValue200ApplicationJson
        )
        request = httpx_mock.get_request()
        assert request.method == "PUT"
        assert request.url.path.endswith(
            "/v2/store/notification-channels/test_notificationChannel"
        )
        body = json.loads(request.content)
        assert "channel" in body

    def test_update(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_update_notification_channel_response_value_200_application_json.json"
            ),
        )
        result = sellapp_sdk.store_notification_channels.update(
            "test_notificationChannel",
            channel=UpdateNotificationChannelRequestApplicationJsonPropertyChannel.from_dict(
                load_fixture(
                    "update_notification_channel_request_application_json_property_channel.json"
                )
            ),
        )
        assert isinstance(
            result, SdkUpdateNotificationChannelResponseValue200ApplicationJson
        )
        request = httpx_mock.get_request()
        assert request.method == "PATCH"
        assert request.url.path.endswith(
            "/v2/store/notification-channels/test_notificationChannel"
        )
        body = json.loads(request.content)
        assert "channel" in body

    def test_delete(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(status_code=200, content=b"")
        result = sellapp_sdk.store_notification_channels.delete(
            "test_notificationChannel"
        )
        assert result is None
        request = httpx_mock.get_request()
        assert request.method == "DELETE"
        assert request.url.path.endswith(
            "/v2/store/notification-channels/test_notificationChannel"
        )

    def test_list_with_request_options(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_list_notification_channels_response_value_200_application_json.json"
            )
        )
        sellapp_sdk.store_notification_channels.list(
            request_options={"headers": {"X-Custom": "value"}}
        )
        request = httpx_mock.get_request()
        assert request.headers["X-Custom"] == "value"

    def test_list_unauthorized(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            status_code=401,
            json={"message": "Unauthorized"},
        )
        with pytest.raises(AuthenticationError):
            sellapp_sdk.store_notification_channels.list()

    def test_list_not_found(self, httpx_mock):
        sellapp_sdk = SellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=404, json={"message": "Not found"})
            with pytest.raises(NotFoundError):
                sellapp_sdk.store_notification_channels.list()
        finally:
            sellapp_sdk.close()

    def test_list_rate_limited(self, httpx_mock):
        sellapp_sdk = SellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(
                status_code=429,
                headers={"Retry-After": "0"},
                json={"message": "Slow down"},
            )
            with pytest.raises(RateLimitExceededError):
                sellapp_sdk.store_notification_channels.list()
        finally:
            sellapp_sdk.close()

    def test_list_server_error(self, httpx_mock):
        sellapp_sdk = SellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=500, json={"message": "Server error"})
            with pytest.raises(ServerError):
                sellapp_sdk.store_notification_channels.list()
        finally:
            sellapp_sdk.close()

    def test_list_bad_request(self, httpx_mock):
        sellapp_sdk = SellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=400, json={"message": "Bad request"})
            with pytest.raises(BadRequestError):
                sellapp_sdk.store_notification_channels.list()
        finally:
            sellapp_sdk.close()

    def test_list_unprocessable(self, httpx_mock):
        sellapp_sdk = SellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=422, json={"message": "Unprocessable"})
            with pytest.raises(UnprocessableEntityError):
                sellapp_sdk.store_notification_channels.list()
        finally:
            sellapp_sdk.close()


class TestAsyncStoreNotificationChannels:
    @pytest.mark.asyncio
    async def test_list(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_list_notification_channels_response_value_200_application_json.json"
            )
        )
        result = await async_sellapp_sdk.store_notification_channels.list()
        assert isinstance(
            result, SdkListNotificationChannelsResponseValue200ApplicationJson
        )
        request = httpx_mock.get_request()
        assert request.method == "GET"
        assert request.url.path.endswith("/v2/store/notification-channels")

    @pytest.mark.asyncio
    async def test_create(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_create_notification_channel_response_value_201_application_json.json"
            )
        )
        result = await async_sellapp_sdk.store_notification_channels.create(
            channel=CreateNotificationChannelRequestApplicationJsonPropertyChannel.from_dict(
                load_fixture(
                    "create_notification_channel_request_application_json_property_channel.json"
                )
            )
        )
        assert isinstance(
            result, SdkCreateNotificationChannelResponseValue201ApplicationJson
        )
        request = httpx_mock.get_request()
        assert request.method == "POST"
        assert request.url.path.endswith("/v2/store/notification-channels")

    @pytest.mark.asyncio
    async def test_get(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_get_notification_channel_response_value_200_application_json.json"
            )
        )
        result = await async_sellapp_sdk.store_notification_channels.get(
            "test_notificationChannel"
        )
        assert isinstance(
            result, SdkGetNotificationChannelResponseValue200ApplicationJson
        )
        request = httpx_mock.get_request()
        assert request.method == "GET"
        assert request.url.path.endswith(
            "/v2/store/notification-channels/test_notificationChannel"
        )

    @pytest.mark.asyncio
    async def test_replace(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_replace_notification_channel_response_value_200_application_json.json"
            )
        )
        result = await async_sellapp_sdk.store_notification_channels.replace(
            "test_notificationChannel",
            channel=ReplaceNotificationChannelRequestApplicationJsonPropertyChannel.from_dict(
                load_fixture(
                    "replace_notification_channel_request_application_json_property_channel.json"
                )
            ),
        )
        assert isinstance(
            result, SdkReplaceNotificationChannelResponseValue200ApplicationJson
        )
        request = httpx_mock.get_request()
        assert request.method == "PUT"
        assert request.url.path.endswith(
            "/v2/store/notification-channels/test_notificationChannel"
        )

    @pytest.mark.asyncio
    async def test_update(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_update_notification_channel_response_value_200_application_json.json"
            )
        )
        result = await async_sellapp_sdk.store_notification_channels.update(
            "test_notificationChannel",
            channel=UpdateNotificationChannelRequestApplicationJsonPropertyChannel.from_dict(
                load_fixture(
                    "update_notification_channel_request_application_json_property_channel.json"
                )
            ),
        )
        assert isinstance(
            result, SdkUpdateNotificationChannelResponseValue200ApplicationJson
        )
        request = httpx_mock.get_request()
        assert request.method == "PATCH"
        assert request.url.path.endswith(
            "/v2/store/notification-channels/test_notificationChannel"
        )

    @pytest.mark.asyncio
    async def test_delete(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(status_code=200, content=b"")
        result = await async_sellapp_sdk.store_notification_channels.delete(
            "test_notificationChannel"
        )
        assert result is None
        request = httpx_mock.get_request()
        assert request.method == "DELETE"
        assert request.url.path.endswith(
            "/v2/store/notification-channels/test_notificationChannel"
        )

    @pytest.mark.asyncio
    async def test_list_with_request_options(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_list_notification_channels_response_value_200_application_json.json"
            )
        )
        await async_sellapp_sdk.store_notification_channels.list(
            request_options={"headers": {"X-Custom": "value"}}
        )
        request = httpx_mock.get_request()
        assert request.headers["X-Custom"] == "value"

    @pytest.mark.asyncio
    async def test_list_unauthorized(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(status_code=401, json={"message": "Unauthorized"})
        with pytest.raises(AuthenticationError):
            await async_sellapp_sdk.store_notification_channels.list()

    @pytest.mark.asyncio
    async def test_list_not_found(self, httpx_mock):
        sellapp_sdk = AsyncSellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=404, json={"message": "Not found"})
            with pytest.raises(NotFoundError):
                await sellapp_sdk.store_notification_channels.list()
        finally:
            await sellapp_sdk.close()

    @pytest.mark.asyncio
    async def test_list_rate_limited(self, httpx_mock):
        sellapp_sdk = AsyncSellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(
                status_code=429,
                headers={"Retry-After": "0"},
                json={"message": "Slow down"},
            )
            with pytest.raises(RateLimitExceededError):
                await sellapp_sdk.store_notification_channels.list()
        finally:
            await sellapp_sdk.close()

    @pytest.mark.asyncio
    async def test_list_server_error(self, httpx_mock):
        sellapp_sdk = AsyncSellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=500, json={"message": "Server error"})
            with pytest.raises(ServerError):
                await sellapp_sdk.store_notification_channels.list()
        finally:
            await sellapp_sdk.close()

    @pytest.mark.asyncio
    async def test_list_bad_request(self, httpx_mock):
        sellapp_sdk = AsyncSellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=400, json={"message": "Bad request"})
            with pytest.raises(BadRequestError):
                await sellapp_sdk.store_notification_channels.list()
        finally:
            await sellapp_sdk.close()

    @pytest.mark.asyncio
    async def test_list_unprocessable(self, httpx_mock):
        sellapp_sdk = AsyncSellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=422, json={"message": "Unprocessable"})
            with pytest.raises(UnprocessableEntityError):
                await sellapp_sdk.store_notification_channels.list()
        finally:
            await sellapp_sdk.close()
