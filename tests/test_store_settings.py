# This file is auto-generated by oagen. Do not edit.

import json

import pytest

from sellapp_sdk import AsyncSellAppClient, SellAppClient
from sellapp_sdk._errors import (
    AuthenticationError,
    BadRequestError,
    NotFoundError,
    RateLimitExceededError,
    ServerError,
    UnprocessableEntityError,
)
from sellapp_sdk.common.models import CourseVisibility
from sellapp_sdk.store_settings.models import (
    ReplaceMarketingSettingsRequestApplicationJsonPropertyAbandonedCart,
    SdkGetStoreSettingsResponseValue200ApplicationJson,
    SdkReplaceAnalyticsSettingsResponseValue200ApplicationJson,
    SdkReplaceGeneralStoreSettingsResponseValue200ApplicationJson,
    SdkReplaceMarketingSettingsResponseValue200ApplicationJson,
    SdkUpdateAnalyticsSettingsResponseValue200ApplicationJson,
    SdkUpdateGeneralStoreSettingsResponseValue200ApplicationJson,
    SdkUpdateMarketingSettingsResponseValue200ApplicationJson,
    UpdateMarketingSettingsRequestApplicationJsonPropertyAbandonedCart,
)
from tests.generated_helpers import load_fixture


class TestStoreSettings:
    def test_get(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_get_store_settings_response_value_200_application_json.json"
            ),
        )
        result = sellapp_sdk.store_settings.get()
        assert isinstance(result, SdkGetStoreSettingsResponseValue200ApplicationJson)
        request = httpx_mock.get_request()
        assert request.method == "GET"
        assert request.url.path.endswith("/v2/store/settings")

    def test_replace_general(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_replace_general_store_settings_response_value_200_application_json.json"
            ),
        )
        result = sellapp_sdk.store_settings.replace_general(
            name="test_name",
            visibility=CourseVisibility("PUBLIC"),
            timezone="test_timezone",
            currency="tes",
        )
        assert isinstance(
            result, SdkReplaceGeneralStoreSettingsResponseValue200ApplicationJson
        )
        request = httpx_mock.get_request()
        assert request.method == "PUT"
        assert request.url.path.endswith("/v2/store/settings/general")
        body = json.loads(request.content)
        assert body["name"] == "test_name"
        assert body["visibility"] == CourseVisibility("PUBLIC")
        assert body["timezone"] == "test_timezone"
        assert body["currency"] == "tes"

    def test_update_general(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_update_general_store_settings_response_value_200_application_json.json"
            ),
        )
        result = sellapp_sdk.store_settings.update_general()
        assert isinstance(
            result, SdkUpdateGeneralStoreSettingsResponseValue200ApplicationJson
        )
        request = httpx_mock.get_request()
        assert request.method == "PATCH"
        assert request.url.path.endswith("/v2/store/settings/general")

    def test_replace_analytics(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_replace_analytics_settings_response_value_200_application_json.json"
            ),
        )
        result = sellapp_sdk.store_settings.replace_analytics()
        assert isinstance(
            result, SdkReplaceAnalyticsSettingsResponseValue200ApplicationJson
        )
        request = httpx_mock.get_request()
        assert request.method == "PUT"
        assert request.url.path.endswith("/v2/store/settings/analytics")

    def test_update_analytics(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_update_analytics_settings_response_value_200_application_json.json"
            ),
        )
        result = sellapp_sdk.store_settings.update_analytics()
        assert isinstance(
            result, SdkUpdateAnalyticsSettingsResponseValue200ApplicationJson
        )
        request = httpx_mock.get_request()
        assert request.method == "PATCH"
        assert request.url.path.endswith("/v2/store/settings/analytics")

    def test_replace_marketing(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_replace_marketing_settings_response_value_200_application_json.json"
            ),
        )
        result = sellapp_sdk.store_settings.replace_marketing(
            abandoned_cart=ReplaceMarketingSettingsRequestApplicationJsonPropertyAbandonedCart.from_dict(
                load_fixture(
                    "replace_marketing_settings_request_application_json_property_abandoned_cart.json"
                )
            )
        )
        assert isinstance(
            result, SdkReplaceMarketingSettingsResponseValue200ApplicationJson
        )
        request = httpx_mock.get_request()
        assert request.method == "PUT"
        assert request.url.path.endswith("/v2/store/settings/marketing")
        body = json.loads(request.content)
        assert "abandoned_cart" in body

    def test_update_marketing(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_update_marketing_settings_response_value_200_application_json.json"
            ),
        )
        result = sellapp_sdk.store_settings.update_marketing(
            abandoned_cart=UpdateMarketingSettingsRequestApplicationJsonPropertyAbandonedCart.from_dict(
                load_fixture(
                    "update_marketing_settings_request_application_json_property_abandoned_cart.json"
                )
            )
        )
        assert isinstance(
            result, SdkUpdateMarketingSettingsResponseValue200ApplicationJson
        )
        request = httpx_mock.get_request()
        assert request.method == "PATCH"
        assert request.url.path.endswith("/v2/store/settings/marketing")
        body = json.loads(request.content)
        assert "abandoned_cart" in body

    def test_get_with_request_options(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_get_store_settings_response_value_200_application_json.json"
            )
        )
        sellapp_sdk.store_settings.get(
            request_options={"headers": {"X-Custom": "value"}}
        )
        request = httpx_mock.get_request()
        assert request.headers["X-Custom"] == "value"

    def test_get_unauthorized(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            status_code=401,
            json={"message": "Unauthorized"},
        )
        with pytest.raises(AuthenticationError):
            sellapp_sdk.store_settings.get()

    def test_get_not_found(self, httpx_mock):
        sellapp_sdk = SellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=404, json={"message": "Not found"})
            with pytest.raises(NotFoundError):
                sellapp_sdk.store_settings.get()
        finally:
            sellapp_sdk.close()

    def test_get_rate_limited(self, httpx_mock):
        sellapp_sdk = SellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(
                status_code=429,
                headers={"Retry-After": "0"},
                json={"message": "Slow down"},
            )
            with pytest.raises(RateLimitExceededError):
                sellapp_sdk.store_settings.get()
        finally:
            sellapp_sdk.close()

    def test_get_server_error(self, httpx_mock):
        sellapp_sdk = SellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=500, json={"message": "Server error"})
            with pytest.raises(ServerError):
                sellapp_sdk.store_settings.get()
        finally:
            sellapp_sdk.close()

    def test_get_bad_request(self, httpx_mock):
        sellapp_sdk = SellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=400, json={"message": "Bad request"})
            with pytest.raises(BadRequestError):
                sellapp_sdk.store_settings.get()
        finally:
            sellapp_sdk.close()

    def test_get_unprocessable(self, httpx_mock):
        sellapp_sdk = SellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=422, json={"message": "Unprocessable"})
            with pytest.raises(UnprocessableEntityError):
                sellapp_sdk.store_settings.get()
        finally:
            sellapp_sdk.close()


class TestAsyncStoreSettings:
    @pytest.mark.asyncio
    async def test_get(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_get_store_settings_response_value_200_application_json.json"
            )
        )
        result = await async_sellapp_sdk.store_settings.get()
        assert isinstance(result, SdkGetStoreSettingsResponseValue200ApplicationJson)
        request = httpx_mock.get_request()
        assert request.method == "GET"
        assert request.url.path.endswith("/v2/store/settings")

    @pytest.mark.asyncio
    async def test_replace_general(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_replace_general_store_settings_response_value_200_application_json.json"
            )
        )
        result = await async_sellapp_sdk.store_settings.replace_general(
            name="test_name",
            visibility=CourseVisibility("PUBLIC"),
            timezone="test_timezone",
            currency="tes",
        )
        assert isinstance(
            result, SdkReplaceGeneralStoreSettingsResponseValue200ApplicationJson
        )
        request = httpx_mock.get_request()
        assert request.method == "PUT"
        assert request.url.path.endswith("/v2/store/settings/general")

    @pytest.mark.asyncio
    async def test_update_general(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_update_general_store_settings_response_value_200_application_json.json"
            )
        )
        result = await async_sellapp_sdk.store_settings.update_general()
        assert isinstance(
            result, SdkUpdateGeneralStoreSettingsResponseValue200ApplicationJson
        )
        request = httpx_mock.get_request()
        assert request.method == "PATCH"
        assert request.url.path.endswith("/v2/store/settings/general")

    @pytest.mark.asyncio
    async def test_replace_analytics(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_replace_analytics_settings_response_value_200_application_json.json"
            )
        )
        result = await async_sellapp_sdk.store_settings.replace_analytics()
        assert isinstance(
            result, SdkReplaceAnalyticsSettingsResponseValue200ApplicationJson
        )
        request = httpx_mock.get_request()
        assert request.method == "PUT"
        assert request.url.path.endswith("/v2/store/settings/analytics")

    @pytest.mark.asyncio
    async def test_update_analytics(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_update_analytics_settings_response_value_200_application_json.json"
            )
        )
        result = await async_sellapp_sdk.store_settings.update_analytics()
        assert isinstance(
            result, SdkUpdateAnalyticsSettingsResponseValue200ApplicationJson
        )
        request = httpx_mock.get_request()
        assert request.method == "PATCH"
        assert request.url.path.endswith("/v2/store/settings/analytics")

    @pytest.mark.asyncio
    async def test_replace_marketing(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_replace_marketing_settings_response_value_200_application_json.json"
            )
        )
        result = await async_sellapp_sdk.store_settings.replace_marketing(
            abandoned_cart=ReplaceMarketingSettingsRequestApplicationJsonPropertyAbandonedCart.from_dict(
                load_fixture(
                    "replace_marketing_settings_request_application_json_property_abandoned_cart.json"
                )
            )
        )
        assert isinstance(
            result, SdkReplaceMarketingSettingsResponseValue200ApplicationJson
        )
        request = httpx_mock.get_request()
        assert request.method == "PUT"
        assert request.url.path.endswith("/v2/store/settings/marketing")

    @pytest.mark.asyncio
    async def test_update_marketing(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_update_marketing_settings_response_value_200_application_json.json"
            )
        )
        result = await async_sellapp_sdk.store_settings.update_marketing(
            abandoned_cart=UpdateMarketingSettingsRequestApplicationJsonPropertyAbandonedCart.from_dict(
                load_fixture(
                    "update_marketing_settings_request_application_json_property_abandoned_cart.json"
                )
            )
        )
        assert isinstance(
            result, SdkUpdateMarketingSettingsResponseValue200ApplicationJson
        )
        request = httpx_mock.get_request()
        assert request.method == "PATCH"
        assert request.url.path.endswith("/v2/store/settings/marketing")

    @pytest.mark.asyncio
    async def test_get_with_request_options(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_get_store_settings_response_value_200_application_json.json"
            )
        )
        await async_sellapp_sdk.store_settings.get(
            request_options={"headers": {"X-Custom": "value"}}
        )
        request = httpx_mock.get_request()
        assert request.headers["X-Custom"] == "value"

    @pytest.mark.asyncio
    async def test_get_unauthorized(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(status_code=401, json={"message": "Unauthorized"})
        with pytest.raises(AuthenticationError):
            await async_sellapp_sdk.store_settings.get()

    @pytest.mark.asyncio
    async def test_get_not_found(self, httpx_mock):
        sellapp_sdk = AsyncSellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=404, json={"message": "Not found"})
            with pytest.raises(NotFoundError):
                await sellapp_sdk.store_settings.get()
        finally:
            await sellapp_sdk.close()

    @pytest.mark.asyncio
    async def test_get_rate_limited(self, httpx_mock):
        sellapp_sdk = AsyncSellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(
                status_code=429,
                headers={"Retry-After": "0"},
                json={"message": "Slow down"},
            )
            with pytest.raises(RateLimitExceededError):
                await sellapp_sdk.store_settings.get()
        finally:
            await sellapp_sdk.close()

    @pytest.mark.asyncio
    async def test_get_server_error(self, httpx_mock):
        sellapp_sdk = AsyncSellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=500, json={"message": "Server error"})
            with pytest.raises(ServerError):
                await sellapp_sdk.store_settings.get()
        finally:
            await sellapp_sdk.close()

    @pytest.mark.asyncio
    async def test_get_bad_request(self, httpx_mock):
        sellapp_sdk = AsyncSellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=400, json={"message": "Bad request"})
            with pytest.raises(BadRequestError):
                await sellapp_sdk.store_settings.get()
        finally:
            await sellapp_sdk.close()

    @pytest.mark.asyncio
    async def test_get_unprocessable(self, httpx_mock):
        sellapp_sdk = AsyncSellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=422, json={"message": "Unprocessable"})
            with pytest.raises(UnprocessableEntityError):
                await sellapp_sdk.store_settings.get()
        finally:
            await sellapp_sdk.close()
