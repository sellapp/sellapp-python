# This file is auto-generated by oagen. Do not edit.

import json

import pytest

from sellapp_sdk import AsyncSellAppClient, SellAppClient
from sellapp_sdk._errors import (
    AuthenticationError,
    BadRequestError,
    NotFoundError,
    RateLimitExceededError,
    ServerError,
    UnprocessableEntityError,
)
from sellapp_sdk._pagination import AsyncPage, SyncPage
from sellapp_sdk.common.models import (
    ListSubscriptionsResponseValue200ApplicationJsonPropertyDataItem,
)
from sellapp_sdk.subscriptions.models import (
    SdkCancelSubscriptionAtPeriodEndResponseValue200ApplicationJson,
    SdkCancelSubscriptionImmediatelyResponseValue200ApplicationJson,
    SdkCancelSubscriptionResponseValue200ApplicationJson,
    SdkConfirmSubscriptionRenewalDateChangeResponseValue200ApplicationJson,
    SdkGetSubscriptionCapabilitiesResponseValue200ApplicationJson,
    SdkGetSubscriptionResponseValue200ApplicationJson,
    SdkPauseSubscriptionResponseValue200ApplicationJson,
    SdkPreviewSubscriptionRenewalDateChangeResponseValue200ApplicationJson,
    SdkResumeSubscriptionResponseValue200ApplicationJson,
    SearchSubscriptionsResponseValue200ApplicationJsonPropertyDataItem,
)
from tests.generated_helpers import load_fixture


class TestSubscriptions:
    def test_cancel(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_cancel_subscription_response_value_200_application_json.json"
            ),
        )
        result = sellapp_sdk.subscriptions.cancel(
            "test_subscription", cancel_at_period_end=True
        )
        assert isinstance(result, SdkCancelSubscriptionResponseValue200ApplicationJson)
        assert result.message == "test_message"
        request = httpx_mock.get_request()
        assert request.method == "PATCH"
        assert request.url.path.endswith("/v2/subscriptions/test_subscription/cancel")
        body = json.loads(request.content)
        assert body["cancel_at_period_end"] == True

    def test_get_capabilities(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_get_subscription_capabilities_response_value_200_application_json.json"
            ),
        )
        result = sellapp_sdk.subscriptions.get_capabilities("test_productSubscription")
        assert isinstance(
            result, SdkGetSubscriptionCapabilitiesResponseValue200ApplicationJson
        )
        request = httpx_mock.get_request()
        assert request.method == "GET"
        assert request.url.path.endswith(
            "/v2/subscriptions/test_productSubscription/capabilities"
        )

    def test_cancel_at_period_end(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_cancel_subscription_at_period_end_response_value_200_application_json.json"
            ),
        )
        result = sellapp_sdk.subscriptions.cancel_at_period_end(
            "test_productSubscription"
        )
        assert isinstance(
            result, SdkCancelSubscriptionAtPeriodEndResponseValue200ApplicationJson
        )
        assert result.message == "test_message"
        request = httpx_mock.get_request()
        assert request.method == "POST"
        assert request.url.path.endswith(
            "/v2/subscriptions/test_productSubscription/actions/cancel-period-end"
        )

    def test_cancel_immediately(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_cancel_subscription_immediately_response_value_200_application_json.json"
            ),
        )
        result = sellapp_sdk.subscriptions.cancel_immediately(
            "test_productSubscription"
        )
        assert isinstance(
            result, SdkCancelSubscriptionImmediatelyResponseValue200ApplicationJson
        )
        assert result.message == "test_message"
        request = httpx_mock.get_request()
        assert request.method == "POST"
        assert request.url.path.endswith(
            "/v2/subscriptions/test_productSubscription/actions/cancel-immediately"
        )

    def test_pause(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_pause_subscription_response_value_200_application_json.json"
            ),
        )
        result = sellapp_sdk.subscriptions.pause("test_productSubscription")
        assert isinstance(result, SdkPauseSubscriptionResponseValue200ApplicationJson)
        assert result.message == "test_message"
        request = httpx_mock.get_request()
        assert request.method == "POST"
        assert request.url.path.endswith(
            "/v2/subscriptions/test_productSubscription/actions/pause"
        )

    def test_resume(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_resume_subscription_response_value_200_application_json.json"
            ),
        )
        result = sellapp_sdk.subscriptions.resume("test_productSubscription")
        assert isinstance(result, SdkResumeSubscriptionResponseValue200ApplicationJson)
        assert result.message == "test_message"
        request = httpx_mock.get_request()
        assert request.method == "POST"
        assert request.url.path.endswith(
            "/v2/subscriptions/test_productSubscription/actions/resume"
        )

    def test_update_payment_method(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(json={})
        sellapp_sdk.subscriptions.update_payment_method("test_productSubscription")
        request = httpx_mock.get_request()
        assert request.method == "POST"
        assert request.url.path.endswith(
            "/v2/subscriptions/test_productSubscription/actions/update-payment-method"
        )

    def test_preview_plan_change(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(json={})
        sellapp_sdk.subscriptions.preview_plan_change(
            "test_productSubscription", target_variant_id=1
        )
        request = httpx_mock.get_request()
        assert request.method == "POST"
        assert request.url.path.endswith(
            "/v2/subscriptions/test_productSubscription/actions/change-plan/preview"
        )

    def test_confirm_plan_change(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(json={})
        sellapp_sdk.subscriptions.confirm_plan_change(
            "test_productSubscription",
            target_variant_id=1,
            preview_token="test_preview_token",
        )
        request = httpx_mock.get_request()
        assert request.method == "POST"
        assert request.url.path.endswith(
            "/v2/subscriptions/test_productSubscription/actions/change-plan/confirm"
        )

    def test_preview_renewal_date(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_preview_subscription_renewal_date_change_response_value_200_application_json.json"
            ),
        )
        result = sellapp_sdk.subscriptions.preview_renewal_date(
            "test_productSubscription", renewal_date="2023-01-01T00:00:00.000Z"
        )
        assert isinstance(
            result,
            SdkPreviewSubscriptionRenewalDateChangeResponseValue200ApplicationJson,
        )
        assert result.message == "test_message"
        request = httpx_mock.get_request()
        assert request.method == "POST"
        assert request.url.path.endswith(
            "/v2/subscriptions/test_productSubscription/actions/change-renewal-date/preview"
        )
        body = json.loads(request.content)
        assert body["renewal_date"] == "2023-01-01T00:00:00.000Z"

    def test_confirm_renewal_date(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_confirm_subscription_renewal_date_change_response_value_200_application_json.json"
            ),
        )
        result = sellapp_sdk.subscriptions.confirm_renewal_date(
            "test_productSubscription", renewal_date="2023-01-01T00:00:00.000Z"
        )
        assert isinstance(
            result,
            SdkConfirmSubscriptionRenewalDateChangeResponseValue200ApplicationJson,
        )
        assert result.message == "test_message"
        request = httpx_mock.get_request()
        assert request.method == "POST"
        assert request.url.path.endswith(
            "/v2/subscriptions/test_productSubscription/actions/change-renewal-date/confirm"
        )
        body = json.loads(request.content)
        assert body["renewal_date"] == "2023-01-01T00:00:00.000Z"

    def test_list_subscriptions(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "list_list_subscriptions_response_value_200_application_json_property_data_item.json"
            ),
        )
        page = sellapp_sdk.subscriptions.list_subscriptions()
        assert isinstance(page, SyncPage)
        assert len(page.data) == 1
        assert isinstance(
            page.data[0],
            ListSubscriptionsResponseValue200ApplicationJsonPropertyDataItem,
        )

    def test_list_subscriptions_empty_page(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(json={"data": [], "list_metadata": {}})
        page = sellapp_sdk.subscriptions.list_subscriptions()
        assert isinstance(page, SyncPage)
        assert page.data == []

    def test_search_subscriptions(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "list_search_subscriptions_response_value_200_application_json_property_data_item.json"
            ),
        )
        page = sellapp_sdk.subscriptions.search_subscriptions()
        assert isinstance(page, SyncPage)
        assert len(page.data) == 1
        assert isinstance(
            page.data[0],
            SearchSubscriptionsResponseValue200ApplicationJsonPropertyDataItem,
        )

    def test_search_subscriptions_empty_page(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(json={"data": [], "list_metadata": {}})
        page = sellapp_sdk.subscriptions.search_subscriptions()
        assert isinstance(page, SyncPage)
        assert page.data == []

    def test_get_subscription(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_get_subscription_response_value_200_application_json.json"
            ),
        )
        result = sellapp_sdk.subscriptions.get_subscription("test_productSubscription")
        assert isinstance(result, SdkGetSubscriptionResponseValue200ApplicationJson)
        request = httpx_mock.get_request()
        assert request.method == "GET"
        assert request.url.path.endswith("/v2/subscriptions/test_productSubscription")

    def test_cancel_with_request_options(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_cancel_subscription_response_value_200_application_json.json"
            )
        )
        sellapp_sdk.subscriptions.cancel(
            "test_subscription",
            cancel_at_period_end=True,
            request_options={"headers": {"X-Custom": "value"}},
        )
        request = httpx_mock.get_request()
        assert request.headers["X-Custom"] == "value"

    def test_cancel_unauthorized(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            status_code=401,
            json={"message": "Unauthorized"},
        )
        with pytest.raises(AuthenticationError):
            sellapp_sdk.subscriptions.cancel(
                "test_subscription", cancel_at_period_end=True
            )

    def test_cancel_not_found(self, httpx_mock):
        sellapp_sdk = SellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=404, json={"message": "Not found"})
            with pytest.raises(NotFoundError):
                sellapp_sdk.subscriptions.cancel(
                    "test_subscription", cancel_at_period_end=True
                )
        finally:
            sellapp_sdk.close()

    def test_cancel_rate_limited(self, httpx_mock):
        sellapp_sdk = SellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(
                status_code=429,
                headers={"Retry-After": "0"},
                json={"message": "Slow down"},
            )
            with pytest.raises(RateLimitExceededError):
                sellapp_sdk.subscriptions.cancel(
                    "test_subscription", cancel_at_period_end=True
                )
        finally:
            sellapp_sdk.close()

    def test_cancel_server_error(self, httpx_mock):
        sellapp_sdk = SellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=500, json={"message": "Server error"})
            with pytest.raises(ServerError):
                sellapp_sdk.subscriptions.cancel(
                    "test_subscription", cancel_at_period_end=True
                )
        finally:
            sellapp_sdk.close()

    def test_cancel_bad_request(self, httpx_mock):
        sellapp_sdk = SellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=400, json={"message": "Bad request"})
            with pytest.raises(BadRequestError):
                sellapp_sdk.subscriptions.cancel(
                    "test_subscription", cancel_at_period_end=True
                )
        finally:
            sellapp_sdk.close()

    def test_cancel_unprocessable(self, httpx_mock):
        sellapp_sdk = SellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=422, json={"message": "Unprocessable"})
            with pytest.raises(UnprocessableEntityError):
                sellapp_sdk.subscriptions.cancel(
                    "test_subscription", cancel_at_period_end=True
                )
        finally:
            sellapp_sdk.close()


class TestAsyncSubscriptions:
    @pytest.mark.asyncio
    async def test_cancel(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_cancel_subscription_response_value_200_application_json.json"
            )
        )
        result = await async_sellapp_sdk.subscriptions.cancel(
            "test_subscription", cancel_at_period_end=True
        )
        assert isinstance(result, SdkCancelSubscriptionResponseValue200ApplicationJson)
        assert result.message == "test_message"
        request = httpx_mock.get_request()
        assert request.method == "PATCH"
        assert request.url.path.endswith("/v2/subscriptions/test_subscription/cancel")

    @pytest.mark.asyncio
    async def test_get_capabilities(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_get_subscription_capabilities_response_value_200_application_json.json"
            )
        )
        result = await async_sellapp_sdk.subscriptions.get_capabilities(
            "test_productSubscription"
        )
        assert isinstance(
            result, SdkGetSubscriptionCapabilitiesResponseValue200ApplicationJson
        )
        request = httpx_mock.get_request()
        assert request.method == "GET"
        assert request.url.path.endswith(
            "/v2/subscriptions/test_productSubscription/capabilities"
        )

    @pytest.mark.asyncio
    async def test_cancel_at_period_end(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_cancel_subscription_at_period_end_response_value_200_application_json.json"
            )
        )
        result = await async_sellapp_sdk.subscriptions.cancel_at_period_end(
            "test_productSubscription"
        )
        assert isinstance(
            result, SdkCancelSubscriptionAtPeriodEndResponseValue200ApplicationJson
        )
        assert result.message == "test_message"
        request = httpx_mock.get_request()
        assert request.method == "POST"
        assert request.url.path.endswith(
            "/v2/subscriptions/test_productSubscription/actions/cancel-period-end"
        )

    @pytest.mark.asyncio
    async def test_cancel_immediately(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_cancel_subscription_immediately_response_value_200_application_json.json"
            )
        )
        result = await async_sellapp_sdk.subscriptions.cancel_immediately(
            "test_productSubscription"
        )
        assert isinstance(
            result, SdkCancelSubscriptionImmediatelyResponseValue200ApplicationJson
        )
        assert result.message == "test_message"
        request = httpx_mock.get_request()
        assert request.method == "POST"
        assert request.url.path.endswith(
            "/v2/subscriptions/test_productSubscription/actions/cancel-immediately"
        )

    @pytest.mark.asyncio
    async def test_pause(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_pause_subscription_response_value_200_application_json.json"
            )
        )
        result = await async_sellapp_sdk.subscriptions.pause("test_productSubscription")
        assert isinstance(result, SdkPauseSubscriptionResponseValue200ApplicationJson)
        assert result.message == "test_message"
        request = httpx_mock.get_request()
        assert request.method == "POST"
        assert request.url.path.endswith(
            "/v2/subscriptions/test_productSubscription/actions/pause"
        )

    @pytest.mark.asyncio
    async def test_resume(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_resume_subscription_response_value_200_application_json.json"
            )
        )
        result = await async_sellapp_sdk.subscriptions.resume(
            "test_productSubscription"
        )
        assert isinstance(result, SdkResumeSubscriptionResponseValue200ApplicationJson)
        assert result.message == "test_message"
        request = httpx_mock.get_request()
        assert request.method == "POST"
        assert request.url.path.endswith(
            "/v2/subscriptions/test_productSubscription/actions/resume"
        )

    @pytest.mark.asyncio
    async def test_update_payment_method(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(json={})
        await async_sellapp_sdk.subscriptions.update_payment_method(
            "test_productSubscription"
        )
        request = httpx_mock.get_request()
        assert request.method == "POST"
        assert request.url.path.endswith(
            "/v2/subscriptions/test_productSubscription/actions/update-payment-method"
        )

    @pytest.mark.asyncio
    async def test_preview_plan_change(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(json={})
        await async_sellapp_sdk.subscriptions.preview_plan_change(
            "test_productSubscription", target_variant_id=1
        )
        request = httpx_mock.get_request()
        assert request.method == "POST"
        assert request.url.path.endswith(
            "/v2/subscriptions/test_productSubscription/actions/change-plan/preview"
        )

    @pytest.mark.asyncio
    async def test_confirm_plan_change(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(json={})
        await async_sellapp_sdk.subscriptions.confirm_plan_change(
            "test_productSubscription",
            target_variant_id=1,
            preview_token="test_preview_token",
        )
        request = httpx_mock.get_request()
        assert request.method == "POST"
        assert request.url.path.endswith(
            "/v2/subscriptions/test_productSubscription/actions/change-plan/confirm"
        )

    @pytest.mark.asyncio
    async def test_preview_renewal_date(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_preview_subscription_renewal_date_change_response_value_200_application_json.json"
            )
        )
        result = await async_sellapp_sdk.subscriptions.preview_renewal_date(
            "test_productSubscription", renewal_date="2023-01-01T00:00:00.000Z"
        )
        assert isinstance(
            result,
            SdkPreviewSubscriptionRenewalDateChangeResponseValue200ApplicationJson,
        )
        assert result.message == "test_message"
        request = httpx_mock.get_request()
        assert request.method == "POST"
        assert request.url.path.endswith(
            "/v2/subscriptions/test_productSubscription/actions/change-renewal-date/preview"
        )

    @pytest.mark.asyncio
    async def test_confirm_renewal_date(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_confirm_subscription_renewal_date_change_response_value_200_application_json.json"
            )
        )
        result = await async_sellapp_sdk.subscriptions.confirm_renewal_date(
            "test_productSubscription", renewal_date="2023-01-01T00:00:00.000Z"
        )
        assert isinstance(
            result,
            SdkConfirmSubscriptionRenewalDateChangeResponseValue200ApplicationJson,
        )
        assert result.message == "test_message"
        request = httpx_mock.get_request()
        assert request.method == "POST"
        assert request.url.path.endswith(
            "/v2/subscriptions/test_productSubscription/actions/change-renewal-date/confirm"
        )

    @pytest.mark.asyncio
    async def test_list_subscriptions(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "list_list_subscriptions_response_value_200_application_json_property_data_item.json"
            )
        )
        page = await async_sellapp_sdk.subscriptions.list_subscriptions()
        assert isinstance(page, AsyncPage)
        assert len(page.data) == 1
        assert isinstance(
            page.data[0],
            ListSubscriptionsResponseValue200ApplicationJsonPropertyDataItem,
        )

    @pytest.mark.asyncio
    async def test_list_subscriptions_empty_page(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(json={"data": [], "list_metadata": {}})
        page = await async_sellapp_sdk.subscriptions.list_subscriptions()
        assert isinstance(page, AsyncPage)
        assert page.data == []

    @pytest.mark.asyncio
    async def test_search_subscriptions(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "list_search_subscriptions_response_value_200_application_json_property_data_item.json"
            )
        )
        page = await async_sellapp_sdk.subscriptions.search_subscriptions()
        assert isinstance(page, AsyncPage)
        assert len(page.data) == 1
        assert isinstance(
            page.data[0],
            SearchSubscriptionsResponseValue200ApplicationJsonPropertyDataItem,
        )

    @pytest.mark.asyncio
    async def test_search_subscriptions_empty_page(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(json={"data": [], "list_metadata": {}})
        page = await async_sellapp_sdk.subscriptions.search_subscriptions()
        assert isinstance(page, AsyncPage)
        assert page.data == []

    @pytest.mark.asyncio
    async def test_get_subscription(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_get_subscription_response_value_200_application_json.json"
            )
        )
        result = await async_sellapp_sdk.subscriptions.get_subscription(
            "test_productSubscription"
        )
        assert isinstance(result, SdkGetSubscriptionResponseValue200ApplicationJson)
        request = httpx_mock.get_request()
        assert request.method == "GET"
        assert request.url.path.endswith("/v2/subscriptions/test_productSubscription")

    @pytest.mark.asyncio
    async def test_cancel_with_request_options(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_cancel_subscription_response_value_200_application_json.json"
            )
        )
        await async_sellapp_sdk.subscriptions.cancel(
            "test_subscription",
            cancel_at_period_end=True,
            request_options={"headers": {"X-Custom": "value"}},
        )
        request = httpx_mock.get_request()
        assert request.headers["X-Custom"] == "value"

    @pytest.mark.asyncio
    async def test_cancel_unauthorized(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(status_code=401, json={"message": "Unauthorized"})
        with pytest.raises(AuthenticationError):
            await async_sellapp_sdk.subscriptions.cancel(
                "test_subscription", cancel_at_period_end=True
            )

    @pytest.mark.asyncio
    async def test_cancel_not_found(self, httpx_mock):
        sellapp_sdk = AsyncSellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=404, json={"message": "Not found"})
            with pytest.raises(NotFoundError):
                await sellapp_sdk.subscriptions.cancel(
                    "test_subscription", cancel_at_period_end=True
                )
        finally:
            await sellapp_sdk.close()

    @pytest.mark.asyncio
    async def test_cancel_rate_limited(self, httpx_mock):
        sellapp_sdk = AsyncSellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(
                status_code=429,
                headers={"Retry-After": "0"},
                json={"message": "Slow down"},
            )
            with pytest.raises(RateLimitExceededError):
                await sellapp_sdk.subscriptions.cancel(
                    "test_subscription", cancel_at_period_end=True
                )
        finally:
            await sellapp_sdk.close()

    @pytest.mark.asyncio
    async def test_cancel_server_error(self, httpx_mock):
        sellapp_sdk = AsyncSellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=500, json={"message": "Server error"})
            with pytest.raises(ServerError):
                await sellapp_sdk.subscriptions.cancel(
                    "test_subscription", cancel_at_period_end=True
                )
        finally:
            await sellapp_sdk.close()

    @pytest.mark.asyncio
    async def test_cancel_bad_request(self, httpx_mock):
        sellapp_sdk = AsyncSellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=400, json={"message": "Bad request"})
            with pytest.raises(BadRequestError):
                await sellapp_sdk.subscriptions.cancel(
                    "test_subscription", cancel_at_period_end=True
                )
        finally:
            await sellapp_sdk.close()

    @pytest.mark.asyncio
    async def test_cancel_unprocessable(self, httpx_mock):
        sellapp_sdk = AsyncSellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=422, json={"message": "Unprocessable"})
            with pytest.raises(UnprocessableEntityError):
                await sellapp_sdk.subscriptions.cancel(
                    "test_subscription", cancel_at_period_end=True
                )
        finally:
            await sellapp_sdk.close()
