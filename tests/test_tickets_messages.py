# This file is auto-generated by oagen. Do not edit.

import json

import pytest

from sellapp_sdk import AsyncSellAppClient, SellAppClient
from sellapp_sdk._errors import (
    AuthenticationError,
    BadRequestError,
    NotFoundError,
    RateLimitExceededError,
    ServerError,
    UnprocessableEntityError,
)
from sellapp_sdk._pagination import AsyncPage, SyncPage
from sellapp_sdk.blacklists.models import (
    SdkReplyToTicketResponseValue201ApplicationJson,
    SdkV2GetTicketMessageResponseValue200ApplicationJson,
    SdkV2ReplyToTicketResponseValue201ApplicationJson,
    SearchTicketMessagesResponseValue200ApplicationJsonPropertyDataItem,
    V2ListTicketMessagesResponseValue200ApplicationJsonPropertyDataItem,
    V2SearchTicketMessagesResponseValue200ApplicationJsonPropertyDataItem,
)
from sellapp_sdk.common.models import (
    ListTicketMessagesResponseValue200ApplicationJsonPropertyDataItem,
    SdkGetTicketMessageResponseValue200ApplicationJson,
)
from tests.generated_helpers import load_fixture


class TestTicketsMessages:
    def test_list(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "list_list_ticket_messages_response_value_200_application_json_property_data_item.json"
            ),
        )
        page = sellapp_sdk.tickets_messages.list("test_ticket")
        assert isinstance(page, SyncPage)
        assert len(page.data) == 1
        assert isinstance(
            page.data[0],
            ListTicketMessagesResponseValue200ApplicationJsonPropertyDataItem,
        )

    def test_list_empty_page(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(json={"data": [], "list_metadata": {}})
        page = sellapp_sdk.tickets_messages.list("test_ticket")
        assert isinstance(page, SyncPage)
        assert page.data == []

    def test_list_encodes_query_params(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(json={"data": [], "list_metadata": {}})
        sellapp_sdk.tickets_messages.list(
            "test_ticket",
            limit=10,
            before="cursor before",
            after="cursor/after",
            page=7,
        )
        request = httpx_mock.get_request()
        assert request.url.params["limit"] == "10"
        assert request.url.params["before"] == "cursor before"
        assert request.url.params["after"] == "cursor/after"
        assert request.url.params["page"] == "7"

    def test_reply(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_reply_to_ticket_response_value_201_application_json.json"
            ),
        )
        result = sellapp_sdk.tickets_messages.reply(
            "test_ticket", content="test_content"
        )
        assert isinstance(result, SdkReplyToTicketResponseValue201ApplicationJson)
        request = httpx_mock.get_request()
        assert request.method == "POST"
        assert request.url.path.endswith("/v1/tickets/test_ticket/messages")
        body = json.loads(request.content)
        assert body["content"] == "test_content"

    def test_get(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_get_ticket_message_response_value_200_application_json.json"
            ),
        )
        result = sellapp_sdk.tickets_messages.get("test_ticket", "test_message")
        assert isinstance(result, SdkGetTicketMessageResponseValue200ApplicationJson)
        request = httpx_mock.get_request()
        assert request.method == "GET"
        assert request.url.path.endswith(
            "/v1/tickets/test_ticket/messages/test_message"
        )

    def test_search(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "list_search_ticket_messages_response_value_200_application_json_property_data_item.json"
            ),
        )
        page = sellapp_sdk.tickets_messages.search("test_ticket")
        assert isinstance(page, SyncPage)
        assert len(page.data) == 1
        assert isinstance(
            page.data[0],
            SearchTicketMessagesResponseValue200ApplicationJsonPropertyDataItem,
        )

    def test_search_empty_page(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(json={"data": [], "list_metadata": {}})
        page = sellapp_sdk.tickets_messages.search("test_ticket")
        assert isinstance(page, SyncPage)
        assert page.data == []

    def test_v2_list_ticket_messages(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "list_v_2_list_ticket_messages_response_value_200_application_json_property_data_item.json"
            ),
        )
        page = sellapp_sdk.tickets_messages.v2_list_ticket_messages("test_ticket")
        assert isinstance(page, SyncPage)
        assert len(page.data) == 1
        assert isinstance(
            page.data[0],
            V2ListTicketMessagesResponseValue200ApplicationJsonPropertyDataItem,
        )

    def test_v2_list_ticket_messages_empty_page(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(json={"data": [], "list_metadata": {}})
        page = sellapp_sdk.tickets_messages.v2_list_ticket_messages("test_ticket")
        assert isinstance(page, SyncPage)
        assert page.data == []

    def test_v2_list_ticket_messages_encodes_query_params(
        self, sellapp_sdk, httpx_mock
    ):
        httpx_mock.add_response(json={"data": [], "list_metadata": {}})
        sellapp_sdk.tickets_messages.v2_list_ticket_messages(
            "test_ticket",
            limit=10,
            before="cursor before",
            after="cursor/after",
            page=7,
        )
        request = httpx_mock.get_request()
        assert request.url.params["limit"] == "10"
        assert request.url.params["before"] == "cursor before"
        assert request.url.params["after"] == "cursor/after"
        assert request.url.params["page"] == "7"

    def test_v2_reply_to_ticket(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_v_2_reply_to_ticket_response_value_201_application_json.json"
            ),
        )
        result = sellapp_sdk.tickets_messages.v2_reply_to_ticket(
            "test_ticket", content="test_content"
        )
        assert isinstance(result, SdkV2ReplyToTicketResponseValue201ApplicationJson)
        request = httpx_mock.get_request()
        assert request.method == "POST"
        assert request.url.path.endswith("/v2/tickets/test_ticket/messages")
        body = json.loads(request.content)
        assert body["content"] == "test_content"

    def test_v2_search_ticket_messages(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "list_v_2_search_ticket_messages_response_value_200_application_json_property_data_item.json"
            ),
        )
        page = sellapp_sdk.tickets_messages.v2_search_ticket_messages("test_ticket")
        assert isinstance(page, SyncPage)
        assert len(page.data) == 1
        assert isinstance(
            page.data[0],
            V2SearchTicketMessagesResponseValue200ApplicationJsonPropertyDataItem,
        )

    def test_v2_search_ticket_messages_empty_page(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(json={"data": [], "list_metadata": {}})
        page = sellapp_sdk.tickets_messages.v2_search_ticket_messages("test_ticket")
        assert isinstance(page, SyncPage)
        assert page.data == []

    def test_v2_get_ticket_message(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_v_2_get_ticket_message_response_value_200_application_json.json"
            ),
        )
        result = sellapp_sdk.tickets_messages.v2_get_ticket_message(
            "test_ticket", "test_message"
        )
        assert isinstance(result, SdkV2GetTicketMessageResponseValue200ApplicationJson)
        request = httpx_mock.get_request()
        assert request.method == "GET"
        assert request.url.path.endswith(
            "/v2/tickets/test_ticket/messages/test_message"
        )

    def test_list_with_request_options(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(json={"data": [], "list_metadata": {}})
        sellapp_sdk.tickets_messages.list(
            "test_ticket", request_options={"headers": {"X-Custom": "value"}}
        )
        request = httpx_mock.get_request()
        assert request.headers["X-Custom"] == "value"

    def test_list_unauthorized(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            status_code=401,
            json={"message": "Unauthorized"},
        )
        with pytest.raises(AuthenticationError):
            sellapp_sdk.tickets_messages.list("test_ticket")

    def test_list_not_found(self, httpx_mock):
        sellapp_sdk = SellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=404, json={"message": "Not found"})
            with pytest.raises(NotFoundError):
                sellapp_sdk.tickets_messages.list("test_ticket")
        finally:
            sellapp_sdk.close()

    def test_list_rate_limited(self, httpx_mock):
        sellapp_sdk = SellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(
                status_code=429,
                headers={"Retry-After": "0"},
                json={"message": "Slow down"},
            )
            with pytest.raises(RateLimitExceededError):
                sellapp_sdk.tickets_messages.list("test_ticket")
        finally:
            sellapp_sdk.close()

    def test_list_server_error(self, httpx_mock):
        sellapp_sdk = SellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=500, json={"message": "Server error"})
            with pytest.raises(ServerError):
                sellapp_sdk.tickets_messages.list("test_ticket")
        finally:
            sellapp_sdk.close()

    def test_list_bad_request(self, httpx_mock):
        sellapp_sdk = SellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=400, json={"message": "Bad request"})
            with pytest.raises(BadRequestError):
                sellapp_sdk.tickets_messages.list("test_ticket")
        finally:
            sellapp_sdk.close()

    def test_list_unprocessable(self, httpx_mock):
        sellapp_sdk = SellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=422, json={"message": "Unprocessable"})
            with pytest.raises(UnprocessableEntityError):
                sellapp_sdk.tickets_messages.list("test_ticket")
        finally:
            sellapp_sdk.close()


class TestAsyncTicketsMessages:
    @pytest.mark.asyncio
    async def test_list(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "list_list_ticket_messages_response_value_200_application_json_property_data_item.json"
            )
        )
        page = await async_sellapp_sdk.tickets_messages.list("test_ticket")
        assert isinstance(page, AsyncPage)
        assert len(page.data) == 1
        assert isinstance(
            page.data[0],
            ListTicketMessagesResponseValue200ApplicationJsonPropertyDataItem,
        )

    @pytest.mark.asyncio
    async def test_list_empty_page(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(json={"data": [], "list_metadata": {}})
        page = await async_sellapp_sdk.tickets_messages.list("test_ticket")
        assert isinstance(page, AsyncPage)
        assert page.data == []

    @pytest.mark.asyncio
    async def test_list_encodes_query_params(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(json={"data": [], "list_metadata": {}})
        await async_sellapp_sdk.tickets_messages.list(
            "test_ticket",
            limit=10,
            before="cursor before",
            after="cursor/after",
            page=7,
        )
        request = httpx_mock.get_request()
        assert request.url.params["limit"] == "10"
        assert request.url.params["before"] == "cursor before"
        assert request.url.params["after"] == "cursor/after"
        assert request.url.params["page"] == "7"

    @pytest.mark.asyncio
    async def test_reply(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_reply_to_ticket_response_value_201_application_json.json"
            )
        )
        result = await async_sellapp_sdk.tickets_messages.reply(
            "test_ticket", content="test_content"
        )
        assert isinstance(result, SdkReplyToTicketResponseValue201ApplicationJson)
        request = httpx_mock.get_request()
        assert request.method == "POST"
        assert request.url.path.endswith("/v1/tickets/test_ticket/messages")

    @pytest.mark.asyncio
    async def test_get(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_get_ticket_message_response_value_200_application_json.json"
            )
        )
        result = await async_sellapp_sdk.tickets_messages.get(
            "test_ticket", "test_message"
        )
        assert isinstance(result, SdkGetTicketMessageResponseValue200ApplicationJson)
        request = httpx_mock.get_request()
        assert request.method == "GET"
        assert request.url.path.endswith(
            "/v1/tickets/test_ticket/messages/test_message"
        )

    @pytest.mark.asyncio
    async def test_search(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "list_search_ticket_messages_response_value_200_application_json_property_data_item.json"
            )
        )
        page = await async_sellapp_sdk.tickets_messages.search("test_ticket")
        assert isinstance(page, AsyncPage)
        assert len(page.data) == 1
        assert isinstance(
            page.data[0],
            SearchTicketMessagesResponseValue200ApplicationJsonPropertyDataItem,
        )

    @pytest.mark.asyncio
    async def test_search_empty_page(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(json={"data": [], "list_metadata": {}})
        page = await async_sellapp_sdk.tickets_messages.search("test_ticket")
        assert isinstance(page, AsyncPage)
        assert page.data == []

    @pytest.mark.asyncio
    async def test_v2_list_ticket_messages(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "list_v_2_list_ticket_messages_response_value_200_application_json_property_data_item.json"
            )
        )
        page = await async_sellapp_sdk.tickets_messages.v2_list_ticket_messages(
            "test_ticket"
        )
        assert isinstance(page, AsyncPage)
        assert len(page.data) == 1
        assert isinstance(
            page.data[0],
            V2ListTicketMessagesResponseValue200ApplicationJsonPropertyDataItem,
        )

    @pytest.mark.asyncio
    async def test_v2_list_ticket_messages_empty_page(
        self, async_sellapp_sdk, httpx_mock
    ):
        httpx_mock.add_response(json={"data": [], "list_metadata": {}})
        page = await async_sellapp_sdk.tickets_messages.v2_list_ticket_messages(
            "test_ticket"
        )
        assert isinstance(page, AsyncPage)
        assert page.data == []

    @pytest.mark.asyncio
    async def test_v2_list_ticket_messages_encodes_query_params(
        self, async_sellapp_sdk, httpx_mock
    ):
        httpx_mock.add_response(json={"data": [], "list_metadata": {}})
        await async_sellapp_sdk.tickets_messages.v2_list_ticket_messages(
            "test_ticket",
            limit=10,
            before="cursor before",
            after="cursor/after",
            page=7,
        )
        request = httpx_mock.get_request()
        assert request.url.params["limit"] == "10"
        assert request.url.params["before"] == "cursor before"
        assert request.url.params["after"] == "cursor/after"
        assert request.url.params["page"] == "7"

    @pytest.mark.asyncio
    async def test_v2_reply_to_ticket(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_v_2_reply_to_ticket_response_value_201_application_json.json"
            )
        )
        result = await async_sellapp_sdk.tickets_messages.v2_reply_to_ticket(
            "test_ticket", content="test_content"
        )
        assert isinstance(result, SdkV2ReplyToTicketResponseValue201ApplicationJson)
        request = httpx_mock.get_request()
        assert request.method == "POST"
        assert request.url.path.endswith("/v2/tickets/test_ticket/messages")

    @pytest.mark.asyncio
    async def test_v2_search_ticket_messages(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "list_v_2_search_ticket_messages_response_value_200_application_json_property_data_item.json"
            )
        )
        page = await async_sellapp_sdk.tickets_messages.v2_search_ticket_messages(
            "test_ticket"
        )
        assert isinstance(page, AsyncPage)
        assert len(page.data) == 1
        assert isinstance(
            page.data[0],
            V2SearchTicketMessagesResponseValue200ApplicationJsonPropertyDataItem,
        )

    @pytest.mark.asyncio
    async def test_v2_search_ticket_messages_empty_page(
        self, async_sellapp_sdk, httpx_mock
    ):
        httpx_mock.add_response(json={"data": [], "list_metadata": {}})
        page = await async_sellapp_sdk.tickets_messages.v2_search_ticket_messages(
            "test_ticket"
        )
        assert isinstance(page, AsyncPage)
        assert page.data == []

    @pytest.mark.asyncio
    async def test_v2_get_ticket_message(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_v_2_get_ticket_message_response_value_200_application_json.json"
            )
        )
        result = await async_sellapp_sdk.tickets_messages.v2_get_ticket_message(
            "test_ticket", "test_message"
        )
        assert isinstance(result, SdkV2GetTicketMessageResponseValue200ApplicationJson)
        request = httpx_mock.get_request()
        assert request.method == "GET"
        assert request.url.path.endswith(
            "/v2/tickets/test_ticket/messages/test_message"
        )

    @pytest.mark.asyncio
    async def test_list_with_request_options(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(json={"data": [], "list_metadata": {}})
        await async_sellapp_sdk.tickets_messages.list(
            "test_ticket", request_options={"headers": {"X-Custom": "value"}}
        )
        request = httpx_mock.get_request()
        assert request.headers["X-Custom"] == "value"

    @pytest.mark.asyncio
    async def test_list_unauthorized(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(status_code=401, json={"message": "Unauthorized"})
        with pytest.raises(AuthenticationError):
            await async_sellapp_sdk.tickets_messages.list("test_ticket")

    @pytest.mark.asyncio
    async def test_list_not_found(self, httpx_mock):
        sellapp_sdk = AsyncSellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=404, json={"message": "Not found"})
            with pytest.raises(NotFoundError):
                await sellapp_sdk.tickets_messages.list("test_ticket")
        finally:
            await sellapp_sdk.close()

    @pytest.mark.asyncio
    async def test_list_rate_limited(self, httpx_mock):
        sellapp_sdk = AsyncSellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(
                status_code=429,
                headers={"Retry-After": "0"},
                json={"message": "Slow down"},
            )
            with pytest.raises(RateLimitExceededError):
                await sellapp_sdk.tickets_messages.list("test_ticket")
        finally:
            await sellapp_sdk.close()

    @pytest.mark.asyncio
    async def test_list_server_error(self, httpx_mock):
        sellapp_sdk = AsyncSellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=500, json={"message": "Server error"})
            with pytest.raises(ServerError):
                await sellapp_sdk.tickets_messages.list("test_ticket")
        finally:
            await sellapp_sdk.close()

    @pytest.mark.asyncio
    async def test_list_bad_request(self, httpx_mock):
        sellapp_sdk = AsyncSellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=400, json={"message": "Bad request"})
            with pytest.raises(BadRequestError):
                await sellapp_sdk.tickets_messages.list("test_ticket")
        finally:
            await sellapp_sdk.close()

    @pytest.mark.asyncio
    async def test_list_unprocessable(self, httpx_mock):
        sellapp_sdk = AsyncSellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=422, json={"message": "Unprocessable"})
            with pytest.raises(UnprocessableEntityError):
                await sellapp_sdk.tickets_messages.list("test_ticket")
        finally:
            await sellapp_sdk.close()
