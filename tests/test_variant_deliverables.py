# This file is auto-generated by oagen. Do not edit.

import json

import pytest

from sellapp_sdk import AsyncSellAppClient, SellAppClient
from sellapp_sdk._errors import (
    AuthenticationError,
    BadRequestError,
    NotFoundError,
    RateLimitExceededError,
    ServerError,
    UnprocessableEntityError,
)
from sellapp_sdk.common.models import (
    SdkReplaceVariantDeliverableConfigurationRequestApplicationJsonTypes,
)
from sellapp_sdk.variant_deliverables.models import (
    ReplaceVariantDeliverableConfigurationRequestApplicationJsonPropertyData,
    SdkGetVariantDeliverableConfigurationResponseValue200ApplicationJson,
    SdkReplaceVariantDeliverableConfigurationResponseValue200ApplicationJson,
)
from tests.generated_helpers import load_fixture


class TestVariantDeliverables:
    def test_get(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_get_variant_deliverable_configuration_response_value_200_application_json.json"
            ),
        )
        result = sellapp_sdk.variant_deliverables.get("test_product", "test_variant")
        assert isinstance(
            result, SdkGetVariantDeliverableConfigurationResponseValue200ApplicationJson
        )
        request = httpx_mock.get_request()
        assert request.method == "GET"
        assert request.url.path.endswith(
            "/v2/products/test_product/variants/test_variant/deliverable"
        )

    def test_replace(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_replace_variant_deliverable_configuration_response_value_200_application_json.json"
            ),
        )
        result = sellapp_sdk.variant_deliverables.replace(
            "test_product",
            "test_variant",
            types=[
                SdkReplaceVariantDeliverableConfigurationRequestApplicationJsonTypes(
                    "DOWNLOADABLE"
                )
            ],
            data=ReplaceVariantDeliverableConfigurationRequestApplicationJsonPropertyData.from_dict(
                load_fixture(
                    "replace_variant_deliverable_configuration_request_application_json_property_data.json"
                )
            ),
        )
        assert isinstance(
            result,
            SdkReplaceVariantDeliverableConfigurationResponseValue200ApplicationJson,
        )
        request = httpx_mock.get_request()
        assert request.method == "PUT"
        assert request.url.path.endswith(
            "/v2/products/test_product/variants/test_variant/deliverable"
        )
        body = json.loads(request.content)
        assert "types" in body
        assert "data" in body

    def test_get_with_request_options(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_get_variant_deliverable_configuration_response_value_200_application_json.json"
            )
        )
        sellapp_sdk.variant_deliverables.get(
            "test_product",
            "test_variant",
            request_options={"headers": {"X-Custom": "value"}},
        )
        request = httpx_mock.get_request()
        assert request.headers["X-Custom"] == "value"

    def test_get_unauthorized(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            status_code=401,
            json={"message": "Unauthorized"},
        )
        with pytest.raises(AuthenticationError):
            sellapp_sdk.variant_deliverables.get("test_product", "test_variant")

    def test_get_not_found(self, httpx_mock):
        sellapp_sdk = SellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=404, json={"message": "Not found"})
            with pytest.raises(NotFoundError):
                sellapp_sdk.variant_deliverables.get("test_product", "test_variant")
        finally:
            sellapp_sdk.close()

    def test_get_rate_limited(self, httpx_mock):
        sellapp_sdk = SellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(
                status_code=429,
                headers={"Retry-After": "0"},
                json={"message": "Slow down"},
            )
            with pytest.raises(RateLimitExceededError):
                sellapp_sdk.variant_deliverables.get("test_product", "test_variant")
        finally:
            sellapp_sdk.close()

    def test_get_server_error(self, httpx_mock):
        sellapp_sdk = SellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=500, json={"message": "Server error"})
            with pytest.raises(ServerError):
                sellapp_sdk.variant_deliverables.get("test_product", "test_variant")
        finally:
            sellapp_sdk.close()

    def test_get_bad_request(self, httpx_mock):
        sellapp_sdk = SellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=400, json={"message": "Bad request"})
            with pytest.raises(BadRequestError):
                sellapp_sdk.variant_deliverables.get("test_product", "test_variant")
        finally:
            sellapp_sdk.close()

    def test_get_unprocessable(self, httpx_mock):
        sellapp_sdk = SellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=422, json={"message": "Unprocessable"})
            with pytest.raises(UnprocessableEntityError):
                sellapp_sdk.variant_deliverables.get("test_product", "test_variant")
        finally:
            sellapp_sdk.close()


class TestAsyncVariantDeliverables:
    @pytest.mark.asyncio
    async def test_get(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_get_variant_deliverable_configuration_response_value_200_application_json.json"
            )
        )
        result = await async_sellapp_sdk.variant_deliverables.get(
            "test_product", "test_variant"
        )
        assert isinstance(
            result, SdkGetVariantDeliverableConfigurationResponseValue200ApplicationJson
        )
        request = httpx_mock.get_request()
        assert request.method == "GET"
        assert request.url.path.endswith(
            "/v2/products/test_product/variants/test_variant/deliverable"
        )

    @pytest.mark.asyncio
    async def test_replace(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_replace_variant_deliverable_configuration_response_value_200_application_json.json"
            )
        )
        result = await async_sellapp_sdk.variant_deliverables.replace(
            "test_product",
            "test_variant",
            types=[
                SdkReplaceVariantDeliverableConfigurationRequestApplicationJsonTypes(
                    "DOWNLOADABLE"
                )
            ],
            data=ReplaceVariantDeliverableConfigurationRequestApplicationJsonPropertyData.from_dict(
                load_fixture(
                    "replace_variant_deliverable_configuration_request_application_json_property_data.json"
                )
            ),
        )
        assert isinstance(
            result,
            SdkReplaceVariantDeliverableConfigurationResponseValue200ApplicationJson,
        )
        request = httpx_mock.get_request()
        assert request.method == "PUT"
        assert request.url.path.endswith(
            "/v2/products/test_product/variants/test_variant/deliverable"
        )

    @pytest.mark.asyncio
    async def test_get_with_request_options(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_get_variant_deliverable_configuration_response_value_200_application_json.json"
            )
        )
        await async_sellapp_sdk.variant_deliverables.get(
            "test_product",
            "test_variant",
            request_options={"headers": {"X-Custom": "value"}},
        )
        request = httpx_mock.get_request()
        assert request.headers["X-Custom"] == "value"

    @pytest.mark.asyncio
    async def test_get_unauthorized(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(status_code=401, json={"message": "Unauthorized"})
        with pytest.raises(AuthenticationError):
            await async_sellapp_sdk.variant_deliverables.get(
                "test_product", "test_variant"
            )

    @pytest.mark.asyncio
    async def test_get_not_found(self, httpx_mock):
        sellapp_sdk = AsyncSellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=404, json={"message": "Not found"})
            with pytest.raises(NotFoundError):
                await sellapp_sdk.variant_deliverables.get(
                    "test_product", "test_variant"
                )
        finally:
            await sellapp_sdk.close()

    @pytest.mark.asyncio
    async def test_get_rate_limited(self, httpx_mock):
        sellapp_sdk = AsyncSellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(
                status_code=429,
                headers={"Retry-After": "0"},
                json={"message": "Slow down"},
            )
            with pytest.raises(RateLimitExceededError):
                await sellapp_sdk.variant_deliverables.get(
                    "test_product", "test_variant"
                )
        finally:
            await sellapp_sdk.close()

    @pytest.mark.asyncio
    async def test_get_server_error(self, httpx_mock):
        sellapp_sdk = AsyncSellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=500, json={"message": "Server error"})
            with pytest.raises(ServerError):
                await sellapp_sdk.variant_deliverables.get(
                    "test_product", "test_variant"
                )
        finally:
            await sellapp_sdk.close()

    @pytest.mark.asyncio
    async def test_get_bad_request(self, httpx_mock):
        sellapp_sdk = AsyncSellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=400, json={"message": "Bad request"})
            with pytest.raises(BadRequestError):
                await sellapp_sdk.variant_deliverables.get(
                    "test_product", "test_variant"
                )
        finally:
            await sellapp_sdk.close()

    @pytest.mark.asyncio
    async def test_get_unprocessable(self, httpx_mock):
        sellapp_sdk = AsyncSellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=422, json={"message": "Unprocessable"})
            with pytest.raises(UnprocessableEntityError):
                await sellapp_sdk.variant_deliverables.get(
                    "test_product", "test_variant"
                )
        finally:
            await sellapp_sdk.close()
