# This file is auto-generated by oagen. Do not edit.

import json

import pytest

from sellapp_sdk import AsyncSellAppClient, SellAppClient
from sellapp_sdk._errors import (
    AuthenticationError,
    BadRequestError,
    NotFoundError,
    RateLimitExceededError,
    ServerError,
    UnprocessableEntityError,
)
from sellapp_sdk.common.models import (
    SdkCreateWalletBonusTierRequestApplicationJsonBonusKind,
    SdkCreateWalletBonusTierResponseValue201ApplicationJson,
    SdkReplaceWalletBonusTierRequestApplicationJsonBonusKind,
)
from sellapp_sdk.wallet_bonus_tiers.models import (
    SdkListWalletBonusTiersResponseValue200ApplicationJson,
    SdkReplaceWalletBonusTierResponseValue200ApplicationJson,
    SdkRestoreWalletBonusTierResponseValue200ApplicationJson,
    SdkUpdateWalletBonusTierResponseValue200ApplicationJson,
)
from tests.generated_helpers import load_fixture


class TestWalletBonusTiers:
    def test_list(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_list_wallet_bonus_tiers_response_value_200_application_json.json"
            ),
        )
        result = sellapp_sdk.wallet_bonus_tiers.list()
        assert isinstance(
            result, SdkListWalletBonusTiersResponseValue200ApplicationJson
        )
        request = httpx_mock.get_request()
        assert request.method == "GET"
        assert request.url.path.endswith("/v2/wallet-bonus-tiers")

    def test_create(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_create_wallet_bonus_tier_response_value_201_application_json.json"
            ),
        )
        result = sellapp_sdk.wallet_bonus_tiers.create(
            minimum_top_up_cents=1,
            bonus_kind=SdkCreateWalletBonusTierRequestApplicationJsonBonusKind("fixed"),
            fixed_bonus_cents=1,
            percent_basis=1,
            maximum_bonus_cents=1,
            priority=1,
            is_active=True,
        )
        assert isinstance(
            result, SdkCreateWalletBonusTierResponseValue201ApplicationJson
        )
        request = httpx_mock.get_request()
        assert request.method == "POST"
        assert request.url.path.endswith("/v2/wallet-bonus-tiers")
        body = json.loads(request.content)
        assert body["minimum_top_up_cents"] == 1
        assert body[
            "bonus_kind"
        ] == SdkCreateWalletBonusTierRequestApplicationJsonBonusKind("fixed")
        assert "fixed_bonus_cents" in body
        assert "percent_basis" in body
        assert "maximum_bonus_cents" in body
        assert body["priority"] == 1
        assert body["is_active"] == True

    def test_replace(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_replace_wallet_bonus_tier_response_value_200_application_json.json"
            ),
        )
        result = sellapp_sdk.wallet_bonus_tiers.replace(
            "test_bonusTier",
            minimum_top_up_cents=1,
            bonus_kind=SdkReplaceWalletBonusTierRequestApplicationJsonBonusKind(
                "fixed"
            ),
            fixed_bonus_cents=1,
            percent_basis=1,
            maximum_bonus_cents=1,
            priority=1,
            is_active=True,
        )
        assert isinstance(
            result, SdkReplaceWalletBonusTierResponseValue200ApplicationJson
        )
        request = httpx_mock.get_request()
        assert request.method == "PUT"
        assert request.url.path.endswith("/v2/wallet-bonus-tiers/test_bonusTier")
        body = json.loads(request.content)
        assert body["minimum_top_up_cents"] == 1
        assert body[
            "bonus_kind"
        ] == SdkReplaceWalletBonusTierRequestApplicationJsonBonusKind("fixed")
        assert "fixed_bonus_cents" in body
        assert "percent_basis" in body
        assert "maximum_bonus_cents" in body
        assert body["priority"] == 1
        assert body["is_active"] == True

    def test_update(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_update_wallet_bonus_tier_response_value_200_application_json.json"
            ),
        )
        result = sellapp_sdk.wallet_bonus_tiers.update("test_bonusTier")
        assert isinstance(
            result, SdkUpdateWalletBonusTierResponseValue200ApplicationJson
        )
        request = httpx_mock.get_request()
        assert request.method == "PATCH"
        assert request.url.path.endswith("/v2/wallet-bonus-tiers/test_bonusTier")

    def test_archive(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(status_code=200, content=b"")
        result = sellapp_sdk.wallet_bonus_tiers.archive("test_bonusTier")
        assert result is None
        request = httpx_mock.get_request()
        assert request.method == "DELETE"
        assert request.url.path.endswith("/v2/wallet-bonus-tiers/test_bonusTier")

    def test_restore(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_restore_wallet_bonus_tier_response_value_200_application_json.json"
            ),
        )
        result = sellapp_sdk.wallet_bonus_tiers.restore("test_bonusTier")
        assert isinstance(
            result, SdkRestoreWalletBonusTierResponseValue200ApplicationJson
        )
        request = httpx_mock.get_request()
        assert request.method == "POST"
        assert request.url.path.endswith(
            "/v2/wallet-bonus-tiers/test_bonusTier/restore"
        )

    def test_list_with_request_options(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_list_wallet_bonus_tiers_response_value_200_application_json.json"
            )
        )
        sellapp_sdk.wallet_bonus_tiers.list(
            request_options={"headers": {"X-Custom": "value"}}
        )
        request = httpx_mock.get_request()
        assert request.headers["X-Custom"] == "value"

    def test_list_unauthorized(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            status_code=401,
            json={"message": "Unauthorized"},
        )
        with pytest.raises(AuthenticationError):
            sellapp_sdk.wallet_bonus_tiers.list()

    def test_list_not_found(self, httpx_mock):
        sellapp_sdk = SellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=404, json={"message": "Not found"})
            with pytest.raises(NotFoundError):
                sellapp_sdk.wallet_bonus_tiers.list()
        finally:
            sellapp_sdk.close()

    def test_list_rate_limited(self, httpx_mock):
        sellapp_sdk = SellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(
                status_code=429,
                headers={"Retry-After": "0"},
                json={"message": "Slow down"},
            )
            with pytest.raises(RateLimitExceededError):
                sellapp_sdk.wallet_bonus_tiers.list()
        finally:
            sellapp_sdk.close()

    def test_list_server_error(self, httpx_mock):
        sellapp_sdk = SellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=500, json={"message": "Server error"})
            with pytest.raises(ServerError):
                sellapp_sdk.wallet_bonus_tiers.list()
        finally:
            sellapp_sdk.close()

    def test_list_bad_request(self, httpx_mock):
        sellapp_sdk = SellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=400, json={"message": "Bad request"})
            with pytest.raises(BadRequestError):
                sellapp_sdk.wallet_bonus_tiers.list()
        finally:
            sellapp_sdk.close()

    def test_list_unprocessable(self, httpx_mock):
        sellapp_sdk = SellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=422, json={"message": "Unprocessable"})
            with pytest.raises(UnprocessableEntityError):
                sellapp_sdk.wallet_bonus_tiers.list()
        finally:
            sellapp_sdk.close()


class TestAsyncWalletBonusTiers:
    @pytest.mark.asyncio
    async def test_list(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_list_wallet_bonus_tiers_response_value_200_application_json.json"
            )
        )
        result = await async_sellapp_sdk.wallet_bonus_tiers.list()
        assert isinstance(
            result, SdkListWalletBonusTiersResponseValue200ApplicationJson
        )
        request = httpx_mock.get_request()
        assert request.method == "GET"
        assert request.url.path.endswith("/v2/wallet-bonus-tiers")

    @pytest.mark.asyncio
    async def test_create(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_create_wallet_bonus_tier_response_value_201_application_json.json"
            )
        )
        result = await async_sellapp_sdk.wallet_bonus_tiers.create(
            minimum_top_up_cents=1,
            bonus_kind=SdkCreateWalletBonusTierRequestApplicationJsonBonusKind("fixed"),
            fixed_bonus_cents=1,
            percent_basis=1,
            maximum_bonus_cents=1,
            priority=1,
            is_active=True,
        )
        assert isinstance(
            result, SdkCreateWalletBonusTierResponseValue201ApplicationJson
        )
        request = httpx_mock.get_request()
        assert request.method == "POST"
        assert request.url.path.endswith("/v2/wallet-bonus-tiers")

    @pytest.mark.asyncio
    async def test_replace(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_replace_wallet_bonus_tier_response_value_200_application_json.json"
            )
        )
        result = await async_sellapp_sdk.wallet_bonus_tiers.replace(
            "test_bonusTier",
            minimum_top_up_cents=1,
            bonus_kind=SdkReplaceWalletBonusTierRequestApplicationJsonBonusKind(
                "fixed"
            ),
            fixed_bonus_cents=1,
            percent_basis=1,
            maximum_bonus_cents=1,
            priority=1,
            is_active=True,
        )
        assert isinstance(
            result, SdkReplaceWalletBonusTierResponseValue200ApplicationJson
        )
        request = httpx_mock.get_request()
        assert request.method == "PUT"
        assert request.url.path.endswith("/v2/wallet-bonus-tiers/test_bonusTier")

    @pytest.mark.asyncio
    async def test_update(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_update_wallet_bonus_tier_response_value_200_application_json.json"
            )
        )
        result = await async_sellapp_sdk.wallet_bonus_tiers.update("test_bonusTier")
        assert isinstance(
            result, SdkUpdateWalletBonusTierResponseValue200ApplicationJson
        )
        request = httpx_mock.get_request()
        assert request.method == "PATCH"
        assert request.url.path.endswith("/v2/wallet-bonus-tiers/test_bonusTier")

    @pytest.mark.asyncio
    async def test_archive(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(status_code=200, content=b"")
        result = await async_sellapp_sdk.wallet_bonus_tiers.archive("test_bonusTier")
        assert result is None
        request = httpx_mock.get_request()
        assert request.method == "DELETE"
        assert request.url.path.endswith("/v2/wallet-bonus-tiers/test_bonusTier")

    @pytest.mark.asyncio
    async def test_restore(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_restore_wallet_bonus_tier_response_value_200_application_json.json"
            )
        )
        result = await async_sellapp_sdk.wallet_bonus_tiers.restore("test_bonusTier")
        assert isinstance(
            result, SdkRestoreWalletBonusTierResponseValue200ApplicationJson
        )
        request = httpx_mock.get_request()
        assert request.method == "POST"
        assert request.url.path.endswith(
            "/v2/wallet-bonus-tiers/test_bonusTier/restore"
        )

    @pytest.mark.asyncio
    async def test_list_with_request_options(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_list_wallet_bonus_tiers_response_value_200_application_json.json"
            )
        )
        await async_sellapp_sdk.wallet_bonus_tiers.list(
            request_options={"headers": {"X-Custom": "value"}}
        )
        request = httpx_mock.get_request()
        assert request.headers["X-Custom"] == "value"

    @pytest.mark.asyncio
    async def test_list_unauthorized(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(status_code=401, json={"message": "Unauthorized"})
        with pytest.raises(AuthenticationError):
            await async_sellapp_sdk.wallet_bonus_tiers.list()

    @pytest.mark.asyncio
    async def test_list_not_found(self, httpx_mock):
        sellapp_sdk = AsyncSellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=404, json={"message": "Not found"})
            with pytest.raises(NotFoundError):
                await sellapp_sdk.wallet_bonus_tiers.list()
        finally:
            await sellapp_sdk.close()

    @pytest.mark.asyncio
    async def test_list_rate_limited(self, httpx_mock):
        sellapp_sdk = AsyncSellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(
                status_code=429,
                headers={"Retry-After": "0"},
                json={"message": "Slow down"},
            )
            with pytest.raises(RateLimitExceededError):
                await sellapp_sdk.wallet_bonus_tiers.list()
        finally:
            await sellapp_sdk.close()

    @pytest.mark.asyncio
    async def test_list_server_error(self, httpx_mock):
        sellapp_sdk = AsyncSellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=500, json={"message": "Server error"})
            with pytest.raises(ServerError):
                await sellapp_sdk.wallet_bonus_tiers.list()
        finally:
            await sellapp_sdk.close()

    @pytest.mark.asyncio
    async def test_list_bad_request(self, httpx_mock):
        sellapp_sdk = AsyncSellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=400, json={"message": "Bad request"})
            with pytest.raises(BadRequestError):
                await sellapp_sdk.wallet_bonus_tiers.list()
        finally:
            await sellapp_sdk.close()

    @pytest.mark.asyncio
    async def test_list_unprocessable(self, httpx_mock):
        sellapp_sdk = AsyncSellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=422, json={"message": "Unprocessable"})
            with pytest.raises(UnprocessableEntityError):
                await sellapp_sdk.wallet_bonus_tiers.list()
        finally:
            await sellapp_sdk.close()
