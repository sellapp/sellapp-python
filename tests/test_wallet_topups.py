# This file is auto-generated by oagen. Do not edit.

import json

import pytest

from sellapp_sdk import AsyncSellAppClient, SellAppClient
from sellapp_sdk._errors import (
    AuthenticationError,
    BadRequestError,
    NotFoundError,
    RateLimitExceededError,
    ServerError,
    UnprocessableEntityError,
)
from sellapp_sdk.common.models import (
    SdkCreateWalletTopUpRequestApplicationJsonPaymentMethod,
)
from sellapp_sdk.wallet_settings.models import (
    SdkCreateWalletTopUpResponseValue201ApplicationJson,
)
from tests.generated_helpers import load_fixture


class TestWalletTopups:
    def test_create(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_create_wallet_top_up_response_value_201_application_json.json"
            ),
        )
        result = sellapp_sdk.wallet_topups.create(
            "test_customer",
            amount_cents=1,
            payment_method=SdkCreateWalletTopUpRequestApplicationJsonPaymentMethod(
                "AUTHNET"
            ),
        )
        assert isinstance(result, SdkCreateWalletTopUpResponseValue201ApplicationJson)
        request = httpx_mock.get_request()
        assert request.method == "POST"
        assert request.url.path.endswith("/v2/wallets/test_customer/top-ups")
        body = json.loads(request.content)
        assert body["amount_cents"] == 1
        assert body[
            "payment_method"
        ] == SdkCreateWalletTopUpRequestApplicationJsonPaymentMethod("AUTHNET")

    def test_create_with_request_options(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_create_wallet_top_up_response_value_201_application_json.json"
            )
        )
        sellapp_sdk.wallet_topups.create(
            "test_customer",
            amount_cents=1,
            payment_method=SdkCreateWalletTopUpRequestApplicationJsonPaymentMethod(
                "AUTHNET"
            ),
            request_options={"headers": {"X-Custom": "value"}},
        )
        request = httpx_mock.get_request()
        assert request.headers["X-Custom"] == "value"

    def test_create_unauthorized(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            status_code=401,
            json={"message": "Unauthorized"},
        )
        with pytest.raises(AuthenticationError):
            sellapp_sdk.wallet_topups.create(
                "test_customer",
                amount_cents=1,
                payment_method=SdkCreateWalletTopUpRequestApplicationJsonPaymentMethod(
                    "AUTHNET"
                ),
            )

    def test_create_not_found(self, httpx_mock):
        sellapp_sdk = SellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=404, json={"message": "Not found"})
            with pytest.raises(NotFoundError):
                sellapp_sdk.wallet_topups.create(
                    "test_customer",
                    amount_cents=1,
                    payment_method=SdkCreateWalletTopUpRequestApplicationJsonPaymentMethod(
                        "AUTHNET"
                    ),
                )
        finally:
            sellapp_sdk.close()

    def test_create_rate_limited(self, httpx_mock):
        sellapp_sdk = SellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(
                status_code=429,
                headers={"Retry-After": "0"},
                json={"message": "Slow down"},
            )
            with pytest.raises(RateLimitExceededError):
                sellapp_sdk.wallet_topups.create(
                    "test_customer",
                    amount_cents=1,
                    payment_method=SdkCreateWalletTopUpRequestApplicationJsonPaymentMethod(
                        "AUTHNET"
                    ),
                )
        finally:
            sellapp_sdk.close()

    def test_create_server_error(self, httpx_mock):
        sellapp_sdk = SellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=500, json={"message": "Server error"})
            with pytest.raises(ServerError):
                sellapp_sdk.wallet_topups.create(
                    "test_customer",
                    amount_cents=1,
                    payment_method=SdkCreateWalletTopUpRequestApplicationJsonPaymentMethod(
                        "AUTHNET"
                    ),
                )
        finally:
            sellapp_sdk.close()

    def test_create_bad_request(self, httpx_mock):
        sellapp_sdk = SellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=400, json={"message": "Bad request"})
            with pytest.raises(BadRequestError):
                sellapp_sdk.wallet_topups.create(
                    "test_customer",
                    amount_cents=1,
                    payment_method=SdkCreateWalletTopUpRequestApplicationJsonPaymentMethod(
                        "AUTHNET"
                    ),
                )
        finally:
            sellapp_sdk.close()

    def test_create_unprocessable(self, httpx_mock):
        sellapp_sdk = SellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=422, json={"message": "Unprocessable"})
            with pytest.raises(UnprocessableEntityError):
                sellapp_sdk.wallet_topups.create(
                    "test_customer",
                    amount_cents=1,
                    payment_method=SdkCreateWalletTopUpRequestApplicationJsonPaymentMethod(
                        "AUTHNET"
                    ),
                )
        finally:
            sellapp_sdk.close()


class TestAsyncWalletTopups:
    @pytest.mark.asyncio
    async def test_create(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_create_wallet_top_up_response_value_201_application_json.json"
            )
        )
        result = await async_sellapp_sdk.wallet_topups.create(
            "test_customer",
            amount_cents=1,
            payment_method=SdkCreateWalletTopUpRequestApplicationJsonPaymentMethod(
                "AUTHNET"
            ),
        )
        assert isinstance(result, SdkCreateWalletTopUpResponseValue201ApplicationJson)
        request = httpx_mock.get_request()
        assert request.method == "POST"
        assert request.url.path.endswith("/v2/wallets/test_customer/top-ups")

    @pytest.mark.asyncio
    async def test_create_with_request_options(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_create_wallet_top_up_response_value_201_application_json.json"
            )
        )
        await async_sellapp_sdk.wallet_topups.create(
            "test_customer",
            amount_cents=1,
            payment_method=SdkCreateWalletTopUpRequestApplicationJsonPaymentMethod(
                "AUTHNET"
            ),
            request_options={"headers": {"X-Custom": "value"}},
        )
        request = httpx_mock.get_request()
        assert request.headers["X-Custom"] == "value"

    @pytest.mark.asyncio
    async def test_create_unauthorized(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(status_code=401, json={"message": "Unauthorized"})
        with pytest.raises(AuthenticationError):
            await async_sellapp_sdk.wallet_topups.create(
                "test_customer",
                amount_cents=1,
                payment_method=SdkCreateWalletTopUpRequestApplicationJsonPaymentMethod(
                    "AUTHNET"
                ),
            )

    @pytest.mark.asyncio
    async def test_create_not_found(self, httpx_mock):
        sellapp_sdk = AsyncSellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=404, json={"message": "Not found"})
            with pytest.raises(NotFoundError):
                await sellapp_sdk.wallet_topups.create(
                    "test_customer",
                    amount_cents=1,
                    payment_method=SdkCreateWalletTopUpRequestApplicationJsonPaymentMethod(
                        "AUTHNET"
                    ),
                )
        finally:
            await sellapp_sdk.close()

    @pytest.mark.asyncio
    async def test_create_rate_limited(self, httpx_mock):
        sellapp_sdk = AsyncSellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(
                status_code=429,
                headers={"Retry-After": "0"},
                json={"message": "Slow down"},
            )
            with pytest.raises(RateLimitExceededError):
                await sellapp_sdk.wallet_topups.create(
                    "test_customer",
                    amount_cents=1,
                    payment_method=SdkCreateWalletTopUpRequestApplicationJsonPaymentMethod(
                        "AUTHNET"
                    ),
                )
        finally:
            await sellapp_sdk.close()

    @pytest.mark.asyncio
    async def test_create_server_error(self, httpx_mock):
        sellapp_sdk = AsyncSellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=500, json={"message": "Server error"})
            with pytest.raises(ServerError):
                await sellapp_sdk.wallet_topups.create(
                    "test_customer",
                    amount_cents=1,
                    payment_method=SdkCreateWalletTopUpRequestApplicationJsonPaymentMethod(
                        "AUTHNET"
                    ),
                )
        finally:
            await sellapp_sdk.close()

    @pytest.mark.asyncio
    async def test_create_bad_request(self, httpx_mock):
        sellapp_sdk = AsyncSellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=400, json={"message": "Bad request"})
            with pytest.raises(BadRequestError):
                await sellapp_sdk.wallet_topups.create(
                    "test_customer",
                    amount_cents=1,
                    payment_method=SdkCreateWalletTopUpRequestApplicationJsonPaymentMethod(
                        "AUTHNET"
                    ),
                )
        finally:
            await sellapp_sdk.close()

    @pytest.mark.asyncio
    async def test_create_unprocessable(self, httpx_mock):
        sellapp_sdk = AsyncSellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=422, json={"message": "Unprocessable"})
            with pytest.raises(UnprocessableEntityError):
                await sellapp_sdk.wallet_topups.create(
                    "test_customer",
                    amount_cents=1,
                    payment_method=SdkCreateWalletTopUpRequestApplicationJsonPaymentMethod(
                        "AUTHNET"
                    ),
                )
        finally:
            await sellapp_sdk.close()
