# This file is auto-generated by oagen. Do not edit.

import json

import pytest

from sellapp_sdk import AsyncSellAppClient, SellAppClient
from sellapp_sdk._errors import (
    AuthenticationError,
    BadRequestError,
    NotFoundError,
    RateLimitExceededError,
    ServerError,
    UnprocessableEntityError,
)
from sellapp_sdk._pagination import AsyncPage, SyncPage
from sellapp_sdk.common.models import (
    SdkCreateWebhookChannelRequestApplicationJsonAllowedNotifications,
    SdkReplaceWebhookChannelRequestApplicationJsonAllowedNotifications,
    SdkSendTestWebhookRequestApplicationJsonEvent,
)
from sellapp_sdk.webhook_channels.models import (
    ListWebhookChannelsResponseValue200ApplicationJsonPropertyDataItem,
    SdkCreateWebhookChannelResponseValue201ApplicationJson,
    SdkGetWebhookChannelResponseValue200ApplicationJson,
    SdkReplaceWebhookChannelResponseValue200ApplicationJson,
    SdkRotateWebhookSigningSecretResponseValue200ApplicationJson,
    SdkSendTestWebhookResponseValue200ApplicationJson,
    SdkUpdateWebhookChannelResponseValue200ApplicationJson,
    SearchWebhookChannelsResponseValue200ApplicationJsonPropertyDataItem,
)
from tests.generated_helpers import load_fixture


class TestWebhookChannels:
    def test_list(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "list_list_webhook_channels_response_value_200_application_json_property_data_item.json"
            ),
        )
        page = sellapp_sdk.webhook_channels.list()
        assert isinstance(page, SyncPage)
        assert len(page.data) == 1
        assert isinstance(
            page.data[0],
            ListWebhookChannelsResponseValue200ApplicationJsonPropertyDataItem,
        )

    def test_list_empty_page(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(json={"data": [], "list_metadata": {}})
        page = sellapp_sdk.webhook_channels.list()
        assert isinstance(page, SyncPage)
        assert page.data == []

    def test_list_encodes_query_params(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(json={"data": [], "list_metadata": {}})
        sellapp_sdk.webhook_channels.list(
            limit=10, before="cursor before", after="cursor/after", page=7
        )
        request = httpx_mock.get_request()
        assert request.url.params["limit"] == "10"
        assert request.url.params["before"] == "cursor before"
        assert request.url.params["after"] == "cursor/after"
        assert request.url.params["page"] == "7"

    def test_create(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_create_webhook_channel_response_value_201_application_json.json"
            ),
        )
        result = sellapp_sdk.webhook_channels.create(
            url="test_url",
            allowed_notifications=[
                SdkCreateWebhookChannelRequestApplicationJsonAllowedNotifications(
                    "booking.cancelled"
                )
            ],
        )
        assert isinstance(
            result, SdkCreateWebhookChannelResponseValue201ApplicationJson
        )
        request = httpx_mock.get_request()
        assert request.method == "POST"
        assert request.url.path.endswith("/v2/webhook-channels")
        body = json.loads(request.content)
        assert body["url"] == "test_url"
        assert "allowed_notifications" in body

    def test_search(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "list_search_webhook_channels_response_value_200_application_json_property_data_item.json"
            ),
        )
        page = sellapp_sdk.webhook_channels.search()
        assert isinstance(page, SyncPage)
        assert len(page.data) == 1
        assert isinstance(
            page.data[0],
            SearchWebhookChannelsResponseValue200ApplicationJsonPropertyDataItem,
        )

    def test_search_empty_page(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(json={"data": [], "list_metadata": {}})
        page = sellapp_sdk.webhook_channels.search()
        assert isinstance(page, SyncPage)
        assert page.data == []

    def test_rotate(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_rotate_webhook_signing_secret_response_value_200_application_json.json"
            ),
        )
        result = sellapp_sdk.webhook_channels.rotate(
            signing_secret="test_signing_secretxxxxxxxxxxxxx"
        )
        assert isinstance(
            result, SdkRotateWebhookSigningSecretResponseValue200ApplicationJson
        )
        request = httpx_mock.get_request()
        assert request.method == "PUT"
        assert request.url.path.endswith("/v2/webhook-channels/signing-secret")
        body = json.loads(request.content)
        assert body["signing_secret"] == "test_signing_secretxxxxxxxxxxxxx"

    def test_get(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_get_webhook_channel_response_value_200_application_json.json"
            ),
        )
        result = sellapp_sdk.webhook_channels.get("test_webhookChannel")
        assert isinstance(result, SdkGetWebhookChannelResponseValue200ApplicationJson)
        request = httpx_mock.get_request()
        assert request.method == "GET"
        assert request.url.path.endswith("/v2/webhook-channels/test_webhookChannel")

    def test_replace(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_replace_webhook_channel_response_value_200_application_json.json"
            ),
        )
        result = sellapp_sdk.webhook_channels.replace(
            "test_webhookChannel",
            name="test_name",
            url="test_url",
            allowed_notifications=[
                SdkReplaceWebhookChannelRequestApplicationJsonAllowedNotifications(
                    "booking.cancelled"
                )
            ],
        )
        assert isinstance(
            result, SdkReplaceWebhookChannelResponseValue200ApplicationJson
        )
        request = httpx_mock.get_request()
        assert request.method == "PUT"
        assert request.url.path.endswith("/v2/webhook-channels/test_webhookChannel")
        body = json.loads(request.content)
        assert "name" in body
        assert body["url"] == "test_url"
        assert "allowed_notifications" in body

    def test_update(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_update_webhook_channel_response_value_200_application_json.json"
            ),
        )
        result = sellapp_sdk.webhook_channels.update("test_webhookChannel")
        assert isinstance(
            result, SdkUpdateWebhookChannelResponseValue200ApplicationJson
        )
        request = httpx_mock.get_request()
        assert request.method == "PATCH"
        assert request.url.path.endswith("/v2/webhook-channels/test_webhookChannel")

    def test_delete(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(status_code=200, content=b"")
        result = sellapp_sdk.webhook_channels.delete("test_webhookChannel")
        assert result is None
        request = httpx_mock.get_request()
        assert request.method == "DELETE"
        assert request.url.path.endswith("/v2/webhook-channels/test_webhookChannel")

    def test_send(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_send_test_webhook_response_value_200_application_json.json"
            ),
        )
        result = sellapp_sdk.webhook_channels.send(
            "test_webhookChannel",
            event=SdkSendTestWebhookRequestApplicationJsonEvent("booking.cancelled"),
        )
        assert isinstance(result, SdkSendTestWebhookResponseValue200ApplicationJson)
        request = httpx_mock.get_request()
        assert request.method == "POST"
        assert request.url.path.endswith(
            "/v2/webhook-channels/test_webhookChannel/test"
        )
        body = json.loads(request.content)
        assert body["event"] == SdkSendTestWebhookRequestApplicationJsonEvent(
            "booking.cancelled"
        )

    def test_list_with_request_options(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(json={"data": [], "list_metadata": {}})
        sellapp_sdk.webhook_channels.list(
            request_options={"headers": {"X-Custom": "value"}}
        )
        request = httpx_mock.get_request()
        assert request.headers["X-Custom"] == "value"

    def test_list_unauthorized(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            status_code=401,
            json={"message": "Unauthorized"},
        )
        with pytest.raises(AuthenticationError):
            sellapp_sdk.webhook_channels.list()

    def test_list_not_found(self, httpx_mock):
        sellapp_sdk = SellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=404, json={"message": "Not found"})
            with pytest.raises(NotFoundError):
                sellapp_sdk.webhook_channels.list()
        finally:
            sellapp_sdk.close()

    def test_list_rate_limited(self, httpx_mock):
        sellapp_sdk = SellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(
                status_code=429,
                headers={"Retry-After": "0"},
                json={"message": "Slow down"},
            )
            with pytest.raises(RateLimitExceededError):
                sellapp_sdk.webhook_channels.list()
        finally:
            sellapp_sdk.close()

    def test_list_server_error(self, httpx_mock):
        sellapp_sdk = SellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=500, json={"message": "Server error"})
            with pytest.raises(ServerError):
                sellapp_sdk.webhook_channels.list()
        finally:
            sellapp_sdk.close()

    def test_list_bad_request(self, httpx_mock):
        sellapp_sdk = SellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=400, json={"message": "Bad request"})
            with pytest.raises(BadRequestError):
                sellapp_sdk.webhook_channels.list()
        finally:
            sellapp_sdk.close()

    def test_list_unprocessable(self, httpx_mock):
        sellapp_sdk = SellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=422, json={"message": "Unprocessable"})
            with pytest.raises(UnprocessableEntityError):
                sellapp_sdk.webhook_channels.list()
        finally:
            sellapp_sdk.close()


class TestAsyncWebhookChannels:
    @pytest.mark.asyncio
    async def test_list(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "list_list_webhook_channels_response_value_200_application_json_property_data_item.json"
            )
        )
        page = await async_sellapp_sdk.webhook_channels.list()
        assert isinstance(page, AsyncPage)
        assert len(page.data) == 1
        assert isinstance(
            page.data[0],
            ListWebhookChannelsResponseValue200ApplicationJsonPropertyDataItem,
        )

    @pytest.mark.asyncio
    async def test_list_empty_page(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(json={"data": [], "list_metadata": {}})
        page = await async_sellapp_sdk.webhook_channels.list()
        assert isinstance(page, AsyncPage)
        assert page.data == []

    @pytest.mark.asyncio
    async def test_list_encodes_query_params(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(json={"data": [], "list_metadata": {}})
        await async_sellapp_sdk.webhook_channels.list(
            limit=10, before="cursor before", after="cursor/after", page=7
        )
        request = httpx_mock.get_request()
        assert request.url.params["limit"] == "10"
        assert request.url.params["before"] == "cursor before"
        assert request.url.params["after"] == "cursor/after"
        assert request.url.params["page"] == "7"

    @pytest.mark.asyncio
    async def test_create(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_create_webhook_channel_response_value_201_application_json.json"
            )
        )
        result = await async_sellapp_sdk.webhook_channels.create(
            url="test_url",
            allowed_notifications=[
                SdkCreateWebhookChannelRequestApplicationJsonAllowedNotifications(
                    "booking.cancelled"
                )
            ],
        )
        assert isinstance(
            result, SdkCreateWebhookChannelResponseValue201ApplicationJson
        )
        request = httpx_mock.get_request()
        assert request.method == "POST"
        assert request.url.path.endswith("/v2/webhook-channels")

    @pytest.mark.asyncio
    async def test_search(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "list_search_webhook_channels_response_value_200_application_json_property_data_item.json"
            )
        )
        page = await async_sellapp_sdk.webhook_channels.search()
        assert isinstance(page, AsyncPage)
        assert len(page.data) == 1
        assert isinstance(
            page.data[0],
            SearchWebhookChannelsResponseValue200ApplicationJsonPropertyDataItem,
        )

    @pytest.mark.asyncio
    async def test_search_empty_page(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(json={"data": [], "list_metadata": {}})
        page = await async_sellapp_sdk.webhook_channels.search()
        assert isinstance(page, AsyncPage)
        assert page.data == []

    @pytest.mark.asyncio
    async def test_rotate(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_rotate_webhook_signing_secret_response_value_200_application_json.json"
            )
        )
        result = await async_sellapp_sdk.webhook_channels.rotate(
            signing_secret="test_signing_secretxxxxxxxxxxxxx"
        )
        assert isinstance(
            result, SdkRotateWebhookSigningSecretResponseValue200ApplicationJson
        )
        request = httpx_mock.get_request()
        assert request.method == "PUT"
        assert request.url.path.endswith("/v2/webhook-channels/signing-secret")

    @pytest.mark.asyncio
    async def test_get(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_get_webhook_channel_response_value_200_application_json.json"
            )
        )
        result = await async_sellapp_sdk.webhook_channels.get("test_webhookChannel")
        assert isinstance(result, SdkGetWebhookChannelResponseValue200ApplicationJson)
        request = httpx_mock.get_request()
        assert request.method == "GET"
        assert request.url.path.endswith("/v2/webhook-channels/test_webhookChannel")

    @pytest.mark.asyncio
    async def test_replace(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_replace_webhook_channel_response_value_200_application_json.json"
            )
        )
        result = await async_sellapp_sdk.webhook_channels.replace(
            "test_webhookChannel",
            name="test_name",
            url="test_url",
            allowed_notifications=[
                SdkReplaceWebhookChannelRequestApplicationJsonAllowedNotifications(
                    "booking.cancelled"
                )
            ],
        )
        assert isinstance(
            result, SdkReplaceWebhookChannelResponseValue200ApplicationJson
        )
        request = httpx_mock.get_request()
        assert request.method == "PUT"
        assert request.url.path.endswith("/v2/webhook-channels/test_webhookChannel")

    @pytest.mark.asyncio
    async def test_update(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_update_webhook_channel_response_value_200_application_json.json"
            )
        )
        result = await async_sellapp_sdk.webhook_channels.update("test_webhookChannel")
        assert isinstance(
            result, SdkUpdateWebhookChannelResponseValue200ApplicationJson
        )
        request = httpx_mock.get_request()
        assert request.method == "PATCH"
        assert request.url.path.endswith("/v2/webhook-channels/test_webhookChannel")

    @pytest.mark.asyncio
    async def test_delete(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(status_code=200, content=b"")
        result = await async_sellapp_sdk.webhook_channels.delete("test_webhookChannel")
        assert result is None
        request = httpx_mock.get_request()
        assert request.method == "DELETE"
        assert request.url.path.endswith("/v2/webhook-channels/test_webhookChannel")

    @pytest.mark.asyncio
    async def test_send(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_send_test_webhook_response_value_200_application_json.json"
            )
        )
        result = await async_sellapp_sdk.webhook_channels.send(
            "test_webhookChannel",
            event=SdkSendTestWebhookRequestApplicationJsonEvent("booking.cancelled"),
        )
        assert isinstance(result, SdkSendTestWebhookResponseValue200ApplicationJson)
        request = httpx_mock.get_request()
        assert request.method == "POST"
        assert request.url.path.endswith(
            "/v2/webhook-channels/test_webhookChannel/test"
        )

    @pytest.mark.asyncio
    async def test_list_with_request_options(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(json={"data": [], "list_metadata": {}})
        await async_sellapp_sdk.webhook_channels.list(
            request_options={"headers": {"X-Custom": "value"}}
        )
        request = httpx_mock.get_request()
        assert request.headers["X-Custom"] == "value"

    @pytest.mark.asyncio
    async def test_list_unauthorized(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(status_code=401, json={"message": "Unauthorized"})
        with pytest.raises(AuthenticationError):
            await async_sellapp_sdk.webhook_channels.list()

    @pytest.mark.asyncio
    async def test_list_not_found(self, httpx_mock):
        sellapp_sdk = AsyncSellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=404, json={"message": "Not found"})
            with pytest.raises(NotFoundError):
                await sellapp_sdk.webhook_channels.list()
        finally:
            await sellapp_sdk.close()

    @pytest.mark.asyncio
    async def test_list_rate_limited(self, httpx_mock):
        sellapp_sdk = AsyncSellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(
                status_code=429,
                headers={"Retry-After": "0"},
                json={"message": "Slow down"},
            )
            with pytest.raises(RateLimitExceededError):
                await sellapp_sdk.webhook_channels.list()
        finally:
            await sellapp_sdk.close()

    @pytest.mark.asyncio
    async def test_list_server_error(self, httpx_mock):
        sellapp_sdk = AsyncSellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=500, json={"message": "Server error"})
            with pytest.raises(ServerError):
                await sellapp_sdk.webhook_channels.list()
        finally:
            await sellapp_sdk.close()

    @pytest.mark.asyncio
    async def test_list_bad_request(self, httpx_mock):
        sellapp_sdk = AsyncSellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=400, json={"message": "Bad request"})
            with pytest.raises(BadRequestError):
                await sellapp_sdk.webhook_channels.list()
        finally:
            await sellapp_sdk.close()

    @pytest.mark.asyncio
    async def test_list_unprocessable(self, httpx_mock):
        sellapp_sdk = AsyncSellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=422, json={"message": "Unprocessable"})
            with pytest.raises(UnprocessableEntityError):
                await sellapp_sdk.webhook_channels.list()
        finally:
            await sellapp_sdk.close()
