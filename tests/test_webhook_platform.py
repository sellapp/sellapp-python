# This file is auto-generated by oagen. Do not edit.


import pytest

from sellapp_sdk import AsyncSellAppClient, SellAppClient
from sellapp_sdk._errors import (
    AuthenticationError,
    BadRequestError,
    NotFoundError,
    RateLimitExceededError,
    ServerError,
    UnprocessableEntityError,
)
from sellapp_sdk._pagination import AsyncPage, SyncPage
from sellapp_sdk.webhook_platform.models import (
    ListWebhookDeliveriesResponseValue200ApplicationJsonPropertyDataItem,
    SdkGetWebhookDeliveryResponseValue200ApplicationJson,
    SdkListWebhookEventTypesResponseValue200ApplicationJson,
    SdkReplayWebhookDeliveryResponseValue201ApplicationJson,
)
from tests.generated_helpers import load_fixture


class TestWebhookPlatform:
    def test_list_webhook_event_types(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_list_webhook_event_types_response_value_200_application_json.json"
            ),
        )
        result = sellapp_sdk.webhook_platform.list_webhook_event_types()
        assert isinstance(
            result, SdkListWebhookEventTypesResponseValue200ApplicationJson
        )
        request = httpx_mock.get_request()
        assert request.method == "GET"
        assert request.url.path.endswith("/v2/webhook-event-types")

    def test_list_webhook_deliveries(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "list_list_webhook_deliveries_response_value_200_application_json_property_data_item.json"
            ),
        )
        page = sellapp_sdk.webhook_platform.list_webhook_deliveries()
        assert isinstance(page, SyncPage)
        assert len(page.data) == 1
        assert isinstance(
            page.data[0],
            ListWebhookDeliveriesResponseValue200ApplicationJsonPropertyDataItem,
        )

    def test_list_webhook_deliveries_empty_page(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(json={"data": [], "list_metadata": {}})
        page = sellapp_sdk.webhook_platform.list_webhook_deliveries()
        assert isinstance(page, SyncPage)
        assert page.data == []

    def test_get_webhook_delivery(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_get_webhook_delivery_response_value_200_application_json.json"
            ),
        )
        result = sellapp_sdk.webhook_platform.get_webhook_delivery("test_delivery")
        assert isinstance(result, SdkGetWebhookDeliveryResponseValue200ApplicationJson)
        request = httpx_mock.get_request()
        assert request.method == "GET"
        assert request.url.path.endswith("/v2/webhook-deliveries/test_delivery")

    def test_replay_webhook_delivery(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_replay_webhook_delivery_response_value_201_application_json.json"
            ),
        )
        result = sellapp_sdk.webhook_platform.replay_webhook_delivery("test_delivery")
        assert isinstance(
            result, SdkReplayWebhookDeliveryResponseValue201ApplicationJson
        )
        request = httpx_mock.get_request()
        assert request.method == "POST"
        assert request.url.path.endswith("/v2/webhook-deliveries/test_delivery/replay")

    def test_list_webhook_event_types_with_request_options(
        self, sellapp_sdk, httpx_mock
    ):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_list_webhook_event_types_response_value_200_application_json.json"
            )
        )
        sellapp_sdk.webhook_platform.list_webhook_event_types(
            request_options={"headers": {"X-Custom": "value"}}
        )
        request = httpx_mock.get_request()
        assert request.headers["X-Custom"] == "value"

    def test_list_webhook_event_types_unauthorized(self, sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            status_code=401,
            json={"message": "Unauthorized"},
        )
        with pytest.raises(AuthenticationError):
            sellapp_sdk.webhook_platform.list_webhook_event_types()

    def test_list_webhook_event_types_not_found(self, httpx_mock):
        sellapp_sdk = SellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=404, json={"message": "Not found"})
            with pytest.raises(NotFoundError):
                sellapp_sdk.webhook_platform.list_webhook_event_types()
        finally:
            sellapp_sdk.close()

    def test_list_webhook_event_types_rate_limited(self, httpx_mock):
        sellapp_sdk = SellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(
                status_code=429,
                headers={"Retry-After": "0"},
                json={"message": "Slow down"},
            )
            with pytest.raises(RateLimitExceededError):
                sellapp_sdk.webhook_platform.list_webhook_event_types()
        finally:
            sellapp_sdk.close()

    def test_list_webhook_event_types_server_error(self, httpx_mock):
        sellapp_sdk = SellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=500, json={"message": "Server error"})
            with pytest.raises(ServerError):
                sellapp_sdk.webhook_platform.list_webhook_event_types()
        finally:
            sellapp_sdk.close()

    def test_list_webhook_event_types_bad_request(self, httpx_mock):
        sellapp_sdk = SellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=400, json={"message": "Bad request"})
            with pytest.raises(BadRequestError):
                sellapp_sdk.webhook_platform.list_webhook_event_types()
        finally:
            sellapp_sdk.close()

    def test_list_webhook_event_types_unprocessable(self, httpx_mock):
        sellapp_sdk = SellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=422, json={"message": "Unprocessable"})
            with pytest.raises(UnprocessableEntityError):
                sellapp_sdk.webhook_platform.list_webhook_event_types()
        finally:
            sellapp_sdk.close()


class TestAsyncWebhookPlatform:
    @pytest.mark.asyncio
    async def test_list_webhook_event_types(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_list_webhook_event_types_response_value_200_application_json.json"
            )
        )
        result = await async_sellapp_sdk.webhook_platform.list_webhook_event_types()
        assert isinstance(
            result, SdkListWebhookEventTypesResponseValue200ApplicationJson
        )
        request = httpx_mock.get_request()
        assert request.method == "GET"
        assert request.url.path.endswith("/v2/webhook-event-types")

    @pytest.mark.asyncio
    async def test_list_webhook_deliveries(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "list_list_webhook_deliveries_response_value_200_application_json_property_data_item.json"
            )
        )
        page = await async_sellapp_sdk.webhook_platform.list_webhook_deliveries()
        assert isinstance(page, AsyncPage)
        assert len(page.data) == 1
        assert isinstance(
            page.data[0],
            ListWebhookDeliveriesResponseValue200ApplicationJsonPropertyDataItem,
        )

    @pytest.mark.asyncio
    async def test_list_webhook_deliveries_empty_page(
        self, async_sellapp_sdk, httpx_mock
    ):
        httpx_mock.add_response(json={"data": [], "list_metadata": {}})
        page = await async_sellapp_sdk.webhook_platform.list_webhook_deliveries()
        assert isinstance(page, AsyncPage)
        assert page.data == []

    @pytest.mark.asyncio
    async def test_get_webhook_delivery(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_get_webhook_delivery_response_value_200_application_json.json"
            )
        )
        result = await async_sellapp_sdk.webhook_platform.get_webhook_delivery(
            "test_delivery"
        )
        assert isinstance(result, SdkGetWebhookDeliveryResponseValue200ApplicationJson)
        request = httpx_mock.get_request()
        assert request.method == "GET"
        assert request.url.path.endswith("/v2/webhook-deliveries/test_delivery")

    @pytest.mark.asyncio
    async def test_replay_webhook_delivery(self, async_sellapp_sdk, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_replay_webhook_delivery_response_value_201_application_json.json"
            )
        )
        result = await async_sellapp_sdk.webhook_platform.replay_webhook_delivery(
            "test_delivery"
        )
        assert isinstance(
            result, SdkReplayWebhookDeliveryResponseValue201ApplicationJson
        )
        request = httpx_mock.get_request()
        assert request.method == "POST"
        assert request.url.path.endswith("/v2/webhook-deliveries/test_delivery/replay")

    @pytest.mark.asyncio
    async def test_list_webhook_event_types_with_request_options(
        self, async_sellapp_sdk, httpx_mock
    ):
        httpx_mock.add_response(
            json=load_fixture(
                "sdk_list_webhook_event_types_response_value_200_application_json.json"
            )
        )
        await async_sellapp_sdk.webhook_platform.list_webhook_event_types(
            request_options={"headers": {"X-Custom": "value"}}
        )
        request = httpx_mock.get_request()
        assert request.headers["X-Custom"] == "value"

    @pytest.mark.asyncio
    async def test_list_webhook_event_types_unauthorized(
        self, async_sellapp_sdk, httpx_mock
    ):
        httpx_mock.add_response(status_code=401, json={"message": "Unauthorized"})
        with pytest.raises(AuthenticationError):
            await async_sellapp_sdk.webhook_platform.list_webhook_event_types()

    @pytest.mark.asyncio
    async def test_list_webhook_event_types_not_found(self, httpx_mock):
        sellapp_sdk = AsyncSellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=404, json={"message": "Not found"})
            with pytest.raises(NotFoundError):
                await sellapp_sdk.webhook_platform.list_webhook_event_types()
        finally:
            await sellapp_sdk.close()

    @pytest.mark.asyncio
    async def test_list_webhook_event_types_rate_limited(self, httpx_mock):
        sellapp_sdk = AsyncSellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(
                status_code=429,
                headers={"Retry-After": "0"},
                json={"message": "Slow down"},
            )
            with pytest.raises(RateLimitExceededError):
                await sellapp_sdk.webhook_platform.list_webhook_event_types()
        finally:
            await sellapp_sdk.close()

    @pytest.mark.asyncio
    async def test_list_webhook_event_types_server_error(self, httpx_mock):
        sellapp_sdk = AsyncSellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=500, json={"message": "Server error"})
            with pytest.raises(ServerError):
                await sellapp_sdk.webhook_platform.list_webhook_event_types()
        finally:
            await sellapp_sdk.close()

    @pytest.mark.asyncio
    async def test_list_webhook_event_types_bad_request(self, httpx_mock):
        sellapp_sdk = AsyncSellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=400, json={"message": "Bad request"})
            with pytest.raises(BadRequestError):
                await sellapp_sdk.webhook_platform.list_webhook_event_types()
        finally:
            await sellapp_sdk.close()

    @pytest.mark.asyncio
    async def test_list_webhook_event_types_unprocessable(self, httpx_mock):
        sellapp_sdk = AsyncSellAppClient(
            api_key="sk_test_123",
            store="store_test",
            access_token="test-oauth",
            customer_session="test-customer",
            browser_session="sell_session=test",
            max_retries=0,
        )
        try:
            httpx_mock.add_response(status_code=422, json={"message": "Unprocessable"})
            with pytest.raises(UnprocessableEntityError):
                await sellapp_sdk.webhook_platform.list_webhook_event_types()
        finally:
            await sellapp_sdk.close()
